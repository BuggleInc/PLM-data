override def run() {
  square(100.0);
}

def square(taille:Double) {
  override def run() {
  forward(taille);
  right(90.0);
  forward(taille);
  right(90.0);
  forward(taille);
  right(90.0);
  forward(taille);
  right(90.0);
}
  
}