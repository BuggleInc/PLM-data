def selectionSort() {
  var i:Int=0
  var m:Int=getValue(0)
  for(i<-0 to getValueCount()-1){
    for(j<-i to getValueCount()){
      if(getValue(j)<=m){
        m=getValue(j)
      }
      swap(m,i)
    }
  }
}
