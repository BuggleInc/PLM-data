def posNeg(a:Int, b:Int, negative:Boolean):Boolean = {
	negative match {
		case true if (b<0 && a<0) => true
		case false if (a<0 && b>0) => true
		case false if (a>0 && b<0) => true
		case _ => false
	}
}
