def goAndGet() {
  var x=0
  while(!estSurBiscuit()){avance();x=x+1}
  porteBiscuit()
  for(y<-1 to x){recule()}
  poseBiscuit()
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}