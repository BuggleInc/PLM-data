override def run() {
  
  def faireCarre() {
    avance(100) ; droite(90) ; avance(100) ; droite(90); avance(100) ; droite(90) ; avance(100) //fait un carr? ? droite
  }
  
  for (k <- 1 to 4){
    faireCarre()
  }
  
}
