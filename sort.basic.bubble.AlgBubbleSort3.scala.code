def bubbleSort3() {
  for (i <- 0 to getValueCount()-2) {
    var sp=false
    for (j <- 0 to getValueCount()-2-i) {
      if (!isSmaller(j,j+1)) {
        swap(j,j+1)
        sp=true
      }
    }
    if (sp==false) {
      return
    }
  }
}