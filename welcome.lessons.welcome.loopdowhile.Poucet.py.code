while not sortieTrouvee() :
    compteur = 0
    PremiereFois = False
    
    while not PremiereFois or not croisement():
        PremiereFois = True
        forward()
        if estSurBiscuit():
            compteur += 1
    if compteur > 2:
        gauche()
    else:
        droite()
forward()    