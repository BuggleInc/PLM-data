def estDansIntervalle(n:Int, a:Int, b:Int): Boolean =
{
  return n >= a && n <= b
}

def in3050(a:Int, b:Int):Boolean =
{
  return (estDansIntervalle(a, 30, 40) && estDansIntervalle(b, 30, 40)) || (estDansIntervalle(a, 40, 50) && estDansIntervalle(b, 40, 50))
}