override def run() {
	for (i<-1 to 10) {
		for (i<-1 to 360/18) {
			setCouleur(Color.red)
			avance(100);
			recule(100);
			droite(1); 
		}
		for (i<-1 to 360/18) {
			setCouleur(Color.black)
			avance(100);
			recule(100);
			droite(1); 
		}
	}
}
