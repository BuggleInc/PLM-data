override def run() {
  faireMotif()
  for (i<-1 to 4) {
    avance
  }
  droite;
  for (i<- 1 to 4) {
    avance
  }
  gauche;
  faireMotif;
  for (i<-1 to 4) {
    recule
  }
  faireMotif;
  
}


def faireMotif() {
  motifGauche(Color.red);
  motifDroite(Color.blue);
    for (i<-1 to 2) {
    recule
  }
  gauche;
  for (i<-1 to 2) {
    avance
  }
  droite
  motifGauche(Color.yellow);
  motifDroite(Color.green);
    for (i<-1 to 2) {
    recule
  }
  gauche;
  for (i<-1 to 2) {
    avance
  }
  droite
}


def motifDroite(b:Color) {
  setCouleurBrosse(b)
  baisseBrosse;
  leveBrosse;
  avance;
  gauche;
  avance;
  baisseBrosse;
  gauche;
  leveBrosse;
  avance;
  gauche;
  avance;
  gauche;
}


def motifGauche(c:Color) {
  setCouleurBrosse(c);
  avance;
  baisseBrosse;
  gauche;
  leveBrosse;
  avance;
  gauche;
  avance;
  baisseBrosse;
  gauche;
  leveBrosse;
  avance;
  gauche;
  for (i<- 1 to 2) {
    avance;
  }
}