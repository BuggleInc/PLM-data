def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  var r=length
  var y=steps+1
  var a = angle
  avance (length:Double)
  if (steps==1){droite(angle:Double)
              avance(length:Double)
              gauche(angle:Double)
              recule(length:Double)
              gauche(length:Double)
              avance(length:Double)
              recule(length:Double)
              droite(angle:Double)}
  
  else(return tree(steps-1,length*shrink,angle*2,shrink))
}