def ado(n:Int):Boolean = {
	if(n<=19 && n>=13)
		return true
	else
		return false
}
def loneTeen(a:Int, b:Int):Boolean = {
	if(ado(a) | ado(b) && ( ado(a) && ado(b)))
		return true
	else
		return false
}
