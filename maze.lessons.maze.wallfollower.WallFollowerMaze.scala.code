override def run() {
def keepHandOnSideWall() {
	gauche();
	if (estFaceMur()) {
		droite();
		avance();
		if (!(estFaceMur())) {
			gauche();
			avance();
			if (!(estFaceMur())) {
				avance();
			}
		}
		
	}
	
}

while (!(estSurBiscuit())) {
keepHandOnSideWall();
}
}
