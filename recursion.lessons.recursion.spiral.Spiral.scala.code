def spiral(steps:Int, angle:Int, length:Int, increment:Int){
	var etape:Int=steps
	
	
	for (i<- 1 to steps) {
	avance(length)
	gauche(angle)
	spiral(etape, angle, length+increment, increment)
	etape=etape-1
	}
	
	
}

