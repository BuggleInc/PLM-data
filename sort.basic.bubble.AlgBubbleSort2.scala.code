def bubbleSort2()  {
  var lastEchange: Boolean = false
  do {
    lastEchange = false
    for (i <- 0 to getNombreValeurs()-2) {
      for (j <- 0 to i) {
        if (plusPetit(i + 1,i)) {
        	echange(i,i+1)
        	lastEchange = true
      	}
    	}
    }
  } while (lastEchange==true);
}