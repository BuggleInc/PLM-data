def has271(nums:Array[Int]): Boolean = {
  
  var l = nums.length
  
  for (i<- 0 to l-3){
    if (nums(i)==nums(i+1)-5 && (nums(i+2)<= nums(i)+1 && nums(i+2)<= nums(i)-1)){
      return true
     }
   }
    
  return false
}