override def run() {
  var i=0
  var x=1
  var y=2
  ecritMessage(i)
  setPos(x,y)
  while (!posFinale()) {
    if (!estFaceMur()) {
    	ecritMessage(i)
    	if (y<getMondeHauteur()) {y+=1;setPos(x,y)}      
    }
		else if (!posFinale() && estFaceMur()) {x+=1;setPos(x,y)}
  }
  
  def posFinale() : Boolean = {
    if (estFaceMur()) {
      gauche()
      if (estFaceMur()) {droite; return true}
      else {return false}
  	}
    else {return false}
  }
}