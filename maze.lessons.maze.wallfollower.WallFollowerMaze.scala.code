override def run() {
    def keepHandOnRightWall() = {
    	while (!(estFaceMur())) {droite()}
    	gauche()
    	if (!(estFaceMur)) {avance()}
    }
    while (!(estSurBiscuit)) {
    	keepHandOnRightWall();
    }
    prendBiscuit();
}
