override def run() {
	quart(Color.black);
	quart(Color.red);
	quart(Color.orange);
	quart(Color.magenta);
}
def quart(c:Color) {
	for (i<- 0 to 89) {
		ligne(c);
		droite(1);
	}
}
def ligne(c:Color) {
	setCouleur(c);
	avance(100);
	recule(100);
}
