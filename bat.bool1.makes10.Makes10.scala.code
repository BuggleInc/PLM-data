def makes10(a:Int, b:Int): Boolean = {
  (a,b) match {
    case (10,c) => return true
    case (d,10) => return true
    case _ => if (a+b==10) {
      return true
    }
    else {
      return false
    }
  }
}