def indexOfValue(nums,lookingFor):
    for i in nums:
        if lookingFor == i:
            return i
    if lookingFor == nums[i]:
        return i
    else:
        return-1
indexOfValue(nums,lookingFor)