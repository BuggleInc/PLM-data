def has271(nums:Array[Int]): Boolean = {
  var n  = nums.length
  var a271 = false
  for (k<-0 to n-3){
    if(nums(k+1)==nums(k)+5 && nums(k+2)==nums(k)-1|| num(k+2)==num(k)+1 || nums(k+2)==nums(k)-2 || num(k+2)==num(k)+2 ){
      a271=true
    }
    else a271=a271
  }
  return a271
}