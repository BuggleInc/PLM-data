def hunt() {
	while (!estSurBiscuit) {
		if (isFacingTrail) {
			progress();
		} else {
			gauche;
		}
	}
     prendBiscuit()
}

def isFacingTrail():Boolean = {
	var b:Boolean = false
     if (!estFaceMur) {
    		avance;
    		if (getCouleurSol() == Color.green) {
    			recule;
    			b = true;
    		} else {
    			recule;
    			b = false;
    		}
    } 
    return b
}        

def progress() {
	avance();
	baisseBrosse();
	leveBrosse();
}
