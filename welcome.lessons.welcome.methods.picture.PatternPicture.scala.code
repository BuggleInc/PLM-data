def peindreSol(c: Color) {
  setCouleurBrosse(c)
  
  // On marque le sol.
  baisseBrosse()
  leveBrosse()
}

def dessineCarre() {
  // Zone rouge.
  avance()
  peindreSol(Color.RED)
  
  gauche()
  avance()
  gauche()
  avance()
  peindreSol(Color.RED)
}

override def run() {
  dessineCarre()
}
