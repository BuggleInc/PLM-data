def insertionSort() {
  var v:Int = 0
  var j:Int = 0
  for (i <- 1 to getNombreValeurs()-2){
    v = getValeur(i)
    j=i
    while (getValeur(j)>v){
      copie(j-1,j)
      j-=1
      
    }
    setValeur(j,v)
  }
}