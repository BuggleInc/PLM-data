def hunt() {
  while (isFacingTrail()) {
    avance();
  }
  if (!isFacingTrail()) {
    avance();
    gauche(); 
  if (!isFacingTrail()) {
   droite();
   isFacingTrail();
  } }


  if (estSurBiscuit) {
    prendBiscuit();
  }
  return
}

def isFacingTrail():Boolean = {
  if (estFaceMur) {
    return false
  } 
  avance();
  if (getCouleurSol() == Color.green)
  { 
    recule();
    return true 
  }
  else {
    recule();
  }

  return false
}