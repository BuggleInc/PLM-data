override def run() {
  
  def keepHandOnSideWall()  {
    
    gauche();
    while (estFaceMur() == true && getDirection() == Direction.NORTH) {
      droite();
    }
    avance();
      
  }
  
  while (estSurBiscuit == false) {
    keepHandOnSideWall();
  }
  
	prendBiscuit();

  
}