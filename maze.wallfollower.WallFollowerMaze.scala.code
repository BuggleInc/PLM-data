override def run() {var x:Int=0;
                    while (!isFacingWall()) {right()}
                    while (!isOverBaggle()) {while(isFacingWall()) {right(); forward(); left()}; forward()}
pickupBaggle()
    // Your code here 
}