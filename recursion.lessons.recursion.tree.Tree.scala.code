def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {

current(steps)
avance(length)
droite(angle)

current(steps)
avance(length)
droite(angle)

current(steps)
avance(length)
droite(angle)




}
