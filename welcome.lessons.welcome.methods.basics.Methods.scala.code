def goAndGet() {
  var compteur:Int = 0
  do {avance(); compteur = compteur + 1}
  while (estSurBiscuit == false)
  prendBiscuit()
  for (compteur <- 1 to compteur) {recule()}
	poseBiscuit
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}