def bubbleSort()  {
	var changed = false ; 
	var i = 0 ; 
	do{
		for ( i <- 0 to getNombreValeurs()-2){
			if(plusPetit(i+1,i)){
				echange(i,i+1);
				changed = true ;
			}
			
		}
		
	}while(changed)
}
