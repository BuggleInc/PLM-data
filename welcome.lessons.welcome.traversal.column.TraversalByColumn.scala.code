override def run() {
  var c=0
  var y=0
  while ((getX(), getY())!=(getMondeLargeur()-1, getMondeHauteur-1)){
    ecritMessage("" + c)
    c+=1
    if(!estDosMur){
      setPos(getX(), c%7);
    } else if(getX()!=getMondeLargeur()-1) {
      setPos(getX()+1, 0);
    }
  }
}