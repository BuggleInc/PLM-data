def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {
   for (l<-0 to length){
   
   	avance(l+increment);
   	gauche(angle);
   	avance(l);
   	gauche(90);
   	avance(l);
   	gauche(90);
   	avance(l);
   	gauche(90);
   }
}
