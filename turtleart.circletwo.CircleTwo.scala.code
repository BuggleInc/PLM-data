override def run() {
  var taille:Double = 115
  leveCrayon()
  droite(90)
  avance(taille)
  baisseCrayon()
  cercle(taille)
  leveCrayon()
  recule(taille/2)
  baisseCrayon()
  cercle(taille/2)
  leveCrayon()
  recule(taille/2)
  gauche(90)
}