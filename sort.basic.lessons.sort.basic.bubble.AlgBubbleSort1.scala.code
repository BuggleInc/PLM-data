def bubbleSort()  {
	var j = 0;
	var i = 0;
	do{
         if(plusPetit(getValeur(i+1),getValeur(i))) {
		echange(i+1,i);
         }
         else {
         	var j = 1;
         }
      }while( i < getNombreValeurs() && j = 0 )
}
