def shellSort()  {
  for(i <- 1 to getNombreValeurs()-1){
    var v = getValeur(i);
    var j = i;
    var ecart = getNombreValeurs()/2;
    while(ecart>0 && !plusPetitQue(j-ecart,v)){
      copie(j-1,j);
      j -= 1;
      ecart /= 2;
    }
    setValeur(j,v);
  }
}