def close10(a:Int, b:Int): Double = {
	if( Math.abs(a-10)!=Math.abs(b-10)){
		return Math.round(inf(a,b));
	}else{
		return 0;
	}
}



def inf (a:Int , b:Int): Double = {
	return (  0.5  *(a + b - Math.abs(b - a)));
}
