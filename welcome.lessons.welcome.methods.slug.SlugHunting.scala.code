def hunt() {
	baisseBrosse()
  while (!estSurBiscuit()){
    if (isFacingTrail()){
      avance();
    }
    else{
      gauche();
      if (isFacingTrail()){
        avance();
      }
      else{
        droite();
        droite();
        avance();
      }
    }
  }
}

def isFacingTrail():Boolean = {
  if (!estFaceMur){
    avance()
  	if (getCouleurSol() == Color.green){
      recule()
      return true
    }
  	else{
      recule()
      return false
    }}else{return false}
}