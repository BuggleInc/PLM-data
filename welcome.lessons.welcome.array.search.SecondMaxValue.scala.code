def max2Value(nums:Array[Int]): Int = {
  var x = Math.max(nums(0),nums(1))
  var y = Math.min(nums(0),nums(1))
  for (i<-2 to (nums.length-1)) {
    if (nums(i)>=x) {
      y=x
      x=nums(i)
    }
  }
  y
}