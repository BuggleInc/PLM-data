override def run() {
  while (getY() != 0) {
    while (estFaceMur()) {
      keepHandOnSideWall()
    }
    if (getDirection() == 	Direction.NORTH) {
    avance()
    }
  }
  
  
  
  
  while (!estSurBiscuit()) {
    keepHandOnSideWall()
  }
  prendBiscuit()
  
  def keepHandOnSideWall() {
    gauche()
    while (estFaceMur()) {
      droite()
    }
    avance()
  }
}
