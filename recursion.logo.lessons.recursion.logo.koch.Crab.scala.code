def crab(levels:Int, length:Double) {
}

def crabKoch(levels:Int, length:Double) {
  if (levels!=0) {
    crabKoch(levels-1,length*sqrt(2));
    gauche(45);
    crabKoch(levels-1,length*sqrt(2));
    droite(45);
    crabKoch(levels-1,length*sqrt(2));
    gauche(45);
    } else {
      avance(length)
    }
}