override def run() {
	var y:Int=0

	def tache() {
		avance();
		baisseBrosse();
		leveBrosse();
		y= y+1;
		avance();
	}

	for (x<-0 to 3) {
		for (x<-0 to 1) {
			tache()
		}
		gauche()
	}

	gauche();
	avance();
	droite();
	avance();

	for (x<-0 to 3) {
		tache();
		gauche();
	}
	recule();
	droite();
	avance();
	gauche();
	
}
