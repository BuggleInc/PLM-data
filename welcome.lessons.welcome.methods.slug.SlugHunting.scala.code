def hunt() {
	if (isFacingTrail==true) {
		avance()
		baisseBrosse()
	}
	else {
		droite()
	}
	gauche()

}
def isFacingTrail():Boolean = {
	var estVert = false;
	if (!estFaceMur) {
		avance()

		if (getCouleurSol()==Color.green) {
			estVert = true;
		}
		recule()

	}
	return estVert;
}        