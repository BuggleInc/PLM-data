def shellSort()  {
	var gap=getNombreValeurs()/2
	while (gap>1) {
		def insertionSort() {
			for (i <-gap to getNombreValeurs()-1 by gap) {
				val v=getValeur(i-gap)
				var j=i
				while (j>0 && v<getValeur(j)) {
					copie(j-1,j)
					j-=1
				}
				gap=gap/2
				setValeur(j,v)
			}
		}
	}
}
