def bubbleSort3()  {
	var break: Boolean = false;
	for (i <- getNombreValeurs()-2 to 0 by -1) {
		break = false
		for (j <- 0 to i) {
			if (plusPetit(j+1,j)) {
				echange(j,j+1)
			} else
				break = true;
		}
		if (break) return
	}	
}
