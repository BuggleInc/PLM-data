def bubbleSort()  {
  var n=getNombreValeurs()
  var count= -1
  while (count != 0){
    count=0
    for (j <- 1 to n-1 ){
      if (plusPetit(getValeur(j),getValeur(j+1))){
        echange(j,j+1)
        count=count+1
      }
    }
  } 
}
