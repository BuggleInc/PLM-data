override def run() {
	var compteur:Int = 0;
	while (!estSurBiscuit()) {
		if (compteur == 0) {
			while (!estFaceMur()) {
				avance();
			}
			droite();
			compteur = compteur - 1;
			ecritMessage(compteur.toString);
		} else if (compteur !=0) {
				while (estFaceMur()) {
					droite();
				}
				compteur = compteur - 1;
				ecritMessage(compteur.toString);
				avance();
				gauche();
			} else {
				compteur = compteur + 1;
				avance();
			}
	}
}
