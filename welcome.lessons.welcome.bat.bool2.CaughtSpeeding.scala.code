def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
	if (isBirthday) {
		speed = speed + 5
	}
	else if (speed < 60 && !isBirthday) {
		return 0;
	}
	else if ( speed <= 80 && 61 <= speed && !isBirthday ) {
		return 1;
	}
	else if ( 81 <= speed && !isBirthday ) {
		return 2;
	}
}