def pentaKoch(levels:Int, length:Double) {
   if (levels==0){
    avance(length)
  }
  else {
    pentaKoch(levels-1, length)
    left(180 - 360/5)
    pentaKoch(levels-1, length)
    right(360/5)
    pentaKoch(levels-1, length)
    right(360/5)
    pentaKoch(levels-1, length)
    right(360/5)
    pentaKoch(levels-1, length)
    left(180-360/5)
    pentaKoch(levels-1, length)
  }
}