def indiceMin(debut:Int): Int = {
  
  var min = debut
  
  for (c <- debut to getNombreValeurs()-1) {
    
    if (plusPetit(c,min)) {min = c}
    
  }
  
  return min
  
}

def selectionSort() {
  
  for (a <- 0 to getNombreValeurs()-1) {
    
    echange(a,indiceMin(a+1))
    
  }
  
}