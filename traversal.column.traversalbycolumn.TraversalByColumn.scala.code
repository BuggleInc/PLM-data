override def run() {
  var x:Int=0;
  var count:Int=0;
  
  while(x!=getWorldWidth()){
    colonne(x);
    x=x+1;
  }
  def colonne(x:Int){
  var y:Int=0
  while(y!=getWorldHeight()){
    setPos(x,y);
    writeMessage(count);
    y=y+1;
    count=count+1;
  }
    }

}