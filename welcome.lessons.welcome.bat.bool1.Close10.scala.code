def close10(a:Int, b:Int): Int = {
  var dista :Int = Math.abs(10 -a)
  var distb :Int = Math.abs(10 -b)
  if (dista == distb){ 
  	if (a > b){return b}
    return a
  }
  if (dista < distb) {return a }
  return b
}