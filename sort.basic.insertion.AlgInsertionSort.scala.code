def insertionSort() {
  for (i <- 1 to getNombreValeurs() -1){
    var v : Int = i
    var indice : Int = i-1
    while(plusPetit(v,indice) && indice>=0){
      echange(indice, indice + 1)
      indice = indice - 1
    }
  setValeur(v,indice)  
    
  }
}