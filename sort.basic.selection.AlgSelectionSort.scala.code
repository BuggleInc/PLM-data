def selectionSort() {
	var tot = getNombreValeurs()
  for (i<- 0 to tot-2){
    var k = getValeur(i)
    for (j<- i+1 to tot-1){
      
      if (getValeur(j)<k){
        k = j
      } 
      
    }
    swap(k,i)
  }
}