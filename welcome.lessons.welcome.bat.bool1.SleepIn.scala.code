def sleepIn(weekday:Boolean, vacation:Boolean): Boolean = {
	var vacation:Boolean 
	var weekday:Boolean
	if (vacation==true) {
		return vacation
	} else if (vacation==false && weekday==true) {
		return false
	} else if (vacation==false && weekday==false){
		return weekday
	}
	
}
