def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {
  if (steps == 0) {
    leveCrayon();
  } else {
    avance(length);
    gauche(angle);
    spiral(steps-1:Int, angle:Int, length+increment:Int, increment:Int)
  }
}