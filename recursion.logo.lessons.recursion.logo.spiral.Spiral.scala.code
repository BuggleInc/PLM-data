def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {
  if (steps!=0){
		avance(length)
		gauche(angle)
		return spiral(steps-1,angle,increment,increment+length)
  
  }
 	return spiral(5,90,0,3)
}