def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if(step<=0){
    
    
    return
  }
  else{
    current(step)
    avance(length)
    tree(steps-1,shirink*length,angle,shrink)
    tree(steps-1,shirink*length,360-angle,shrink)
  }
}