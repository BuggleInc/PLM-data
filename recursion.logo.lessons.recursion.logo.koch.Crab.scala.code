def crab(levels:Int, length:Double) {
  if (levels==0){
    avance(length)
  }
  else {
    crab(levels-1,length/2)
    gauche(45)
    crab(levels-1,length/2)
    droite(90)
    crab(levels-1,length/2)
    gauche(45)
    crab(levels-1,length/2)
  }
}