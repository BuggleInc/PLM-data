def island(num:Array[Int]): Int = {
  var level:Int = num(0);
  var levelCount:Int = 0;
  
  for(i <- 1 until num.length) {
    if(num(i) > level && num(i) != 1) {
      levelCount += 1;
    }
    else if(num(i - 1) > num(i)) {
      level = num(i);
    }
  }
  
  return levelCount;
}