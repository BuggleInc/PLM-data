for (t<-1 to 7) {
	for (g<-1 to 4 ) {avance()}; gauche() ; avance(); avance()
	; droite() ; for (h<-1 to 4 ) {avance()} ; droite() ; 
	avance(); avance(); gauche(); for (i<-1 to 4 ) {avance()} ; gauche()
	
}
