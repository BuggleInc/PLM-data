def cocktailSort() {
  var ok:Boolean = false
  var n:Int = getNombreValeurs()-2
  while(ok == false){
    ok=true
    for(i<-0 to n){
      if( !plusPetit(i,i+1)){echange(i,i+1);ok=false; }      
    }
    for(i<-0 to n){
      if( !plusPetit(n-i+1,n-i)){echange(n-i+1,n-i);ok=false; }      
    }
    n+= -1
    
  }

}