def insertionSort() {
  var v:Int = 0
  var j:Int = 0
  for (i <- 1 to getNombreValeurs() - 1) {
    v = getValeur(i)
    j = i - 1
    while (j >= 0 && getValeur(j) > v) {
      setValeur(j+1,getValeur(j))
      j -= 1
    }
    setValeur(j+1,v)
  }
}
