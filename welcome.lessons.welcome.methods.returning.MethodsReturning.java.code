boolean haveBaggle() {
boolean seenBaggle = false;
int steps = 6;

//		for(int i = 0; i < steps; i++){
			forward(steps);
			
			if(isOverBaggle()){
				seenBaggle = true;
			}
//		}

		for(int i = 0; i < steps; i++){
			backward();

			if(isOverBaggle()){
				seenBaggle = true;
			}
		}
		
		return seenBaggle;
}//end function
