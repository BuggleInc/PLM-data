def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {

  if(steps > 0) {
    
    //On avance
    forward(length);
    
    //Arbre droit
    right(angle);
    tree(steps*shrink, length, angle, shrink);
    
    //Arbre gauche
    left(angle*2);
    tree(steps*shrink, length, angle, shrink);
    
    //On recule
    backward(steps);
  }
  
}