def close10(a:Int, b:Int): Int = {
	var rep = 0
	var i1 = 10-a
	var i2 = 10 - b
	if (!(math.abs(i1) == math.abs(i2))) {
		 if (math.abs(i1) < math.abs(i2)) {
		 	rep = a
		 	
		 } 
		 else 
		 	rep = b
	} 
	return rep
}
