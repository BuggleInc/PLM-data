$package 

import plm.universe.bugglequest.AbstractBuggle;

class Conditions extends AbstractBuggle {
	override def run() { 
		/* BEGIN SOLUTION */
		if (isFacingWall())
			backward();
		else
			forward();
		/* END SOLUTION */
	}
}
