 def faireV() {
		avance(2)
 		baisseBrosse(); leveBrosse()
	 	avance()
 		gauche(); avance()
 		baisseBrosse(); leveBrosse()
  	recule(); droite()
 		avance()
  	baisseBrosse(); leveBrosse()
  	avance(2)
  	gauche()
	}

def faireMotif() {
  for (c <- 1 to 4) {
    faireV()
  }
}

def faireLigne() {
  for (m <- 1 to 9) {
    faireMotif()
    avance(7)
  }
}

override def run() {
  for (d <- 1 to ) {
    faireLigne()
    gauche(); avance(7); droite()
  }
}