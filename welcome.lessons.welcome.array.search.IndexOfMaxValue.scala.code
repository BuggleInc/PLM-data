def indexOfMaxValue(nums:Array[Int]):Int = {
  var max = nums(0)
  var indice = 0
  for (k <- 1 to nums.length-1) {
    if (nums(k) > max) {
      max = nums(k)
      indice = k
    }
  }
  return indice
}