while (!sortieTrouvee()) {
  var nbBagel : Int = 0
  for (compteur <- 1 to 5) {
    avance()
    if (estSurBiscuit())
   		nbBagel = nbBagel + 1
  }
  if (nbBagel >=3)
  	gauche()
  else
  	droite()
}
avance()