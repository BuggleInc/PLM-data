while not sortieTrouvee():
    nbBis=0
    premiereFois=True
    while premiereFois==True or not (croisement() or estSurBiscuit()):
        premiereFois=False
        if estSurBiscuit() and croisement():
            nbBis=nbBis+1
        forward()