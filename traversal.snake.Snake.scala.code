override def run() {
  baisseBrosse()
  while(!estSurPositionFinale){
    while(!estFaceMur){
      avance()
    }
    if (getDirection()==Direction.EST) {
      gauche(); avance(); gauche()
    } else droite(); avance(); droite()
  }
}

def estSurPositionFinale():Boolean = {
  if(getDirection()==Direction.EST) {
    gauche()
    if(estFaceMur) return true
  }
  if(getDirection()==Direction.OUEST) {
    droite()
    if(estFaceMur) return true
  }
  return false
}