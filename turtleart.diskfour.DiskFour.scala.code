override def run() {
  def quart(){
    for(i<-1 to 90){
      forward(100);
      backward(100);
      right(1);
    }
  }
  quart();
  setColor(Color.red);
  quart();
  setColor(Color.orange);
  quart();
  setColor(Color.magenta);
  quart();
  
}

