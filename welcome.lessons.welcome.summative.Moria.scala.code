while(!Out){
  retourne();
  while(!estSurBiscuit() && !estFaceMur()){
    avance();
  }
  if(estSurBiscuit()){
    prendBiscuit();
    retourne();
    while(!estSurBiscuit()){
      avance();
    }
  recule();
  poseBiscuit();
  recule();
  }
}
right(); avance(); left(); avance();


def Out:Boolean={
  if (estFaceMur() && !estSurBiscuit()){
    return true
  }
  return false
}