def island(num:Array[Int]): Int = {
  
  var altSeq:Int = 0
  var tabSize:Int = num.length;
  
  for (i <- 0 to tabSize-2) {
    
    if (num(i) < num(i+1)) {
      
      altSeq = altSeq + 1
      
    }
        
  }
  
  return altSeq
  
}