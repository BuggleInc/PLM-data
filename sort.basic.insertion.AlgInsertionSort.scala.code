def insertionSort() {
  for(i<- 1 to getNombreValeurs()-1){
    var v = getValeur(i)
    var compte = 0
    for(j<- i to 1 by -1){
      if(getValeur(j-1)>v){
        copie(j-1,j)
        compte = j-1
      }
    }
    setValeur(compte,v)
  }
}