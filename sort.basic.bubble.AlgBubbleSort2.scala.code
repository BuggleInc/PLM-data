def bubbleSort2()  {
  var listeTriee:Boolean=true
  var x=0
  x=getNombreValeurs
  do {
    listeTriee=true
    for (i <- 0 to x-2) {
       if (plusPetit(i+1,i)) {
         echange(i+1,i)
         listeTriee=false
         x-=1
       }
    }
  } while (x!=0)
}