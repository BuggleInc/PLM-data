def shellSort()  {
  val taille=getNombreValeurs()
  var temp=0
  var j=0
  var ecart=taille/2
  for (i<-1 to taille-1){
    temp=getValeur(i)
    j=ecart
    while ((j>0)&&(getValeur(j-j/2)>temp)){
      setValeur(j,getValeur(j-j/2))
      j=j-j/2
      }
     	setValeur(j,temp) }
}