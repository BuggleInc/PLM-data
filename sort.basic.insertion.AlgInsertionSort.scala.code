def insertionSort() {
  for (i<- 1 to getNombreValeurs()-1){
    var v=getValeur(i)
    var k=i
    while (k>=1 && !plusPetitQue(v,k-1)){
      copie(k-1, k)
      k-=1
    }
    setValeur(k, v)
  }
}