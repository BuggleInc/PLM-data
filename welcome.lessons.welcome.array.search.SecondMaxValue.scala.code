def max2Value(nums:Array[Int]): Int = {
  var un:Int =nums(0)
  var deux:Int=nums(0)
  for (i<- 0 to nums.length-1){
    if (nums(i)>un){un=nums(i);deux=un}
    else if (nums(i)>deux){deux=nums(i)}
  }
  return deux
}