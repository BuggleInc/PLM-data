override def run() {
  var direction=0
  while (!estSurBiscuit){
  direction = random.3()
     direction match{
    case 0 => if (!estFaceMur){avance()}
    case 1 => droite()
    case 2 => recule()}
  }
}