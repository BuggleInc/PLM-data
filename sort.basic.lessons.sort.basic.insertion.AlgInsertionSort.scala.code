def insertionSort() {
	val l = getNombreValeurs()-1
		for (i<- 1 to l ) {
			var p = i
			var v = getValeur(i)
				while ((p !=  0) && plusPetitQue(p-1,v)) {
					setValeur(p,(getValeur(p-1)))
					p -=1
			}
			setValeur(p,v)
					
			
		}
}
