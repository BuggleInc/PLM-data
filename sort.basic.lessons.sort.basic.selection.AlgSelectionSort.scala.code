def selectionSort() {
  var min = 0
  var v = 0
  for (i <- 0 to getNombreValeurs()-2) {
    min=i
    for (j <- i to getNombreValeurs()-1) {
      if (getValeur(j)<v) {
        min=j
        v=getValeur(min)
      }
    }
    echange(i,min)
  }
}