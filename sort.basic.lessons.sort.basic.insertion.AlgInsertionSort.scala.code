def insertionSort() {
	
	for(i<-1 to getNombreValeurs()-1)
	{
		val temp=getValeur(i)
		var j=i
		while(j>0 && getValeur(j-1)>temp){
			setValeur(j,getValeur(j-1))
			j=j-1
		}
		setValeur(j,temp)
	}
}
