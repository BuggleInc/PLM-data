def step(rule:Array[Char], colors:Array[Color]) 
{

	var colorId = 0;


	
	for(i <- 0 to colors.length - 1)
	{
		if(colors(i) == getCouleurSol())
		{
			colorId = i;
		}
	}

		setCouleurBrosse(colors(colorId));
		baisseBrosse();
		leveBrosse();

	if(colorId < rule.length)
	{
	
		if(rule(colorId) == "L")
		{
			gauche();
			avance();
		}
		else if (rule(colorId) == "R")
		{
			droite();
			avance();
		}
	
	}



}
