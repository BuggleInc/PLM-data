override def run() {
  var hauteur : Int = getMondeHauteur()
  var T = new Array[Color](hauteur)
  T(0) = getCouleurSol()
  for (i <- 1 to T.length-1) {
    T(i) = getCouleurSol() ; avance()}
  var largeur : Int = getMondeLargeur()
  for (k  <- 1 to largeur-1) {
    recule(hauteur) ; droite() ; avance() ; gauche() ;
    baisseBrosse()
    for (color <- T) 
    	{setCouleurBrosse(color) ; avance()}
    leveBrosse()

    }
  }