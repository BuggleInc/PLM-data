def disque(ouverture:Double,divisions:Int,longeur:Double) {
  
  var angle = ouverture/divisions
  
  for (div <- 1 to divisions) {
    
    avance(longeur)
    recule(longeur)
    droite(angle)
    
  }
  
}

override def run() {
  
  disque(90,90,100)
  
}