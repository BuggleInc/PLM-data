def bubbleSort3() {
var fini:Boolean=false
	for (j <- getNombreValeurs-2 to 0 by -1) {
		fini=true
		for (i <- 0 to j) {
			if (getValeur(i) > getValeur(i+1)) {
				echange(i,i+1)
				fini=false
				}
			}
		if (fini==true) {
			return
		}
	}
}
