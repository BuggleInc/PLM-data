def insertionSort() {
  var v=0
  var n=getValueCount()-1
  
  for(i <- 1 to n){
    v = getValue(n)
    n = n-1
    
    for(j <- 1 to n){
      
      if(isSmaller(v,n-j)){
        copy(n-j,n-j+1)
      }
      else{setValue(n-j,v)
        
      }
    }
  }
}