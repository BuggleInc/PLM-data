var taille: Int=0
def indexOfValue(nums:Array[Int] ,lookingFor:Int): Int = {
  
  taille=nums.length
  for (i <- 0 to (taille -1)){
    if (nums(i)==lookingFor){
      return i
    }else{
      return -1
    }
  }
}