def occurrences(nums:Array[Int],lookingFor:Int):Int = {
  var occu:Int= 0
  var len:Int=nums.length
  for (i <- 0 to (len-1) ) {
    if ( lookingFor== nums(i) ) {
      return occu+1
    }else {
      return occu
    }
  }
}