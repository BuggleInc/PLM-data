override def run():Unit= {
  for (i<-1 to 8) {avance(30);recule(30);droite(45)} ;avance(30);droite(90);avance(30)
  for (i <-1 to 4) {droite(90);avance(60)};recule(30);droite(90);avance(30);gauche(135);avance(30);droite(90);recule(30)
  for (i <-1 to 4) {avance(60);droite(90)};gauche(135);
  for (i<- 1 to 8) {avance(30);droite(90);avance(30);gauche(90);avance(30);gauche(90);avance(60);gauche(90);avance(30);gauche(90);avance(30)}
}