def withoutDoubles(die1:Int, die2:Int, noDoubles:Boolean):Int = {
  var res1=die1
  var res2=die2
  if (noDoubles && die1==die2)
  {
    if (die1==6)
    	res1 = 1
    else
    	res1 = die1 +1
    return res1 + res2
  }
  else
  	return res1 + res2
}