override def run() {
  
  def maison{
      
  avance(30)
  droite(30)
  avance(30)
  droite(120)
  avance(30)
  droite(120)
  avance(30)
  recule(30)
  gauche(90)
  avance(30)
  droite(90)
  avance(30)
  droite(90)
  }
  
  var k=0
  for(k<- 1 to 4){
    maison
    cache()
    recule(50)
    montre()
    droite(90)
  }
}