def gnomeSort() {
	var sorted : Boolean = false;
	var x=1;
	do {
		if (plusPetit(x-1,x)) {
			x=x+1;
		}
		else {
			echange(x-1,x);
			x=x-1;
		}
		if (x==getNombreValeurs()-1) {
			 return
		}
	} while (!sorted);
}
