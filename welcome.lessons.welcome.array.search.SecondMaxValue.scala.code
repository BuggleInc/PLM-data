def max2Value(nums:Array[Int]): Int = {
  var un:Int =Integer.MIN_VALUE
  var deux:Int=Integer.MIN_VALUE
  for (i<- 0 to nums.length-1){
    if (nums(i)>un){deux=un;un=nums(i)}
    else if (nums(i)>deux){deux=nums(i)}
  }
  return deux
}
