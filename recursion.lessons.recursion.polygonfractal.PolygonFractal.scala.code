def polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double) {
	if (levels == 1)
	{
		for ( i <- 0 to sides - 1)
		{
			avance(length)
			gauche((sides - 2) * 360/(2*sides) )
		}
	}
	else 
	{
		
	}

		
}
