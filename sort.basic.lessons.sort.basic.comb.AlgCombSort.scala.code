def combSort() {
  var lgr:Int=getNombreValeurs()
  var ecart=lgr
  if (ecart>1){
    cart = (ecart.asInstanceOf[Double] / 1.3).asInstanceOf[Int]
    i=0
    while (i+ecart < lgr){
      if(getValeur(i)>getValeur(i+1))
        echange(i,i+ecart)
        i+=1
      }
    } 
  }