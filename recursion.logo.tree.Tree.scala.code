def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if (steps==0){}

  else{
    current(steps)
    baisseCrayon()
    avance(length)
    droite(angle)
    
   
    tree(steps-1,shrink*length,angle,shrink)
    recule(length)
    gauche(2*length)
    //recule(length)
    //gauche(2*length)
    //tree(steps-1,shrink*length,angle,shrink)

  }
}

