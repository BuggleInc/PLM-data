override def run() {
  	var rand = random3()
    while(!estSurBiscuit()) {
      if (rand == 0) {
        avance()
      } else if (rand == 1) {
        droite()
      } else {
        gauche()
      }
    }
  prendBiscuit()
}