def bubbleSort2() {
var swapped=true

while (swipped)
swipped = false
	for (i <- getNombreValeurs()-2 to 0) {
		for (j <- 0 to i ){
		if (plusPetit(i+1,i)) {
		echange(i,i+1)	
		swapped=true	
		}
		
	}
}
