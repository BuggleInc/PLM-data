def shellSort()  {
  for( i <- 1 to getNombreValeurs()-1){
		var v=getValeur(i);
		var a=(getNombreValeurs()-1)/2
		while(i-a>0 && !plusPetitQue(i-a,v)){
      var temp=getValeur(i);
      copie(i-a, i-a+1)
      a=a/2;
    }
    setValeur(i-a+1,v);
  }
}