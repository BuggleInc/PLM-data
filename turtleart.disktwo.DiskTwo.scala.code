override def run() {
  for (j<-1 to 9) {
  for (k<-1 to 20) {
    avance(100:Int)
    recule(100:Int)
    droite(1:Double)
  }
  setCouleur(Color.red:Color)
  for (k<-1 to 20) {
    avance(100:Int)
    recule(100:Int)
    droite(1:Double)
  }
  setCouleur(Color.black:Color)
  }
}