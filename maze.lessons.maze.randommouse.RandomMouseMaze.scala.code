override def run() {
  var a:Int = 0
  while(!isOverBaggle()){
    a = random3()
    if(a == 0 && !isFacingWall())
    	forward()
    else if(a == 1)
    	left()
    else
    	right()
  }
  pickupBaggle()
}