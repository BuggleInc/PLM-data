override def run() {
	var nb:Int = 0
  do{
    ecritMessage(nb)
    nb+=1
    setPos((getX()+1)%getMondeLargeur(), (getY())%getMondeHauteur())
   }while(getX() != getMondeLargeur() && getY() != 0)
  
  
}