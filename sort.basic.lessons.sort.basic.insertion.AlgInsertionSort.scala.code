def insertionSort() {
var v =0
var j=0
    for (i<-1 to getNombreValeurs()-1){
      j=i
      v=getValeur(i)
      while (j>0 && !plusPetitQue(j-1,v)){
        copy(j-1,j)
        j=j-1
        
        
      }
      setValeur(j,v)
    }
}
