def insertionSort() {
	for (i=1 to getNombreValeurs()-1) {
		var v:Int <- getValeur(i)
		var j = i-1
		while (getValeur(j)<v) {
			 setValeur(j+1) <- getValeur(j);
			 j=j-1;
		}
		setValeur(j)=v
	}
}
