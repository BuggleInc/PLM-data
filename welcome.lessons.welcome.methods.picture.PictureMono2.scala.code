override def run() {
	
	def canvas() {
		for (x<-0 to 3) {
		for (x<-0 to 1) {
			tache()
		}
		gauche()
	}

	gauche();
	avance();
	droite();
	avance();

	for (x<-0 to 3) {
		tache();
		gauche();
	}
	recule();
	droite();
	avance();
	gauche();
	}
}
