def bubbleSort()  {
  var flag = false;
  var nb = getValueCount();
	do{
		flag = false;
    	for(i <- 0 to nb){
        if(isSmaller(i,i+1)){
          swap(i,i+1);
          flag = true;     
        }
        
      }
    
  }while(flag);
}