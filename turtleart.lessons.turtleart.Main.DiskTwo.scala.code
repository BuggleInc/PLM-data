override def run() {

	val x = 39

	def quartier(c:Color) {
		setCouleur(c)
		for(i<-1 to x) {
			avance(100)
			recule(100)
			droite(20.0/x)
		}
	}

	for (i<-1 to 9) {
		quartier(Color.black)
		quartier(Color.red)
	}

}
