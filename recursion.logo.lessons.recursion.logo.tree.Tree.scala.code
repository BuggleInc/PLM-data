def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if (steps==0){
    recule(length*(1/shrink))
  }else{
    avance(length)
    droite(angle)
    current(steps)
    tree(steps-1,length*shrink,angle,shrink)
    gauche(2*angle)
    current(steps)
    tree(steps-1,length*shrink,angle,shrink)
    droite(angle)
    current(steps)
    recule(length)
  }
}