def hunt(c:Color) {
	while(!estSurBiscuit) {
    if (isFacingTrail){
      baisseBrosse()
      avance()
      leveBrosse()
    } else gauche()
  }
  prendBiscuit()
  
}

def isFacingTrail(c:Color):Boolean = {
  if (estFaceMur) {
    return false
  }
  avance()
  if (getCouleurSol()==c) {
    recule()
    return true
  } else recule(); return false
}