override def run() {

def positionFinale(x:Int, y:Int):Boolean = {
	if (x==getLargeurMonde-1 && y==getHauteurMonde-1) {
		return true
	}
	else {
		return false
	}
}

	var x=0
	var y=0
	while (!positionFinale()) {
		
		setPos(x, y+1)		
		if (y == getHauteurMonde()-1)
			setPos(x+1, 0)
	}
}
