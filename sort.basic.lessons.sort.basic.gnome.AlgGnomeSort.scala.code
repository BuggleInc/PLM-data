def gnomeSort() {
  var n = getNombreValeurs
  var pos = 0
  while (pos != n) {
    if (plusPetit(pos+1,pos)) {
      echange(pos, pos+1);
      pos = pos - 1
    }
    else {
      pos = pos + 1
    }
    if (pos == -1) {
      pos = pos + 1
    }
  }
}