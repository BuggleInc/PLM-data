def maxMod5(a:Int, b:Int):Int = {
	if((a==b) && (a%5)==(b%5)){
		return 0;
	}
	if ((a%5)==(b%5)) {
		return min(a,b);
	}else{
		return max(a,b);
	}
	
}













def min (a:Int , b:Int): Int = {
	return   (  (a + b - Math.abs(b - a))/2);
}


def max (a:Int , b:Int): Int = {
	return   (  (a + b - Math.abs(b - a))/2);
}
