while (!sortieTrouvee()) {
  var baggels:Int = 0
  
  // On parcourt le corridor
  for (compteur <- 1 to 5) {
    avance()
    
    if (estSurBiscuit())
    	baggels = baggels + 1
  }
  
  if (baggels >= 3) {
    gauche()
  }
  else {
    droite()
  }
}

avance()
