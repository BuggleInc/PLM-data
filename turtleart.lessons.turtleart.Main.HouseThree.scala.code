override def run() {
	for (i <- 1 to 5) {
	baisseCrayon();
	avance(30);
	droite(30);
	for (i <- 1 to 3) {
		avance(30);
		droite(120.0);
	}
	droite(60);
	avance(30);
	droite(90);
	avance(30);
	droite(90);
	avance(30);
	droite(90);
	leveCrayon();
	droite(90);
	avance(50);
	gauche(90);
	}
}
