override def initialize():Array[Int] = {
	var Liste = getSol()
	var Segment = new Array[Int](2)
	for (i <- 0 to Liste.length-2) {
		if (Liste(i).y==Liste(i+1).y) {
			Segment(0)=Liste(i)
			Segment(1)=Liste(i+1)
		}
	}
	while (
}

  override def step() {
  }
