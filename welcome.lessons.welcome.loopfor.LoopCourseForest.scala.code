for (i<- 0 to 6 } {
	for ( ava <- 0 to 3) { avance()}
	gauche()
	for (g <- 0 to 1 ) {avance()}
	droite()
	for ( ava <- 0 to 3) { avance()} 
	droite()
	for ( ava <- 0 to 1) { avance()}
	gauche()
	for ( ava <- 0 to 3) { avance()}
	gauche()}

