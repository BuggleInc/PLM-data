public void bubbleSort()  {
  
  boolean swap = true;
  while(swap){
  swap = false;  
  for(int i = 0 ; i < getNombreValeurs()-2; i++ )
{
    if(!plusPetit(i,i+1)){
      swap = true;
      echange(i,i+1);
      
    }
    
  } 
  }
}