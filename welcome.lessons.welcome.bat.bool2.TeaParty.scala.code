def teaParty(tea:Int, candy:Int): Int = {

	(tea, candy) match {
		
		case (tea, candy)  if (tea==2*candy || candy==2*tea) => return 2		
		case (tea, candy)  if (tea>=5 && candy>=5) => return 1		
		case (_,_)=> return 0		
	}

	
}
