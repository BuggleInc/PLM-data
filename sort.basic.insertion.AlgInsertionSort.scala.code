def insertionSort() {
  var n = getValueCount();
  
  for (i <- 1 to n - 1) {
    var v = getValue(i);
    
    for (j <- 1 to i) {
      var w = getValue(j)
      if(w > v) {
        setValue(j+1, w);
      }
    }
  }
}