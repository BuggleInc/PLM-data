def growFlowers(color):
    setCouleurBrosse(color)
    baisseBrosse()
    forward(2)
    backward()
    droite()
    forward()
    backward(2)
    forward()
    setCouleurBrosse(Color.YELLOW)
    leveBrosse()
    forward(3)


couleur=[Color.RED,Color.CYAN,Color.GREEN,Color.PINK]
forward()
gauche()
backward()
for i in range(len(couleur)):
    color=couleur[i]
    growFlowers(color)
backward()
droite()
forward()
growFlowers(Color.ORANGE)
backward()
gauche()
backward(2)

        
