def butNlast(l:List[Int], n:Int): List[Int] = {
  def lambda(tl:List[Int], c:Int): List[Int] = {
    if(c == 0 return Nil; 
    return tl.head::lambda(tl.tail, c-1);
    
  }
  return lambda(l, l.size - n)
}