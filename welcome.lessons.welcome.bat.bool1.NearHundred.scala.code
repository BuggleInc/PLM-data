def nearHundred(n:Int): Boolean = {
	if (valAbs(100) <= 10 || valAbs(200) <= 10){
		return true
	}else{
		return false
	}
}
def valAbs(n:Int):Int = {
	Math.abs(n - 100)
}
def valAbs(n:Int):Int = {
	Math.abs(n - 200)
}