
PremiereFois=True

while (not sortieTrouvee()):

    avance()
    while (PremiereFois or not croisement()):
        PremiereFois=False

        if estSurBiscuit():
            biscuit=biscuit+1
        forward()

    if biscuit>=3:
        gauche()
        forward()
    else:
        droite()
        forward()

forward()