def insertionSort() {
  var v:Int = 0
  var h:Int = 1
  
  for (i <- 1 to len-1) {
    v = getValue(i)
    while (i-h > 0) {
      if (getValue(i-h) > v)
      	copy(i-h, i-h+1);
      
      h+=1;
    }
  }
}