fin = False
while not fin:
    action = False
    for j in range(getNombreValeurs()):
        
        if plusPetit(j+1,j):
            echange(j+1,j)
            action = True
    if action == True:
        fin = True
