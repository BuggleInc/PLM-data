def answerCell(isMorning:Boolean, isMom:Boolean, isAsleep:Boolean):Boolean = {
	if(isAsleep==true)
	return false
	if (isMorning==true && isMom==true)
	return true
	if (isMorning==true)
	return false
	else
	return true
}
