override def run() {
  def pasDeSerpent(){
    while (!estFaceMur){
      avance()
    }
    if (getDirection()==Direction.EAST){
      gauche()
      avance()
      gauche()
    }else(getDirection()==Direction.WEST){
      droite()
      avance()
      droite()
    }  
  }
  pasDeSerpent()
  pasDeSerpent()
}