def bubbleSort2() {



	val taille = getNombreValeurs();
     var échange = true;
     while ( échange == true ) {
     	échange = false;
     for (x <- taille-2 to 0 by -1) {
	for (i <- 0 to x) {
		if ( plusPetit(i,i+1) == false) {
			echange(i,i+1);
			échange = true;
		} 
	} }
}
}
