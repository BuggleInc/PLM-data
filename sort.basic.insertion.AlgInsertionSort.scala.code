def insertionSort() {

  for(i<- 1 to getNombreValeurs()-1){
    var v = getValeur(i)
    var j=i-1
    while(v<getValeur(j)){
 			setValeur(j+1,getValeur(j))
      j=j-1
    }
    setValeur(j,v)
    
  }
}