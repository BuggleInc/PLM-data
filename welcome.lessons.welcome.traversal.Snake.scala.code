override def run() {
 baisseBrosse()
 while (!arrive()) 
 {
 	if (!estFaceMur())
 	  {avance()}
 	 		
 }
}


def arrive() {
	if (estFaceMur())
		{ if (getDirection() == Direction.EST)
			  {gauche()
			    if (estFaceMur())
			     { true
			   droite()}}
			else { if (getDirection == Direction.OUEST)
					 {droite()
					 estFaceMur()
					 gauche}}
		}
	else false
}
