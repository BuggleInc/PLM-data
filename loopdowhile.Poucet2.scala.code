do {
  var nbBaggle=0
  forward()
  while (crossing()==false){
       if (isOverBaggle()==true){
        forward()
        nbBaggle=nbBaggle+1
       }
   }
  if (nbBaggle>=3){
       left()
  }
  else {
    right()
  }
} while(exitReached==false)