def bubbleSort2() {
  var nb : Int = getNombreValeurs()
  var rang : Int = 0
  var bool : Boolean = true
  while (bool==true) {
    bool = false
    for (i<-0 to nb-2-rang) {
    	if (plusPetit(i+1,i)) {
        echange(i,i+1)
        bool = true
    	} 
  	}
    rang = rang+1
  }
}