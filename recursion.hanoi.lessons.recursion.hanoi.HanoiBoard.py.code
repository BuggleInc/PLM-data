def solve(src,dst,other, height=-1):
    if height==-1: # initial case
        height=getSlotSize(src) 
    if getRayon(src)==getRayon(src):
        deplace(src, dst)
    deplace(src, dst)    
    print getRayon(dst)
        

        
    
    # Your code here
