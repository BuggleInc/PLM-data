def spiral(steps:Int, angle:Int, length:Int, increment:Int) = steps match{
  case 0 => 
  case steps => {
    avance(length)
    droite(angle)
    spiral(steps-1, angle, length + increment, increment)
  }
  
}