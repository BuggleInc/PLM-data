def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {
  if (steps > 0){
  	avance(length.asInstanceOf[Double])
  	gauche(angle.asInstanceOf[Double])
  	spiral(steps-1,angle,length+3,3)
  }
}