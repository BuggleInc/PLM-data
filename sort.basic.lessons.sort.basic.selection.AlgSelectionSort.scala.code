def selectionSort() {
  var long= getNombreValeurs
  var min=0
  
  for (i <- 1 to long-1 ){
    min = i
    
    for (j <- i+1 to long ){
      if (getValeur(j)>getValeur(i)){
        min= j 
      }
    }
    
    if (min!= i){
      echange(i,min)
    }
    
  }
  
}
