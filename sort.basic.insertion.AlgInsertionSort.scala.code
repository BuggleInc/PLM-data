def insertionSort() {
  var lgr = getNombreValeurs();
  for ( i<-1 to lgr-1 ){
  	var v=getValeur(i);
  	for ( j<-0 to i){
      if (getValeur(i-j)>v){
        setValeur(i-j,getValeur(i-j-1));
      }
      setValeur(i-j,v)
    }
  }
}