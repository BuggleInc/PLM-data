def selectionSort() {
  var lgr:Int = getNombreValeurs()
  var min:Int = 0
  var position:Int = 0
  for (i <- 0 to lgr-1) {
    min = i
    for (j <- i to lgr-1) {
      if (getValeur(j) < getValeur(min)) {min = j}
    }
    echange(i,min)
  }
}