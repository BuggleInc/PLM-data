do {var compteur:Int = 0;
	avance()
	do {
	if (estSurBiscuit()) {
		(compteur = compteur + 1);
		avance();
	} else { 
		avance();
	}
	} while (!croisement());
	
		if (compteur >= 3) {
			gauche();
		} else {
			droite();
		}
		compteur = 0;
	
	}

} while (sortieTrouvee)
