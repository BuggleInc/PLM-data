def insertionSort() {

  for(i<- 1 to getNombreValeurs()-1){
    var v = getValeur(i)
    var j=i-1
    while(v<getValeur(j)){
 			setValeur(j,getValeur(j-1))
      if(j!=0){
      	j=j-1
      }
    }
    setValeur(j,v)
    
  }
}