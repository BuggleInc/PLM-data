override def run() {
  var x=0
  for (x <- 1 to 4){
    avance(100)
    droite(90)
  }
  avance(100)
  droite(30)
  for (x <- 1 to 2){
    avance(100)
    droite(120)
  }
  avance(100)
  gauche(90)
  avance(100)
  gauche(180)
}