def indexOfMaxValue(nums:Array[Int]):Int = {
  var maximum=0
  var indexMax=0
  for (i<-0 to nums.length-1){
    if (maximum < nums(i)){
      maximum=nums(i)
      indexMax=i
    }
  }
  return indexMax
}