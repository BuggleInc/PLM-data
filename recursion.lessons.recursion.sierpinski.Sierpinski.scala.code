def sierpinski(level:Int, length:Double) {
	if(level == 0) {
		avance(length);
		droite(180);
		avance(length);
		droite(180);
		avance(length);
		droite(180);
	}
	else {
		sierpinski(level-1, length/3);
	}
}
