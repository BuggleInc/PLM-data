override def run() {
  setBrushColor(Color.RED);
  brushDown();
  while (isFacingWall==false){
    forward();
  }
  if (getDirection()==Direction.EAST){
    right();forward();right();
  } else {
    left();forward();left();
  }
}