def golomb(num:Int): Int = {
	var Nieme=1
	var T=new Array[Int](num+3)
	T(0)=1
	T(Nieme)=T(Nieme-1)+1
	do {
		for (i<-1 to T(Nieme)-1){
			T(Nieme+i)=Nieme
		}
		Nieme+=T(Nieme)
	} while (Nieme < num)
	return T(num)
}
