override def run() {
  
  
  def carre(){
  var x=100.0;
  var o=90.0;

  avance(x);
  droite(o);
  
  avance(x);
  droite(o);
  
  avance(x);
  droite(o);
  
  avance(x);
  droite(o);
    droite(o);
  }
  carre();
  carre();
  carre();
  carre();
}
