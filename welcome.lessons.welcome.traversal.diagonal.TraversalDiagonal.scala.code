var diag = 0;

override def run() {
	var cpt : Int =0
	ecritMessage(cpt);
	setPos(getX(),getY()+1);
	while(!posF)
	 if(!estFaceMur){
	 setPos(getX()+1,getY()-1);
	
	 setPos(getX()-1,getY()+2)
	for(p<-1 to 5){ 
	 for (k<-1 to p){
	 	 setPos(getX()+1,getY()-1);
	 }
	 setPos(getX()-p,getY()+(p+1));
	}

	 
	 } 

	
}



def posF():Boolean ={
	if( getX()== (getMondeLargeur()-1)  && getY()==(getMondeHauteur()-1)){
		return true
	}else{
		return false
	}
}
