override def run() {
  var height =  getWorldHeight()
  var width = getWorldWidth()
  var compteur = 0
  
  for (x <- 0 to height-1){
    for (y <- 0 to width -1){
      setPos(x,y)
      writeMessage(compteur)
      compteur = compteur +1
      
    }
  }
}
