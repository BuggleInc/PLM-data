def bubbleSort2() {
  var n:Int = getNombreValeurs - 1
  for(i <- 0 to getNombreValeurs - 2) {
    n -= i
    for(j <- 1 to n + 1) {
      if(getValeur(j - 1) > getValeur(j))
        echange(j - 1, j)
    }
  }
}