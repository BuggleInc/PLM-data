override def run() {
  for (ligne <- 1 to 3){
    faireLigne()
  }
}

def faireV(c: Color) {
  setCouleurBrosse(c)
  avance()
  baisseBrosse()
  leveBrosse()
  avance()
  gauche()
  avance()
  baisseBrosse()
  leveBrosse()
  recule()
  droite()
  avance()
  baisseBrosse()
  leveBrosse()
  avance()
  gauche()
}

def faireCarre(){
  faireV(Color.yellow)
  faireV(Color.red)
  faireV(Color.blue)
  faireV(Color.green)
}

def faireLigne(){
  for (carre <- 1 to 3) {
  faireCarre()
  avance(5)
  }
  recule(15)
  gauche()
  avance(5)
  droite()
}