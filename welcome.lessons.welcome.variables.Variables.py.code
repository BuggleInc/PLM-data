int stepFwd = 0;
int stepBack = 0;

    while(not isOverBaggle()):
        forward()
        stepFwd ++

        if(isOverBaggle()):
            pickupBaggle()
            
            while(stepFwd != stepBack):
                backward()
                stepBack ++
            
            dropBaggle()
