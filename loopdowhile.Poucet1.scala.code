while (!sortieTrouvee()) {
  
  for (i <- 1 to 5) {
    
    var baggles = 0
    
    avance();
    
    if (estSurBiscuit()) {
      baggles = baggles + 1
    }
    

  }
    
  if (baggles <= 2) {
      droite();
  } else if (baggles >= 3) {
      gauche();
  }

  
}