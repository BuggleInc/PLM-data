override def run() {
	def PositionFinale():Boolean{
		(estFaceMur && getDirection==Direction.nord)
	}
	baisseBrosse
	while (!PositionFinale){
		avance
		if (estFaceMur) {
			gauche
			avance
			droite
			recule(6)
		}
	}
}
