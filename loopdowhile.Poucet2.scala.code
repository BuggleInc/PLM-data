var compt = 0

do {
  compt = 0
  do
  	{
      avance();
      if (estSurBiscuit())
      	compt = compt + 1
    } while (crossing() == false)
   if (compt > 2)
   	gauche();
   else
   	droite();
} while (sortieTrouvee == false)
avance();