override def run() {
  var cpt:Int = 0
  var l:Int = getMondeLargeur()
  var h:Int = getMondeHauteur
  for (i <- 0 to l-1) {
    for (j <- 0 to h-1) {
      setPos(j, i)
      ecritMessage(cpt)
      cpt = cpt + 1
    }
  }
}