def polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double) {
  if (levels==1){
    for (i <- 1 to sides){
    avance(length);
    gauche(180-180/sides);
    droite(180-180/sides);
    droite(180-180/sides);
  }
} 