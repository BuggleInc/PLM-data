def goAndGet() {
  var n = 0;
  while (!estSurBiscuit){
    avance;
    n+=1;
  }
  prendBiscuit;
  for(i <- 1 to n) recule;
  poseBiscuit;
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}