def setGroundColor(col):
  brushDown()
  brushUp()

def step():
  col = getGroundColor()
  if col == Color.black:
    setGroundColor(Color.white)
    turnLeft()
  else:
    setGroundColor(Color.black)
    turnRight()
  forward()
