def avS() {if (!estFaceMur()) avance()}
override def run() {
  var d = 0
  while (!estSurBiscuit()) {d = random3();
          d match {
            case 0 => avS()
            case 1 => droite();avS()
            case 2 => gauche();avS()
          }}
  prendBiscuit()
    // Your code here 
}