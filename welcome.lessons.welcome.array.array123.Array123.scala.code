def array123(nums:Array[Int]): Boolean = {
  var i = nums(0)
  var compteur = 0
  while (compteur<nums.length)
  {
    if (nums(compteur) == 1)
    {
      if (nums.length > compteur +1 && nums(compteur+1)==2 && nums(compteur + 2) == 3)
      return true
    }
  compteur +=1
  }
  return false
}