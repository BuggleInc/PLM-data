def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if(steps != 0){
    forward(length)
    right(angle)
    tree(steps-1, length-1, angle, shrink-1)
    left(2*angle)
    tree(steps-1, length-1, angle, shrink-1)
    right(angle)
    backward(length)
  }
}