override def run() {
  var cpt=0
  var x=0
  var y=0
  while (!(x==6 & y==6)) {
    ecritMessage(cpt)
    y+=1
    setPos(x,y)
    cpt+=1
    if ((x==6 || y==6) & x>y) {
      y+=1
      ecritMessage(cpt)
      setPos(x,y)
      cpt+=1
    }
  }
  ecritMessage(cpt)
}

def versDroite(x:Int,y:Int) {
  
}