while not sortieTrouvee() :
    compteur = 0
    PremiereFois = False
#tant qu'il n'y a pas de croisement avancer et le compteur augmente de 1
#Puis on fait une conditions sur le nombre de biscuits sur le sol 
    while not PremiereFois or not croisement():
        PremiereFois = True
        forward()
#La variable PremiereFois ne s'exécute qu'une fois 
        if estSurBiscuit():
            compteur += 1
    if compteur > 2:
        gauche()
    else:
        droite()
forward()    