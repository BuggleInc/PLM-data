def combSort() {
  var ecart = getNombreValeurs()
  var echanges = true
  do{
    if(ecart>1){
      ecart = (ecart.toDouble/1.3).toInt
    }
    var i=0
    echanges = false
    for(boucle<- i to getNombreValeurs()-1 by ecart){
      if(!plusPetit(i,i+ecart)){
        echange(i,i+ecart)
        echanges = true
      }
    }
  }while((ecart > 1) || (!echanges))
}