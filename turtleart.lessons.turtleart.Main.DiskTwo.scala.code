override def run() {
	def tranche(c:Color) ={
	setCouleur(c)	
	var i = 0
	for (i<- 1 to 20) {
		avance(100)
		recule(100)
		droite(1)
	}
	}
	var j = 0
	for (j<-1 to 9){
		tranche(Color.black)
		tranche(Color.red)
	}
}
