def move(nbPas : Int, versLAvant : Boolean) : Int = {
  if (versLAvant == true) {
    for (cpt <- 1 to nbPas) {
      avance()
    }  
  }
  else {
    for (cpt1 <- 1 to nbPas) {
    	recule(nbPas)
  	}
	}
}