def teaParty(tea:Int, candy:Int): Int = {
  
  (tea, candy) match {
    case (i, j) if ((i < 5) | (j < 5)) => return 0
    case (i, j) if ((i >= 2*j) | (j >= 2*i)) => return 2
    case (i, j) if ((i >= 5) & (j >= 5)) => return 1
    
    
  }
}