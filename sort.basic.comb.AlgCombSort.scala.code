def combSort() {
  var l = getNombreValeurs;
  var ecart = l;
  var fini = false;
  
  while(!fini){
    if(ecart > 1) ecart=(ecart.toDouble/1.3).toInt ;
    for(i <- 0 to l-ecart-1){
      if(plusPetit(i+ecart, i)){ 
        echange(i, i+ecart);
        fini = false;
      }
    }
  }
}