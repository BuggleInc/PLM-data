def hunt(c:Color) {while(!estSurBiscuit()){if (isFacingTrail(c:Color)){avance();baisseBrosse();leveBrosse()};else{droite()} };prendBiscuit()
   
     // Write your code here
 }

def hunt() {while(!estSurBiscuit){if (isFacingTrail()){avance();baisseBrosse();leveBrosse()};else{droite()} };prendBiscuit()
   
}


def isFacingTrail(c:Color):Boolean = {
    if(estFaceMur()){return false};
  	else{ avance()
  					if(getCouleurSol()==c){recule ();return true};
        			else{recule();return false}}}