override def run() {
  var cpt: Int = -1
    for (i <- 0 to getMondeLargeur()-1){
      for (j <- 0 to getMondeHauteur()-1){
        setPos(j,i)
        cpt = cpt +1
        ecritMessage(cpt)
      }
    }
}