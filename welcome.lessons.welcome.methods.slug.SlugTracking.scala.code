def isFacingTrail():Boolean = {
   while (!estSurBiscuit())
   	{ avance()
   		if (estFaceMur()) {
   			if (getCouleurSol() != Color.green)
   		 		{recule(); droite()}
   		 		else { droite()}
   		 		}
   return true			
}        
