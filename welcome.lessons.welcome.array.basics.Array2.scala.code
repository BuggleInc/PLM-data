
override def run() {

	var T = new Array [Color](getWorldHeight())
	var p : Int = 0 ;
	for (i <- 0 to (getWorldHeight()-1)) {
	if(!estFaceMur ){
		
		T(i) = getCouleurSol();
		avance();
		}
	}
	if(estFaceMur){
		for(k<-0 to getWorldHeight()-1){
			recule();
			if (estDosMur){
				gauche();
				avance();
				droite();
				}else{
					baisseBrosse();
					for(z<-0 to (getWorldHeight()-1)){
					setCouleurBrosse(T(z));
					baisseBrosse();
					leveBrosse();
					avance();
				}
		}
 	}
 	
	
}
}