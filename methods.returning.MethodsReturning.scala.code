def haveBaggle():Boolean = {
  var vuBaggle : Boolean = false
  for (i <- 1 to 6) {avance();if (estSurBiscuit()) {vuBaggle=true}}
  recule();recule();recule();recule();recule();recule()
  return vuBaggle
}