def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {

	if(steps!=1){
		current(steps)
		avance(length)
		droite(angle)
		tree(steps-1, length*shrink, angle, shrink)
	}

	else if(steps==1){
		gauche(angle)
		recule(length)
		droite(angle)
		avance(length)
	}
}
