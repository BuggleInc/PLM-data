def close10(a:Int, b:Int): Int = {
  var a_diff:Int = Math.abs(a - 10)
  var b_diff:Int = Math.abs(b - 10)
  
  if (a_diff < b_diff){
    return a
  }
  else if (a_diff > b_diff){
    return b
  }
  else{
    return 0
  }
}