override def run() {
	setCouleur(Color.black)
	for (j <- 1 to 90){
			avance(100)
			recule(100)
			droite(1)
	}
	setCouleur(Color.red)
	for (j <- 1 to 90){
			avance(100)
			recule(100)
			droite(1)
	}
	setCouleur(Color.yellow)	
	for (j <- 1 to 90){
			avance(100)
			recule(100)
			droite(1)
	}
	setCouleur(Color.pink)	
	for (j <- 1 to 90){
			avance(100)
			recule(100)
			droite(1)
	}		
}
