override def run() {
  if (getDirection() == Direction.NORD && !estFaceMur()) {
    avance()
  }
  
  while (!estSurBiscuit()) {
    keepHandOnSideWall()
  }
  prendBiscuit()
  
  def keepHandOnSideWall() {
    gauche()
    while (estFaceMur()) {
      droite()
    }
    avance()
  }
}