def bubbleSort()  {

	Boolean stop = false;
	do{
		stop = true;
		for (i <- 0; i < getNombreValeurs()-2; i++){
			if (plusPetit(getValeur(i+1), getValeur(i)){
				echange(i,j);
				stop = false;
			}
		}
	}while(!stop)
}
