override def run() {
for (j<- 1 to 3) {


for (i<-1 to 4) {
	avance(100:Double);
	droite(90:Double);
}
gauche(90:Double);
recule(100:Double);
gauche(90:Double);

for (i<-1 to 3) {
	avance(100:Double);
	droite(90:Double);
}
}

}
