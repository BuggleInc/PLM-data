def haveBaggle():Boolean = {
	var vu=false;
	for (i<- 1 to 7) {
		for (j<- 1 to 6) {
			vu=vu || estSurBiscuit();
			avance();
		}
		vu=vu || estSurBiscuit();
		avance();
		if (!vu) {
			droite();
			avance();
			gauche();
		}
	return vu
}

	
}
