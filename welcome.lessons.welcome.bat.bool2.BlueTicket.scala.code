def blueTicket(a:Int, b:Int, c:Int):Int = {
  if ( som(a,b)==10 || som(b,c)==10 || som(a,c) ==10 ){
    return 10;
  } else if ( som(a,b) == som(b,c)+ 10 || som(a,b) == som(a,c)+ 10 ){
    return 5;
  } else {
    return 0;
  }
}
   
def som(a:Int,b:Int):Int ={
  return a+b;
}