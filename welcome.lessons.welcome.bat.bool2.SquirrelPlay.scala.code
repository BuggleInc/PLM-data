def squirrelPlay(temp:Int, isSummer:Boolean):Boolean = {
  var temp_max: Int = 90;
  if (isSummer) {
    temp_max == temp_max + 10;
  }
  print("60 - "+temp+" - "+temp_max+"    ")
  return temp >= 60 && temp <= temp_max;
}