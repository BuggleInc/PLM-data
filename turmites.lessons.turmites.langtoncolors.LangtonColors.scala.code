def marqueSol(c:Color) { 
		setCouleurBrosse(c);
		baisseBrosse();
		leveBrosse()
}
		
def tourne(rule:Char) {
	if (rule == 'L') { gauche() }
	else if ( rule == 'R') { droite() }
	else {}
}


def step(rule:Array[Char], colors:Array[Color]) {
	var x = 0
	
	while  (colors(x) == getCouleurSol) {
		x=x+1;
	}
	tourne(rule(x))
	marqueSol(colors((x+1)%colors.length))
	avance()
	
}

