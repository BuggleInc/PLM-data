override def run() {
  var compt:Int=0;
  while (!posiFinal) {
   ecritMessage(compt);   
   if (!estFaceMur) {
     setY(getY()+1)
   }
    else {setY(0)
          setX(getX+1)}
  }}

def posiFinal():Boolean = {
  return (estFaceMur && estDroiteMur)
    
}

def estDroiteMur():Boolean = {
   var a:Boolean=false ;
   gauche;
   a=estFaceMur
   droite ;
  return a
}