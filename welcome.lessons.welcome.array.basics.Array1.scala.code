override def run() {
  def colore(c:Color){
    setCouleurBrosse(c)
    baisseBrosse()
    leveBrosse()
  }
  var tab = new Array[Color](getmondeHauteur())
  tab(0)=getCouleurSol()
  for(i<-1 to tab.length-1){
    avance()
    tab(i)=getCouleurSol()
  }
  recule(getmondeHauteur()-1)
  for(i<- 1 to getmondeLargeur()-1){
    gauche()
  	avance()
  	droite()
  	avance()
    for(j<-0 to getmondeHauteur()-1){
      colore(tab(j%(tab.length-1)))
             avance()
             
             
    }
  }
}