def has271(nums:Array[Int]): Boolean = {
  var n = nums.length
  var contient = false
  for (i <- 0 to (n-3))
  {
    if (nums(i+1)==nums(i)+5 &&  Math.abs(nums(i+2)-((nums(i)-1)))<=2)
    {
      contient= true
    }
  }
  return contient
}