def indexOfMaxValue(nums:Array[Int]):Int = {
       var L=nums.length
       var pos=nums(0)
       var pos2 = 0
  for (i<-0 to L-1) {
    
     if (nums(i)>=pos&&pos2!=0) {
       pos=nums(i)
       pos2=i 
     }
  }
return pos2
}