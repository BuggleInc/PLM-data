def insertionSort() {
  var len=getValueCount();
  var tmp=0;
  var tmp2=0;
  for (i <- 1 to len) {
    tmp=getValue(i)
    for (j <- 1 to i) {
      if (!isSmaller(i-j,i)) {
        copy(i-j,i-j+1)
        tmp2=j
      }
    }
    setValue(i-tmp2,tmp)
  }
}