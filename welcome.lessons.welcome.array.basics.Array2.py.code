def storeArray():
    L = []
    for a in range(getWorldHeight()-1):
        L.append(getGroundColor())
        forward()
    L.append(getGroundColor())
    backward(getWorldHeight()-1)
    return L

def array():
    offset = int(readMessage())
    for b in range(len(storeArray())):
        setBrushColor(storeArray()[b]
        brushDown()
        brushUp()
        
storeArray()
left()
forward()
right()
array()
