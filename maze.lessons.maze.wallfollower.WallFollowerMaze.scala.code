override def run() {
	def keepHandOnSideWall() {
		droite()
		if (estFaceMur()){
		gauche()
		 if (!estFaceMur()){avance()}
		else {gauche()}
	}
	else {avance()
	gauche()}}
while(!estSurBiscuit()){	
keepHandOnSideWall()
}
prendBiscuit();
}
