override def run() {
    def keepHandOnLeftWall() = {
    	if (!(estFaceMur)) {
		gauche();
		if (estFaceMur) {droite();avance()} else {avance()}
    	} else {droite()}
    }
    while (!(estSurBiscuit)) {
    	keepHandOnLeftWall();
    	if (!estFaceMur) {avance()};
    }
    prendBiscuit();
}
