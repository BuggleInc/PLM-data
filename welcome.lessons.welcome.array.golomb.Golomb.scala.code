def golomb(num:Int): Int = {
	var tab= new Array[Int](num); var k:Int=2
	tab(0) = 1; tab(1) = 2; tab (2) = 3;
	for (i<- 3 to ((num.length)-1)) {
		for (j<- 1 to tab(i)) {
			if (k+j<tab.length) {
				tab(k+j) = i;
			}
		k = k+1;
		}
	}
	return tab(tab.length-1);
}
