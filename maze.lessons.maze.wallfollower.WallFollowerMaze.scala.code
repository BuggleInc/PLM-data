override def run() {
  while(!estSurBiscuit()){
    if(estFaceMur()){gauche()}
    else{
      droite()
      if(!estFaceMur()){avance()}
      else{
        retourne()
        if(!estFaceMur()){avance()}
        else{droite();avance()}
      }
    }
  }
  prendBiscuit()
}
