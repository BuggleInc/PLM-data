def combSort11() {
  var len = getValueCount();
  var gap = len;
  var fin = false;
  
  while(gap > 1 || !fin) {
    fin = true;
    if(gap > 1){
      gap = (gap.toDouble / 1.3).toInt;
      if(gap == 9 || gap == 10){
        gap = 11;
      }
    }
   
    var i = 0;
    while(i+gap < len){
      if(getValue(i+gap) < getValue(i)){
        swap(i, i+gap);
        fin = false;
      }
      i += 1;
    }
      
  }
}
