def crab(levels:Int, length:Double) {
  if (levels==0){
    length
    return
  }
  baisseCrayon()
  gauche(135)
  crab(levels-1,length/Math.sqrt(2))
  droite(90)
  crab(levels-1,length/Math.sqrt(2))
  gauche(135)
  leveCrayon()
}