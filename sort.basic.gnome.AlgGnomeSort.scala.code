def gnomeSort() {
	var gnome:Int = 0;
  
	while (gnome < getNombreValeurs() - 1) {
    
    if (gnome == 0 || getValeur(gnome + 1) > getValeur(gnome)) {
			gnome += 1;
      
		} else {
			echange(gnome, gnome + 1)
			gnome -= 1 ;
		}	
	}
}
