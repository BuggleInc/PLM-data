def isFacingTrail():Boolean ={
  while (!isOverBaggle) {
    if (!isFacingWall()) {
    	forward()
    	if (getGroundColor()==Color.green) {
    		backward()
    		return true
  		} else {
    		backward()
    		left()
  	}
    }else {
        left()
      }
    return true
  }
  }
}