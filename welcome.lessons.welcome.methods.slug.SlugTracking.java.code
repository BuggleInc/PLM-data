boolean isFacingTrail() {
	if (!estFaceMur())
		avance();
	else
		return false;
	if (!getCouleurSol().equals(Color.green) && !estDosMur())
		recule();
    	return getCouleurSol().equals(Color.green);
}        
