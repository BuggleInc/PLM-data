       var pas= 
       0
       var dejafait = 0
      def goAndGet() {
      while (!estSurBiscuit) {avance() }
      prendBiscuit
      poseBiscuit
        }

        for (i <- 1 to 7) {
            goAndGet();
            right();
            forward();
            left();
        }
