override def run() {
  while (!isOverBaggle){
    while(!isFacingWall) {
      avance();
    }
    left();
    while(!isFacingWall && getDirection!=Direction.NORTH) {
      keepHandOnWall();
    }
    right();
    
  }
  pickupBaggle();
}

def keepHandOnWall() {
  left();
  while(isFacingWall) {
    right();
  }
  forward();
}