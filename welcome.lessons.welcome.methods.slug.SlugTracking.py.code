def isFacingTrail():
    if not isFacingWall():
        forward()
        Color = getGroundColor()
        backward()
        if Color != Color.green:
            return False
        else:
            return True
    else: 
        left()
        return False

isFacingTrail()
if isFacingTrail():
    forward()

    
    
