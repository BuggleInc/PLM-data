# Copy your isFacingTrail, and write your code below
def isFacingTrail():
    
    if not isFacingWall():
        forward()
        res = (getGroundColor() == Color.green)
        backward()
        return res

