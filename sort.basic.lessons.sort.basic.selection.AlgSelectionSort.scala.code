def selectionSort() {
  for (i <- 0 to getNombreValeurs()-1){
    var v = i
    for (c <- i+2 to getNombreValeurs()-1){
      if (getValeur(c) < getValeur(v)){
        v = c
      }
    }
    echange(v,i)
  }
}