def isFacingTrail():
   # write your code here
    forward()
    while isOverBaggle != True:
#        forward()
        if (isFacingWall()):
            left()
        elif (getGroundColor() != Color.green):
            backward()
            left()
            forward()
        else:
            forward()

isFacingTrail()