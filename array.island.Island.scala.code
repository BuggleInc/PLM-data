def island(num:Array[Int]): Int = {
  var i=0
  var x=num(1)
  var nombre=0
  var l=num.length
  for (i <- 2 to l){
    if (num(i)<x){
      nombre = nombre + 1
      x=num(i)
    }
    else {
      x=num(i)
    }
  }
  return nombre
}