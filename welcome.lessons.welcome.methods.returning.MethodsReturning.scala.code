def haveBaggle():Boolean = { var vuBiscuit = false;
                            for (compteur <- 1 to 6) {if (estSurBiscuit()) {avance();vuBiscuit = true} else {avance()};for (compteur <-1 to 6) {recule()}}
                            
                            return vuBiscuit
}