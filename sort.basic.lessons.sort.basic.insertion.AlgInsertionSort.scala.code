def insertionSort() {
	var v=0;
	var j=0;
	for (i <- 1 to getNombreValeurs()-1 by 1) {
		v=getValeur(i);
		j=i;
		while (j>0 && !plusPetit(j-1,v)) {
			copie(j-1,j)
			j=j-1;
		}
		setValeur(j,v);
	}
}
