def goAndGet() {
  var nbBiscuit = 0
  var compteur = 0
  while (estSurBiscuit == false) {
    avance()
    compteur += 1
  };prendBiscuit()
  for (i<- 1 to compteur) {
    recule()
  };poseBiscuit()
}


for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}

