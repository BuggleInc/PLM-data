def cocktailSort() {
  var enOrdre = true ;
  while (enOrdre1 || enOrdre2){
    enOrdre = false ;
  for (i <- 0 to getNombreValeurs()-2){
    if (plusPetit(i+1,i)){
    	echange(i+1,i)
      enOrdre = true ;
    }
  }
  for (j <- getNombreValeurs()-2 to 0 by -1){
    if (plusPetit(i+1,i)){
    	echange(i+1,i)
    	enOrdre = true ;
    }
  }    
 }
}
