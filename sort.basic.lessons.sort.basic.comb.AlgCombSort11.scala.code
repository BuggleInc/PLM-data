def combSort11() {
 val n=getNombreValeurs()
 var ecart:Int = n
 var swapped :Boolean= true
 var i:Int =0
 while (swapped || ecart>1) {
 	swapped =false
 	if (ecart>1) {ecart = (ecart.toDouble / 1.3).toInt}
 	if (ecart == 9 || ecart == 10) {ecart = 11}
 	i=0
 	while( (i+ecart) < n){
 	if (plusPetit(i+ecart,i)) {echange(i+ecart,i);swapped=true}
 	i += 1
 }}
}
