def posNeg(a:Int, b:Int, negative:Boolean):Boolean = {
  (a,b,negative) match {
    case (c,d,true) if (c<0 &&d<0) => return true
    case (c,d,false) if (c<0 && d>0) => return true
    case (c,d,false) if (c>0 && d<0) => return true
    case (_,_,_) =>  return false
  }
}