def isFacingTrail():Boolean = {
  var res = false;
  avance();
  if (estFaceMur()) {
    gauche();
    if (getCouleurSol()==Color.green) {
  		recule();
    	res = true;
    	return res;
    } else gauche();
  }
}
