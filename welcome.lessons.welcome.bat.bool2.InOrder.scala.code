def inOrder(a:Int, b:Int, c:Int, bOk:Boolean):Boolean = {
  (a, b, c, bOk) match {
    case (i, j, k, false) if ((b > a) & (c > a)) => return true
    case (i, j, k, true) if (c > a) => return true
    case (_, _, _, _) => return false
  }