def crab(levels:Int, length:Double) {
  if (levels == 0) {
    avance(length)
    return
  }
  gauche(45.0)
  crab(levels-1, length/Math.sqrt(2))
  droite(90.0)
  crab(levels-1, length/Math.sqrt(2))
  gauche(45.0)
}