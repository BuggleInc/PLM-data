def cocktailSort() {
  var taille = getNombreValeurs()
  var parcours = true
  while(parcours)
  	{
      for (i <-1 to taille-1)
      	{
          if(getValeur(i)<getValeur(i-1))
          	{
              echange(i,i-1)
            }
        }
      for(i<-taille-1 to 1)
      	{
          if(getValeur(i)<getValeur(i-1))
          {
              echange(i,i-1)
            }
        }
    }
}