override def run() {
  var cpt:Int = 0
  ecritMessage(cpt)
  cpt+=1
	while (getX()<=6 && getY()<6) {
    if (getX()==6) {
      setPos(0, getY()+1)
    }
    if (getX()<6) {
      setPos(getX()+1, getY())
    }
    ecritMessage(cpt)
    cpt=cpt+1
  }
}