def crab(levels:Int, length:Double) {
  if (levels == 0) {
    forward(length);
  } else {
    left(45);
    crab(levels, length / Math.sqrt(2));
    right(90);
    crab(levels, length / Math.sqrt(2));
    
  }
}