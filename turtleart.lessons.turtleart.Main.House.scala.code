override def run() {
	for (i<- 0 to 3) {
		avance(100)
		droite(90)
	}
	avance(100)
	droite(30)
	for (i<- 0 to 1) {
		avance(100)
		droite(120)
	}
	avance(100)
	gauche(90)
	avance(100)
	gauche(180)
}
