def insertionSort() {
  var L = getNombreValeurs()
  var v = 0
  var cpt = -1
  for (i <- 1 to L-1){
    var k = i-1
    v = getValeur(i)
    while (plusPetit(k+1,k) && k>0) {
      echange(k,k+1)
        k=k-1
    }
    setValeur(k,v)
  }
}