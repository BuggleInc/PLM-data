def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if (shrink == 0){
    
  } else {
    gauche(angle)
    avance(length)
    tree(steps, length, angle, shrink -1)
    recule(lenght)
    droite(angle*2)
    avance(length)
    tree(steps length, angle, shrink -1)
    recule(length)
    gauche(angle)
  }
}