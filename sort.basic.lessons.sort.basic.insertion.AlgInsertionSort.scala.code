def insertionSort() {
  for (i <- 1 to getValueCount()-1) {
    var value = getValue(i)
    var j = i
    while ((j > 0) & (!isSmallerThan(j-1,value))) {
      copy(j-1,j)
      j = j-1
    }
    setValue(j,value)
  }
}