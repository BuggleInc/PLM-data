def polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double) {
	if(levels==0){
		for(i<-1 to sides-1){
			forward(length);
			right(360/sides);	
		}
	}
	else{
		for(i<-0 to sides-1){
			forward(length);
			left(360/sides);
			polygonFractal(levels-1,sides,shrink*length,shrink);
			
		}
	}
}
