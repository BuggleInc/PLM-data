def insertionSort() {
	var v = 0
	var ech = 0

	
	for (i <- 1 to getNombreValeurs()-1) {
		var k = i-1
		v = getValeur(i)
		while (v < getValeur(k) && k >= 0) {
				copie(k,k+1)
				k=k-1
		}
		

		if (k != -1) {setValeur(k,v)}
	
	}
}
