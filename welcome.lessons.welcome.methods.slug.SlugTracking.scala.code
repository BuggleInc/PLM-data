def isFacingTrail():Boolean = {
  var vert: Boolean = false;
  avance()
	while (!estSurBiscuit()) {
  	if (estFaceMur()) {
    	droite()
  	}
  	else if (getCouleurSol() == Color.green) {
      vert = true
     	avance()}
  	else {
    	recule()
    	droite()
  	}
    	}

  return vert
  }
