def haveBaggle():Boolean = {
  var seenBaggle: Boolean = false
  var i: Int = 1
  while(i < 7){
    if(isOverBaggle()){
      seenBaggle = true
    } else {
      forward()
    }
    i = i + 1
  }
  
  for(j <- 1 to 6){
		backward()
  }
  
  return seenBaggle
}