def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
	(speed,isBirthday) match {
		case(_,false) => if (speed<60) {0} else
						if (speed<81) {1} else {2}
		case(_,true)  =>if (speed<65) {0} else
						if (speed<86) {1} else {2}
	}
}
