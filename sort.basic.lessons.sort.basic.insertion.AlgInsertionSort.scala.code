def insertionSort() {
  val taille=getNombreValeurs()
  var temp=0
  var j=0
  for (i<-1 to taille-1) {
    temp=getValeur(i)
    while ((getValeur(j)<temp)||(j==0)){
      if (plusPetit(i,j)) {echange(j,i)}
      j=j-1
    }
    copie(temp,j)
    j=i-1
  }
}