def solve() {
  while(!isSorted()){
    for(i <- 0 to getStackSize()-2){
      if(getPancakeRadius(i) > getPancakeRadius(i+1)){
        for(j <- 0 to i){
          flip(i+1-j)
        }
      }
    }
  }
}