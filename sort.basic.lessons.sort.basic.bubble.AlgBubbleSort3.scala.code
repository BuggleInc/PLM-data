def cocktailSort3() {
  var inversion = true;
  var debut = 0 ; 
  var fin = getNombreValeurs() - 2;
  do {
    inversion = false;
    for (i<- debut to fin) {
      if (plusPetit(i+1,i)) {
        echange(i,i+1);
        inversion = true;
      } else return;
    }
    fin = fin - 1;
    for (i<- fin to debut by -1) {
      if (plusPetit(i+1,i)) {
        echange(i,i+1);
        inversion = true;
      }
    }
    debut = debut + 1;
  } while (inversion);
}