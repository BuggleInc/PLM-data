def insertionSort() {
  var v = 0
  var a = 0
  for (i<-1 to getNombreValeurs()-1){
    v = getValeur(i)
    for(j<-i-1 to 0 by -1){  
      if (plusPetitQue(j,v)){
        a = getValeur(j)
        setValeur(j+1,a)
      } else {
        setValeur(j,v)
        return()
      }
    }
  }
}