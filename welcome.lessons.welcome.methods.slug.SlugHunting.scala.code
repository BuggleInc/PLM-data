def hunt() {
	while (!isFacingTrail) {
		leveBrosse()
		gauche()	
	}
	baisseBrosse()
}
def isFacingTrail():Boolean = {
	var estVert = false;
	if (!estFaceMur) {
		avance()
		if (getCouleurSol()==Color.green) {
			estVert = true;
		}
		recule()

	}
	return estVert;
}        