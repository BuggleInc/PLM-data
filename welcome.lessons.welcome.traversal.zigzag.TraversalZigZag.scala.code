override def run() {
var x:Int=getMondeHauteur()
	var y:Int=getMondeLargeur
	var c:Int=0
	
	     for(j <- 0 to y-1){setPos(j-k,0);ecritMessage(c);c=c+1}
		for(j <- 0 to y-1){
			for(k <- 0 to y-1){
				setPos(j-k,1);ecritMessage(c);c=c+1;
			}
		}
			
		
	
}
