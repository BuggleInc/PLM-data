def altPairs(str:String):String={
  
  var sortie:String = "";
  
  for(i<-0 to str.length){
    if(i*4+1<str.length){
	    sortie+=str.charAt(i*4);
      sortie+=str.charAt(i*4+1);
    }
  }
  
  return sortie;
}