def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  var r=length
  var y=steps
  while(y!=0){avance(r:Double)
            current(y)
            right(angle:Double)
           	r=length*shrink
          	y=y-1}
}