def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
if (steps !=0) {
/*def branche() = { 
	
	droite(angle);
	avance(length);
	recule(length);
	gauche(angle*2);
	avance(length);
	recule(length);
}*/


current(steps);

droite(angle);
avance(length);
recule(length);
gauche(angle*2);
avance(length);
tree(steps-1, length*shrink, angle, shrink);






}
}
