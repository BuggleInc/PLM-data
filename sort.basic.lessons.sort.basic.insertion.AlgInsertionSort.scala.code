def insertionSort() {
  for(i <- 1 to getNombreValeurs()-1){
    var c = i
    var n = getValeur(i:Int)
    while(c>=1 && plusPetit(c:Int,c-1:Int)){
      copie(c:Int,c-1:Int)
      c = c-1
    }
    setValeur(c:Int, n:Int)
  }
}