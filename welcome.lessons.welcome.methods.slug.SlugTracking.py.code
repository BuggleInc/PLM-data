def isFacingTrail():
   # write your code here
    while isOverBaggle != True:
        brushDown()
        brushUp()
        forward()
        if (isFacingWall() == True):
            left()
        if (getGroundColor() != Color.green):
            backward()
            left()
        else:
            brushDown()
            brushUp()
            forward()

isFacingTrail()