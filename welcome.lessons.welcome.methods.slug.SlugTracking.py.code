def isFacingTrail():
   # write your code here
    while isOverBaggle() != True:
        if (isFacingWall()):
            left()
        else:
            if (getGroundColor() != Color.green):
                backward()
                left()
                forward()
            brushDown()
            brushUp()
            forward()
        

isFacingTrail()