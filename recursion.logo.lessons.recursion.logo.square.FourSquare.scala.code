
def carre() {
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
  droite(90)
}
  
  
  
  override def run() {
    for (1<-1 to 4) {
      carre()
    }
 
    