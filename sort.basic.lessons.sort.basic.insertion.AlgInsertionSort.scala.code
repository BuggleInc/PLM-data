def insertionSort() {
  var v:Int = 0
  var lgr:Int = getNombreValeurs()
  for (i <- 1 to lgr-1) {
    v = getValeur(i)
    var j:Int = i
    while (j>0 && v < getValeur(j-1)) {
      copie(j,j-1)
      j =j-1
    }
      setValeur(j,v)
  }
}