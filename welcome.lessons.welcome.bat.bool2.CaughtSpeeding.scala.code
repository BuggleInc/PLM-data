def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {

var vitesse = speed;
	if(isBirthday)
	{
		vitesse = vitesse - 5;
	}

	return amende(vitesse);

	
}
def amende(speed:Int): Int = {
var amende = 0;
	if(speed > 60)
	{
		amende = 1;
	}
	if(speed > 80)
	{
		amende = 2;
	}

	return amende;	
}