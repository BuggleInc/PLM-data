def indexOfMaxValue(nums:Array[Int]):Int = {
    var maximum = nums(0)
    var indice = 0
  for (i  <- 1 to nums.length -1){
    if (nums(i)> maximum){
      indice = i
      maximum = nums(i)
    }
  }
  return indice
}
