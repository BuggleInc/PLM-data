def indexOfMaxValue(nums:Array[Int]):Int =
{
  var max=nums(0);
  var index=0
  
  for(i <- 0 to nums.length-1)
  {
    if(nums(i)>max)
    {
      max=nums(i)
      index=i
    }
  }
  return index
}