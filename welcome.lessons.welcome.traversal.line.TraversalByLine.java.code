public void run() {
	lineByLine();
}

void lineByLine(){
int width = getWorldWidth();
int height = getWorldHeight();
int gx = getX();
int gy = getY();
int count = 0;

	for(int x = gy; x < height; x++){
		for(int y = gx; y < width; y++){
			
			if(!finalPos()){
				setPos(y, x);
				writeMessage(count);
				count++;
			}			
		}
	}
}

boolean finalPos(){
	if(getX() == getWorldWidth() && getY() == getWorldHeight()){
		return true;
	}

	return false;
}//end finalPos

