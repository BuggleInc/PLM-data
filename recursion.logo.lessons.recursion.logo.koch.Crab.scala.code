def crab(levels:Int, length:Double) {
  Krab(levels,length)
}
def Krab(levels:Int , length:Dooble){
  if (levels==0){
    avance(length)
  }
  else {
    gauche(45)
    Krab(levels-1,length/(Math.sqrt(2)))
    
    droite(135)
    Krab(levels-1,length/(Math.sqrt(2)))
    
    gauche(45)
  }
}
