def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {
var count:Int = 0;
var T = Array(a,b,c,d);
for(i <- 1 to T.length){
	if(isTeen(T(i-1))){
		count = count + 1;
	}
}
return count;
}

def isTeen(n:Int):Boolean = {
var bool = true;
	if(n < 13 || n > 19) 
	{
	bool = false;	
	}
	return bool;
}
