def cocktailSort() {
  var b =  true
  while (b) {
    b=false
  	for (i <- 1 to getNombreValeurs()-1) {
      if (getValeur(i-1)>getValeur(i)) {
        echange(i-1,i)
        b=true
      }
    }
    for (i <- getNombreValeurs()-1 to 1) {
      if (getValeur(i-1)>getValeur(i)) {
        echange(i-1,i)
        b=true
      }
    }
  }
}