def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {
 if(length==0){
 	return 
 } 
 else {
   spiral(steps, 90 , length , increment)
   avance(length)
 	gauche(90)
 	spiral(steps-1, 90 , length+increment , increment)
 	
 }
}
