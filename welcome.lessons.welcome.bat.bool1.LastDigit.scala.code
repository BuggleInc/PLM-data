def lastDigit(a:Int, b:Int):Boolean = {
	var A:Int = a
	var B:Int  = b
		if(A==10 && B == 0)	{
			return true
		}
		
			A = A%(10)
			B = B%(10)
	
		
			return (A==B) 
		
}//Je sais c'est archi trafiqué et si b vaut 10 et que a vaut 0
 // Je vais avoir une erreur 
 