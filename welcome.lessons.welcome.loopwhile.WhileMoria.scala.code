droite(); droite(); 

while (!estFaceMur) { 

	while (!estSurBiscuit && !estFaceMur) {
		avance();
	}
	if (estFaceMur) { droite(); avance(); gauche(); avance(); }
	else (!estFaceMur) {
	prendBiscuit();}
	
	while (!estSurBiscuit) {
		recule();
	}
	avance()
	poseBiscuit(); avance()} 

