def hunt(c:Color) {
  isFacingTrial(c)
     
 }

def isFacingTrial(Color): Boolean={
if( estFaceMur()){
  return(false)
}
  avance()
  if (getCouleurSol()==Color.Color){
  recule()
    return(true)
}else{
    recule()
  return false
}
}