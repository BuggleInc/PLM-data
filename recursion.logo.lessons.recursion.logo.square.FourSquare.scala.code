override def run() {
  def carre() :
  	{
      avance(100.)
      droite(90.)
      avance(100.)
      droite(90.)
      avance(100.)
      droite(90.)
      avance(100.)
    }
 
for(i <- 0 to 3)
  {
    carre()
    droite(180)
  }
}