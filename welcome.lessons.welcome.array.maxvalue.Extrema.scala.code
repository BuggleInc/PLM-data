def extrema(nums:Array[Int]): Int = {
	var x=0
	var posmax=0
	var posmin=0
	for (i<-0 to nums.length-1){
		if (nums(i)>nums(posmax)) posmax=i
		if (nums(i)<nums(posmin)) posmin=i
	}
	x=nums(posmax)-nums(posmin)
	return x
}
