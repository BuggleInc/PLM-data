def solve() {
  var i=0
  for (a<- 0 to getSize()-1){
    if (getColor(a)==BLUE){
      swap(i,a)
      i=i+1
    }
    
  }
  for (a<-i to getSize()-1){
    if (getColor(a)==WHITE){
      swap(i,a)
      i+=1
    }
  }
}