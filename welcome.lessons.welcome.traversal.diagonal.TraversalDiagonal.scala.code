override def run() {
var diag = 0;
var c:Int=0

	for (k<-0 to getMondeHauteur()-1) 
	{ 
		for (y<-(0 to k).reverse) 
	{	setPos(k-y,y)
		ecritMessage(c)
		c=c+1		
	}
	}
	for (k<-1 to getMondeHauteur()-1)
		for (y<-(6 to k) by -1)
	{
		setPos(y-5,k-y+6)
		ecritMessage(c)
		c=c+1
	}
}