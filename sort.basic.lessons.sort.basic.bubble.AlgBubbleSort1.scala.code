def bubbleSort()  {
  var test:Boolean= true 
  while (test) {
    var ech:Int=0
    var nombre:Int = getNombreValeurs()
    for (i<-0 to nombre-2){
      if (!(plusPetit( getValeur(i), getValeur(i+1)))) {
        echange(i, (i+1))
        ech = (ech +1) }}
    if (ech==0) {test=false}
  }  
}