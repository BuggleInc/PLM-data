def goAndGet() {
  while(!isOverBaggle()){
  forward()
  }
  if(isOverBaggle()){
    pickupBaggle();
    left();
    left();
  }
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}