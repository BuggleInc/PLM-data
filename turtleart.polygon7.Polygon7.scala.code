override def run() {  
  avance(80:Double)
  droite(40:Double)
  avance(80:Double)
  droite(40:Double)
  avance(80:Double)
  droite(40:Double)
  avance(80:Double)
  droite(40:Double)
  avance(80:Double)
  droite(40:Double)
  avance(80:Double)
  droite(40:Double)
   avance(80:Double)
  droite(40:Double)
                   }