def insertionSort() {
  var j:Int = 0
  var len:Int = getNombreValeurs()
  
  for (i <- (len - 1) to 1 by -1) {
    j = i
    while (plusPetit(j,(j-1))) {
      echange(j,(j-1))
      j -= 1
    }
  }
}