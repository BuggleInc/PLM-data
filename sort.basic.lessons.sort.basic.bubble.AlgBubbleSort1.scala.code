def bubbleSort()  {
  var c:Int=1
  while (!c==0) {
    c=0
    for (i <- 0 to getNombreValeurs()-2) {
      if (getValeur(i:Int)>getValeur((i+1):Int)){
        c=1
        p=getValeur(i:Int)
        getValeur(i:Int)=getValeur((i+1):Int)
        getValeur((i+1):Int)=p
      }
    }
  }
}