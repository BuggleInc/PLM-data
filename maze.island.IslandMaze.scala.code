def checkMur():Boolean = {
  gauche()
  if (estFaceMur()){
    droite()
    return true
  }
  droite()
  return false
}

def keepHandOnSideWall(){
  if(!estFaceMur()){
    if (checkMur()){
      avance()
    } else {
      gauche()
      avance()
    }
  } else{
    if (checkMur()){
    	droite()
      if (!estFaceMur()){
      	avance()
      } else {
        droite()
        avance()
      }
    } else {
      gauche()
      avance()
    }
  }
}

def estSurMurNord():Boolean = {
  return getY() == 0
}

def eviteObstacle(){
  if (estFaceMur()){
    droite()
    
  }
}

override def run() {
  while (!estSurMurNord()){
    eviteObstacle()
  }
  while (!estSurBiscuit()){
    keepHandOnSideWall()
  }
  prendBiscuit()
}