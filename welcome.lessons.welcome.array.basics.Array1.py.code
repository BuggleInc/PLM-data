def array():
    n = 0
    brushDown()
    for b in L:
        setBrushColor(b)
        forward()
        if n == len(L)-2:
            brushUp()
        n= n+1
    
def storeArray():
    global L
    L = []
    for a in range(getWorldHeight()-1):
        L.append(getGroundColor())
        forward()
    L.append(getGroundColor())
    backward(getWorldHeight()-1)

storeArray()
for c in range(getWorldHeight()-1):
    forward()
    left()
    forward()
    right()
    array()

    