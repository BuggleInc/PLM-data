def max2Value(nums:Array[Int]): Int = {
  var n =nums.length
  var max=0
  var max2=0
  for (i<-0 to n-1){
    if(max<nums(i)){
      max2=max
      max=nums(i)
    }
    else if(max2<nums(i)){
      max2=nums(i)
    }
    
  }
  return max2
}