override def run() {
    while (!estSurBiscuit) {
      if (random3()==0) {
        avance()
      } else if (random3()==1) {
        droite();avance()
      } else {
        gauche();avance()
      }
    }
} 