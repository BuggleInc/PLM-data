def has271(nums:Array[Int]): Boolean = {
  var n=0
  for (i<- 0 to nums.length-3){
    for (l<- (-10) to 10){
      for (m<- (-2) to 2){

        if (nums(i)==2+l && nums(i+1)==7+l && nums(i+2)==1+l+m)
        n=n+1
      }}}
      if (n>0)
      return true
      else
      return false
    }
