def indexOfMaxValue(nums:Array[Int]):Int = {
  var max:Int=nums(0)
  var imax:Int=0
  val taille:Int=nums.length
  
  for (i<-1 to (taille-1)){
    if (nums(i)>max){
      max=nums(i)
      imax=i
    }
  }
  return imax
}
