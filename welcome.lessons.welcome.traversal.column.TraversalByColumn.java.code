public void run() {
	boolean a = isFacingWall();

	int index = 0;
	for(int x = 0; x < getWorldHeight(); x++){
		setY(y);
		for(int y = 0; y < getWorldWidth(); y++){ 
			setX(x);
			writeMessage(index);
			index++;		
		}
	}
}

