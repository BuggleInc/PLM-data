<html><head>  <style type="text/css">
    body { font-family: tahoma, "Times New Roman", serif; font-size:10px; margin:10px; }
    code { background:#EEEEEE; }
    pre { background: #EEEEEE;
          margin: 5px;
          padding: 6px;
          border: 1px inset;
          width: 640px;
          overflow: auto;
          text-align: left;
          font-family: "Courrier New", "Courrier", monospace; }
   .comment { background:#EEEEEE;
              font-family: "Times New Roman", serif;
              color:#00AA00;
              font-style: italic; }
  </style>
</head><body>
<!-- Please don't translate this file but l10n/missions/plm.pot (see https://github.com/oster/PLM/wiki/Working-on-translations) -->
<h2>Rep?rer une zone plate</h2>

<p>Apr?s 200 jours de voyage intersid?ral ? manger des c?r?ales, vous ?tes
enfin parvenu ? la plan?te rouge. Il est enfin temps de se poser !</p>

<p>Le module est en pilote automatique, et vous n'avez qu'? regarder autour de
vous pour s?lectionner une zone d'alunissage. Vous devez vous poser sur
<b>une zone plate</b> si vous ne voulez pas vous scratcher. Vous indiquez au
pilote automatique o? se poser en modifiant la fonction
<code>getLandingZone</code> de fa?on ? ce qu'elle retourne un
Segment repr?sentant la zone de votre choix.

<p>Un Segment est compos? de deux <code>Point</code>s, chacun compos? de deux
doubles. On construit un nouveau segment de la fa?on suivante :
<code>Segment(debut:Point, fin:Point)</code>. Pour cr?er de nouveaux points, il faut
utiliser le constructeur <code>Point(x:Double,
y:Double)</code>. Les coordonn?es d'un point
sont accessibles gr?ce ? ses m?thodes <code>x():Double</code> et <code>y():Double</code>.</p> 

<p>Pour d?terminer la zone cible, vous devez appeler
<code>getSol():List[Point]</code>,
qui retourne les points qui composent la surface sous la forme d'une list de
<code>Point</code> .</p>

<p>Une derni?re chose : votre fonction doit retourner une zone valide dans
toutes les situations, pas seulement celle que l'on voit en ce moment. C'est
que votre code sera utilis? dans ce cas, mais ?galement dans la
configuration que nous avions dans l'exercice pr?c?dent. Comme d'habitude,
vous pouvez voir les autres mondes en les s?lectionnant dans le combobox au
dessus de la vue du monde.</p>

<p>Bon alunissage ! N'oubliez pas de mettre votre combinaison spatiale en
sortant.</p>
</body></html>