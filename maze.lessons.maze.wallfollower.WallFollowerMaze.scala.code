override def run() {
	while (!estSurBiscuit()) {
	keepHandOnSideWall(); avance()}
prendBiscuit()
}

	 
def keepHandOnSideWall() {
	gauche();
	if(!estFaceMur()) {avance()}
	if(estFaceMur()) {droite()} 
			
}
