def noTriples(nums:Array[Int]): Boolean = {
	var i:Int = 0
  
  while (i < nums.length) {
    if (!(i > nums.length - 3)) {
      (nums(i), nums(i + 1), nums(i + 2)) match {
        case (nums(i), nums(i), nums(i)) => return false
      }
    }
  }
  
  return true
}
