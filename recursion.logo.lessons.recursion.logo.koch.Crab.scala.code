def crab(levels:Int, length:Double) {
  if (levels==0) {
    avance(length)
  }
  else {
  	hexaKoch(levels-1,length*Math.sqrt(2))
    gauche(45)
    hexaKoch(levels-1,length*Math.sqrt(2))
    droite(45)
    hexaKoch(levels-1,length*Math.sqrt(2))
    droite(45)
    hexaKoch(levels-1,length*Math.sqrt(2))
    droite(45)
    hexaKoch(levels-1,length*Math.sqrt(2))
    droite(45)
    hexaKoch(levels-1,length*Math.sqrt(2))
    gauche(45)
    hexaKoch(levels-1,length*Math.sqrt(2))
  }
}