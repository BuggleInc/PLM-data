<html><head>  <style type="text/css">
    body { font-family: tahoma, "Times New Roman", serif; font-size:10px; margin:10px; }
    code { background:#EEEEEE; }
    pre { background: #EEEEEE;
          margin: 5px;
          padding: 6px;
          border: 1px inset;
          width: 640px;
          overflow: auto;
          text-align: left;
          font-family: "Courrier New", "Courrier", monospace; }
   .comment { background:#EEEEEE;
              font-family: "Times New Roman", serif;
              color:#00AA00;
              font-style: italic; }
  </style>
</head><body>
<!-- Please don't translate this file but l10n/missions/plm.pot (see https://github.com/oster/PLM/wiki/Working-on-translations) -->
<h2>Arbres</h2>

<p>Nous allons maintenant dessiner des arbres. Pour cela, nous allons ?crire
une fonction doublement r?cursive de prototype</p> 
<pre>tree(nbPas:Int, longueur:Double, angle:Double, multiplicateur:Double)</pre>

<p>Pour dessiner un arbre ? quatre ?tages, il faut dessiner un tronc de la
taille indiqu?e, tourner ? droite de l'angle indiqu?, faire un arbre ? 3
?tages, tourner ? gauche de deux fois l'angle indiqu?, faire un autre arbre
? 3 ?tages, puis revenir ? sa position initiale. N'oubliez pas de revenir ?
la position initiale !<p>

<p>Si le tronc d'un arbre mesure length ? l'?tage N, il mesure shrink*length ?
l'?tage N+1.<p>

<p>Comme vous pouvez le voir, chaque ?tage de la r?cursion est repr?sent? par
une couleur distinctive. Pour cela, appelez la fonction
<code>current(step)</code> en lui passant en param?tre l'?tage actuel de la
r?cursion. Elle se chargera de choisir la couleur ad?quate. N'effacez pas
vos couleurs quand vous retournez ? votre position initiale.</p>

<p>Si vous vous trompez dans votre fonction, il peut facilement devenir tr?s
compliqu? de debugger votre code. Vos erreurs se produisant ? tous les
?tages de la r?cursion, le dessin peut vite devenir illisible. Pour vous
aider ? debugger, vous pouvez utiliser la fonction <code>subtree()</code>
qui dessine un sous-arbre correct. Cette fonction est tr?s (tr?s) semblable
par ailleurs ? la fonction que vous tentez d'?crire. La seule diff?rence est
que <code>subtree()</code> ne dessine qu'en noir. Vous pouvez donc
l'utiliser ? la place de vos appels r?cursifs pendant le debug, mais vous
devez changer pour des appels r?cursifs ? votre propre code (quand il
marche) pour avoir les couleurs justes, et donc pour passer l'exercice.</p>  
</body></html>