def sierpinski(level:Int, length:Double) {
	if (levels == 0) {
		avance(length);
		droite(120);
		avance(length);
		droite(120);
		avance(length);
		droite(120);
	}
	else {
		sierpinski(levels-1,length/2);
		avance(length/2);
		sierpinski(levels-1,length/2);
		droite(240);
		avance(length/2);
		gauche(240);
		sierpinski(levels-1,length/2);
		gauche(240);
		avance(length/2);
		droite(240);
	}
}
