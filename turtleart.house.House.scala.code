override def run() {
  var x=100
  var i=0
  for (i <- 1 to 4){
    avance(x)
    droite(90)
  }
  avance(x)
  droite(30)
  for (i <- 1 to 3){
  avance(x)
  droite(180-60)
  }
  droite(180)
  avance(x)
  droite(180)
}