def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  
  if(steps > 0){
    current(steps)
    avance(length)
    droite(angle)
    gauche(2*angle)
    avance(length)
    droite(angle)
  	recule(length)
  }
}