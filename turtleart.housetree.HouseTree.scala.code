override def run() {
  for (i<-1 to 4) {
    house()
		allerVers(getX+80000,getY)
  }
}

def house() {
  avance(30)
  droite(30)
  avance(30)
  droite(120)
  avance(30)
  droite(120)
  avance(30)
  recule(30)
  gauche(90)
  avance(30)
  droite(90)
  avance(30)
  droite(90)
}