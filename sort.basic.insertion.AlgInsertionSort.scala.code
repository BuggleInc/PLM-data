def insertionSort() {
  for (i <- 1 to getNombreValeurs() -1){
    var v : Int = i
    var indice : Int = i-1
    while(indice >=0 && plusPetit(v,indice)){

      indice = indice - 1
    }
  echange(v,indice+1)  
    
  }
}