var buggle = 0
do {
    if (isOverBaggle()) {
    buggle = buggle + 1 ;
    } 
    forward() ;
} while (!crossing())
if (buggle>=3) {
  left() ; forward() ;
}
else right() ; forward() ;
if (exitReached()) {
  forward() ;
}