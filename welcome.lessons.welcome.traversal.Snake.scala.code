override def run() {
  for(i<-1 to 7*4)
  	serpent()
}

def serpent(){
  if(!estFaceMur()){
    avance()
  } else{
    if(getDirection==Direction.OUEST){
      gauche()
      avance()
      gauche()
    }
    if(getDirection==Direction.EST){
      droite()
      avance()
      droite()
    }
  }
}