def averageValue(nums:Array[Int]): Int = {
  var sum:Int=0
  var res:Double=0.0
  val taille=nums.length
  
  for (i<-0 to (taille-1)){
    sum+=nums(i)
  }
  
  res=sum/taille
  
  return res.toInt
  
}