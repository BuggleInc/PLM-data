def island(num:Array[Int]): Int = {
  return level(num,0,1)
}
  
  
  
def level(num:Array[Int],niveau:Int,x:Int):Int ={
  if (x==num.length){
    return niveau
  }
  else
  if (num(x)>num(x-1)){
  return level(num,niveau+1,x+1)
  }
  else return level(num,niveau,x+1)
}