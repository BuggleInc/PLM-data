public void insertionSort() {
	int lgr = getNombreValeurs();
	int v = 0;
	int k=0;
	for(int i = 1 ; i < lgr; i++)
	{
		v = getValeur(i);
		k=i-1;
		
		while(k>0 && getValeur(k)>v)
		{
			setValeur(k+1, getValeur(k));
			k--;
		}
		setValeur(k,v);
	}
}

