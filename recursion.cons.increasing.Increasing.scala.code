def increasing(l:List[Int]): Boolean = {
  def lambda(l:List[Int], last:Int): Boolean = {
    if (last < l.head){
      return lambda(l.tail, l.head);
    }
    else return false;
  }
  return lambda(l.tail,l.head);
}