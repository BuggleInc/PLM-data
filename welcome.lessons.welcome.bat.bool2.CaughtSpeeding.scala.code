def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
  if (isBirthday) {return amende(65,85,speed)}
  else {return amende(60,80,speed)}
}

def amende(a:Int,b:Int,c:Int): Int= {
  var d=c-a
  var e=c-b
  if (d<=0) {return 0}
  else if (d>0 && e<=0) {return 1}
  else {return 2}
}