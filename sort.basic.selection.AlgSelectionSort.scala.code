var len = getValueCount();
def selectionSort() {
  
  for(i <- 0 to len){
    var ind = select(i);
    swap(ind, i);
    
  }
}

def select(i:Int) : Int = {
  var indMini = i;
  for(j<- i to len){
    if(getValue(j) < getValue(i))
    	indMini = j;
  }
  return indMini;
  
}