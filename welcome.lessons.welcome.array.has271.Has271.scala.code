def has271(nums:Array[Int]): Boolean = {
  var a =false
  for(cpt1<-0 to nums.length-3) {
    if (nums(cpt1+1)==nums(cpt1)+5 && (nums(cpt1)>=nums(cpt1+2)+1) && nums(cpt1)<=nums(cpt1+2)-3) {
      a=true
    }
  }
  return a
}