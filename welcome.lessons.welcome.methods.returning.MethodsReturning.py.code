def haveBaggle():
    seenBaggle = False
    #steps = 0
    while seenBaggle == False:
        forward()
        if isOverBaggle() == True:
            seenBaggle = True
        #steps = steps + 1
    backward(6)
    right()
    forward()
    left()
    return seenBaggle
    
