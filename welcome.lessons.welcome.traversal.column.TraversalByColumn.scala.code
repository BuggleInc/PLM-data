override def run() 
{
	var compt = 0

	for(i <- 0 to getMondeHauteur()*getMondeLargeur()-1)
	{
		ecritMessage(i)
		droite(); droite()
		if(estFaceMur())
		{
			setPos(getX()+1,0)
		}
		else setPos(getX(),getY()+1)
		droite(); droite()
	}
}
