def dragon(order:Int, x:Double, y:Double, x2:Double, y2:Double) {
	if (order == 1) {
		setPos(x,y)
		allerVers(x2,y2)
	}
	else {
		dragon(order - 1,x, y,((x + y + x2 - y2) / 2.), ((y + y2 - x2 + x) / 2.))
		dragon(order - 1 , ((x + y+ x2 - y2) / 2.) , ((y + y2 - x2 + x) / 2.)), x2 , y2)
	}
	
}
