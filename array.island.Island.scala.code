def island(num:Array[Int]): Int = {
  var level = 0
  var lastHigh = 0
  for( i <-0 to num.length-1)
  {
    if(num(i)>lastHigh)
      level = level +1
    
    lastHigh = num(i)
  }
  
  return level
}