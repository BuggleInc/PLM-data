def bubbleSort()  {
  var ok:Boolean = false
  var n:Int = getNombreValeurs()-1
  while (!ok){
    ok=true
    for (i<- 1 to n){
      if (!plusPetit(i,i+1)){
        echange(i,i+1)
        ok = false
    println(ok)
      }
    }
  }
}