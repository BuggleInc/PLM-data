def isFacingTrail():Boolean = {
  if(!estFaceMur()){
    avance()
    if(getCouleurSol()==Color.green){
      baisseBrosse()
      leveBrosse()
      avance()
    }
    else {gauche()}
    return(true)
  }
  else{return(false)}
}