def gnomeSort() {
  var pos=2;
  while(pos<getNombreValeurs()-1){
    if(plusPetit(pos-1,pos)){
      pos=pos+1;
    }
    else{
      echange(pos,pos-1);
      if(pos>1){
        pos=pos-1;
      }
    }

}
}