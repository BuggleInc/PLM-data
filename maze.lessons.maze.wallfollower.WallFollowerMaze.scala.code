/*override def run() {
  while (!estSurBiscuit()) {
    if (checkg()) {gauche(); avance()}
    else if (!estFaceMur()) {avance()}
    else {droite}
  }
prendBiscuit()
}
    


def checkg():Boolean = {
  gauche()
  var r = !estFaceMur()
  droite()
  return r
}*/
  
override def run() {
  while (!estSurBiscuit()) {
    KeepHandWall()
    go()
  }
}

def KeepHandWall() = {
  gauche()
  if (estFaceMur()) {
    droite()
  }
}

def go() = {
  if (estFaceMur()) {
    droite()
  } else {avance()}
}

