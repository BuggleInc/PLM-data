def isFacingTrail():
   # write your code here
    while isOverBaggle != True:
        forward()
        if (getGroundColor() != Color.green):
            backward()
            left()
        else:
            brushDown()
            brushUp()
            forward()

isFacingTrail()