def insertionSort() {

var temp = 0;
var j = 0;
for (i<-1 to getNombreValeurs() -1) {
	temp = getValeur(i);
	j = i;
	while (!plusPetitQue(j-1,temp) && j>=1 ) {
		echange (j-1,j);
		j-= 1;
	}
	setValeur (j,temp);
}
}
