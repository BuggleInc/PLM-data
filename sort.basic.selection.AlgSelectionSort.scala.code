def selectionSort() {
  var lgr=getNombreValeurs()
  var idx=0
  var min=0
  for (i<-0 to lgr-1) {
    for (j <- i to lgr) {
      if (getValeur(j)<min) {idx=j;min=getValeur(j)}
    }
    echange(i,idx)
  }
}