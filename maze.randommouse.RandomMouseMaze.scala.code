override def run() {
    if(!estFaceMur() || !estSurBiscuit()){
      avance();
    } else {
      var r = random3();
      if(r == 1){
        droite();
      } else {
        gauche();
      }
    }
  if(estSurBiscuit()){
    prendBiscuit();
  }
}