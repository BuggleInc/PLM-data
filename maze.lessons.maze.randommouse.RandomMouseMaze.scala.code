override def run() {
  var a = random3();  
  while (!estSurBiscuit()) {
  	if (a==0) {
      if (!estFaceMur()) {
        avance();
      }
    } else if (a==2) {
      droite();
    } else gauche()
  } 
  prendBiscuit()
}