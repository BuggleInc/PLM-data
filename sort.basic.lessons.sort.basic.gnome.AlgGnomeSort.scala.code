def gnomeSort() {
  var position:Int = 0
  while(position < getValueCount() - 1){
	  if(getValue(position + 1) > getValue(position + 2)){
      swap(position + 1, position + 2)
    	position -= 1
    }
    else
    	position += 1
  }
}