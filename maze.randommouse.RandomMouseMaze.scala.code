override def run() {
  while (!isOverBaggle) {
    if (random3()==0) {
      if (!isFacingWall) {
        forward()
      }
    } else if (random3()==1) {
      left();
    } else {
      right()
    }
  }
  pickupBaggle()
}