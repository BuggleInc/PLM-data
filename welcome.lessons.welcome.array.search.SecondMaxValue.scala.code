def max2Value(nums:Array[Int]): Int = {
  var max : Int = nums(0)
  var max2 : Int = 0
  for (i <- 0 to nums.length-1){
    if (max<nums(i)){
      max2=max
      max=nums(i)
      } else if (max2<nums(i)){
      	max2=nums(i)
    }
  }
  return max2
}