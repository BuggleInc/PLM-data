def isFacingTrail():
    piste = False
    forward()
    if getCouleurSol() == Color.green:
        piste = True
    backward()
    if piste == True:
        baisseBrosse()
        leveBrosse()
        forward()
