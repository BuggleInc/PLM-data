override def run() {
    def keepHandOnRightWall() = {
    	while (!(estFaceMur())) {gauche()}
    	droite()
    	if (!(estFaceMur)) {avance()}
    }
    while (!(estSurBiscuit)) {
    	keepHandOnRightWall();
    }
    prendBiscuit();
}
