public void bubbleSort()  {
  
  boolean swap = true;
  while(swap){
  swap = false;  
  for(int i = 0 ; i < getNombreValeurs()-1; i++ )
{
    if(!plusPetit(i,i+1)){
      
      echange(i,i+1);
      swap = true;
    }
    
  } 
  }
}