def sierpinski(level:Int, length:Double) {
	if (levels == 0) {
		avance(length)
		droite(120.)
		avance(length)
		droite(12.)
		avance(length)
		droite(12.)
	}
	else {sierpinski(level-1,length / 2.)
		avance(length / 2.)
		sierpinski(levels - 1, length / 2.)
		recule(length / 2.)
		droite(60.)
		avance(length / 2.)
		gauche(60.)
		sierpinski(levels - 1, length / 2.)
	}
}
