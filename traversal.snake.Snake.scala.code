def posFinal() :Boolean {
  right()
  var a = estFaceMur()
  back()
  var b = estFaceMur()
  right()
	if (estFaceMur() && ((getDirection() == Direction.EST && b) || (getDirection() == Direction.WEST && a)){
    return true
  }
  else {
    return false
  }
      }

 

override def run() {
  brushDown()
	do {forward()
    
  } while (!posFinal())
    

  
}