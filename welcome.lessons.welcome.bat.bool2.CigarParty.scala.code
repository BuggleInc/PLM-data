def cigarParty(cigars:Int, isWeekend:Boolean):Boolean = {

	var success = false;

	if(cigars >= 40)
	{
		if(isWeekend)
		{
			success = true;
		}
		else
		{
			if(cigars <= 60)
			{
				success = true;
			}	
		
		}

		
	}
	return success;

	
}
