def haveBaggle():Boolean = {
  var baggle = false
  for (i <- 1 to 7) {
  	for (j <- 1 to 6) {
    	if (estSurBiscuit())
    		baggle = true
      avance()
    }
  	for (i <-1 to 6)
  		recule()
  	if (baggle)
    	return baggle
  	else {
    	droite()
    	avance()
    	gauche()
  	}
	}
  return false
}
