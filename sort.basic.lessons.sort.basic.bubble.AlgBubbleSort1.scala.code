def bubbleSort()  {
  var continue = true
  while (continue){
  for (i <- 0 to getValueCount()-1){
      continue = false
      if (isSmaller(i,i-1)){
        swap(i-1, i)
        continue = true
      } 
  }
  }
}