def stringX(str:String):String = {
  if (str.length==1){
    return str
  }
  var S=str.slice(0, 1)
  for (i<-1 to str.length-2){
    if (str.charAt(i)!='x'){
      S+=str.slice(i, i+1)
    }
  }
  S+=str.slice(str.length-1,str.length)
  return S
}
