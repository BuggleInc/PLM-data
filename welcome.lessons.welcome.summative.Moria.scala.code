back()
while (! isFacingWall) {
  if (isOverBaggle())
  	moveBaggle();
  
  forward();
  
  
}

def moveBaggle() {
  pickupBaggle();
  back();
  while (!isFacingWall() && !isOverBaggle() && (getDirection() == Direction.WEST)) {
    forward();
  }
  
  
  backward();
  dropBaggle();
  back()
}