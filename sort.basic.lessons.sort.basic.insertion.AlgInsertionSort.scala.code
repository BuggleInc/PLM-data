def insertionSort() {
  var n = getNombreValeurs ()-1
  var temp= 0
  

  for (i <- 1 to n){
    temp=getValeur(i)
    for (j <- i to 1 by -1){
      if (getValeur(j)<getValeur(j-1)){
        echange(j,j-1)
      }
      
    }
    //getValeur(n)=temp
    
  }
  
}
