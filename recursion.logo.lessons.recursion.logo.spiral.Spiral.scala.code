def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {
  if ( steps == 0 ){  
  } else if ( steps == 1 ){
    gauche(angle);
    avance(length);
    spiral(steps,angle,length+increment,increment);
  } else {
    gauche(angle);
    avance(length);
    spiral(steps-1,angle,length+increment,increment);
  }
}