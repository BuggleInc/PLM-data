var nbBiscuit = 0;
while (!sortieTrouvee) {
	do { avance(); if (estSurBiscuit) { nbBiscuit = nbBiscuit + 1; } } 
	while (!croisement())
	if (estSurBiscuit) { nbBiscuit = nbBiscuit + 1; }
	if (nbBiscuit <= 2) { gauche(); }
	else { droite() }
	
	
}
