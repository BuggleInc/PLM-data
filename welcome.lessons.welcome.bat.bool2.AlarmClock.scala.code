def alarmClock(day:Int, vacation:Boolean): String = {
	if ((day==0 && vacation) || (day==6 && vacation)) { 
		return "off" 
	}
	if ((day==0 && !vacation) || (day==6 && !vacation)) {
		return "10h00"
	}
	else {return "7:00"}
}
