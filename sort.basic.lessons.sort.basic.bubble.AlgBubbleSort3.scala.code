def bubbleSort2()  {
  var swapped=false
for (i<-getNombreValeurs() -1 to 0 by -1) {
  for (j<-0 to i-1) {
  if (!plusPetit(j,j+1)) {
    echange(j,j+1)
    swapped=true
  }
}
  if (!swapped) return
}
}