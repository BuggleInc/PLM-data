def isFacingTrail():
   # write your code here
    while isOverBaggle != True:
        if (getGroundColor() == Color.lightGray):
            if (isFacingWall()):
                left()
            else:
                forward()
        elif (getGroundColor() == Color.green):
            brushDown()
            brushUp()
            forward()

isFacingTrail()