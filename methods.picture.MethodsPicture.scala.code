override def run() 
{
  var L:Array[Color]=Array(Color.yellow,Color.red,Color.blue,Color.green)
  for (k<-1 to 3)
  	{
  	for (j<-1 to 3)
  		{
  		for (i <- L)
  			{
    			avance()
    			gauche()
    			faireV(i)
  			}
      	avance(5)
    	}
  	gauche()
  	avance(5)
  	droite()
    }
}

def faireV(color:Color)
{
  for (i<-1 to 2)
  {
  	marquer(color)
  	avance()
  	droite()
  	avance()
  }
  marquer(color)
  gauche()
  avance()
  gauche()
}

def marquer(color:Color)
{
  setCouleurBrosse(color)
  baisseBrosse()
  leveBrosse()
}