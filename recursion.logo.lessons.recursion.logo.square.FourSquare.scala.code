override def run() {
  FaitCarre1()
  for (i<-1 to 3){
    FaitCarre2()
  }
}

def FaitCarre1(){
  baisseBrosse()
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
  leveBrosse()
  droite(180)
  avance(100)
  droite(90) 
}

def FaitCarre2(){
  baisseBrosse()
  
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
  droite(180)
  avance(100)
  leveBrosse()
  droite(90) 
}
