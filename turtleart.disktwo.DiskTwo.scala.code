override def run() {
  for (i<-1 to 9) {
    for (i<-1 to 20) {
      setCouleur(Color.black)
      avance(100)
      recule(100)
      droite(1)
    }
    for (i<-1 to 20) {
      setCouleur(Color.red)
      avance(100)
      recule(100)
      droite(1)
    }
  }
}