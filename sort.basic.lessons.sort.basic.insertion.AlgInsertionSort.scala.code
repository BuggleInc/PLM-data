def insertionSort() {
  var n:Int = getNombreValeurs()
  
  for (i <- 1 to n - 1) {
    var v:Int = getValeur(i)
    
    var j:Int = i
    
    while (getValeur(j - 1) > v && j > 0) {
      copie(j - 1, j)
      
      j -= 1
    }
    
    setValeur(j, v)
  }
}