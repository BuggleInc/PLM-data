def bubbleSort()  {
	var inverse = true;
	var i = 0;
	do{
         if(plusPetit(i+1,i)) {
		echange(i+1,i);
         }
         else {
         	var inverse = false;
         }
      }while( inverse )
}
