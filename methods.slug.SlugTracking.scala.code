def isFacingTrail():Boolean = {
  	var vuVert: Boolean = false
  	if (estFaceMur()==false) {
      forward();
      if (getCouleurSol()==Color.green) {
        vuVert=true
      } else {
        isFacingTrail();
      }
    } else {
      isFacingTrail();
    }
  	backward();
  	return vuVert
}

