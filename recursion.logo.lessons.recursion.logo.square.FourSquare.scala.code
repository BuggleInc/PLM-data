def carré() {
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
  droite()
}

override def run() {
  carré()
  gauche(90)
  carré()
  gauche(90)
  carré()
  gauche(90)
  carré()
}