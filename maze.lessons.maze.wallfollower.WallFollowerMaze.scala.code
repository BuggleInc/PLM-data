override def run() {
def keepHandOnSideWall() {
	gauche();
	if (estFaceMur()) {
		droite();
		avance();
		gauche();
		if (!(estFaceMur())) {
			avance();
			gauche();
			if (!(estFaceMur())) {
				avance();
				gauche();
			}
		}
		
	}
	
}

while (!(estSurBiscuit())) {
keepHandOnSideWall();
}
}
