override def run() {

	def quartier(c:Color) {
		setCouleur(c)
		for(i<-1 to 15) {
			avance(100)
			recule(100)
			droite(90.0/90)
		}
	}

	for (i<-1 to 9) {
		quartier(Color.black)
		quartier(Color.red)
	}

}
