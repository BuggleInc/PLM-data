def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {

	setCouleur(Color.orange);
	cercle(1);
	setCouleur(Color.black);
	avance(length);
	gauche(angle);
	spiral(5,angle,length + 3 , increment );

	
}
