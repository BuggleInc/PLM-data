override def run() {
	for(i<- 1 to 9 ) {
	arcCircle (100,360/18,360/9)
	}
	
}
def arcCircle(radius:Int, arc : Int, rays:Int) {
	setCouleur(Color.black)
	for (i<-1 to rays) {
		avance(radius)
		recule(radius)
		droite(1)
	}
	setCouleur(Color.red)
	for (i<-1 to rays) {
		avance(radius)
		recule(radius)
		droite(1)
	}
}
