var cookies:Int =0

while (estSurBiscuit) {
	cookies=cookies+1; //quand tu es sur un cookie le compteur prend plus 1
}


while (cookies!=cookies+4) {
  avance()
} //tant que le compteur ? cookie n'est pas ?gal ? 4 tu recommence