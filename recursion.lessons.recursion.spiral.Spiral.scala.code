def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {	
	while(steps!=0){
		var x = increment
		var longueur = length + x
		
		avance(longueur)
		gauche(angle)

		pas = steps-1
		
		spiral(pas, 90, longueur, 5)
	}
	
}

