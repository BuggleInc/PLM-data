def bubbleSort2() {
	var x:Boolean = false
	var y:Int = 2 //compteur tours
	do {
		x = false
		for (i <- getNombreValeurs() -y to 0 by -1) {
			if (plusPetit(i+1,i)) {
			echange(i,i+1);
			x = true}
			
		}
		y = y+1
	} while(x)
}
