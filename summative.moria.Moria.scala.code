

while(!isFacingWall()){
  
  
  while ( !isOverBaggle() && !isFacingWall()){
		backward()
  }
  
  if(isOverBaggle()){
    pickupBaggle()
    while(!isOverBaggle())
    	forward()
    backward()
    dropBaggle()
    backward()


  }
  
  
}

left();forward();//right();forward()