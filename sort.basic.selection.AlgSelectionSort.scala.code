def selectionSort() {
  for (k <- 0 to getNombreValeurs()-2){
    var xmin = k;
    for (i<- k to getNombreValeurs()-1){
      if(getValeur(i)<getValeur(xmin)){
        xmin = i
      }
    }
    if (k != xmin){
      echange(k,xmin)
    }
  }
}
