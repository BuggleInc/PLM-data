def isFacingTrail():Boolean = {
    if(estFaceMur()){return(false)}
  	else{
      avance()
      if(getCouleurSol() == green){
        recule()
      	return(true)
      }
     	else{
        recule()
        return(false)
      }
    }
}
