def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
if ( steps==0)  { return  }
else {
    avance(length)
    droite(length)
    tree(steps-1,length,angle,shrink)
    avance(length)
    gauche(length)
    tree(steps-2,length,angle,shrink)
    }
}

