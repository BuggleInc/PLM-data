override def run() {
  var cpt:Int = 0
  while(!positionFinale) {
    if(getX()<(getMondeHauteur()-1)) {
      setPos(getX()+1, getY())
    } else setPos(0,getY()+1)
  }
}

def positionFinale():Boolean = {
  if (getX()==(getMondeHauteur()-1) && getY()==(getMondeLargeur()-1)) return true
  return false
}
