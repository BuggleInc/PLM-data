def insertionSort() {
	for (i <- 1 to getNombreValeurs() - 1) {
		var valeur = getValeur(i)
		var j = i - 1
		while (plusPetit(valeur,j) && j >= 0) {
			copie(j,j+1)
			j = j - 1
		}
		setValeur(valeur,j)
	}
}
