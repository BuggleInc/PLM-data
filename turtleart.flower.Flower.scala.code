override def run() {
  for(i<-1 to 8)
  	{
      avance(50)
      droite(360.0/8.0)
    }
  for(i<-1 to 16)
  	{
      avance(25)
      droite(30)
      avance(25)
      gauche(50)
      avance(25)
      droite(90)
    }
  
}