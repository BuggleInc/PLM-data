override def run() {
  var colonne = 0;
  var ligne = 0;
  do {if(estFaceMur()==true){ligne=0;colonne=colonne+1;setPos(colonne,ligne)}
      else {ligne=ligne+1;(setPos(colonne,ligne))}}
  while (!(getX==getMondeLargeur-1 && getY==getMondeHauteur-1))
}