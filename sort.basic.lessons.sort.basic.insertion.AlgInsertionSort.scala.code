def insertionSort() {
  for (i<-1 to getNombreValeurs-1){
    var v=getValeur(i)
    for (k<-(i-1) to 0 by -1){
      if (!plusPetitQue(k,v) && getValeur(k)!=v){
        setValeur(k,v)
      }
    }
  } 
}