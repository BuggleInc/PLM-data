var cp:Int=0
do{
	for (cd <- 0 to 4) 
	{
	avance();
	if (estSurBiscuit)
	{
		cp = cp + 1
	}
	}
	
	if (cp>=3 && croisement)
	{
		gauche();
	}

	if (cp<=2 && croisement)
	{
	droite();
	}
	cp=0;
}while(!sortieTrouvee)

if (sortieTrouvee)
avance();