override def run() {
    while (!estSurBiscuit){
      do {
      var a:Int= random3();
      if (a==0){
        gauche
      }
        else {if (a==1) {
        droite;
        
            }
           }}
      while (estFaceMur)
      
      avance;
       
      
    }
  prendBiscuit
		}