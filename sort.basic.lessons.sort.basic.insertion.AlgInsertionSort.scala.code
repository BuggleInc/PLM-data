def insertionSort() {
  var lgr = getNombreValeurs()
  for (i<-1 to lgr-1){
    insert(i)
  }
}



def insert(i:Int){
  var v = getValeur(i)
  for (k<-i to 1 by -1){
    if(plusPetit(v,getValeur(k-1))){
      copie(k,k-1)
    }
  }
}
