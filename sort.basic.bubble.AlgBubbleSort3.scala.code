def bubbleSort3() {
  var fin:Boolean = false
  for (i <- getNombreValeurs - 2 to 0 by -1){
    fin = true
    for(j <- 0 to i) {
      if(plusPetit(j, j + 1)){
        echange(j, j + 1)
        fin = false
      }
    }
    if(fin)
    	return
  }
}