def pentaKoch(levels:Int, length:Double) {
  if(levels == 0){
  avance(length)
  return
    
  }
  else{
    pentaKoch(levels-1 , length*0.4)
    gauche(100)
    pentaKoch(levels-1 , length*0.4)
    droite(70)
    pentaKoch(levels-1 , length*0.4)
    droite(75)
    pentaKoch(levels-1 , length*0.4)
    droite(70)
    pentaKoch(levels-1 , length*0.4)
    gauche(100)
    pentaKoch(levels-1 , length*0.4)
	}
}