def concat(l1:List[Int],  l2:List[Int]): List[Int] = {
	if (l1 == Nil)
		return l2.head :: concat (Nil, l2.tail)
	else if (l1 == Nil && l2 == Nil)
		return Nil
	else return l1.head::concat(l1.tail, l2)
}
