def stringX(str:String):String = {
  val n = str.length
  if (n <= 2) {return str}
  var s = str.charAt(0).toString
  for (i <- 1 to n-2) {
    if (str.charAt(i) != 'x') {s = s + str.charAt(i).toString}
  }
  return s + str.charAt(n-1).toString}
