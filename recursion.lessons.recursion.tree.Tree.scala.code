def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
if(length==0){
	
}
else{
avance(length)
droite(angle)
current(steps)
tree(steps,shrink*length,angle,shrink)
gauche(2*angle)
tree(steps,shrink*length,shrink)
droite(angle)
recule(steps)

}
