def haveBaggle():Boolean = {
  
  var vuBiscuit = false
  for (c <- 1 to 6) {
    if (estSurBiscuit()) {vuBiscuit = true}
    avance()
  }
  
  for (c <- 1 to 6) { 
    recule()
  }
  return vuBiscuit
}

while(!haveBaggle()) {
  
  droite()
  avance()
  gauche()
  
}