def bubbleSort()  {
  var sorted:Boolean = false
  while(!sorted){
    sorted = true
  	for(i <- 0 to getValueCount() - 2){
      if(getValue(i - 1) > getValue(i)){
        swap(i - 1, i)
        sorted = false
      }
    }
  }
}