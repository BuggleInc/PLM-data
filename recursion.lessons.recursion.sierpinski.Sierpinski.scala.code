def sierpinski(level:Int, length:Double) {
	avance(length)
	droite(60)
	avance(length)
	droite(60)
	avance(length)
	droite(60)
	
	avance(length/2)
	droite(120)
	sierpinski(level-1, length/2)
	avance(length/2)
	droite(60)
	
	
}
