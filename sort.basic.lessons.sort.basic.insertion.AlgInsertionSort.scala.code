def insertionSort() {
  
  for(i<-1 to getNombreValeurs()-1)
  {
    var v=i;
    var valeur=getValeur(i);
    while(v>0 && getValeur(v-1)>valeur)
    {
      
      copie(v-1,v);
      v-=1;
    }
    setValeur(v,valeur)
    
  }
}