var diag = 0;
var cpt:Int=0;
override def run() {
 	for(diag<-0 to 6){
    parcours()
  }
  
  

  
  
}


def parcours(){
  var x:Int=0
  var y:Int=0
  x=getX()
  y=getY()
  ecritMessage(cpt)

  setPos(x,y+1)
  cpt+=1
  ecritMessage(cpt)
  setPos(x+1,y)
  cpt+=1
  ecritMessage(cpt)
  setPos(x,y+2)
  diag+=1
  
  
}
def changeDiag(){

}