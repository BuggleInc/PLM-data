while (! boutDuTunel) {
  setDirection(Direction.EAST) 
  while (! estSurBiscuit()) {
    avance()
  }
  prendBiscuit()
  retourne()
  
  
  
  
  
  
}
droite()
avance()
gauche()
avance()



def boutDuTunel () : Boolean = {
  return getDirection()==Direction.EAST && estFaceMur()
}