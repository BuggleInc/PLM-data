def insertionSort() {
  var i=0;
  for(i <- 0 to getNombreValeurs()-1){
    var j = 0;
    for(j <- i-1 to 0 by -1){
      if(plusPetit(i,j)){
        echange(j+1,j);
      }else{
        echange(j,i);
      }
    }
  }
}
