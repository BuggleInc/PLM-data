
def answerCell(isMorning:Boolean, isMom:Boolean ,  isAsleep:Boolean) {
  if (isAsleep) {
    
    return false} 
  
   
  else if   ((isMorning && isMom) || !isMorning) {
    return true }
    
  else {return false}
}
