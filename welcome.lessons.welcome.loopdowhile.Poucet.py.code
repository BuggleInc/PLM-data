
PremiereFois=True

while (not sortieTrouvee()):
    biscuit=0

    while (PremiereFois or not croisement()):
        if PremierFois:
            forward()
        
        PremiereFois=False
        
            
        if estSurBiscuit():
            biscuit=biscuit+1
        forward()
       
    if biscuit>=3:
        gauche()
        
    else:
        droite()

    forward()


forward()
