def insertionSort() {
  var valeur = 0;
  for (i <- 0 to getNombreValeurs-2){
   	valeur = getValeur(i);
    var place = i-1;
   	while (place>=1 && !(plusPetitQue(place,valeur))){
      	copie(place-1,place);
      	place=place-1;
    }
    setValeur(valeur,place)
  }
}