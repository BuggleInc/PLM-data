def insertionSort() {
	val lgr=getNombreValeurs()
  for(pivot<-1 to lgr-1){
    var vtemp=getValeur(pivot-1)
    var ptemp=pivot-1
    while(ptemp>0 && getValeur(ptemp)>=vtemp){copie(ptemp,ptemp+1);ptemp-=1}
    setValeur(ptemp,vtemp)
  }}