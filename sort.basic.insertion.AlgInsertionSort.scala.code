def insertionSort() {
  for (i <- 1 to getValueCount()-1) {
    var j = 1
    var v = getValue(i)
    while (!isSmallerThan(i-j,v) && j<=i) {
      copy (i-j,i-j+1)
      j=j+1
    }
    setValue(i-j+1,v)
  }
}