def bubbleSort()  {
	var trié=true
	while (trié==true) {
		trié=false
	for (i<-0 to getNombreValeurs()-2)
	{
		if (plusPetit(i:Int, i+1:Int)==false) 
		{	
			echange(i:Int, i+1:Int)
			trié=true
		}
		} 
}
}
