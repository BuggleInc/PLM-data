def haveBaggle():Boolean = {

var vuBiscuit:Boolean = false
var pas = 0

for (compteur <- 1 to 6)
	if (!estSurBiscuit) {
		avance()
		pas = pas + 1
	} else {
		vuBiscuit = true;
	}

for (compteur <- 1 to pas) {
	recule()
}

return vuBiscuit

}
