override def run() {
  while (!estSurBiscuit){
    setDirection(Direction.NORTH)
    if (estFaceMur){
      droite()
      if (estFaceMur){
        droite()
        avance()
        gauche()
      }
    }else{
      avance()
    }
  }
  prendBiscuit()
}