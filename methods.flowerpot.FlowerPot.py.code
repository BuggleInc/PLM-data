def flower(c):
  setBrushColor(c);brushDown()
  for i in range(3):
    forward()
  backward();left();forward();
  for i in range (2):
    backward()
  forward()
  setBrushColor(Color.yellow);brushUp()
    
def growFlowers():
  flower(Color.red)
  #setPos(x, y)