def indexOfMaxValue(nums:Array[Int]):Int = { 
  var ind: Int = 0
  var max: Int = nums(0)
  for (i <- 0 to nums.length-1) {
    if (nums(i)>max) {
      max=nums(i)
      ind=i
    }
  } 
  return ind
}