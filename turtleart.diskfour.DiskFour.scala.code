def draw (rayon:Double, color:Color) {
  setColor(color);
  penDown();
  for (i <- 0 to 89 by 1) {
    forward(rayon);
    backward(rayon);
    right(1);
  }
}

override def run() {
  draw(100.0, Color.black);
  draw(100.0, Color.red);
  draw(100.0, Color.orange);
  draw(100.0, Color.magenta);
}