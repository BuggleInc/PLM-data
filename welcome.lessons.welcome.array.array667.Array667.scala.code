def array667(nums:Array[Int]): Int = {
  var i = 0
  var compteur = 0
  while (compteur<nums.length)
  {
    if (nums(compteur) == 6 && nums.length > compteur+1 && (nums(compteur+1)== 6 || nums(compteur+1) == 7))
    {
      i +=1
    }
    compteur +=1
  }
  return i
}
