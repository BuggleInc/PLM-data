def stringYak(str:String):String = 
{
	var chaine = ""
	var i = 0

	while(i != str.length-3)
	{
		if(str.substring(i,3)=="yak")
		{
			i+=3
		}
		else 
		{
			chaine+=str(i); i+=1
		}
	}

	chaine+=str.substring(str.length-2)

	return chaine
}
