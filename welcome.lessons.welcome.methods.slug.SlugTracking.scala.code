def isFacingTrail():Boolean = {
		avance();
		if (getCouleurSol()==Color.green) {
			recule();
			return true;
		}
		else if (estFaceMur())  {
			recule();
			gauche();
			return false;
		}
		else {
			recule();
			gauche();
			return false
		}
}        
