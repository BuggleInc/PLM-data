def lastDigit(a:Int, b:Int, c:Int):Boolean = {
var resteA= a%10
var resteB=b%10
var resteC= c%10

	if (resteA == resteB )  return true
	if (resteA == resteC ) return true
	if (resteB == resteC) return true
	if (resteA==resteB && resteB==resteC) return true
	else return false
}