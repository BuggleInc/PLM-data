def haveBaggle():
    sawBaggle = False
        for i in range(6):
            forward()
            if isOverBaggle():
                sawBaggle = True
        backward()
        backward()
        backward()
        backward()
        backward()
        backward()
        return    

while not sawBaggle:
haveBaggle()