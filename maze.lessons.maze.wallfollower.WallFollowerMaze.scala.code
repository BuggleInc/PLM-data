override def run() {
  while(!estSurBiscuit){
    while(!estFaceMur() && !estSurBiscuit()){
      MainAuMur();
    }
    left();
  }
  
  prendBiscuit();
}

def MainAuMur(){
  left();
  if (estFaceMur()){
    right();
    avance();
  } else {
    avance();
  }
  
}