def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
	if (isBirthday) {
		speed = speed + 5
	}
	if (speed < 60) {
		return 0;
	}
	if ( (speed <= 80) && (61 <= speed)) {
		return 1;
	}
	if ((81 <= speed)) {
		return 2;
	}
	return 111;
}