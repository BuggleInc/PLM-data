def stringMatch(a:String, b:String):Int = 
{
	var compt = 0
	var j = 0
	
	for(i <- 0 to a.length-2)
	{
		if(a(i) == b(j) && a(i+1) == b(j+1))
			compt+=1

		if(j != b.length-2)
			j+=1
	}
	
	return compt
}
