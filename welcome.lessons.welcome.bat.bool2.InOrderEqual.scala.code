def inOrderEqual(a:Int, b:Int, c:Int, equalOk:Boolean):Boolean = {
	def ordre(a, b, c):Boulean {
		return true
	}
	if (!equalOK) {
		if (a==b || a==c || b==c) {return false}
	}
	return order(a, b, c)
}
