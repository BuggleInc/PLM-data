override def run() {
  def quart(){
    for(i<-1 to 360/18){
      forward(100);
      backward(100);
      right(1);
    }
  }
  for(i<- 1 to 18) {
    quart();
    setColor(Color.red);
    quart();
    setColor(Color.black);
    quart();
  }
  
}