override def initialize() 
{
	for(i <- 0 to getSol().length-2)
	{
		if(getSol()(i).y == getSol()(i+1).y)
			return new Segment( getSol()(i), getSol()(i+1) )
	}
	return new Segment(new Point(0,0), new Point(1,0))
}

override def step() 
{

}
