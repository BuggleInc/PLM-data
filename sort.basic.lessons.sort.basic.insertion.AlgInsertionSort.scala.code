def insertionSort() {
  for (i<-1 to getNombreValeurs()-1){
    var stop=false
    var j=i
     var memory=getValeur(i)
    while (stop==false && j>=1){
      if (plusPetitQue(j, memory)){
      	copie(j-1, j)
      }
      else{
        setValeur(j, memory)
        stop=true
      }
      j=j-1
    
    }
  }
}