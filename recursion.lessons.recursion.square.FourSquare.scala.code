val quart = 90;
val distance = 100;
override def run() {
	for(i<-4 until 0 by -1){
		square(quart*i);
	}
}
def square(angle:Double){
	setCap(angle);
	for(i<-4 until 0 by -1){
		avance(distance);
		droite(quart);
	}
}
