def selectionSort() {
  var r = 0
  var debut = 1
  var len = getNombreValeurs()-1
  for (i<-0 to len-1){
    r=i
  	  for (k<-i to len){
      	if (plusPetit(k,r)){
        	r=k
     }
    }
  echange(i,r)
  }
}