var diag = 0;
var cpt:Int=0;
override def run() {
  for (i <- 0 to 6) {
    for (j <- 0 to 6) {
        writeMessage(cpt)
        cpt+=1
      if (getY==0) {
        setPos(0,diag)
        diag+=1
      }
      else {
        setPos(getX+1,getY-1)
      }
    }
  }
}