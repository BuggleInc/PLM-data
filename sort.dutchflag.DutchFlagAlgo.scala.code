def solve() {
  for (i <- 0 to getSize()-1){
    getColor(i)
    var k = getSize()-1
    var j = i
    if (getColor(i)==RED){
      while (getColor(j)==RED){
        j=j+1
      }
      swap(i,j)
    }
    if (getColor(k)==BLUE){
      k=k+1
    }
    swap (i,k)
  }
}