override def run() {
	baisseBrosse()
  while(!finalPos())
  	sneakForward()
  leveBrosse()
}

def finalPos():Boolean =
{
  if(estFaceMur())
  {
    var oldD = getDirection()
    setDirection(Direction.NORTH)
    var r =estFaceMur() 
    setDirection(oldD)
    return r
  }
  
  return false
}

def sneakForward()
{
  if(!estFaceMur())
    avance()
  else
  {
    var oldD = getDirection()
    setDirection(Direction.NORTH)
    avance()
    setDirection(oldD)
    droite()
    droite()
  }
}