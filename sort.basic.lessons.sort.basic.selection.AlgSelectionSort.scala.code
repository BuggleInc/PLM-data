def selectionSort() {

	var min = 0

	for (i<-1 to getNombreValeurs()-2) {
		min = i
		//cherche le min
		for (j<-i+1 to getNombreValeurs()-1) {
			if (plusPetit(j, min)) {min = j}
		}
		echange(i,min)
	}
	
}
