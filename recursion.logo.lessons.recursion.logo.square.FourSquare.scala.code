override def run() {
  avance(100)
  droite(90)
  for (k <- 0 to 3) {
    carre()
  }
  def carre() {
    for (k <- 0 to 2) {
      avance(100)
      droite(90)
    }
    droite(90)
    avance(100)
    droite(90)
  }
}