override def run() {
  var etat = 0;
  while(getY() != 0){
   if(estFaceMur()){
     gauche();
     if(estFaceMur()){
       droite();droite();avance();gauche();
     } else {
       avance();
     }
   } else {
     avance();
   }
  }
  run2();
  prendBiscuit();
}

def run2(){
  while(!estSurBiscuit()){
    fun();
  }
}

def fun(){
  gauche();
  while(estFaceMur())
  {
    droite();
  }
  avance();
}