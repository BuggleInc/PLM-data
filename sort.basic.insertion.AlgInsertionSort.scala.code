def insertionSort() {
  
  for (i <- 1 to getNombreValeurs()-1) {
    
    var v = getValeur(i)
    var last = 0
    
    for (c <- 0 to i) {
      
      if (!plusPetitQue(i-c,v)) {
        
        setValeur(i,getValeur(i-c))
        last = c
        
      }
      
    }
    
    setValeur(i-last,v)
    
  }
  
}
