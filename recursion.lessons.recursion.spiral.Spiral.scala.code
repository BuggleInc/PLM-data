def spiral(steps:Int, angle:Int, length:Int, increment:Int){
	var etape:Int=steps
	var pas:Int=increment
	gauche(90)
	while (etape!=0) {
	avance(pas)
	gauche(angle)
	etape=etape-1
	pas=pas+increment
	}
	
	
}

