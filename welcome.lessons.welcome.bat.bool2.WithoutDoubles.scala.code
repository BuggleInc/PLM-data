def withoutDoubles(die1:Int, die2:Int, noDoubles:Boolean):Int = {
  if (die1 == die2) {
    if (noDoubles) {
      if (die1 == 6)
      	return 7
      return die1 + die2 + 1
    }
    return die1 + die2
  }
  return die1 + die2
}