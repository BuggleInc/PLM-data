def insertionSort() {
  for( i <- 1 to getNombreValeurs()-1){
		var v=getValeur(i);
		var a=1
		while(i-a>=0 && !plusPetitQue(i-a,v)){
      var temp=getValeur(i);
      copie(i-a, i-a+1)
      a=a+1;
    }
    setValeur(a-i+1,v);
  }
}