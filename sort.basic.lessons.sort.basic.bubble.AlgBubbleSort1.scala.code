def bubbleSort()  {
  var nb : Int = getNombreValeurs()
  var bool : Boolean = true
  while (bool==true) {
    bool = false
    for (i<-0 to nb-1) {
    	if plusPetit(i,i+1) {
        echange(i,i+1)
        bool = true
    	}
  	} 
  }
}