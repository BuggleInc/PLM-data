override def run() {
  var r = 0
  while(!estSurBiscuit())
  {
    r = random3()
    if(r==0 && !estFaceMur())
    	avance()
    else if(r==1)
    	droite
    else
    	gauche()
  }
  prendBiscuit()
}
