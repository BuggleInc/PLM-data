override def run() {
  val nb = 360 ;
  for (i <- 1 to nb) {
    forward(2);
    right(360/nb);
  }
  for (i <- 1 to nb/2) {
    forward(1);
    right(360/nb);
  }
  for (i <- 1 to nb/2) {
    forward(1);
    left(360/nb);
  }
}