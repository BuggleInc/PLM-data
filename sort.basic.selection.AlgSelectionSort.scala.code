def selectionSort() {
  
  for (a <- 0 to getNombreValeurs()) {
    
    var min = 0
    
    for (b <- a to getNombreValeurs()) {
      
      if (plusPetit(a+b,min)) {
        
        min = a+b
        
      }
     
     echange(min,a)
      
    }
    
  }
  
}