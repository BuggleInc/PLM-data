def step(rule:Array[Char], colors:Array[Color]) 
{

	var colorId = 0;

	for(i <- 0 to colors.length)
	{
		if(colors(i) == getCouleurSol())
		{
			colorId = i;
		}
	}

		setCouleurBrosse(colors(colorId));
		baisseBrosse();
		leveBrosse();
		
		if(rule(colorId-1) == "L")
		{
			gauche();
			avance();
		}
		else if (rule(colorId-1) == "R")
		{
			droite();
			avance();
		}
	

}
