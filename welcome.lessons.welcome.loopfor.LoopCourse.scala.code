var nbCote = 0;


	while (nbCote != 4) {
		for (compteur <- 1 to 8) {
			avance();
		}
	gauche;
	nbCote = nbCote + 1;	
	}
while (nbCote != 4) {
		for (compteur <- 1 to 8) {
			avance();
		}
	gauche;
	nbCote = nbCote + 1;	
	}
