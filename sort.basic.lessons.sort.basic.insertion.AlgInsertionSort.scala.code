def insertionSort() {
  var temp = 0
  for (i<- 1 to getNombreValeurs() -1) {
    temp = getValeur(i)
    var j=i
    while(getValeur(j-1)>temp&&j>0){
      setValeur(j,getValeur(j-1))
      j=j-1
    }
    setValeur(j,temp)
  }
}