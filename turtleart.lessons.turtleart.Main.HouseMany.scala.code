override def run() {
def maison {
	baisseCrayon(
	avance(30)
	droite(30)
	avance(30)
	droite(120)
	avance(30)
	droite(120)
	avance(30)
	recule(30)
	gauche(90)
	avance(30)
	droite(90)
	avance(30)
	droite(90)
	leveCrayon()
}
	def lignemaison {
		for ( k <- 1 to 4 ) {
	
			maison
			droite(90)
			leveCrayon()
			avance(50)
			gauche(90)
			baisseCrayon()
		}
	}

lignemaison
leveCrayon()
gauche(90)
avance(200)
droite(90)
avance(75)
maison
droite(90)
avance(150)

}
	
