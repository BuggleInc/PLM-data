override def run() {
def maison(){
	baisseCrayon
	for(i <- 1 to 4){
		avance(30)
		droite(90)
	}

	avance(30)
	for(i <- 1 to 3){
		droite(30)
		avance(30)
		droite(90)
	}
	recule(30)
}

	for(i <- 1 to 4){
		maison
		droite(90)
		avance(50)
		gauche(90)
	}
}
