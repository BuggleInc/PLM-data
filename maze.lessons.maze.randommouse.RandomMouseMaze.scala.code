override def run() {
  var direction=0
  while (!estSurBiscuit){
  direction = random3()
  if (direction==0 && !estFaceMur()){
    avance()
  } 
  else {retourne()
       retourne()}
  direction match {
    case 1 => droite()
    case 2 => gauche()
  }
}
}