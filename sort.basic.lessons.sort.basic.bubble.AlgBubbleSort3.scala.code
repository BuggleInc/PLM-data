def bubbleSort3() {
  val n = getNombreValeurs()
  for (i <- n-1 to 0 by -1 ){
    var swapped = false
  	for (k <- 0 to i-1) {
			if (!plusPetit(k,k+1)){
      	echange(k,k+1)
        swapped = true
   	  	}
      }
    if (!swapped) {
      return
    }
    }
  }
}