def move(stepCount: Int, stepForward: Boolean) {
  if (stepForward) {
    for (stepper <- 1 to stepCount) {
      forward()
    }
  } else {
    for (stepper <- 1 to stepCount) {
      backward()
    }
  }
}