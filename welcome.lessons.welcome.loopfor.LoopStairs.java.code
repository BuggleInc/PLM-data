public void run() { 
	boolean done = false;
	
	while (!isFacingWall())
		forward();

	while (!done) {
		left();
		forward();
		right();
		forward();
		if (!isFacingWall())
			done = true;
	}

	for (int i=0; i < 3; i++)
		forward();
}
