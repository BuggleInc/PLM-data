override def run() {
  avance(100);
  droite(90);
  avance(100);
  droite(90);
  avance(100);
  droite(90);
  avance(100)
  ;droite(90)
	avance(100)
	droite(30);for (i<-0 to 1) {avance(100);droite(120)}
avance(200);droite(120);avance(200);droite(30);avance(100);droite(90);avance(100);droite(90)
  
}