for i in range(7):
    for n in range(4):
        avance(4)
        gauche()
        forward(2)
        droite()
        forward(4)
        droite()
        forward(2)
        gauche()
        forward(4)
        gauche()
