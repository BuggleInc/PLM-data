def alarmClock(day:Int, vacation:Boolean): String = {
  if (vacation & !weekday) {
    return "off"
  }
  if (vacation & weekday) {
    return "10:00"
  }
  if ( !vacation & weekday) {
    return "7:00"
  }
  if((!vacation) & (!weekday)) {
    return "10:00"
  }
}