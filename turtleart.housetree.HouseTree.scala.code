override def run() {
  for (i <- 0 to 3) {
    house()
    allerVers(50,0)
  }
}


def house() {
  for (i <- 0 to 3) {
    avance(30)
    droite(90)
  }
  avance(30)
  droite(30)
  for (i <- 0 to 2) {
  	avance(30)
  	droite(120)
  }
  droite(150)
  avance(30)
  droite(180)
}