def indexOfMaxValue(nums:Array[Int]):Int = {
	var i:Int = 0;
	var indexOfMax:Int = - 1;
	var max:Int = - 9999;
	for(i <- 0 to nums.length-1) {
		if(nums(i) > max) {
			max = nums(i);
			indexOfMax = i;
		}
	}
	return indexOfMax;
}
