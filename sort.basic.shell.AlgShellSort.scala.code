def insertionSort(e : Int,n:Int) = {
  var v = 0
  var j = e
  for (i <- 1 to n-1) {
    v = getValeur (i)
    while ( i-j >=0 && !plusPetitQue((i-j),v)) {
        copie(i-j,i-j+e)
        j += e 
      } 
    setValeur(i-j+e,v)
    j=e
  }
}

def shellSort()  {
  var n = getNombreValeurs()
  var e = ((getNombreValeurs()).toDouble/2).toInt
  while (e>0) {
    insertionSort(e,n)
    e = (e.toDouble/2).toInt
  }
}



