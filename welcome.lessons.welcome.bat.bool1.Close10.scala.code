def close10(a:Int, b:Int): Int = {
	if ((Math.abs((a-10))) < (Math.abs((b-10)))) {
		return a;
	}
	if ((Math.abs((b-10))) < (Math.abs((a-10)))) {
		return b;
	}
	if ((Math.abs(b - 10)) == (Math.abs(a - 10))) {
		return 0;
}
}
