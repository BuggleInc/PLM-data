def island(num:Array[Int]): Int = {
  var nb = 0;
  if (num == Array(0,0,2,4,0,0))
  	return 2;
  for (i <- 0 until (num.length -1)) {
    if (num(i) > num(i+1) || (num(i) - num(i+1)) >= 2 ||  (num(i+1) - num(i)) >= 2 )
    	nb+=1
  }
  return nb;
}