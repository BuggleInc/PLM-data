def withoutDoubles(die1:Int, die2:Int, noDoubles:Boolean):Int = {
  var rep=die1+die2
  if(noDoubles){
    if(die1==die2){
      if(die1==6){
        return(rep-5)
     }else{return(rep+1)}
    }else{return(rep)}
  }else{return(rep)}
  
}