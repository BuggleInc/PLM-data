def isFacingTrail():Boolean ={
  if (!isFacingWall()) {
    	forward()
    	if (getGroundColor()==Color.green) {
    		backward()
    		return true
  		} else {
    		backward()
    		left()
  	}
    }else {
    		backward()
        left()
      }
    return true
  }
  