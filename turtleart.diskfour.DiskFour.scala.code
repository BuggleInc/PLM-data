override def run() {
  var Couleurs:Array[Color] = Array(Color.black, Color.red, Color.orange, Color.magenta)
  for (couleur <- Couleurs) {
    setCouleur(couleur)
    for (i <- 1 to 90) {
      avance(100)
      recule(100)
      droite(1)
    }
  }
}