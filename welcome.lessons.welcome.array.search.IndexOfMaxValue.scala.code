def indexOfMaxValue(nums:Array[Int]):Int = {
	var n = nums.length
  var max = -100
  var indice = 0
  for (pas <- 0 to (n-1))
  {
    if (nums(pas)>max)
    {
      max=nums(pas)
      indice=pas
    }
  }
  return indice
}