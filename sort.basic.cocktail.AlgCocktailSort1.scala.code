var condition = false
def cocktailSort() {
  do{
    condition = false
    for(i <- 0 to getNombreValeurs()-2){
      if(plusPetit(i+1,i)){
      	echange(i+1,i)
        condition = true}
    }
    for(j <- getNombreValeurs()-2 to 0){
      print("bla")
      if(plusPetit(j,j+1)){
      	echange(j,j+1)
      	condition = true}
    }
  }while(condition)
  return
}