def insertionSort() {
  var a= getNombreValeurs()
  for ( i<-1 to a-1){
    var v=getValeur(i);var j=i-1
    while(j>=0 && getValeur(j-1)>v){
      setValeur(j,getValeur(j-1))
      j-=1
    }
  setValeur(j,v)  
  }}
  