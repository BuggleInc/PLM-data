while sortieTrouvee() == False :
    premiereFois = True
    while premiereFois or not croisement() :
        premiereFois = False
        b=1
        for i in range (5):
            if estSurBiscuit() == True :
                b = b+1
            avance()
        if b <= 2 :
            droite()
        if b >= 3 :
            gauche()
    avance()