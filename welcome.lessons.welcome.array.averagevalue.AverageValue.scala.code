def averageValue(nums:Array[Int]): Int = {
	var taille: Int = nums.length;
	var n: Int = 0;
	var moy: Int = 0;
	for(n <- 1 to taille - 1){
	moy = moy + nums(n);
	}
	moy = moy / taille;
	return moy;

}
