def hunt() {
    while (isFacingTrail()) {
      baisseBrosse()
      avance()     
  	if (estSurBiscuit()) {
      prendBiscuit()
      
    }
  }
}


def isFacingTrail():Boolean = {
  if (estFaceMur()) {
    return false 
  }
  var estVert= false
  avance()
  if ( getCouleurSol() == Color.green ) {
    estVert = true
  } else estVert = false
  recule()
  return estVert 
}