def haveBaggle():Boolean={
  var x = 0
  var vuBiscuit : Boolean = false
  for(compteur <- 1 to 6){
    avance()
    if(estSurBiscuit()){
      x = compteur
      vuBiscuit = true
    }
    for(compteur <- 1 to x){
      recule()
    }
    return vuBiscuit
  } 
}
