override def run() {
  avance()
  faireLigne()
}

def faireV(c:Color){
  setCouleurBrosse(c:Color)
  baisseBrosse()
  leveBrosse()
  avance()
  gauche()
  avance()
  baisseBrosse()
  leveBrosse()
  recule()
  droite()
  avance()
  baisseBrosse()
  leveBrosse()
  avance()
  gauche()
  avance()

}

def faireMotif(){
  faireV(Color.YELLOW)
  faireV(Color.RED)
  faireV(Color.BLUE)
  faireV(Color.GREEN)
}
def faireLigne(){
  for (compteur<- 1 to 3){
    faireMotif()
    for(compteur1<-1 to 5){
      avance()
    }

  }
  recule()
}