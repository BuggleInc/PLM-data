def noTriples(nums:Array[Int]): Boolean = {
  var res=true
  val taille=nums.length
  
  for (i<-0 to (taille-3)){
    if (((nums(i))==(nums(i+1)))&&((nums(i+2))==(nums(i+1)))){
      res=false
    }
  }
  return res
  
}