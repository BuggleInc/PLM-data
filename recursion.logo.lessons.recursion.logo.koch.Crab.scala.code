def crab(levels:Int, length:Double) {
  if (levels != 0) {
    for (i <- 1 to levels) {gauche(45)}
    crab(levels-1, length/Math.sqrt(2))
    for (i <- 1 to levels) {droite(45)}
  } else {avance(length)}
}