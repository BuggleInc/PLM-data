def bubbleSort()  {
  var cycleTri : Boolean = false
  while (cycleTri==false) {
    cycleTri=true
    for (i<-0 to getNombreValeurs()-2) {
      if (!plusPetit(i,i+1)) {
        echange(i,i+1)
        cycleTri=false
      }
    }
  }
  
}