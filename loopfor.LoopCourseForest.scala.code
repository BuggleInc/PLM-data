for (compteur <- 1 to 7) {
  for (compteur <- 1 to 4) {
    for (compteur <- 1 to 4) {
      avance()
    }
    gauche()
    for (compteur <- 1 to 2) {
      avance()
    }
    droite()
    for (compteur <- 1 to 4) {
      avance()
    }
    droite()
    for (compteur <- 1 to 2) {
      avance()
    }
    gauche()
    for (compteur <- 1 to 4) {
      avance
    }
    gauche()
  }
}