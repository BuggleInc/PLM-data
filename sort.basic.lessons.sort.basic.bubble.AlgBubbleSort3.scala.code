def bubbleSort3()  {
	var flag = true;
	for (i <- (getNombreValeurs()-2) to 0 by -1) {
		flag = true;
		for (j <- 0 to i) {
			if (!plusPetit(j, j+1)) {
				echange(j,j+1);
				flag = false;
			}
		}
		if (flag) return;
	}
}
