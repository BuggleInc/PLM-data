override def run() {

	for(i<- 0 to 3) {
		avance(100);
		gauche(90);
	}
	for(i<- 0 to 3) {
		gauche(90);
		avance(100);
		
	}
	for(i<- 0 to 3) {
		avance(100);
		droite(90);
	}
	for(i<- 0 to 3) {
		droite(90);
		avance(100);
		
	}
}
