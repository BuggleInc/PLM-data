def inOrderEqual(a:Int, b:Int, c:Int, equalOk:Boolean):Boolean = {
  (a,b,c,equalOk) match {
    case (a,b,c,equalOk) if equalOk == false && a<b && b<c => true
    case (a,b,c,equalOk) if equalOk == true && <= b && b<= c => true
    case (_,_,_,_) => false
  }