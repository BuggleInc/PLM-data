def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
if (steps !=0) {
def branche() = { 
	
	droite(angle);
	avance(length*shrink);
	recule(length*shrink);
	gauche(angle*2);
	avance(length*shrink);
	recule(length*shrink);
}
current(steps);
avance(length);
branche();
avance(length*shrink);
branche();


}
}
