override def run() {
 baisseBrosse()
 while (arrive() == false) 
 {
 	if (!estFaceMur())
 	  {avance()}
 	 		
 }
}


def arrive() {
	if (estFaceMur())
		{ if (getDirection() == Direction.OUEST)
			  {gauche()
			    if (estFaceMur())
			     { return true
			   droite()}}
			else { if (getDirection == Direction.EST)
					 {droite()
					 estFaceMur()
					 gauche}}
		}
	else return false
}
