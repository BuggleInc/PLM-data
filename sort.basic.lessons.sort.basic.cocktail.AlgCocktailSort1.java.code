public void cocktailSort() {
	boolean swapped;
	do {
		swapped = false;
		for(int i = 0; i < getValueCount() - 1; i++)
			hasToSwap(i);
		for(int i = getValueCount() - 1; i >= 0 ; i--)
			
	} while(swapped);
}	

boolean hasToSwap(int i){
	if(isSmaller(i, i+1)){
		swap(i, i+1);
		return true;
	} else 
		return false;
}
