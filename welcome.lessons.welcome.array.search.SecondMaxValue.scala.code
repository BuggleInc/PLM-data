def max2Value(nums:Array[Int]): Int = {
  var max=Integer.MIN_VALUE
  var max2nd=Integer.MIN_VALUE
  for (i <- 0 to nums.length-1){
    if (nums(i)>=max){
      max2nd=max
      max=nums(i)
    }
    if ((nums(i)<max)&&(nums(i)>max2nd)){
      max2nd=nums(i)
    }
  }
  return max2nd
}
