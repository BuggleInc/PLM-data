def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  current(steps)
  if (steps==0)	{
  }
  else	{ 
    avance(length)
    droite(angle)
    tree(steps-1,length*shrink,angle,shrink)
    current(steps)
    
    gauche(2*angle)
    
    tree(steps-1,length*shrink,angle,shrink)
    current(steps)
    droite(angle)
    recule(length);
  }
}