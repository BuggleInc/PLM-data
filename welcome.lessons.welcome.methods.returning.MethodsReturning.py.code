estDevantLibre() {
    if (estFaceMur() == ) {
        return ;
        /* interdit d'écrire du code ici */
    } else {
        return ;
        /* pareil ici */
    }
    /* même ici, oubliez */
}[/!]def estDevantLibre():
    if estFaceMur() == True:
        return False
        # interdit d'écrire du code ici
    else
        return True
        # pareil ici
# même ici, oubliez