def extrema(nums:Array[Int]): Int = {
  var maximum=nums(0)
  var minimum=nums(0)
  var extrema=0
  for (i<-0 to nums.length-1){
    if (maximum < nums(i)){
      maximum=nums(i)
    }
    if (minimum>nums(i)){
      minimum=nums(i)
    }
  }
  extrema=maximum-minimum
  return extrema
}
