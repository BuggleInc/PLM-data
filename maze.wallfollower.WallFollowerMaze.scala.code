def keepHandOnSideWall(){
  if (!estFaceMur()){
    gauche()
    if (estFaceMur()){
      droite()
      avance()
    }else{avance()}
  }else{
    gauche()
    gauche()
    if (estFaceMur()){
      droite()
      avance()
    } else {avance()}
  }
}

override def run() {
  while (!estSurBiscuit()){
    keepHandOnSideWall
  }
  prendBiscuit()
}