def maxValue(nums:Array[Int]): Int = {
  var max = nums(0)
  var maxIndice = 0
  for (i <- 1 to nums.length - 1) {
    if (nums(i) > max) {
    	max = nums(i)
      maxIndice = i
  }
  }
  return maxIndice
  }