def cocktailSort() {
	var x:Boolean=true
	do {
		for(i<-0 to getMondeValeurs()-2) {
			x=false
			if (plusPetit(i,i+1)){
			echange(i,i+1)}
			x=true}
		for(i<-getMondeValeurs()-2 to 0 by -1) {
			x=false
			if (plusPetit(i,i+1)){
			echange(i,i+1)}
			x=true} 
		}
		while x==false
	}

