def hunt(c:Color) {
  while (!estSurBiscuit()) {
    if (isFacingTrail(c)) {avance();baisseBrosse();leveBrosse()}
      else {gauche()}
  }
  prendBiscuit()
}

def isFacingTrail(c:Color):Boolean = {
  var bool1=false
  if (estFaceMur) {return false}
  else {avance();
  if (getCouleurSol()==c) {bool1=true}
    else {bool1=false}
  recule()
  }
  return bool1
}
