        def goAndGet() {
        	var nbPas = 0;
        	while(!estSurBiscuit()) {
        		avance();
        		nbPas += 1;
        	}
        	if(estSurBiscuit()){
        		prendBiscuit();
        		for(i<- 0 to nbPas -1) 
        			recule();
        	}
        }

        for (i <- 1 to 7) {
            goAndGet();
            right();
            forward();
            left();
        }
