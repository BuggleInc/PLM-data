def extrema(nums:Array[Int]): Int = {
  var max:Int=nums(0)
  var min:Int=nums(0)
  for (i<-1 to nums.length-1){
    var x=nums(i)
    if (x>max){
      max=x
    }
    else if (x<min){
      min=x
    }
  }
  return max-min
}