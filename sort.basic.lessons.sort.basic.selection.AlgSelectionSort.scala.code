def selectionSort() {
  
  
  var test=false;
  var j=0;
  for(j<-0 to getNombreValeurs()-2)
  {
    var min=getValeur(j)
    var ind=j;
    
    for(i<-j to getNombreValeurs()-2)
    {
      if(min>getValeur(i))
      {
        min=getValeur(i)
        ind=i;
      }
    }
    echange(j,ind)
  }
}