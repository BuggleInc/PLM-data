var int i= 0
def bubbleSort()  {
	var faitechange:Boolean=true
while(faitechange==true)
{
faitechange=false
  for(i<-0 to getNombrevaleurs())
   if plusPetit(i+1,i)
    {echange(i+1,i)
     faitechange=true}}
}
