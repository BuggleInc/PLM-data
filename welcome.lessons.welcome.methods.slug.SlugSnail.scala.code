def hunt(c:Color) {
    while (!estSurBiscuit()) {
       if (isFacingTrail(c)) {
         baisseBrosse() ; avance() ; leveBrosse()
       } else droite()
}
  prendBiscuit }


def isFacingTrail(c:Color):Boolean = {
  if (estFaceMur()) {
    return false 
  }
  var estVert= false
  avance()
  if ( getCouleurSol() == c ) {
    estVert = true
  } else estVert = false
  recule()
  return estVert 
}
