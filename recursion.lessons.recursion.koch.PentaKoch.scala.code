def pentaKoch(levels:Int, length:Double) {
	if(levels==0){
		avance(length)
	}
	else{
		avance(length)
		gauche(108.0)
		avance(length)
		droite(72.0)
		avance(length)
		droite(72.0)
		avance(length)
		droite(72.0)
		avance(length)
		gauche(108.0)
		avance(length)
	}
}
