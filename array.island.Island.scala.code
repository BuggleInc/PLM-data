def island(num:Array[Int]): Int = {
  var n:Int = 0
  var nump:Int = 0
  for (i <- 0 to num.length){
    if (num(i) < nump){
      n = n+1
      nump = num(i)
    } else {
      nump = num(i)
    }
  }
  return n
}