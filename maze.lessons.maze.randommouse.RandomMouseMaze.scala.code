override def run() {
    // Your code here 
  var choix=0
  while (!estSurBiscuit()) {
    choix = random3()
    if (choix == 0 && !estFaceMur()) avance()
    else if (choix ==1) droite()
    else if (choix==2) gauche()
    
  }
  prendBiscuit()
}
