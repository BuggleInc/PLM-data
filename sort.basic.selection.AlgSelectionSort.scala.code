def selectionSort() {
  for (i <- 0 to getNombreValeurs() -1){
    var indMin:Int = i
    for (j <- i to getNombreValeurs() -1){
      if (getValeur(j)<indMin){
        indMin = j
      }
    }
    echange(i,indMin)
  }
}