override def run() {
  
  
  
  while (!estSurBiscuit()) {
   
    
    keepHandOnSideWall()
  }
  prendBiscuit() 
  
}

def keepHandOnSideWall() {
  
  var a=0
  if (estFaceMur()) {
    gauche()
    if(!estFaceMur) {
      avance()
      droite()
    }
    else {
      gauche()
      if (!estFaceMur) {
        avance()
        a+=1
        droite()
      }
      else {
        gauche()
        if (!estFaceMur) {
          avance()
          droite()
        }
      }
    }
  }
  else {
    avance()
    a-=1
    droite()
  }
  if (a==0) {
    setDirection(Direction.NORTH)
    while (!estFaceMur) {
      avance() 
    }
  }
}

