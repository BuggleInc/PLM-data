override def run() {
  for (i <- 1 to 48) {
    while (!fonctionTest()) {
      baisseBrosse();
      avance();
    }
    if (getDirection() == Direction.EAST) {
      gauche();
      avance();
      gauche();
    }
    if (getDirection() == Direction.WEST) {
      droite();
      avance();
      droite();
    }
  }
}

def fonctionTest() : Boolean = {
  if (estFaceMur()) {
    gauche();
    if (estFaceMur()) {
      droite();
    }
    return true }

  return false
}
