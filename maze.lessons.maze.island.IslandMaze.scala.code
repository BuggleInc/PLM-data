override def run() {
  while (!estSurBiscuit()) {
    while (!estFaceMur()) {
      setDirection(Direction.NORTH)
      avance()
    }
    keepHandOnSideWall()
  }
}

def keepHandOnSideWall() = {
    while (estFaceMur()) {
      droite()
    }
  	avance()
  	gauche()
  }
}
