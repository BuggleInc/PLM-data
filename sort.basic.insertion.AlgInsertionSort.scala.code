def insertionSort() {
  for (i <- 1 to getNombreValeurs() -1){
    
    var indice : Int = i-1
    while(indice >=0 && plusPetit(indice+1,indice)){
			echange(indice, indice+1)
      indice = indice - 1
    }
  echange(indice,indice+1)  
    
  }
}