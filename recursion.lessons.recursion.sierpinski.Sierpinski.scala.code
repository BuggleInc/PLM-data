def sierpinski(level:Int, length:Double) {
	sierpinski(level-1, length/2)
		avance(length)
		droite(120)
		sierpinski(level-1, length/2)
		avance(length)
		droite(120)
		sierpinski(level-1, length/2)
		avance(length)
		droite(120)
		}







