def posNeg(a:Int, b:Int, negative:Boolean):Boolean = {
  def isNeg(x:Int): Boolean = x < 0;
  
  return (isNeg(a) && !isNeg(b) || isNeg(b) && !isNeg(a)) && !negative ||
  	negative && isNeg(a) && isNeg(b);
}