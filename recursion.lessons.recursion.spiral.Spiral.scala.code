def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {
	baisseCrayon()
	if ( steps == 0){
		return 
	}
	else{
		avance(lenght:Double)
		gauche(angle:Double)
		return  spiral( steps-1, angle, lenght + increment, increment)
		
	}
}
