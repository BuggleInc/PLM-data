def answerCell(isMorning:Boolean, isMom:Boolean, isAsleep:Boolean):Boolean = {
	if (isMorning() && !(isMom()) && !(isAsSleep())) {
		return false;
	}
	else if (isAsSleep()) {
		return false;
	}
	else true;
}
