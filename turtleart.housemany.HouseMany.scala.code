override def run() {
  for(i <- 1 to 5) {
    makeHouse(20)
    penUp()
  }
}

def makeHouse(length:Int) {
  forward(length)
  
  right(30)
  for(i <- 1 to 3) {
    forward(length)
  	right(120)
  }
  
  right(60)
  
  for(i <- 1 to 3) {
    forward(length)
    right(90)
  }
}