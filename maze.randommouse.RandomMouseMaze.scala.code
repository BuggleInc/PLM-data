override def run() {
  var r = 0;
  while(!estSurBiscuit()){
    r = random3();
    r match {
      case 0 => if(!estFaceMur()){avance()};
      case 1 => droite();
      case 2 => gauche();
    }
  }
  prendBiscuit();
}