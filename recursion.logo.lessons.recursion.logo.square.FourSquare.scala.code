override def run() {
  carre()
  gauche(180)
  avance(100)
  droite(180)
  carre()
  avance(100)
  gauche(90)
  carre()
  gauche(180)
  avance(100)
  droite(180)
  carre()
}

def carre(){
  for (j<-0 to 3){
    	avance ( 100)
    	droite ( 90)
  	}
}
