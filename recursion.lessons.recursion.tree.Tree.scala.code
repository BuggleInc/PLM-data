def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
var s= steps
var l=length
var x=getX()
var y=getY()

	if(s!=3){
		current(s)
		avance(l)
		droite(angle)
		tree(s-1,l*shrink,angle,shrink)
	}
}
