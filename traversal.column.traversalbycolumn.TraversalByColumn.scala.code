override def run() {
  var x:Int=0;
  var count:Int=0;
  
  def colonne(y){
    var y:Int()=0;
    while(y!=getWorldHeight()){
      setPos(x,y);
      writeMessage(count);
      y+=1;
      count+=1;
    }
  }
  colonne(y);
}