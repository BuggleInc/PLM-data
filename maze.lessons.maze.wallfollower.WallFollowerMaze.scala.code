def keepHandOnSideWall = {if (!estFaceMur) {avance();gauche()} else {while(estFaceMur()) {droite};avance()};gauche() }

override def run() { 
  
  
  while (!estSurBiscuit()) {keepHandOnSideWall}; prendBiscuit()
}