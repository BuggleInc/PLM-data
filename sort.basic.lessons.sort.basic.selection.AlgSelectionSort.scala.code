def selectionSort() {
	for (i <- 0 to getNombreValeurs()-1) { 
		var temp=""
		val minpos=i
		for (j <- i to getNombreValeurs()-1) {
			if (j<minpos) { 
				copie(j,minpos)
			}
		}
		copie(i,temp)
		setValeur(minpos,i)
		setValeur(temp,minpos)
	}
}
