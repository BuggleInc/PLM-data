override def run() {
  while (!fonctionTest()) {
    baisseBrosse();
    avance();
  }
}

def fonctionTest() : Boolean = {
  if (estFaceMur()) {
    if (getDirection() == Direction.EST)
    { retourne(); }
     if (estFaceMur()) {
       gauche();
       avance();
       gauche();
     }

    
    if (getDirection() == Direction.OUEST)
    { retourne(); }
     if (estFaceMur()) {
       droite();
       avance();
       droite();
      }

    return true
  }
  else {return false}
}