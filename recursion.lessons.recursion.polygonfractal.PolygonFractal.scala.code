def polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double) {
	if(levels == 1) {
		var angle = 360/sides;
		for(i<-1 to sides) {
			avance(length);
			droite(angle);
		}
	}
	else {
		
	}
}
