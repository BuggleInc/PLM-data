override def run() {
	for (c<-1 to 4) { //Début ligne maisons
		avance(30)
		droite(30)
		for(i<-1 to 3) {
			avance(30)
			droite(120)
		}
		droite(60)
		for(i<-1 to 3) {
			avance(30)
			droite(90)
		}
		leveCrayon()
		droite(90)
		avance(50)
		baisseCrayon()
	} // Fin ligne maisons
	recule(200)
	gauche(90)
	avance(75)
	baisseCrayon()
	for(i<-1 to 3) {
			avance(30)
			droite(120)
		}
		droite(60)
		for(i<-1 to 3) {
			avance(30)
			droite(90)
		}
	leveCrayon()
		
}
