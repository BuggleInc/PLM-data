def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
baisseCrayon()
current (steps)
if (steps ==0) {recule (length/shrink); gauche(2*angle); tree(steps+1, length/shrink, angle, shrink)}
else {avance(length)
	droite(angle)
	tree(steps-1, length*shrink, angle, shrink)
	









}

}
