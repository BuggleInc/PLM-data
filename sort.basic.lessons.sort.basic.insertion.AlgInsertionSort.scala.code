def insertionSort() {
  var temp = 0
  
  for(i<- 1 to getValueCount()-1)
  {
  	temp = getValue(i)
    for(j<-i-1 to 0 by -1)
    {
      if(isSmallerThan(temp,getValue(j)))
      {
        setValue(j,j+1)
      }
    }
    
  }
}