def insertionSort() 
{
  var v = 0
  for(i <- 1 to getNombreValeurs() -2)
  {
    copie(getValeur(i),v)
    for(j <- i to 1 by -1)
    {
      if(plusPetit(i-j+1,i-j))
      {
        v = getValeur(i-j)
      }
    }
    setValeur(i,v)
  }
}