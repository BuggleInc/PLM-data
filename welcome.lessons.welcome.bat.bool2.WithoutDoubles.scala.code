def withoutDoubles(die1:Int, die2:Int, noDoubles:Boolean):Int = {
  if (noDoubles==false) {
    return (die1+die2)
  } else if ((die1==die2)) { die1 = ((die1 + 1)%6)
                             die2 = ((die1 + 1)%6)
    return (die1+die2)
  } else {
    return (die1+die2)
  }
}