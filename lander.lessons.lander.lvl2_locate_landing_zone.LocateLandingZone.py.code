def getLandingZone():
    lastPoint = getGround()[0]
    for point in getGround():
        if isSelected():
            print "point"+str(point)
            print "lastPoint"+str(lastPoint)
        if (point != lastPoint):
            if lastPoint.y == point:
                return (lastPoint.x , point.x)
        lastPoint = point

