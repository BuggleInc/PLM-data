override def run() {
  //while(!isOverBaggle()){
  for(i <- 1 to 100000){
      keepHandOnSideWall()
	}
}

def keepHandOnSideWall(){
  
  if(isFacingWall()){
    
    //Si mur devant et ? gauche
    right()
  }
    
    if(!isFacingWall()){
      
      if(!isFacingWall()){
        forward()
        right()
      }
      
      
      else{
        left()
        if(!isFacingWall()){
          forward()
        }
      }
      
        
    	}
  }
