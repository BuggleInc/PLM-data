override def run() {
  
  def toucheMur():Boolean={
    gauche()
    if (estFaceMur){
      droite()
      return true
    }
    return false
  }
  
  def keepHandOnSideWall(){
    while (!estSurBiscuit){
      if ((toucheMur())&&(!estFaceMur)){
        droite()
      } else if (toucheMur()&&(estFaceMur())){
        gauche()
        avance()
      }
    }
    prendBiscuit()
	}
  droite()
  keepHandOnSideWall()
}