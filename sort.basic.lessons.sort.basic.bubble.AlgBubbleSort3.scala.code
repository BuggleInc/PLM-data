def bubbleSort3() {
	var swapped:Boolean=true;
	for(i<- getValueCount()-2 to 0 by -1){
		swapped=false;
		for(j<-0 to i)
			if( isSmaller(j+1,j)){
				swap(j,j+1);
				swapped=true;
			}	
	}
	if (swapped==false)
		return; 			
}
