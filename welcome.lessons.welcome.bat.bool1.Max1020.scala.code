def max1020(a:Int, b:Int):Int = {
  var aIntervalle = (10 <= a) && a<= 20
  var bIntervalle = 10 <=  b && b<= 20
  if (aIntervalle && bIntervalle ) {return Math.max(a,b)}
  if (aIntervalle) {return a}
  if (bIntervalle) {return b}
  return 0
}