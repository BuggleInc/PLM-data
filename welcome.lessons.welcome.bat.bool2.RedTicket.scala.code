def redTicket(a:Int, b:Int, c:Int):Int = {
  (a,b,c) match{
    case (a,b,c) if a==2 && b==2 && c==2 => 10
    case (a,b,c) if a==b && b==c => 5
    case (a,b,c) if b!=a && c!=a => 1
    case (_,_,_) => 0
  }
}