var baggels: Int = 0

while (getCouleurSol()=="white") {
  for (compteur <- 1 to 5) {
    avance()
    if (estSurBiscuit()) {
      baggels += 1
    }
  }
  if (baggels>=3) {gauche()}
  if (baggels<=2) {droite()}
  
}