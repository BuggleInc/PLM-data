import math
def posNeg(a, b, negative):
  if negative==False:
    if (a=math.fabs(a) and b!=math.fabs(b)) or (a=!math.fabs(a) and b=math.fabs(b)):
      return(True)
    return(False)
  if (a!=math.fabs(a) and b!=math.fabs(b)) or (a=math.fabs(a) and b=math.fabs(b)):
    return(True)
  return(False)