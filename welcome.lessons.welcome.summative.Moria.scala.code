
while (true){
  
  getDirection() match{
    case Direction.EAST =>
    	if (isOverBaggle()){
        pickupBaggle()
        setDirection(Direction.WEST)
      }else{
        
      }
    
    case Direction.WEST =>
    	if (isOverBaggle()){
        pickupBaggle()
				setDirection(Direction.EAST)
      }else{
        forward()
      }
  }
}