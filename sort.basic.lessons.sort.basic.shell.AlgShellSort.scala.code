def shellSort()  {

	var ecart = (getNombreValeurs().toDouble / 2).toInt
	var v = 0
	var cpt = 0

	while (ecart>0) {
		//algo du tri par insertion
		for (i<-ecart-1 to getNombreValeurs()-1) {
		v=getValeur(i)
		cpt=i-ecart
			while (cpt>=0 && !plusPetitQue(cpt, v)) {
			copie(cpt, cpt+ecart)
			cpt=cpt-ecart
			}
		setValeur(cpt+ecart, v)
		//fin
		}
		//ecart divisé par 2
		ecart = (ecart.toDouble / 2).toInt
	}

}
