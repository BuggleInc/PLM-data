override def run() {
  faireL(Color.RED, Color.BLUE)
}

def faireL(c: Color, d: Color) {
  avance()
  setCouleurBrosse(c)
  baisseBrosse()
  leveBrosse()
  avance()
  setCouleurBrosse(d)
  baisseBrosse()
  leveBrosse()
  avance()
}