def solve() {
  var i = 0;
  while(!estTriee()){
    var c = i;
    for(j<- c+1 to getTaillePile() -1 ){
      if (getRayonCrepe(j)<getRayonCrepe(c)){
        retourne(j);
        retourne(c);
        c += 1;
      }
    }
    i += 1;
  }
}