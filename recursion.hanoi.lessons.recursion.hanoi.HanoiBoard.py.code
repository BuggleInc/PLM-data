def solve(src,dst,other, height=-1):
    if height==-1: # initial case
        height=getSlotSize(src)
    
        
            
        
        
        
        
        
    
    # Your code here
    deplace(src,dst)
    if getRayon(src)<getRayon(dst) or getTaillePiquet(dst)==0:
        deplace(src,dst)
    deplace(src,dst)
