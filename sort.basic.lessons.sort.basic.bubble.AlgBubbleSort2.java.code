public void bubbleSort2() {
	boolean sorted = false;
	 for(int i = getValueCount() - 1; i > 0; i--){ 
	 	sorted = false;
	 	for(int j = 0; j < i ; j++)
	 		if(isSmaller(j + 1,j)){
	 			swap(j,j+1);
	 			sorted = true;
	 		}
	 	if(!sorted){
	 		break;
	 	}
	 }
}
