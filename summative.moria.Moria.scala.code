while(!isFacingWall()) {
  left()
  left()

  while(!isOverBaggle() && !isFacingWall()) {
		forward()
  }

  if(isOverBaggle()) {
    pickupBaggle()
    left()
    left()
  }

  while(!isOverBaggle() && !isFacingWall()) {
		forward()
  }

  if(isCarryingBaggle()) {
    backward()
    dropBaggle()
    backward()
  }
}

right()
forward()
left()
forward()