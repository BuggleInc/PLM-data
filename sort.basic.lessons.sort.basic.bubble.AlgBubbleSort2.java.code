public void bubbleSort2()  {
	int count = 0;
	boolean swapped;
	for (int i = getValueCount() - 2; i >= 0; i--){
		for (int j = 0; j <= i; j++) {
			if (isSmaller(i + 1, i)) {
				swap(i, i + 1);
			}
		}
	}
}