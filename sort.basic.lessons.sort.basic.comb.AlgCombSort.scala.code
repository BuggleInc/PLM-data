def combSort() {
	var ecart = getNombreValeurs() ; 
	var i = 0;
	while (ecart > 1 || changed ){
		if (ecart > 1 ) {
			 ecart = ecart / 1.3;
		}
		while (i+ecart < getNombreValeurs()){
			if(plusPetit(i+ecart,i)){
					echange(i,i+1);
					changed1 = true ;
					i +=1;
				}
		}
	}
}
