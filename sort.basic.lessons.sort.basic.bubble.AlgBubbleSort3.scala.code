def bubbleSort3() {
	var lim = getNombreValeurs()-2;
	do{
		var ech = false;
		for(i<-0 to lim) {
			if(plusPetit(i+1, i)) {
				echange(i, i+1);
				ech = true;
			}
		}
		if(ech == false) {
			return;
		}
		lim = lim-1;
	}while(lim >= 0)
}
