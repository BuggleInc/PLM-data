public void run() {
	int count = 0;
	int initX = 0;
	int initY = 0;
	while (initY < getWorldWidth()) {
		int x = 0;
		int y = initY;
		while (x <= getWorldWidth() && y >= 0){
			setPos(x,y);
			x++;
			y--;
			writeMessage(count);
			count++;
		}
		initY++;
	}
}