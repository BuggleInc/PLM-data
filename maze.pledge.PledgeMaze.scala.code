override def run() {
  var virage:Double = 0
 	while (!estFaceMur())
  	avance()
  while (!estSurBiscuit())
  	virage = keepHandOnSideWall(virage)
  prendBiscuit()
}

def keepHandOnSideWall(virage:Double):Double = {
  droite()
  var v:Double = 0
  if (estFaceMur()) {
  	gauche()
    if (estFaceMur()) {
    	gauche()
    	v=virage+1
    }
    else
    	avance()
  }
  else {
  	avance()
  	v=virage-1
  }
  return v
}