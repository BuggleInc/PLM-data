def cocktailSort() {
  var n=1
  var m=1
  while (n!=0 || m!=0) {
   n=0
   m=0
  for (i <- 0 to getValueCount() - 2) {
    if (isSmaller(i+1,i)) {swap(i,i+1); n=1}
  }
  for (i <- getValueCount()-2 to 1 by -1) {
    if (isSmaller(i,i-1)) {swap(i,i-1); m=1}
  }}
}