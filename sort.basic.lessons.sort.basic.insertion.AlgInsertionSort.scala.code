def insertionSort() {
  var valeur = 0
  for (i <- 0 to getNombreValeurs-2){
   	valeur = getValeur(i);
    var place = i;
   	while (place>=0 && !plusPetitQue(place,valeur)){
      	copie(place-1,place);
      	place=place-1;
    }
    setValeur(valeur,place)
  }
}