def run () {
  var compt: Int = 0
  while (!estSurBiscuit || !estFaceMur) {
    forward();
    if (estSurBiscuit) {
      prendBiscuit(); 
      while (!estSurBiscuit) {
        backward()
      }
      poseBiscuit();
    }
  }
}