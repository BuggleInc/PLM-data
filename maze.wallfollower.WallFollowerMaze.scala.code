override def run() {
  def keepLeftHand() {
    
      left()
      if ( ! isFacingWall()) {
        forward() 
        } else {
    					right()
    					if (! isFacingWall()) {
                forward
              } else {
                	right()
                	if (! isFacingWall()) {
                  	forward
                	} else {
                  right()
                }
              }
            }
  
			}
  while (! isOverBaggle()) {
    keepLeftHand()
  }
  pickupBaggle()
}
  