def bubbleSort()  {
  var trie = false
  while(!trie){
    trie = true
    for (i <- 0 to getNombreValeurs()-2 ){
      if (getValeur(i) > getValeur(i+1)){
        echange(i,i+1)
        trie = false
      }
    }
  }
}
  