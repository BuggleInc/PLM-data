def hunt(c:Color) {
    while (!estSurBiscuit) {
    if (isFacingTrail() == true) {
      brushDown(); forward(); brushUp();
    } else {
      left();
    }
  }
  prendBiscuit();
  
  
     // Write your code here
 }

 // here comes your isFacingTrail method

def isFacingTrail():Boolean = {
  	var vuVert: Boolean = false
  	if (estFaceMur()==false) {
      forward();
      if (getCouleurSol()==Color.pink) {
        vuVert=true
      } 
      backward();
    } 
  	return vuVert
}