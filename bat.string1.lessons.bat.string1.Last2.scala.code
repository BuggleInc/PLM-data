def last2(str:String):Int = 
{
	var chaine : String = str(str.length-2)+str(str.length-1)+""
	var compt = 0

	for(i <- 0 to str.length-3)
	{
		if(chaine(0) == str(i) && chaine(1) == str(i+1))
			compt+=1
	}

	return compt
}
