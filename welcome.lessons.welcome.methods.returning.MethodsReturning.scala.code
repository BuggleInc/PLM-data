def haveBaggle():Boolean = {
  var seenBaggle:Boolean = false
  while(!seenBaggle){
    for(i <- 0 to 6){
      if(isOverBaggle())
        seenBaggle = true
      forward()
    }
    if(!seenBaggle){
      right(); forward(); left()
    }
  }
  return seenBaggle
}