var compteur:Int=0
while(!sortieTrouvee())
{
	compteur=0
	if(estSurBiscuit())
			compteur=compteur+1
	do
	{
		avance()
		if(estSurBiscuit())
			compteur=compteur+1
	} while(!croisement())
	if(compteur > 2)
		droite()
	else
		gauche()
}
