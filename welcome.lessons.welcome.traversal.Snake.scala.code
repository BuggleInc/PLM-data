override def run() {
 baisseBrosse()
 while (arrive() != true) 
 {
 	if (!estFaceMur())
 	  {avance()}
 	 else {if (getDirection == Direction.EST)
 	 		{ gauche()
 	 			if (!estFaceMur())
 	 			  {avance();gauche()}
 	 		}
 	 	   else {droite; if (!estFaceMur())
 	 	   				{avance();droite()}
 	 	   }}
 }
}


def arrive() {
	if (estFaceMur())
		{ if (getDirection() == Direction.EST)
			  {gauche()
			    if (estFaceMur())
			     { return true
			 	}}
			else { if (getDirection == Direction.OUEST)
					 {droite()
					 estFaceMur()
					 }}
		}
	else return false
}

