def haveBaggle():Boolean = { var x = 0
    if (estSurBiscuit() == true) {
    	for (i <- 1 to x){recule}
        return false;
        /* interdit d'écrire du code ici */
    } else {
    	avance()
    	x=x+1
        return true;
        /* pareil ici */
    }
    /* même ici, oubliez */
}

