def insertionSort() {
  var n: Int=getNombreValeurs-1
  var h: Int=0
  var l: Int=0
  for (i<- 1 to n){
    h=getValeur(i)
    for (j<- i-1 to 0 by -1){
      if (!plusPetitQue(j,h)){
        copie(j,j+1)
        l=j
      }
    }
    setValeur(l,h)
  }
}
