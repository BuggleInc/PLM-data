override def run() {
  var cpt = 0
  var h = 75
  var l = 30+20
  for (cpt <- 1 to 4){
    maison()
    droite(90)
    avance(l)
    gauche(90)
  }
  
}

def maison(){
var x=30
  var i=0
  for (i <- 1 to 4){
    avance(x)
    droite(90)
  }
  avance(x)
  droite(30)
  for (i <- 1 to 3){
  avance(x)
  droite(180-60)
  }
  droite(180-30)
  avance(x)
  droite(180)
}

