var depart = 0
def isFacingTrail():Boolean = {
	if((getCouleurSol() == Color.green && !estFaceMur()) || depart == 0) {
    depart = 1
 		avance()
    return true
  } else {
    recule()
    return false
  }
}