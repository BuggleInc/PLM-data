def selectionSort() {
  val n = getNombreValeurs
  def min(a:Int,b:Int):Int = {
    var M = getValeur(a)
    var j = a
    for (i <- a+1 to b-1) {
      if ( getValeur(i) < M) {j=i;M = getValeur(i)}
    }
   return j
  }
  for (i <- 0 to n-1) {
    echange(i,min(i,n))
  }
}