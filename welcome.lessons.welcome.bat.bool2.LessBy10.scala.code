def lessBy10(a:Int, b:Int, c:Int):Boolean = {
	def tresPetit(x:Int):Boolean = {
		if (x<=a-10 || x<=b-10 || x<=c-10) {return true}
		return false
	}
	if (tresPetit(a) || tresPetit(b) || tresPetit(c)) {return true}
	return false
}
