def haveBaggle():Boolean = {
  var seenBaggle:Boolean = false
  for (i <- 1 to 6){
    forward()
    if (isOverBaggle()){
     seenBaggle = true 
    }
  }
  for(i <- 1 to 6){
    backward()
  }
  return seenBaggle
}