override def run() {
  while(!isOverBaggle()){
    var dir = random3()
    tourne(dir)
    if(!isFacingWall()){
      brushDown()
      forward()
      brushUp()
    }
    
  }
  
  pickupBaggle()
}


def tourne(k : Int){
  
  right()
  for(i <- 0 to k-1)
  	left();
}