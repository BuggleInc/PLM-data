def insertionSort() {
  var temp=0
  var j=0
	for (i<-2 to getValueCount()-1) {
    temp= getValue(i)
    j=0
    
      do {
        copy(i-j-1,i-j)
        j=j+1
        }
    while ((getValue(i-j)>temp)&&(j<i))
     setValue(j,temp)
    }
  }

