def squirrelPlay(temp:Int, isSummer:Boolean):Boolean = {
	if (temp<91 && 59<temp && isSummer==false) {
		return true
	} else if (temp<101 && 59<temp && isSummer) {
		return true
	} else {return false
	}
}
