do {
  var nbBaggle=0
  do{
      forward()
      if (isOverBaggle()==true){
       nbBaggle=nbBaggle+1
      }
   } while(crossing()==false)
  if (nbBaggle>=3){
       left()
  }
  else {
    right()
  }
  
} while(exitReached==false)