def insertionSort() {
  var valeur = 0
  for (i <- 0 to getNombreValeurs-1){
   	valeur = getValeur(0);
    var place = i;
   	while (place>=0 && !plusPetitQue(place,valeur)){
      	copie(place,place+1);
      	place=place-1;
    }
    setValeur(valeur,place)
  }
}