while (!exitReached()) {
	var buggle = 0
  do {
    	forward() ;
    	if (isOverBaggle()) {
    	buggle = buggle + 1 ;
    	}     	
	} while (!crossing())
	if (buggle<=2) {
  	right() ; forward() ; 
	}
	else left() ; forward() ; 
}