Erreur de syntaxe : mismatched input ')' expecting COLON
Ligne 25 :         if (getX() == (getMondeHauteur()-1) or getX() == 0)and getY()<getMondeLargeur()-1) :

En cas de doute, vérifiez les indentations, et veillez à ne pas mélanger espaces et tabulations.
