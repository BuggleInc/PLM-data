override def run() {
def estPositionFinale() { estFaceMur() && getDirection== Direction.NORD}
baisseBrosse()
for ( i<-0 to 6 ){
	while(!estPositionFinale()){
		avance()
	}
	 	if (getDirection== Direction.OUEST){ 
	 	 droite(); avance(); droite()
	 }
	 	if ( getDirection==Direction.EST){
	 	gauche(); avance(); gauche()
	 	}
	 	
	 }
}

