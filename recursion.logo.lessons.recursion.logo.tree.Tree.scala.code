def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  avance(length)
  if (steps==0) {
    return
   }
  else {
    avance(length)
    droite(angle)
    tree(steps-1,length*shrink,angle,shrink)
		recule(length)
    gauche(2*angle)
    avance(length)
    tree(steps-1,length*shrink,angle,shrink)
    recule(length)
  }
}