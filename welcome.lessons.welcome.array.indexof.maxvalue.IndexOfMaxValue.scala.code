def indexOfMaxValue(nums:Array[Int]):Int = {
	var valeur: Int = 0
	var position: Int = 1
	for (i <- 0 to nums.length-1) {
		if ( nums(i) > valeur ) {
			valeur = nums(i)
			position = position + 1;
		}
		else {position = position + 1;}
	}
	return position
}