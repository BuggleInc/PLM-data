def step() {
  if (getCouleurSol()==Color.black) {
    point(Color.white)
    gauche()
    avance() 
  } else {
    point(Color.black)
    droite()
    avance()
  }
}

def point(c:Color) = {
  setCouleurBrosse(c)
  baisseBrosse()
  leveBrosse()
}
