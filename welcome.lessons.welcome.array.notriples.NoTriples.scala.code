def noTriples(nums:Array[Int]): Boolean = {
  var rep = true
  var last = nums.length -1
  for (i <- 0 to last-2){
    if (nums(i) == nums(i+1) && nums(i)==nums(i+2)){
      rep=false
    }
  }
   
  return rep
}
