def goAndGet() {
  var p = 0 ;               
  while (estSurBiscuit ==false) {avance(); p+=1;};
  prendBiscuit();
  for (i<-1 to p) {recule();}
  poseBiscuit();
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}