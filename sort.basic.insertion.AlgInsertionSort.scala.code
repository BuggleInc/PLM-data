def insertionSort() {
  for (i <- 1 to getNombreValeurs() -1){
    var v : Int = getValeur(i)
    var indice : Int = i
    while(indice >=0 && plusPetit(indice,indice-1)){
			copie(indice-1, indice)
      indice = indice - 1
     
      
    }
    setValeur(indice,v)
 
    
  }
}