def deplacerBiscuit() {
  prendBiscuit();
  retourne();
  while(!estSurBiscuit) {
    avance();
  }
  recule();
  poseBiscuit();
  retourne();
}

override def run() {
  retourne();
  while (getCouleurSol() != Color.GREEN) {
    if (estSurBiscuit()) {
      deplacerBiscuit();
    } else if (estFaceMur()) {
      droite();
      avance();
      gauche();
    } else {
      avance();
    }
  }
}