retourne();
while (estFaceMur() == false) {

  if (!estSurBiscuit()) {
    avance();
  }
  
  prendBiscuit();
  retourne();
  if (!estSurBiscuit()) {
    avance();
  }
  recule();
  poseBiscuit();
  
  retourne();
  avance();  
  
}

droite();
avance();
gauche();
avance();