var mem = 0
override def run() {
  while(!estTrie) {
    mem=getTrouBase()+1
    if (getTrouBase()+1>3) {
      mem=mem%4
    }
    if (getCouleurJoueur(mem,0)< getCouleurJoueur(mem,1)) {
      deplace(getTrouBase+1,1)
    }
  }
}