def insertionSort(){
  var lgr = getNombreValeurs();
  var v = 0 ;
  var j = 1 ;
	for(i<- 1 to lgr-1){
    j = 1 ;
    v = getValeur(i);
    while(j<=i && v<getValeur(i-j)){
      copie(i-j,i-j+1);
      j += 1;
    }
    setValeur(i-j+1,v)
  }
}