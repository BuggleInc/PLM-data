public void run() { 
   int nbBiscuit = 0;
  int nbCase = 0;
    
  do{
    
    for (int x=1;x==1;x++){
      if(estSurBiscuit()&&crossing()){
      nbBiscuit++;
    	}
    }
    
    avance();
    
 		if(estSurBiscuit()){
      nbBiscuit++;
    }
    
    

    if(nbBiscuit >2 && crossing()){
      gauche();
      nbBiscuit=0;
    } else if(nbBiscuit <=2 && crossing()){
      droite();
      nbBiscuit=0;
    }
    
    
    
  }while(!sortieTrouvee());
}
