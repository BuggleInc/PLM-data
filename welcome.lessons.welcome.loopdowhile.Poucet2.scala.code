while (exitReached == false){
  //Next corridor
    var compteurBaggle = 0
    
  	do{
    	forward()
      if (isOverBaggle())
    		compteurBaggle = compteurBaggle+1
    }while (!crossing())

    if (compteurBaggle >= 3)
    	left()
    else 
    	right()
  
}
forward()
