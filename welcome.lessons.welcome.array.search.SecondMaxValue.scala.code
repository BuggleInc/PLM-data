def max2Value(nums:Array[Int]): Int = {
  var max:Int = nums(0)
  var max2:Int = nums(0)
  for(i<- 1 to nums.length-1){
    if(nums(i) >= max && nums(i) >= max2)
    	if(max2 == max){max2=nums(i+1);max=nums(i)}
    else{max2=max;max=nums(i)}
  }
  return(max2)
}