def triangle(length:Double){
	avance(length)
	droite(120)
	avance(length)
	droite(120)
	avance(length)
	droite(120)
}


def sierpinski(level:Int, length:Double) {
	if (level == 0){triangle(length) ; return}
	sierpinski(level-1, length/2)
		
		
	
}
