def trace() :
	baisseBrosse()
	leveBrosse()

def fairemotif(c) :
	setCouleurBrosse(c)
	avance()
  trace()
  recule()
  gauche()
  avance()
  trace()
  recule()
  droite()
  avance(3)
  gauche()
    
couleur = [Color.red, Color.blue, Color.green, Color.yellow]
for k in range(0,9):
    for j in range(0,9):
        for i in range(0,4):
            faireV(couleur[i])
        recule(4)
    gauche()
    avance(4)
    droite()