override def run() {
  while(!isOverBaggle()) {
    while(!isFacingWall()) {
      left()
      
      if(!isFacingWall()) {
        forward()
      }
    }
    
    right()
    
    if(!isFacingWall())
    forward()
  }
}