def bubbleSort3() {
  var compteur:Int=0
  var indice:Int=1
  for (j<-1 to getNombreValeurs()){
    indice +=1
    compteur=0
    for (i<-0 to getNombreValeurs()-indice){
      if (plusPetit( i+1, i)) {
        echange(i, (i+1))
        compteur+=1}
    if (compteur ==0) {return}}
  }
}