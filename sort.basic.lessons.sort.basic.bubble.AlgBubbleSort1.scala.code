def bubbleSort()  {
  var nb : Int = getNombreValeurs()
  var bool : Boolean = true
  while (bool==true) {
    bool = false
    for (i<-0 to nb-2) {
    	if (plusPetit(i,i+1)) {
        echange(i+1,i)
        bool = true
    	}
  	} 
  }
}