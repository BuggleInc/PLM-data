def insertionSort() {
  for (i <- 1 to getNombreValeurs() - 1){
    var v = getValeur(i);
    var j = i - 1;
    while (j >= 0 && getValeur(j) > v){
      copie(j, j+1);
      j -= 1;
    }
    if (j <= 0){
      j = 0;
    }
    setValeur(j, v);
  }
}