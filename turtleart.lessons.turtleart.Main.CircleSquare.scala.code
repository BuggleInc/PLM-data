override def run() {
	avance(100)
	droite(90)
	avance(100)
	droite(90)
	avance(100)
	droite(90)
	avance(100)
	droite(90)
	setY(getY()-100)
		val angle:Double = 1
	for ( i<- 0 to 359 ) {
		avance(1)
		droite(angle)
	}
	droite(90)
	avance(50)

}
