def isFacingTrail(c):
     while estSurBiscuit()==False:
        if estFaceMur()==True:
            while estFaceMur()==True:
                gauche()
        avance()
        if getCouleurSol()==c:
            baisseBrosse()
            leveBrosse()
        else:
            recule()
            gauche()             

def hunt(c):
    isFacingTrail(c)
    prendBiscuit()
