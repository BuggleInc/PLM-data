def polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double) {
  
  if(levels==1){
    var angle = 360/sides
    
    for(i<-0 to sides-1){
    	avance(length)
      droite(angle)
    }
    
    
    
    
  }
  
  
  
}