def bubbleSort3()
{
  var n = getNombreValeurs()-1;
  var isEchange = true;
  
  for (i <- n to 1)
  {
    isEchange = false
    for (j <- i-1 to 0)
    {
      if (!plusPetit(i,j))
      {
        echange(i,j);
        isEchange = true;
      }
    }
    if (!isEchange) return;
  }
}