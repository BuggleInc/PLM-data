def keepHandOnSideWall() {
  while (estDosMur()){gauche()}
  recule();droite()
  
}

override def run() {
  var i : Int = 0
  var atteint : Int = 20
  while(!estSurBiscuit() && i!=20){
    keepHandOnSideWall()
    i=i+1
    if (i==atteint){
      while(!estFaceMur()){avance()}
      gauche()
      gauche()
      i=0
      atteint=random20()
                   }
  }
  prendBiscuit()
 while(estDosMur()){gauche()}
}