override def run() {
	val taille:Int = getMondeHauteur()
		def remplirTabCol(taille:Int):Array[Color]={
			
			
			var tabCol(taille:Int):Array[Color]=new Array[Color](taille)
			for(i<- 0 to taille)
	
}
