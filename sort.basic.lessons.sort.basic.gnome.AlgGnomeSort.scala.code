def gnomeSort() {
  var swap=true
  do {
    swap=false
    for (i <- 0 to getNombreValeurs()){
      if (!plusPetit(i,i+1)) {
        echange(i,i+1)
        swap=true
      }
    }
  } while (swap==false)
}