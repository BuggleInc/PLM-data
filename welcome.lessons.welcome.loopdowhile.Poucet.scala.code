do {var compteur:Int = 0;
	for (pas <- 1 to 5) {
		avance()
	if (estSurBiscuit()) {
		(compteur = compteur + 1);
	} 
	}
	if (compteur >= 3) {
			gauche();
		} else {
			droite();
		}
		compteur = 0;

}


while (!sortieTrouvee())
avance();
