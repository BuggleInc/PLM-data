def hunt(c:Color) {while (!porteBiscuit()) {
  while (isFacingTrail(c)) {avance();baisseBrosse();leveBrosse();if (estSurBiscuit) {return prendBiscuit()} };gauche() };
}

def isFacingTrail(c:Color):Boolean = {if (!estFaceMur()){avance(); if (getCouleurSol() == c) {recule();return true} else {recule();return false }} else {return false}
}