def gnomeSort() {
  val n = getNombreValeurs()-1
  var compteur = 0
  while (compteur < n) {
    if (!plusPetit(compteur,compteur+1)) {
      echange(compteur,compteur+1)
      compteur -=1
    }
    else {
      compteur +=1
    }
  	if (compteur == -1) {
      compteur = 1
    }
  }
}
