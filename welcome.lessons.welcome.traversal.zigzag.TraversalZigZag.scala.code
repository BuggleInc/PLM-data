override def run() {
	var cpt = 0
	var h = getMondeHauteur() - 1
	var l = getMondeLargeur() - 1
	for (i<- 0 to h) {
		for (g<- 0 to l ) {
			setPos(g,i)
			ecritMessage(cpt)
			cpt = cpt +1
		}
	}
	
	
	
}
