def cocktailSort() {

	var i = 0 ;
	var j = 0 ; 
	var changed = true;  
	while(changed){
		changed = false ; 
		for ( i <- 0 to getNombreValeurs()-2 ){
				if(plusPetit(i+1,i)){
					echange(i,i+1);
					changed = true ;
				}
				for (i 
			
			
		}
		
	}
}
