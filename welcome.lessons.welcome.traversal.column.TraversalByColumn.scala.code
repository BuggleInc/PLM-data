override def run() {


def posfinale(): Boolean = {
	var h = getMondeHauteur()-1;
	var l = getMondeLargeur()-1;
	if (getX() == l && getY() == h) {
		return true;
	} else {
		return false;
	}
	 
}


def possuivante() = {
	var l = 0;
	var c = 0;
	if (estDosMur()) {
		(c = c + 1);
		setPos(c, 0);
		} else {
	(l = l + 1);
	setPos(c, l);
	}
}

while (!(posfinale())) {
	possuivante();
}
}