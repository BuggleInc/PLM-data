def squirrelPlay(temp:Int, isSummer:Boolean):Boolean = {
	(temp,isSummer) match {
		case (x,true) if (x>=60 && x<=100)	=> return true
		case (x,false) if (x>=60 && x<=90)	=> return true
		case (_,_)					=> return false
		}
}