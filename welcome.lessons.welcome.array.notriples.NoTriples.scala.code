def noTriples(nums:Array[Int]): Boolean = {
  var taille : Int = nums.length
  var ind : Int = 0
  var nb : Int = 0
  while (ind < taille-2) {
    nb = nums(ind)
  	if (nums(ind+1)==nb && nums(ind+2)==nb) {return false}
    else {ind = ind +1}
  }
  return true
}