def indexOfValue(nums:Array[Int] ,value:Int): Int = {
   val paslà = -1;
   var pos = 0;
   while ( nums(pos) != value ) {
   	pos = pos + 1;
   }
   if ( nums(pos) == value ) {
   	pos;
   } else {
   	paslà;
   }
}
