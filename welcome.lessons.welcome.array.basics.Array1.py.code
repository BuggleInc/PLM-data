def array():
    brushDown()
    n=0
    for b in L:
        setBrushColor(b)
        if n<len(L)-1:
            forward()   
        n= n+1
     brushUp()          
def storeArray():
    global L
    L = []
    for a in range(getWorldHeight()):
        L.append(getGroundColor())
        forward()
    L.append(getGroundColor())

storeArray()
for c in range(getWorldHeight()-1):
    forward()
    left()
    forward()
    right()
    array()

    