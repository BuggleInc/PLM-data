def in3050(a:Int, b:Int):Boolean = {
  return ((isInRange(a, 30, 40) && isInRange(b, 30, 40)) ||
         (isInRange(a, 40, 50) && isInRange(b, 40, 50)))
}


def isInRange(v:Int, a:Int, b:Int):Boolean = {
  return (v >= a && v <= b)
}
