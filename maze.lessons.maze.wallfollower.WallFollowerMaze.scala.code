override def run() {
    def keepHandOnRightWall() = {
    	if (estFaceMur) {gauche()} 
    	else {while (!(estFaceMur)) {droite()}}
    }
    while (!(estSurBiscuit)) {
    	keepHandOnRightWall();
    	if (!estFaceMur) {avance()};
    }
    prendBiscuit();
}
