def hunt() {
	if (isFacingTrail==true) {
		baisseBrosse()
	}
	else while (isFacingTrail==false) {
		leveBrosse()
		gauche()
		
	}

}
def isFacingTrail():Boolean = {
	var estVert = false;
	if (!estFaceMur) {
		avance()

		if (getCouleurSol()==Color.green) {
			estVert = true;
		}
		recule()

	}
	return estVert;
}        