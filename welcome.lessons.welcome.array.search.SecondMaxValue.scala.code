def max2Value(nums:Array[Int]): Int = {
  var max:Int=nums(0)
  var max2:Int=nums(0)
  for (i <- 0 to nums.length-1){
    if (nums(i)>=max){
      max2=max
      max=nums(i)
    }
  }  
  return max2
}