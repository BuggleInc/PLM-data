def bubbleSort3() {
	
var swapped=false


	for (i <- getNombreValeurs()-2 to 0 by -1) {
		
		 
		
		for (j <- 0 to i ){
			swapped=false
			if (plusPetit(j+1,j)) {
			echange(j,j+1)	
			swapped=true	
			}
		}
			if (swapped==false){
				return
			}
		
		
		
	}
}
