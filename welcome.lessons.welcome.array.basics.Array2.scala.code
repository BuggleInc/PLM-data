def peindreSol(c: Color) {
  setColeurBrosse(c)
  
  baisseBrosse()
  leveBrosse()
}

override def run() {
  var hauteurMonde:Int = getHauteurMonde()
  var largeurMonde:Int = getLargeurMonde()
  
  // On commence par lire les couleurs des cases.
}