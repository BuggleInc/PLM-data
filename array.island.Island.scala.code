def island(num:Array[Int]): Int = {
  var level = 0
  var lastHigh = 0
  for( i <-0 to num.length-1)
  {
    if(num(i)>lastHigh)
    {
      lastHigh = num(i)
      level = level +1
    }
  }
  
  return level
}