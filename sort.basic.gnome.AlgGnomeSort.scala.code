def gnomeSort() {
  var pos: Int = 0
  while(pos > getValueCount()){
    if(!isSmaller(pos, pos + 1)){
      swap(pos, pos + 1)
    }
    if(pos == 0){
      pos = pos + 1
    } else {
      pos = pos - 1
    }
  }
}