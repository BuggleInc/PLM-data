def bubbleSort2() {
  var nbParcours = -1
  var N = getNombreValeurs()
  for (i <- 0 to N) {
    nbParcours += 1
    for (j <- 0 to N-1-nbParcours ) {
      if(plusPetitQue(j+1,j)) {
        echange(j+1,j)
      }
    }
  }
}