def indexOfMaxValue(nums:Array[Int]):Int = {
  var indice:Int=0
  var max:Int=nums(0)
  for (i <- 1 to nums.length-1) {
    if (nums(i) > max) {
      max=nums(i)
      indice=i
    }
  }
  return indice
}