public void run() {
int baggleCount = 0;
int loops = 0;

	do{
		forward();
		
		if(isOverBaggle()){
			baggleCount++;
		}
		
		loops++;

		if(crossing()){
			if(baggleCount >= 3){
				left();
			}else{
				right();
			}
			baggleCount = 0;
			loops = 0;
		}

		if(exitReached()){
			forward();
		}
		
	}while(loops < 5 && !exitReached()); 
}
