def insertionSort() {
  var v=0
  
  for (i<-1 to getNombreValeurs()-1){
    var n=i
    v = getValeur(n)
    while(n >0 && !plusPetitQue(n-1,v)  ){
      copie(n-1,n)
      n= n-1
    }
   setValeur(n,v) 
  }
}