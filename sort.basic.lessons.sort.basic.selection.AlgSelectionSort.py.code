
tri_selection(Array[t],n:Int)
      for i from 1 to n - 1
          min := i
          for j  from i + 1 to n
              if t[j] < t[min], min := j
          end for
          if min != i,  echanger t[i] et t[min]
      end for
  end proc

