def isFacingTrail():Boolean = {
  while (isOverBaggle!=true){
  	forward();
  	if (getGroundColor()==Color.green) {
    	backward();forward();
  	} else {
      backward();
      left();
    }
  }
  pickupBaggle();
  return true
}