def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {

  if(steps > 0) {
    
    //On avance
    forward(length);
    
    //Arbre droit
    right(angle);
    tree(steps-1, length, angle, shrink);
    
    //Arbre gauche
    left(angle*2);
    tree(steps-1, length, angle, shrink);
    
    //On recule
    backward(steps);
  }
  
}