def selectionSort() {
  val lgr:Int = getNombreValeurs()
  for (i <- 0 to lgr-2) {
    var min:Int = getValeur(i)
    var posMin:Int = i
    for (j <- i to lgr-1) {
      if (getValeur(j)<min) {
        min = getValeur(j)
        posMin = j
      }
    }
    echange(i,posMin)
  }
}