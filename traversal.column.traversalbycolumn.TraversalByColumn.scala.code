override def run() {
  val width = getWorldWidth();
  val height = getWorldHeight();
  var i = 0;
  var x = 0;
  var y = 0;
  while(x!=width){
    writeMessage(x*height + y);
    y=y+1;
    if(y==height){
      y=0;
      x=x+1;
    }
    if(x!=width) {
      setPos(x, y);
    }
  }
}