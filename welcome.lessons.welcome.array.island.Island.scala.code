def island(num:Array[Int]): Int = {
var cliffs = 0;

for(i <- 1 to num.length - 1)
{
	if(Math.abs(num(i) - num(i-1)) <= 2)
	{
		cliffs = cliffs + 1
	}
}
	//		mountain		 mountain
	// cliff			cliff		cliff

	return cliffs - 1;
}

