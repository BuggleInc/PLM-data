override def run() {
  
  def mur():Boolean = {
    gauche();
    if(estFaceMur()){droite();return true}
    else{droite();return false} 
  }
  
  def placement(){
  	if(!estFaceMur()){
      avance();
    }
    else if( !mur()){ gauche();avance();droite()}
    else if( estFaceMur()){droite();avance();gauche()}
  }
  

  while(!estSurBiscuit()){       
    placement()
  }
  prendBiscuit()
}