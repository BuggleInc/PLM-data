def hunt() {while(!estSurBiscuit){if (isFacingTrail()){avance();baisseBrosse();leveBrosse()};else{droite()} }
    // Write your code here
}

// Copy your  isFacingTrail() here
def isFacingTrail():Boolean = {
    if(estFaceMur()){return false};
  	else{ avance()
  					if(getCouleurSol()==Color.green){recule ();return true};
        			else{recule();return false}}}