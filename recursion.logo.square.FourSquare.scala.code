override def run() {
  for(i <- 0 until 4){
    avance(90)
    gauche(90)
  }
  for(i <- 0 until 4){
    recule(90)
    gauche(90)
  }
  for(i <- 0 until 4){
    avance(90)
    droite(90)
  }
  for(i <- 0 until 4){
    recule(90)
    droite(90)
  }
}