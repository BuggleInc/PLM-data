def max1020(a:Int, b:Int):Int = {
	(a,b) match {
		case _ if (a>=10 && a<=20 && b>=10 && b<=20) => Math.max(a,b)
		case _ if (a>=10 && a<=20) => a
		case _ if (b>=10 && b<=20) => b
		case _ => 0
	}
}
