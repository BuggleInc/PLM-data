def bubbleSort()  {
	var fin = false
	val l = getNombreValeurs()-2
	while (!fin) {
		var a = 0
		for ( i<- 0 to l ) {
			if (plusPetit(i+1,i)) {
			echange(i,i+1);a=a+1
			}
		}
		if (a==0) {
			fin = true
		}
		
	}
}
