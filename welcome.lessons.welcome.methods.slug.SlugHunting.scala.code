def hunt() {
	while (!estSurBiscuit) {
	while (!isFacingTrail) {
		gauche()	
	}
	if (isFacingTrail) {
		avance()
		baisseBrosse()
		leveBrosse()
	}
	prendBiscuit()
	}
}
def isFacingTrail():Boolean = {
	var estVert = false;
	if (!estFaceMur) {
		avance()
		if (getCouleurSol()==Color.green) {
			estVert = true;
		}
		recule()

	}
	return estVert;
}        