override def run() {
def keepHandOnSideWall() {
	gauche();
	while (estFaceMur()) {
	droite();
	}
	avance();
		}
 while (!(estSurBiscuit())) {
var etat=0;
etat match  {
  case 0 => setDirection(Direction.NORTH); avance(); etat = 1;
  case 1 => keepHandOnSideWall(); etat = 0;
  case _ => println("Ce cas ne devrait pas arriver. Corrigez ce problème svp.")
}
 }
    prendBiscuit();
}
