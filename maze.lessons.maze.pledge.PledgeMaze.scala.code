var a=0
var b=0

override def run() {
  
  
  
  while (!estSurBiscuit()) {
   
    
    keepHandOnSideWall()
  }
  prendBiscuit() 
  
}

def keepHandOnSideWall() {
  
  
  if (a==0 && getDirection==Direction.NORD) {
    while(!estFaceMur) {
      avance()
    }
    droite()
    a+=1
  }
  else {
    if (estFaceMur) {
      a+=1
    }
    while (!estFaceMur) {
      avance() 
      gauche()
      if (!estFaceMur) {
        a-=1
      }
     
    }
    droite()
    
  }
}