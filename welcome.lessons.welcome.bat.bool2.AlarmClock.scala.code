def alarmClock(day:Int, vacation:Boolean): String = {
	var semaine:Boolean=true
	if(day==0 | day==6)
		semaine=false
	if(vacation)
	{
		if(semaine)
			return "10:00"
		else
			return "off"
	}
	else
	{
		if(semaine)
			return "7:00"
		else
			return "10:00"
	}
}
