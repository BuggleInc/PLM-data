def hunt(c:Color) = {
	while(!estSurBiscuit){
		leveBrosse()
		if(isFacingTrail == true) {
			baisseBrosse()
			avance()
		}
		else {
			gauche()
		}
	}
	prendBiscuit()	
}

def isFacingTrail():Boolean = {
	var sol:Boolean = false
	if(!estFaceMur) {
		avance()
		def 
		if(getCouleurSol == Color.green) {
			solVert = true
		}
		recule()
	}
	return solVert
}