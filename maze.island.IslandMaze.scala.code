override def run() {var state=0;
state match  {
  case 0 =>while (getDirection()!=Direction.North) {right()};
  while (!isFacingWall()) {forward()}; // North runner
     state = 1;
  case 1 =>  // Left follower
     state = 0;
  case _ => println("This case should not happen. Please fix me")
}}

getDirection()