override def run() {
	def maison() { 
		avance(30)
		droite(30)
		for ( x <- 1 to 3) {
			avance(30)
			droite(120)
		}
		gauche(120)
		recule(30)
		gauche(90)
		avance(30)
		droite(90)
		avance(30)
	}

	for ( x <- 1 to 4 ) { 
		maison()
	}

}


