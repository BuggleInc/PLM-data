def step():
    lastPoint = getGround()[0]
    for point in getGround():
        if (point != lastPoint):
            if lastPoint.y == point.y:
                return Segment(lastPoint,point)
        lastPoint = point
    return null;
