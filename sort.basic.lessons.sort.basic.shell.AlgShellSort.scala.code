def shellSort()  {
	var gap=getNombreValeurs()/2
	while (gap>0) {
		def insertionSort() {
			for (i <- 1 to getNombreValeurs()-1) {
				val v=getValeur(i-gap)
				var j=i
				while (j>0 && v<getValeur(j)) {
					copie(j-1,j)
					j-=1
				}
				setValeur(j,v)
			}
		}
	}
}
