
override def run() {
  carre()
  carre()
  droite(180)
  avance(50)
  carre()
  carre()
}

def carre(){
  avance(50)
  droite(90)
  avance(50)
  droite(90)
  avance(50)
  droite(90)
  avance(50)
}

 