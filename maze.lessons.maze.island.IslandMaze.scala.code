def keepHandOnSideWall = {gauche(); if (!estFaceMur) {avance()} else {while(estFaceMur()) {droite};avance()} }

override def run() { 
  
  
  while (!estSurBiscuit()) { if (!estFaceMur){setDirection(Direction.NORTH);if (!estFaceMur) {avance()}};keepHandOnSideWall}; prendBiscuit()
}