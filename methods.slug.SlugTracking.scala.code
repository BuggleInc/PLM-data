def isFacingTrail():Boolean = {
  while (isOverBaggle!=true && isFacingWall!=true){
  	forward();
  	if (getGroundColor()==Color.green) {
    	backward();brushDown();forward();brushUp();
  	} else {
      backward();
      left();
    }
  }
  pickupBaggle();
  return true
}