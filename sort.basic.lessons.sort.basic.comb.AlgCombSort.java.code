public void combSort() {
	boolean swapped;
	int gap = getValueCount();
	do {
		swapped = false;
		if(gap > 1)
			gap = gap / 1.3;
		i = 0;
		while(gap + i < getValueCount()){
			if(isSmaller(i+gap, i)){
				swap(i+gap, i);
				swapped = true;
			}
			i++;
		}
	} while(gap > 1 || swapped);
}
