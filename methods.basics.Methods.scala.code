def goAndGet() {
  var x=0
  while (!estSurBiscuit()) {
    avance();
    x=x+1;
  }
  prendBiscuit()
  gauche()
  gauche()
  var j = 0
  while (j!=x) {
    avance();
    j=j+1;
  }
  poseBiscuit()
  gauche()
  gauche()

}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}