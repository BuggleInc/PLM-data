def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
  var vitesse = speed
  if (isBirthday)
  	vitesse = vitesse -5
  vitesse match{
    case i if i <=60 => return 0;
    case i if i <=80 => return 1;
    case i if i >=81 => return 2;
  }
}
