def insertionSort() {
	var l:Int =getNombreValeurs()
	for (i<- 0 to l-1) {
		var buffer:Int = getValeur(i)
		var k = 0
		while(getValeur(k)<buffer) {
			setValeur(k,getValeur(k+1))
			k+=1
		}
		setValeur(k,buffer)
	}
}
