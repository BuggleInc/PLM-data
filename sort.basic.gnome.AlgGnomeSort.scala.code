def gnomeSort() {
  
  var pot = 1
  
  while (pot < getNombreValeurs()-2)
      {if (plusPetit(pot,pot+1))
          {pot+=1}
       else if (pot == 0)
          {echange(pot,pot+1)}
       else 
          {echange(pot,pot+1);pot-=1}}
}