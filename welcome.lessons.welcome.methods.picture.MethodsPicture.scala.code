override def run() {
}

def faireV(c:Color) {
  setCouleurBrosse(c)
  for (i<-1 to 2) {
    avance
  }
  baisseBrosse
  gauche;
  leveBrosse
  avance;
  droite;
  avance;
  baisseBrosse;
  droite;
  leveBrosse;
  avance;
  gauche;
  avance;
  baisseBrosse;
  leveBrosse;
  for (i<-1 to 2) {
    avance
  }
  gauche
}

def faire4v() {
  for (i<- 1 to 3) {
      faireV(Color.yellow);
      faireV(Color.red);
      faireV(Color.blue);
      faireV(Color.green);
  }
  for (j<- 1 to 7) {
     avance;
  }
}
