override def run() {
  def carre(){
    avance(100)
    droite(90)
  	avance(100)
  	droite(90)
  	avance(100)
  	droite(90)
  	avance(100)
  }
  carre()
  droite(180)
  carre()
  droite(180)
  carre()
  droite(180)
  carre()
  
}
