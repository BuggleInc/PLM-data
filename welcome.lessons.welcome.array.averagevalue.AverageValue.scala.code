def averageValue(nums:Array[Int]): Int = {
	

  var s = 0;
  var i=0;
  
  for( i <- 0 to nums.length-1) {
     	       
     	     s = s + ( nums(i)/(nums.length));

 	
 	                      }
        return (s);}