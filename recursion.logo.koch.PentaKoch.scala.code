def pentaKoch(levels:Int, length:Double) {
      var i = levels
  if (i != 0){
    pentaKoch(levels -1,length*0.4)
    gauche (120)
    pentaKoch(levels -1,length*0.4)
    droite(60)
    pentaKoch(levels -1,length*0.4)
    droite(60)
    pentaKoch(levels -1,length*0.4)
    droite(60)
    pentaKoch(levels -1,length*0.4)
    gauche(60)
    pentaKoch(levels -1,length*0.4)
  } else {
    avance(length)
 }
}