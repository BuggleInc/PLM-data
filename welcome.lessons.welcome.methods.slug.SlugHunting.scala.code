def hunt() {
	baisseBrosse()
	while (!isFacingTrail) {
		leveBrosse()
		gauche()	
	}
}
def isFacingTrail():Boolean = {
	var estVert = false;
	if (!estFaceMur) {
		avance()
		if (getCouleurSol()==Color.green) {
			estVert = true;
		}
		recule()

	}
	return estVert;
}        