def gnomeSort() {
  var pos : Int = 1
  while (pos != getNombreValeurs()-1){
      if (plusPetit(pos+1,pos)) {echange(pos+1,pos); if (pos>0) {pos=pos-1}
                                }
      else {pos = pos +1}
    }
  }