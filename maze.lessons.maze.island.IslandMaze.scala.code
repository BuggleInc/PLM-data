def keepHandOnSideWall() {
    gauche();
    if (estFaceMur()) {
      droite();
      if (estFaceMur()) {
        droite();
      } else {
      	avance();
      }
    } else {
      avance();
    }
}

override def run() {
  while (!estSurBiscuit()) {
    setDirection(Direction.NORTH);
    while(!estFaceMur()) {
      avance();
    }
    keepHandOnSideWall();
  }
  prendBiscuit();
}