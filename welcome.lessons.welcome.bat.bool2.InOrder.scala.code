def inOrder(a:Int, b:Int, c:Int, bOk:Boolean):Boolean = {
  if (bOk) {
    if (c >= b) {
      return true;
    } else {
      return false;
    }
  }else {
   if (b >= a && c >= b) {
     return true;
   } else {
     return false;
   }
  }
}
