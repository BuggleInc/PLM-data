def nearTen(num:Int):Boolean = {
  if ( dernierChiffre(num) <= 2 || dernierChiffre(num) >= 8 ){
    return true;
  } else {
    return false;
  }
}
  
var b:Int = 0;

def dernierChiffre(a:Int) : Int = {
  b = a;
  while ( b%10 != b ){
    b = b%10;
  }
  return b;
}
