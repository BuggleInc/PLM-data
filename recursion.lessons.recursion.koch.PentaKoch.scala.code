def pentaKoch(levels:Int, length:Double) {
	if (levels != 0) { 
		avance(length*0.4)
	} else { 
	avance(length*0.4)
	droite(120)
	avance(length*0.4)
	droite(120)
	avance(length*0.4)
	droite(120)
	avance(length*0.4)
}
}
