def island(num:Array[Int]): (Int,Int) = {
  
}

def trouveIle(liste:Array[Int]): Array[Int] = {
  var debut:Int = 0
  var fin:Int = 0
  for (i <- 0 to (liste.length - 1)) {
    if (liste(i-1)==0) {
      debut=i
    } else if (liste(i+1)==0) {
      fin=i
    }
  }
  return (debut,fin)
}