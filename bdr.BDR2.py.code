fini=False

while not fini :

  # variable de nombre de pas :
  pas=0
  
	if estSurMessage():
		msg=litMessage()
	
  	if msg in ("R","L","I","A","Z") :
      pas=1
    if msg in ("B","Y") :
      pas=2
    if msg in ("C","X") :
      pas=3
    if msg in ("D","W") :
      pas=4
    if msg in ("E","V") :
      pas=5
    if msg in ("F","U") :
      pas=6
  
  	if msg in ("A","B","C","D","E"):
      avance(pas)
    if msg in ("U","V","W","X","Y","Z") :
      recule(pas)
    if msg == "I" :
      left(2)
      avance(pas)
    if msg == "R" :
      right()
      avance(pas)
    if msg =="L" :
      left()
      avance(pas)
    
		
	else :
		fini=True