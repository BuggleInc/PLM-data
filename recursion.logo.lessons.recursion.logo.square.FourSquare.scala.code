override def run() {
  carre()
  droite(90)
  carre()
  droite(90)
  carre()
  droite(90)
  carre()
  
}

def carre() {
  baisseCrayon()
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  leveCrayon()
}