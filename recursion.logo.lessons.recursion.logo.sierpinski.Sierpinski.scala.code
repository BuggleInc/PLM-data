def sierpinski(level:Int, length:Double) {
  if (level==0){
    avance(length)
    return
  }
  baisseCrayon()
  sierpinski(level-1,3*length)
  droite(60)
  sierpinski(level-1,3*length)
  droite(60)
  sierpinski(level-1,3*length)
  droite(60)
  leveCrayon()
}
