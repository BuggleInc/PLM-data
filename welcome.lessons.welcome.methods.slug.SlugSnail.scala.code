def hunt(c:Color) = {
	while(!estSurBiscuit){
		leveBrosse()
		if(isFacingTrail == true) {
			baisseBrosse()
			avance()
		}
		else {
			gauche()
		}
	}
	prendBiscuit()	
}

def isFacingTrail():Boolean = {
	var sol:Boolean = false
	if(!estFaceMur) {
		avance()
		var c:  
		if(getCouleurSol == Color.green) {
			sol = true
		}
		recule()
	}
	return sol
}