
PremiereFois=True

while (not sortieTrouvee()):

    while (PremiereFois or not croisement()):
        PremiereFois=False
        biscuit=0

        if estSurBiscuit():
            biscuit=biscuit+1
        forward()

    if biscuit>=3:
        gauche()
        forward()
    else:
        droite()
        forward()

forward()