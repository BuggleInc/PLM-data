var diag = 0;
override def run() {
var c:Int=0


			
	do {
		for{k<-0 to ( getMondeHauteur()-1)}
		
		for (y<-(0 to 2*k).reverse)
		{
			setPos(k-y,y)
			ecritMessage(c)
			c=c+1
		}
		}

	while (
		getX!=getMondeHauteur()-1
		&&
		getY!=getMondeHauteur()-1
		)
}