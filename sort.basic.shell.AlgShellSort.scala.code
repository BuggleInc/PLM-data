def shellSort()  {

  var v=0
  var gap = getValueCount()/2
  while(gap>0){
      for(i <- 1 to getValueCount()-1){  
    var n = i
    v = getValue(n)
    
    while(n > 0 && !isSmallerThan(n-1,v)){
      copy(n-1,n)
      n = n-1
    }
   setValue(n,v)
  
}
}
  }

  
