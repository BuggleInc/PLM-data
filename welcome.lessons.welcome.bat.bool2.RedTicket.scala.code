def redTicket(a:Int, b:Int, c:Int):Int = {
	(a,b,c) match {
		case (2,2,2)					=> return 10
		case (x,y,z) if (x==y && y==z)	=> return 5
		case (x,y,z) if (y!=x && z!=x)	=> return 1
		case (_,_,_)					=> return 0
	}
}
