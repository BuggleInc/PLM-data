override def run() {

def keepHandOnSideWall() {
	while (!(avoirMurGauche()) {
		gauche();
		avance();
	}
	gauche();
	while (estFaceMur()) {
	droite();
	}
	avance();
		}

def avoirMurGauche(): Boolean = {
	gauche();
	if (estFaceMur()) {
		return true;
		droite();
	}
	droite();
	return false;
	
}

 while (!(estSurBiscuit())) {
 	if (avoirMurGauche()) {
 	keepHandOnSideWall()
 }
 }
    prendBiscuit();

}
