override def run() {
	def mkHouse {
	baisseCrayon();
	avance(30);
	droite(30);
	for (i <- 1 to 3) {
		avance(30);
		droite(120.0);
	}
	droite(60);
	avance(30);
	droite(90);
	avance(30);
	droite(90);
	avance(30);
	droite(90);
	}
	def ligneHouse {
	for (i <- 1 to 4) {
	mkHouse;
	leveCrayon();
	droite(90);
	avance(50);
	gauche(90);
	}
	gauche(90);
	avance(200);
	droite(90);
	}

	ligneHouse;
	avance(75);
	mkHouse;
	droite(90);
	avance(150);
	gauche(90);
	mkHouse;
	gauche(90);
	avance(150);
	droite(90);
	avance(75);
	ligneHouse;
	avance(75);
}
