var compt:Int = 1

override def run() {
  
  
  var posFin:Boolean = false
  
  while(posFin != true){
    avancer()
    
    if(getX() == getMondeLargeur() && getY() == getMondeLargeur){
      posFin = true;
    }
  }

  }


def avancer() {
  for (x <- 0 to getMondeLargeur()){
    for (y <- 0 to getMondeHauteur()){
      ecritMessage(compt)
      compt = compt + 1;
      setPos(x, y)
    }
  }
}