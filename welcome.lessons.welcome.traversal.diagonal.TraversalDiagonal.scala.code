override def run() {
var diag = 0;
var c:Int=0
var x=0
var y=0
	for (k<-0 to getMondeHauteur()-1) 
	{ 
		for (y<-(0 to k).reverse) 
	{	setPos(k-y,y)
		ecritMessage(c)
		c=c+1
		
	}
	for (y<-0 to k) 
	{	setPos(k+y,y)
		ecritMessage(c)
		c=c+1
	}
}
}