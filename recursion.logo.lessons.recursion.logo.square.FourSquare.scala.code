override def run() {
  for (i <- 1 to 4) {
  	baisseCrayon()
  	carre()
    gauche(90)
  	leveCrayon()
  }
}

def carre() {
  avance(100)
  gauche(90)
  avance(100)
  gauche(90)
  avance(100)
  gauche(90)
  avance(100)
  gauche(90)
}