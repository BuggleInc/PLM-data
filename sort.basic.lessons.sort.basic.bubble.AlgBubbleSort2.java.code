public void bubbleSort2()  {
	int count = 0;
	boolean swapped;
	do {
		count++;
		swapped = false;
		for (int i = 0; i < getValueCount() - count; i++) {
			if (isSmaller(i+ 1, i)) {
				swap(i,i +1);
				swapped = true;
			}
		}
	} while (swapped);
}