def bubbleSort3() {
  var swapped: Boolean = false
	for(i <- 0 to (getValueCount() - 2)){
    swapped = false
    for(j <- 0 to (getValueCount() - i - 2)){
      if(!isSmaller(j, j + 1)){
        swap(j, j + 1)
        swapped = true
      }
    }
    if(!swapped){
      return
    }
  }
}