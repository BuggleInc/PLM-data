def crab(levels:Int, length:Double) {
  if (levels != 0) {
    for (i <- 0 to levels-1) {gauche(45)}
    crab(levels-1, length/Math.sqrt(2))
    for (i <- 0 to levels) {droite(45)}
    crab(levels-1, length/Math.sqrt(2))
  } else {avance(length)}
}