override def run() {
	def square(steps:Double,angle:Double) {
		for(i<-1 to 4) {
			avance(steps);
			gauche(angle);
		}
	}
square(100,90);
droite(90);
avance(100);
droite(90);
square(100,90);
droite(90);
avance(100);
square(100,90);

}
