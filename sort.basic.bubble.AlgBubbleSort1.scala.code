def bubbleSort()  {
  val dim : Int = getNombreValeurs()
  var a : Int = getValeur(0)
  var b : Int = getValeur(1)
  var echangeFait : Boolean = true
  while (echangeFait) {
    echangeFait = false
    for (i <- 0 to (dim - 2)) {
      if (a >= b) {
        echange(i, i + 1)
        echangeFait = true
      }
      a = getValeur(i+1)
      b = getValeur(i+2)
    }
  }
}