def bubbleSort3() {
    var length : Int = getValueCount();
  var i : Int = 0;
  var tri : Boolean = true;
  
  while(tri)
	{
  	tri = false;
    for(j<-0 to (length-2-i))
    {
      if(isSmaller(j+1,j))
      {
        swap(j,j+1);
        tri=true;
        
      }
    }    
  }
    
}