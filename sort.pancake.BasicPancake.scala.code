def solve() {
  val n = getTaillePile()
  var max = 0
  var iMax = n-1
  for(i <- n-1 to 0 by -1) {
    iMax = i
    max = 0
    for(j <- n-1 to i by -1) {
      if(getRayonCrepe(j)>max) {
        max = getRayonCrepe(j)
        iMax = j
      }
    }
    retourne(iMax+1)
    retourne(i+1)
  }
}