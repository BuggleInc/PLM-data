def last(l:List[Int]): Int = {
  if (l==Nil){return Nil}
  else if (length(l)==1){
    return last(l.head)
  }
  else{
    last(l.tail)
  }
}

def length(a:List[Int]): Int = {
  if (a==Nil){ return 0}
  else length(a.tail)+1
}