def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if(steps==0){
    
    recule(length)
    gauche(2*angle)
  	avance(length)
  	recule(length)
  	return
  }
  else{
  
    
  	droite(angle)
  	avance(length)
  	tree(steps -1,length*shrink,angle,shrink)
  }
  
  
}