override def run() {
  hexagone();
  couronne();
}


def hexagone() {
  for(i<-1 to 8) {
    forward(50);
    right(45);
  }
}

def couronne() {
  for (i<-1 to 8) {
    right(45);
    forward(25);
    left(45);
    forward(25);
    right(45);
  }
}