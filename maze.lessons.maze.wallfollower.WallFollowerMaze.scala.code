override def run() {
    def keepHandOnRightWall() = {
    	droite();
    	if (estFaceMur) {gauche();avance()}
    	else {
    }
    while (!(estSurBiscuit)) {
    	keepHandOnRightWall();
    }
    prendBiscuit();
}
