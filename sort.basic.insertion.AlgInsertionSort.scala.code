def insertionSort() {
  var len=getValueCount();
  var tmp=0;
  for (i <- 1 to len) {
    tmp=getValue(i)
    for (j <- 1 to i) {
      if (!isSmaller(i-j,i)) {
        copy(i-j,i-j+1)
        setValue(i-j,tmp)
      }
    }
  }
}