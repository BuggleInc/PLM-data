def insertionSort() {
	var v = 0;
	var j = 0;
	for(i<-1 until getNombreValeurs()){
		v = getValeur(i);
		j = i;
		while(j>0 && getValeur(j-1)>v){
			setValeur(j,getValeur(j-1))
			j -= 1
		}
		setValeur(j,v);
	}
}
