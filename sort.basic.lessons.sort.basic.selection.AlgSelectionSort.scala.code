def selectionSort() {
  var min = 0;
  var cel = 0;
  
  for(i<-0 to getNombreValeurs()-2){
    min = -1;
    cel = 0;
    for(j<-i to getNombreValeurs()-1){
      if(getValeur(j)<min || min==(-1)){
        min = getValeur(j);
        cel = j;
      }
    }
    echange(i,cel);
  }
}