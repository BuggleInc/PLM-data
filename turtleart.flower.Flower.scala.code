override def run() {
  for(i <- 1 to 8){
    avance(50);droite(360/8)
  }
  avance(25);droite(360/8)
  for(i <- 1 to 8){
  avance(25);gauche(360/8);avance(25);droite(360/4)
  }
 	for (i <- 1 to 8){
     avance(25);droite(360/8); avance(25);droite(360/8)
     avance(25);recule(25);gauche(360/8);gauche(90);avance(25)
    droite(120);avance(25);gauche(90);avance(25);droite(120)
  }
}