 var nbBiscuits: Int=0
  for (n <- 1 to 5){
    avance();
    nbBscuits = nbBiscuits + 1
 do {case nbBiscuits >= 3 => gauche();
   case nbBiscuits <= 2 => droite(); }
    while (sortieTrouvee)
avance();