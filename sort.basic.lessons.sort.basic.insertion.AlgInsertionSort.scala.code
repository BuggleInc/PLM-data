def insertionSort() {
  var v = 0
  var k = 1
    for (i <- 1 to getNombreValeurs()-1) {
      v = getValeur(i)
      k = i;
      print(plusPetit(v, k-1));
      while ((k>1)&&(plusPetit(v, (k-1)))) {
        setValeur(k, getValeur(k-1));
        k=k-1
      }
      setValeur(k,v)
    }
}