def cocktailSort() {
  
  for(k <- 1 to getValueCount()) {
    for(i <- 0 to (getValueCount() - 2); j <- 0 to (getValueCount() - 2) ) {
      if(isSmaller(i+1,i))
          swap(i,i+1);
      if(isSmaller(j+1,j))
          swap(j,j+1);
    }
  }
      
}