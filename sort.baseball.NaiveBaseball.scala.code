override def run() {
  var n=0
  do{
    var trouB=getTrouBase()
    var trouBP=getTrouPosition()
    if(n%4==0){
      deplace(trouB+1,1)}else{deplace(trouB+1,0)}
    n+=1
    
  }while(!estTrie())
}