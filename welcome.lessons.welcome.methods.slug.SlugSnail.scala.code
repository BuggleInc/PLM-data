def hunt(c:Color) = {
	while(!estSurBiscuit){
		leveBrosse()
		if(isFacingTrail(c) == true) {
			baisseBrosse()
			avance()
		}
		else {
			gauche()
		}
	}
	prendBiscuit()	
}

def isFacingTrail(c:Color):Boolean = {
	var sol:Boolean = false
	if(!estFaceMur) {
		avance()
		if(getCouleurSol() == c) {
			sol=true
		}
		recule()
	}
	return sol
}