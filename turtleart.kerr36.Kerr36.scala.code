override def run():Unit= {
  for (i <- 0 to 7){
    motif()
  }
}

def coteMotif():Unit = {
  avance(20)
  gauche(45)
  avance(20)
  droite(90)
  avance(20)
  gauche(45)
  avance(20)
}
def motif():Unit = {
  coteMotif()
  gauche(90)
  avance(20)
  droite(45)
  avance(20)
  droite(135)
  coteMotif()
  droite(135)
  avance(20)
	droite(45)
  avance(20)
  gauche(90)
  coteMotif()
  gauche(135)
}