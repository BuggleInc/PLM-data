for (compteur <- 1 to 10) {
  for (premierCote <- 1 to 8) {
    avance();
  }
  gauche();
  for (deuxCote <- 1 to 8) {
    avance();
  }
  gauche();
  for (troiCote <- 1 to 8) {
    avance();
  }
  gauche();
  for (quatreCote <- 1 to 8) {
    avance();
  }
  gauche();
}

  