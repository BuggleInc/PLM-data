def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  
	if (steps==0){
   	recule(length)
    tree(steps+1,length,angle,shrink)
  }
  else {
    if(steps==0){
      
    }
    avance(length)
    droite(angle)
  	avance(length)
  	
  	current(steps)
  	tree(steps-1,length*shrink,angle,shrink)
    
  }
  	
  	
 
}