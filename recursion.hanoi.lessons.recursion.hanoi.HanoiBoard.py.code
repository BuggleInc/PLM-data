def solve(src,dst,other, height=-1):
    if height==-1: # initial case
        height=getSlotSize(src)
    
        
            
        
        
        
        
        
    
    # Your code here
    if getRayon(dst)<getRayon(src) or getTaillePiquet(dst)==0:
deplace(src, dst)
        
