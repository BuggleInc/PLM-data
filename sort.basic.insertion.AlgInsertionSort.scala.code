def insertionSort() {
  var x:Int=getNombreValeurs()
  var v=0
  for (i<-0 to x-1) {
    v=getValeur(i)
    for (j<-i to 0 by -1) {
      if (!plusPetit(j-1,j) {
        setValeur(i,v)   
      }
          
  }
}