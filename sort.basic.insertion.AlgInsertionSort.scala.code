def insertionSort()
{
  var n = getNombreValeurs()-1;
  var min = 0;
  var x = 0;
  
  for (i <- 0 to n)
  {
    min = getValeur(i);
    for (j <- i to n)
    {
      x = getValeur(j);
      if (x < min) min = x;
    }
    setValeur(i, min);
  }
}