def indexOfMaxValue(nums:Array[Int]):Int = {
	
	var indexOfMax = 0;
	var maxVal = nums(0);

	
	for(i <- 0 to nums.length - 1)
	{
		if(nums(i) > maxVal)
		{
			maxVal = nums(i);
			indexOfMax = i;
		}
	}
	return indexOfMax;
}


