def island(num:Array[Int]): Int = {
  var precedent = num(0)
  val n = num.length
  var c = 0
  for (i <- 1 to n-1) {
    if (num(i) > precedent) {
      c = c + 1
    }
    precedent = num(i)
  }
  return c
}
