override def run() {
  for (compteur <- 1 to 9) {
    faireMotif2()
  }  
}


def faireV() {
  avance(2)
  baisseBrosse()
  leveBrosse()
  gauche()
  avance()
  droite()
  avance()
  baisseBrosse()
  leveBrosse()
  droite()
  avance()
  gauche()
  avance()
  baisseBrosse()
  leveBrosse()
  avance(2)
  gauche
}

def faireMotif2() {
  for (compt1 <- 1 to 9)  {
  	for (compt <- 1 to 4) {
  	faireV()
  	}
    avance(7)
  }
  gauche()
  avance(7)
  droite()
}
