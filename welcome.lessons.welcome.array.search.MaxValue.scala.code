def maxValue(nums:Array[Int]): Int = {
  var taille : Int = nums.length
  var maxi : Int = Math.max(nums(0),nums(1))
  var mini : Int = Math.min(nums(0),nums(1))
  for (i <- 2 to taille-1) {
    maxi = Math.max(maxi, nums(i))
    mini = Math.min(mini, nums(i))
  }
  return mini,maxi}