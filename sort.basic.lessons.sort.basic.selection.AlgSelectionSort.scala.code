def selectionSort() {
  for (i<-0 to getNombreValeurs()-2) {
    var mini=i
    for(j<-i+1 to getNombreValeurs()-1) {
      if (getValeur(j)<=getValeur(mini)) {
        mini=j
      }
    }
    echange(mini,i)
  }
}