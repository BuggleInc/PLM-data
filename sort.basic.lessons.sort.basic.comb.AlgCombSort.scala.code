def combSort() {
	var ecart = getNombreValeurs();
  var echanges = 0;
  var stop = false
  while(stop==false||ecart>1){
    	stop = false
    	if (ecart>1){
      	ecart = (ecart.toDouble / 1.3).toInt
    	}
    	var i = 0;
    	while((i+ecart)<getNombreValeurs()){
      		if(plusPetit(i+ecart,i)){
            	echange(i,i+ecart);
            	stop==true}
      		i=i+1;
    	}
  		}
}

