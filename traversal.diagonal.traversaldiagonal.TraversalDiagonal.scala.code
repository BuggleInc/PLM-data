var diag = 0;

override def run() {
  var cpt = 0
  while(cpt != (getMondeLargeur()+getMondeHauteur()-1)){
    var x = diag
    var y = 0
    while(y != diag){
      setPos(x,y);
      ecritMessage(cpt);
      x -= 1;
      y += 1;
    }
    diag += 1;
  }
}