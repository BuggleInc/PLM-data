while (!sortieTrouvee()) {
  var baggles = 0;
  do {
    avance()
    if (estSurBiscuit()){
      baggles = baggles + 1;
    }
   } while (!crossing);
  if (baggles <= 2) {
    droite();
  }else {
    gauche();
  }
}
avance()
