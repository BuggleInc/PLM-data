override def run() {
	for (i <- 0 to 3) {
		house()
		setPos(getX+50,getY)
	}

}
def house() {
	avance(30)
	droite(30)
	for (i<- 0 to 2) {
		avance(30)
		droite(120)
	}
	droite(60)
	for (i<- 0 to 2) {
		avance(30)
		droite(90)
	}
}
