def gnomeSort() {
	var gnome:Int = 1;

	while (gnome < getNombreValeurs()) {
		if (gnome == 0 || (getValeur(gnome) > getValeur(gnome - 1))) {
			gnome += 1;
		} else {
			echange(gnome - 1, gnome)
			gnome -= 1 ;
		}
		
	}
	
}