override def run() {
  forward();
  for (compteur <- 1 to 4) {
    faireV(); forward; forward(); left(); forward(); 
  }
  backward();
}

def faireV() {
    forward(); brushDown(); brushUp(); left(); forward(); right(); forward(); brushDown(); brushUp(); forward(); right(); forward(); brushDown(); brushUp(); left();
}