def bubbleSort3()  
{
var estRange = 0
for(i <- getNombreValeurs()-2 to 0 by -1)
{
	estRange = 0
	for( j <- 0 to i  )
	{
		if(getValeur(j) > getValeur(j+1))
		{
			echange(j,j+1)
			estRange = 1
		}
	}
	if(estRange == 0)
	{
		return
	}
}
}
