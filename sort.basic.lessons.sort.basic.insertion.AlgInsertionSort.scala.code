def insertionSort() {
	val n = getNombreValeurs()
  for ( k <- 0 to n-1){
    var v = getValeur(k)
    var i = k-1
    while(i>0 && getValeur(i)>v) {
      setValeur(i,getValeur(i+1))
      i -= 1
    }
  }
}
      
