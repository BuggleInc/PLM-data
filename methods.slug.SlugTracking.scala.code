def isFacingTrail():Boolean = {
  while (isOverBaggle!=true){
  	forward();
  	if (getGroundColor()==Color.green) {
    	backward();forward();
  	} else {
      left();
    }
  }
  pickupBaggle();
  return true
}