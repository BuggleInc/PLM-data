def isFacingTrail():Boolean = {
   		if(isFacingWall()){
      return false
      }
 			 
      else if (getGroundColor() == Color.green){
        forward()
      return true
    }
    
  		else {
      backward()
      return false
  }
}