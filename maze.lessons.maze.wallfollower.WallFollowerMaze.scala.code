override def run() {
	while(!estSurBiscuit())
  keepHandOnSideWall()
}

def keepHandOnSideWall(){
  if (estDosMur()){
  droite()
  avance()
    gauche()}
  else if (estDosMur() && estFaceMur()){
  recule()
  droite()}
  else{
  recule()
    droite()}
  
}