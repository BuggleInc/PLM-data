def selectionSort() {
  var indice = 0
  while (indice < getNombreValeurs()) {
    var min = getValeur(indice)
    for (i <- indice to getNombreValeurs()-1) {
      if (getValeur(i)<min)
      	min = getValeur(i)
    }
    echange(getValeur(indice),min)
    indice += 1
  }
}