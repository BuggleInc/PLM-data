override def run() {
    while (!estSurBiscuit) {
      if (!estFaceMur) {
        if (random3()==0) {
          avance()
        } else if (random3()==1) {
          droite();avance()
        } else {
          gauche();avance()
        }
      } else if (estFaceMur) {gauche()}
    }
} 