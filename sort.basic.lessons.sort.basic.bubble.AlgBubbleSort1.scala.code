def bubbleSort()  {
  var trie = true
  while (trie) {
    trie = false 
    for (i<-0 to getNombreValeurs()-2) {
      if (!(plusPetit(i,i+1))) {
        echange(i,i+1)
      }
    trie=true
  	}
	}
}
