compteur = int(0)
for compteur in range(n):
    if estSurBiscuit() == True:
        prendBiscuit()
        poseBiscuit()
    else: 
        avance()
        if estSurBiscuit() == True:
            prendBiscuit()
