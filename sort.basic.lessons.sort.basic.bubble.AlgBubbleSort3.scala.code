def bubbleSort3() {
	var n = getNombreValeurs()
	for (i <- n-2 to 0 by -1) {
		var x = true
		for (j <- 0 to i) {
			if (plusPetit(j+1,j)) {
				echange(j,j+1)
				x = false
			}
			else x = x & true
		}
		if (x = true) {
			return
		}
	}
}

