var x=0
do{ 
	for (i<-1 to 5) {avance()}
	if (estSurBiscuit()) {x=x+1}
	if ((croisement()) && x<3) {droite(); avance(); x=0}
	if ((croisement()) && x>2) {gauche(); avance(); x=0}
} while (!sortieTrouvee())

