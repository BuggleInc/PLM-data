def tree(n:Int, l:Double, t:Double, s:Double)    {
if (n==0) {
	return
}
else {
	avance (l);
	droite (t);
	avance (l);
	recule (l);
	gauche (2*t);
	avance (l);
	recule (l);
	tree (n-1, l, t, s);
}
}
