def posNeg(a:Int, b:Int, negative:Boolean):Boolean = {
  
  if (b*a<0 && negative == false){
    return true;}
  else if (b < 0 & b < 0 && negative == true){
    return true;
  }else { return false;}
    
}
