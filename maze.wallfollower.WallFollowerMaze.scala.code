override def run() {
  //while(!isOverBaggle()){
  for(i <- 1 to 100000){
      keepHandOnSideWall()
	}
}

def keepHandOnSideWall(){
  
  if(isFacingWall()){
    right()
   	 forward()
        	}
        else{left(); forward()}

  else{
    right()
        if(!isFacingWall()){
          forward()
        	}
        else{left(); forward()}
  }
}

