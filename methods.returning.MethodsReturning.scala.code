def haveBaggle():Boolean = {
  var seenBaggle=false
  var compteur=0
  while (seenBaggle==false){
    compteur=0
    for (k<- 1 to 6){
      compteur=compteur+1
      if (isOverBaggle()==true){
        seenBaggle=true
        for (h<-1 to compteur-1){
          backward()
          }
        return(seenBaggle)
        }
      forward()
      }
    for (k<- 1 to 6){
      backward()
    }
    right()
    forward()
    left()
    }
    
  return(seenBaggle)
}