override def run() {
  
  avance(100);
  droite(90);
  avance(200);
  droite(90);
  avance(200);
  droite(90);
  avance(200);
  droite(180);
  avance(100);
  gauche(90);
  avance(200);
  droite(180);
  avance(100);
  gauche(90);
  avance(100);
  droite(180);
  avance(200);
  droite(180);
  avance(100);
  droite(180);
  
}