def nth(l:List[Int], n:Int): Int = {
  def lambda(tl:List[Int], tn:Int, count:Int): Int = {    
    Int c=count + 1;
    if (tl == Nil) return c;
    if(c == tn){
      return tl.head;
    }
    else{
      return lambda(tl.tail, n, c);
    }
  }
  return lambda(l,n,0);
}