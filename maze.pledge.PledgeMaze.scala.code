override def run() {
 	while (!estFaceMur())
  	avance()
  while (!estSurBiscuit())
  	keepHandOnSideWall()
  prendBiscuit()
}

def keepHandOnSideWall() {
  droite()
  if (estFaceMur()) {
  	gauche()
    if (estFaceMur())
    	gauche()
    else
    	avance()
  }
  else
  	avance()
}
