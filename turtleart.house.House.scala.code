override def run() {
  penDown();
	carre();
  forward(100);
  triangle();
}

def carre() {
  for (i<-1 to 4) {
    forward(100);
    right(90);
  }
}

def triangle() {
  for(i<-1 to 3) {
    forward(100);
    right(120);
  }
}