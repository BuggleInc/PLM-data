def bubbleSort()  {
	val taille = getNombreValeurs();
	for (x <- 0 to taille-2) { 
		if (plusPetit(x,x+1) == false) {
	for (i <- 0 to taille-2) {
		if ( plusPetit(i,i+1) == false) {
			echange(i,i+1);
		}
	}
} } }

