override def run() {
  for (compteur <- 1 to 2){
    repMotif()
  }
}


def repMotif(){
  for (compteur <- 1 to 2){
    Motif()
  }
  recule(3)
  gauche()
  avance(3)
  droite()
}

def Motif(){
  faireMotif1(Color.RED)
  faireMotif2(Color.BLUE)
  faireMotif2(Color.YELLOW)
  faireMotif1(Color.GREEN)
  avance(3)
}

def faireMotif1(c:Color){
  setCouleurBrosse(c)
  avance()
  brosse()
  gauche()
  avance()
  gauche()
  avance()
  brosse()
  gauche()
  avance()
  gauche()
  avance()
  
}
def faireMotif2(c:Color){
  setCouleurBrosse(c)
  avance()
  brosse()
  gauche()
  avance()
  brosse()
  gauche()
  avance()
  
  
}

def brosse(){
  baisseBrosse()
  leveBrosse()
}