def isFacingTrail():
   # write your code here
    while isOverBaggle != True:
        brushDown()
        brushUp()
        forward()
        if (isFacinWall() == True):
            left()
        if (getGroundColor() != Color.green):
            backward()
            left()
        else:
            brushDown()
            brushUp()
            forward()

isFacingTrail()