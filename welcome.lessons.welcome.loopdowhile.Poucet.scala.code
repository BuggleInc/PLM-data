var compteur = 0;
if (estSurBiscuit()){
	compteur = compteur + 1
	avance();
}


do {
   while (!croisement()){
    if (estSurBiscuit()){
	compteur = compteur + 1
	
}
  avance()
  }
  if (compteur <= 2) {
  	gauche(); compteur = 0;
  }
  else   droite(); 
         compteur = 0; 
}
while (!sortieTrouvee());
avance();


