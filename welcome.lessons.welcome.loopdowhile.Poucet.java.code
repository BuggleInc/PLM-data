public void run() { 
	while (!exitReached()){
		int baggleCount = 0;
		for (int i = 0; i < 5; i++){
			forward();
			if (isOverBaggle()) baggleCount++;	
		}
		if (baggleCount <= 2) right();
		else left();
	}
}
