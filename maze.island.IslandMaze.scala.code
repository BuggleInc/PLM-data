override def run() {
  while (!estSurBiscuit()) {
    setDirection(Direction.NORTH)
    while (!estFaceMur()) {
      avance()
    }
    if (estFaceMur()) {
      keepHandOnSideWall()
      if (getDirection() != Direction.NORTH) {
        keepHandOnSideWall()
      }
      setDirection(Direction.NORTH)
    }
  }
}

def keepHandOnSideWall() = {
  if (!estFaceMur()) {
    gauche()
  }
  while (estFaceMur()) {
    droite()
  }
  avance()
}