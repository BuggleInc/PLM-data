def answerCell(isMorning:Boolean, isMom:Boolean, isAsleep:Boolean):Boolean = {
	if ( (isMorning) && (isMom) && (!isAsleep) ) { true }
	else if ( (isAsleep) ) { false }
	else if ( ((!isMorning) && (!isAsleep)) || (isMom) ) { true }
	else { false }
	
}
