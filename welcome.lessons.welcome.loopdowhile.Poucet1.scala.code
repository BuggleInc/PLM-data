var baggle = 0
do{
	
for (compteur <- 1 to 5) // compteur 5 cases
			if (estSurBiscuit())
				baggle=baggle +1
  avance()
	if (baggle <=2)
		droite()
	if (baggle >=3)
		gauche()
}	while (!sortieTrouvee())