override def run(){
  droite()
  recule(2)
  faireMotif()
  gauche()
  avance(4)
  droite()
  faireMotif()
  recule(4)
  droite()
  recule(3)
  faireMotif()
  gauche()
  avance(4)
  droite()
  faireMotif()
}

def faireMotif(){
  faireDiag(Color.RED)
  faireDiag(Color.BLUE)
  faireDiag(Color.GREEN)
  faireDiag(Color.YELLOW)
}

def faireDiag(c:Color){
  avance()
  marquer(c)
  avance()
  gauche()
  avance()
  marquer(c)
}


def marquer(c:Color){
  setCouleurBrosse(c)
  baisseBrosse()
  leveBrosse()
}