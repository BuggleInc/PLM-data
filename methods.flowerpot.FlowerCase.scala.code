def growFlower() {
  Flowers(Color.RED)
  for(b<-1 to 4) { avance()}
  Flowers (Color.CYAN)
  gauche(); for(b<-1 to 4) { recule()} ; droite();
  Flowers (Color.GREEN)
  gauche();gauche();for(b<-1 to 2) { avance();droite();avance();gauche};droite();droite();
  Flowers(Color.ORANGE)
  for(b<-1 to 2) { recule();gauche();recule();droite()}
  Flowers(Color.PINK)
}

def growFlowers() {
   growFlower()
   for (c<-1 to 2) {avance();avance();avance();gauche();avance();droite()}
   Flowers(Color.BLUE)
   for(b<-1 to 2) { avance();gauche();avance();droite()}
   growFlower()
   droite();avance();droite();for (v<- 1 to 8) {avance()}
   droite();avance();avance();droite()
   growFlower()
   for (c<-1 to 2) {avance();avance();avance();gauche();avance();droite()}
   Flowers(Color.BLUE)
   for(b<-1 to 2) { avance();gauche();avance();droite()}
   growFlower()
 }


def Flowers(c:Color) {
  for (a<- 1 to 4) {
    marquer(c);
    avance();
    marquer(Color.YELLOW)
    gauche();
    avance();
    droite();
    droite()
  }
}

def marquer(c:Color)  {
  setCouleurBrosse(c);
  baisseBrosse();leveBrosse();
}