
def selectionSort() {
  var len = getValueCount();
  for(i <- 0 to len-1){
    var ind = select(i,len);
    swap(ind, i);
    
  }
}

def select(i:Int, len:Int) : Int = {
  var indMini = i;
  var vali =  getValue(i);
  for(j<- i to len-1){
    if(getValue(j) < vali)
    	indMini = j;
  }
  return indMini;
  
}