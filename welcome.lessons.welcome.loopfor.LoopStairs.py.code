for i in range(8):
    altitude = getY()
    longitude = getX()
    if not estFaceMur():
        setX(longitude + 1)
    else : 
        gauche()
        if estFaceMur() :
            droite()
            fini = True
        setY(altitude - 1)
        droite()
        setX(longitude + 1)
    i = i+1
longitude = getX()
setX(longitude + 2)

