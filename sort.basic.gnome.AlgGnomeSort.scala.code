def gnomeSort() { var a=getNombreValeurs() 
                 var b=0
                 while (b!=getNombreValeurs()-1)
                 {if (plusPetit(b+1,b))
                  {echange(b,b+1)
                  if (b==0)
                  {b+=1}
                  else {b-=1}}
                  else {b+=1}
                }
                 
}