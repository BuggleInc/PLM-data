override def run() {

  
  for (k <-0 to 5){
    do{
      baisseBrosse()
      avance()
    }while (!estFaceMur())
    
    
  	if (getDirection ()== Direction.EST){
      gauche()
      avance()
      gauche()
    }
    else {
      droite()
      avance()
      droite()
    }
  }
  avance();avance();avance();avance();avance();avance()
}
