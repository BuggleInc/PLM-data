while (!sortieTrouvee()) {
  var baggles = 0;
  for (pas <- 1 to 5){
    avance();
    if (estSurBiscuit()){
      baggles = baggles + 1;
    }
  }
  if (baggles <= 2) {
    droite();
  }else {
    gauche();
  }
}