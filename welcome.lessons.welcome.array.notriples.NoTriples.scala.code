def noTriples(nums:Array[Int]): Boolean = {
	let flag=true;
	if (nums.length < 3)
	else {
		for (i<- 0 to nums.length-3) {
			if (nums(i) == nums(i+1) == nums (i+2)) flag = false;
		}
	}return flag;
}
