def combSort() {
	var ecart = getNombreValeurs();
     var swapped = false;
		do
       	{
			if (ecart > 1)
				ecart = (ecart.toDouble / 1.3).toInt
			
       		
       	} while (ecart > 1 && swapped)
}
