def hunt() {
  for (i<-1 to 8)
  check;
}

def isFacingTrail():Boolean = {
  var couleur=Color.red
  if (estFaceMur()==false) {
    avance;
    getCouleurSol;
    couleur=getCouleurSol
    recule;
    if (couleur==Color.green) {
      return true
    } else {
      return false
    }
  }
  return false
}

def check {
  if (isFacingTrail==true) {
    baisseBrosse;
    avance;
    leveBrosse;    
  } else if (isFacingTrail==false) {
      gauche;
      isFacingTrail;
  }
} 