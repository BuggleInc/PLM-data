override def run(){
  for (i <- 1 to 81){
    faireMotif()
  	avance(7)
		if (i%9==0){
      gauche()
      avance(7)
      droite()
    }
  }
}


def faireV(){
  avance(2)
  baisseBrosse()
  leveBrosse()
  gauche()
  avance()
  droite()
  avance()
  baisseBrosse()
  leveBrosse()
  avance()
  droite()
  avance()
  baisseBrosse()
  leveBrosse()
  gauche()
  avance(2)
  gauche()
}

def faireMotif(){
  for (i <- 1 to 4){
  	faireV()
	}
}