def gnomeSort() {
  var rg = 0
  var len = getNombreValeurs()
  for (i <- 0 to len-1) {
    rg=i
    while (getValeur(rg+1)<getValeur(rg)) {
      echange(rg+1,rg)
      if (rg>0)
      	rg-=1
    }	
  }
}