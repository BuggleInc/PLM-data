var baggels: Int = 0

while (!sortieTrouvee()) {
  do {
		avance()
    if (estSurBiscuit()) { baggels += 1 }
  }
  while(!crossing());
  if (baggels>=3) {
    gauche()
  }
  if (baggels<=2) {
    droite()
  }
  baggels = 0
}
avance()