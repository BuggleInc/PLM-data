def solve() {
  while (isSorted()==false){
    var i =0
    while (getPancakeRadius(i)<getPancakeRadius(i+1) && i < getStackSize()){
      i=i+1
    }
    if (i==getStackSize()){
      return 0
    }
    else {
      flip(i+1)
    }
  }
}