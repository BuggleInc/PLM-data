public void run() {
  double x = 100;
  int y = 90;
  avance(x);
  droite(y);
  avance(x);
  droite(y);
  avance(x);
  droite(y);
  avance(x);
  droite(2*y);
  avance(x);
  droite(y);
  avance(x);
  droite(y);
  avance(x);
  droite(y);
  avance(x);
  droite(180);
  avance(x);
  droite(y);
  avance(x);
  droite(y);
  avance(x);
  droite(y);
  avance(x);
  droite(180);
  avance(x);
  droite(y);
  avance(x);
  droite(y);
  avance(x);
  droite(y);
  avance(x);
  droite(180);
  
}