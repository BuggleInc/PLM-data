var caseVerte = false

def isFacingTrail():Boolean = {
  if (estFaceMur==true) {
    gauche();
  }
	avance();
  if (getCouleurSol()==Color.green) {
    recule();
    caseVerte = true;
  }else { recule();
  	gauche();
  }
  return caseVerte
}