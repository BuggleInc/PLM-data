public void bubbleSort()  {
  
  boolean swap = true;
  while(swap){
  swap = false;  
  for(int i = 0 ; i < getNombreValeurs(); i++ )
{
    if(plusPetit(i+1,i)){
      swap = true;
      echange(i+1,i);
      
    }
    
  } 
  }
}