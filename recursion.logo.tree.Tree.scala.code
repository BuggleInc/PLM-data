def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  var r=length
  var y=steps
  while(steps!=0){avance(r:Double)
            
            right(angle:Double)
           	r=length*shrink
            
            y=y-1}
}