def selectionSort() {
  for (i<-0 to getNombreValeurs()-2){
    var indiceMin=i
    for (j<-i+1 to getNombreValeurs()-1){
      if (plusPetit(j,indiceMin)){
        indiceMin=j
      }
    }
   echange(indiceMin,i)
  }
}