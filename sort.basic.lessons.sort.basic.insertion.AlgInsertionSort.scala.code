def insertionSort() {
	var l:Int =getNombreValeurs()
	for (i<- 1 to l-1) {
		var buffer:Int = getValeur(i)
		var k = i-1
		while((getValeur(k)<buffer)&&(k<l)) {
			setValeur(k,getValeur(k+1))
			k+=1
		}
		setValeur(k,buffer)
	}
}
