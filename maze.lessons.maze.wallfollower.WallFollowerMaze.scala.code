override def run() {
  while (!estSurBiscuit()) {
    keepHandOnSideWall()
  }
  prendBiscuit();
}


def keepHandOnSideWall() = {
  if (!estFaceMur) {
  	gauche();
    if (!estFaceMur()) {
      gauche();
      gauche();
      avance();
    }
  }
  if (estFaceMur()) {
    	droite();
    	avance();
  }
  if (!estFaceMur()) {
  }
 
}