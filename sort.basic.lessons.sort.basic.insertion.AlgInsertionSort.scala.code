def insertionSort() {
  var lgr = getNombreValeurs()
  var v = 0
  var diffcase = 1
  for (i <- 1 to lgr-1){
    copie(i,v)
  while(plusPetit(v,i-diffcase)){
    if (plusPetit(v,i-diffcase)){
      setValeur(i,getValeur(i-diffcase))
  }
    diffcase=diffcase+1
  }
    setValeur(lgr-1,v)
  }
}
  	