override def run() {
  carre();
  droite(90);
  avance(100);
  droite(90);
  carre();
  droite(180);
  gauche(90);
  avance(200);
  carre();
  droite(90);
  avance(100);
  droite(90);
  carre();
}

def carre(){
  for (i<-0 to 3){
  avance(100);
  droite(90);
}
}