override def run() {
def fonctionTest() : Boolean =  {
	var rep = false
	if (estFaceMur) {
		if (getDirection() == Direction.EST) {
		droite() ; 
		if (estFaceMur) {
			gauche() ; rep = true; 
		}
	}
	if (getDirection() == Direction.OUEST) { 
		gauche();
		if (estFaceMur) {
			droite() ; rep = true; 
		}
	}
	}
	return rep
}
while (!fonctionTest()) {
	if (!estFaceMur()) { 
		avance()
	}
}

}
