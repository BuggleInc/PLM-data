def hunt() {
  while (!estSurBiscuit()){
    while (isFacingTrail()){
      forward()
      baisseBrosse()
      leveBrosse()
    }
  }
  prendBiscuit()
}

def isFacingTrail():Boolean = {
  while (!estSurBiscuit){
    if (!estFaceMur){
      forward()
      if (getGroundColor()==Color.green){
        backward ; return true
      }
      else 
        backward()
        left()
    }
    else 
      left()
    }
  return true
  }