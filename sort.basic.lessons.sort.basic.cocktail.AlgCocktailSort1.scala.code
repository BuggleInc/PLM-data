def cocktailSort() {
	var swap=false
	do {
	for (i <- 0 to getNombreValeurs()-2) {
		if ( plusPetit(i,i+1) ) {
			swap=true
			echange (i+1,i); } else {
				return
			
			
		
	}
}
for (i <- getNombreValeurs()-2 to 0) {
		if ( plusPetit(i,i+1) ) {
			swap=true
			echange (i+1,i);
		} else {
			return
		}
		}
		}
}
