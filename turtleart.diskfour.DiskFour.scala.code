override def run() {
  for (k<-1 to 90) {
    avance(100:Int)
    recule(100:Int)
    droite(1:Double)
  }
  setCouleur(Color.red:Color)
  for (k<-1 to 90) {
    avance(100:Int)
    recule(100:Int)
    droite(1:Double)
  }
  setCouleur(Color.orange:Color)
  for (k<-1 to 90) {
    avance(100:Int)
    recule(100:Int)
    droite(1:Double)
  }
  setCouleur(Color.magenta:Color)
  for (k<-1 to 90) {
    avance(100:Int)
    recule(100:Int)
    droite(1:Double)
  }
}