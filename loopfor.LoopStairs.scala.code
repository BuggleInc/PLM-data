avance() //la première case est déjà colorée

for (c <- 1 to 8) {
  baisseBrosse()

  while (!estFaceMur()) {
    avance()
  }

  leveBrosse()
  gauche()
  avance()
  droite()
  avance()
  
}

baisseBrosse()
for (c <- 1 to 3) {
  avance()
}