def bubbleSort3()  {
  for ( i <- 0 to getNombreValeurs()-1){
    var res : Int = 0
    for ( j <- 0 to getNombreValeurs()-2 - i){
      if (plusPetit(j+1,j)) {echange ( j , j +1 )
                             res = res + 1}
    }
    if (res==0){return}
  }
}