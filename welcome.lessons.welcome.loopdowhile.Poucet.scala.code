var compteur=0

while (!sortieTrouvee())
{
	
 do {


 	
if(estSurBiscuit)	{
	compteur=compteur +1
}
 	
 if (compteur>=3) {
 	gauche()
 	avance();avance();avance();avance();avance()
}
 else {
 	droite()
 	avance();avance();avance();avance();avance()	
}
 		
 } while(!croisement())

}


if (sortieTrouvee()) {
	avance()
}
