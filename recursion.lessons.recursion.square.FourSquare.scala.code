override def run() {
	for (i<-1 to 4) {carre()}
}

def carre() {
	baisseCrayon()
	avance(100:Double)
	gauche(90:Double)
	avance(100:Double)
	gauche(90:Double)
	avance(100:Double)
	gauche(90:Double)
	avance(100:Double)
}
