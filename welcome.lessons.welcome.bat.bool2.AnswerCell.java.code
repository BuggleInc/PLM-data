boolean answerCell(boolean isMorning, boolean isMom, boolean isAsleep) {
  if(!isMorning)
    return true
  else if(isAsleep)
    return false
  else{
    if(isMom)
      return true
    else
      return false
  }
}