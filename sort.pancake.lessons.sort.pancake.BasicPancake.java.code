public void solve() {
	boolean todo = true;
	while(todo) {
		todo = false;

		for(int i = 0; i < 4; i++) {
			if(getPancakeRadius(i) < getPancakeRadius(i+1)) {
				todo = true;
				flip(i+1);
				flip(2);
				flip(i+1);
			}
		}
	}
}
