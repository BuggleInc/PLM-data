def isFacingTrail():Boolean = {
  var trail = false
  if (! isFacingWall() ) {
    forward()
    if (getGroundColor() == "Color.green") {
      trail = true
    }
    backward()
  }
  return trail
}        
