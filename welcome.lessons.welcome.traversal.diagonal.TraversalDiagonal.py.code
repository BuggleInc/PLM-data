nDia=1
n=0
while getX() != getWorldWidth()-1 or getY() != getWorldHeight()-1:
    if nDia <getWorldHeight():
        for i in range(nDia):
            writeMessage(n)
            setPos(getX()+i-1,getY()-i-1)
            n+=1
        setPos(getX()-nDia-1,getY()+nDia-1)
        nDia+=1
            