while not sortieTrouvee():
    forward()
    nbBis=0    
    premiereFois=True
    while premiereFois==True or not croisement():
        forward()
        premiereFois=False
        if estSurBiscuit():
            nbBis=nbBis+1
    if nbBis==2 or nbBis==1 or nbBis==0:
        droite()
    else:
        gauche()
