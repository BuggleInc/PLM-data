def sleepIn(weekday:Boolean, vacation:Boolean): Boolean = {if (weekday == false || (weekday && vacation ))
                                                           {return true}
                                                          else 
                                                          {false}}