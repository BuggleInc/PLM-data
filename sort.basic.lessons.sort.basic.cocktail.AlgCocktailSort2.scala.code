def cocktailSort2() {
for(n <- 2 to getNombreValeurs()-1){
	for(k <- 0 to getNombreValeurs()-n){
			if(plusPetit(k+1,k)){
				echange(k,k+1)
				
			}
		}
		for(i <- getNombreValeurs()-n to 0 by -1 ){
			if(plusPetit(i+1,i)){
				echange(i,i+1)
				
			}
		}
}	
}
