def insertionSort() {

	for(i<- 1 to getNombreValeurs()-1) 
	{
		val valeur = getValeur(i);
		var j=i;
		while((j>0) && (!plusPetit(j-1,valeur)))
		{	
			copie(j,j-1);
			j-=1
		}
//	setValeur(j,valeur);
	}	
}
