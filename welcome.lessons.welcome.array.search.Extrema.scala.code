def extrema(nums:Array[Int]): Int = {
  val l = nums.length
  var max = nums(0)
  var min = nums (0)
  for (i <- 0 to l-1){
    	if (max<=nums(i)) {max=nums(i)}
    	if (min>=nums(i)) {min=nums(i)}
  }
  return max-min
}