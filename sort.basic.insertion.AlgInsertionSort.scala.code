def insertionSort() {
  var l = getNombreValeurs() -1
  var v = 0
  var j = 0
  for (i <- 1 to l) {
    j = i
    v = getValeur(i)
    while (v < getValeur(j-1) && j > 0) {
      setValeur(j-1,getValeur(j))
      j -= 1
    }
  	setValeur(j,v)
  }
}