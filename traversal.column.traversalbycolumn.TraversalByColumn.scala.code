override def run() {
  var x:Int=0;
  var count:Int=0;
  
  while(x!=getWorldWidth()-1){
    colonne(x);
    x=x+1;
  }
  def colonne(x:Int){
  var y:Int=0
  while(y!=getWorldHeight()-1){
    setPos(x,y);
    writeMessage(count);
    y=y+1;
  }
    }

}