def combSort11() {
	var ecart:Int = getNombreValeurs()
	var chan:Boolean = false
	do {
		if (ecart > 1 && (ecart==10)) {
			ecart = 11
		} else if (ecart>1 && ecart==9) {
			ecart = 11
		  }
			else if (ecart>1) {
			ecart = (ecart.toDouble/1.29).toInt;
		     }
		var i = 0
		chan = false
		while (i+ecart<getNombreValeurs()) {
			if (plusPetit(i+ecart,i)) {echange(i,i+ecart);chan=true}
			i = i+1;
		}
	} while (ecart>1 || chan==true)
}
