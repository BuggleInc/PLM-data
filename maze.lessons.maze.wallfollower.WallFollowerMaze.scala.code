override def run() {
	while (!estSurBiscuit) {
		while ((murAGauche) && (!estFaceMur)) {
			avance();
		}
	}
	
}
def murAGauche():Boolean ={
	gauche();
	if (!estFaceMur) {
		droite()
		return false
	} else {
		droite()
		return true
	}
}
