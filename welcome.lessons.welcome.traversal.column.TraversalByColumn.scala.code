override def run() {
  var cpt:Int=0
  for(x<-0 to getMondeLargeur()-1){
    cpt=col(x,cpt)
  }
}

def col(x:Int,cpt:Int):Int={
  var cpt2:Int=cpt
  for (y<-0 to getMondeHauteur()-1){
    setPos(x,y)
    cpt2=cpt2+1
    ecritMessage(""+cpt2)
  }
  return cpt2
}