        def goAndGet() {
var NbC:Int=0
        while (!estSurBiscuit()){
        	avance()
        }
        prendBiscuit()
        for(compteur<-1 to NbC){recule()}
        poseBiscuit()
        }

        for (i <- 1 to 7) {
            goAndGet();
            right();
            forward();
            left();
        }
