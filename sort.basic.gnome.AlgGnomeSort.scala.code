def gnomeSort() {
  var cond=true
  var i=0
  do{
    
    
    if (!plusPetit(i+1,i)){
      i+=1;
    }else if (i>=0){
      echange(i+1,i);i-=1;
    }else (i+=1)
    if (i>getNombreValeurs()-2){
      cond=false
    }
    
  
    
  }while (cond)
}