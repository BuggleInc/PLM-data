def insertionSort() {
  var lgr = getNombreValeurs()
  for (i<- 1 to lgr){
    var v= i
    for (k<-i-1 to 0){
      if (!plusPetit(k,v)){
        copie(k,v);copie(v,k)
        v-=1
      }
      
    }
  }
}