def close10(a:Int, b:Int): Int = {
  (a,b) match {
    case (a,b) if Math.abs(a)==Math.abs(b) =>0
    case (a,b) if Math.abs(a-10)<Math.abs(b-10) => a
    case (a,b) if Math.abs(a-10)>Math.abs(b-10) => b
    case (_,_) => 0
  }
}
