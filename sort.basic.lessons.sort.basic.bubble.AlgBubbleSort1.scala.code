def bubbleSort()  {
	
	var indic :Boolean = false;
	
	do{
		indic=false;
		length=getNombreValeurs();
		for(i<-0 to length-2){
			if(plusPetit(i:Int,j:Int)){
				echange(i:Int,j:Int);
				indic= true;
			}
		}
		
		
	}
	while(indic==true)
	
}
