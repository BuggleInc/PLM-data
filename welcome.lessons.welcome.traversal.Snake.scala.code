override def run() {
	baisseBrosse();
	while (estFaceMur==false){
		avance();
	}
	if (getDirection()==Direction.EST){
		gauche();
		if (estFaceMur==false){ 
		avance();
		gauche()();
		else  {
		droite();
	}
	if (getDirection()==Direction.OUEST){
		droite();
		if (estFaceMur==false){ 
		avance();
		droite();
		else {
			gauche();
		}
		
	}
	
}
	}
}
