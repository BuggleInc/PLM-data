def crab(levels:Int, length:Double) {
   if(levels>0){
    var newLength = length/(math.sqrt(2))
    var nL = levels-1		// newLevel
    
     left(45)
     crab(nL,newLength)
     right(90)
     crab(nL,newLength)
     left(45)
    
    
  }else if(levels==0){
    avance(length)
  }
}
