
do {
  var nbbaggles:Int =0
  for (compteur <- 1 to 5) {
    if (estSurBiscuit==true) {
      nbbaggles= nbbaggles + 1
    }
    avance()
  }
  if (nbbaggles <= 2) {
    droite()
  } else if (nbbaggles >= 3) {
    gauche()
  }
} while (sortieTrouvee==false)