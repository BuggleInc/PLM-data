override def run() {
def alpha() {
	var T = new Array[Color](getMondeHauteur());
	for(w<- 0 to T.length-1) {
		T(w) = getCouleurSol();
		avance();
		}
	}
var x = getMondeLargeur();
for(y<-1 to x) { 
	gauche();
	avance();
	droite();
	}
}
