 def hunt(c:Color) {
     while ( !estSurBiscuit() ) {
    if ( isFacingTrail == true ) {
    	avance();
    	baisseBrosse();
    	leveBrosse();
    } else {
    	gauche();
    }
    }
    prendBiscuit();
 }









def isFacingTrail():Boolean = { 
	 if ( !estFaceMur() ) { avance(); }
	 else { gauche(); avance(); }
	if ({getCouleurSol() == Color.pink}||
	{getCouleurSol() == Color.orange}) {
		 recule();
		 return true
	} else {
           recule();
		 return false
		}
		
	} 