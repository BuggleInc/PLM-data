override def run() {
var tab = new Array[Color] (6)
for (i<- 0 to 5 ) {tab(i) = getCouleurSol();avance()} 
for (h<-0 to 5 ) {gauche();avance();droite();baisseBrosse()
for (j<- 0 to 5 ) { avance();setCouleurBrosse(tab(h-j));}}
}
