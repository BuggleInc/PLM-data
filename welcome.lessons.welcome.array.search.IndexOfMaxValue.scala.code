def indexOfMaxValue(nums:Array[Int]):Int = {
  var indexMaxi:Int = 0
  var maxi:Int = 0
  for (i <- 0 to nums.length) {
    if (nums(i) > maxi) {
      maxi = nums(i)
      indexMaxi = i
    }
  }
  return indexMaxi
}