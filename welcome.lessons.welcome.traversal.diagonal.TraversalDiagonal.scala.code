var diag = 0
var cpt = 0;
override def run() {
ecritMessage(diag)
	for (i <- 1 to 7){
		for (i <- 1 to diag){
			ecritMessage(cpt)
			setPos(getX()+1, getY()-1)
			cpt=cpt+1
		}
		setPos(0, getY()-diag+1)
		diag=diag+1	
	}
}
