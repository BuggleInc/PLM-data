int i,cote,pas;
    for (i = 0; i<7;i++):
    for (cote=0;cote<4;cote++):
        for (pas=0;pas<4;pas++)
            avance(1);
        gauche();
        for (pas=0;pas<2;pas++)
            avance(1);
        droite();
        for (pas=0;pas<4;pas++)
            avance(1);
        droite();
        avance(1);
        avance(1);
        gauche();
        for (pas=0;pas<4;pas++)
            avance(1);
        gauche();