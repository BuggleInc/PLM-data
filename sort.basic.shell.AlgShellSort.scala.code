def shellSort()  {
  var ecart = getNombreValeurs()/2.0
  while(ecart>0){
  var v=0
  
  for (i<-1 to getNombreValeurs()-1){
    var n=i
    v = getValeur(n)
    while(n >0 && !plusPetitQue(n-1,v)  ){
      copie(n-ecart,n)
      n= n-1
    }
   setValeur(n,v) 
  }
}
  }
