var fini=false
if (estSurMessage()) {
  litMessage(): String;
  if (litMessage()==R) {
    droite();
    avance(1);
  } else if (litMessage()==L) {
    gauche();
    avance(1);
  } else if (litMessage()==I) {
    retourne();
    avance(1);
  } else if (litMessage()==A) {
    avance(1);
  }
  
}

  
  
