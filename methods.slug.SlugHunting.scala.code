def hunt() {
  while (!(estSurBiscuit())){
      if (isFacingTrail()) {baisseBrosse();leveBrosse();avance()}
    	else {while (!(isFacingTrail())) {gauche()}}
  } 
  prendBiscuit() }
  

  

def isFacingTrail():Boolean = {
  if (estFaceMur()) {return false}
  else {avance()
  var res : Boolean = (getCouleurSol()==Color.green)
  recule()
  return res}
}