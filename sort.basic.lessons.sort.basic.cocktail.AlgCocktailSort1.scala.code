def cocktailSort() {
var l = getNombreValeurs()
var tri = true

while (tri) {
tri = false
for (i <- 0 to l-2)
		{if (plusPetit (i+1, i)) {echange(i,i+1); tri = true}}

for (i <- l-2 to 0 by -1)
		{if (plusPetit (i-1, i)) {echange(i,i-1); tri = true}}


}

}
