var diag = 0;
override def run() {
	var x:Int = 0
	for (i <- 1 to 6) {
		ecritMessage(x);
		while (!(getY()==0)) {
			ecritMessage(x);
			setPos(getX()+1,getY()-1);
			x=x+1;
		}
		if (getY()==0) {
			ecritMessage();
			setPos(0,i);
			x=x+1;
		}
	}
	
}