override def run() {
    
  var r = random3()
  while (!estSurBiscuit) {
    r = random3()
    if (r==0 && !estFaceMur())
      avance()
    if (r==1)
      gauche()
    if (r==2)
      droite()
  }
  prendBiscuit()
  
}