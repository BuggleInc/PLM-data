def gnomeSort() {
  var pos : Int = 1
  while (pos != getNombreValeurs()){
    if (pos==0) {pos=1}
    else {
      if (plusPetit(pos+1,pos)) {echange(pos+1,pos); pos=pos-1}
      else {pos = pos +1}
    }
  }
}