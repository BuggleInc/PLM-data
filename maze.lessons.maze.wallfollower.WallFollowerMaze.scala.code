override def run() {
    def keepHandOnRightWall() = {
    	droite();
    	if (estFaceMur) {gauche();avance()}
    	else if (!(estFaceMur())) {avance()}
    		else {while (estFaceMur()) {droite()}}
    }
    while (!(estSurBiscuit)) {
    	keepHandOnRightWall();
    }
    prendBiscuit();
}
