def gnomeSort() {
  var i:Int = 0
  while (i != getNombreValeurs() - 1) {
    if (i == 0) {
      i += 1
    } else if (getValeur(i) > getValeur(i+1)) {
      echange(i,i+1)
      i -= 1
    } else {
      i += 1
    }
  }
}
