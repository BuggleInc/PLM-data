 def hunt(c:Color) {
def hunt() {
	while (!estSurBiscuit()) {
		if (isFacingTrail()) {
			avance()
    			baisseBrosse()
    			leveBrosse()
    		}
    		else {
    			droite()
    			if (isFacingTrail()) {
    				avance()
    				baisseBrosse()
    				leveBrosse()
    				}
    			else {
    				droite()
    				droite()
    				if (isFacingTrail()) {
    					avance()
    					baisseBrosse()
    					leveBrosse()
    					}
    				}
    			}
		}
	prendBiscuit()
}
 }

def isFacingTrail():Boolean = {
	var sol: Boolean = false;
	if (!estFaceMur()) {
		avance()
		if (getCouleurSol() == Color.green) {
			recule()
			sol = true;
		}
		else {recule()}
	}
	return sol
}
