def insertionSort() {
  var valeur=0
  var last=0
  for (i<-1 to getNombreValeurs()-1){
    valeur = getValeur(i)
    for (n<- i-1 to 1 by -1){
      if (plusPetit(i,n)){
        copie(n+1,n)
        last=n-1
      }
    }
    setValeur(last, valeur)
  }
}