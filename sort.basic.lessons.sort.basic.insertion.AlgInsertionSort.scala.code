def insertionSort() {

	var j=0
	for(i<- 1 to getNombreValeurs()) 
	{
		val valeur = getValeur(i)
		j=i
		while((j>0) && (!plusPetit(j-1,i)))
		{
			
			echange(j,j-1)
			j=j-1
		}
	setValeur(j,valeur);
	}	
}
