def crab(levels:Int, length:Double) {
  if (levels==0){
    return
  }
  baisseCrayon()
  crab(levels-1,length/Math.sqrt(2))
  avance(length)
  gauche(135)
  crab(levels-1,length/Math.sqrt(2))
  droite(90)
  crab(levels-1,length/Math.sqrt(2))
  gauche(135)
  leveCrayon()
}