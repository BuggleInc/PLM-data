override def run() {
  
  while (!estSurBiscuit()){
    var hasard=random3()
    if (hasard==0){
      droite()
      avance()
    }
    else if (hasard==1){
      avance()
    }
    else{
      gauche()
      avance()
    }
  }
  prendBiscuit()
}