def bubbleSort()  {
	var swapped = false
	do{
		for(k <- 0 to getNombreValeurs()-1){
			if(plusPetitQue(getValeur(k+1),getValeur(k))){
				echange(k,k+1)
				swapped = true
			}
		}
	
}while(swapped == true)
}
