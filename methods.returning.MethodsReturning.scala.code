def haveBaggle():Boolean = {
  var baggle:Boolean = false
  for (i <- 1 to 7) {
  	for (i <- 1 to 6) {
    	avance()
    	if (estSurBiscuit())
    		baggle = true
    }
  	for (i <-1 to 6)
  		recule()
  	if (baggle)
    	return baggle
  	else {
    	droite()
    	avance()
    	gauche()
  	}
	}
}
