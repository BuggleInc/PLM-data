def house(){
  
  avance(30)
  droite(30)
  avance(30)
  droite(120)
  avance(30)
  droite(120)
  avance(30)
  droite(180)
  avance(30)
  droite(90)
  avance(30)
  droite(90)
  avance(30)
  droite(90)
}
override def run() {
  for(i<-1 to 5)
  {
    house()
  setPos(getX(),getY()+50)
  }
}