def faireV(){
  baisseBrosse()
  leveBrosse()
  avance()
  gauche()
  avance()
  baisseBrosse()
	leveBrosse()
  recule()
  droite()
  avance()
  baisseBrosse()
  leveBrosse()
}

def faireMotif(){
  for(compteur <- 1 to 4){
  	avance(2);
		faireV();
		avance(2);	
    gauche()};
}

def faireLigne(){
  for (compteur<- 1 to 9){
  faireMotif()
    avance(7)}
  gauche()
}


override def run() {
	for (compteur <- 1 to 9){
    faireLigne()
  avance(7)
  droite()}
}