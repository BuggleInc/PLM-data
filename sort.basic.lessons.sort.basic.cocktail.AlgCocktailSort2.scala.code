def cocktailSort2() {
  var debut = 0;
  var fin = getNombreValeurs()-2;
  var trie = false;
  
  do
  {    
    trie = false;
  	for(i <- debut to fin)
  	{
			if(!plusPetit(i,i+1))
   	 	{
   			echange(i,i+1);
        fin-=1;
        trie = true;
     	}
      
  	}
  
  	for(i <- fin-2 to debut by -1)
  	{
    	if(!plusPetit(i,i+1))
    	{
      	echange(i,i+1);
 				debut+=1;
        trie = true;
    	}
  	}
    
  }while(trie)
  
}