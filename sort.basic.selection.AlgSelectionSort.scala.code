def selectionSort() {
  var i:Int=0
  var m:Int=getValue(i)
  for(i<-0 to getValueCount()-1){
    for(j<-i to getValueCount()-1){
      if(getValue(j)<=m){
        m=getValue(j)
      }
      swap(m,i)
    }
  }
}
