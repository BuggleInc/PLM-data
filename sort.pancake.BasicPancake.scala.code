def solve() {
  var posPancakeMax = 2
  
  while(!isSorted()){
    for (i <- 0 to getStackSize() - 1){
      if (getPancakeRadius(i) > getPancakeRadius(posPancakeMax)){
        posPancakeMax = i
      }
      flip(posPancakeMax)
    }
  }
}