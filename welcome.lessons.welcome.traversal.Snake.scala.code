def fini(){
  	return false
  
}

override def run() {
  baisserBrosse();
  while(!fini()){
    if(!estFaceMur()){
      avance();
    }else{
      setY(get(Y)-1);
      gauche();
      gauche();
    }
  }
}
