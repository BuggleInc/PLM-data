def hunt() {
    while (!estSurBiscuit) {
		if (isFacingTrail) {
			avance()
		} else {
			gauche
		}
    }
    prendBiscuit()
}

def isFacingTrail():Boolean = {
    if (!estFaceMur) {
    		leveBrosse
    		avance()
    		if (getCouleurSol()==Color.green) {
    			recule()
    			baisseBrosse()
    			return true
    		} else {
    			recule()
    			baisseBrosse()
    			return false
    		}
    }
    return false
}        
