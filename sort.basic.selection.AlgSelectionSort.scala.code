def selectionSort() {
  var m?moire=0
  var compteur=0
  var len=getValueCount()
  while (compteur!=(len-1)){
    for (i<-compteur to (len-1)){
      m?moire=compteur
      if (isSmaller(i,m?moire)){
        m?moire=i
      }
    }
    swap(compteur,m?moire)
    compteur=compteur+1
  }
}