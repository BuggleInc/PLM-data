def hunt() {
    while (!estSurBiscuit) {
baisseBrosse();leveBrosse();	
if (isFacingTrail) {avance()} 
else { gauche(); 
if (isFacingTrail()) { avance()} 
else { droite();droite();
if (isFacingtrail()) {avance()}
    }}
}}

def isFacingTrail():Boolean = {
var test = false;
if (estFaceMur) {}

else {avance(); if (getCouleurSol() == Color.green) { test = true}
recule()}
return test


  
}        

