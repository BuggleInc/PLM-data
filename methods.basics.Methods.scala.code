def goAndGet() {
  var nbPas: Int = 0
  while (!estSurBiscuit()) {
    avance()
    nbPas += 1
  }
  prendBiscuit()
  for (compteur <- 1 to nbPas) {
    recule()
  }
  poseBiscuit()
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}