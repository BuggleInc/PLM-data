override def run() {
  growFlowers()
}

def growFlowers() {
  fairefleurrouge()
  avance(4)
  fairefleurcyan()
  recule(4)
  droite()
  avance(2)
  gauche()
  avance(2)
  fairefleurorange()
  recule(2)
  droite()
  avance(2)
  droite()
  fairefleurrose()
  avance(4)
  fairefleurverte()
  recule(4)
}

def fairefleur() {
  baisseBrosse()
  avance()
  avance()
  recule()
  gauche()
  avance()
  recule()
  recule()
  avance()
  setCouleurBrosse(c:Color.yellow)
  leveBrosse()
  droite()
  recule()
}

def fairefleurrouge() {
  setCouleurBrosse(c:Color.red)
  fairefleur()
}

def fairefleurcyan() {
  setCouleurBrosse(c:Color.cyan)
  fairefleur()
}

def fairefleurorange() {
  setCouleurBrosse(c:Color.orange)
  fairefleur()
}

def fairefleurrose() {
  setCouleurBrosse(c:Color.pink)
  fairefleur()
}

def fairefleurverte() {
  setCouleurBrosse(c:Color.green)
  fairefleur()
}

