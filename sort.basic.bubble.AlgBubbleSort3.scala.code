var echange = false

def bubbleSort3()  {
  for(i <- 0 to getNombreValeurs()-1){
    for(j <- 0 to getNombreValeurs()-2-i){
      if(plusPetit(j+1,j))
      	echange(j,j+1) 
      else
      	echange=true
      
      	
        
      	
    }
    if(echange==true)
    	return
  }
}