def insertionSort() {
  var temp = 0
  var g = 0
  var j = 0
  
  for(i<- 1 to getValueCount()-1)
  {
  	temp = getValue(i)
    j=i
    g=getValue(j)
    while(isSmallerThan(temp,g)&&(j>0))
    {
      setValue(j+1,g)
      j-=1
      g = getValue(j)
    }
    setValue(j+1,temp)
  }
          
}