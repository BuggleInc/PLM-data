def tree(steps:Int, length:Double, angle:Double, shrink:Double){
  baisseCrayon()
  avance(length)
  if (steps!=0){
    droite(angle);
    current(steps);
    return(ree(steps-1,length*shrink,angle,shrink));
    tree(steps-1,length*shrink,angle,shrink);
  }
  
}