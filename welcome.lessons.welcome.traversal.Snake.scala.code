override def run() {
 baisseBrosse()
 while (arrive() != false) 
 {
 	if (!estFaceMur())
 	  {avance()}
 	 		
 }
}


def arrive() {
	if (estFaceMur())
		{ if (getDirection() == Direction.EST)
			  {gauche()
			    if (estFaceMur())
			     { return true
			 	}}
			else { if (getDirection == Direction.OUEST)
					 {droite()
					 estFaceMur()
					 }}
		}
	else return false
}

