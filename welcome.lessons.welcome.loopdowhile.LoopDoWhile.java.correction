$package 

import java.awt.Color;

public class Program extends plm.universe.bugglequest.AbstractBuggle {
	boolean isGroundWhite() { 
		return getGroundColor().equals(Color.white)?true:false;
	}
	/* BINDINGS TRANSLATION */
	boolean estSurBlanc() { return isGroundWhite() ; }
	
	@Override
	/* BEGIN TEMPLATE */
	public void run() { 
		/* BEGIN SOLUTION */
		do {
			forward();
		} while (!isGroundWhite());
		/* END SOLUTION */
	}
	/* END TEMPLATE */
}
