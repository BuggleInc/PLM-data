def bubbleSort3() { val a = getNombreValeurs() - 1 
                   
                   for (i <- 0 to a) {
                      for (j <- i+1 to a) { 
                         if (plusPetit(j,i)) { echange(i,j)
                         } 
                      } 
                    var ok = false
                     for (i <- 0 to a-1) { if (plusPetit(i,i+1)) ok=false else ok = true }
                     if (ok) return
                   }
                 
               }