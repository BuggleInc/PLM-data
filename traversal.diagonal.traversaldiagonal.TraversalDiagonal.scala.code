var diag = 0;
var finalpos : Boolean = true;
override def run() {
  while (finalpos==false){
    for ( a <- 0 to diag -1){
      setPos(diag-a,diag-a)
      writeMessage(diag)
      diag = diag + 1
    }
    setPos(diag,diag)
    writeMessage(diag)
    finalpos=true
  }
  
}