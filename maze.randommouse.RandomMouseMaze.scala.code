override def run() {
  var random:Int = 0
    while (!estSurBiscuit) {
      random = random3()
      if ((random == 0) && (!estFaceMur)) {
        avance()
      } else if (random == 1) {
        droite()
      } else if (random == 2) {
        gauche()
      }
    }
  prendBiscuit
}