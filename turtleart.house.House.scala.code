override def run() {
  for (k<-1 to 5) {
    avance(100:Int)
    droite(90:Double)
  }
  gauche(90:Double)
  droite(30:Double)
  for (k<-1 to 3){
  	avance(100:Int)
  	droite(120:Double)
  }
  gauche(210:Double)
  avance(100:Int)
  gauche(180:Double)
}