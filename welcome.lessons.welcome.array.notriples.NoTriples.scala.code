def noTriples(nums:Array[Int]): Boolean = {
  var n  = nums.length
  var pasTriple = true
  for (k<-0 to n-3){
    if(nums(k)==nums(k+1) && nums(k)==nums(k+2)){
      pasTriple=false
    }
    else pasTriple=pasTriple
  }
  return pasTriple
}
