def step(rule:Array[Char], colors:Array[Color]) {
  var color : Color = getCouleurSol()
  var indice : Int = 0
  while (colors(indice) != color) {indice = indice +1}
  if (rule(indice) == 'R'){
    marquer(Color.black) //colors((indice+1)%colors.length-1))
    droite()
    avance()
  }
  if (rule(indice) == 'L'){
    marquer(colors((indice)%(colors.length-1))
    gauche()
    avance()
  }
}

def marquer(c : Color) {
  setCouleurBrosse(c)
  baisseBrosse()
  gauche()
  droite()
  leveBrosse()
}