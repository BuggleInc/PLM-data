def max2Value(nums:Array[Int]): Int = {
  var max = nums(0)
  var dmax = nums(0)
  for (i <- 1 to nums.length-1){
    if(max<nums(i)){
      dmax=max;
      max=nums(i);}
    else if(dmax<nums(i)){dmax = nums(i)}
  }
  return dmax
  
}