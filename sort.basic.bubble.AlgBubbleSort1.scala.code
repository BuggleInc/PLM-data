def bubbleSort()  {
  var len=getNombreValeurs()
  var sp=true
  while (sp==true) {
    for (i <- 0 to len-2) {
      sp=false
      while (!isSmaller(i,i+1)) {
        swap(i,i+1)
        sp=true
      }
    }
  }
}