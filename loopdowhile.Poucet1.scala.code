while (!sortieTrouvee()) {
  var nbBaggle : Int = 0
  for (pas<- 1 to 5) {
    avance() ;
    if (estSurBiscuit()) {
      nbBaggle = nbBaggle + 1
    }
  }
  if (nbBaggle <= 2) {
    droite()
  } else {
    if (nbBaggle >= 3) {
      gauche()
    }
  }
}
avance()