def noTriples(nums):
    for i in range(len(nums)):
        for a in range(len(nums)):
            for b in range(len(nums)):
                if nums[i]==nums[a] and nums[a]==nums[b]:
                    return False
    return True                