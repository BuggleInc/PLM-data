override def run(){
  def keepHandOnSideWall(): Boolean={
    droite()
    if(estFaceMur()){ 
      gauche()
      return true}
    else{
      gauche()
      return false}
  }
  
  while(!estSurBiscuit()){
    if(keepHandOnSideWall()){avance()}
    else{droite()
         avance()
        }
    
  }
  prendBiscuit()
}