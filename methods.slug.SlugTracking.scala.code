def isFacingTrail():Boolean = {
  while (!isOverBaggle()){
    getGroundColor()
    if(color.green()){
      forward()
    }else {
        backward()
        right()
        forward()
    }else {
      backward()
      lfet()
      forward()
    }
  }
}