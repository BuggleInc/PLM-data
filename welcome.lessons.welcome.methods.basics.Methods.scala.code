def goAndGet() {
  var nbPas:Int = 0;
  while (!estSurBiscuit()) {
    avance();
    nbPas = nbPas + 1;
  }
  prendBiscuit();
  var Pas:Int = 0;
  for (Pas <- 0 to nbPas-1) {
    recule();
  }
  poseBiscuit();
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}