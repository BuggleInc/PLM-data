def golomb(num:Int): Int = {
  var tab = new Array[Int](40)
  tab(1) = 1
  tab(2) = 2
  var nombre:Int = 2
  var cmb:Int = 1
  for (i <- 3 to tab.length -1){
    tab(i) = nombre;
    cmb -= 1;
    if (cmb <= 0){
      nombre += 1;
      cmb = tab(nombre);
    }
  }
  return tab(num)
}
