override def run() {
    def keepHandOnRightWall() = {
    	if (!(estFaceMur)) {
		droite();
		if (estFaceMur) {gauche()} else {avance()}
    	} else {droite(); if (!(estFaceMur)) {avance()} else {gauche(); gauche()}}
    }
    while (!(estSurBiscuit)) {
    	keepHandOnRightWall();
    	if (!estFaceMur) {avance()};
    }
    prendBiscuit();
}
