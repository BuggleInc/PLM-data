def crab(levels:Int, length:Double) {
  if(levels==0){
    avance(length)
  }
  else{
    var facteur = 1/Math.sqrt(2)
    gauche(45)
    crab(levels-1,length*facteur)
    droite(90)
    crab(levels-1,length*facteur)
    gauche(45)
  }
}
