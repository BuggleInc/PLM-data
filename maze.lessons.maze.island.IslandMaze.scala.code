def keepHandOnSideWall = {gauche(); if (!estFaceMur) {avance()} else {while(estFaceMur()) {droite};avance()} }

override def run() { 
  
  
  while (!estSurBiscuit()) {setDirection(Direction.NORTH); while (!estFaceMur) {avance()} ;keepHandOnSideWall}; prendBiscuit()
}