def squirrelPlay(temp:Int, isSummer:Boolean):Boolean = {
  if(isSummer){
    if((temp <= 100) && (temp >= 60)){
      return true
    }
    return false
  }
  if((temp <= 90) && (temp >= 60)){
    return true
  }
  return false
}
