def posNeg(a:Int, b:Int, negative:Boolean):Boolean = {
  
  if (negative) {
    
    return a<0 && b<0
    
  } else {
    
    a match {
      
      case i if i<0 => return b>=0
      case i if i>=0 => return b<0
       
    }
    
  }
  
}