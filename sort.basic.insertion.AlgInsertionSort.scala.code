def insertionSort() {
  for( i <- 1 to getNombreValeurs()-1){
		var temp=getValeur(i);
    var stop=false;
		for(a <- i-1 to 1 by -1){
      if(stop!=true){
        if(!plusPetitQue(a, temp)){
          echange(a, a-1)
        }
        else{setValeur(a, temp)}
      }
    }
  }
}