def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
  var s = speed
  if (isBirthday)
  	s = s - 5
  s match {
    case s if s <= 60 => return 0
    case s if s <= 80 => return 1
    case s if s > 80 => return 2
  }
}
