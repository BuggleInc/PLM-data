def cocktailSort2() {
  var n = getNombreValeurs();
  var parcours = true;
  var k = 0
  for (i<-0 to n/2) {
    
	
   
  	for (i<-k to n-2) {
    	if (getValeur(i)>getValeur(i+1)) {
      	echange(i,i+1)
        
      }
      
    }
    for (i<-n-2 to k by -1) {
      if (getValeur(i)>getValeur(i+1)) {
      	echange(i,i+1)
        
      
    }
  }
k=k+1
    n=n-1
}
}