def maxMod5(a:Int, b:Int):Int = {
  if (a%5==b%5){
    (a,b) match{
      case (a,b) if a<b => a
      case (a,b) if a>b => b
      case (_,_) => 0
    }
  }else{
    (a,b) match{
      case (a,b) if a>b => a
      case (a,b) if a<b => b
      case (_,_) => 0
  	}
  }
}