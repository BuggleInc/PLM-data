override def run() {
}

var c : Color=Color.blue

def point(c: Color)
{
	setCouleurBrosse(c: Color);
	baisseBrosse();
	leveBrosse();
}

def faireV (c: Color)
{
	avance;
	point(c: Color);
	avance; gauche; avance;point(c: Color);recule; droite;
	avance; point(c: Color); avance; gauche;
}
