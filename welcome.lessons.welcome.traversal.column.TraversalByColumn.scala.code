override def run() {
	var hauteur: Int = getMondeHauteur();
	var largeur: Int = getMondeLargeur();
	var valeur: Int = 0;

	for(n1 <- 0 to largeur){
		for(n2 <- 0 to hauteur){
			setPos(n1, n2);
			ecritMessage(valeur);
			valeur = valeur + 1;
		}
	}
	
}
