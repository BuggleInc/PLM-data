override def run() {
def keepHandOnSideWall() {
	gauche();
	if (estFaceMur()) {
		droite();
		avance();
		gauche();
		if (!(estFaceMur())) {
			avance();
			gauche();
			if (!(estFaceMur())) {
				avance();
			}
		}
		
	}
	
}

while (!(estSurBiscuit())) {
keepHandOnSideWall();
}
}
