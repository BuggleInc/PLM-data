def insertionSort() {
  for (j<- 0 to getNombreValeurs()-1 ){
    var i:Int=j
    var a:Int=getValeur(i)
    while(i!=0 && a<getValeur(i-1) ){
      setValeur(i,getValeur(i-1))
      i-=1
    }
    setValeur(i,a)
  }
}