def insertionSort() {
  var valeur=0
  var last=0
  for (i<-1 to getNombreValeurs()-1){
    valeur = getValeur(i)
    for (n<- i-1 to 2 by -1){
      if (getValeur(n)<valeur){
        copie(n,n+1)
        last=n-1
      }
    }
    setValeur(last, valeur)
  }
}