def selectionSort() {
  for (i <- 0 to getNombreValeurs()-1){
    var cand=getValeur(i)
    for (j <- i+1 to getNombreValeurs()-1){
      if (!plusPetitQue(j,cand)){
        cand=getValeur(j)
        echange(i,j)
      }
    }
  }
}