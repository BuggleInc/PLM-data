
	/**Pour un exercice de ce type il est conseille de ranger
	 * chaque bout dans des variables booleennes pour factoriser
	 * une partie du code sinon nous nous retrouvons avec des 
	 * expressions de ce genre if(((a >= 13 && a <= 19) || (b >= 13 && b<= 19)) && !((a >= 13 && a <= 19) && (b >= 13 && b<= 19)){
		return true
		ce qui est complètement illisible et source d'erreurs très fréquentes. 
	 */
def loneTeen(a:Int, b:Int):Boolean = {	 
	var ado1:Boolean = 
	