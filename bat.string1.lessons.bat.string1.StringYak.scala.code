def stringYak(str:String):String = 
{
	var chaine = ""
	var i = 0

	while(i != str.length-3)
	{
		if(i<str.length-3 && str.substring(i,3)=="yak")
		{
			i+=3
		}
		else chaine+=str(i)
	}

	return chaine
}
