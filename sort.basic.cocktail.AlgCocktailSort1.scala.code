def cocktailSort() {
  for (j <-0 to getNombreValeurs()){
  for (i <-0 to getNombreValeurs()-2 ){
    if(plusPetit(i+1,i)){
      echange(i,i+1)
    }
  }
  for (i <-getNombreValeurs()-2 to 0 ){
    if(plusPetit(i,i-1)){
      echange(i,i+1)
}
       }
       }
}