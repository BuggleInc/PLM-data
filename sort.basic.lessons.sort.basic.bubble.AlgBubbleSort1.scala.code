def bubbleSort()  {
  
  var test=false;
  while(test==false )
  {
    test=false; 
    for(j<-0 to getNombreValeurs()-1)
    {
      if(plusPetit(j+1,j))
      {
        echange(j+1,j)
        test=true;
      }
    }
    
  }
  
  
}