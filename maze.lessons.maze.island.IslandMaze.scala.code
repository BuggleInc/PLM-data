override def run() {
  while (!estSurBiscuit()) {
    setDirection() = Direction.NORTH
		while (!estFaceMur())  {
   		avance()
		}
    keepHandOnSideWall()
	}
  prendBiscuit()
}


def keepHandOnSideWall() { 
    gauche();
  	if (estFaceMur()) {
    droite();
      if (!estFaceMur()) {
        avance();
      } else droite()
    } else {
    	avance();
    }
}

def murGauche() {
  var res = false;
  gauche();
  if (estFaceMur()) {
    res = true
  }
  droite
  return res
}
