override def run() {
  baisseCrayon()
  for (i <- 0 to 3){
    avance(100)
    droite(90)
  }
  avance(100)
  droite(30)
  for (i <- 0 to 2){
    avance(100)
    droite(120)
  }
  gauche(30)
  recule(100)
}