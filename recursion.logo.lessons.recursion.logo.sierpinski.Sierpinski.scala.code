def sierpinski(level:Int, length:Double) {
  if(level > 0){
    var nlvl = level-1;
    var nlng = length/2;

    sierpinski(nlvl,nlng);
    right(180);
    sierpinski(nlvl,nlng);
    right(180);
    sierpinski(nlvl,nlng);
    right(60);
  }
  else{
    forward(length);
  }
}