public void run() { 
	for (int l = 0; l < 7; l ++) {
		for (int s = 0; s < 4; s++) {
			oneSide();
		}
	}
}

public void oneSide() {
	takeSteps(4);
	
	left();
	takeSteps(2);
	
	right();
	takeSteps(4);

	right();
	takeSteps(2);

	left();
	takeSteps(4);
}

public void takeSteps(int n) {
	for (int i = 0; i < n; i++) {
		forward();
	}
}
One side is now 4 steps forward,
2 steps to the left,
4 steps to the right,
2 steps to the right,
4 steps to the left,
and you want to run 7 around.