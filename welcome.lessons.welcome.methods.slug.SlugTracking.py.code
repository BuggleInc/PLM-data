def isFacingTrail():
   # write your code here
    while isOverBaggle != True:
        if (getGroundColor() == Color.lightGray):
            if (isFacingWall()):
                left()
#            elif (getGroundColor() != Color.lightGray or Color.green):
#                backward()
            else:
                forward()
        elif (getGroundColor() == Color.green):
            brushDown()
            brushUp()
            forward()

isFacingTrail()