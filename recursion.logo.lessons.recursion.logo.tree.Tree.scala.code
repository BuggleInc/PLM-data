def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if (steps == 0) {
    return 0;
  } else {
    current(steps);
    avance(length);
    droite(angle);
    tree(steps-1: Int, length*shrink:Double, angle:Double, shrink:Double);
    gauche(2*angle);
    tree(steps-1: Int, length*shrink:Double, angle:Double, shrink:Double);
    droite(angle);
    current(steps);
    recule(length)
  }
}