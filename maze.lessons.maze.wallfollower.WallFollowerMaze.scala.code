override def run() {
    def keepHandOnRightWall() = {
	droite();
	if (estFaceMur) {gauche(); if (estFaceMur()) {gauche();gauche()}} else {avance()}
    }
    while (!(estSurBiscuit)) {
    	keepHandOnRightWall();
    	if (!estFaceMur) {avance()};
    }
    prendBiscuit();
}
