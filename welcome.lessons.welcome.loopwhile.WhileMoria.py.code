setDirection(Direction.EAST)
while getX()!=18:
    while estSurBiscuit()==False:
            if getX()<18:
                avance()
            else:
                setDirection(Direction.SOUTH);avance();setDirection(Direction.EAST);avance();setDirection(Direction.NORTH);avance()            
    prendBiscuit()
    while estSurBiscuit()==False:
            recule()
    avance();poseBiscuit();avance()
