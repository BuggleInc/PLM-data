override def run() {
  var compt:Int=0;
  while (!posiFinal) {
   ecritMessage(compt);   
   if (!estDosMur) {
     setY(getY()+1);
     compt=compt+1
   }
    else {setY(0)
          setX(getX+1);
         compt=compt+1}
  }}

def posiFinal():Boolean = {
  return (estDosMur && estDroiteMur)
    
}

def estDroiteMur():Boolean = {
   var a:Boolean=false ;
   droite;
   a=estFaceMur
   droite ;
  return a
}