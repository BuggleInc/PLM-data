def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {

var test = length

def avancer() {
	for (i <- 0 to steps-1) {
		current(steps-i)
		avance(test)
		droite(angle)
		test = shrink*test
	}
}	

avancer()
	
}

