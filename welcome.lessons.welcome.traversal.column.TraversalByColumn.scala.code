override def run() {
  var cpt:Int = 0
  ecritMessage(cpt)
  cpt+=1
	for (i <- 0 to 47) {
    if (getY()==6) {
      setPos(getX()+1, 0)
    }
    else {
      setPos(getX(), getY()+1)
    }
    
    ecritMessage(cpt)
    cpt=cpt+1
  }
}
