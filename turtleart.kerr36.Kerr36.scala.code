def polygone(n:Int,x:Int) {
  for (i <- 1 to n) {
    avance(x);droite(360/n)
  }
}

override def run():Unit= {
  avance(20);droite(45);avance(20);gauche(90);avance(20);droite(45);avance(20);droite(90);avance(20);gauche(45);avance(20);gauche(135);avance(20);droite(45);avance(20);gauche(90);avance(20);droite(45);avance(20);gauche(135);avance(20);gauche(45);avance(20);droite(90);avance(20);droite(45);avance(20);gauche(90);avance(20);droite(45);avance(20)
}