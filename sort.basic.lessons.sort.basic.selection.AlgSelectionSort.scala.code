def selectionSort() {
	for(i<-0 to getNombreValeurs()-1){
		//var min:Int=getValeur(i);
		//var mem:Int=getValeur(i);
		//var ind:Int=i;
		var min:Int=i;
		for(j<-getNombreValeurs()-1 to i+1 by -1){
			if(plusPetit(j,min)){
				min=j;//getValeur(j);
				//ind=j;
			}
		}
		echange(i,min);
		//setValeur(i,min);
		//setValeur(ind,mem);
	}
}
