def insertionSort() {
  var v=0
  for(i <- 1 to getValueCount()-1){
    v = getValue(i)
    
    for(j <- 1 to i){
      
      if(isSmaller(v,i-j)){
        copy(i-j,i-j+1)
      }
      else{setValue(i-j,v)
        
      }
    }
  }
}