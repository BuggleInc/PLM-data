var compteBaggle = 0

while (!sortieTrouvee() ) {

	do {

    avance()
    
		if(estSurBiscuit())
		{
			compteBaggle = compteBaggle + 1
		}
    
	} while(! crossing() )

	if(compteBaggle >= 3)
		gauche();
	else
		droite();

	
	compteBaggle = 0
}

avance()