def selectionSort() {
  for(i<- 0 to getNombreValeurs()-3){
    var petit = i
    for(o<- i to getNombreValeurs()-1){
      if(getValeur(o)<getValeur(petit)){
        petit=o
      }
    }
    echange(petit,i)
  }
}