def is_at_bottom_right():
    if getX() == getWorldWidth() and getY() == getWorldHeight():
        return True
    else:
        return False

def write_value():
    counter = 0
    for i in range getWorldWidth() * getWorldHeight():
        writeMessage(counter)
        if
        counter += 1
