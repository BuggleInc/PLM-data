var worldSize = getWorldHeight()

for (i <- 1 to worldSize){
  
  if (isOverBaggle){
    pickupBaggle()
    for (j <- 1 to i  -1 ){
      backward()
    }
    dropBaggle()
  	return   
  }
  
  forward()
}
