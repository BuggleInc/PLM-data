var a=1


override def run() {
  
  
  
  while (!estSurBiscuit()) {
   
    
    keepHandOnSideWall()
  }
  prendBiscuit() 
  
}

def keepHandOnSideWall() {
  
  
  if (a==6 && getDirection==Direction.NORD) {
    while(!estFaceMur) {
      avance()
    }
    droite()
    a=1
  }
  else {
    while (!estFaceMur) {
      avance() 
      gauche()
      if (!estFaceMur) {
        a+=1
      }
    }
    droite()
  }
}