def golomb(num:Int): Int = {
  var tab = new Array[Int](num)
  tab(0) = 1
  var flag = 1
  for (k <- 0 to num-1) {
    var nb = tab(k+1)
    for (j <- flag to (flag+nb)) {
      if (j <= num-1) {
      	tab(j) = k
      }
    }
    flag = tab(k)
  }
  return tab(num-1)
}