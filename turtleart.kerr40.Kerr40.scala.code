def faireRectangle(){
  gauche(90)
  avance(30)
  droite(90)
  avance(30)
  droite(90)
  avance(60)
  droite(90)
  avance(30)
  droite(90)
  avance(30)
  droite(90)
}

override def run():Unit= {
  avance(30)
  gauche(90)
  leveCrayon()
  recule(10)
  baisseCrayon()
  avance(20)
  droite(135)
  avance(20)
  droite(90)
  avance(20)
  leveCrayon()
  recule(20)
  baisseCrayon()
  gauche(135)
  avance(30)
  faireRectangle()
}