def sierpinski(level:Int, length:Double) {
  if(levels==0){avance()}
  else{
    sierpinski(level-1,length/3)
    droite(60)
    sierpinski(level-1,length/3)
    droite(60)
    sierpinski(level-1,length/3)
    droite(60)
  }
}