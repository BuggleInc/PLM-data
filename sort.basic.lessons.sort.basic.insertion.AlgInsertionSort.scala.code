def insertionSort() {
	var stockage=0
  var k=0
  for (i<- 1 to getNombreValeurs()-1) {
    stockage=getValeur(i)
    k=i-1
    while(!plusPetitQue(k,stockage) && k>1) {
      copie(k,k+1)
      if (k!=0) {k=k-1}
      else {setValeur(k,stockage)}
    }
    setValeur(k,stockage)
  }
}