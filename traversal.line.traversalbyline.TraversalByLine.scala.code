override def run() {
  var compt:Int=0;
  gauche;
  while (!posiFinal) {
   ecritMessage(compt);   
   if (!estDosMur) {
     setX(getX()+1);
     compt=compt+1
   }
    else {setX(0)
          setY(getY+1);
         compt=compt+1}
  } ;
  ecritMessage(48);
droite;}

def posiFinal():Boolean = {
  return (estDosMur && estDroiteMur)
    
}

def estDroiteMur():Boolean = {
   var a:Boolean=false ;
  gauche;
   a=estFaceMur
  droite ;
  return a
}