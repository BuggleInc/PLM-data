def insertionSort() {
  var n:Int = getNombreValeurs()
  
  for (i <- 1 to n - 1) {
    var v:Int = getValeur(i)
    
    var j:Int = i
    
    while (j > 0 && getValeur(j - 1) > v) {
      copie(j - 1, j)
      
      j -= 1
    }
    
    setValeur(j, v)
  }
}