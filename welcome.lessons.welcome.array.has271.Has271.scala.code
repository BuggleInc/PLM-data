def has271(nums:Array[Int]): Boolean = {
var n = nums.length;
for (k<- 0 to n-3) {
	if (((nums(k)==2) && (nums(k+1)==7) && (nums(k+2)==1)) || (((nums(k)==3) && (nums(k+1)==8) && (nums(k+2)==2)))) {
		return true; 
	}
}
return false;
}
