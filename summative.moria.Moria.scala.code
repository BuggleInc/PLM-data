def run () {
  while (!estSurBiscuit || !estFaceMur) {
    forward();
    if (estSurBiscuit) {
      prendBiscuit(); 
      while (!estSurBiscuit) {
        backward()
      }
      poseBiscuit();
    }
  }
}