def increasing(l:List[Int]): Boolean = {
  if (l.tail!=Nil and l.tail.tail==Nil) {
    if (l.head<=l.tail.head) {
      return true
    } else {return false}
  } else {
    if (l.head<=l.tal.head) {
      return increasing(l.tail)
    } else {return false}
  }
}