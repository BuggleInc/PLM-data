def insertionSort() {

	var v = 0
	var cpt = 1

	for (i<-1 to getNombreValeurs()-1) {
		v=getValeur(i)
			while (!plusPetitQue(i-cpt, v) && cpt<i) {
			copie(i-cpt, i-cpt+1)
			cpt+=1
			}
		setValeur(i-cpt+1, v)
		cpt=1
	}
	
}
