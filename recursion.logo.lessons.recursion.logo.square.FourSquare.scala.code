override def run() {
	for (i <- 0 to 3) {
    faireCarre()
  }
}

def faireCarre() {
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  droite(90)
}