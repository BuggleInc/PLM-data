def Moria() {
  while(!fin) {
    setDirection(Direction.EAST)
    while(!estSurBiscuit()) {
      avance()
    }
    prendBiscuit()
    setDirection(Direction.WEST)
    while(!estSurBiscuit()) {
      avance()
    }
    recule()
    poseBiscuit()
  }
}


def fin() :Boolean={
  if (getDirection==Direction.EST && estFaceMur) {
    return true
  }
  else {
    return false
  }
}