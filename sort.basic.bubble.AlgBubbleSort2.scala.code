def bubbleSort2()  {
  var listeTriee:Boolean=true
  var x=0
  x=getNombreValeurs
  do {
    listeTriee=true
    for (i <- x-2 to 0) {
      for (j <- 0 to i) {
       if (plusPetit(i+1,i)) {
         echange(i+1,i)
         listeTriee=false
       }
       }
    }
  } while (!listeTriee)
}