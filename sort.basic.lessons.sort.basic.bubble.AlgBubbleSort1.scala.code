def bubbleSort()  {
  var n = getNombreValeurs();
  var c = 1;
  while ( c != 0 ){
    for ( i <- 0 to n-2 ){
      c = 0;
      if (plusPetit(i+1:Int,i:Int)) {
        echange(i:Int,i+1:Int);
        c += 1;
      }
    }
  }
}