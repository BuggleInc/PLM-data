def insertionSort() {
 	val n = getNombreValeurs()
  for(i<- 1 to n-2)
  {
    if(plusPetit(i+1, i))
    {
      var j = i
      while(j != 0) 
      {
        if(plusPetit(j, i+1))
        	echange(j+1, i+1)
        j -= 1
      }
    }
  }
}