def cocktailSort() {
	var swapped=true
	{
		for(i<-0 to getNombreValeurs()-2) {
			swapped = false
			if(plusPetit(i,i+1)){
				echange(i+1,i)
				}
	 	for(i<-getNombreValeurs()-2 to 0 by -1) {
	  		swapped = false
	  		if(plusPetit(i,i+1)) {
	  			echange(i+1,i)
	  				} 		
	  			}
			}
		}
	 	
	}

