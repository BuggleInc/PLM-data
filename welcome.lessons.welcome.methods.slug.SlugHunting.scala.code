def hunt() {
  var c:Boolean = true
  while (c){
    
    if (isFacingTrail()){
      baisseBrosse()
      leveBrosse()
      avance()
    }
    else{
      gauche()
      if (!isFacingTrail()){
        gauche()
        gauche()
      }
      
    }
  if (estSurBiscuit()){
      c=false
      prendBiscuit()
    }
  }
  
}

def isFacingTrail():Boolean = {
  var bo:Boolean = !estFaceMur()
  if (bo){
  	avance()
  	bo = (getCouleurSol()==Color.green)
  	recule()
  }
  return bo
  
}