override def run() {
  while (!isOverBaggle()) {
    var nbr=random3();
    if (nbr==0 && !isFacingWall()) {
      forward()
    }
    else if (nbr==1) {
      right()
    }
    else {
      left()
    }
  }
  pickupBaggle()
}