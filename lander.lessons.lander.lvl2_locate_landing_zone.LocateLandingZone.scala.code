def getLandingZone():Segment = {
	var debut:Point = Point(0.0,0.0)
	var fin:Point = Point(0.0,0.0)
	var Liste = getSol()
	for (i <- 0 to Liste.length-2) {
		if (Liste(i).y==Liste(i+1).y) {
			debut=Liste(i)
			fin=Liste(i+1)
		}
	}
	return Segment(debut,fin)
}
