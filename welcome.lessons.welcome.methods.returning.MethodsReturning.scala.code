
def testcol():Boolean= {
	for  (x<-0 to 6) {
		avance();
		if (estSurBiscuit) {
			setY(7);
			return true;
		}
	else return false;
	}
}

def haveBaggle():Boolean = {
	for(x<-0 to 6) {
		testcol();
		writeMessage(testcol());
		
		droite();
		avance();
		gauche();
	}
}

haveBaggle()