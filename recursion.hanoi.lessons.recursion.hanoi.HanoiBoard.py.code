def solve(src,dst,other, height=-1):
    if height==-1: # initial case
        height=getSlotSize(src)
    if  getTaillePiquet(dst)<getTaillePiquet(src):
        deplace(src,dst)
        return solve(other,src,dst, height)
        

        
         
