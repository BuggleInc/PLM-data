def bubbleSort3()  {
  for(i <- getValueCount()- 1 to 1 by -1)
  	traverse(i);
}


def traverse(fin:Int) : Unit  = {
   var swapped = false;
   for (i <- 0 until fin){
     if(isSmaller(i+1, i)){
   		 swap(i,i+1);
       swapped = true;
	   }
   }
   if(!swapped)
  	 return;
}