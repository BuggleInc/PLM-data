def insertionSort() {
  var valeur = 0
  val longueur = getNombreValeurs()
  var j = 0
  for (i <- 1 to longueur - 1 ) {
    valeur = getValeur(i)
    j = i
    while (j != 0 && getValeur(j-1)>valeur) {
      copie(j-1,j)
      j -= 1
    }
    setValeur(j,valeur)
  }
}