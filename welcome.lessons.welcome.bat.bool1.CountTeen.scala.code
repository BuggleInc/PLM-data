def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {
  var x=0
  if (adoOuPas(a)){
    x=x+1}
  if (adoOuPas(b)){
    x=x+1}
  if (adoOuPas(c)){
    x=x+1}
  if (adoOuPas(d)){
    x=x+1}}

def adoOuPas(arg: Int): Boolean ={
	if (arg>=13 && arg<=19){
		return true}
 return false
 }
           