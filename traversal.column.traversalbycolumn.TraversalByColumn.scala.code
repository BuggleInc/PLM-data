override def run() {
  retourne()
  var cpt=0
  ecritMessage(cpt)
  while(getX()!=getMondeLargeur()-1 | getY()!=getMondeHauteur()-1) {
    if(!estFaceMur()) {
      setPos(getX(),getY()+1)
    }
    else {
      setPos(getX()+1,0)
    }
    cpt=cpt+1
    ecritMessage(cpt)
  }
  retourne()
}