def hunt(c:Color) {
  avance()
  while(!estSurBiscuit()){
    if (isFacingTrail())
    avance()
    else {
      gauche()
      if (isFacingTrail())
      avance()
      else {
        droite();droite();avance()
      }
    }
    baisseBrosse()
    leveBrosse()
  }
  prendBiscuit()
}

def isFacingTrail():Boolean = {
  var k=false
  var c=getCouleurSol()
  if (!estFaceMur()){
    avance()
    if (getCouleurSol()==c){
      k=true
    }
    recule()
  }
  return k 
}
