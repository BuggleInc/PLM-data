def insertionSort() {

  
var v =0
var j=0
    for (i<-1 to getNombreValeurs()-1){
      j=i
      while (j>0 && plusPetit(j,j-1)){
        echange(v,j)
        echange(j,j-1)
        echange(j-1,v)
        j=j-1
        
      }
    }
}
