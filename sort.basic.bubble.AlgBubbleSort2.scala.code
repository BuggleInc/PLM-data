def bubbleSort2() {
  var nbParcours = -1
  var N = getNombreValeurs()
  for (i <- 1 to N) {
    nbParcours += 1
    for (j <- 1 to N-1-nbParcours ) {
      if(plusPetitQue(j+1,j)) {
        echange(j+1,j)
      }
    }
  }
}