def indexOfMaxValue(nums:Array[Int]):Int = {
  var maximum = nums(0)
  var indiceMax=0
  for (i <- 0 to nums.length -1) {
    if (nums(i)>maximum) {
      maximum=nums(i)
      indiceMax=i
    }
  }
  return indiceMax
}