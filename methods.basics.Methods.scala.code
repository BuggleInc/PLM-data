def goAndGet() {
  var compte: Int = 0 
  while (!estSurBiscuit) {
    forward();
    compte = compte + 1
  }
  prendBiscuit();
  for (compteur <- 1 to compte) {
    backward();
  }
  
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}