def occurrences(nums:Array[Int],lookingFor:Int):Int = {
var n = nums.length;
var comp = 0;
for (k <- 0 to (n-1)) {
		if (nums(k) == lookingFor) {
			(comp = comp + 1);
		}
		return comp;
}
return comp;
}
