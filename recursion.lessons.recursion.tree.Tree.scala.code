def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
if(steps ==0){
avance(length)
droite(angle)
tree(3,length*shrink,angle,shrink)
}else{
	gauche(2*angle)
	tree(3,length*shrink,angle,shrink)
	
}
}
