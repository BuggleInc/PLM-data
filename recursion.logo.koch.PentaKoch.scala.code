def pentaKoch(levels:Int, length:Double) {
  if(levels==0){
    avance(length)
  }
  else{
    pentaKoch(levels-1, length*0.4)
    left(120)
    pentaKoch(levels-1, length*0.4)
    right(108)
    pentaKoch(levels-1, length*0.4)
    right(108)
    pentaKoch(levels-1, length*0.4)
    right(108)
    pentaKoch(levels-1, length*0.4)
    left(108)
    pentaKoch(levels-1, length*0.4)
  }
}