override def run() {
 var (x, y):(Int, Int) = (0, 0)
  
 for (i <- 0 to getWorldWidth()){
   for (j <- 0 to getWorldHeight()){
     if (hasBaggle(i, j)){
      	x = i
       	y = j
     }
   }
 }
  
  System.out.println(x, y)
}