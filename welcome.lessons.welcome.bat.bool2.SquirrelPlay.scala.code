def squirrelPlay(temp:Int, isSummer:Boolean):Boolean = {
  if (isSummer==true && (temp>100 || temp<59)){
    return false
  }
  if ( isSummer==false  &&(temp<59 || temp>90)){
    return false
  }
  else { 
  return true}
}