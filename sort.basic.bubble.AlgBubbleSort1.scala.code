def bubbleSort()  {
  
  var x=getValueCount()
  var i = 0
  var echange = true
  
  if(isSmaller(getValue(1), getValue(0))) {
    swap(0, 1)
  }
  
  /*
  while ((echange==true) && (i<x)){
    
    echange = false;
    
    for (j <- 0 to x-i-2){
      
      if (isSmaller(getValue(j+1), getValue(j))){
        
        swap(j,j+1);
        echange = true;
        
      }  
      
    }
    
    i=i+1
    
  }*/
  
}