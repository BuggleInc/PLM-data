override def run() {
  var  a=360.toDouble/20
  avance(30);droite(a);avance(30);droite(a);avance(30);droite(a);avance(30);droite(a);avance(30);droite(a);avance(30);droite(a);avance(30);droite(a);
    
  }