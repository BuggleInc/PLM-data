def sierpinski(level:Int, length:Double) {
	if(level==0) {return;}
	else{
		forward(length);
		right(120);
		forward(length);
		right(120);
		forward(length);
		right(120);
		forward(length/2);
		sierpinski(level-1,length/2);	
	}	
}
