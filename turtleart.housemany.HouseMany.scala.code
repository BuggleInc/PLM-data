override def run() {
  var nb_house = 4;
  var size = 30;
  
  for( i <- 1 to nb_house){
    draw_house(size);
    leveCrayon();
    droite(90);
    avance(size + 20);
    gauche(90);
    baisseCrayon();
    
  }
  
  
  
  def draw_house(size : Int) = {
  var i=0;
  
  avance(size)
  droite(30)
  for( i<- 1 to 3){
    avance(size)
  	droite(120);
  }
  
  droite(60)
  for(i <- 1 to 3){
    avance(size)
    droite(90);
  }
  }
}