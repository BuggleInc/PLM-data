def extrema(nums:Array[Int]): Int = {
  var min=nums(0)
  var max=nums(0)
  for (i<-0 to nums.length-1){
    if (min>nums(i))
    	min=nums(i)
    else if (max<nums(i))
    	max=nums(i)
  }
  return max-min
}
