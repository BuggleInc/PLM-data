def selectionSort() {
  var min=getValeur(1)
  for (i<- 1 to getNombreValeurs()-1){
    if (min>getValeur(i)){
      echange(min,getValeur(i))
    }
 
  for(j<- i to getNombreValeurs() ){
    echange(min,j)
  }
   }
}