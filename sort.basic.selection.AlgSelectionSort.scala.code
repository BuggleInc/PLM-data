def selectionSort() {
  var n = 0;
  for (i<-0 to (getNombreValeurs().toDouble/2).toInt){
    var pos = i;
    for (j<- i to getNombreValeurs()-1){
      if(plusPetit(j,pos)){
        pos = j;
      }
    }
    echange(i,pos);
  }
}