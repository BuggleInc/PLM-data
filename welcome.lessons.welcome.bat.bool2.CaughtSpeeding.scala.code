def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
	var bonus: Int = 0;
	if(isBirthday){
		bonus = 5;
	}
	if(speed > 80 + bonus){
		return 2;
	}
	else if (speed > 60 + bonus){
		return 1;
	}
	else{ 
		return 0;
	}
}
