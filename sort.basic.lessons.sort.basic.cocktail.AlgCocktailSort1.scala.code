def cocktailSort() {
	var swap=false
	do {
	for (i <- 0 to getNombreValeurs()-2) {
		if ( plusPetit(i,i+1) ) {
			echange (i+1,i);
			swap=true 	
	}
}
for (j <- getNombreValeurs()-2 to 0 by -1) {
		if ( plusPetit(j,j+1) ) {
			echange (j+1,j);
			swap=true
	
		}
		}
		}
		while( swap==true)
}
