def max1020(a:Int, b:Int):Int = {
  
  if ((a<=20 && a>=10) && !(b<=20 && b>=10))
  return a
  if (!(a<=20 && a>=10) && (b<=20 && b>=10))
  return b
  
  if ((a<=20 && a>=10) && (b<=20 && b>=10))
  {
    if (a>b)
    return a
    else
    return b
  }
  else
  return 0
}