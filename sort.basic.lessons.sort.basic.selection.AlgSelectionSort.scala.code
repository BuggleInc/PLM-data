def selectionSort() {
	var length = getNombreValeurs();
	var i = 0;
	var j = 0;
	var min = 0;
	for(i <- 0 to length - 1){
		j = i + 1;
		min = i;
		while(j < length){
			if(getValeur(j) < getValeur(min)){
				min = j;		
			}
			j += 1;
		}
		swap(i, min);
	}
}
