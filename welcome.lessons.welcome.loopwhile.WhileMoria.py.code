while(getCouleurSol()!="Color.green"):
    while(SurBiscuit()==False):
        setDirection(est)
        avance()
    if(estSurBiscuit()==True):
        prendBiscuit()
        setDirection(ouest)
    while(estSurBiscuit()==False):
        avance()
    recule()
    poseBiscuit()
    recule()