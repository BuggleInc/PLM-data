def bubbleSort()  {
  var n = getNombreValeurs()
  var parcours = false
  while (parcours==false) {
  	for (i<-0 to n-1) {
    	if (getValeur(i-1)>getValeur(i)) {
      	echange(i-1,i)
        parcours=false
      }
      else parcours=true
    }
  }
}
	