override def run() {
  avance(100.00) 
  droite(90:Double)
  avance(100:Double)
  
 
  droite(90:Double)
  avance(200:Double)
  droite(90:Double)
  avance(200:Double)
  
  droite(90:Double)
  avance(200:Double)
  droite(90:Double)
  
  avance(100:Double)
  
  droite(90:Double)
  avance(200:Double)
  recule(100:Double)
  droite(90:Double)
  avance(100:Double)
  recule(200:Double)
  
  avance(100:Double)
  droite(90:Double)
  
	}

