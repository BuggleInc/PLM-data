def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {
  var ados:Int =0
  if ((a<=19 && a>=13)) {ados=ados+1}
  if ((b<=19 && b>=13)) {ados=ados+1}
  if ((c<=19 && c>=13)) {ados=ados+1}
  if ((d<=19 && d>=13)) {ados=ados+1}
  return ados