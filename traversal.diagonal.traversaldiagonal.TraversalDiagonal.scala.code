var diag = 0;
override def run() {var count:Int=0; for (i<-0 to 6) {for (j<-0 to diag) {setPos((diag-j), j); writeMessage(count); count=count+1}; diag=diag+1}; diag=6; for (i<-1 to 6) {for (j<-0 to diag) {setPos((diag-j), j)}}
}
