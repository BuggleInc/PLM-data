
PremiereFois=True

while (not sortieTrouvee()):
    biscuit=0
    
    while (PremiereFois or not croisement()):
        PremiereFois=False

        if estSurBiscuit():
            biscuit=biscuit+1
        forward()

    if biscuit>=3:
        gauche()
        forward()
    else:
        droite()
        forward()

forward()