override def run() {
def testg(): Boolean = {
	gauche();
	if (estFaceMur()) {
		droite();
		return true;
	} else { return false}
}

def keepHandOnSideWall() {
	if (testg() && estFaceMur()) {
		droite();
		avance();
	}
	else if (!testg()) {
		gauche();
		avance();
	} else {
		avance();
	}
		}



 while (!(estSurBiscuit())) {
 	keepHandOnSideWall()
 }
    prendBiscuit();
}
