def combSort() {
  var ecart = getNombreValeurs()
  while(ecart > 1){
   ecart = (ecart.asInstanceOf[Double] / 1.3).asInstanceOf[Int]
  var i = 0
  while(i+ecart < getNombreValeurs()){
    if(plusPetit(i+ecart:Int,i:Int)){
      echange(i:Int, i+ecart:Int)
    }
    i = i+1
  }
  }
}