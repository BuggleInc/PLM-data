def estAdo(a:Int):Boolean = {
  if (a>=13 && a<=19) {
    return true
  }
  return false
}


def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {
  var g = 0
  if (estAdo(a)) { g=g+1 }
  if (estAdo(b)) { g=g+1 }
  if (estAdo(c)) { g=g+1 }
  if (estAdo(d)) { g=g+1 }
  return g
}
