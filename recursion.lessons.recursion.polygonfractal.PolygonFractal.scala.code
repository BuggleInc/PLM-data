def polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double) 
{
	if(levels==1)
	{
		avance(length)
		droite(180-(sides-2)*180)
		avance(length)
		droite(180-(sides-2)*180)
		avance(length)
		droite(180-(sides-2)*180)
		avance(length)
		droite(180-(sides-2)*180)
		avance(length)
		droite(180-(sides-2)*180)		
	}
}
