def golomb(num:Int): Int = {var T = new Array[Int](num+1);
                            var j= 2;
                            T(1) = 1;
                            for (i<- 1 to num) {var compteur = 0; while (compteur < T(i) && j<num) {j= j+1;T(j) = i; compteur = compteur+1} } ; return T(num)
}