override def run() {
	baisseBrosse()
	var b : Boolean = false
	while (!b) {
	while(!estFaceMur)
	{avance()}
	if (getDirection() == Direction.EST) {gauche();avance()}
	if (getDirection() == Direction.OUEST) {droite();avance()}
	
}}
