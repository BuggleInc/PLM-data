public void run() { 
	int nBaggles = 0, nOrange = 0;

	while (nBaggles * 2 < nOrange +1) {
		forward();
		if (isOverBaggle())
			nBaggles++;
		if (getGroundColor() == Color.orange)
			nOrange++;
	}
}
