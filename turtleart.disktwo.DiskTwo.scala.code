override def run() {
  for (j<-1 to 18) {
    if (j%2==1){
    	setColor(Color.black);
    }
    else {
      setColor(Color.red)
    }
  	for (i<-1 to 20) {
    	forward(100);
    	backward(100);
    	right(1);
  	}
    
  }
  
}