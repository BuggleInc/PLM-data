def cocktailSort2() {
  var deb=0
  var fin=getNombreValeurs()-2
  var tri:Boolean = false
  while (!tri) {
    tri=true
  for (i<-deb to fin) {
    if (getValeur(i+1)<getValeur(i)) {
      echange(i,i+1)
      tri=false
    }
  }
   fin=fin-1
  for (i<- fin+1 to deb+1 by -1) {
    if (getValeur(i)<getValeur(i-1)) {
      echange(i,i-1)
      tri=false
    }
  }
   deb=deb+1
 }   
}
