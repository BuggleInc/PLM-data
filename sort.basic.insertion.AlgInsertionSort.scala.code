def insertionSort() {
  var l = getNombreValeurs() -2
  var v = 0
  var j = 0
  for (i <- 1 to l) {
    j = i
    v = getValeur(i)
    while (v < getValeur(j)) {
      setValeur(j-1,getValeur(j))
      j -= 1
    }
  	setValeur(j,v)
  }
}