var nbBiscuits = 0;

while (!sortieTrouvee())
{
  nbBiscuits = 0;

  do
  {
    avance();
    if (estSurBiscuit()) nbBiscuits+=1;
  }
	while (!crossing())

  if (nbBiscuits >= 3) gauche();
  else droite();
}

avance();