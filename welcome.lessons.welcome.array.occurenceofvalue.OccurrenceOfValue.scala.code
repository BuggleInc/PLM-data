def occurrences(nums:Array[Int],lookingFor:Int):Int = {

    var i = 0;
    var aux=0;
 
        while( i <  nums.length) {
     	       
     	       if(nums(i)== lookingFor) aux = aux + 1;
                 i=i+1;
 	
 	                      }
        if(aux==0) return 0;
 	       else return(aux);}