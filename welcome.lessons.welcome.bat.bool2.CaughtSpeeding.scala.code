def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
                var x=0
                var vi=speed
                if (isBirthday) vi=vi-5
                if (vi>60) {
                               x=1
                               if (vi>81) x=2
                }
                return x
}
