def stringYak(str:String):String = 
{
  var s=""
  var i=0;
  while(i<=str.length()-1)
  {
    if(str.drop(i).take(3)!="yak")
    {
      s+=str.drop(i).take(3)
      i=i+2
    }
    i+=1
  }
  
  return s
}