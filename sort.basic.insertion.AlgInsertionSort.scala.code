def insertionSort() {
  var a:Int=0;
  var v:Int = 0;
  for (i<- 1 to getNombreValeurs -1) {
    v=getValeur(i);
    a=i-1;
    var j:Int =i-1
    do {
      if (!plusPetitQue(a,v)){
        copie(a,a+1);
        a=a-1;
      }
      j=j-1 ;
     }
    while (a==j && j>0)
    }
    setValeur(a,v)
  }
