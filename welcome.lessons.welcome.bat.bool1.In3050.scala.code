def in3050(a:Int, b:Int):Boolean = {
  if ((estDans(a,30,40))&&(estDans(b,30,40))){
    return true}
  if ((estDans(a,40,50))&&(estDans(b,40,50))){
    return true}
  return false}

def estDans(a:Int,b:Int,c:Int):Boolean={
  return (a>=b)&&(a<=c)
  
}