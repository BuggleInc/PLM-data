def array123(nums:Array[Int]): Boolean = {
  for(i <- 0 to nums.length){
    if(i < nums.length - 3){
      if((nums(i) == 1) && (nums(i+1) == 2) && (nums(i+2) == 3)){
        return true
      }
    }
  }
  return false
}