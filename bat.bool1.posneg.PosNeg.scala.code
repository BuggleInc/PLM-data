def posNeg(a:Int, b:Int, negative:Boolean):Boolean = {
  (a,b,negative) match {
    case (i,y,false) if (i<0 && y>0) || (i>0 && y<0) => return true
    case (i,y,true) if (i<0 && y<0) => return true
    case _ => return false
  }
}