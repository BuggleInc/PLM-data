override def run() {  avance(80:Double)
  droite(50:Double)
  avance(80:Double)
  droite(50:Double)
  avance(80:Double)
  droite(50:Double)
  avance(80:Double)
  droite(50:Double)
  avance(80:Double)
  droite(50:Double)
  avance(80:Double)
  droite(50:Double)
   avance(80:Double)
  droite(50:Double)
}