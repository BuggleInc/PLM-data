def sierpinski(level:Int, length:Double) {
  if(level==0) { return avance(length)}
	else {
    sierpinski(level, length*0.5)
    right(120)
    sierpinski(level, length*0.5)
    right(120)
    sierpinski(level, length*0.5)
    right(120)
  }
}