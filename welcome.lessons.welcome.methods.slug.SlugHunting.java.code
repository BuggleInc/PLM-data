public void hunt() {
	while(!isOverBaggle()){
	while (isFacingTrail()){
		brushDown();
		forward();
		brushUp();
	}
	while (!isFacingTrail()) {
		right();
	}}
    pickupBaggle();
} 

boolean isFacingTrail() {
	boolean stillOnTrail = false;
	if (!isFacingWall()){
	forward();
	if (getGroundColor().equals(Color.green)) {
		stillOnTrail = true;
	}
	backward();}
    	return stillOnTrail;
}