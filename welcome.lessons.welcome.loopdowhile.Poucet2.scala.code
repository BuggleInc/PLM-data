do {
  var nbBiscuits: Int=0
  do{
    if (estSurBiscuit) {
      nbBiscuits = nbBiscuits + 1
    }
    avance(); 
  } while (!crossing());
    
  if (nbBiscuits >= 3) {
       gauche();
  } else {
       droite();
  } 
} while (!sortieTrouvee)
avance();