def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {

	if(steps != 2)
	{
		gauche(angle);
		avance(length);
		droite(angle);
		tree(steps + 1, length, 360 - angle, shrink);
	}
	else
	{
		
		tree(1, length * shrink, 360 - angle, shrink);
		
	}

}
