def remove(l:List[Int], v:Int): List[Int] = {
  
  if(l == Nil){
    
    return Nil;
    
  } else if(l.head == v){
    	
    				return remove(l.tail, v);
    
  			 } else{
           
    				return l.head::remove(l.tail, v);       
    
         }
  
}
