def bubbleSort2() {
  var k: Int = getNombreValeurs()-1
	for (j<-0 to getNombreValeurs()-1) {
    for(i<-0 to k-1) {
      if (!plusPetit(i,i+1) ) {
        echange(i,i+1);
      }
    }
    k=k-1;
  } 
}
