def noTriples(nums:Array[Int]): Boolean = {
  var un =19
  var deux = 27
  for ( i <- 0 to nums.length - 1){
    if (un == deux){
      if (deux == nums(i)){
        return true;
      }
      else{
        un = deux;
        deux = nums(i);
      }
    }
  }
  return false
}