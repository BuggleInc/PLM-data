count=0
cmop=0
while isOverBaggle()!=1:
    count=count+1
    forward()
    while isOverBaggle()==1:
        pickupBaggle()
        setDirection(Direction.SOUTH)
        while cmop!=count:
            cmop=cmop+1
            forward()
    dropBaggle()
setDirection(Direction.NORTH)
