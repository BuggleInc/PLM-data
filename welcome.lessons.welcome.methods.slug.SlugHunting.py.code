# Copy your isFacingTrail, and write your code below
def Mark():
    brushDown()
    brushUp()

def isFacingTrail():
    if not isFacingWall():
        forward()
        getGroundColor()
        if getGroundColor() != Color.green:
            backward()
            left()
            return False
        else:
            backward()
            return True
    elif not isOverBaggle() and not isFacingWall(): 
        left()
        return False
def hunt():
    while not isOverBaggle():
        if isFacingTrail():
            forward()
            Mark()
        isFacingTrail()

hunt()
pickupBaggle()

