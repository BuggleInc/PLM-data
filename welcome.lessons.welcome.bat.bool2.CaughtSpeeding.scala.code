def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
  var limite:Int=0
  if (isBirthday){
    limite = limite + 5
  }
  speed match{
    case a if a<=60+limite => 0
    case a if 61+limite<=a && a<=80+limite => 1
    case _ => 2
  }
}
