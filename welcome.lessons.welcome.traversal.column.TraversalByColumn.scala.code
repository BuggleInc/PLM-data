override def run() {
  var cpt:Int=0
  for(x<-0 to getMondeLargeur()-1){
    col(x,cpt)
  }
}

def col(x:Int,cpt:Int):Int={
  for (y<-0 to getMondeHauteur()-1){
    setPos(x,y)
    cpt=cpt+1
    ecritMessage(""+cpt)
  }
  return cpt
}