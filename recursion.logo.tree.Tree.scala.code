def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  
	if (steps==0){
    cache()
    gauche(angle)
   	recule(length)
   	gauche(angle)
    avance(length)
  }
  else {
    if(steps==0){
      
    }
    avance(length)
    droite(angle)
  	
  	
  	current(steps)
  	tree(steps-1,length*shrink,angle,shrink)
    
  }
  	
  	
 
}