def insertionSort() {
  for( i <- 1 to getNombreValeurs()-1){
		var v=getValeur(i);
    var stop=false;
		for(a <- i-1 to 1 by -1){
      if(stop!=true){
        if(!plusPetitQue(a, v)){
          var temp=getValeur(a)
          setValeur(a-1, temp)
        }
      }
    }
  }
}