def insertionSort() {
  var k = 0
  var v = 0
  for (i<- 1 to getNombreValeurs() -1) {
    k=1
    v=getValeur(i)
    while(plusPetit(i+k+1,i+k) && i+k+1<getNombreValeurs()) {
      copie(i-k-1,i-k)
    }
    setValeur(i-k,v)
  }
}
