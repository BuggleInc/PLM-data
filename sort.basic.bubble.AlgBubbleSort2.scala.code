def bubbleSort2() {
  

  var n:Int = getNombreValeurs()
  for (i <- 0 to n-2 ){
    
    for (j <- 0 to n-2-i){

      if (plusPetit(j+1, j)){
        var tampon:Int = getValeur(i)
        setValeur(i, getValeur(i+1))
        setValeur(i+1, tampon)

      }
    
    }
  }
}