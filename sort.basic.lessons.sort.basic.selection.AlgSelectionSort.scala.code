def selectionSort() {
  for (i <- 0 to getNombreValeurs()) {
    var min = getValeur(i)
    var ind = i
    for (j <- i to getNombreValeurs()-1) {
      if (getValeur(j)<min) {
        min = getValeur(j)
        ind = j
      }
    }
    echange(i,ind)
  }
}

