while (!sortieTrouvee()) {
  var nbBaggle : Int = 0
  do {
    avance() ;
    if (estSurBiscuit()) {
      nbBaggle = nbBaggle + 1
    }
  } while (crossing())
  if (nbBaggle <= 2) {
    droite()
  } else {
    if (nbBaggle >= 3) {
      gauche()
    }
  }
} 
avance()