public void run() { 
	boolean done = false;
	
	while (!isFacingWall())
		forward();

	while (!done) {
		left();
		forward();
		right();
		forward();
		if (!isFacingWall())
			done = true;
	}

	forward(3);
}
