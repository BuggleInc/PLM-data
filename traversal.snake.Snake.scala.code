override def run() {
  baisseBrosse()
	var x = 0
  while (x==0) {
    if (estFaceMur) {
    		if (getDirection()==Direction.EST) {
          gauche()
          if (estFaceMur()) {
            x=1
          } else {
          avance()
          gauche()
          }
        } else if (getDirection() == Direction.OUEST) {
          droite()
          avance()
          droite()
        } 
  	} else {
    	avance()
  	}
  }
}

