override def run() {
  for (cote <- 1 to 8){ //tour
    avance(50)
    droite(45)
  }
  avance(25)
  droite(45)
  for (branche <- 1 to 8){ //etoile 
    avance(25)
    gauche(45)
    avance(25)
    droite(90)
  }
}