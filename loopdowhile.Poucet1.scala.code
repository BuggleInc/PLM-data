var x: Int = 0


do {   
  x = 0;
  for (compteur <- 1 to 5) {
    avance();
    if (estSurBiscuit()) {
      x = x + 1; }
  }


  if (x>=3) {
    gauche();
  }
  else {
    droite();
  }
} while (!sortieTrouvee())
avance();
