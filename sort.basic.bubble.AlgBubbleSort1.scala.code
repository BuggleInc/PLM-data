def bubbleSort() {
  var echangeDernier:Boolean = false
  do {
    echangeDernier = true
    for (i <- 0 to getNombreValeurs() - 2) {
      if(getValeur(i) > getValeur(i + 1)) {
        echange(i, i + 1)
        echangeDernier = false
      }
    }
  } while(!echangeDernier)
}