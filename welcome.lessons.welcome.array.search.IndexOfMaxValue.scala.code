def indexOfMaxValue(nums:Array[Int]):Int = {
  var max:Int = nums(0)
  var pos:Int = 0
  for (i <- 0 to (nums.length-1) ) {
    if (max<nums(i)) {
      max = nums(i)
      pos = i
    }
  }
  return pos
}