def bubbleSort()  {
  var tri:Boolean = true
  while (tri) {
    tri=false
  for(i<-1 to getNombreValeurs()-3) {
      if(getValeur(i)<getValeur(i-1)) {
        echange(i,i+1)
        tri=true
      }
    }
  }
}