override def run() {
	var i=getMondeHauteur();var j=getMondeLargeur();
	var tab = new Array[Color] (i);
	def marque(c:Color) {
	setCouleurBrosse(c);baisseBrosse();leveBrosse();
     }
	for (x<-0 to i-1) {
		tab(x)=getCouleurSol();
		avance();
	}
	for (y<-0 to j-2) {
		avance();gauche();avance();droite();
		for (z<-0 to i-1) {
		marque(tab(z));avance();
	     }
	}
}