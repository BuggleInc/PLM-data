override def run() {
	for (i<-1 to 90) {
	avance(100);
	recule(100);
	droite(1); 
	}
	leveCrayon;
	setCouleur(Color.red);
	baisseCrayon;
	for (i<-1 to 90) {
	avance(100);
	recule(100);
	droite(1);
	}
	leveCrayon();
	setCouleur(Color.yellow);
	baisseCrayon();
	for (i<-1 to 90) {
	avance(100);
	recule(100);
	droite(1);
	}
	leveCrayon();
	setCouleur(color.pink);
	baisseCrayon();
	for (i<-1 to 90) {
	avance(100);
	recule(100);
	droite(1);
	}
}
