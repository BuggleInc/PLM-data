def in3050(a:Int, b:Int):Boolean = {

	return (appartient(a, 30, 40) && appartient(b, 30, 40)) || (appartient(a, 40, 50) && appartient(b, 40, 50));
}

def appartient(a:Int, x:Int, y:Int):Boolean = 
{
	return (a >= x) && (a <= y);
}
