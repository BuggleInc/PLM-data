override def run() {
  avance(100)
  droite()
  avance(100)
  droite()
  avance(100)
  droite()
  avance(100)
  droite(180)
  avance(100)
  droite()
  
  avance(100)
  droite()
  avance(100)
  droite(180)
  avance()
  
  droite()
  avance(100)
  droite()
  avance(100)
  droite()
  avance(100)
  droite(180)
  avance(100)
  droite()
  
  avance(100)
  droite()
  avance(100)
  
  droite()
  avance(100)
  droite(180)
  
}