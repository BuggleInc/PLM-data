def alarmClock(day:Int, vacation:Boolean): String = {
	if (vacation==true && {day==6 || day==5})
	return "off"
	else
	if (vacation==true)
	return "10:00"
	else
	if (day==6 || day==5)
	return "10:00"
	else 
	return "7:00"
}
