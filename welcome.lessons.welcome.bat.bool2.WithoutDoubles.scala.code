def withoutDoubles(die1:Int, die2:Int, noDoubles:Boolean):Int = {
	var die = die1
	if ((die==die2) && (noDoubles)) {
		die = die + 1
		if (die1==6) { 
			die1 == 1
		}
		if (die2==6) {
			die2 == 1
		}
		return (die1 + die2);
	}
	return (die1 + die2);
}