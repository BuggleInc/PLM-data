def bubbleSort()  
{
  for(i<-0 to (getNombreValeurs()-2)) 
  //on prend chaque élément de la liste pour le remonter
  	{
      for (j<-1 to (getNombreValeurs()-i))
      //chaque élément va être comparé lgr-i fois
      	{
      		if (plusPetit(getValeur(j),getValeur(i)))
      			{
          		echange(i,j)
        		}
    		}
		}
}