right();
right();
while (!isFacingWall() || isOverBaggle()) {
  if (isOverBaggle && !isCarryingBaggle) {
    pickupBaggle();
    while (!isOverBaggle()) {
      backward();
    }
    forward();
    dropBaggle();
    forward();
  } else if (!isOverBaggle) {
    forward();
  }
}
right();
forward();
left();
forward();
