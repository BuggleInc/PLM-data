def solve() {
  val lgr:Int = getTaillePile()
  while(!estTriee()) {
    var largeur:Int = getRayonCrepe(0)
    var k:Int = 0
    while(getRayonCrepe(k)<largeur) {
      k += 1
    }
    retourne(1)
    retourne(k-1)
  }
}