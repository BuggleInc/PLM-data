def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
var s= steps
var l=length
var x=getX()
var y=getY()
var n=0

	if(s!=3){
		var xn=getX()
		var yn=getY()
		current(s)
		avance(l)
		droite(angle)
		n=n+1
		tree(s-1,l*shrink,angle,shrink)
	}
	else{
		droite(angle)
		
	}
}
