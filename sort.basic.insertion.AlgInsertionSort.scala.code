def insertionSort() {
  var lgr:Int=getNombreValeurs()
  var a:Int=0
  for (i<-1 to lgr-1) {
    var v:Int=getValeur(i)
    var i2=i
    while (i2 > 0 && plusPetit(v,i2-1)) {
      copie(i2-1,i2)
      i2-=1
    }
   	copie(v,i)
 	}
}