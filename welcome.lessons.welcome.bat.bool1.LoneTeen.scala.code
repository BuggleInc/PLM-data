def loneTeen(a:Int, b:Int):Boolean = {
  if ( ( verif(a) && !verif(b) ) || ( !verif(a) && verif(b) ) ){
    return true;
  } else { 
    return false;
  }
}
  
  
def verif(a:Int):Boolean = {
  if ( a<=19 && a>=13 ){
    return true;
  } else {
    return false;
  }
}
