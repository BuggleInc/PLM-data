def bubbleSort2() {
  var trie = false
  while(!trie){
    trie = true
    for (i <- getNombreValeurs() to 0 by -1; j <-0 to i-1){
      
      if (getValeur(i) > getValeur(i+1)){
        echange(i,i+1)
        trie = false
      }
      
    }
    
  }
  
}