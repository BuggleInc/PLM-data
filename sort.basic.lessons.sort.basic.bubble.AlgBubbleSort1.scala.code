def bubbleSort()  {
  var n = getNombreValeurs()
  var parcours = false
  while (parcours==false) {
  	for (i<-0 to n-1) {
    	if (getValeur(i)>getValeur(i+1)) {
      	echange(i,i+1)
        parcours=false
      }
      else parcours=true
    }
  }
}
	