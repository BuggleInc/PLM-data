override def run() {
  while (!estSurBiscuit())
  	keepHandOnSideWall()
  prendBiscuit()
}

def keepHandOnSideWall() {
  var dir=getDirection()
  gauche()
  if (estFaceMur()) {
  	droite()
    if (estFaceMur())
    	droite()
  }
  else
  	avance()
}