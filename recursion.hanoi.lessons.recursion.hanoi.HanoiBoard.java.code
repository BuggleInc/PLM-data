public void hanoi(int height, int src, int other, int dst) {
//  if (getTaillePiquet(src)==1)
  if (height==1)
    deplace(src,dst);
  else {
    hanoi(height-1, src, dst, other);
    deplace(src,dst);
    hanoi(height-1, other, src, dst);
  }
}
