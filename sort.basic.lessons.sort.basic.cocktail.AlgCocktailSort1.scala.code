def cocktailSort() {
val p=getNombreValeurs()
var swap=true
do {
	swap=false
	for (i<-0 to p-2){
		if (plusPetit(i,i+1)){
			echange(i,i+1)
			swap=true
		}else (swap=false)
	}
	for (i<-p-2 to 0 by -1){
		if (plusPetit(i,i+1)){
			echange(i,i+1)
			swap=true
		}		
	}
	
}while (swap==true)
}
