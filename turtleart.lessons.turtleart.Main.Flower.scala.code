override def run() {
	for (p<-1 to 8) {
		avance(50)
		droite(45)
	} // Contour
	avance(25)
	droite(45)
	for(i<-1 to 8) {
		avance(25)
		gauche(45)
		avance(25)
		droite(90)
	} // Intérieur
	avance(25)
	for (m<-1 to 8) {
		droite(45)
		avance(25)
		droite(45)
		avance(25)
		recule(25)
		gauche(135)
		avance(25)
	}
}
