var nbCote:Int = 0
var i:Int=0
while (nbCote<4*7){
  for (i<-1 to 4){avance()}
  gauche()
  for (i<-1 to 2){avance()}
  droite()
  for (i<-1 to 4){avance()}
  droite()
	for(i<-1 to 2){avance()}
  gauche()
  for(i<-1 to 4){avance()}
  gauche()
  nbCote = nbCote + 1
}