override def run() {
  for(i<-1 to 7*4)
  	serpent()
}

def serpent(){
  if(!estFaceMur()){
    avance()
  } else{
    gauche()
    if(getDirection==Direction.OUEST){
      avance()
      gauche()
    }
    retourne()
    if(getDirection==Direction.EST){
      avance()
      droite()
    }
  }
}