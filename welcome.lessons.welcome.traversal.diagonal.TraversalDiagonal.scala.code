override def run() {
	var hauteur: Int = getMondeHauteur();
	var largeur: Int = getMondeLargeur();
	var nbDiag: Int = 2* largeur - 1;
	var valeur: Int = 0;

	for(n1 <- 0 to hauteur - 1){
		for(n2 <- 0 to n1 + 1){
			setPos(n1, n2);
			ecritMessage(valeur);
			valeur = valeur + 1;
		}
		
	}
	
}
