def squirrelPlay(temp:Int, isSummer:Boolean):Boolean = {
	var rep = false
	if (isSummer) {
		if ( (temp >= 60 && temp <= 90) ) {
			rep = true
		}
	} else {
		if ( (temp >= 60 && temp <= 100) ) {
			rep = true
		}
	}
	return rep
}
