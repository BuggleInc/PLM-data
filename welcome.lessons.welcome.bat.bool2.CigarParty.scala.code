def cigarParty(cigars:Int, isWeekend:Boolean):Boolean = {
	(cigars,isWeekend) match {
		case(_,false)	=> if (cigars<40) false else
						if (cigars<61) true else false
		case(_,true)	=> if (cigars<40) false else true
	}
}
