def getLandingZone():
    lastPoint = getGround()[0]
    for point in getGround():
        if (point != lastPoint):
            if lastPoint[1] == point[1]:
                return (lastPoint[0],point[0])
        lastPoint = point

