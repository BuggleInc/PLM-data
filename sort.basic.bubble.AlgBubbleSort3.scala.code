def bubbleSort3() { val a = getNombreValeurs() - 1
                   var ok= true 
  for (i <- 0 to a) {
                      for (j <- i+1 to a) { 
                         if (plusPetit(j,i)) { echange(i,j)
                           ok = false } else {ok = true}
                        
                       } 
                      if (ok) return 
                     }
}

