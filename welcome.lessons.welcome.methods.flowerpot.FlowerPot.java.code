void growFlowers(){
	flower(Color.red);
	
}
void printFlowerAt(){
	findPosition(x);
	flower(
}
void twoFlowers(c1, c2){
	flower(c1);
	forward(2);
	flower(c2);
	toWall();
}
void toWall(){
	while(!backingWall){
		backward();
	}
}
void flower(Color c){
	setBrushColor(c);
	brushDown();
	forward(2);
	backward(1);
	left();
	backward(1);
	forward(2);
	backward(1);
	setBrushColor(Color.yellow);
	brushUp();
}
