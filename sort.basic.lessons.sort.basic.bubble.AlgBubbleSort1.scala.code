def bubbleSort()  {
	
	var inverse = true

	do {
		var inverse = true
		for (i <- 0 to getNombreValeurs()-2) {
				if (!plusPetit(i,i+1)) {
					echange(i,i+1)
					inverse = false
				}
		}
	} while (inverse)
}
