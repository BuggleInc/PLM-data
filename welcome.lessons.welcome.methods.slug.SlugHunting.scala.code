def hunt() {
    do {
      while ( ! isFacingTrail()) {
      gauche() 
      }
      avance()
      
    } while( ! estSurBiscuit())
  prendBiscuit()
}

def isFacingTrail():Boolean = {
    if (estFaceMur()){return false}
  	var vert = false
  	avance()
    if (getCouleurSol() == Color.green) {
      vert = true
    baisseBrosse()
  	leveBrosse()}
  	
  	recule()
  	return vert
  
}
