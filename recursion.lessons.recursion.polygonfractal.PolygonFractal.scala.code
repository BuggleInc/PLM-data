def polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double) {
	if (level == 0)
	{
		avance(length)
		droite(72)
	}
	else
	{
		sierpinski(level - 1, length /2 )
		avance(length)
		droite(120)
		sierpinski(level - 1, length /2 )
		avance(length)
		droite(120)
		sierpinski(level - 1, length /2 )
		avance(length)
		droite(120)

		
}
