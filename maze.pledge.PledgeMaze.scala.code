override def run() {
  while(!estFaceMur){
      avance()
    }
  var virages = 0
  while(!estSurBiscuit){
    gauche(); virages = virages + 1
    while (!estFaceMur){
      avance()
      droite(); virages = virages -1  
    }
    while(estFaceMur){
      gauche(); virages = virages + 1
    }
    
  }
}