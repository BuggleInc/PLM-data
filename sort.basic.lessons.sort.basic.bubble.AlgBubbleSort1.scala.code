def bubbleSort()  {
	var modifie = 0
	do
	{
		modifie=0
		for(k<-0 to getNombreValeurs()-2)
		{
			if(getValeur(k)>getValeur(k+1))
			{
				echange(k,k+1)
				modifie=1
			}		
		}
	}
	while(modifie==1)
}
