def isFacingTrail():Boolean = {
	if(!estFaceMur) {avance();
	var sol:Color = getCouleurSol();
	recule();
	return sol == Color.green;}
	else{gauche();avance();
		var sol : Color = getCouleurSol();
		recule();
		return sol == Color.green}}

def hunt() {do(avance;baisseBrosse;leveBrosse;)
		   while(isFacingTrail)
		   if(!isFacingTrail) {gauche();isFacingTrail(); 
		   if(!isFacingTrail){droite();isFacingTrail();}}

hunt()
    // Write your code here
}

// Copy your isFacingTrail() here
