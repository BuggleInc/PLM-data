def bubbleSort3()  {
  var n = getNombreValeurs();
  var c = 1;
  for ( i <- 1 to n-1){
    c = 0;
    for ( j <- 0 to n-i-1 ){
      if (plusPetit(j+1:Int,j:Int)) {
        echange(j:Int,j+1:Int);
        c += 1;
      }
    }
    if (c == 0) {
      return;
    }
  }
}
