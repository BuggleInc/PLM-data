override def run() {  
  avance(80:Double)
  droite(30:Double)
  avance(80:Double)
  droite(120:Double)
  avance(80:Double)
  droite(30:Double)
  avance(80:Double)
  droite(120:Double)
  avance(80:Double)
  droite(30:Double)
  avance(80:Double)
  droite(120:Double)
   avance(80:Double)
  droite(30:Double)
                   }