def gnomeSort() {
	val n:Int = getNombreValeurs()

	def Sort(pos:Int) {
		if (pos==0) Sort(pos+1)
		else if (pos==n) return
		else if (plusPetit(pos, pos-1)) {echange(pos, pos-1); Sort(pos-1)}
			else {Sort(pos+1)}
	}
	
	Sort(2)
}
