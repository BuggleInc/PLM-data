def hunt() {
  var c:Boolean = true
  while (c){
    if (isFacingTrail()){
      baisseBroche()
      leveBrosse()
      avance()
    }
    else{
      gauche()
      if (!isFacingTrail()){
        gauche()
        gauche()
      }
    }
  }
  
}

def isFacingTrail():Boolean = {
  var bo:Boolean = !estFaceMur()
  if (bo){
  	avance()
  	bo = (getCouleurSol()==Color.green)
  	recule()
  }
  return bo
  
}