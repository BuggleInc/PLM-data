def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
current(step)
if (steps ==0){
	return
}
else{
	
avance(length);
droite(angle);
subtree(steps,shrink*length,angle,shrink)
gauche(angle*2)
subtree(steps,shrink*length,angle,shrink)
}}