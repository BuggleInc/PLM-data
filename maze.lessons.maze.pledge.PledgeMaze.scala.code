var c=0

override def run() {
	while (!estSurBiscuit()) {
		while (!estFaceMur()) {
			avance();
		}
		Droite();
		keepHandOnSideWall()
	}
}

def keepHandOnSideWall() {
	if(!estFaceMur()) {
		Gauche();
		if(estFaceMur() && c!=0) {
			Droite();
			avance();
		} else {
			avance();
		}
	} else {
		Gauche()
		if(estFaceMur() && c!=0) {
			retourne();
			if (estFaceMur()) {
				Droite();
				avance();
			} else {
			avance();
			}
		} else {
			avance();
		}
	}
}

def Gauche() {
	gauche();
	c=c+1
}

def Droite() {
	droite();
	c=c-1
}
