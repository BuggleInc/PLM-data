override def run() {
	baisseBrosse();
while (!estFaceMur() && !(getDirection()==Direction.EST))
		{
if (!estFaceMur)
{
	avance();
}
else if (estFaceMur())
	{
	gauche();
	avance();
	droite();
	}
		}
			    }
