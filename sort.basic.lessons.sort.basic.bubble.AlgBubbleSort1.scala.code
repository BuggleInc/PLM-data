def bubbleSort()  {
  var swapped = true
	var i : Int = 0;
  while (swapped){
    swapped = false;
    for(i<-0 to length -2){
      if(tab(i)>tab(i+1)){
        val tmp = tab(i);
        tab(i)=tab(i+1);
				tab(i+1)=tmp;
        swapped = true;
      }
    }
  }
}
