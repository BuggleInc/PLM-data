def insertionSort() {
  
  for (i <- 1 to getNombreValeurs()-1) {
    
    var v:Int = getValeur(i)
    for (j <- 1 to v) {
      
      if (plusPetitQue(getValeur(v-j),getValeur(v)) == false) {
        
        copie(v-j, v-j+1)
        
      }
      
    }
    
  }
  
  
}