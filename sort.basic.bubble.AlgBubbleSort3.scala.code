def bubbleSort3() {
  var lgr = getNombreValeurs()
  var x = getNombreValeurs()-2
  var echang =1
  while(x>0 && echang == 1){
    echang = 0
    for(i <-0 to x){
   		if(plusPetit(i+1,i)){
     		echange(i+1,i)
     		echang = 1
   		}
   	x = x -1
  	}
  }
}