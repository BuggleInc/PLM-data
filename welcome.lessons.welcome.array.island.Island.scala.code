def island(num:Array[Int]): Int = {
  var taille = num.length
  var x = 0
	for (i <- 0 to taille - 2)
  
  		{
        if (num(i)< num(i+1))
        	{
            x = x +1
          }
      }
  
 
  return x }
