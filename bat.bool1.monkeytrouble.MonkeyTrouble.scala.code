def monkeyTrouble(aSmile:Boolean, bSmile:Boolean): Boolean = {
  var probleme:Boolean=false
  if (aSmile & bSmile) {
    probleme=true
  } else if (!aSmile & !bSmile) {
  	probleme=true
  }
  return probleme
}