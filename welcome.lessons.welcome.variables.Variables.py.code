steps = 0

while (isOverBaggle() == False):
    forward()
    steps = steps + 1
    if(isOverBaggle() == True):
        pickupBaggle()
        while(isCarryingBaggle==True):
            backward(steps)
            if(isBackingWall()==True):
                dropBaggle()