def cigarParty(cigars:Int, isWeekend:Boolean):Boolean = {
	(cigars,isWeekend) match {
		case (x,false) if (x>=40 && x<=60)	=> return true
		case (x,true) if (x>=40)			=> return true
		case (_,_)					=> return false
	}
}