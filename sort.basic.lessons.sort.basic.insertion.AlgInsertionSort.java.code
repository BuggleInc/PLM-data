public void insertionSort() {
	
	for(int i=1; i<getNombreValeurs()-1; i++){
		int temp = getValeur(i);
		int j=0;
		
		for(j=i; j>0; j--){
			if(plusPetit(j, j-1)){
				int t = getValeur(j);
				copie(j+1, j);
				setValeur(j+1, t);
			}
		}

		setValeur(j, temp);
	}

}
