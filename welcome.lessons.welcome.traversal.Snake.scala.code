override def run() {
	def PositionFinale():Boolean {
		((estFaceMur) && (getDirection==Direction.est))
	}
	baisseBrosse
	var y=0
	while (!PositionFinale){
		avance
		if (estFaceMur){
			if (y%2=0) {
				gauche
				avance
				gauche
			}
			else {
				droite
				avance
				droite
			}
			y=y+1
		}
	}
}