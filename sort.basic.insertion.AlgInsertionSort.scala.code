def insertionSort() {
 	val n = getNombreValeurs()
  for(i<- 1 to n-2)
  {
    if(plusPetit(i+1, i))
    {
      for(j<- 0 to i)
      {
        if(plusPetit(j, i+1))
        	echange(j+1, i+1)
      }
    }
  }
}