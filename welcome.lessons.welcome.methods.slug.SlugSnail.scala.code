def hunt() {
    	do {
     if (isFacingTrail()==true) {
     baisseBrosse;
     avance();
     leveBrosse;
	}  
	else {
	gauche();
	}
} while (!estSurBiscuit)
prendBiscuit
}

def isFacingTrail():Boolean = {
    if (estFaceMur) {
    	return false;
    }
    avance();
    var trace:Boolean=getCouleurSol()==Color.green;
    recule();
    return trace;
}        



