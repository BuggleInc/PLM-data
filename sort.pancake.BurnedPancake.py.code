n = getTaillePile()

while n > 1:
  im = 0
  rm = 0
  for i in range(n):
    r = getRayonCrepe(i)
    if r > rm:
      rm = r
      im = i
  retourne (im+1)
  retourne (n)
  retourne (n-im-1)
  n = n-1