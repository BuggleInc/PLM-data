int greenTicket(int a, int b, int c) {
	switch(countEqual){
		case 0:
			return 0;
		case 1:
			return 10;
		default:
			return 20;
	}
}

int countEqual(int a, int b, int c){
	int count = 0;
	if(a == b)
		count++;
	if(a == c)
		count++;
	if(b == c)
		count++;
	return count;
}
