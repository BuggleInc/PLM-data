def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {	

	while(steps!=0){
		var x = increment
		var longueur = length + x
		var pas =steps-1
		
		avance(longueur)
		gauche(angle)

		spiral(pas, 90, longueur, 3)
	}
	return
}

