def countTeen(a:Int, b:Int,c:Int,d:Int): Int =
{
   var nb=0;
  if(estAdo(a))nb+=1
  if(estAdo(b))nb+=1
  if(estAdo(c))nb+=1
  if(estAdo(d))nb+=1
  
  return nb
}

def estAdo(n:Int): Boolean=
{
  return n>=13 && n<=19
}