def lessBy10(a:Int, b:Int, c:Int):Boolean = {
  var x=Math.min(a,b)
  var z=Math.max(a,b)
  var w=Math.min(x,c)
  var y=Math.max(z,c)
  var d=0
  d=y-w
  if (d>=10){
    return true
  } else {
    return false
  }
}
