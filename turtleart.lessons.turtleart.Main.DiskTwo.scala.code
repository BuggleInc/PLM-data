override def run() {
	for (i <- 1 to 18) {
		if (i%2==0) {
			setCouleur(Color.red); 
		}
		else {
			setCouleur(Color.black);
		}
		for (l <- 1 to 40) {
			avance(100);
			recule(100);
			droite(.5);
		}
	}
}
