override def run() {

	for(i <- 0 to 7)
	{
		avance(50);
		droite(360.0/8);
	}
	avance(25);
	droite(45);

	for(i <- 0 to 7)
	{
		avance(25);
		gauche(30);
		avance(25);
		droite(90);
	}
	leveCrayon();
	avance(25);
	
}
