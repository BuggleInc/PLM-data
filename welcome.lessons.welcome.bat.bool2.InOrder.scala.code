def inOrder(a:Int, b:Int, c:Int, bOk:Boolean):Boolean = {
  if ( c > b ){
    if ( b > a ){
      return true;
    }
    if ( bOk == true ){
      return true;
    }
  } else {
    return false;
  }
}