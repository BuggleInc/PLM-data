def answerCell(isMorning:Boolean, isMom:Boolean, isAsleep:Boolean):Boolean = {
  if(isAsleep == false)
    if(isMorning == true)
      if(isMom == true){return true}
      else(return false)
    else(return true)
  else(return false)
}