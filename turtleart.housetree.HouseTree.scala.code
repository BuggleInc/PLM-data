override def run() {
  
  for(i<-1 to 4){
    maison()
  }
}
def maison(){
   avance(30:Double)
  droite(30:Double) 
  avance(30:Double)
  droite(120:Double)
  avance(30:Double)
  droite(120:Double)
  avance(30:Double)
  droite(180:Double)
  avance(30:Double)
  droite(90:Double)
  avance(30:Double)
  droite(90:Double)
  avance(100:Double)
  droite(30:Double)
  avance(20:Double)
  gauche(90:Double)
}