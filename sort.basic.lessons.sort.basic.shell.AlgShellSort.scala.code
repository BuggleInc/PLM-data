def shellSort()  {
  val taille=getNombreValeurs()
  var temp=0
  var j=0
  var ecart=taille/2
  for (i<-1 to taille-1){
    temp=getValeur(i)
    j=ecart
    while ((j>0)&&(getValeur(j-1)>temp)){
      setValeur(j,getValeur(j-1))
      j=j-ecart
      }
     	setValeur(j,temp) }
}