def bubbleSort()  {

	var swapped = true
	while (swapped)
	swapped=false
	for (i<- 0 to length-2)
	{
		if (getNombreValeurs(tab(i))>getNombreValeurs(tab(i+1)))
		{
			swapped=false
			tmp=tab(i+1)
			tab(i+1)=tab(i)
			tab(i)=tmp
		}
		swapped=true
	}
	
}
