def selectionSort() {
  var min = 0;
  var va = 0;
  for(i<-0 to getNombreValeurs()-2){
    min = -1;
    cel = 0;
    for(j<-i to getNombreValeurs()-1){
      va = getValeur(j);
      if(va<min || min==(-1)){
        min = va;
        cel = j;
      }
    }
    echange(i,cel);
  }
}