def array667(nums:Array[Int]): Int = {
  var count:Int = 0
  
	for (i <- 0 to nums.length) {
    if (nums(i) == 6) {
      if (!(i > nums.length - 2) && (nums(i + 1) == 6 || nums(i + 1) == 7)) {
        count += 1
      } 
    }
  }
  
  return count
}
