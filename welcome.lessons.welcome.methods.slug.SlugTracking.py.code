def isFacingTrail():
   # write your code here
    while isOverBaggle != True:
        forward()
        if (isFacingWall()):
            left()
        if (getGroundColor() != Color.green):
            backward()
            left()
            forward()
        else:
            forward()

isFacingTrail()