def bubbleSort3() {
  var ech = true
  for (i<-1 to getNombreValeurs()){
    ech = false
    for (j<-0 to getNombreValeurs-1-i){
      if (plusPetit(j+1,j)){
        echange(j,j+1)
        ech = true
      }
    }
    if (!ech){
      return
    }
  }
}
