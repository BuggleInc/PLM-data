def insertionSort() {
  
  for(i<- 1 to getValueCount()-1){
	  var lastValue = getValueCount()-1;
    var v = getValue(i);
    for(j<- 0 to i-1){
      if(getValue(i-j) > v){
	      copy(i-j-1,i-j);
	      lastValue = i-j-1; 
    	}
    }
    setValue(lastValue, v);
  }
  
}