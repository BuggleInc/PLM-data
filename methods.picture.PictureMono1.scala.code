override def run() {
  def makeV() {
    left();forward();brushDown();brushUp();backward()
    right ;forward();brushDown();brushDown();backward()
    forward();brushDown();brushUp();backward()
  }
  
}