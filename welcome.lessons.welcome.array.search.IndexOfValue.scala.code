def indexOfValue(nums:Array[Int] ,lookingFor:Int): Int = {
  var n =nums.lenght
  for ( j <- o to n){
    if(lookingFor!=nums(i)){
      return -1
    }
    else if ( lookingFor=nums(i)){
      return nums (i)
    }
    else {
      return 0
    }
  }
}