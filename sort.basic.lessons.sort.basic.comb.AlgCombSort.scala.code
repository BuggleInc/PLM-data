def combSort() {
	var ecart: Int = getNombreValeurs();
	var inverse: Boolean = false;
	do{
		if(ecart > 1){
     		ecart = (ecart.toDouble / 1.3).toInt;
		}
   		var i: Int = O;
   		while(i + ecart < lgrtant que i+ecart < lgr faire:
     si i et i+ecart doivent être inversés, le faire
     incrémenter i de 1
	while(ecart > 1 | inverse)
}
