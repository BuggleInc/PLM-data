var diag = 1;
var count:Int=1;
var y:Int=1;
override def run() {
  setPos(0,0);
  writeMessag(0);
  
  def diagonale(y:Int){
    var x:Int=0
    setPos(x,y);
    while(y!=0){
      x+=1;
      y-=1;
      count+=1;
      setPos(x,y);
      writeMessage(count);
    }
  }
}