def insertionSort() {
  for (i<-0 to getNombreValeurs()-2){
    var v=getValeur(i)
    var j=i
    while (j>0 && getValeur(j-1)>v){
      setValeur(j,getValeur(j-1))
      j=j-1
      setValeur(j,v)
    }
  }
}