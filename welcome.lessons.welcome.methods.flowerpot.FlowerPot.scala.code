def growFlowers() {
  fleur(Color.RED)
  avance(4)
  fleur(Color.CYAN)
  recule(2)
  droite()
  avance(2)
  gauche()
  fleur(Color.ORANGE)
  recule(2)
  droite()
  avance(2)
  gauche()
  fleur(Color.PINK)
  avance(4)
  fleur(Color.GREEN)
  recule(4)
  
}

def fleur(c:Color) {
  setCouleurBrosse(c)
  baisseBrosse()
  avance()
  avance()
  recule()
  gauche()
  avance()
  recule()
  recule()
  avance()
  setCouleurBrosse(Color.YELLOW)
  leveBrosse()
  droite()
  recule()
}
