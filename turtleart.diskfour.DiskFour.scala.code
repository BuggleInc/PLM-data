override def run() {
  quart(Color.BLACK); quart(Color.RED); quart(Color.YELLOW); quart(Color.PINK);
}



def quart(c) {
  for (i<- 1 to 90) {
    ligne(c); right(1);
  }
}

def ligne(c) {
  setCouleur(c);forward(100); backward(100);
}