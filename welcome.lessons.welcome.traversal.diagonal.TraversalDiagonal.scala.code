var diag = 0;
override def run() {
ecritMessage(diag)
	for (i <- 1 to 7){
		for (i <- 1 to diag+1){
			ecritMessage(diag)
			setPos(getX()+1, getY()+1)
		}
		setPos(0, getY()+diag+1)
		diag=diag+1
		
		
	}
}
