def hunt() {    
	while (isFacingTrail) {
		baisseBrosse()
		avance()
		leveBrosse()
	}
	if (!isFacingTrail && !estFaceMur) {
		droite()
		avance()
	}
}

def isFacingTrail():Boolean = {
	if (estFaceMur) {
		return false;
	} else {
		avance()
		if (getCouleurSol() == Color.green) {
			recule()
			return true;
		}
		recule()
		return false
	}
}        

