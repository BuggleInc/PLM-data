override def run() {
  while(!estSurBiscuit()){
  	if(!estFaceMur())
    	avance()
    longeGch()
	}
  prendBiscuit()
}

def longeGch(){
  gauche()
  if(!estFaceMur())
    avance()
  else {
    droite()
    while(estFaceMur())
      droite()
    avance()
       }
}
