def tri(t:Array[Int]) {
	var i,j,k:Int=0;

	for (x<- 0 to t.length-1) {
		for (z<-x+1 to t.length-1) {
			i=t(x);
			j=t(z);
			if (i>=j) {
				k=i;
				i=j;
				j=k;
			}
		}
	}
}

def maxValue(nums:Array[Int]): Int = {
	tri(nums);
	return nums(nums.length-1)
}
