def clearMoria(){
  droite();droite();
  while(!estFaceMur()){
    while( !estSurBiscuit() && !porteBiscuit() && !estFaceMur()){
      avance();
    }
    prendBiscuit();droite();droite()
    while(!estSurBiscuit() && porteBiscuit() && !estFaceMur()){
      avance();
    }
    recule();poseBiscuit();droite();droite();avance();
  }
  setPos(getX() + 1,getY() - 1)
}


clearMoria();