override def run() {
    def makeV() {
    left();forward();brushDown();brushUp();backward()
    right() ;forward();brushDown();brushUp();backward()
    right();forward();brushDown();brushUp();backward()
  }
  
  
  
  def makePattern(){}
  forward(3);left();makeV()
  for i<-1 to 3 {
  forward(3);left();forward(3);left();makeV() 
  
    forward(3);left()}
}
  
  for k<-1 to 4 {
    for j<-1 to 4 {makePattern() ,forward(7)}
    forward(6); left()
  }
  
}