def insertionSort() {

	var v = 0
	var cpt = 1

	for (i<-1 to getNombreValeurs()-1) {
		v=getValeur(i)
		cpt=i
			while (cpt>0 && !plusPetitQue(cpt-1, v)) {
			copie(cpt-1, cpt)
			cpt-=1
			}
		setValeur(cpt, v)
	}
	
}
