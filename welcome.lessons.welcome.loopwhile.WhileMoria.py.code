while isBackingWall()==0:
 while isOverBaggle()==0:
     if isBackingWall()==0:
      backward()
     else:
      left()
      forward()
      right()
      backward()
      right()
      right()
      break
     break
    
 else:
     pickupBaggle()
     while isOverBaggle()==0:
       forward()
     else:
       backward()
       dropBaggle()
       backward()

  
