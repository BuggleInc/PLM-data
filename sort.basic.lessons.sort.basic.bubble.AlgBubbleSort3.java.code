public void bubbleSort3() {
	 boolean swapped = false;
	 for(int i = getValueCount() - 1; i > 0; i--){ 
	 	swapped = false;
	 	for(int j = 0; j < i ; j++)
	 		if(isSmaller(j + 1,j)){
	 			swap(j,j+1);
	 			swapped = true;
	 		}
	 	if(!swapped) break;
	 }
	 
}
