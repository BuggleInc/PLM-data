def growFlowers() {def makeFlower(c:Color) {for (i<-1 to 4) {setBrushColor(c); right(); brushDown(); brushUp(); forward(); left(); forward(); left()}; forward(); setBrushColor(Color.YELLOW); brushDown(); brushUp(); backward()}
  
  
  def makepattern() {
makeFlower(Color.RED); forward(); forward(); forward(); forward(); makeFlower(Color.CYAN); right(); forward(); forward(); forward(); forward(); left(); makeFlower(Color.GREEN); backward(); backward(); backward(); backward(); makeFlower(Color.PINK); forward(); forward(); left(); forward(); forward(); right(); makeFlower(Color.ORANGE); backward(); backward(); left(); backward(); backward(); right()}
makepattern()                   
for (i<-1 to 6) {forward()}; left(); forward(); forward(); right(); makeFlower(Color.BLUE); forward(); forward(); left(); forward(); forward(); right(); makepattern(); right(); forward(); left(); for (i<-1 to 8) {backward()}; right(); for (i<-1 to 4) {forward()}; right(); right(); makepattern()                   
for (i<-1 to 6) {forward()}; left(); forward(); forward(); right(); makeFlower(Color.BLUE); forward(); forward(); left(); forward(); forward(); right(); makepattern()            }