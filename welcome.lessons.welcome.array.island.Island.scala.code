def island(num:Array[Int]): Int = {
	var levelNb = 0
	var level = 0
	for (i<-0 to num.length) {
		while (num(i) != 0) {
			if ((level==0 && num(i)==1) || (level==1 && num(i)==2)) level +=1
			i +=1
		}
		levelNb +=level
		level=0
	}
	return levelNb
	
}
