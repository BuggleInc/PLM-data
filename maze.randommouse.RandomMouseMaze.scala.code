override def run() {
	var x=random3()
  if(x==0 && !estFaceMur()){
    avance()
  }
  else if (x==1 && !estFaceMur()){
    gauche()
    avance()
  }
  else if (!estFaceMur()){
    droite()
    avance()
  }
}
