override def run() {
	octogone();
	avance(25);
	droite(45);
	deuxiemeLigne();
}
def ligne() {
	avance(50);
	droite(45);
}
def octogone() {
	for (i<- 0 to 7) ligne();
}
def deuxiemeLigne() {
	for (i<- 0 to 7) {
		avance(25);
		gauche(45);
		avance(25);
		droite(90);
	}
}
