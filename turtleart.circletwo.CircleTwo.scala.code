override def run()
{
  val rayon = 57.2;
  
  droite(90);
  leveCrayon();
  avance(rayon);
  baisseCrayon();
  cercle(rayon);
  
  leveCrayon();
  avance(rayon);
  baisseCrayon();
  cercle(rayon*2);
  
  leveCrayon();
  gauche(180);
  avance(rayon*2);
  droite(90);
}