def isFacingTrail():Boolean = {
  if (estFaceMur()) {
      gauche();
    	return false;
  } else {
  	avance();
    var couleur = getCouleurSol()
    if (couleur == Color.green) {
  		return true;
    }
  }
}