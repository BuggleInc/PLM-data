def arrayFront9(nums:Array[Int]): Boolean = {
  var cpt:Int = 1
  for (i <- 0 to (nums.length - 1)) {
    if ((nums(i)==9) && (cpt <= 4)) {
      return true
    } else {
      cpt = cpt + 1
    }
  }
  return false
}