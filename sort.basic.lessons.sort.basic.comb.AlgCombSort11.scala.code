def combSort11() {
  var ecart:Int = getNombreValeurs()
  var inv:Boolean = false
  do{
    if (ecart>1){
      ecart = (ecart.asInstanceOf[Double] / 1.3).asInstanceOf[Int]
    }
    if (ecart == 9 || ecart == 10){ecart=11}
    var i:Int =0
    inv=false
    while (i + ecart < getNombreValeurs()){
      if (plusPetit(i + ecart,i)){echange(i+ecart,i);inv=true}
      i+=1
    }
  }while(ecart > 1 || inv)
}
