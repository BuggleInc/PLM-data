def solve() {
  var afterBlue = 0;
  var whiteRed, beforeWhite = getSize-1;
  
  while(afterBlue <= beforeWhite){
  	var colorAB = getColor(afterBlue);
    
    if(colorAB == RED){
      swap(afterBlue, beforeWhite);
      swap(whiteRed, beforeWhite);
      beforeWhite -= 1;
      whiteRed -= 1;
    }
    if(colorAB == WHITE){
      swap(afterBlue, beforeWhite);
      beforeWhite -= 1;
    }
    if(colorAB == BLUE){
      afterBlue += 1;
    }
    
  }
}


def ordre(x:Int, y:Int) : Boolean = {
  var A = getColor(x);
  var B = getColor(y);
  
  if (A == B )
   	return true;
  else{
  	if( A == "BLUE" || B == "RED" )
    	return true;
  	else{
      if (A == "WHITE")
      	return true
      else
      	return false
    }
  }
}