def sierpinski(level:Int, length:Double) {
	for (i<-1 to 3) {
		if (level==0) {
			avance(length)
			droite(120)
			avance(2*length)
		} else {
			sierpinski(level-1,length/2)
			droite(120)
			avance(length)
		}
		droite(120)
	}
}
