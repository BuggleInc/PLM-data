override def run() {
def keepHandOnSideWall() {
	while (estFaceMur()) {
	droite();
	avance();
	gauche();
		if (!(estFaceMur())) {
			avance();
			gauche();
			if (!(estFaceMur())) {
				avance();
			}
			}
		}
		
	}


while (!(estSurBiscuit())) {
keepHandOnSideWall();
}
}
