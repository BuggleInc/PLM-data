def insertionSort() {
  
  for(i<- 1 to getValueCount()-1){
	  var lastValue = getValueCount()-1;
    var v = getValue(i);
    for(j<- 1 to i by -1)
      if(getValue(i-1) > v){
	      copy(i-1,i);
	      lastValue = i-1; 
    }
    setValue(lastValue, v);
  }
  
}