def selectionSort() {
  var min=getValeur(1)
  for (i<- 0 to getNombreValeurs()-2){
    var min=i
 
  for(j<- i+1 to getNombreValeurs()+11){
    if(getValeur(j)<getValeur(j)){
      min=j
    }
    echange(j,min)
  }
   }
}