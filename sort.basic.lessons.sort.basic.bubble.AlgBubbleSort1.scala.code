def bubbleSort()  {
	var inverse=false
	do {
		inverse=false
		for (i<-0 to getNombreValeurs()-2) {
		if (getValeur(i)>getValeur(i+1)) {
			echange(i,i+1)
			inverse=true	
			}
		}
	} while (inverse)		
}
