def bubbleSort3()  {
  
  var c = 1
  var continue = true
  while (continue){
      continue = false
      for (i <- 1 to getValueCount() - c){
          if (isSmaller(i,i-1)){
            swap(i, i-1)
            continue = true
          } 
      }
    c += 1
  }
}