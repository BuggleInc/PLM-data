override def run() {
  baisseCrayon()
  avance(100)
  droite(30)
  avance(100)
  for (i<- 1 to 2 ) {
  droite(120)
  avance(100)
  }
  droite(180)
  for (i <- 1 to 3) {
    avance(100)
    droite(90)
  }
}