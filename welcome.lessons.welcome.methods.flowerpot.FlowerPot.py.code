def fleur(color):
    setCouleurBrosse(color)
    baisseBrosse()
    forward(2)
    backward()
    droite()
    forward()
    backward(2)
    forward()
    setCouleurBrosse(Color.YELLOW)
    leveBrosse()


couleur=[Color.RED,Color.BLUE,Color.GREEN,Color.PINK,Color.ORANGE,Color.YELLOW]
for i in range (len(couleur)):
    color=couleur[i] 
    fleur(Color.BLUE)
    forward(3) 