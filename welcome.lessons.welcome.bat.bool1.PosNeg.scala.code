def posNeg(a:Int, b:Int, negative:Boolean):Boolean = {
	if ( (a<0)&&(b>0) ) { true } else {
		if ( (b<0)&&(a>0) ) { true } else {
			if (negative && ((a<0)&&(b<0))) { true } else {
				false
			}
		}
	}
}
