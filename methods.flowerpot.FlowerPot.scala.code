var flowerPattern:Array[Array[Int]] = Array(
	Array(0, 1, 0),
  Array(1, 0, 1),
  Array(0, 1, 0)
)

var flowerPositions:Array[Array[Int]] = Array(
	Array(2, 2),
  Array(2, 6),
  Array(4, 4),
  Array(6, 2),
  Array(6, 4)
)

def drawFlower(c:Color) {
  for(i <- 0 to 2) {
    for(j <- 0 to 2) {
      
    }
  }
}

def initialPos() {
	left()
  forward()
  righ()
}

def growFlowers() {
  initialPos()
}