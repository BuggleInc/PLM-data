override def run() {
  avance(2)
  droite()
  avance(4)
  while (!estSurBiscuit()){
    keepHandOnSideWall()
  }
  prendBiscuit()
  recule()
}

def keepHandOnSideWall(){
  gauche()
  if (!estFaceMur()){
    avance()
  } else {
    while(estFaceMur()){
      droite()
    }
    avance()
  }
}