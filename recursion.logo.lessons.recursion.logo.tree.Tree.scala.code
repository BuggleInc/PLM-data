def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  avance(length)
  droite(angle)
  if(steps==0){
    return
    
  }
  else{
    current(steps)
    return subtree()
  }
  
  gauche(2*angle)
  if 
}