override def run() {
	avance(100)
	droite(30)
	avance(100)
	droite(120)
	avance(100)
	droite(30)
	for (k<-1 to 5) {
		avance(100)
		droite(90)
	}
	gauche(60)
	
	avance(100)
	droite(120)
	avance(100)
	droite(30)
	for (k<-1 to 2) {
		avance(100)
		droite(90)
	}
}
