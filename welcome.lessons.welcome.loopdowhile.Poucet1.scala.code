
do{
var baggle = 0
var compteur = 0	
for (compteur <- 1 to 5) // compteur 5 cases
			if (estSurBiscuit())
				baggle=baggle +1
  avance()
  compteur=compteur +1
	if (baggle <=2	&& compteur =5)
		droite()
	if (baggle >=3 && compteur =5)
		gauche()
}	while (!sortieTrouvee())