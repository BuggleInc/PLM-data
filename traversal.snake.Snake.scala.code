override def run() {

while (!posiFinal) {
  if (!estFaceMur) {
    avance;
  }
  else {gauche;
       avance;
       gauche}
}
}
def posiFinal():Boolean = {
  return (estFaceMur && estDroiteMur && getDirection==Direction.EAST)
    
}

def estDroiteMur():Boolean = {
   var a:Boolean=false ;
   gauche;
   a=estFaceMur
   droite ;
  return a
}