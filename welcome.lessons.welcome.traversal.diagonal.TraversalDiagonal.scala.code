//var diag = 0;

override def run() 
{
	for(i <- 0 to getMondeHauteur()*getMondeLargeur()-1)
	{
		ecritMessage(i)

		if(getX()==0 && getY()==0)
			setPos(0,1)
		else if(estFaceMur())
			setPos(getY()+1,0)	
		else setPos(getX()+1,getY()-1)	
	}
}
