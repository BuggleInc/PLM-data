override def run() {
  while( ! positionFinale()){
    parcours()
  }
  
}


baisseBrosse()
  while( ! positionFinale()){
    serpente()
  }
  droite()
}


def positionFinale(): Boolean ={
  return getMondeHauteur()-1 == getY() && getMondeLargeur()-1 == getX() 
}

def parcours(){
  if (estFaceMur()){
    setPos(getX() +1 , 0)
    }
  } else{avance()}
}