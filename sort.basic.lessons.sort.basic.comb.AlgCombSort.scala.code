def combSort() {
	var ecart = getNombreValeurs()
     var i = 0
     var échagé = true
	while ( (ecart > 1 ) || ( échangé = true  ) ) {
		ecart = (ecart.toDouble / 1.3).toInt
		échangé = false
		while ( i+ecart < getNombreValeurs() ) {
			if (plusPetit(i,i+ecart)==false) {
				echange(i,i+ecart)
			}
			i=i+1
		}
	}
}
