def marquerSol() {
  baisseBrosse();
  avance();
  leveBrosse();
}

def motif1(c:Color) {
  setCouleurBrosse(c);
  for (compteur <- 1 to 2) {
    marquerSol();
    for (compteur2 <- 1 to 2) {
    	avance();
    	gauche();
    }
  }
}