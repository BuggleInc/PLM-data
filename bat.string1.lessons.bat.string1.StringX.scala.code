def stringX(str:String):String = {
  val n = str.length
  var s = str.charAt(0).toString
  for (i <- 1 to n-2) {
    if (str.charAt(i).toString != "x") {s = s + str.charAt(i).toString}
  }
  return s + str.charAt(n-1).toString}