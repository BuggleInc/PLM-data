def bubbleSort3() {
  var ok : Boolean = false
  var n = getNombreValeurs()-2
  do{
    ok = true
    for (i<-0 to n){
      if (getValeur(i)>getValeur(i+1)){
        echange(i,i+1)
        ok = false
      }
      
    }
    n=n-1
  } while (!ok)
}