def in1To10(n:Int, outsideMode:Boolean):Boolean = 
{
	var rep=false
	if(outsideMode == false & n <= 10 &  n >= 1)
	{
		rep = true;
		return rep;
	}
	if(outsideMode == true & ( n >= 10 ||  n <= 1  ))
	{
		rep = true;
		return rep;
	}	
	return rep;
}
