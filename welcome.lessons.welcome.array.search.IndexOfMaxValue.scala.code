def indexOfMaxValue(nums:Array[Int]):Int = {
  var max:Int = 0
  var posMax:Int = 0
  val l:Int = nums.length
  for(i<- 0 to l-1){
    if(nums((l-1)-i)>max){max=nums((l-1)-i);posMax=(l-1)-i}
  }
  return(posMax)
}