def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
	var vitbirth:Int = 0;
	if (isBirthday) vitbirth = 5;
	
	if (speed <= 60 + vitbirth) return 0;
	else if (speed > 60 + vitbirth && speed <= 80 + vitbirth) return 1;
	else return 2;
}
