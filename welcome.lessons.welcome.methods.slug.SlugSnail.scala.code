def hunt(c:Color) {
	var i:Boolean = 0
  while (i){
    if(estSurBiscuit()){
      prendBiscuit()
      baisseBrosse()
      leveBrosse()
      i=false
    }
    else if (isFacingTrail(c)){
      avance()
    }
    else{
      gauche()
      if (!isFacingTrail(c)){
        gauche();gauche()
      }
    }
  }
 }

def isFacingTrail(c:Color):Boolean = {
  var bo:Boolean = !estFaceMur()
  if (bo){
  	avance()
  	bo = (getCouleurSol()==c)
  	recule()
  }
  return bo
  
}