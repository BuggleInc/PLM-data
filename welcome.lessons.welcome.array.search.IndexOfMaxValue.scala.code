def indexOfMaxValue(nums:Array[Int]):Int = {
  var x = nums(0)
  var taille = nums.length
  var indice = 0
  for (i <- 1 to taille - 1)
  	{if (nums(i)>x)
      {
        x = nums(i)
        indice = i
      }
    }
  return indice
}