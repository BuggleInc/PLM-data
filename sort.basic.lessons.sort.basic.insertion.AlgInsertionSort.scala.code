def insertionSort() 
{
  var v = 0
  var cpt = 0
  for(i <-1 to getNombreValeurs() - 2)
  {
    copie(getValeur(i),v)
    cpt = i - 1
    while(cpt >=0 && getValeur(cpt) > v)
    {
      setValeur(cpt+1,getValeur(cpt))
      cpt -= 1
    }
    setValeur(cpt+1,v)
  }
}