def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {
  
  	if(steps == 0){
      leveCrayon();
    }
		else{
      
  		gauche(angle);
      avance(length+increment);
      spiral((steps-1),angle,(length+increment),increment);
  		
    }
}