def cocktailSort() {
  test=false
  while (!test){
    test = true
    for (i <- 1 to getNombreValeur()-2){
      if plusPetit(i+1,i){
        test = false
        echange(i+1,i)
      }
    }
    c=getNombreValeur()
    while(i!=2){
      i-=1
      if (plusPetit(i-1,i)){
        test = false
        echange(i-1,i)
    }
    }
  }
}