def combSort11() {

  
  var swapped=false
  var gap:Int=getValueCount()
  do {swapped=false
    if (gap>1) {
      
      gap = (gap.toDouble / 1.3).toInt
    if ((gap==9)||(gap==10))
      gap=11
    }
    
    var i:Int=0
    while ((i+gap)<getValueCount()) {
      if (!isSmaller(i,i+gap)) {
        swap(i+gap,i)
        swapped=true
        
        
      }
       i+=1
    }
  } while ((gap>1)||(swapped))
  
}