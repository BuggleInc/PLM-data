def withoutDoubles(die1:Int, die2:Int, noDoubles:Boolean):Int = {
	var rep = 0
	if (noDoubles) {
		if ( die1 == die2 ) {
			if (die1 + 1 == 6 ) {
				rep = die1 + 1
			}
		
		} else {
		rep = die2 + die1
	}
	} 
	else {
		rep = die1 + die2
	}
	
	return rep
}
