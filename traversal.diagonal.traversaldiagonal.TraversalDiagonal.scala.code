var diag = 0;
var count:Int = 0;
var a:Int = 1;

override def run() {
  
  avancer1()
  avancer2()  
  }
  
  
def avancer1(){
  
  for (y <- 0 to 6){
    for (x <- 0 to y){
      setPos(x,y-x);
      ecritMessage(count);
      count+=1;
    }    
  }
}

def avancer2(){
  
  setPos(1,6)
  ecritMessage(count)
  for (x <- 1 to 6 ){
    for (a <- 0 to 6-x) {   
      setPos(getX()+1,getY()-1);
      ecritMessage(count);
      count+=1;
      
    }
  }
}