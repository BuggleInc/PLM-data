def insertionSort() {
	for (i <- 1 to getNombreValeurs()-1) {
		var v:Int = getValeur(i)
		var j = i-1
		while (getValeur(j-1)<v) {
			 setValeur(j,getValeur(j-1));
			 j=j-1;
		}
		setValeur(j,v)
	}
}
