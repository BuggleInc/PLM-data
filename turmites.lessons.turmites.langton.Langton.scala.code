def step() {
  if (getCouleurSol() == Color.black) {
    marquer(Color.white)
    gauche()
    avance()
  }
  else if (getCouleurSol() == Color.white) {
    marquer(Color.black)
    droite()
    avance()
  }
}


def marquer(c : Color) {
  setCouleurBrosse(c)
  baisseBrosse()
  leveBrosse()
}
