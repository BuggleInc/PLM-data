def insertionSort() {
  for (i<- 0 to getValueCount()-2){
    if (isSmaller(i+1, i)){
      swap(i+1, i);
      var a=false;
      var j=i;
      while (!a && j!=0) {
        if (isSmaller(j, j-1)){
          swap(j-1, j);
          j-=1;
        } else {
          a=true;
        }
      }
    }
  }
}