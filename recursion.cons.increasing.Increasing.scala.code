def compare(a:Int,b:Int):Boolean = {
  
  return a<=b
}

def increasing(l:List[Int]): Boolean = {
  if(l==Nil) { return true}
  else { 
  if (!compare(l.head,(l.tail).head))
    {
      return false
    }
    else { return increasing(l.tail)}
  }
}