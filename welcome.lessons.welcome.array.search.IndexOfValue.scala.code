def indexOfValue(nums:Array[Int] ,lookingFor:Int): Int = {
  var y:Int=(-1)
  var z=0
  while (nums(z)!= lookingFor && z<=nums.length){
    z=z+1  
  }
  if (z!=0 && z<nums.length){
    y=nums(z)
  }
  return y
}
