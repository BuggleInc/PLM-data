def goAndGet() {
  var nbDeplacement = 0;
  while(!estSurBiscuit()){
    avance();
    nbDeplacement+=1;
  }
  prendBiscuit();
  for(i<-1 to nbDeplacement){
    recule();
  }
  poseBiscuit();
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}
