def hunt(c:Color) {
     while (!(estSurBiscuit())){
      if (isFacingTrail(c)) {baisseBrosse();leveBrosse();avance()}
    	else {while (!(isFacingTrail(c))) {gauche()}}
     } 
     baisseBrosse();prendBiscuit() }
 


def isFacingTrail(c:Color):Boolean = {
  if (estFaceMur()) {return false}
  else {avance()
  var res : Boolean = (getCouleurSol()==c)
  recule()
  return res}
}