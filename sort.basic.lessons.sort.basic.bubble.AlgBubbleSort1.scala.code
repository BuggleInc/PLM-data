def bubbleSort()  {
  var end:Boolean = false
  while (!end) {
    end = true
    for (i <- 0 to getNombreValeurs() - 2) {
      if (getValeur(i) > getValeur(i+1)) {
        echange(i,i+1)
        end = false
      }
    }
  }
}
