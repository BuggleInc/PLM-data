def insertionSort() {
  var j=1
  var v=0
  var a= getNombreValeurs()
  for ( i<-1 to a-1){
    v=getValeur(i); 
    j=i
    while(j>0 && (!plusPetit(j-1,v))){
      echange(j,j-1);j=j-1
      }
    setValeur(j,v)
  }}