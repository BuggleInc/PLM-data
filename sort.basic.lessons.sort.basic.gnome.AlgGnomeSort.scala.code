def gnomeSort() {

	var i:Int = 1;
	var taille = getNombreValeurs();

	while (i != taille)
	{
		
		if (i == 1 || !plusPetit(i, i - 1))
			i += 1;
		else 
		{
			echange(i, i - 1);
			i -= 1;
		}	
	}
}
