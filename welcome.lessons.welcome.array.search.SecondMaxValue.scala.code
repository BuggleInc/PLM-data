def max2Value(nums:Array[Int]): Int = {
	var n = nums.length
  var max = 0
  var max2 = 0
  for (pas <- 0 to (n-1))
  {
    if (nums(pas)>max)
    {
      max2 = max
      max=nums(pas)
    }
    if (nums(pas)>max2 && nums(pas)<max)
    {
      max2=nums(pas)
    }
  }
  return max2
}