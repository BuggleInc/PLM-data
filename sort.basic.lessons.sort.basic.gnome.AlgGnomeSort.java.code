public void gnomeSort() {
 int nb = getValueCount();
 boolean swap, back=true;
 
  do{
    swap=false;
  for(int i = 0; i<nb-2 ; i++){
    if(isSmaller(i+1,i)){
      swap(i,i+1);
      swap=true;
      i=0;
    }
  }
  }while(swap);
}