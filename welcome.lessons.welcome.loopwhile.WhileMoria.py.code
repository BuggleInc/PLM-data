while not estFaceMur() or estSurBiscuit():
    if estSurBiscuit():
        prendBiscuit()
        setDirection(Direction.WEST)
        avance()
        while not estSurBiscuit():
           avance()
        setDirection(Direction.EAST)
        avance(1)
        poseBiscuit()
        avance(1)
    else:
        setDirection(Direction.EAST)
        avance(1) 
droite()
avance()
gauche()
avance()      
    
