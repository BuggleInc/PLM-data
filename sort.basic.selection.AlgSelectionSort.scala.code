def selectionSort() {
  var i:Int=0
  
  for(i<-0 to getValueCount()-1){
    var m=i
    for(j<-i+1 to getValueCount()-1){
      if(getValue(j)<= getValue(m)){
        m=j
      }
    }
    swap(m,i);
  }
}