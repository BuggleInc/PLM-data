def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  
  right(angle);
  
  forward(length);
  tree(steps-1,length,angle);
  
  left (2*angle);
  tree(steps-1,length);
  
  backward(length);
  
}