def selectionSort()
{
  var n = getNombreValeurs()-1;
  var min = 0;
  
  for (i <- 0 to n-1)
  {
    min = i;
    
    for (j <- i+1 to n)
    {
      if (plusPetit(j,min)) min = j;
    }
    echange(min, i);
  }
}