override def run() {
	for (i<- 1 to 4){
	avance(100)
	gauche(90)
	 }
	 droite(90)
	 for (i<- 1 to 4){
	avance(100)
	gauche(90)
	 }
	 droite(90)
	 for (i<- 1 to 4){
	avance(100)
	gauche(90)
	 }

	droite(90)
	 for (i<- 1 to 4){
	avance(100)
	gauche(90)
	 }
	 droite(90)
}
