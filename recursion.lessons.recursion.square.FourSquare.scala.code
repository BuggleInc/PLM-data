val quart = 90;
val distance = 100;
override def run() {
	for(i<-0 until 4){
		square(quart*i);
	}
}
def square(angle:Double){
	setCap(angle);
	for(i<-0 until 4){
		avance(distance);
		droite(quart);
	}
}
