def bubbleSort()  { for (i <- 0 to 9) {
                      for (j <- i+1 to 9) { 
                         if (plusPetit(j,i)) { echange(i,j)
                           }
                        
                       } 
                     }
}
