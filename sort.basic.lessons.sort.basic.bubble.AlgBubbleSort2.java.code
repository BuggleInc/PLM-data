public void bubbleSort2() {
	boolean sorted = false;
	 for(int i = 0; i < getValueCount() ; i++){ 
	 	sorted = false;
	 	for(int j = getValueCount() - i - 1; j > 0; j--)
	 		if(isSmaller(j + 1,j)){
	 			swap(j,j+1);
	 			sorted = true;
	 		}
	 	if(!sorted){
	 		break;
	 	}
	 }

}
