def shellSort() {
	var gap=getNombreValeurs()/2
	for(i<-1 to getNombreValeurs()-1)
	{
		val temp=getValeur(i)
		var j=i
		while(getValeur(j-(i*gap))>0){
			setValeur(j,getValeur(j-(i*gap)))
			j=j-1
		}
		setValeur(j,temp)
	}
}
