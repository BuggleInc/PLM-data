override def run() {
  def maison(l:Int) {
  avance(l);
  droite(90);
  avance(l);
  droite(90);
  avance(l);
  droite(90);
  avance(l)
  ;droite(90)
	avance(l)
	droite(30);avance(l);droite(120)
avance(l);droite(30);avance(l);droite(90);avance(l);droite(90)
  }
  
  for (i<-0 to 3) {baisseCrayon();maison(30);droite(90);leveCrayon();avance(50);gauche(90)} 
  
  gauche(90);avance(200); droite(90);avance(75);baisseCrayon();
  maison(30);leveCrayon();droite(90);avance(150);gauche(90);baisseCrayon();maison(30);leveCrayon();avance(75);gauche(90);avance(150);droite(90)
  for (i<-0 to 3) {baisseCrayon();maison(30);droite(90);leveCrayon();avance(50);gauche(90)}
  
  gauche(90);avance(200);droite(90);avance(75)
}