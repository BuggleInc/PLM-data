def cocktailSort() {
	var swapped = true
	do{
		swapped = true
		for(k <- 0 to getNombreValeurs()-2){
			if(plusPetit(k+1,k)){
				echange(k,k+1)
				swapped = false
			}
		
		for(i <- getNombreValeurs()-2 to 0 by -1 ){
			if(!plusGrand(k+1,k)){
				echange(k,k+1)
				swapped = false
			}
		}
		
	
}while(swapped == false)
}
