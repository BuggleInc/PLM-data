def move(nbPas : Int,versLAvant : Boolean) {
  if (getDirection() == Direction.NORTH) {
    for (i <- 1 to nbPas) {
      avance()
    }
  } else {
    for (i <- 1 to nbPas) {
      recule()
    }
  }
}
