
override def run() {

	var T = new Array [Color](getWorldHeight())
	var p : Int = 0 ;
	for (i <- 0 to (getWorldHeight())) {
	if(!estFaceMur ){
		
		T(i) = getCouleurSol();
		avance();
		}
	}
	if(estFaceMur){
		for(k<-1 to getWorldHeight()){
			recule();
		
	if (estDosMur  ){
		gauche();
		avance();
		droite();
		for(j<-0 to getWorldHeight()-1){
		baisseBrosse();
		setCouleurBrosse(T(j));
		leveBrosse();
		avance();
		}
		}
		}
	
}
}