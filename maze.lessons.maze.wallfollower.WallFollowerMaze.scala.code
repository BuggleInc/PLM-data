override def run() {
	def keepHandOnSideWall() {
		if (estFaceMur()) {
			gauche() 
			if (estFaceMur()) {gauche() ; avance()}
		} else {avance()}
	}

	while (!porteBiscuit()) {
		keepHandOnSideWall()
		if (estSurBiscuit()) {prendBiscuit()}
	}
}
