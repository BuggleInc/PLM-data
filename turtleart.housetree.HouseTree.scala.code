override def run() {
  for (i<- 0 to 3){
    baisseCrayon()
    littleHouse()
    leveCrayon()
    droite(90)
    avance(50)
    gauche(90)
  }
  gauche(90)
  avance(30)
  droite(90)
}

def littleHouse(){
  avance(30)
  
  droite(30)
  avance(30)
  droite(120)
  avance(30)
  droite(120)
  avance(30)
  
  droite(180)
  
  avance(30)
  droite(90)
  avance(30)
  droite(90)
  avance(30)
  droite(90)
}