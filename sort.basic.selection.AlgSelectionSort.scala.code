def selectionSort() {
  var m = 0
  while(m<getNombreValeurs()-2) {
    var k = m
    for(i <- m to getNombreValeurs()-1) {
      if(plusPetit(i,k)) {
        k=i
      }
    }
    echange(m,k)
    m+=1
  }
}