def stringX(str:String):String = {
    
  println(str);
  
  var debut:String = "";
  var fin:String = "";
  
  var pointer:Int = 0;

  while(str.charAt(pointer)=="x"){
    pointer+=1;
    debut+="x";
      println("x");

  }
  
  
  pointer = 0;
  
  while(str.charAt(str.length-1-pointer)=="x"){
    pointer+=1;
    fin+="x";
  }
    
  var entre:String = "";
  
  for(i<-0 to str.length-1){
    if(str.charAt(i)!="x"){
      entre+=str.charAt(i);
    }
  }
  
  return debut+entre+fin;
}