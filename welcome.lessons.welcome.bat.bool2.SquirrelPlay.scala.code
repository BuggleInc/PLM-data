def squirrelPlay(temp:Int, isSummer:Boolean):Boolean = {
  if (isSummer == true && temp <= 100 && temp >= 60) {return true}
  if (isSummer == false && temp >= 60 && temp <= 90) {return true}
  return false
}