def hunt(c:Color) {
     while(isFacingTrail()){
    baisseBrosse()
    avance()
    leveBrosse()
    if(estSurBiscuit()){
       prendBiscuit()
    }
  }
}
 }

def isFacingTrail(c : Color):Boolean = {
  if(getCouleurSol() == "c"){
    avance()
    return true
  }
  while(1==1){
    while(estFaceMur()){
      droite()
    }
    avance()
    if(getCouleurSol() == "c"{
      return true
    }
    recule()
    droite()
  }
  return false
}
