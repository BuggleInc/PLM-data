def golomb(num:Int): Int = {
var suite = new Array[Int](num);
suite(0) = 1;
nbOccurs(suite, 2);

return 3;
}
def nbOccurs(table : Array[Int], num:Int){

	var count = 0;
	for(i <- 0 to table.length - 1){

		if(table(i)==num){
			count = count + 1;
		}
		
	}
	return count;
}
