def insertionSort() {
  for ( i <- 1 to getNombreValeurs()-1){
    var a = getValeur(i)
    var j = i
    while (plusPetit(j, j-1)==true && j>0){
      setValeur(j, getValeur(j-1))
    	j -= 1
    }
   	setValeur(j , a)    
  }
}