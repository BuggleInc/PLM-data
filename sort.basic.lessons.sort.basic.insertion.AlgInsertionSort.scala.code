def insertionSort() {
  var valeur = 0;
  for (i <- 1 to getNombreValeurs-1){
   	valeur = getValeur(i);
    var place = i-1;
   	while (place>=1 && getValeur(place)>valeur){
      	copie(place-1,place);
      	place=place-1;
    }
    setValeur(valeur,place)
  }
}