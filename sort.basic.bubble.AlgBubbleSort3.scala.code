def bubbleSort3()  {
  var test=true
  for(i <- 0 to getNombreValeurs()-2){
    test=false
    for(j <- 0 to getNombreValeurs()-2-i){
      if(plusPetit(j,j+1)==false){
        echange(j,j+1)
        test=true
      }
    }
    if(test==false){
      return
    }
  }
}