def decalerCol():
    gauche()
    avance()
    droite()

motif=[]
a = getMondeLargeur()-1
for i in range(a):    # on récupère le motif
    c = getCouleurSol()
    motif.append(c)
    forward()
recule(a)
# on a le motif

for i in range(a):
    decalerCol()
    decalage = int( litMessage() )  # on lit le décalage
    for j in range(len(motif)):
        b=0
        if j+decalage>len(motif):
            b=j+decalage-len(motif())
        else: 
            b=j+decalage
        c = motif[b]
        setCouleurBrosse(c)
        baisseBrosse()
        leveBrosse()
        avance()
    recule(a)