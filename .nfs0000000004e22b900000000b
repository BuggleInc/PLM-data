def isFacingTrail():Boolean = {
    // Write your code here 
var sol : Boolean = false
  
  if (!estFaceMur()) {avance()}
	if (getCouleurSol()!=Color.green) {recule()} 
  if (getCouleurSol()==Color.green) {sol = true ; recule()} 
   
  
   
return sol}        
