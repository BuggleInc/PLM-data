def max1020(a:Int, b:Int):Int = {
	(a,b) match {
		case (a,b) if ((a>=10 && a<=20) && (b>=10 && b<=20))	=> return Math.max(a,b)
		case (a,_) if (a>=10 && a<=20)					=> return a
		case (_,b) if (b>=10 && b<=20)					=> return b
		case (_,_)									=> return 0
	}
}