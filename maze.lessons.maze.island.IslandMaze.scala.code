override def run() {
  while (!estSurBiscuit) {
    if (getDirection()==Direction.NORD) {
      while (!estFaceMur) {
        avance()
      }
    }  
    keepHandOnSideWall()
    
  }
  prendBiscuit()
}
def keepHandOnSideWall() {
  gauche()
  while (estFaceMur) {
    droite()
  }
  avance()
}

