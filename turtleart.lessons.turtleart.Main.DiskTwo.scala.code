override def run() {

for (i <- 1 to 9)
	{	setCouleur(Color.black)
		for (j <- 1 to 40)
			{avance(100)
			recule(100)
			droite(0.5)}
		setCouleur(Color.red)
		for (j <- 1 to 20)
			{avance(100)
			recule(100)
			droite(1)}
		}
}
