override def run() {
	def brossage() {
		baisseBrosse();
		leveBrosse();
	}
	def faireV() {
		avance();
		brossage();
		avance();
		gauche();
		avance();
		brossage();
		droite();
		avance();
		gauche();
		recule();
		brossage();
		droite();
		avance();
		gauche();
	}
	
	for (l <- 1 to 3) { // 1 par ligne
		for (j <- 1 to 3) { // 1 case
			for (i <- 1 to 4) { faireV() }
			for (k <- 1 to 5) { avance() }
		}
		gauche();
		for (m <- 1 to 5) { avance() } //remontée de ligne
		droite();
	}
}


