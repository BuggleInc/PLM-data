def sierpinski(level:Int, length:Double) {
	
def aux (level:Int, length:Double)=
	if (level==0) {avance(length)}
	else {sierpinski(level-1, length/3)
		avance(length)
		droite(120)
		sierpinski(level-1, length/3)
		avance(length)
		droite(120)
		sierpinski(level-1, length/3)
		avance(length)
		droite(120)
		}


aux(level,length)
}




