while (!exitReached()) {
	var buggle = 0
	do {
    	if (isOverBaggle()) {
    	buggle = buggle + 1 ;
    	} 
    	forward() ;
	} while (!crossing())
	forward() ;
  if (buggle>=3) {
  	left() ; forward() ;
	}
	else right() ; forward() ;
}