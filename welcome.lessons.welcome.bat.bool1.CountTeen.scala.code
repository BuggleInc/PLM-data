def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {
  var nbrTeens:Int = 0
  val args = Iterator(a,b,c,d)
  
  for (age <- args){
    if (age <= 19 && age >= 13)
        nbrTeens = nbrTeens + 1
  }
  
  return nbrTeens
}