def insertionSort() {
  var memoire = 0
  for (i <- 1 to getNombreValeurs()-1){
    memoire=getValeur(i)
    
    var n=1
    while (i-n > 0 && getValeur(i-n)>memoire){
      echange(i-n,i-n+1)
      n=n+1
    }   
    if (memoire<getValeur(i-n)){
      echange(i-n,i-n+1)
    }
  }
}