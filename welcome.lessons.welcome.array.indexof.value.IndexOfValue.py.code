def indexOfValue(nums,lookingFor):
    i = 0
    while lookingFor != tab[i] and i<=len(tab):
        i = i +1
    if lookingFor == tab[i]:
        return i
    else:
        return-1
indexOfValue(nums,lookingFor)