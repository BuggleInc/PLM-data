def insertionSort() {
  val n = getNombreValeurs()
  for (k <- 1 to n-1) {
    var indice = 0
    var v = getValeur(k)
    for(i <- 0 to k-1) {
      if (v< getValeur(i)){
        for(l <- 0 to k-i-2) {
          echange(k-l,k-l-1)
        } 
      }
    }
  }
}
