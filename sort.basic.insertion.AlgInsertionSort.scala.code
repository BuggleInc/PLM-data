def insertionSort() {
  var v=0
  var n=getValueCount()-2
  
  for(i <- 1 to getValueCount()-1){
    v = getValue(i)
    
    while(!isSmallerThan(n,v) && n>0){
      copy(n,n+1)
      n = n-1
    }
   setValue(n,v)
  }
}