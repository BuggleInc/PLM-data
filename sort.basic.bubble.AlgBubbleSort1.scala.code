def bubbleSort()  {
  var listeTriee:Boolean=true
  var x=0
  x=getNombreValeurs
  do {
    for (i <- 0 to x-2) {
      listeTriee=true
       if (plusPetit(i+1,i)) {
         echange(i+1,i)
       } else {
         listeTriee=false
       }
    }
  } while (!listeTriee)
}