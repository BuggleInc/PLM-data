def insertionSort() {
  var lgr:Int = getNombreValeurs()
  for (i <- 1 to lgr-1) {
    var valeur:Int = getValeur(i)
    var k:Int = i
    while (getValeur(k)>=valeur && k>0) {
      copie(k-1,k)
      k -= 1
    }
    setValeur(k, valeur)
  }
}