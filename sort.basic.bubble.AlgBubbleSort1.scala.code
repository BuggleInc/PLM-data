def bubbleSort()  {
  var len=getValueCount()
  var bool=false
  var swapp=false
  while(bool==false){
    bool=true
  	for(i<-0 to len-2){
      if(!isSmaller(i,i+1)){
        swap(i,i+1)
        bool=false
      }
      }
    
  }
}
