def cocktailSort()  { val a = getNombreValeurs() - 1
                    
                       for (i <- 0 to a) {
                         for (j <- 0 to a) { 
                           if (plusPetit(i,i+1)) { echange(i,i+1)}
                           if (plusPetit(n-i,n-i-1) {echange(n-i,n-i-1)})
                        =
                       } 
                     }
}