def hunt(c:Color) {
    do {
    check
  } while (estSurBiscuit==false)
  prendBiscuit
}

def isFacingTrail(co:Color) {
  var couleur=Color.red
  if (estFaceMur()==false) {
    avance;
    getCouleurSol;
    couleur=getCouleurSol
    recule;
    if (co==couleur) {
      return true
    } else {
      return false
    }
  }
  return false
}


def check {
  if (isFacingTrail==true) {
    baisseBrosse;
    avance;
    leveBrosse;    
  } else if (isFacingTrail==false) {
      gauche;
  }
}