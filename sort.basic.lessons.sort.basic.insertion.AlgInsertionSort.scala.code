def insertionSort() {
  
  val lgr = getNombreValeurs()
  var continue = true
  var a = 1
  
  for (i <- 1 to lgr-1){
    var stockeur = getValeur(i)
    a = 1
    
    while(plusPetit(i,i-a) && continue){
      
 			continue = true
      copie(i-a,i-a+1)
      a+=1
      
      if (i-a <0){
        continue = false
      }
      
    }
    setValeur(i-a, stockeur)
  }
}