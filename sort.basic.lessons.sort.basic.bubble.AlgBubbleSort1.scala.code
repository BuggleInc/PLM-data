def bubbleSort ()  {
  val n = getNombreValeurs()
  var swapped = false
  do {
  for (k <- 0 to n-2) {
		if (!plusPetit(k,k+1)){
      echange(k,k+1)
      swapped = true
    }
    }
  } while(swapped)
}