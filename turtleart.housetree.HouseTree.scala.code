override def run() {
  for(i <- 1 to 4) {
    maison()
    droite(90)
    leveCrayon()
    avance(50)
    gauche(90)
  }
}

def maison() {
  baisseCrayon()
  for(i <- 1 to 4) {
  avance(30)
  droite(90)
  }
  avance(30)
  gauche(90)
  for(i <- 1 to 3) {
    recule(30)
    gauche(120)
  }
  droite(90)
  recule(30)
}