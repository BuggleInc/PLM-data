def cocktailSort2() {
  var continuer:Boolean = true
  var length:Int = getNombreValeurs() - 2
  var debut:Int = 0
  while(continuer){
    continuer = false
    for (i <- debut to length){
      if (!plusPetit(i,i+1)){
        echange(i,i+1)
        continuer = true
      }
    }
    for (i <- debut to length){
      if (!plusPetit(length - i,length + 1 - i)){
        echange(length - i,length + 1 - i)
        continuer = true
      }
    }
    length += -1
    debut += 1
  }
}