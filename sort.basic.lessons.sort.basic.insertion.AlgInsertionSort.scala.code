def insertionSort() {
  var i=0;
  for(i <- 0 to getNombreValeurs()-1){
    var v = getValeur(i);
    var j = i;
    while(j>0 && plusPetit(v,j-1)){
      copie(j-1,j);
      j -= 1;
    }
    setValeur(j,v);
  }
}