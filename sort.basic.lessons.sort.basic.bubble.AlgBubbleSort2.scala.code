def bubbleSort2() {
  var ech = true 
  for (j <- 1 to getNombreValeurs()){
  	for (i <- 0 to getNombreValeurs()-1 -j ){
    	if (plusPetit(i+1,i)){
    	  echange(i,i+1)
      	ech = true
      }
    }
  }
}
