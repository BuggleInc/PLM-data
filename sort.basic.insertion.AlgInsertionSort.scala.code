def insertionSort() {
  var v=0
  var n=getValueCount()-1
  
  for(i <- 1 to n){
    v = getValue(n)
    n = n-1
    
    for(j <- 1 to i){
      
      if(isSmaller(v,i-j)){
        copy(i-j,i-j+1)
      }
      else{setValue(i-j,v)
        
      }
    }
  }
}