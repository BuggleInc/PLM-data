def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  baisseCrayon()
	if (steps==0){
   
  }
  else {
 		avance(length)
    droite(angle)
    
    tree(steps-1,length*shrink,angle,shrink)
    current(steps-1)
    gauche(angle)
    gauche(angle)
    
    tree(steps-1,length*shrink,angle,shrink)
    current(steps-1)
    droite(angle)
    recule(length)
    
  }
  	
  	
 
}