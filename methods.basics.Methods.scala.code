def goAndGet() {
  var p:Int = 0;
  while(!(estSurBiscuit())){
    avance();
    p = p + 1;
  }
  prendBiscuit();
  for (i <- 1 to p){
    recule();
  }
  poseBiscuit();
	}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}