def cocktailSort2() {
  var debut = 0
  var fin = getNombreValeurs-2
  	for(i <- debut to fin) {
   	  if (!plusPetit(i,i+1)){
      	echange(i,i+1)
        fin -=1
    	}
  	}
  	for(k <- fin to debut by -1) {
    	if (!plusPetit(k-1,k)){
    	  echange(k-1,k)
        debut +=1
    	}
  	}
	}
