def cocktailSort2() {
	var b:Int=0
	var c:Int=getNombreValeurs()-2
	var x:Boolean=true
	do {x=false
		for(i<-b to c) {
			if (!plusPetit(i,i+1)){
			echange(i,i+1)
			x=true
			}}
			c -= 1
			
		for(i<-c to b by -1) {
			if (!plusPetit(i,i+1)){
			echange(i,i+1)
			x=true
			}}
			b += 1
		
		
	} 
	while(x)}
