def indexOfMaxValue(nums:Array[Int]):Int = {
  var max = nums(0)
  var position = 0
  for (i <- 1 to nums.length-1) {
		if (max < nums(i)) {
      max = nums(i)
      position = i
    }
  }
  return position
}