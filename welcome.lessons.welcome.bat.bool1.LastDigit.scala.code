def lastDigit(a:Int, b:Int):Boolean = {
  var restea:Int = a
  var resteb:Int = b
  while (restea>9) {
    restea=(restea%10)
  }
  while (resteb>9) {
    resteb=(resteb%10)
  }
  return (restea==resteb)
}