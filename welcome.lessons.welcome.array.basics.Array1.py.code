def array():
    n = 0
    brushDown()
    for b in L:
        setBrushColor(b)
        forward()
        if n == len(L)
            brushUp()
        n=+1
    
def storeArray():
    L = []
    for a in range(getWorldHeight()-1):
        L.append(getGroundColor())
        forward()
    L.append(getGroundColor())


backward(getWorldHeight()-2)
left()
forward()
right()
array()
    