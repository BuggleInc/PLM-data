override def run() {
  maison()
  
}

def maison(){
var x=35
  var i=0
  for (i <- 1 to 4){
    avance(x)
    droite(90)
  }
  avance(x)
  droite(30)
  for (i <- 1 to 3){
  avance(x)
  droite(180-60)
  }
  droite(180-30)
  avance(x)
  droite(180)
}