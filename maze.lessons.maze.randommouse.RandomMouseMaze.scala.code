override def run() {
	while (!estSurBiscuit()) {
  var a = random3(); 
    if (a==0) {
      if (!estFaceMur()) {
        avance();
      }
    } else if (a==2) {
      droite();
    } else gauche()
  } 
  prendBiscuit()
}
