def has271(nums:Array[Int]): Boolean = {
	var l = nums.length -3
	var rep = false
	for (i<- 0 to l ) {
		if (nums(i+1) == (nums(i)+5)) {
			if (nums(i+2) == (nums(i)-1)) {
				rep = true
			}
		}
	 
		
	}
	return rep
	
}
