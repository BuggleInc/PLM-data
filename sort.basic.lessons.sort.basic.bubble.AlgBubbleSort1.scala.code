def bubbleSort()  {
	var swapped = false
	do{
		for(k <- 0 to getNombreValeurs()-1){
			if(!plusPetit(getValeur(k),getValeur(k+1))){
				echange(k,k+1)
			swapped = true
		}
	}
	
}while(swapped == tru
}