def bubbleSort2()  {
  var tri : Boolean = true
  var compteur = 0
  while (tri) {
    tri = false
    for (i <- 0 to getNombreValeurs()-compteur-2) {
      if (!plusPetit(i,i+1)) {
        echange(i,i+1)
        tri = true
      }
    }
    compteur += 1
  }
}