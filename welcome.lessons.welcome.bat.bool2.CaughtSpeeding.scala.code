def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
  var vit_max = 0;
  if (isBirthday) {
    vit_max = vit_max + 5;
  }
  if (speed <= 60 + vit_max) {
    return 0;
  } else if (speed <= 80 + vit_max) {
    return 1;
  } else {
    return 2;
  }
}