        def goAndGet() {
        	for (a<-1 to 6) {avance(); if(estSurBiscuit()) {prendBiscuit();}}
        	droite();droite();
          for(b<-1 to 6) {avance(); if (estFaceMur()) {poseBiscuit();}}
        }

        for (i <- 1 to 7) {
            goAndGet();
            right();
            forward();
            left();
        }
