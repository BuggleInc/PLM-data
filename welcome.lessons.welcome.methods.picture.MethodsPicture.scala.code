override def run() {
  for (k<-1 to 3) {
  	for (i<-1 to 4) {
    	faireV(Color.YELLOW);  
      faireV(Color.RED);
      faireV(Color.BLUE);
      faireV(Color.GREEN);
      avance();avance();avance();avance();avance();
    }
		for (l<-1 to 15) {
    	recule();
  	}
    gauche();
    avance();avance();avance();avance();avance();
    droite();
	}
  
  
  
  
}

def faireV(c:Color) {
  
  baisseBrosse();
  avance();
  leveBrosse();
  avance();
  
  gauche();
  
  baisseBrosse();
  avance();
  leveBrosse();
  recule();
  
  droite();
  
  baisseBrosse();
  avance();
  leveBrosse();
  avance();
  
  gauche();  
}

