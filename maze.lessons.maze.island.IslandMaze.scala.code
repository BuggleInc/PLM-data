override def run() {
  while (!estSurBiscuit()){
    var ancien = getDirection()
    setDirection(Direction.NORTH)
    if (!estFaceMur()){
      avance();
    }
    else{
      setDirection(ancien);
    
      gauche();
      if (estFaceMur()){
        droite();
        if (estFaceMur()){
          droite();
        }
        else{
        avance();
        }
      }
      else{
        avance();
      }
    }
  }
  prendBiscuit()
}