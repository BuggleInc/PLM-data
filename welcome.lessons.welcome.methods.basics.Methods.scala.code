def goAndGet() {
  var compteurpas:Int = 0
  while (estSurBiscuit==false) {
   avance()
   compteurpas = compteurpas + 1
  }
  prendBiscuit
  retourne()
  for (compteur <- 1 to compteurpas) {
    avance()
  }
  retourne()
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}