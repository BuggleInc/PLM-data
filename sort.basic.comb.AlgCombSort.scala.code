def combSort() {
  var ecart = getNombreValeurs()
  val n = getNombreValeurs()
  var inverse = true
  var i = 0
  while (ecart > 0 && inverse) {
    if (ecart >= 1) {
      ecart = (ecart.toDouble / 1.3).toInt
    }
    inverse = false
    i = 0
    while (i + ecart < n) {
      if (getValeur(i+ecart) < getValeur(i)) {
        echange(i+ecart,i)
        inverse = true
        i += 1
      }
    }
  }
}