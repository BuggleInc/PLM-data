def insertionSort() {
  var temp = 0
  var j = 0
  for (i <- 1 to getValueCount()-1) {
    temp = getValue(i)
    j = i-1
    while (j > 0 & getValue(j-1) > temp) {
      copy(j-1,j)
      j = j-1
    }
    setValue(j,temp)
  }
}