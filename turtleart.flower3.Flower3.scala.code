override def run():Unit= {
  for(j<- 0 to 7){
    patern();
    droite(360/16);
  }
}

def patern() {
  gauche(45);
  avance(20);
  droite(90);
  avance(20);
  gauche(45);
  avance(20);
  
  gauche(90);
  avance(20);
  droite(90);
  avance(20);
  droite(90);
  avance(20);
  avance(20);
  droite(90);
  avance(20);
  droite(90);
  avance(20);
  
  gauche(90);
  avance(20);
  
  gauche(45);
  avance(20);
  droite(90);
  avance(20);
  
  droite(180);
}