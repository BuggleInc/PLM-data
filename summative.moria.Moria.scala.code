right();right()
while (!isFacingWall) {
  forward();
  if (isOverBaggle) {
    pickupBaggle()
    while (!isOverBaggle) {
      backward()
    }
    forward()
    dropBaggle()
  }
}
right();forward();left();forward()