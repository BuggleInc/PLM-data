def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
	if (length == 0) {
		return;
	}
	else {
		avance(steps);
		x = getX();
		y = getY();
		cap = getCap();
		droite(angle);
		tree(steps, length-1, angle, shrink);
		setPos(x, y);
		setCap(cap);
		gauche(angle);
	}
}
