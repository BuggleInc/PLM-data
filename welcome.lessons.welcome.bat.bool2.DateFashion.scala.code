def dateFashion(you:Int, date:Int):Int = {
	if (Math.max(you,date)>=8) {
		if (Math.min(you,date)>2) {
			return 2
		} else {
			return 0
		}
	} else {
		if (Math.min(you,date)>2) {
			return 1
		} else {
			return 0
		} 
	}
}
