override def run() {
	for(i <- 0 to 9) {
		quart(Color.black);
		quart(Color.red);
	}
}
def quart(c:Color) {
	setCouleur(c);
	for (i <- 0 to 19) {
		ligne();
	}
}
def ligne() {
	avance(100);
	recule(100);
	droite(1);
}
