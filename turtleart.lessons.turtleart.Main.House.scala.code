override def run() {

def carre(c:Double) {
	for (k <-0 to 3) {
	avance(c)
	droite(90)
	
	}

def triangle(t:Double) {
	for (k <-0 to 2) {
	droite(30)
	avance(t)
	droite(90)
	
	
	}
	droite(30)
}

}



carre(100)
allerVers(0,100)
triangle(100)
allerVers(0,0)

}
