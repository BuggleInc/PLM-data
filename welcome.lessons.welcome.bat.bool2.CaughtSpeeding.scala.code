def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
  var rep=0
  if (!isBirthday){
    if (61<=speed && speed<=80){rep=1}
    else if (speed >80){rep=2}
  }
  else {
    if (66<=speed && speed<=85){rep=1}
    else if (speed >85){rep=2}    	
  }
  return rep
}