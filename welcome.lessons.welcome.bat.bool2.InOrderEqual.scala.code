def inOrderEqual(a:Int, b:Int, c:Int, equalOk:Boolean):Boolean = {
  if(!equalOk){
    if((a < b) && (b < c)){
      return true
    }
  }
  if((a <= b) && (b <=c)){
    return true
  }
  return false
}