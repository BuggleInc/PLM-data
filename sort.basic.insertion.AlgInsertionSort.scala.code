def insertionSort() {
  var lgr=getNombreValeurs
  for (i <- 1 to lgr-1) {
    var v=getValeur(i)
    for (j <- i to 1 by -1) {
      if (plusPetit(v,j-1)) {
        copie(j-1,j)
        } else {
      }
    }
        setValeur(0,v)
  }
}