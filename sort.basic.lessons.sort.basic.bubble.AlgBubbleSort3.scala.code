def bubbleSort3() {
	val l = getNombreValeurs()-2
	var fin = false
	for ( i <- l to 0 by -1 ) {
		var a = 0
		for ( j<- 0 to i ) {
			if (plusPetit(j+1,j)) {
			echange(j,j+1);a=a+1
			}
		}
		if (a==0) {
			return
		}
	}
}
