override def run() {
  while (posFinale() == false) {
    	posSuivante()
  }
}


def posFinale() {
  return ((getMondeLargeur() == getX()) && (getY() == 0))
}

def posSuivante() {
  if (getY() == getMondeHauteur() - 1) {
    	setPos(getX()+1, 0)
  }
  else
  		setPos(getX(), getY() + 1)
}