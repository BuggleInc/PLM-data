def insertionSort() {
  var ok = false
  var v = 0
  var k = 0
  for(i<- getNombreValeurs() -1  to 0 by -1) {
    v=getValeur(i)
    while(ok==false) {
      copie(i-1, i)
      if(plusPetit(i -k-1,i-k)) {
        setValeur(i-k-1,v)
        ok= true
      }
      k=k+1
    }
  }
}