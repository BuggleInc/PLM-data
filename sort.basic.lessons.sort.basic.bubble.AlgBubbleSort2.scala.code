def bubbleSort2() {
	var sorted:Boolean = false;
	var c=0;
	do {
		sorted = true;
		for (i <- 0 to getNombreValeurs()-2-c) {
			if (!plusPetit(i,i+1)) {
				sorted = false;
				echange(i,i+1);
				c=c+1;
			}
		}
	} while (!sorted);
}
