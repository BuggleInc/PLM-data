def insertionSort() {
  var L = getNombreValeurs()
  var v = 0
  var cpt = -1
  for (i <- 1 to L-1){
    var I = i-1
    v = getValeur(i)
    while (plusPetit(i,I)) {
      setValeur(i,getValeur(i-1))
      cpt = cpt +1
      if (i!=0){
        I =I-1
      }
    }
    setValeur(i-cpt,v)
  }
}