override def run():Unit= {
  for (branche <- 1 to 8) {
    avance(30)
    avgauche()
    droite(45)
    avance(30)
    avgauche()
    avdroite()
    avdroite()
    gauche(45)
    avance(30)
    avdroite()
    gauche(45)
    avance(30)
    avdroite()
    gauche(45)
    avance(30)
    avdroite()
    avdroite()
    avgauche()
    droite(45)
    avance(30)
    avgauche()  
  }
}

def avdroite(){
  droite(90)
  avance(30)
}
def avgauche(){
  gauche(90)
  avance(30)
}