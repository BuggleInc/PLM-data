def solve() {
  val n = getTaillePile()
  var max = 0
  var idMin = 0
  for(i <- 0 to n-2) {
    idMin = i
    max = getRayonCrepe(i)
    for(j <- i+1 to n-1) {
      if(max<getRayonCrepe(j)) {
        idMin = j
        max = getRayonCrepe(j)
      }
    }
    retourne(idMin+1)
  }
}
