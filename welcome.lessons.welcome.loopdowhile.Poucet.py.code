while estSurBlanc()==1:
    baggle=0
    corridor1=1
    while croisement()==0 or corridor1=1:
        avance()
        if estSurBiscuit()==1:
            baggle=baggle+1
    if baggle<=2:
        droite()
    else:
        gauche()
avance()
