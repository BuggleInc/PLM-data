override def run() {
  var Color= new Array[Color](getMondeHauteur())
  for (i<-0 to getMondeHauteur()-1){Color(i)=getCouleurSol();recule()}
  for(k<-1 to getMondeLargeur()-1){
  gauche();avance();droite();avance()
  for (j<-0 to getMondeHauteur()-1){
    baisseBrosse()
    setCouleurBrosse(Color(j))
    avance()
    leveBrosse()
  }  }
}