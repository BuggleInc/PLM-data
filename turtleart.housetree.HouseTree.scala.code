override def run() {
  
  for (i <- 1 to 4) {
    
    maison()
    droite(90)
    avance(30)
    leveCrayon()
    avance(20)
    gauche(90)
    baisseCrayon()
    
  }
  
}

def maison() {
  
  for (i <- 1 to 4) {  	
  	avance(30)
  	droite(90)
    
  }
  
  avance(30)
  
  droite(30)
  
  for (i <- 1 to 3) {
  
  	avance(30)
  	droite(120)
   
	}
  
  gauche(30)
  recule(30)
  
}