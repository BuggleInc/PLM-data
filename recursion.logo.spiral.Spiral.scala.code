def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {
  if (steps == 0 ) {
    avance(0.0)
    gauche(angle)
  } else {
    avance(length)
    gauche(angle)
    steps = steps -1
    length = lenght + increment
  }
}

spiral(5,90,0,3)