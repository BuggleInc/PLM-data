def goAndGet() {
  var nbreCases:Int = 0
  
  while(!estSurBiscuit()) {
    avance()
    nbreCases = nbreCases + 1
  }
  
  prendBiscuit()
  
  for(compteur <- 1 to nbreCases) {
    recule()
  }
  
  poseBiscuit()
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}