override def run() {
    def keepHandOnRightWall() = {
    	if (estFaceMur) {gauche()} 
    	else {while (!(estFaceMur)) {droite()};gauche()}
    }
    while (!(estSurBiscuit)) {
    	keepHandOnRightWall();
    	avance();
    }
    prendBiscuit();
}
