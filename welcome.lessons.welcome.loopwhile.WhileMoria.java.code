// isOverBaggle(), isCarryingBaggle(), pickupBaggle() or dropBaggle(). 
public void run() { 
	// Turn around!
	right();
	right();

	// Find first baggle
	while (!isOverBaggle()) {
		forward();
	}

	// Move baggle
	pickupBaggle();
	while (isOverBaggle()) {
		backward();
	}
	
	dropBaggle();
}
