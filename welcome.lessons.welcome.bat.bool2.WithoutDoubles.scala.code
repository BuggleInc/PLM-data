var die:Int=0

def withoutDoubles(die1:Int, die2:Int, noDoubles:Boolean):Int = {
  if (noDoubles==true  && (die1==die2)){
    if(die1==6){
      die=1
      return die+die2
    }else{
      die=die1+1
      return die+die2
    }
  
  }else{
    return die1+die2
    
  }
}
