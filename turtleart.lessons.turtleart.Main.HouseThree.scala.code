override def run() {
	def maison() {
		for (i<-0 to 4) {
		avance(30)
		droite(90)
	}
	gauche(60)
	avance(30)
	droite(120)
	avance(30)
	droite(30)
	avance(30)
	}

	for (j<-0 to 4) {
		maison()
		leveCrayon()
		gauche(90)
		avance(20)
		gauche(90)
		baisseCrayon()
	}
}