def max1020(a:Int, b:Int):Int = {
  if (a < 10 && a > 20 && b < 10 && b > 20) {
    return 0;
  } else if (10 <= a && a <= 20) {
    if (10 <= b && b <= 20) {
      if (a <= b) {
        return b;
      } else {
        return a;
      }
    } else {
      return a;
    }
  } else if (10 <= b && b <= 20) {
    if (10 <= a && a <= 20) {
      if (a <= b) {
        return b;
      } else {
        return a;
      }
    } else {
      return b;
    }
  }
}