def gnomeSort() {
  var cpt=0
  var swap=true
  do {
    for (i <- 0 to getNombreValeurs()){
      swap=false
      if (!plusPetit(i,i+1)) {
        echange(i,i+1)
        swap=true
      }
    }
  } while (swap==false)
}