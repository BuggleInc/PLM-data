def hunt() {
 	if (estSurBiscuit()) { 
  	prendBiscuit();
  } else {
    if (isFacingTrail()) {
 			baisseBrosse();
       avance();
    } else {
     	leveBrosse();
     	gauche();
     }
   }
}     
        

// Copy your isFacingTrail() here
def isFacingTrail():Boolean = {
  if (estFaceMur()) {
      gauche();
    	return false;
  } else {
  	avance();
    if (getCouleurSol() == Color.green) {
  		recule();
      return true;
    } else {
      recule();
      return false;
    }
  }
}