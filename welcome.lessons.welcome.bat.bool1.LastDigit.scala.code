def lastDigit(a:Int, b:Int):Boolean = {
	return (a%10 == b%10)
}

/**	var A:Int = a
	var B:Int  = b
		
		
			A = A%(10)
			B = B%(10)
	
		
			return (A==B) 
			}
 // Attention, la difficulté de cet exercice c'était de 
 // Bien faire la différence entre a qui est une constante 
 // Ainsi que b et qu'il fallait les sauvegarder dans des 
 // variables var 

 //Ne pas se laisser pièger 
 */

 