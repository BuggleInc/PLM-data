public void run() {
	int index = 0;
	for(int x = 0; x < getWorldHeight(); x++){
		setY(x);
		for(int y = 0; y < getWorldWidth(); y++){ 
			if(x % 2 == 0){
				setX(getWorldWidth() - y - 1);
			}
			writeMessage(index);
			index++;		
		}
	}
}
