def pentaKoch(levels:Int, length:Double) {
  forward(length*0.4)
  left(120)
  forward(length*0.4)
  right(120)
  forward(length*0.4)
  right(120)
  forward(length*0.4)
  right(120)
  forward(length*0.4)
  left(120)
  forward(length*0.4)
}