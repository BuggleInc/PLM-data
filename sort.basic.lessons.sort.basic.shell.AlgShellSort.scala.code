def shellSort()  {
  var l = getNombreValeurs()/2
  while(l>0){
    for(i <- l-1 to getNombreValeurs()-1){
    	var n = getValeur(i)
      var v = i
    	while(v>=l && getValeur(v-l)>n){
      	copie(v-l,v)
    	}
    	setValeur(v, n)
  }
    l = l / 2
  }
}