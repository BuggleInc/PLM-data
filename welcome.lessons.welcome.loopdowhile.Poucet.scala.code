var cpt:Int=0;
while(!sortieTrouvee()){
	
	while(!croisement()){
	avance()
	if(estSurBiscuit()){
		cpt=cpt+1;
	}
	if(croisement() && cpt==3){
		gauche()
	}
	else if(croisement() && cpt==2){
		droite()
	}
	}
}
