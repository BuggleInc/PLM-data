override def run() {
  
  avance(100:Double)
  droite(90:Double)
  avance(100:Double)
  droite(90:Double)
  avance(100:Double)
  droite(90:Double)
  avance(100:Double)
  baisseCrayon()
  recule(100:Double)
  leveCrayon()
}