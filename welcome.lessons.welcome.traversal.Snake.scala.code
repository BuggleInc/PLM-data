def positionFinale():Boolean = {
		if ((estFaceMur()) && (getDirection == Direction.EST)){
			gauche()
			if (estFaceMur()){
				droite()
				return true
			} else { gauche() avance() gauche() return false} 
		} 
		if ((estFaceMur()) && (getDirection == Direction.EST)){
		
		droite() avance() droite() return false
		}
}		
override def run() {
baisseBrosse()
	while (positionFinale() == false){
		while (!estFaceMur()){
				avance() 
		}
  	}
}
