def selectionSort() {
  for (i<-1 to getNombreValeurs()-1) {
    var mini=getValeur(i)
    for(j<-i to getNombreValeurs()-1) {
      if (getValeur(j)<mini) {
        mini=getValeur(j)
      }
    } 
  }
}