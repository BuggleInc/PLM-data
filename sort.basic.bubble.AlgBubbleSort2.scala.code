def bubbleSort2() {
  var n:Int = getNombreValeurs
  for(i <- 0 to getNombreValeurs - 1) {
    n -= i
    for(j <- 1 to n) {
      if(getValeur(j - 1) > getValeur(j))
        echange(j - 1, j)
    }
  }
}