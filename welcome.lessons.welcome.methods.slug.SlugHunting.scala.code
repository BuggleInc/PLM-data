def hunt() {
  if (isFacingTrail()) {
    avance()
  }
}

def isFacingTrail():Boolean = {
  if (estFaceMur())
  	return false
  
  // On avance d'une case
  avance()
  
  var result:Boolean = (getCouleurSol() == Color.green)
  
  recule()
  
  return result
}
