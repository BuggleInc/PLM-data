def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
  if (isBirthday)
  	speed = speed - 5
  speed match {
    case speed if speed <= 60 => return 0
    case speed if speed <= 80 => return 1
    case speed if speed > 80 => return 2
  }
}