def averageValue(nums:Array[Int]): Int = {
  var somme:Int = 0
  for (i <- 0 to nums.length -1) {
    somme = somme + nums(i)
  }
  var resultat:Int = 0
  resultat = Math.round(somme/nums.length)
  return resultat
}