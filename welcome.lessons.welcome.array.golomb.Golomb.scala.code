def golomb(num:Int): Int = {	
  var t = new Array[Int](num)
  t(0)=1
  var compteur:Int = 1
  var valeur:Int = 2
  var repetition:Int = 2
  while(compteur < num){
    if(compteur + repetition - 1 < num){
      for(i<- compteur to compteur + repetition -1){
        t(i)=valeur
        compteur += 1
      }
      valeur += 1
      repetition = t(valeur-1)
    }else{
      for(j<- compteur to num -1){
        t(j)=valeur
        compteur += 1
      }
    }
  }
}