def indexOfMaxValue(nums:Array[Int]):Int = {
  val l = nums.length
  var resultat = nums(0)
  var placeres = 0
  for (i <- 0 to l-1){
    	if (resultat<=nums(i)) {resultat=nums(i) placeres=i}
  }
  return placeres
}
