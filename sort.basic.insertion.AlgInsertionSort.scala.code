def insertionSort() {
  
  for (i <- 1 to getNombreValeurs()-1) {
    
    var v = getValeur(i)
    var last = 0
    var c = 1
    
    while (!plusPetitQue(i-c,v)) {
      
      copie(i-c,i-c+1)
      c = c + 1
      last = c
      
    }
    
    setValeur(i-last,v)
    
  }
  
}