def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  
  if (steps != 0) {
    
    current(steps);
  	avance(length);
 	 	droite(angle);
    current(steps);
  	tree(steps-1, shrink*length, angle, shrink);
    current(steps);
  	gauche(angle);
  	gauche(angle);
    current(steps);
		tree(steps-1, shrink*length, angle, shrink); 
    current(steps);
    droite(angle);
    current(steps);
    recule(length)
    
  }
  
}