def selectionSort() {
  var i:Int=0
  var m:Int=getValue(0)
  for(i<-1 to getValueCount()){
    for(j<-i to getValueCount()){
      if(getValue(i)<=m){
        m=getValue(j)
      }
      swap(m,i)
    }
  }
}
