override def run() {
  var cpt:Int=0
  for(y<-0 to getMondeLargeur()-1){
    cpt=col(y,cpt)
  }
}

def col(y:Int,cpt:Int):Int={
  var cpt2:Int=cpt
  for (x<-0 to getMondeHauteur()-1){
    setPos(x,y)
    ecritMessage(""+cpt2)
    cpt2=cpt2+1
  }
  return cpt2
}