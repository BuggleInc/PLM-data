def indexOfValue(nums:Array[Int] ,lookingFor:Int): Int = {
 	var i=0
  var trouve: Boolean = false
  while(trouve == false && lookingFor && i<(nums.length-1))
  {
    i+=1
    if(nums(i)!= lookingFor){trouve = true}
  }
  if(trouve == true){return i}
  else{ return -1 }
}