def isFacingTrail():
   # write your code here
    while not isOverBaggle:
        if (isFacingWall() == True):
            left()
        else:
            brushDown()
            brushUp()
            forward()
        if (getGroundColor() != Color.green):
            backward()
            left()
        else:
            brushDown()
            brushUp()
            forward()

isFacingTrail()