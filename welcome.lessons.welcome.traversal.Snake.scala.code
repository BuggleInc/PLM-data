override def run() {
  baisseBrosse()
  if (estFaceMur & getDirection==Direction.EST) {
    gauche()
    avance()
    gauche()
  } else if (estFaceMur & getDirection==Direction.OUEST)  {
    droite()
    avance()
    droite()
  } else if (!estFaceMur) {
    avance()
  }
    
  }
