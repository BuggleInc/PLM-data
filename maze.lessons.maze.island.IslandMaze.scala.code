override def run(){
  while(getY()!=0){
    if(!estFaceMur()){avance()}
    else{gauche(); avance(); droite()}
  }
  
  def keepHandOnSideWall(): Boolean={
    droite()
    if(estFaceMur()){ 
      gauche()
      return true}
    else{
      gauche()
      return false}
  }
  
  while(!estSurBiscuit()){
    if(keepHandOnSideWall()){
      if(!estFaceMur()){avance()}
      else{gauche()}
    }
    else{droite()
         avance()
        }
    
  }
  prendBiscuit()
}