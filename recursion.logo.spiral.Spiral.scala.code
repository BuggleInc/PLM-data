def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {
  if (steps == 0) {
    gauche(angle); avance(length)
  }
  else {
    spiral(0, angle, length, increment); spiral(steps-1, angle, length + increment, increment)
  }
}
