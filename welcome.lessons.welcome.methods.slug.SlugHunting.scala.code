def hunt() {
  
  baisseBrosse()
  while (estSurBiscuit()==false) {
    
    if (isFacingTrail()==true) {
      avance()
      avance()
      }
      
        
    
  }
  prendBiscuit()
}

def isFacingTrail():Boolean = { 
  var test = false
  if (estFaceMur()==true) {
    gauche()
  }
  avance()
  if (getCouleurSol()==Color.green) {
    recule()
    test = true
    return test;
    
  } else {
    recule()
    return test;
    
    }
  
    
}