def faireMotif() {
  
  def faireV() {
		avance(2)
 		baisseBrosse(); leveBrosse()
	 	avance()
 		gauche(); avance()
 		baisseBrosse(); leveBrosse()
  	recule(); droite()
 		avance()
  	baisseBrosse(); leveBrosse()
  	avance(2)
  	gauche()
	}
  for (c <- 1 to 4) {
    faireV()
  }
}

override def run() {
  for (m <- 1 to 3) {
    faireMotif()
    avance(6)
  }
}