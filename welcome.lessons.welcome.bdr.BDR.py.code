       if litMessage(R):
        droite()
        avance(1)
    elif estSurMessage(L):
        gauche()
        avance(1)
    elif estSurMessage(I):
        gauche(2)
        avance(1)
    elif litMessage(A):
        avance(1)
    elif estSurMessage(B):
        avance(2)
    elif estSurMessage(C):
        avance(3)
    
