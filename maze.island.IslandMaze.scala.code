override def run() {
  while(!estSurBiscuit())
  	setDirection(Direction.nord) 
  	if (estFaceMur)
  		longeGch()
  	else(avance())
  prendBiscuit()
}

def longeGch(){
  gauche()
  if(!estFaceMur())
    avance()
  else {
    droite()
    while(estFaceMur())
      droite()
    avance()
       }
}
