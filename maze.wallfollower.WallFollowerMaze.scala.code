override def run() {
  //while(!isOverBaggle()){
  for(i <- 1 to 100){
      keepHandOnSideWall()
	}
}

def keepHandOnSideWall(){
  
  if(isFacingWall()){
    left()
    if(isFacingWall()){
      left(); left()
      if(isFacingWall()){
        right()
      }    }
    if(!isFacingWall()){
      forward()
    		}
   	 
     }

  else{
    		left()
        if(!isFacingWall()){
          forward()
        	}
        else{right(); forward()}
  }
}

