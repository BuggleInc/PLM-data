def shellSort()  {

  var v=0
  var gap = getValueCount()/2
  while(gap<getValueCount()){
      for(i <- 1 to getValueCount()-1){  

    v = getValue(i)
    
    while(i > 0 && !isSmallerThan(i-gap,v)){
      copy(i-gap,i)
      gap = gap + gap
    }
   setValue(i,v)
  
}
}
  }

  
