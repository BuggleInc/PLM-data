def goAndGet() {
  var compteur = 0
  while (!isOverBaggle){
    forward()
    compteur = compteur +1 
  }
  pickupBaggle()
  for (i <- 1 to compteur)
  	backward();
  dropBaggle
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}