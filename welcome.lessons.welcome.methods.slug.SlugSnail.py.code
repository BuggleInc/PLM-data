def isFacingTrail(color):
       if estFaceMur():
            return False
       forward()
       coul = getCouleurSol()
       backward()
       if coul == color:
           return True

def hunt(color):
    if estSurBiscuit():
        prendBiscuit()
    elif isFacingTrail() == True:
        baisseBrosse()
        avance()
        leveBrosse()
    else:
        left()