override def run() {
def keepHandOnSideWall() {
	gauche();
	if (estFaceMur()) {
		droite();
		avance();
		if (!(estFaceMur())) {
			gauche();
			avance();
			gauche();
			if (!(estFaceMur())) {
				avance();
				gauche();
			}
		}
		
	}
	
}

while (!(estSurBiscuit())) {
keepHandOnSideWall();
}
}
