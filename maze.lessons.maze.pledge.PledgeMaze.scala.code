var sommeAngle:Int = 0

def keepHandOnSideWall() {
  gauche()
  if (estFaceMur() == false) {
    avance()
    sommeAngle = sommeAngle + 1
  } else {
    droite()
    if (estFaceMur() == false) {
      avance()
    } else {
      droite()
      if (estFaceMur() == false) {
        avance()
        sommeAngle = sommeAngle - 1
      } else {
        droite(); avance()
      }
    }
  }
}

def isDirectionFree() {
  var dire:Direction = getDirection()
  if (estFaceMur == false) {
    return true
  }
  return false
}

override def run() {
  while (getY() != 0) {
    while (estFaceMur() == false) {
      avance()
    }
    droite()
    keepHandOnSideWall()
  }
  setDirection(Direction.EAST)
  while (porteBiscuit() == false) {
    if (estSurBiscuit == true) {
      prendBiscuit()
    } else {
      
      keepHandOnSideWall()
    }
  }
}