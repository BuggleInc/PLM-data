def insertionSort() {
  var n = getNombreValeurs();
  for ( i <- 1 to n-1 ){
    var v = i;
    var j = 0;
    while ( plusPetit(i-j,i-j-1) ){
      echange(i-j,i-j-1);
      j += 1;
    }
    copie(v,i-j);
  }
}