override def run() {
  while(!estSurBiscuit())
  {
    keepHandOnSideWall()
  }
  prendBiscuit()
}

def keepHandOnSideWall()
{
  
  if(!estFaceMur())
  {
    avance()
    gauche()
    if(estFaceMur())droite()
    
  }
  else {
    gauche();
    if(estFaceMur()){
      droite()
      droite();
      if(estFaceMur())droite()
      avance()
    	gauche()
    	if(estFaceMur())droite()
    }
    
  }
}
