for(n <- 1 to 7) {
  for(m <- 1 to 4) { //Pour faire un côté il faut : 4 avant, 2 gauche, 4 droite, 2 droite, 4 gauche
    for(l <- 1 to 4) {avance()}
    gauche() 
    for(l <- 1 to 2) {avance()}
    droite()
    for(l <- 1 to 4) {avance()}
    droite()
    for(l <- 1 to 2) {avance()}
    gauche()
    for(l <- 1 to 4) {avance()}
    gauche()
  }
}