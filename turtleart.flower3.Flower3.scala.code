override def run():Unit= {
  for (k<-1 to 8) {
    motif()
  }
}

def motif() {
  gauche(45:Double)
  avance(20:Int)
  droite(90:Double)
  avance(20:Int)
  gauche(45:Double)
  avance(20:Int)
  gauche(90:Double)
  avance(20:Int)
  droite(90:Double)
  avance(20:Int)
  droite(90:Double)
  avance(20:Int)
  avance(20:Int)
  droite(90:Double)
  avance(20:Int)
  droite(90:Double)
  avance(20:Int)
  gauche(90:Double)
  avance(20:Int)
  gauche(45:Double)
  avance(20:Int)
  droite(90:Double)
  avance(20:Int)
  droite(180:Double)
}