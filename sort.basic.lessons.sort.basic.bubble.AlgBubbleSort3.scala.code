def bubbleSort3()  {
  var swapped:Boolean=true
  while(swapped){
    swapped=false
  	for (k<- getNombreValeurs()-2 to 0 by -1 ){
    	if (plusPetit(k,k+1)){
      	echange(k,k+1)
        swapped=true
      }
      else{
        return
      }
      
      
    
    }
  
  }
  
  
}