override def run() {
  var c=0;
  var x=1;
  var y=7;
  while (readMessage()!="48"){
    for (i<- 1 to 7){
      writeMessage(c);
      y= y-1;
      setPos(x,y);
    }
    y=7;
    x=x+1;
    setPos(x,y);

    
  }
}