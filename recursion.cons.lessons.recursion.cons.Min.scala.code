def min(l:List[Int]): Int = {
	if (l.tail.length == 0)
		return l.head
	else return min( min2(l.head, l.tail.head)::l.tail.tail)
	
}

def min2(a:Int, b:Int): Int = {
	if (a < b)
		return a
	else return b
}
