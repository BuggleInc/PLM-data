def selectionSort() {
	var l = 0
	
	for (i <- 0 to getNombreValeurs()-2) {
		for (j <- i to getNombreValeurs()-1) {
			for (k <- j to getNombreValeurs()-1) {
				if (plusPetit(j,k)) {l=j}
				else {l=k}
			}
		}
		echange(i,l)
	}
}
