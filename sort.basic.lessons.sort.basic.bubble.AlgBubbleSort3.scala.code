def bubbleSort3()  {
  var swapped:Boolean=true
  var L =getNombreValeurs()
  for (k<- L-2 to 0 by -1 ){
    swapped=false
    L=L-1
    for(i<-0 to i)
    	if (plusPetit(i+1,i)){
      	echange(i,i+1)
        swapped=true
      }     
      
    
    }
  if (swapped==false){
    return
  }
  }
  
  
}