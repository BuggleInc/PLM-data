def cigarParty(cigars:Int, isWeekend:Boolean):Boolean = {
  if (!isWeekend){
    return estDans(cigars,40,60)  
  }
  else{
    return (cigars>=40)
  }
}

def estDans(a:Int,b:Int,c:Int):Boolean={
  return (a>=b)&&(a<=c)
}