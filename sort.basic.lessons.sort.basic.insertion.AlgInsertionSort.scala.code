def insertionSort() {
  var valeur=0
  var last=0
  for (i<-1 to getNombreValeurs()-1){
    var test=false
    valeur = getValeur(i)
    var n=i-1
    while ( n>=0 && valeur< getValeur(n) ){
        copie(n,n+1)
        n-=1
      last=n+1
      test=true
      }
    setValeur(last, valeur)
   
  }
}