def indexOfMaxValue(nums:Array[Int]):Int = {

  var n =nums.length
  var max=nums(0)
  var imax=0
  for (i <- 0 to n-1){
    if (max<nums(i)){
      max=nums(i)
      imax=i
     
    }
  }
  return imax
  
  
}