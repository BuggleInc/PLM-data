def mark(color):
    setBrushColor(color)
    brushDown()
    brushUp()
def step():
    if getGroundColor() == Color.white:
        color = Color.black
        mark(color)
        right()
        forward()
    else:
        color = Color.white
        mark(color)
        left()
        forward()
        