def insertionSort() {
 	val n = getNombreValeurs()
  for(i<- 0 to n-2)
  {
    if(plusPetit(i+1, i))
    {
      for(j<- 0 to i+1) 
      {
        if(!plusPetit(i+1, j))
        	echange(j, i+1)
      }
    }
  }
}