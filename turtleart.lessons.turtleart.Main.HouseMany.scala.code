override def run() {
	def maison() { 
		avance(30)
		droite(30)
		for ( x <- 1 to 3) {
			avance(30)
			droite(120)
		}
		gauche(120)
		recule(30)
		gauche(90)
		avance(30)
		droite(90)
		avance(30)
	}

	for ( x <- 1 to 4 ) { 
		maison()
		leveCrayon()
		recule(50)
		droite(90)
		baisseCrayon()
	}

	for ( x <- 1 to 2 ) {
		leveCrayon()
		gauche(90)
		avance(50)
		droite(90)
		maison()
	}

}


