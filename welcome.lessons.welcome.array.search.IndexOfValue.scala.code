def indexOfValue(nums:Array[Int] ,lookingFor:Int): Int = {
  var n : Int = nums.length
  for (i <- 0 to n-1){
    if (lookingFor!=nums(i)){
      return -1
    } else if (lookingFor!=num(i){
      return nums(i)
    }
  }
}