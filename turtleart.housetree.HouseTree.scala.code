override def run() {
  for(k <- 1 to 4){
    maison()
    leveCrayon()
    droite(90)
    avance(50)
    left(90)
    baisseCrayon()
  }
}

def maison() {
  for(i <- 1 to 4){
    avance(30)
    droite(90)
  }
  avance(30)
  droite(30)
  for(i <- 1 to 3){
    avance(30)
    droite(120)
  }
  left(30)
  recule(30)
}