def cocktailSort() {
var n=getNombreValeurs()
var compteur=1
var tictac=1

	
 while(compteur!=0){
 	compteur=0
	for (k<- 0 to n-2-tictac){
		if (plusPetit(k,k+1)){
		}
		else {
		    	echange(k,k+1)
		    	compteur=1
		}
	}
	for (k<- n-2-tictac to 0){
		if (plusPetit(k,k+1)){
			echange(k,k+1)
			compteur=1
		}
	}
	tictac=tictac+1
}
}
