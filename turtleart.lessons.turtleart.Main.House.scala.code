override def run() {
	avance(100);
	droite(30);
	for (i <- 1 to 3) {
		avance(100);
		droite(120.0);
	}
	droite(60);
	avance(100);
	droite(120);
	avance(100);
	droite(90);
	avance(100);
	droite(90);
	avance(100);
	droite(90);
}
