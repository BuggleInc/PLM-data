def crab(levels:Int, length:Double) {
  if(levels==0){
    avance(length)
  }else{
    gauche(45.0)
    avance(length/Math.sqrt(2))
    droite(90.0)
    avance(length/Math.sqrt(2))
    gauche(45.0)
  }
}