var score:Int = 0
var nbPas:Int = 0
var fini = false
while (!fini){//Tant qu'on a pas 4 biscuits, on continue :
  
  if(!estSurBiscuit()){
    avance()
  }
  else if (estSurBiscuit() && score < 3) {
    score += 1
    avance()
  }
  else fini = true
  
}