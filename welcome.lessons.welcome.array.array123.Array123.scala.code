def array123(nums:Array[Int]): Boolean = {
	var x=false
	var y=0
	for (i<-0 to nums.length-1){
		if ((y==0 && nums(i)==1) || (y==1 && nums(i)==2) || (y==2 && nums(i)==3)) y=y+1
		if (y==3) x=true
	}
	return x
}
