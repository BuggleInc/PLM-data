def island(num:Array[Int]): Int = {
  var res:Int = 0;
  var long:Int = num.length;
  if (num(0)>0) {
    res = res +1;
  }
  for (i<-2 to long) {
    if (num(i)>0 && num(i)>num(i-1)) {
      res = res+1;
    }
  }
  return res
}
