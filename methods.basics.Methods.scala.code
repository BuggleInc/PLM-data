def goAndGet() {
  var pas:Int=0;
  while (!isOverBaggle()) {
    forward();
    pas=pas+1
  }
  pickupBaggle();
  for (i <- 1 to pas) {
    backward();
  }
  dropBaggle();
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}