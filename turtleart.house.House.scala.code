override def run() {
  carre(100)
  avance(100)
  droite(30)
  triangle(100)
  droite(160)
  avance(100)
  droite(180)
}

def triangle(n:Int){
  for(i<-0 to 2){
    avance(n)
    droite(120)
  }
}

def carre(n:Int){
  for(i<-0 to 3){
    avance(n)
    droite(90)
  }
}