def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if(steps==0){
    leveCrayon();
  }
  else{
    avance(length);
    droite(angle);
    avance(length);
    gauche(2*angle);
  }
}