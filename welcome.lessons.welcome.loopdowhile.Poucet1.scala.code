while (!sortieTrouvee())
{
  var nbBiscuit:Int = 0
  
  for (i <- 1 to 5)
  {
    avance()
    if (estSurBiscuit())
    	nbBiscuit = nbBiscuit + 1
  }
  
  if (nbBiscuit <= 2)
  	droite()
  else
  	gauche()
}

avance()
