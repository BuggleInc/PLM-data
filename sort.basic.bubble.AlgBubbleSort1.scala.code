def bubbleSort()  {
	var trie: Boolean = false
  while (!trie){
    trie = true
    for (i <- 1 to getNombreValeurs()){
      if (plusPetit(i+1,i)){
        echange(i,i+1)
        trie = false
      }
    }
    }
  }