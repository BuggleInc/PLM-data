while (!estSurBiscuit || !estFaceMur) {
  backward();
  if (estSurBiscuit) {
    prendBiscuit(); 
    while (!estSurBiscuit) {
      forward();
    }
    backward(); 
    poseBiscuit(); 
  }
  backward();
}
