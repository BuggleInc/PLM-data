def insertionSort() {
  val n=getNombreValeurs()
  var x = 0
  var save = 0
  
  for(i<-1 to n-1)
   {
     save = getValeur(i)
     var cpt = i
     while(cpt<1 && getValeur(cpt)<save)
     {
       copie(cpt,cpt-1)
       cpt -= 1
     }
   }
}