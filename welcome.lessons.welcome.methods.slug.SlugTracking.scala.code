var vert:Boolean = 0

def isFacingTrail():Boolean = {
  avance()
  if (getCouleurSol() == Color.green ) {return true;}
  
  
  else {vert==false;}
  if (vert==false) {recule()}
  else return
}        
