do {
  var nbBaggle=0
  forward()
  while (crossing()==false){
       if (isOverBaggle()==true){
        nbBaggle=nbBaggle+1
       }
        forward()
   }
  if (nbBaggle>=3){
       left()
  }
  else {
    right()
  }
} while(exitReached==false)