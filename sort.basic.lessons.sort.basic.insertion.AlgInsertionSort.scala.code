def insertionSort() {
  for (i<-1 to getNombreValeurs()-1){
    var v=getValeur(i)
    var boucle=i
    while (boucle>0 && getValeur(boucle-1)>v) {
      echange(boucle,boucle-1)
      boucle=boucle-1
    }
    setValeur(boucle,v)
  }
}