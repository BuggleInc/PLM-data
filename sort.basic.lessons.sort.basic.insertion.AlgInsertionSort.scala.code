def insertionSort() {

	var cochon = 0;
	var lastId = 0;
	for(i <- 1 to getNombreValeurs() )
	{	
		cochon = getValeur(i);
		for(j <- 1 to i)
		{
			
			if(getValeur(i-j) > cochon)
			{
				echange(i-j,i - lastId);
				lastId = lastId + 1;
			}
		}
	}

}
