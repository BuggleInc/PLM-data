def hunt() {while(!estSurBiscuit()){if (isFacingTrail()){avance();baisseBrosse();leveBrosse()};else{droite()} };prendBiscuit()
   
     // Write your code here
 }

def hunt() {while(!estSurBiscuit){if (isFacingTrail()){avance();baisseBrosse();leveBrosse()};else{droite()} };prendBiscuit()
   
}


def isFacingTrail():Boolean = {
    if(estFaceMur()){return false};
  	else{ avance()
  					if(getCouleurSol()==Color.orange){recule ();return true};
        			else{recule();return false}}}
