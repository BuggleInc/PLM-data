def isFacingTrail():Boolean = {
  var caseVerte:Boolean = false
  if(!estFaceMur()){
    avance()
    if(getCouleurSol()==Color.green){
      caseVerte = true
      return(caseVerte)
    }
    else(return(caseVerte))
    recule()
  }
  else(return(caseVerte))
}