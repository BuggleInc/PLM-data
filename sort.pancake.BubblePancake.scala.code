def solve() {
  var l=getStackSize()
  for( i <- 0 to l-2){

    for(j <- i to l-1){
      if(getPancakeRadius(j)>getPancakeRadius(j+1))
	    	swap(j)
    }
  }
}




def swap(n : Int){
  flip(n)
  flip(n+1)
}