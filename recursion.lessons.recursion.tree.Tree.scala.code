def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
while(steps!= 0){
current(steps);

tree(steps-1,shrink*length,angle,shrink);avance(length);
droite(angle);

tree(steps-1,shrink*length,angle,shrink);gauche(2*angle);
recule(length*shrink);

}
}




