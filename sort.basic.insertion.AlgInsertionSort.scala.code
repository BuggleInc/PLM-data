def insertionSort() {
  var v=0
  var n=getValueCount()-1
  
  for(i <- 1 to getValueCount()-1){
    v = getValue(n-1)
    
    while(n > 0 && !isSmallerThan(n,v)){
      copy(n-1,n)
      n = n-1
    }
   setValue(n,v)
  }
}