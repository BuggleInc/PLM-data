override def run() {
  for (i <- 1 to 4){
    avance(30)
  droite(30)
  for (i <- 1 to 3){
    avance(30)
    droite(120)
  }
  droite(60)
  for (i <- 1 to 3){
    avance(30)
    droite(90)
  }
  droite(90); avance (50); gauche(90)
  }
}