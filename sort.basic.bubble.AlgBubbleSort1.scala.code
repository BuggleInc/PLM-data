def bubbleSort()  
{
  var echanger=false //on definit un booleen qui nous permettra d'avoir une condition d'arrêt: si on n'a pas échangé deux valeurs précédemment, alors c'est que le tableau est correctement ordonné, donc on arrete la boucle
  do
  	{
  		for(i<-0 to (getNombreValeurs()-2)) 
      	{
  				var j=i+1
          echanger=false
					if (plusPetit(getValeur(j),getValeur(i)))
      			{
							echange(i,j)
          		echanger=true
       			}
        }
		}
	while(!echanger)
}