def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
	if (isBirthday)
	{
		if (speed < 66)
		{
			return 0
		}
		else if ( (66 <= speed) && (speed <= 86))
		{
			return 1
		}
	}
	else
	{
		return 0
	}
}
