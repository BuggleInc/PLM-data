override def run() {
  while (!estSurBiscuit()) {
    while (getY() != 0) {
    keepHandOnSideWall()
  }
  }
  prendBiscuit()
  
  def keepHandOnSideWall() {
    gauche()
    while (estFaceMur()) {
      droite()
    }
    avance()
    while (getDirection == Direction.NORD && !estFaceMur()) {
      avance()
    }
  }
}