def teaParty(tea:Int, candy:Int): Int = {
var outcome = 0
	(tea,candy) match {
		case (x,y) if (x<5 || y<5)		=> outcome = 0; return outcome;
		case (x,y) if (x>=2*y || y>=2*x)	=> outcome = 2; return outcome;
		case (x,y) if (x>=5 && y>=5)		=> outcome = 1; return outcome;
	}
}