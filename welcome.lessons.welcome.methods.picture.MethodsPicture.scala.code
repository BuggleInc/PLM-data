def faireV(c:Color) {
  setCouleurBrosse(c:Color)
  avance()
  baisseBrosse()
  leveBrosse()
  avance()
  gauche()
  avance()
  baisseBrosse()
  leveBrosse()
  recule()
  droite()
  avance()
  baisseBrosse()
  leveBrosse()
  gauche()
}


override def run() {
  for (compteur1 <-1 to 3) {
    for (compteur <-1 to 5) {
      faireV(Color.YELLOW)
      faireV(Color.RED)
      faireV(Color.BLUE)
      faireV(Color.GREEN)
    }
    droite() 
    avance()
    gauche()
    avance()
    avance()
    avance()
    avance()
    avance()
    droite()
  }
}