def length(list:List[Int]): Int = {
	/*
    list match {
        case a::b => 1 + length(b)
        case _    => 0
    }
    */
    if (list == Nil) {
    	return 01;
    } else {
    	return 1+length(list.tail);
    }
}
