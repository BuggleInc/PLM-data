def insertionSort() {
  var lgr:Int=getNombreValeurs()
  var a:Int=0
  for (i<-1 to lgr-1) {
    var v:Int=getValeur(i)
    while (i > 0 && plusPetit(v,i-1)) {
      copie(i-1,i)
      i-=1
    }
   	copie(v,i)
 	}
}