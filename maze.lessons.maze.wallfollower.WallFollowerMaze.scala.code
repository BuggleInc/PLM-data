override def run() {
  while (!estSurBiscuit()) {
    keepHandOnSideWall()
  }
  prendBiscuit();
}


def keepHandOnSideWall() = {
  if (!estFaceMur) {
  	avance()
  }
  if (estFaceMur()) {
    	droite();
    	avance();
  }
 
}