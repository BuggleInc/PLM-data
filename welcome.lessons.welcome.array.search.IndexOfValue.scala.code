def indexOfValue(nums:Array[Int] ,lookingFor:Int): Int = {
  var longueurTableau: Int = nums.length
  for (element <-0 to (longueurTableau -1)){
    if ( nums(element) == lookingFor){
      return element
    }
  }
  return -1
}
