def combSort11() {
  var length : Int = getValueCount();
  var gap : Int = length;
  var I : Int = 0;
  var tri : Boolean = true;
  
  while(gap>1 | tri)
  {
    tri=false;
    if(gap>1)
    {
      if(gap == 9 | gap == 10)
      {
        gap = 11;
      }
      gap = (gap.toDouble / 1.3).toInt;
    }
    I=0;
    while(I+gap<length)
    {
      if(isSmaller(I+gap,I))
      {
        tri=true;
        swap(I,I+gap);        
      }
      I+=1;
    }
  }
}