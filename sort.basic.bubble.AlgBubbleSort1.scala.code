def bubbleSort()  {
  var swapped : Boolean = false
  do{
    swapped = false
    for(i <- 0 to getValueCount()){
      if(isSmallerThan(i+1, i)){
        swap(getValue(i+1), getValue(i +1))
        swapped = true
      }
    }
  } while(swapped)
}