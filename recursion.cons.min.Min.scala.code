def min(l:List[Int]): Int = {
  if (l.tail==Nil){
    return(l.head);
  }else{
    if (l.head<(l.tail).head){
      return(min(l.head::(l.tail).tail));
    }else{
      return(min(l.tail));
    }
  }
}