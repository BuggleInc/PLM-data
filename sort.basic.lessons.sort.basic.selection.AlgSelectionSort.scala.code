def selectionSort() {
  var N:Int=0
  var M:Int=0
  val lgr:Int=getNombreValeurs()
  var a:Int=0
  var pos:Int=0
  for(i<-0 to lgr-1){
    a=getValeur(i)
    for (j<-N to lgr-1){
      if (getValeur(i)>getValeur(j)){
        a=getValeur(j)
        pos=j
      }
    }
    echange(i,pos)
  }
}