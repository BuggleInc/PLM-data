override def run() {
	var cpt = 0
	var h = getMondeHauteur() - 1
	var l = getMondeLargeur() - 1
	for (i<- 0 to l) {
		for (p<- 0 to h ) {
			
		
		cpt = getX() + getY();
		ecritMessage(cpt)
		setPos(i,p)
		}
	}
	
	
	
}
