override def run() {
  
  def keepHandOnSideWall()  {
    
    while (estSurBiscuit() == false) {
      
      if (estFaceMur() == true) {
        gauche();
      } else {
        avance();
      }
    }
    
  }
  
keepHandOnSideWall();
prendBiscuit();
  
}