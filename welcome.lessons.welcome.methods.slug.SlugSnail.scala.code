var couleur = false

def hunt(c:Color) {
  
  
  	isFacingTrail(c:Color);
    if (couleur==true){
      baisseBrosse();
      avance();
      leveBrosse();
    }
  	else gauche()
  
     
}


def isFacingTrail(c:Color):Boolean = { 
	if (estFaceMur==false) {
		avance();
  	if (getCouleurSol()==c) {
  		recule();
    	couleur = true;
  	
    	}
    	else { 
      	recule();
  			couleur = false
      }   
 	}
  else gauche()
  return couleur  
}