def insertionSort() {
for(i <- 1 to getNombreValeurs - 1){
	var v = getValeur(i)
	var j = i
	while(plusPetitQue(v,j){
		if(getValeur(j-1)>getValeur(j)){copie(j-1,j)}
		j = j - 1
	}
	setValeur(v,j)
}
}
