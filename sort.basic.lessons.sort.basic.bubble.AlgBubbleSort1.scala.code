def bubbleSort()  {
  var continue = true
  while (continue){
    
  continue = false
  for (i <- 1 to getValueCount()-1){
      if (isSmaller(i,i-1)){
        swap(i, i-1)
        continue = true
      } 
  }
  }
}
