def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {
  if (steps == 0){
    return 0
  }
  else {
    for (i<- 0 to steps-1) {
     
      avance(length + increment)
      gauche(90)
      spiral(steps-1,angle,length+3,increment)
 
    
    }
  }
  
  
  
  
}