
def golomb(num:Int): Int = {
  var tab = new Array[Int](num)
  tab(0)=1
  if (num>=2) {
    tab(1)=2
    var rang = 1
    for (i <- 2 to num-1) {
      for (j <- 1 to tab(i-1)) {
        tab(rang)=i
        rang+=1
      }
    }
  }
  return tab(num)
}