def solve() {
  var beforeRed = 0
  var afterBlue = getSize() - 1
  for(i <- 0 to getSize() - 1){
    if(getColor(i) == RED){
      swap(i, beforeRed)
      beforeRed += 1
    }
    if(getColor(i) == BLUE){
      swap(i, afterBlue)
      afterBlue -= 1
    }
  }
}