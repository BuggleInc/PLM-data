def max2Value(nums:Array[Int]): Int = {
  var n =nums.length
  var max=nums(0)
  var max2=nums(0)
  for (i<-1 to n-1){
    if(max<nums(i)){
      max2=max
      max=nums(i)
    }
    else if(max2<nums(i)){
      max2=nums(i)
    }
    
  }
  return max2
}