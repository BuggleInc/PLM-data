def cocktailSort() {
  var fin1 = false
  var fin2 = false
  var len = getNombreValeurs()
  while (fin1 == false || fin2 == false) {
    var fait2 = 0
    for (i <- 0 to len -2) {
      	if ((plusPetitQue(len - 1 - i, len -2 -i))) {
        echange(len - 1 - i, len -2 -i)
        fait2 += 1
      }
    };if (fait2 == 0) {
      fin2 = true
    }
  }
  }