def insertionSort() {
  var memoire = 0
  for (i <- 1 to getNombreValeurs()-1){
    memoire=getValeur(i)
          var n=1
    if (getValeur(i-n)>memoire){
    while (i-n > 0 && getValeur(i-n)>memoire){
      n=n+1
    }
      copie(i-n, i)
      setValeur(i-n, memoire)
  }
}
}