def isFacingTrail():
   # write your code here
    while isOverBaggle != True:
        if (isFacingWall()):
            left()
        else:
            brushDown()
            brushUp()
            forward()
        if (getGroundColor() != Color.green):
            backward()
            left()
            forward()

isFacingTrail()