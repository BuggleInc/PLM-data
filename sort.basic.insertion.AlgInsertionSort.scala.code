def insertionSort() {

  for(i<- 1 to getNombreValeurs()-1){
    var v = getValeur(i)
    var j=i-1
    while(getValeur(j)>v){
 			setValeur(j+1,getValeur(j))
      if(j!=0){
      	return
      }
    }
    setValeur(j,v)
    
  }
}