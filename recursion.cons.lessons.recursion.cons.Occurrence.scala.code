def occurences(l:List[Int], v:Int): Int = {
	if(l==Nil){
		return 0;
	}
	if(l.head == v){
		return occurences(l.tail,v);
	}
	
	return 0 + occurences(l.tail,v);
	
}
// il faut list[char] et v:char parce qu'on utilise l.tail  ???