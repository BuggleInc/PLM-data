def cocktailSort() {
  var c=1
  while (c!=0){
    c=0
    for (k<- 1 to getValueCount()-1){
      if (!isSmaller(k-1,k)){
        swap(k,k-1)
        c=c+1
      }
    }
    for (k<- getValueCount()-2 to 0 by -1){
      if (!isSmaller(k,k+1)){
        swap(k+1,k)
        c=c+1
      }
    }
  }
}