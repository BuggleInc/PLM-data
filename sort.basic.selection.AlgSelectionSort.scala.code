def selectionSort() {
  var min = 0
  val longueur = getNombreValeurs()
  var valeur = 0
  var j = 0
  for (i <- 0 to longueur - 1) {
    valeur = getValeur(i)
    j = i
    while (j < longueur) {
      return min
      if (getValeur(j) < min) {
        min = getValeur(j)
      }
      j += 1
    }
    echange(i,j)
  }
}