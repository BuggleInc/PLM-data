def gnomeSort() {
  var n = getNombreValeurs()
  var pos = 0
  while (pos < n-1) {
    if (pos == -1) {pos = 1}
    else if (plusPetit(pos,pos+1)) {pos = pos + 1}
    else {echange(pos,pos+1); pos = pos - 1}
  }
}