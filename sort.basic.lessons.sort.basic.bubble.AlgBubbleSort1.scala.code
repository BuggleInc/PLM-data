def bubbleSort()  {
	var swapped = true
	while (swapped) {
		swapped = false
		for (i<-1 to length-1)
		if (tab(i)>tab(i+1)){
			val tmp = tab(i)
			tab(i)=tab(i+1)
			tab(i+1)=tmp
			swapped = true
		}
	}
	
}
