       var pas= 0
       var dejafait = 0
      def goAndGet() {
      while (!estSurBiscuit) {avance() }
     pas = pas+1
 
       }
       prendBiscuit()
       while(dejafait!=pas) {recule}
       poseBiscuit()
       

        for (i <- 1 to 7) {
            goAndGet();
            right();
            forward();
            left();
        }
