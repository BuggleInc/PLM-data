override def run() { 
  do {
    avance()
      if (estFaceMur() && getDirection()== Direction.EST){
    			gauche()
    			avance()
    			gauche()
      }
    	else if (estFaceMur() && getDirection()== Direction.OUEST) {
    			droite()
    			avance()
    			droite()
      }
    			
    			
    	
    
    
  } while (positionFinale())
  
  
  
}

def positionFinale():Boolean = {
	return ((estFaceMur()==true ) && (getDirection()== Direction.NORD))
    
  
     
  }