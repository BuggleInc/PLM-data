def indexOfMaxValue(nums:Array[Int]):Int = {
  var max:Int = 0
  for(i<- 0 to nums.length-1){
    if(nums(i)>max){max=nums(i)}
  }
  for(j<- 0 to nums.length-1){
    if(nums(j) == max){return j}
  }
}