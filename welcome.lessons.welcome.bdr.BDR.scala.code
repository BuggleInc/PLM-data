while (estSurMessage) {
	var u=litMessage()
	if (u=="A") {
		avance()}
	if (u=="B") {
		avance();
		avance()}
	if (u=="C") {
		avance();
		avance();
		avance()}
	if (u=="Z") {
		recule()}
	if (u=="Y") {
		recule();
		recule();}
	if (u=="X") {
		recule();
		recule();
		recule()}
	if (u=="I") {
		droite();
		droite();
		avance()}
	if (u=="L") {
		gauche();
		avance()}
	if (u=="R") {
		droite();
		avance()}
}
