'''def isFacingTrail():
    forward()
    if getCouleurSol()==Color.green:
        return(False)'''
        
    
def isFacingTrail():
    
    if not isFacingWall():
        forward()
        res = (getGroundColor() == Color.green)
        backward()
        return res

isFacingTrail()

