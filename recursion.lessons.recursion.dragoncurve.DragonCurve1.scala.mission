<html><head>  <style type="text/css">
    body { font-family: tahoma, "Times New Roman", serif; font-size:10px; margin:10px; }
    code { background:#EEEEEE; }
    pre { background: #EEEEEE;
          margin: 5px;
          padding: 6px;
          border: 1px inset;
          width: 640px;
          overflow: auto;
          text-align: left;
          font-family: "Courrier New", "Courrier", monospace; }
   .comment { background:#EEEEEE;
              font-family: "Times New Roman", serif;
              color:#00AA00;
              font-style: italic; }
  </style>
</head><body>
<!-- Please don't translate this file but l10n/missions/plm.pot (see https://github.com/oster/PLM/wiki/Working-on-translations) -->
<h2>Courbe du Dragon (1)</h2>

<p>Un exemple classique de la m?thode r?cursive est la courbe du Dragon.</p>

<p>La d?finition de cette courbe est la suivante :<br/> 
la courbe du Dragon d'ordre 1 est un vecteur entre deux points quelconques P
et Q,<br/>
la courbe du Dragon d'ordre n est la courbe du Dragon d'ordre n-1 entre P et
R suivie de la m?me courbe d'ordre n-1 entre Q et R, o? PRQ est le triangle
isoc?le rectangle en R, et R est ? droite du vecteur PQ.  Donc, si P et Q
sont les points de coordonn?es (x, y) et (z,t), les coordonn?es (u, v) de R
sont les suivantes:</p>
<pre>
u = (x + z)/2 + (t - y)/2
v = (y + t)/2 - (z - x)/2
</pre>

<p>Le prototype de la m?thode tra?ant la courbe est le suivant :</p>
<pre>dragon(ordre:Int, x:Double, y:Double, z:Double, t:Double)</pre>


<p>Vous utiliserez la m?thode <code>setPos(x,y)</code> pour d?placer votre
tortue aux coordonn?es (x,y) et la m?thode <code>allerVers(z,t)</code> pour
tracer un trait depuis le point o? la tortue est positionn?e vers le point
de coordonn?es (z,t)</p>

<p>Consultez les objectifs de chaque monde pour comprendre comment ?crire la
fonction.</p> 
</body></html>