def insertionSort() {
	var stockage=0
  var k=0
  for (i<- 1 to getNombreValeurs()-1) {
    stockage=getValeur(i)
    k=i
    while(k!=0 && !plusPetitQue(k-1,stockage)) {
      copie (k-1,k)
      k-=1
    }
    setValeur(k,stockage)
  }
}
