def hunt() {
  while (!estSurBiscuit()) {
		if (isFacingTrail()) {
      avance();
      baisseBrosse();
      leveBrosse();
    } else gauche()
	}
  prendBiscuit();
}     
        

// Copy your isFacingTrail() here
def isFacingTrail():Boolean = {
  if (estFaceMur()) {
      gauche();
    	return false;
  } else {
  	avance();
    if (getCouleurSol() == Color.green) {
  		recule();
      return true;
    } else {
      recule();
      return false;
    }
  }
}
