def bubbleSort2() {
  
  var term = false
  var n = 0
  
      term = false
      
      for (i <- 0 to getValueCount()-2){
        for(j <- 0 to getValueCount()-2-i){
        n = n + 1

        if (isSmaller(i+1,i)){
       		swap(i,i+1)
      	  term = true
        }
      		}
          
        }

  
  
}