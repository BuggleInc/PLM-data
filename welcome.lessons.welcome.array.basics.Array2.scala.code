override def run() {
	var T = new Array[Color](getMondeHauteur());
	T(0) = getCouleurSol();
	for(w<- 1 to T.length-1) {
		T(w) = getCouleurSol();
		avance();
	}
	for(x<-1 to T.length-1) {
		recule();
	}
	def beta() {
		gauche();
		avance();
		droite();
		litMessage();
		val a = litMessage().toInt
		for ( w<- 1 to T.length + 1  ) {
			var o:Int = w + a % ( T.length +1 )
			var c:Color = T(o)
			setCouleurBrosse(c);
			baisseBrosse();
			leveBrosse();
			avance();
								}
			}
			
	beta();

}
