


def isFacingTrail(c:Color): Boolean = { 
  var test = false
  if (estFaceMur()) {
    gauche()
  }
  avance()
  if (getCouleurSol()==c) {
    recule()
    test = true
    return test;
    
  } else {
    recule()
    return test;
  }
}