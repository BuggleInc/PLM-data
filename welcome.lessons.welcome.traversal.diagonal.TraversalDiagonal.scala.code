var diag = 0;
override def run() {
  var cpt : Int = 0
  diagonale(cpt)
}

def diagonale(cpt : Int) {
  for(i<-0 to diag-1){
    ecritMessage(cpt)
    setPos(getX()+1,getY()-1)
    cpt+=1
  }
  diag+=1
  
}