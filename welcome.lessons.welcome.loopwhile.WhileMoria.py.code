back()
while isFacingWall() != True:
    #forward()
    if (isOverBaggle()):
        pickupBaggle()
        back()
    else:
        if (isCarryingBaggle()):
            
        forward()
    '''if (isOverBaggle()):
        back()
        forward(1)
        dropBaggle()
        forward()
    else:
        forward()'''


'''while isFacingWall() != True:
    while isOverBaggle() != True:
        forward()
    while isOverBaggle() == True:
        pickupBaggle()
        back()
    while isCarryingBaggle() == True:
        if (isOverBaggle()):
            back()
            forward(1)
            dropBaggle()
            forward()
        else:
            forward()'''
    
