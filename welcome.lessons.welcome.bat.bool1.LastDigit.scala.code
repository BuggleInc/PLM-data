def lastDigit(a:Int, b:Int):Boolean = {
  var un:Int = a
  var deux:Int = b
  def dernierChiffre(n:Int):Int={
    while (n >9){
      n=n%10
    }
    return n
  }
  dernierChiffre(a)==dernierChiffre(b)
}