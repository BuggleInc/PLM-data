def golomb(num:Int): Int = {
  var compteur = 2
  var tableau = new Array[1,2,2]
  while(compteur<num)
  {
    var T = new Array[Int]
    tableau = tableau + T
    compteur +=1
  }
  return tableau(num)
}