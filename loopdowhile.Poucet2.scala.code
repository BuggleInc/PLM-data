var step = 0;
while (!exitReached()){
  step = 0 
  do{
    forward() 
    if(isOverBaggle()){
      step =step + 1
    }
  }while (!crossing())
     if (step < 3){
       right()
     }else {
       left()
     }
}
forward()