override def run() {
	setCouleur(Color.red)
	for (i <- 1 to 18) {
		if (getCouleur()==Color.black) {
			setCouleur(Color.red)
		} else {
			setCouleur(Color.black)
		}
		for (j <- 1 to 20) {
			avance(100)
			recule(100)
			droite(1)
		}
	}
}
