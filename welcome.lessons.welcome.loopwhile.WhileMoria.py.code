while(estFaceMur()==False):
    if(estSurBiscuit()==False):
        setDirection(Direction.EAST)
    while(estSurBiscuit()==False and estFaceMur()==False):
        avance()
    if(estSurBiscuit()==True):
        prendBiscuit()
        setDirection(Direction.WEST)
        while(estSurBiscuit()==False):
            avance()
        recule()
        poseBiscuit()
        recule()
if(estFaceMur()==True):
    droite()
    avance()
    gauche()
    avance()