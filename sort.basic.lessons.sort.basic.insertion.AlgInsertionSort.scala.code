def insertionSort() {

	for(i<-1 to getNombreValeurs()) 
	{
		val valeur = getValeur(i);
		var j=i;
		while((j>0) && (getValeur(j)>valeur))
		{	
			setValeur(j+1,getValeur(j))
			j-=1
		}
	setValeur(j+1,valeur);
	}	
}
