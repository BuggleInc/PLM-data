for (cpt <- 1 to 10) {
  for (cpt2 <- 1 to 8) {
    avance()
  } 
  gauche()
  
	for (cpt1 <- 1 to 8) {
  	avance()
	}
	gauche()

  for (cpt <- 1 to 8) {
    avance()
  }
	gauche()

	for (cpt3 <- 1 to 8) {
    avance()
  }
	gauche()
}