override def run() {
    while (!estSurBiscuit) {
      if (!estFaceMur) {
        avance()
        if (random3()==0) {
          retourne()
        } else if (random3()==1) {
          droite()
        } else {
          gauche()
        }
      } else {droite()}
    }
  prendBiscuit()
  retourne();droite()
}