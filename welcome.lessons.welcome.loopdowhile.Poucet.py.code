
PremiereFois=True

while (not sortieTrouvee()):
    biscuit=0

    while (PremiereFois or not croisement()):
        PremiereFois=False

        for pas in range(5):
            forward()
            
            if estSurBiscuit():
                biscuit=biscuit+1
       
    if biscuit>=3:
        gauche()
        
    else:
        droite()


forward()
