def solve() {
  var length:Int = getTaillePile()-1
  var max:Int = 0
  while(!estTriee()){
    for (i <- 0 to length){
      if (getRayonCrepe(i) >= getRayonCrepe(max)){
        max = i
      }
    }
    retourne(max+1)
    retourne(length)
    length -= 1
  }
}