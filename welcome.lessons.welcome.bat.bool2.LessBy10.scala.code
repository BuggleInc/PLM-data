def lessBy10(a:Int, b:Int, c:Int):Boolean = {
	if(a<=math.abs(10-b)||a<=math.abs(10-c)||b<=math.abs(10-a)||b<=math.abs(10-c)||c<=math.abs(10-b)||c<=math.abs(10-a)){
		return true
	}
	else{
		return false
	}
}
