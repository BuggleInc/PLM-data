def solve() {
  var l=getStackSize()
  for( i <- 0 to l-1){

    for(j <- 0 to i){
      if(getPancakeRadius(j)>getPancakeRadius(j+1))
	    	swap(j+1)
    }
  }
}




def swap(n : Int){
  flip(n)
  flip(n+1)
}