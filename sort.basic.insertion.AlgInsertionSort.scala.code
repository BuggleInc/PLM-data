def insertionSort() {
  var len = getNombreValeurs()
  for (i <- 1 to len-1) {
    var v = getValeur(i)
    for (j <- 1 to i) {
      if (!plusPetitQue(i-j,v)) {
        copie(i-j,i-j+1)
      }
    }

}
  }