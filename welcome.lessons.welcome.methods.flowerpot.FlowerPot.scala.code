def fleur(c:Color) = {
  setCouleurBrosse(c)
  baisseBrosse()
  for (i <- 1 to 2) {avance()}
  recule()
  gauche()
  for (i <- 1 to 2) {avance()}
  recule()
  droite()
  setCouleurBrosse(Color.YELLOW)
  leveBrosse()
  recule()
}

def growFlowers() = {
  fleur(Color.RED)
}
