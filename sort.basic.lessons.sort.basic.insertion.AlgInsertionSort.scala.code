def insertionSort() {
  var n = getNombreValeurs()
  var v = 8
  var marqueur = 0
  for (i <- 1 to (n-1)) {
    v = getValeur(i)
    marqueur = 0
    for (j<-1 to i) {
       if (getValeur(i-j)>v) {
         echange(i-j,i-j+1)
         marqueur=(i-j)
       }
      setValeur(marqueur,v)
    }
  }
}