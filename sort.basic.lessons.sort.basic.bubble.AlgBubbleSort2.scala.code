def bubbleSort2()
{
  var max = getNombreValeurs() - 2
 
  for (l <- 0 to getNombreValeurs() - 1)
  {
    for (i <- 0 to max)
    {
      if (plusPetit(i + 1, i))
    		echange(i, i + 1)
    }
    max -= 1
  }
}
