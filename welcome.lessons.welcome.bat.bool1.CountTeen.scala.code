def countTeen(a:Int, b:Int,c:Int,d:Int): Int = { 

var ados =0
	if((a>=13) && (a<=19)){
    	ados=ados+1
  		}
	if((b>=13) && (b<=19)){
    	ados=ados+1
  		}
	if((c>=13) && (c<=19)){
    	ados=ados+1
  		}
	if((d>=13) && (d<=19)){
    	ados=ados+1
  		}
  return ados
}