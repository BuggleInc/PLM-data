def isFacingTrail():Boolean = {
  while (estSurBiscuit==false) {
    if (estFaceMur()==true) {
      left();
    }
    forward();brushUp();
    if (getCouleurSol()==Color.green) {
      backward(); brushDown();brushUp(); forward();
    } else {
      backward(); left();
    }
  }
  return true
}

