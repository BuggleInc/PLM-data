def solve() {
  var lgr = getSize()
  for (i<-0 to lgr-2){
    getColor(i) match {
      case BLUE => 
      case RED => if(getColor(i+1)!=RED){
        swap(i,i+1)
      }
      case WHITE=> if(getColor(i+1)!=BLUE){
        swap(i,i+1)
      }
    }
  }
  
}