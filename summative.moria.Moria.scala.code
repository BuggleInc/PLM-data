var posfinale=false
while (posfinale==false){
  droite(); droite()
  while ((estSurBiscuit()==false) && (estFaceMur()==false)){
    avance();
  }
  if (estSurBiscuit==true){
    prendBiscuit()
    droite(); droite();
    while ((estSurBiscuit==false) && (estFaceMur()==false)){
      avance();
    }
    if (estSurBiscuit==true){
      recule(); poseBiscuit(); recule()
    } else {
      poseBiscuit(); recule()
    }
  } else {
    gauche(); avance(); droite(); avance(); posfinale=true
  }
}