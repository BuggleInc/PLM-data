def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if(steps != 0){
    avance(length);
    droite(angle);
    tree(steps-1,length*shrink,angle,shrink);
    gauche(angle);
    recule(length);
    tree(steps-1,length*shrink,360-angle,shrink);
    gauche(angle);
    recule(length);
    
  }else{
    return
  }
  
  
}