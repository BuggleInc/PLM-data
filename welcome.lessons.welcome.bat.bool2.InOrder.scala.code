def inOrder(a:Int, b:Int, c:Int, bOk:Boolean):Boolean = {
  (a, b, c, bOk) match {
    case (i, j, k, false) if ((j > i) & (k > i)) => return true
    case (i, j, k, true) if (k > i) => return true
    case (_, _, _, true) => return false
    case (_, _, _, false) => return false
  }
}