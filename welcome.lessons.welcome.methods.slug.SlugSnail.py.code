def isFacingTrail(color):
    if not estFaceMur():
        forward()
        if getCouleurSol()==Color:
            backward()
            return True
        else:
            backward()
            return False

def hunt(color):
