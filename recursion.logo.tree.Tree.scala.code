def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  baisseCrayon()
	if (steps==0){
   
  }
  else {
 		avance(length)
    droite(angle)
    current(steps)
    tree(steps-1,length*shrink,angle,shrink)
    gauche(angle)
    gauche(angle)
    
    tree(steps-1,length*shrink,angle,shrink)
    droite(angle)
    recule(length)
    
  }
  	
  	
 
}