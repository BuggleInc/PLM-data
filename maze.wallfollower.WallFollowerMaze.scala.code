override def run() {
  
  def keepHandOnSideWall() {
    while (estDosMur && !estFaceMur) {
      droite()
      if (estDosMur) {return}
    }
    
  }
  while (!estSurBiscuit) {
    keepHandOnSideWall()
    avance()
  }
  
  
  
}