x=a=getX()
setCouleurBrosse(Color.pink)
y=b=getY()
n=0
for n in range(10):
    for y in range(b-8):
        setPos(x,y)
        baisseBrosse()
        leveBrosse()
    gauche()
    for x in range(a-8):
        setPos(x,y)
        baisseBrosse()
        leveBrosse()
    gauche()
    for y in range(b):
        setPos(x,y)
        baisseBrosse()
        leveBrosse()
    gauche()
    for x in range(a):
        setPos(x,y)
        baisseBrosse()
        leveBrosse()
    gauche()
    if(n==9):
        setCouleurBrosse(Color.red)
