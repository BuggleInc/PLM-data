def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
  var b=0;
  if (isBirthday) b+=5;
  if (speed<=60+b) {
    return 0
  } else if (speed>=61+b & speed<=80+b) {
    return 1
  } else {
    return 2
  }
}