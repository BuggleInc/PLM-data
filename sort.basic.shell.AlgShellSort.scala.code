def shellSort()  {
  


var ecart = (getNombreValeurs()/ 2).toInt
while (ecart >=1)
  
  {insertion(ecart)
 
  ecart = (ecart.toDouble / 2).toInt
  }
}
  
  
  
def insertion(ecart:Int) {
  for (i<- ecart to getNombreValeurs()-1)
      {
       var j = i
       var v = getValeur(i)
       
       while ((j>=ecart)&&(!plusPetitQue(j-ecart,v)))
           
          {copie(j-ecart,j); j=j-ecart}
       
        setValeur(j, v) }
}