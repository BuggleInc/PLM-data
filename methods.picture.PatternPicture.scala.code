override def run() {
  faireMotif()
  change()
  faireMotif()
}


def faireMotif(){
  
    Ligne(Color.RED, Color.BLUE)
    Ligne(Color.BLUE, Color.GREEN)
    Ligne(Color.GREEN, Color.YELLOW)
    Ligne(Color.YELLOW, Color.RED)
		for (j<- 1 to 3){
      avance()
    
   }
  	avance()
    Ligne(Color.RED, Color.BLUE)
    Ligne(Color.BLUE, Color.GREEN)
    Ligne(Color.GREEN, Color.YELLOW)
    Ligne(Color.YELLOW, Color.RED)
		for (j<- 1 to 3){
      avance()
    
   }
  
}

  
  





def Ligne(a:Color, b:Color){
  marque(a)
  marque(b)
  avance()
  gauche()
  
 
}

def marque(c:Color){
  setCouleurBrosse(c)
  avance()
  baisseBrosse()
  leveBrosse()
}

def change(){
  
  for(i<- 1 to 5){
    avance()
  }
  droite()
  for(i<- 1 to 8){
    recule()
  }
  
}