override def run() {
  while (!estSurBiscuit()) {
		if (getDirection == Direction.NORTH) {
    	while (!estFaceMur()) {
    		avance();
    	}  
    } else keepHandOnSideWall()
  }
  prendBiscuit()
}


def keepHandOnSideWall() { 
    gauche();
  	if (estFaceMur()) {
    droite();
      if (!estFaceMur()) {
        avance();
      } else droite()
    } else {
    	avance();
    }
}

def murGauche() {
  var res = false;
  gauche();
  if (estFaceMur()) {
    res = true
  }
  droite
  return res
}