def isFacingTrail():Boolean = {
	var y:Boolean=false;

	if (!estFaceMur()) {
		avance()
	}
	
	avance();
    	if (getCouleurSol()== Color.green) {
    		y= true;
    	}
    	recule();
    	return y;
 }
