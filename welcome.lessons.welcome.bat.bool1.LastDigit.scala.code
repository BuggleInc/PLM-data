def lastDigit(a:Int, b:Int):Boolean = {
  var c : Int = a;
  var d : Int = b;
      
  while (c >= 10 || d >= 10)
  	{
      c = c % 10 ;
      d = d % 10 ;
    }
  if (c == d )
  		{return true }
  else 
  {false}}