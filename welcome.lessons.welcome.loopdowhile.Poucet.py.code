b=1
premiereFois = True
while sortieTrouvee() == False :
    while premiereFois == True or not croisement():
            premiereFois == False
            forward()
    if estSurBiscuit() :
            b=b+1
    if croisement() == True and baggel>=3 :
            gauche()
            for k in range(4):
                forward()
    elif croisement() == True and baggel<=2 :
            droite()
            for k in range(4) :
                avance()
