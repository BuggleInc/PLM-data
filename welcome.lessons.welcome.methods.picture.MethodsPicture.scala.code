override def run() {
  faireV(Color.YELLOW)
  faireV(Color.RED)
  faireV(Color.BLUE)
  faireV(Color.GREEN)
}

def faireV(c:Color){
  avance(2)
  marquer(c)
  gauche()
  avance()
  droite()
  avance()
  marquer(c)
  avance()
  droite()
  avance()
  marquer(c)
  gauche()
  avance(2)
  gauche()
}

def marquer(c:Color){
  setCouleurBrosse(c)
  baisseBrosse()
  leveBrosse()
 
}