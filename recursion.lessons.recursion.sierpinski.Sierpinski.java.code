public void sierpinski(int level, double length) {
	if (level == 0) {
		for (int i = 0; i < 3; i++){
			forward(length);
			right(120.0);
		}
		return;
	}
	for (int i = 0; i < 3; i++) {
		sierpinski(level - 1, length / 2);
		forward(length / 2);
		sierpinski(level - 1, length / 2);
		forward(length / 2);
		right(120.0);
	}
}
