override def run() {
  var cpt:Int = 0
  ecritMessage(cpt)
  cpt+=1
	while (cpt!=49) {
    if (getY()==6) {
      setPos(getX()+1, 0)
    }
    if (getY()<6) {
      setPos(getX(), getY()+1)
    }
    ecritMessage(cpt)
    cpt=cpt+1
  }
}