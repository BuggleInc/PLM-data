def polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double) {
	avance(length)
	droite(360/sides)
	avance(length)
	droite(360/sides)
	avance(length)
	droite(360/sides)
	avance(length)
	droite(360/sides)
	avance(length)
	droite(360/sides)
}
