def insertionSort() {
  var len = getNombreValeurs()
  for (i<- 1 to len-1) {
    var j = 1
    if (plusPetit(i,i-1)) {
      while(plusPetit(i,i-j) && i-j>=0) {
      j+=1
    };echange(i,i-j)
    }
  }
}