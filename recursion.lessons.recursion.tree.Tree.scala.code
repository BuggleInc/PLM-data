def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {

	if(steps%6 != 0)
	{
	current(steps % 5);
	avance(length);
	droite(angle);
	tree(steps + 1, length * shrink, angle, shrink);
	}
	else
	{
	current(steps % 6);
	recule(length);
	droite(angle);
	tree(1, length * shrink, angle, shrink);
	}
}
