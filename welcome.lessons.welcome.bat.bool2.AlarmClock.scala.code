def alarmClock(day:Int, vacation:Boolean): String = {
  if ( (0==day && 6==day) && vacation )
  	return "off"
  else if ( !(0==day && 6==day) && vacation )
  	return "10:00"
  else if ( (0==day && 6==day) && !vacation )
  	return "10:00"
  else
  	return "7:00"
}