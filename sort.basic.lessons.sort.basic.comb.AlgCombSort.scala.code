def combSort() {
	var ecart=getNombreValeurs()
	var fin=false
	do {
		fin=true
		if (ecart>1) ecart=(ecart.toDouble / 1.3).toInt
		var i=0
		while ((i+ecart)<getNombreValeurs()) {
			if (plusPetit(i+ecart,i)) {
				echange(i,i+ecart)
				fin=false
			}
		}
	} while (fin==false || ecart>1)
}
