def alarmClock(day:Int, vacation:Boolean): String = {
	vacation match {
		case false if (day>0 && day<6) => "7:00"
		case false if (day==0 || day==6) => "10:00"
		case true if (day>0 && day<6) => "10:00"
		case _ => "off"
	}
}
