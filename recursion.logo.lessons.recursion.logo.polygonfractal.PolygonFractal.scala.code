def polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double) {
  if (levels==0){
    for(i<- 0 to sides-1){
    avance(length);
    droite(360/sides);
    }
  }else{
    for(i<- 0 to sides-1){
    polygonFractal(levels-1, length/2);
    avance(length);
  	droite(360/sides);
    }
}
}