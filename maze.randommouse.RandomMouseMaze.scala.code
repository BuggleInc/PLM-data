override def run() {
    while (!estSurBiscuit) {
      if (!estFaceMur) {
        avance()
        if (random3()==0) {
          avance()
        } else if (random3()==1) {
          droite()
        } else {
          gauche()
        }
      } else {droite()}
    }
}