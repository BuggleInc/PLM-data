def bubbleSort2() {
  var a=true
  var b=1
  while (a==true){
    a=false
  	for (i <- 1 to (getNombreValeurs()-b)){
  		if (getValeur(i-1)>getValeur(i)){
        echange(i-1,i)
       	a=true
      }
    }
    b=b+1
  }
  for (i <- 1 to (getNombreValeurs())){
  }
        
}