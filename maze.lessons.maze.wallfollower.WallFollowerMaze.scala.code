override def run() {
    def keepHandOnRightWall() = {
    	if (!(estFaceMur)) {
		droite();
		if (estFaceMur) {gauche()} else {avance()}
    	} else {gauche()}
    }
    while (!(estSurBiscuit)) {
    	keepHandOnRightWall();
    	if (!estFaceMur) {avance()};
    }
    prendBiscuit();
}
