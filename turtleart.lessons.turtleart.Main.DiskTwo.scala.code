override def run() {
	for (c <- 1 to 8)
	{
		quartCercle(Color.black)
		quartCercle(Color.red)
	}

}

def quartCercle(couleur:Color)
{
	setCouleur(couleur)
	for (c <- 1 to 20)
	{
		avance(100); 
		recule(100); 
		droite (1);
	}
}