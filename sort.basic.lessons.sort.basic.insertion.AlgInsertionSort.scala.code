def insertionSort() {

	var j=0
	for(i<- 1 to getNombreValeurs()) 
	{
		val valeur = getValeur(i)
		j=i
		while((i>0) && (!plusPetit(i-1,i)))
		{
			
			echange(j,j-1)
			j=j-1
		}
	setValeur(j,valeur);
	}	
}
