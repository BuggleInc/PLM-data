        def goAndGet() {
        while (!estSurBiscuit) {
        	avance()
        }
        while (estSurBiscuit) {
        	prendBiscuit
        }
        setY(0);
        avance();
        poseBiscuit
        }

        for (i <- 1 to 7) {
            goAndGet();
            right();
            forward();
            left();
        }
