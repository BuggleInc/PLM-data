def selectionSort() {
  
  
  var test=false;
  for(j<-0 to getNombreValeurs()-2)
  {

    var ind=j;
    
    for(i<-j+1 to getNombreValeurs()-1)
    {
      if(plusPetit(ind,i))
      {
        ind=i;
      }
    }
    if(j!=ind)
    	echange(j,ind)
  }
}