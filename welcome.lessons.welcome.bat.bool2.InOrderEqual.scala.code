def inOrderEqual(a:Int, b:Int, c:Int, equalOk:Boolean):Boolean = {
	if(equalOK && a <= b && b <= c) {
		return true
		}
	return false
	
	if(equalOK && a < b && b < c) {
		return true
	}
	return false
}
	
