public void insertionSort() {
	int lgr = getNombreValeurs();
	int v = 0;
	for(int i = 1 ; i < lgr; i++)
	{
		v = getValeur(i);

		for(int k=i; k>=0; k--)
		{
			if(getValeur(k)<v)
			{
				echange(i,k+1);
			}
		}
	}
}
