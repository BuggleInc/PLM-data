override def run() {
  for (k<- 1 to 8) {
    avance(50:Int)
    droite(45:Double)
  }
  avance(25:Int)
  for (k<- 1 to 8) {
  	droite(45:Double)
  	avance(25:Int)
  	gauche(45:Double)
  	avance(25:Int)
  	droite(45:Double)
  }
  for (k<- 1 to 4) {
    for (j<-1 to 6) {
    	droite(45:Double)
    	avance(25:Int)
    	}
    /*droite(45:Double)
    avance(25:Int)
    droite(45:Double)
    avance(50:Int)
    droite(45:Double)
    avance(50:Int)
    droite(45:Double)
    avance(25:Int)*/
  }
}