def indexOfMaxValue(nums:Array[Int]):Int = {
  var index = 0
    var i = nums(0)
  var compteur = 0
  while (compteur<nums.length)
  {
    if (nums(compteur)>i)
    {
      i = nums(compteur)
      index = compteur
    }
    compteur += 1
  }
  return index
}
