override def run() {
  var x =0
    while(!estSurBiscuit()){
      x=random3()
      if(x==0 && !estFaceMur()){avance();}
      else if(x==1){droite()}
      else{gauche()}
      
    }
  prendBiscuit()
}
