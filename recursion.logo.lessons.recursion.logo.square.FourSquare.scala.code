
 def carre() =
  	{
      avance(100)
      droite(90)
      avance(100)
      droite(90)
      avance(100)
      droite(90)
      avance(100)
    }
 
override def run() {
 
for(i <- 0 to 3)
  {
    carre()
    droite(180)
  }
}
