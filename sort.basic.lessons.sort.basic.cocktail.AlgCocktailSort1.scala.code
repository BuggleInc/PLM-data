def cocktailSort() {
var a=true
    if (a==false){
      return
    }
    a=false
  for (j <- 1 to getValueCount()-1){
    if (!isSmaller(j,j+1)){
      swap(j,j+1)
      a=true
		}
}
	for (j <- getValueCount()-1 to 1 by -1){
    if (!isSmaller(j,j+1)){
      swap(j,j+1)
      a=true
		}
}    
}