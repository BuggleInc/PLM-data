def polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double) {

if(levels==1){
	for(x<-1 to sides)
	avance(length)
	if(levels%2==0){
		droite(360/sides)
	}else{gauche(360/sides)}
	
}else{

}
}