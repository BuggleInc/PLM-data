def spiral(steps, angle, length, increment):
      if steps==0:
          avance(length+steps*increment)
          gauche(angle)  
      else :
         spiral(steps-1, angle, length, increment)

spiral(10, 90, 0, 3)