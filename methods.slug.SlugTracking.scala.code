def isFacingTrail():Boolean = {
  		forward()
   		if(isFacingWall()){
      return false
      }
 			 
      else if (getGroundColor() == Color.green){
      return true
    }
    
  		else {
      backward()
      return false
  }
}