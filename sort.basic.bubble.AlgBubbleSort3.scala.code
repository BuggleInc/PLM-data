def bubbleSort3() {

  var b=getNombreValeurs()-2 
  var inv=true
  
  for (i<-b to 0 by -1) {
    inv=false
     for (j<-0 to i) {
      if (!plusPetit (j,j+1)) {
       echange(j,j+1)
        inv=true
        
      }
       
     }
  if (inv==false) {return}}
    }