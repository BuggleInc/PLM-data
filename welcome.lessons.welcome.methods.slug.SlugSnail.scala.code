def hunt(c:Color) {  
  while(!estSurBiscuit()){
   	while (!isFacingTrail(c)){
  	 	gauche()
    }
  	 while (isFacingTrail(c)){
   			
    	avance()
      baisseBrosse()
     	leveBrosse()
     }
  	hunt(c)
  }
  
  prendBiscuit()
  return
}
 // Write your code here
 }

 // here comes your isFacingTrail method