override def run() {
  val width = getWorldWidth();
  val height = getWorldHeight();
  var i = 0;
  var x = 0;
  var y = 0;
  while(x!=9){
    writeMessage(x*height + y);
    y=y+1;
    if(y==9){
      y=0;
      x=x+1;
    }
    setPos(x, y)
  }
}