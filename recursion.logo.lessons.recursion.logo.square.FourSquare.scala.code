override def run() {
  faireCarre()
  droite(90)
  faireCarre()
  droite(90)
  faireCarre()
  droite(90)
  faireCarre()
  droite(90)
}

def faireCarre() 
{
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
  droite(90)
}
