var diag = 0;
override def run() {
  var cpt:Int = 0
  for (i <- 0 to getMondeHauteur()-1) {
    setPos(0,i)
    ecritMessage(cpt)
    cpt+=1
    while(getX()!=i) {
      setPos(getX()+1,getY()-1)
      ecritMessage(cpt)
      cpt+=1
    }
  }
  for (j <- 1 to getMondeLargeur()-1) {
    setPos(j, getMondeHauteur()-1)
    ecritMessage(cpt)
    cpt+=1
    while(getY()!=j) {
      setPos(getX()+1,getY()-1)
      ecritMessage(cpt)
      cpt+=1
		}
  }
}