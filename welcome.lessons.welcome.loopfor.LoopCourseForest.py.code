tour=0
cote=0

for tour in range(7):
    for cote in range(4):
        forward()
        gauche()
        forward(2)
        droite()
        forward(4)
        droite()
        forward(2)
        gauche()
        forward(4)
        gauche()
    cote=cote+1
tour=tour+1
cote=0