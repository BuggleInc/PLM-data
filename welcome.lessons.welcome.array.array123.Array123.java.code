boolean array123(int[] nums) {
	int[] sequence = {1,2,3};
	int seqIndex = 0;

	for(int i = 0; i < nums.length; i++){
		if(sequence[seqIndex] == nums[i])
			seqIndex++;
		else if(sequence[0] == nums[i])
			seqIndex = 1;
		
		if (seqIndex == sequence.length)
			return true;
	}
	
	return false;
}
