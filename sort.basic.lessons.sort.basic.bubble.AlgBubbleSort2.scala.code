def bubbleSort2()  {
  
  var count = 0
  var continue = true
  while (continue){
  count += 1
  continue = false
  for (i <- 1 to getValueCount()-1 - count){
      if (isSmaller(i,i-1)){
        swap(i, i-1)
        continue = true
      } 
  }
  }
}