def gnomeSort() {
  var pos=0
  do {
    if (!plusPetit(pos,pos+1)) {
      echange(pos,pos+1)
      if (pos==0) {
        pos=pos+1
      } else {
        pos=pos-1}
    } else {
      pos=pos+1
    }
  } while (pos!=getNombreValeurs()-1)
}
