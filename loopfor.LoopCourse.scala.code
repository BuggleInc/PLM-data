for (compteur1 <- 1 to 10) {
  for (compteur <- 1 to 8) {
    avance();}
  gauche();
  for (compteur <- 1 to 8) {
    avance();}
  gauche();
  for (compteur <- 1 to 8) {
    avance();}
  gauche();
  for (compteur <- 1 to 8) {
    avance();}
  gauche();
   }