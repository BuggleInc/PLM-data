def insertionSort() {
	var v=0;
	var tmp=0;
	for (i <- 1 to getNombreValeurs()-1 by 1) {
		v=getValeur(i);
		for (j <- i-1 to 0 by -1) {
			if (plusPetit(j+1,j)) {
				copie(j,j+1)
				tmp=getValeur(j);
			}
		}
		setValeur(v,tmp);
	}
}
