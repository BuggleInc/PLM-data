override def run() {

	def faireCarre() {
		for (i<-1 to 4) {
			avance(100)
			gauche(90)
		}
	}

	faireCarre()
	recule(100)
	faireCarre()
	droite(90)
	faireCarre()
	avance(100)
	gauche(90)
	avance(100)
	faireCarre()
	
}
