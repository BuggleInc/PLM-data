def bubbleSort2() {
  for (i<-0 to getNombreValeurs()-2) {
    for (j<-0 to getNombreValeurs()-2-i) {
      if (plusPetit(j+1:Int,j:Int)) {
        echange(j+1:Int,j:Int)
      }
    }
  }
}

/*for (i<-getNombreValeurs()-2 to 0) {
    for (j<-0 to i) {
      if (plusPetit(j+1:Int,j:Int)) {
        echange(j+1:Int,j:Int)
*/
