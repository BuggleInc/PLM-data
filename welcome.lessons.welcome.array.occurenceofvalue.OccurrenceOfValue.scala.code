def occurrences(nums:Array[Int],lookingFor:Int):Int = {
   val taille = nums.length;
   var nbB = 0;
   for ( i <- 0 to taille-1 ) {
   	if ( nums(i) == lookingFor ) {
   		nbB += 1
   	}
   }
   nbB;
}
