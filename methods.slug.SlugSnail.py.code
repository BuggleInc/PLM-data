def isFacingTrail(color):
   if isFacingWall():
      return False
   	else :
      forward()
      res =(getGroundColor()==color)
      backward()
      return res

def hunt(color):
  while isOverBaglle()== False :
    brushUp()
    if isFacingTrail(color):
      brushDown()
      forward()
      brushUp()
    else : 
      left()
	pickupBaggle()
hunt(getParam(0))
      
