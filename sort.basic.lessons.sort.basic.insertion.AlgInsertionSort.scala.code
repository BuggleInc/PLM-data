def insertionSort() {
  var v:Int = 0
  for (i <- 1 to getNombreValeurs()-1) {
    v = getValeur(i)
    var j:Int = 0
    while (v < getValeur(i-j)) {
      if (i-j > 0) {
      	copie(i-j,i-j+1)
      	j += 1
      }
    }
    setValeur(i-j-1,v)
  }
}