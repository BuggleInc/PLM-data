def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {
 var ados = 0
  if ((13<=a) && (a<=19)){ ados= ados+1}
  if (13<=b && b<=19){ ados= ados+1}
  if (13<=c && c<=19){ ados= ados+1}
  if (13<=d && d<=19){ ados= ados+1}
  return ados
}
