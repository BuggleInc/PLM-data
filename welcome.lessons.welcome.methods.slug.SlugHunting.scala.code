def hunt() {
  while (estSurBiscuit()==false) {
    baisseBrosse()
    for (i<- 1 to 4) {
    if (isFacingTrail()==true) {
      avance()
      gauche();}
      
        
    }
  }
    
}

def isFacingTrail():Boolean = { 
  var test = false
  if (estFaceMur()==true) {
    gauche()
  }
  avance()
  if (getCouleurSol()==Color.green) {
    recule()
    test = true
    return test;
    
  } else {
    recule()
    return test;
    
    }
  
    
}