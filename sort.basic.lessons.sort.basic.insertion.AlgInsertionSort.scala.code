def insertionSort() {
  var lgr = getNombreValeurs()
  var v = 0
  for (i <- 1 to lgr){
    copie(i,v)
    for (j <- 1 to lgr){
      if (plusPetit(v,i-j)){
        setValeur(i,getValeur(i-j))        
      }      
    }    
  }
}