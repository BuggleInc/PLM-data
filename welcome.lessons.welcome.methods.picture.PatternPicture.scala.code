override def run() {
  makePattern();
}

def  mark() {
  brushDown();
  brushUp();
}

def makeTile(c:Color) {
  setBrushColor(c);
  forward();
  mark();
  
  left(); forward(); left(); forward();
  mark();
  
  left(); forward(); left();
  
  forward(3); left();
}

def makeSquare() {
  makeTile(Color.yellow);
  makeTile(Color.red);
  makeTile(Color.blue);
  makeTile(Color.green);
}

def makePattern() {
  makeSquare()
	
}