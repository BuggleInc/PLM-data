def insertionSort() {
  
  var i = 0;
  var j = 0;
  var v = 0;
  
  for (i <- 1 to getNombreValeurs()-1) {
    v = getValeur(i);
    j = i;
    
    while (plusPetitQue(j-1,v) == false && j >0) {

      copie(j-1, j);
      j -= 1

    } 

    setValeur(j, v);

    
  }
  
  
}