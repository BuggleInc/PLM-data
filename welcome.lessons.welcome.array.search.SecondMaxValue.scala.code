def max2Value(nums:Array[Int]): Int = {
  var max = 0
  max = nums(0)
  
  var max2 = Integer.MIN_VALUE
  
  
  for(i<-nums)
  {
    if (i>max)
    {
   		max2 = max
 			max = i
    }   
    else if (i < max && i> max2)
    {
      max2 = i
    }
  }
  return max2
}