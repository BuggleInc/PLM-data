def solve() {
  for(i<-0 to getSize()/2-1){
    
    if(getColor(i)<getColor(getSize()-1-i)){
			swap(i,getSize()-1-i);
    }
    if(getColor(i)==getColor(getSize()-1-i)){
			swap(i,getSize()-i);
    }
    
  }
}