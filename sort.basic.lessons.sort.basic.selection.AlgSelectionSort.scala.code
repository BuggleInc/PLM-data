def selectionSort() {
  var min : Int = 0
  var imin : Int = 0
  var temp : Int =0
  for(i<-0 to getNombreValeurs()-2){
    min=getValeur(i);
    imin = i;
    for(k <-i to getNombreValeurs()-1){
      if(plusPetit(k,imin)){imin=k;}
    }
    echange(i,imin)
    
  }
  
}