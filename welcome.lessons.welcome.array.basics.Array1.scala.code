override def run() {
  var tab = new Array[Color](6);
  remplirTableau(tab);
  colorier(tab)
}


def remplirTableau(t:Array[Color]){
  for (i<- 0 to 4){
    t(i+1)=getCouleurSol();
    avance();
  }
  t(0)=getCouleurSol();
  avance();
}

def seReplacer(){
  gauche();avance();droite();
  recule(5)
}

def colorier(t:Array[Color]){
  for (i<-0 to 4){
  	setCouleurBrosse(t(i));
    baisseBrosse();
    leveBrosse();
    avance();
  } 
}

