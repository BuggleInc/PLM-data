def selectionSort() {
  for (i <- 1 to getNombreValeurs()) {
    var min = getValeur(i)
    var ind = i
    for (j <- i to getNombreValeurs()) {
      if (getValeur(j)<min) {
        min = getValeur(i)
        ind = j
      }
    }
    echange(i,ind)
  }
}

