def insertionSort() {
  var L = getNombreValeurs()
  var v = 0
  var cpt =0
  for (i <- 1 to L-1){
    v = getValeur(i)
    while (getValeur(i-1) > v) {
      setValeur(i,getValeur(i-1))
      cpt = cpt +1
      if (i!=0){
        i = i - 1
      }
    }
    setValeur(i-1-cpt,v)
  }
}