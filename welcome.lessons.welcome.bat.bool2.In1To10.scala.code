def in1To10(a:Int, outsideMode:Boolean):Boolean = {

	var bool = false;
	
	if(!outsideMode)
	{
		if(a >= 10 && a<=20)
		{
		bool = true;
		}
	}
	else
	{
		if(a <= 10 && a >= 20)
		{
		bool = true;
		}
	}

		return bool;
	
}
