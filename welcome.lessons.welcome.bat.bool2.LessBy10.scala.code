def lessBy10(a:Int, b:Int, c:Int):Boolean = {
  if ( diff(a,b) >= 10 || diff(b,c) >= 10 || diff(c,a) >= 10 ){
    return true;
  } else {
    return false;
  }
}
 
def diff(a:Int,b:Int) : Int = {
  if ( a-b >= 0 ){
    return a-b;
  } else {
    return b-a;
  }
}