def insertionSort() {

  var v = 0
  var mem = 0

  for (i<- 1 to getValueCount()-1){

    v = getValue(i)

    for (j<- 1 to i){
      if(!isSmallerThan(i-j,v+1)){
        mem=i-j;
        copy(i-j,i-j+1);
      }
    }
    
		setValue(mem,v);
  }
}