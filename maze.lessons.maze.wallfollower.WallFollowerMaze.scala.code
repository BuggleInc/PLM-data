override def run() {

while (!estSurBiscuit) {
	if (murDroite() && !estFaceMur) {
		avance()		
	} else if (murDroite() && estFaceMur) {
		gauche()
	} else if (!murDroite()) {
		droite()
	} else {
		droite();droite()	
	}
}

def murDroite():Boolean = {
	droite()
	if (!estFaceMur) {
		gauche()
		return false
	} else {
		gauche()
		return true
	}
	
}

}