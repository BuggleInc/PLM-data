def bubbleSort2()  {
  var a:Boolean = false
  var iteration:Int = 0
  while (a == false) {
    a = true
    for (i <- 0 to getNombreValeurs()-2-iteration) {
      if (getValeur(i)>getValeur(i+1)) {
        a = false
        echange(i,i+1)
      }
    }
    iteration = iteration + 1
  }
}