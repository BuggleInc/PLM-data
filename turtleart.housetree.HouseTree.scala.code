override def run() {
  def maison(l:Int) {
  avance(l);
  droite(90);
  avance(l);
  droite(90);
  avance(l);
  droite(90);
  avance(l)
  ;droite(90)
	avance(l)
	droite(30);avance(l);droite(120)
avance(l);droite(30);avance(l);droite(90);avance(l);droite(90)
  }
  
  for (i<-0 to 3) {maison(30);gauche(90);avance(50)} gauche(90)
  
  
}