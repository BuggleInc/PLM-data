boolean isFacingTrail() {
boolean greenCell = getGroundColor().equals(Color.green);

    		if(isFacingWall()){
    			return false;
    		}
    		forward();
    			
/*
		if(getGroundColor().equals(Color.green)){
    			greenCell = true;
    		}

		if(!getGroundColor().equals(Color.green)){
			greenCell = false;
		}    		

    		if(isOverBaggle()){
    			return true;
    		}
*/
    return (greenCell || isOverBaggle() );
}//end function 
