def sierpinski(level:Int, length:Double) {
  
  if(level == 0){
  
    for(i <- 0 to 2){

      avance(length);
      right(120.00);

    }
    
  } else{
    
    sierpinski(level-1, length/2)
    avance(length);
 		right(120.00);
    sierpinski(level-1, length/2)
    
  }
  
}