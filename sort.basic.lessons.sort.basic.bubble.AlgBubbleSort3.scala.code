def bubbleSort3() {

var chgt:Boolean = false

	for (i <- getNombreValeurs()-2 to 0 by -1) {
		for (j <- 0 to i) {
			chgt = false
			if (getValeur(j+1) < getValeur(j)) {
				echange(j,j+1)
				chgt = true
			} 
		}
		if (chgt = false) {
			return
		}
	}
	
}
