override def run() {

def keepHandOnSideWall() {
	while (!(avoirMurGauche())) {
		gauche();
		avance();
		droite();
	}
	gauche();
	while (estFaceMur()) {
	droite();
	}
	avance();
		}

def avoirMurGauche(): Boolean = {
	gauche();
	if (estFaceMur()) {
		return true;
		droite();
	}
	droite();
	return false;
	
}

 while (!(estSurBiscuit())) {
 	if (avoirMurGauche()) {
 	keepHandOnSideWall()
 }
 }
    prendBiscuit();

}
