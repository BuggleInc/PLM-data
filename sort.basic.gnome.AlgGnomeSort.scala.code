def gnomeSort() {
	var gnome:Int = 0;

	while (gnome < getNombreValeurs()) {
		if (gnome == 0) {
			gnome += 1;
		} else if (getValeur(gnome) < getValeur(gnome - 1)){
			echange(gnome - 1, gnome)
			gnome -= 1 ;
		} else {
			gnome += 1;
		}
		
	}
	
}
