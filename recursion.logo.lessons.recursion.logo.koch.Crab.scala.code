def crab(levels:Int, length:Double) {
  if(levels==0){
    avance(length)
  }else{
    var angle=45;
    var newLength = length/(Math.sqrt(2))
    gauche(angle)
    crab(levels-1,newLength)
    droite(2*angle)
    crab(levels-1,newLength)
    gauche(angle)
  }
}