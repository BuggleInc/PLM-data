def shellSort() {
  var ecart:Int = ((getNombreValeurs().toDouble)/2).toInt
  while(ecart>0){
    for (i <- 1 to getNombreValeurs()-1){
      var tmp:Int = getValeur(i)
      var j:Int = i
      while ((j-ecart>=0)&&(tmp<getValeur(j-ecart))){
        setValeur(j,getValeur(j-ecart))
        j = j-ecart
      }
      setValeur(j,tmp)
    }
    ecart = ((ecart.toDouble)/2).toInt
  }
}