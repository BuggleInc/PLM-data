def posNeg(a:Int, b:Int, negative:Boolean):Boolean = {
	if (a<=0 && b>=0 || a>=0 && b<=0 ) {
		return true 

	if ( negative==true) {
		a<=0 && b<=0
	}
	else { return true }
	}
	else {return false }
}
