def isFacingTrail():Boolean = {
  var res=false
  avance();
  if (estFaceMur()) {
    gauche();
    	if (getCouleurSol()==Color.green) {
  	res = true;
  	recule();
      } else gauche()
  }
  return res;
}
