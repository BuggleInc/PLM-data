def array667(nums:Array[Int]): Int = {
  var n =nums.length
  var cnt=0
  
  for (i <- 0 to n-2){
    if ((nums(i)==6 && nums(i)==nums(i+1)) || (nums(i)==6 && nums(i+1)==7) ){
      cnt=cnt+1
    }
  }
  return cnt
}
