override def run() { 
  doTheThing()
}

def doTheThing() {
  pasDeSerpent()
  while (!estFaceMur()) {
    avance()
    droite()
    if (estFaceMur())
    	gauche(); gauche()
    pasDeSerpent()   
  }
}

def pasDeSerpent() {
  while (!estFaceMur()) {
    avance()
  }
  setDirection(Direction.NORTH)
}