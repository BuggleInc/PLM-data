def insertionSort() {
	var v = 0
	var ech = 0
	
	for (i <- 1 to getNombreValeurs()-1) {
		v = getValeur(i)
		for (k <- i-1 to 1 by -1) {
			if (v < getValeur(k)) {
				echange(k,k+1)
				ech = k
			}
		}
		setValeur(ech,v)
	}
}
