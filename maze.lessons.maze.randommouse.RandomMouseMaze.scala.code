override def run() {
    while (!isOverBaggle()){
      random3() match{
        case 0 => left()
        case 1 => right()
        case 2 => 
        	if (!isFacingWall()){
            forward()
          }
      }
    }
  
  	pickupBaggle()
}
