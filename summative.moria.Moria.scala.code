var color:Color=getCouleurSol()

while (color!=Color.GREEN) {

	while (!estSurBiscuit() && !estDosMur()) {
    recule();
    color=getCouleurSol();
  }
  if (estSurBiscuit) {
  prendBiscuit();
  avance();
  }
  
  while (!estSurBiscuit() && !estDosMur()) {
  	avance();
    color=getCouleurSol();
  }
  
  if (estSurBiscuit) {
  recule();
  poseBiscuit();
  recule();
  }
  
	if (!estSurBiscuit && estDosMur) {
    setPos(getX()+1,getY()+1); gauche();gauche()
    return true
  }
  
}