override def run() {
  do (baisseBrosse();
      avance();)
  while { (estFaceMur()) && (if (getDirection() == Direction.EST)) || (if getDirection() == Direction.OUEST)) {
    estFaceMur();

  }
        }