override def run() {
	if (keepHandOnSideWall()) {
		if (estFaceMur()) gauche();
		else avance();
	}
	else {
		droite();
		avance();
	}
}
def keepHandOnSideWall():Boolean {
 	droite();
 	var flag = estFaceMur();
 	gauche();
 	return flag;
 }
