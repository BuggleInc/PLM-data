def isFacingTrail():Boolean = {
  while (isOverBaggle!=true){
  	forward();
  	if (getGroundColor()==Color.green && isFacingWall!=true) {
    	backward();brushDown();forward();brushUp();
  	} else {
      backward();
      left();
    }
  }
  pickupBaggle();
  return true
}