setDirection(Direction.EAST)
while getX()!=18:
    while estSurBiscuit()==False:
            if getX()<18:
                avance()
            else:
                setDirection(Direction.SOUTH);avance();setDirection(Direction.EAST);avance();setDirection(Direction.NORTH)
            
    prendBiscuit()
    while estSurBiscuit()==False and getX()<18:
            recule()
    avance();poseBiscuit();avance()
