def flower(c:Color){
  setCouleurBrosse(Color.c)
  
  baisseBrosse()
  avance(2);recule();gauche();avance();recule(2)
  leveBrosse()
  avance()
  
  setCouleurBrosse(Color.yellow)
  baisseBrosse()
  leveBrosse()
  droite()
}

def growFlowers(){
  flower(red)
}