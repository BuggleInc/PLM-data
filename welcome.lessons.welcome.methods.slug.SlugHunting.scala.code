def hunt() {while (!porteBiscuit()) {
  while (isFacingTrail()) {avance();baisseBrosse();leveBrosse();if (estSurBiscuit) {return prendBiscuit()} };gauche() };
}

def isFacingTrail():Boolean = {if (!estFaceMur()){avance(); if (getCouleurSol() == Color.green) {recule();return true} else {recule();return false }} else {return false}
}
