override def run() {
  var i = 0;
  
  draw_rectangle();
  droite(90);
  draw_rectangle();
  droite(90);
  draw_rectangle();
  droite(90);
  draw_rectangle();
  droite(90);
  
  def draw_rectangle()= {
  for(i <- 1 to 4){
    avance(100);
    droite(90);
  }
  }
}