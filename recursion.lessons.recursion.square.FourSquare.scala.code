override def run() {
	carre()
	gauche(90)
	carre()
	gauche(90)
	carre()
	gauche(90)
	carre()
	gauche(90)
}


def carre(){
	avance(100)
	gauche(90)
	avance(100)
	gauche(90)
	avance(100)
	gauche(90)
	avance(100)
	gauche(90)
	
}
