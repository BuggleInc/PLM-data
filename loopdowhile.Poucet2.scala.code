var nbBaggle = 0
while (!exitReached()) {
  while (!crossing()) {
    forward()
    if (isOverBaggle()) {
      nbBaggle = nbBaggle + 1
    }
  }
  if (nbBaggle >= 3) {
    left()
  } else {
    right()
  }
  nbBaggle = 0
}
forward()
left()