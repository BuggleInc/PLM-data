def close10(a:Int, b:Int): Int = {
	var d = 0;
	var p = 0;
d = 10 - a
p = 10 - b
	if (Math.abs(d) < Math.abs(p))
	{
		return a 
	}
	else if (Math.abs(d) > Math.abs(p))
	{
		return b 
	}
	else 
		return 0
}
