def bubbleSort()  {
  var sorting:Boolean = true;
  
  while (sorting == true) {
    sorting = false;
    
  	for (i <- 0 to getValueCount()-2 ) {
      
      if (! (isSmaller(i, i+1)))  {
        swap(i, i+1);
        sorting = true;
      }
    }
  }
}
