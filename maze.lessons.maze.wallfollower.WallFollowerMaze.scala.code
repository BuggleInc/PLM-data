override def run() {
    def keepHandOnRightWall() = {
    	if (!(estFaceMur)) {
		droite();
		if (estFaceMur) {gauche()} else {avance()}
    	} else {droite()}
    }
    while (!(estSurBiscuit)) {
    	keepHandOnRightWall();
    	if (!estFaceMur) {avance()};
    }
    prendBiscuit();
}
