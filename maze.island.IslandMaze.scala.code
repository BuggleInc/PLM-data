override def run() {
  while (!isOverBaggle) {
    while (!isFacingWall())  {forward()}
    
		while ((!isFacingWall()) && (getDirection()!=Direction.NORTH))
    	{forward(); left()};
    right()
	}                 
}















