override def run() {
	quart(Color.black)
	quart(Color.red)
	quart(Color.orange)
	quart(Color.magenta)
	
}
def quart(c:Color) {
	setCouleur(c)
	for (i<- 1 to 90) {
		avance(100)
		recule(100)
		droite(1)
	}
}
