def gnomeSort() {

	var i:Int = 0;
	var taille = getNombreValeurs();

	while (i != taille - 1)
	{
		if ((!plusPetit(i, i - 1)) || i == 1 || i == 0)
			i += 1;
		else
		{
			echange(i, i - 1);
			i -= 1;
		}	
	}
}
