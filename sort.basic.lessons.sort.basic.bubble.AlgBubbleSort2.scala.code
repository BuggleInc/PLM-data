def bubbleSort2()  {
	  var x=getNombreValeurs();
	  var estTrie=false;
	  while (!estTrie) {
	  	estTrie=true;
	  	for (i<-0 to x-2) {
	  		for (j<-0 to i) {
	  			if (!plusPetit(j, j+1)) {
	  			echange(j, j+1);
	  			estTrie=false;
	  		}
	  	}
	  	}
	}
}
