def insertionSort() 
{
  var v = 0
  for(i <- 1 to getNombreValeurs() - 2)
  {
    copie(getValeur(i),v)
    var j = i
    while(j > 0 && getValeur(j-1) > v)
    {
      setValeur(j,j-1)
      j -= 1
    }
    setValeur(j,v)
  }
}