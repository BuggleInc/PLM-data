def cocktailSort() {
var n=getNombreValeurs()
var compteur=1

	
 while(compteur!=0){
 	compteur=0
	for (k<- 0 to n-2){
		if (plusPetit(k,k+1)){
		}
		else {
		    	echange(k,k+1)
		    	compteur=1
		}
	}
	for (k<- n-2 to 1 by -1){
		if (plusPetit(k,k-1)){
			echange(k-1,k)
			compteur=1
		}
	}
}
}