def indexOfMaxValue(nums:Array[Int]):Int = {
  var comparateur:Int=nums(0)
  for (i<-1 to nums.length){
    if (comparateur<nums(i)){
      comparateur=nums(i);i
    }else{return 0}
  }
  return nums(i)
}
