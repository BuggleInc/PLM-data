def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if (steps <= 0){
    return
  }
  current(steps)
  droite(angle)
  avance(length)
  tree(steps-1, shrink * length, angle, shrink)
  tree(steps-1, shrink * length, -2*angle, shrink)
  recule(length)
}