def step(rule:Array[Char], colors:Array[Color]) {
	var x = 0
	while (getCouleurSol() != colors(x)){
		x=1+x
	}
	var z = colors.length
	setCouleurBrosse(colors((x+1)%z))
	baisseBrosse()
	leveBrosse()
	if (rule(x) == "R"
	) {
		droite()
		avance()}else{
		gauche()
		avance()
	}
}
