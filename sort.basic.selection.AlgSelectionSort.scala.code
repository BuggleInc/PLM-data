def selectionSort() {
  var n = 0;
  for (i<-0 to getNombreValeurs()-3){
    var pos = i;
    for (j<- i to getNombreValeurs()-1){
      if(plusPetit(j,pos)){
        pos = j;
      }
    }
    echange(i,pos);
  }
}