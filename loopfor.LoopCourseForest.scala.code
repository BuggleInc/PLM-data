for (stepper1<-1 to 7) {
  for (stepper2<-1 to 4) {forward()};
  left(); forward(); forward(); 
  right()
  for (stepper3<-1 to 4) {forward()};
  right(); forward(); forward();
  left(); for (stepper4<-1 to 4) {forward()};
} 