def selectionSort() {
  for (i <- 0 to getNombreValeurs()-2){
    var v = i
    for (c <- i+1 to getNombreValeurs()-1){
      if (getValeur(c) < getValeur(v)){
        v = c
      }
    }
    echange(v,i)
  }
}
