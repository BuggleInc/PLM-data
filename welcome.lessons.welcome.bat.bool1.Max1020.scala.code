def max1020(a:Int, b:Int):Int = {
  if (Math.max(a,b)>9 && Math.max(a,b)<21){
    return Math.max(a,b)
  }
  if (Math.min(a,b)>9 && Math.min(a,b)<21){
    return Math.min(a,b)
  }
  else{
    return 0
  }
}