def bubbleSort3() {

	var inverse = true

	for ( i <- getNombreValeurs -2 to 0 by -1) {
		inverse = false
		for (j <- 0 to i) {
			if (!plusPetit(j,j+1)) {
				echange(j,j+1)
				inverse = true
			}
			if (inverse) {
				return
			}
		}
	}

}
