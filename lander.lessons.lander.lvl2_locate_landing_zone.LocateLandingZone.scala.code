def getLandingZone():Segment = {
	var debut:Point = Point(0.0,0.0)
	var fin:Point = Point(0.0,0.0)
	var Liste = getSol()
	var Y:Point = y(Liste(i))
	for (i <- 0 to Liste.length-1) {
		y
		if (y(Liste(i))==y(Liste(i+1))) {
			debut=Liste(i)
			fin=Liste(i+1)
		}
	}
	return Segment(debut,fin)
}
