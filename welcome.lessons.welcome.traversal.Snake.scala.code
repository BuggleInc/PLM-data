override def run() {
  serpent()
}

def posfinale():Boolean={
}

def serpent(){
  if(!estfaceMur()){
    avance()
  } else{
    gauche()
    if(getDirection==Direction.OUEST){
      avance()
      gauche()
    }
    droite(2)
    if(getDirection==Direction.EST){
      avance()
      droite()
    }
  }
}