def getLandingZone():
    lastPoint = getGround()[0]
    for point in getGround():
        if (point != lastPoint):
            if lastPoint.y == point.y:
                return (lastPoint.x,point.x)
        lastPoint = point

