def maxMod5(a:Int, b:Int):Int = {
  (a,b) match {
    case (a,b) if a == b => 0
    case (a,b) if a%5 == b %5 && a<b => a
    case (a,b) if a%5 == b %5 && b<a => b
    case (a,b) if a<b => b
    case (a,b) if b<a => a
  }
}