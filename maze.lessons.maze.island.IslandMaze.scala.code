def keepHandOnSideWall = {gauche(); if (!estFaceMur) {avance()} else {while(estFaceMur()) {droite};avance()} }

override def run() { 
  setDirection(Direction.NORTH)
  
  
  
  
  while (!estSurBiscuit()) {while (!estFaceMur) {
                       avance()} ; keepHandOnSideWall;setDirection(Direction.NORTH)}; prendBiscuit()
}