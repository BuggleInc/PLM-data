override def run() 
{
	def brosse(c:Color)
	{
		setCouleurBrosse(c); poseBrosse(); leveBrosse()
	}

	def colonne()
	{
		val decalage = litMessage().toInt

		for(i <- 1 to 6)
		{
			if(i
		}
	}

	for(i <- 1 to 5)
	{
		gauche(); avance(); droite()
		colonne()
	}
}
