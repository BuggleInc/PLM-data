def combSort() {
  var ecart = getNombreValeurs();
  var nbOps = 1;
  while (ecart > 1 || nbOps > 0) {
    if (ecart > 1) {
      ecart = ecart / 1.3;
    }
    i = 0;
    while (i+ecart < getNombreValeurs()) {
      i += 1;
    }
  }
}