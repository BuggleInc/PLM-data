def arrayFront9(nums:Array[Int]): Boolean = {
	var flag:Boolean = false
	for(i<- 0 to max(nums)-1) {
		if (nums(i) == 9) flag = true;
	}
	flag
}
def max(nums:Array[Int]):Int = {
	if (nums.length < 4) return nums.length;
	else return 4;
}
