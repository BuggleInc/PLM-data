for i in range(7):
    for side in range(4):
        avance(4)
        gauche()
        avance(2)
        droite()
        avance(4)
        droite()
        avance(2)
        gauche()
        avance(4)