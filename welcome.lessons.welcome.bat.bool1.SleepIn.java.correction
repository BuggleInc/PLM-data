$package import plm.universe.bat.BatEntity; import plm.universe.bat.BatWorld; import plm.universe.bat.BatTest; import plm.universe.World; public class sleepIn extends BatEntity {  @SuppressWarnings("unchecked") public void run(BatTest t) {
		t.setResult( sleepIn((Boolean)t.getParameter(0),(Boolean)t.getParameter(1)) );		 }
	boolean sleepIn(boolean weekday, boolean vacation) {
		return !weekday || vacation;
	}
 }