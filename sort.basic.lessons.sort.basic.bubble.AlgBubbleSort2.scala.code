def bubbleSort2() {
	var trie: Int = 0;
	while (trie == 0 ){
		trie = 1;
		
		for(i <- (getNombreValeurs()-2) to 0){
			for ( j<-0 to i){
			if(getValeur(j+1) < getValeur(j)) {
				echange(j+1,j);
				trie = 0;
				}
			}
		}
	}
