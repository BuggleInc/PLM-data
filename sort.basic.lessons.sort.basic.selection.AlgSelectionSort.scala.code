def selectionSort() {
  for(i<-0 to getNombreValeurs()-2){
    var minimum=i
    for (j<-i+1 to getNombreValeurs()-1){
      if (getValeur(j)<getValeur(minimum)){
        minimum=j
      }
    }
    echange(i,minimum)
  }
}