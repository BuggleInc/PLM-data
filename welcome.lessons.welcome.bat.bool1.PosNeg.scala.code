def posNeg(a:Int, b:Int, negative:Boolean):Boolean = {
  if (a < 0 && b < 0 && negative) {
    return true
  } !negative match {
    case ((a < 0) && (b > 0)) => return true
    case ((a > 0) && (b < 0)) => return true
    case _ => return false
  }
}
