for (compteur1 <- 1 to 7; compteur2 <- 1 to 4){
  for (compteur3 <- 1 to 4) avance(4);gauche()
  for (compteur4 <- 1 to 2) avance(2);droite()
  for (compteur5 <- 1 to 4) avance(4);droite()
  for (compteur6 <- 1 to 2) avance(2);gauche()
  for (compteur3 <- 1 to 4) avance(4);gauche()
}