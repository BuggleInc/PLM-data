override def run() {
def keepHandOnSideWall() {
	while (estFaceMur()) {
	droite();
	avance();
	gauche();
		if (!(estFaceMur())) {
			avance();
			gauche();
			if (!(estFaceMur())) {
				avance();
			} else {
				while (estFaceMur()) {
	droite();
	avance();
	gauche();
			}
		}
		
	}
 
}
}

while (!(estSurBiscuit())) {
keepHandOnSideWall();
}
}
