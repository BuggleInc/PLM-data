def keepHandOnSideWall = {gauche(); if (!estFaceMur) {avance()} else {while(estFaceMur()) {droite};avance()} }

override def run() { 
 
  
  
  while(getY!=0) {while (!estFaceMur) {
    avance()} ; keepHandOnSideWall;setDirection(Direction.NORTH)}
  
  while (!estSurBiscuit()) {keepHandOnSideWall}; prendBiscuit()
}