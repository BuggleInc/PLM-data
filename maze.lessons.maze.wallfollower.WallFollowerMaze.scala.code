override def run() {
  while (!estSurBiscuit()) {
    if (checkg()) {gauche(); avance()}
    else if (!estFaceMur()) {avance()}
    else {droite}
  }
prendBiscuit()
}
    


def checkg():Boolean = {
  gauche()
  var r = !estFaceMur()
  droite()
  return r
}