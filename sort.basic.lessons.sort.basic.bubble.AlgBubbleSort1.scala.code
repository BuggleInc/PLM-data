def bubbleSort()  {
  var n = getNombreValeurs();
  var parcours = true;
  while (parcours) {
    parcours=false
  	for (i<-0 to n-1) {
    	if (getValeur(i)>getValeur(i+1)) {
      	echange(i,i+1)
        parcours=true
      }
      
    }
  }
}
	