def bubbleSort()  {
  var nb=getNombreValeurs()
  var k=0
  while (k!=nb-1){
    for (i<-0 to nb-2){
      if (plusPetit(getValeur(i),getValeur(k))){
        echange(i,k)
      } 
      k=i+1
    }
  }
}