def bubbleSort()  {
	
	for (k<-0 to getNombreValeurs()-2){
		if(plusPetit(k+1,k)){
			echange(k+1,k)
		}
	}
	for (k<-0 to getNombreValeurs()-2){
		if(plusPetit(k+1,k)){
			echange(k+1,k)
		}
	}
	
	
}

