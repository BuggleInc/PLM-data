def combSort() {
  var ecart = getNombreValeurs()
  var i =0
  var b = true
  do {
    b=false
    if (exart>1) {
      ecart = ecart/1.3
    }
    i=0
    while (i+ecart < getNombreValeurs()) {
      if (getValeur(i)>getValeur(i+ecart)) {
        echange(i,i+ecart)
        i+=1
        b=true
      }
    }
  } while (ecart>=1 || b)
}