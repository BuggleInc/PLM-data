def insertionSort() {
  val n = getNombreValeurs()
  var v = 0
  var k = 0
  var m = 0
  for (i<- 1 to n-1) {
    k = i
    v = getValeur(i)
    for (j <- i-1 to 0 by -1) {m = getValeur(j);if (m > v) {setValeur(j+1,m);k = j}
   setValeur(k,v)
                              }}}