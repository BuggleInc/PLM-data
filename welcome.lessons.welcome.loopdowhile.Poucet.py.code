while not sortieTrouvee()==True:
    compteur=0
    for i in range(5):
        avance()
        if estSurBiscuit():
            compteur=compteur+1
    if compteur==3 or compteur==4 or compteur==5:
        gauche()
        compteur=0
    else: 
        droite()
        compteur=0