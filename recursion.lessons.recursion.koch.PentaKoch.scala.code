def pentaKoch(levels:Int, length:Double) {
		squareSide(levels-1, length/3)
		gauche(90)
		squareSide(levels-1, length/3)
		droite(90)
		squareSide(levels-1, length/3)
		droite(90)
		squareSide(levels-1, length/3)
		gauche(90)
		squareSide(levels-1, length/3)
	}
}