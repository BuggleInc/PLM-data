def isFacingTrail():Boolean = {
  if (estFaceMur() == true) return false
  else {avance();
        def ok : Boolean = false;
        if (getCouleurSol() == Color.green) {ok = true;};
        return ok;
       }
