def insertionSort() {
  val taille=getNombreValeurs()
  var temp=0
  var minj=0
  for (i<-1 to taille-1) {
    temp=getValeur(i)
    
    for (j<-i-1 to 0 by -1){
      if (plusPetit(i,j)) {
        copie(j,j+1)
      	minj=j
      }
    }
    setValeur(minj,temp)
  }
}