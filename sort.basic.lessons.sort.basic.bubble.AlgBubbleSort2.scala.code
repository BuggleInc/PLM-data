def bubbleSort2() {
	var trie: Int = 0;
	while (trie == 0){
		trie = 1;
		var k:Int = 1
		for(i <- 0 to (getNombreValeurs()-2-k)){
			if(getValeur(i+1) < getValeur(i)) {
				echange(i+1,i);
				k = k+1
				trie = 0;
				}
			}
		}
	}
