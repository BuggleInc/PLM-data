def greenTicket(a:Int, b:Int, c:Int):Int = {
  if ((a != b) && (a != c) && (b != c))
  	return 0
  else if ((a == b) && (a == c) && (c == b))
    return 20
  else if ( ((a == b) && (a == c)) || ((b == c) && (c == a)))
    return 10
  
  
  return 0

}