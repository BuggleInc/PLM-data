def gnomeSort() {
  var n = getNombreValeurs();
  var i = 0;
  while ( i < n-1 ){
    if ( i < 0 ){
      i = 1;
    }
    if ( plusPetit(i+1:Int,i:Int) ){
      echange(i+1,i);
      i -= 1;
    } else {
      i += 1;
    }
  }
}