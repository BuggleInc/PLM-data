while estSurMessage()==True:
     M=litMessage()
     if M=="W":
        recule(4)
     if M=="V":
          recule(5)
    if M=="U":
        recule(6)
    if M=="B":
        avance(2)
    if M=="C":
        avance(3)
    if M=="E":
       avance(5)
    if M=="I":
        droite()
        droite()
        avance()
    if M=="L":
       gauche()
       avance()
    if M=="R":
       droite()
       avance()
