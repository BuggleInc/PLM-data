def insertionSort() {
  for (i<- 1 to getNombreValeurs()-1){
    var k:Int=i
    var v:Int=getValeur(i)
    while (k>0 && !plusPetit(k-1,k)){
      copie(k-1,k)
      k=k-1
    }
    setValue(k,v);
  }
}
