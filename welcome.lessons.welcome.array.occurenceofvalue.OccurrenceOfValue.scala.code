def occurrences(nums:Array[Int],lookingFor:Int):Int = {
	var y:Int = -1
	var compteur:Int = 0
	for ( x <- 0 to nums.length-1) { 
		if ( nums(x) == lookingFor ) { 
		compteur = compteur + 1}
		return compteur
	}
}


