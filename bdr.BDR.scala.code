var fini = false

while (fini() == false) {
  
  if (estSurMessage():Boolean) {
    
    if (litMessage():String == A){
      avance()
    } else if (litMessage():String == Z) {
      recule()
    }
    
   
  }
}