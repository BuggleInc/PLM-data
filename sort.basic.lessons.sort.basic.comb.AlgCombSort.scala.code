def combSort() {
var ecart : Int=getNombreValeurs()
var swap:Boolean =true
do{
	
	if(ecart>1){
		ecart = (ecart.asInstanceOf[Double] / 1.3).asInstanceOf[Int]
	}
	var i:Int=0

	while(plusPetit(i+ecart,getNombreValeurs())){
			swap=false
		if(plusPetit(i+ecart,i)){
			echange(i, i+ecart)
			swap=true
		}
		i+=1
	}
}while( ecart>1 || swap==true)
}
