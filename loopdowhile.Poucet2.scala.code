do {
  var nbBaggle=0
  while (crossing==false){
       forward()
       if (isOverBaggle()==true){
         nbBaggle=nbBaggle+1
       }
   }
  if (nbBaggle>=3){
       left()
  }
  else {
    right()
  }
  forward()
} while(exitReached==false)