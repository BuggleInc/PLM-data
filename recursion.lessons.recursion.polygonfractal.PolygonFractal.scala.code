def polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double) {
	if (levels == 0)
	{
		avance(length)
		droite(72)
	}
	else if (sides != )
	{
		polygonFractal(levels - 1, 0, length, shrink)
		polygonFractal(levels - 1, sides - 1, length, shrink)
	}
	else 
	{
		
	}

		
}
