override def run() {
avance(6)
for (i <- 1 to 9)
	{	setCouleur(Color.black)
		for (j <- 1 to 20)
			{avance(100)
			recule(100)
			droite(1)}
		setCouleur(Color.red)
		for (j <- 1 to 20)
			{avance(100)
			recule(100)
			droite(1)}
		}
}
