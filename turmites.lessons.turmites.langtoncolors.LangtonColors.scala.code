def step(rule:Array[Char], colors:Array[Color]) {
}

def test(rule:Char, color:Color)
{
	if (rule == "R")
		{
			setCouleurSol (Color)
			droite; avance;
		}

	if (rule == "L")
		{
			setCouleurSol (Color)
			gauche; avance;
		}
}

def setCouleurSol(couleur:Color)
{
	setCouleurBrosse(couleur)
	baisseBrosse()
	leveBrosse()
	
}
