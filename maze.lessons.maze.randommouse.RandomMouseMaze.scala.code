override def run() {
	while (!estSurBiscuit())
  {
    var decision = random3()
    if (decision==0)
   	{
      if (!estFaceMur())
      	avance()
    }
    if (decision==1)
    	droite()
    if (decision==2)
    	gauche()
  }
  prendBiscuit()
}