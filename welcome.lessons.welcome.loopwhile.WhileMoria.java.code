public void run() {
	
	while ( getGroundColor() != Color.green ) {
		right(); right();

		while ( !isOverBaggle() )
			forward();

		if ( isOverBaggle() )	{
			pickupBaggle();
			right(); right();

			/*while ( !isOverBaggle() )
				forward();
		}

		if ( isOverBaggle() ) {
			right(); right();
			forward();
			dropBaggle();
		}*/
		
		
	}
}
