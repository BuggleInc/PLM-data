override def run() {
  var compt = 0
  val n = getMondeLargeur()
  for (i <- 0 to n-1) {
    for (j <- 0 to n-1) {
      if (i%2==0) setPos(j,i)
      else setPos(n-1-j,i)
      ecritMessage(compt)
      compt += 1
    }
  }
}