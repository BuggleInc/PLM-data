def selectionSort() {
  for (i <- 0 to getNombreValeurs()-1){
    var v = getValeur(i)
    var c = i
    var r = 1
    while(c < getNombreValeurs()){
      if (getValeur(c) < v){
        v = getValeur(c)
        r = c
        c = c+1
      }
    }
    echange(r,i)
  }
}