def indexOfValue(nums:Array[Int] ,lookingFor:Int): Int = {
	var p = -1
	for (pas <- 0 to (nums.length-1))
  {
   if (p == -1)
    {
      if (nums(pas)==lookingFor)
      {
        p=pas
      }
    }
  }
  return p
}
