while (sortieTrouvee==false) {

var nbBaggles:Int=0; //ATTENTION : il faut initialiser (et donc r?initilaiser pour les passages de boucle suivants) la compteur "nbBaggles" avant la boucle o? l'on s'en sert. La variable doit ?tre d?clar?e avant la boucle o? on l'utilise, sinon erreur !
  
do { 
  
avance();

if (estSurBiscuit==true) {
      nbBaggles=nbBaggles+1;
    }
    
    
  } while(crossing==false)

if (nbBaggles >= 3) {
  gauche(); }
else {
  droite();
}
  
} 

  
avance();