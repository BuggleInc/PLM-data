def gnomeSort() {
  var pos = 0
  val longueur = getNombreValeurs()
  while (pos != longueur-1) {
    if (getValeur(pos) > getValeur(pos + 1)) {
      echange(pos,pos+1)
      if (pos == 0) {
        pos += 1
      } else {
        pos -= 1
      }
    } else {
      pos += 1
    }
  }
}