def indexOfMaxValue(nums:Array[Int]):Int = {
  var plusGrand:Int =nums(0)
  var index:Int = 0
  for (i<- 0 to nums.length-1){
    if (nums(i)>plusGrand){plusGrand=nums(i);index=i}
  }
  return index
}
