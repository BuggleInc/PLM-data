def isFacingTrail():Boolean = {
  
  var caseVerte = false
  
  avance()
  if (estFaceMur == false) {
		if (getCouleurSol == Color.green) {
  		recule();
  		avance();
    	caseVerte = true;
		} 
  }
  return caseVerte
}