def isFacingTrail():
    if isFacingWall():
        return False
    else:
        forward()
    if getGroundColor()==Color.green:
        brushDown()
        brushUp()
        backward-)
        return True
