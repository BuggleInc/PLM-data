override def run() {
  var nbBases = getBasesAmount()
  var holeBaseIndex = getHoleBase()
  var holeIndex = getHolePosition()
  
  var playersColor:List[Int] = List()
  
  for(i <- 0 to nbBases) {
    playersColor += getPlayerColor(i, 0)
    playersColor += getPlayerColor(i, 1)
  }
}