override def run() {
  
  while (!isOverBaggle()){
    keepHandOnSideWall()
    if (isFacingWall()){
      right()
    }
    if (isFacingWall()){
      right()
    }
		forward()
  }
  
  
  pickupBaggle()
}



def keepHandOnSideWall(){
	left()
  if (isFacingWall()){
    right()
  }
}