def haveBaggle():Boolean = {
  /* Renvoie true si la colonne contient un baggle */
  var sawBaggle = false
  
  for (i <- 1 to 6){
    if (isOverBaggle())
    	sawBaggle = true
    forward()
  }
  if (isOverBaggle())
    	sawBaggle = true
  for (j <- 1 to 6){
    backward()
  }
  return sawBaggle
}
