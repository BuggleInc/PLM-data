def insertionSort() {
  var v : Int =0
  var k : Int =0
  for(i<-1 to getNombreValeurs()-1){
    v = getValeur(i)
    k=i
    while(getValeur(k-1)>v && k>1){setValeur(k,getValeur(k-1));k+= -1;}
    setValeur(k,v);
  }
}