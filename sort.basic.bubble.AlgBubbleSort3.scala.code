def bubbleSort3(){
  var lgr = getNombreValeurs;
  for(k <- 0 to lgr - 2){
    var i = lgr - 2 - k;
    var fini = true;
    for (j <- 0 to i){
      if(plusPetit(i,j)){
        echange(i,j);
        fini = false;
      }
      if(fini) return;
    }
  }
}