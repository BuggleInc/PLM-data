public void run() { 

	int BaggleCount = 0;

	while (BaggleCount<4) {

		//While not over a baggle, keep moving forward
		while (!isOverBaggle()) {
			forward();
		}
	
		//Now over a baggle, so add to count
		BaggleCount = BaggleCount + 1;
	}	
}
