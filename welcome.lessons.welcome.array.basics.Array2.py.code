def getMondeLargeur(L):
    n=len(L)
    for i in range(n):
        setCouleurBrosse(L[i])
        baisseBrosse()
        leveBrosse()
        forward()

L=[]
H=getWorldHeight()
for i in range(H):
    L.append(getCouleurSol())
    if estFaceMur()==False:
        forward()

backward(H)