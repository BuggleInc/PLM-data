def answerCell(isMorning:Boolean, isMom:Boolean, isAsleep:Boolean):Boolean = {
  if (isMorning && isMom)
  {return true}
  if (isMorning && !isMom)
  {return false}
  if (!isMorning)
  {return true}
  return false