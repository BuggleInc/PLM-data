def bubbleSort3() {
  var n: Int=getNombreValeurs()-2
	for (i<- n to 0 by -1){
    var swapy=false
    for (j<- 0 to i){
      if (plusPetit(j+1,j)){
        echange(j+1,j)
        swapy=true}}
     if (!swapy){return()}
      
    }
  }
