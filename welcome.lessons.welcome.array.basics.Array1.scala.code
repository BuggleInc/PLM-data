override def run() {
	var h = getMondeHauteur()
  var mot = motif(h) 
  for(i<-1 to h-1){
    gauche()
    avance()
    for(j<-1 to h){
      setCouleurBrosse(mot(j))
      baisseBrosse()
      avance()
      leveBrosse()
    }
  }
}

def motif(hauteur : Int):Array[Color] = {
  var mot = new Array[Color](hauteur)
  for(i<-0 to hauteur-1){
    mot(i) = getCouleurSol()
    avance()
  }
  return mot
}