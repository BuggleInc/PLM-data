def solve() {
  var beforeRed:Int = 0
  var afterBlue:Int = getSize() - 1
  var i:Int = 0
  while(i < afterBlue){
    if(getColor(i) == RED){
      swap(i, beforeRed)
      beforeRed += 1
    }
    if(getColor(i) == BLUE){
      swap(i, afterBlue)
      afterBlue -= 1
    }
    i += 1
  }
}