override def run() {
	def octogone(taille:Int) ={
		for (i<-1 to 8) {
			avance(taille)
			droite(45)
		}
	}
	
	octogone(50)
	avance(25)
	gauche(45)
	
	def cote() ={
		droite(90)
		avance(25)
		gauche(45)
		avance(25)
	}
	var i = 0
	for (i<-1 to 8){
		cote
	}

	droite(90)

	def losange() {
		avance(25)
		droite(45)
		avance(45)
		droite(180)
	}

	for (i<- 1 to 16) {
		losange
	}
}
