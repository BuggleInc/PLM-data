override def run() {
  	var rand = random3()
    while(!estSurBiscuit()) {
      if (rand == 0 && !estFaceMur()) {
        avance()
      } else if (rand == 1) {
        droite()
      } else {
        gauche()
      }
    }
  prendBiscuit()
}