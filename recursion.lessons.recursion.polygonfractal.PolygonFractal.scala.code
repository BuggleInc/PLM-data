def polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double) {
if(levels==1){
	avance(length)
	droite(72)
	avance(length)
	droite(72)
	avance(length)
	droite(72)
	avance(length)
	droite(72)
	avance(length)
	
}else{
	
	
}
}
