def array():
    n = 0
    brushDown()
    for b in L:
        setBrushColor(b)
        forward()
        if n == len(L)-1:
            brushUp()
        n= n+1
    
def storeArray():
    global L
    L = []
    for a in range(getWorldHeight()):
        L.append(getGroundColor())
        forward()
    L.append(getGroundColor())

storeArray()
for c in range(getWorldHeight()-1):
    forward()
    left()
    forward()
    right()
    array()

    