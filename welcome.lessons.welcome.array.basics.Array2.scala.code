override def run() {
		var depart:Color = getCouleurSol()
	var tab = new Array[Color](getWorldHeight())
	for(i <- 0 to getWorldHeight()-2){
		avance()
		tab(i) = getCouleurSol();
				}
		for(r <- 1 to getWorldHeight()-1){
			recule()
		}
		for(c <- 1 to getWorldHeight() - 1){
			gauche()
			avance()
			droite()
			var decal = litMessage().toInt
			setCouleurBrosse(tab(c%decal))
			baisseBrosse()
	leveBrosse()
		for(i <- 2 to getWorldHeight()-1){
		avance()
		setCouleurBrosse(tab(i%decal))
	baisseBrosse()
	leveBrosse()
	}
	for(p<-1 to getWorldHeight()){
		recule()
	}
		}
}
