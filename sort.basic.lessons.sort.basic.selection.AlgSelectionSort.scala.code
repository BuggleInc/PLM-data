def selectionSort() {
  var lgr:Int = getNombreValeurs()
  var min:Int = 0
  for (i <- 0 to lgr-2) {
    min = i
    for (j <- i to lgr-1) {
      if (plusPetit(j,min) == true) {min = j}
    }
    echange(i,min)
  }
}