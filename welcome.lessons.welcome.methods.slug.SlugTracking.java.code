boolean isFacingTrail() {
boolean greenCell = true;
	do{
    		if(getGroundColor().equals(Color.green)){
    			greenCell = true;
    			forward();
    		}

    		if(isFacingWall()){
    			greenCell = false;
    			backward();
    		}

    		if(isOverBaggle()){
    			greenCell = true;
    		}
    		
	}while(!greenCell);

    return greenCell;
}//end function 
