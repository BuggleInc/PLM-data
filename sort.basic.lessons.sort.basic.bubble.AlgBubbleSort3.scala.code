def bubbleSort3()  {
  var n = getNombreValeurs()
  var tri = true
  for (j<-0 to n-1) {
    tri=false
    for (i<-0 to n-2) {
      if (!(plusPetit(i,i+1))) {
        echange(i,i+1)
        tri = true
      }
  	}
    n = n-1
    if (!tri) {
      return
    }
	}
}
