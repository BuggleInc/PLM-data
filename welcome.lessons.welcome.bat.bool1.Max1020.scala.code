def max1020(a:Int, b:Int):Int = {
  var interA : Boolean = (a<=20)&&(a>=10)
  var interB : Boolean = (b<=20)&&(b>=10)
  
  if(interA&&interB){
    return Math.max(a,b)
  }
  else if(interA&&(!interB)){
    return a
  }
  else if(interB&&(!interA)){
    return b
  }
  else{
    return 0
  }
}