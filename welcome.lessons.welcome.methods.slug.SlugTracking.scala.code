def isFacingTrail():Boolean = {
  var vert: Boolean = false;
	while (!estSurBiscuit()) {
  	if (estFaceMur()) {
    	droite()
  	}
    else {avance()}
  	if (getCouleurSol() == Color.green) {
      vert = true
     	avance()}
  	else {
    	recule()
    	droite()
  	}
    	}

  return vert
  }
