def selectionSort() {
	val l =getNombreValeurs()-1
	for (i<-0 to l ) {
		var indice = i
		for (h<- i to l ) {
			if (plusPetit(h,indice)) {
				indice = h
			}
		}
		if (indice != i ) {
		echange(i,indice)
	}

}
