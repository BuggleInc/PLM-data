override def run() {
  var diag = 0
  var count = 0
  while (diag!=12){
    ecritMessage(count)
        count=count+1
    if(getX()==getMondeLargeur || getY()==getMondeHauteur()) {
      setPos(getX()+1,getY()-1)
    }
    else {
      diag=diag+1
      setPos(0,diag)
    }
  }
}

