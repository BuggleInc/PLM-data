fini = False

while fini = False :
  msg = litMessage()
  if (msg = 'R'):
    droite()
    avance()
  elif (msg = 'L'):
    gauche()
    avance() 
  elif (msg = 'I'):
    *demi tour*
    avance() 
  elif (msg = 'A'):
    avance() 
  elif (msg = 'B'):
    avance(2) 
  elif (msg = 'C'):
    avance(3)  
  elif (msg = 'Z'):
    *reculer* 
  elif (msg = 'Y'):
    *reculer*
  elif (msg = 'X'):
    *reculer*
  else :
    fini = True
