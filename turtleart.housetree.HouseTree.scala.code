override def run() {
  for (compteur <- 1 to 4) {
    house()
    droite(90)
    leveCrayon()
    avance(50)
    gauche(90)
    baisseCrayon()
  }
}

def house() {
  for (compt <- 1 to 4) {
    avance(30)
    droite(90)
  }
  avance(30)
  droite(30)
  for (compt <- 1 to 3) {
    avance(30)
    droite(120)
  }
  gauche(30)
  recule(30)
}