def noTriples(nums:Array[Int]): Boolean = {
  for(i <- 0 to nums.length){
    if(i < nums.length - 2){
      if((nums(i+1) == nums(i)) && (nums(i+2) == nums(i))){
        return true
      }
    }
  }
  return false
}