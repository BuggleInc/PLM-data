def bubbleSort3()  {
  var compteur = 0
  var tri : Boolean = true
  while (tri) {
    tri = false
    for (i <- 0 to getNombreValeurs()-compteur-2) {
      if (!plusPetit(i,i+1)) {
        echange(i,i+1)
        tri = true
      }
    }
    compteur += 1
  }
}
