def bubbleSort2() {
	var trie: Boolean = false
  var nbVal: Int = getNombreValeurs()
  while (!trie){
    trie = true
    for (i <- 0 to nbVal-2){
      if (plusPetit(i+1,i)){
        echange(i,i+1)
        trie = false
      }
    }
    nbVal = nbVal - 1    
    }
  }
