override def run() {var x:Int=0;
                    while (!isFacingWall()) {right()}
                    while (!isOverBaggle()) {while(isFacingWall()) {right(); if (!isFacingWall()) {forward()} else {right(); if (!isFacingWall()) {forward()} else {left(); left(); forward()}} 
                ; left(); forward()}}
pickupBaggle()
    // Your code here 
}