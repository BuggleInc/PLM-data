override def run() {    while(!estSurBiscuit()){

val rand = new scala.util.Random
    rand.nextInt(3)
    if (rand == 0) {droite()}
    if (rand ==1 && !estFaceMur()) {avance()}
    if (rand == 2) {gauche()} 
}
prendBiscuit();
}
