override def run() {
  def carre() {
    for (i <- 1 to 4) {
      avance(100); droite(90); avance(100); droite(90); avance(100); droite(90); avance(100)
    }
  }
  carre(); carre(); carre(); carre()
}
