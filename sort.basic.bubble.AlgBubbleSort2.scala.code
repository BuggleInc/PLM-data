def bubbleSort2()  {
  var max = getNombreValeurs();
  for(i <- 0 to max-1 ){
    for(j<-max-1 to i+1 by -1){
      var a = getValeur(j);
      var b = getValeur(j-1);
      if(a<b){
        echange(j,j-1);
      }
    }
  }
}