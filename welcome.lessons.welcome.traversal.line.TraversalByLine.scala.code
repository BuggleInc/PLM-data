override def run() {
  var compteur = 0
  ecritMessage(""+compteur)
  while (getX() != getMondeLargeur()-1 || getY() != getMondeHauteur()-1){
    if (getX() != getMondeLargeur() -1){
      setPos(getX() + 1, getY());
      compteur += 1;
      ecritMessage(""+compteur);
    }
    else{
      setPos(0, getY() + 1);
      compteur += 1;
      ecritMessage(""+compteur);
    }
  }
}
