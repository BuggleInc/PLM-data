def posNeg(a:Int, b:Int, negative:Boolean):Boolean = {
	if (a - Math.abs(a) == 0 && b - Math.abs(b) == 0) {
		return false
	}else {
		if (a + Math.abs(a) == 0 && b + Math.abs(b) == 0) {
			return negative
		}else {
			return !negative
	}
}
}