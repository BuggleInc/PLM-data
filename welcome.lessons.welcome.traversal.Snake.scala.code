override def run() {
def fonctionTest() : Boolean =  {
	var rep = true
	if (estFaceMur) {
		if (getDirection() == Direction.EST) {
		droite() ; 
		if (estFaceMur) {
			gauche() ; rep = false; 
		} else { avance();droite()}
	}
	if (getDirection() == Direction.OUEST) { 
		gauche();
		if (estFaceMur) {
			droite() ; rep = false; 
		} else { avance() ; gauche()}
	}
	}
	return rep
}
baisseBrosse()
while (fonctionTest()) {
	if (!estFaceMur()) { 
		avance()
	
	}
		
}
leveBrosse()
}
