def insertionSort() {
	var stockage=0
  var k=0
  for (i<- 1 to getNombreValeurs()-1) {
    stockage=getValeur(i)
    k=i-1
    while(plusPetit(i,k) && k>=1) {
      copie(k,i)
      k-=1
    }
    setValeur(k,stockage)
  }
}