def selectionSort() {
  var a=getValeur(0)
  
  for (i<-0 to getNombreValeurs()-1) {
  
  	for (N<-i to getNombreValeurs()-2) {
    	if(plusPetit(N+1,N)) {
      	a=N+1
    	}
  	}
  	echange(a,i)
  }
}