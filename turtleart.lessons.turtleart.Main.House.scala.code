override def run() {
 avance(100);
 toit();
 gauche(30);
 base();
}
def toit() {
	droite(30);
	for (i <- 0 to 2) {
		avance(100);
		droite(120);
 }
}
def base() {
	droite(90);
	avance(100);
	droite(90);
	avance(100);
	droite(90);
	avance(100);
	droite(90);
}
