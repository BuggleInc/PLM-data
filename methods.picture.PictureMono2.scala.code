def faireV() {
  avance()
  avance()
  baisseBrosse
  leveBrosse()
  gauche()
  avance()
  droite()
  avance()
  baisseBrosse()
  leveBrosse()
  avance()
  droite()
  avance()
  gauche()
  baisseBrosse()
  leveBrosse()
  avance()
  avance()
  gauche()
}

def faireSousMotif() {
  for (compteur <- 1 to 4) {
    faireV()
  }
  avance(7)
}

def faireMotif() {
  for (banane <- 1 to 3) {
    faireSousMotif()
  }
  gauche()
  avance(7)
  droite()
}

override def run() {
  for (poire <- 1 to 3) {
    faireMotif()
  }
}