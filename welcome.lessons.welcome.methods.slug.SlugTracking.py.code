'''def isFacingTrail():
    forward()
    if getCouleurSol()==Color.green:
        return(False)'''
        
    
def isFacingTrail():
    return(False)
    else:
          forward()
          res = (getGroundColor() == Color.green)
          backward()
          return res

isFacingTrail()

