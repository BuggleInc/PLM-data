def hunt() {
  
  while(!estSurBiscuit()) {
    
  	if (isFacingTrail()==true) {
    	//baisseBrosse()
      avance()
    	}
  	else {
      while(isFacingTrail==false) {
        gauche()
      }
      }
//prendBiscuit()
}}

def isFacingTrail():Boolean = {
    
  if (estFaceMur) {
    return false
  }
  avance()
  if (getCouleurSol()==Color.green){
      recule()
      return true}
  recule
    return false
    
}