def verif(a:Int) : Boolean = {
  if ( a <= 19 && a >= 13 ){
    return true;
  } else {
    return false;
  }
}

def hasTeen(a:Int, b:Int, c:Int): Boolean = {
  if ( verif(a) || verif(b) || verif(c) ){
    return true;
  } else {
    return false;
  }
}

