override def run() {
	setCouleur(Color.black)
	for (j <- 1 to 90){
			avance(100)
			recule(100)
			droite(1)
	}
	setCouleur(Color.red)
	for (j <- 1 to 90){
			avance(100)
			recule(100)
			droite(1)
	}
	setCouleur(Color.orange)	
	for (j <- 1 to 90){
			avance(100)
			recule(100)
			droite(1)
	}
	setCouleur(Color.magenta)	
	for (j <- 1 to 90){
			avance(100)
			recule(100)
			droite(1)
	}		
}
