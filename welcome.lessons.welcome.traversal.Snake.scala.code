override def run() {
	def finale():Boolean = {
		if(!estFaceMur) {
			return false
		}
		val dir = getDirection()
		setDirection(Direction.NORTH)
		val mur = estFaceMur()
		setDirection(dir)
		return mur
	}
	def verif() {
		
	}
	def snake() {
		while(!estFaceMur) {
			avance()
		}
		if(getDirection() == Direction.EST) {
			gauche()
			avance()
			gauche()
		}
		else {
			droite()
			avance()
			droite()
		}
	}
	while(!finale()) {
		snake()
	}
}

	
		
