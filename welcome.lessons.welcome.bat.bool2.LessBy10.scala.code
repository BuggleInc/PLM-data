def lessBy10(a:Int, b:Int, c:Int):Boolean = {

	var diff1 = Math.abs(a - b);
	var diff2 = Math.abs(a - c);
	var diff3 = Math.abs(b - c);

	var result = false;

	if(diff1 > 10 || diff2 > 10 || diff3 > 10)
		{
			result = true;
		}

			return result;
}
