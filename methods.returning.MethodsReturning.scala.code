def haveBaggle():Boolean = {
  var seenBaggle: Boolean = false
  var i: Int = 1
  while(!seenBaggle && i < 7){
    if(isOverBaggle()){
      seenBaggle = true
    } else {
      forward()
    }
    i = i + 1
  }
  
  for(j <- 1 to i){
		backward()
  }
  
  return seenBaggle
}