for (stepper<-1 to 28) {
  for(stepper2<-1 to 4) {
    forward();
  }
  left();forward();forward();right();
  for(stepper2<-1 to 4) {
    forward();
  }
  right();forward();forward();left();
  for(stepper2<-1 to 4) {
    forward();
  }
  left();
}

