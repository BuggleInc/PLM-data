def gnomeSort() {
  
  var n : Int =getValueCount()
  
  for(it<-1 to n-1){
    var element : Int = it-1
    
    while(element >=0 && isSmaller(it,element)){
      element-=1
    }
    swap(it,element)
  }
}