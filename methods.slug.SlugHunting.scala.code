def hunt() {
    if (isFacingTrail==true) {
      baisseBrosse();
      avance();
      leveBrosse();
    }
  	if (isFacingTrail==false) {
      gauche();
      if (isFacingTrail==false) {
        droite();
       
      }
    }
  
  
}

def isFacingTrail():Boolean = {
    if (estFaceMur==true) {
      return false;
    }
  
  avance();
  if (getCouleurSol==Color.green) {
    recule();
    return true;
  } else { 
    recule();
    return false; 
  }
}
