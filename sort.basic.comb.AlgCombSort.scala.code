var condition = false
def combSort() {
  var ecart = getNombreValeurs()
  do{
    
    if(ecart>1)
      ecart = (ecart.toDouble / 1.3).toInt
    var i=0
    while(i+ecart<getNombreValeurs()){
      if(plusPetit(i+1,i)){
        echange(i,i+1)
        i=i+1
        condition=true
    }
    }
    condition=false
  }while(ecart>1 || condition)
}