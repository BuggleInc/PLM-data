def isFacingTrail():Boolean = {
  var rep = false
  if (!estFaceMur()) {
    avance()
    var couleurSol = getCouleurSol()
    recule()
    if (couleurSol==Color.green) {
      rep = true
    }
  }
  return rep
}