def lessBy10(a:Int, b:Int, c:Int):Boolean =
{
/*Soit trois entiers, a b c, renvoyez vrai si l'un d'entre eux
 est plus petit de 10 ou plus que l'un des autres.*/

 (a,b,c) match
 {
 	case(a,_,_) if(a+10<b || a+10<c) => return true
 	case(_,b,_) if(b+10<a || b+10<c) => return true
 	case(_,_,c) if(c+10<a || c+10<b) => return true
 	
 }
}