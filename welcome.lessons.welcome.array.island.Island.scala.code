def island(num:Array[Int]): (Int,Int) = {
  trouveIle(num,1)
}

def trouveIle(liste:Array[Int], niveau:Int): (Int,Int) = {
  var debut:Int = 0
  var fin:Int = 0
  for (i <- 1 to (liste.length - 2)) {
    if (liste(i-1)==niveau) {
      debut=i
    } else if (liste(i+1)==niveau) {
      fin=i
    }
  }
  return (debut,fin)
}