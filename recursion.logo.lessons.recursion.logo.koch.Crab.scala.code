def crab(levels:Int, length:Double) {
  if(levels==0){
    avance(length)
  }else{
    gauche(levels*45.0)
    crab(length/Math.sqrt(2))
    droite(90.0)
    crab(length/Math.sqrt(2))
    gauche(levels*45.0)
  }
}