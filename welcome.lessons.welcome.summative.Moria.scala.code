def Moria() {
  while(!fin) {
    droite()
    droite()
    while(!estSurBiscuit()) {
      avance()
    }
    prendBiscuit()
    gauche()
    gauche()
    while(!estSurBiscuit()) {
      avance()
    }
    recule()
    poseBiscuit()
  }
}


def fin() :Boolean={
  if (getDirection()==Direction.EAST && estFaceMur) {
    return true
  }
  else {
    return false
  }
}