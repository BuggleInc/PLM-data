override def run():Boolean ={
  def keepHandOnSideWall(){
    droite()
    if(estFaceMur()){ gauche(); return true}
    else{gauche();return false}
  }
  
  while(!estSurBiscuit()){
    var test=keepHandOnSideWall()
    if(test){avance()}
    droite()
    avance()
    
  }
  prendBiscuit()
}