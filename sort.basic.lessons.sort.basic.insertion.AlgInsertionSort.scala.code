def insertionSort() {
  var n:Int = getNombreValeurs()
  
  for (i <- 1 to n - 1) {
    var v:Int = getValeur(i)
    
    var j:Int = i - 1
    
    while (getValeur(j) > v) {
      copie(j - 1, j)
      
      j -= 1
    }
    
    setValeur(j, v)
  }
}