def gnomeSort() {
var pot:Int=1;

	while(pot!=getNombreValeurs()){

		if(pot==0){
			pot+1
		}
		if(plusPetit(pot-1,pot+1)){
			echange(pot-1,pot+1);
			if(pot>0){
				pot-=1;	
			}
		}else{
			pot+=1;
		}
	}
}
