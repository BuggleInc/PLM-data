def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
  if(!isBirthday){
    if(speed<61){return 0}
    if(60<speed && speed<81){return 1}
    else{ return 2}
  }
  else{
    if(speed<61+5){return 0}
    if(60+5<speed && speed<81+5){return 1}
    else{ return 2}
  }
}