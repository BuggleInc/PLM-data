while (!estFaceMur()) {
  setDirection(Direction.EAST)
  while ((!estSurBiscuit()) || (!estFaceMur())) {
    avance()
  }
  prendBiscuit()
  setDirection(Direction.WEST)
  while (!estSurBiscuit()) {
    avance()
  }
  recule()
  poseBiscuit()
  retourne()
}