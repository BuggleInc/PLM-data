def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
	if (steps == 0){
		return;
	}
	current(steps);
	avance(length);
	droite(angle);
	tree(steps-1,length*shrink,angle,shrink);
	gauche(2*angle);
	tree(steps-1,length*shrink,angle,shrink);
	droite(angle);
	leveCrayon();
	recule(length);
	baisseCrayon();
}

/*
 Pour dessiner un arbre � quatre �tages, il faut dessiner un tronc de la taille indiqu�e, 
 tourner � droite de l'angle indiqu�,
 faire un arbre � 3 �tages, tourner � gauche de deux fois l'angle indiqu�, 
 faire un autre arbre � 3 �tages, puis revenir � sa position initiale.
 N'oubliez pas de revenir � la position initiale !

Si le tronc d'un arbre mesure length � l'�tage N, il mesure shrink*length � l'�tage N+1.

 */
