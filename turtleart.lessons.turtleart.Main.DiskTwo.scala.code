override def run() {

for (j <- 1 to 36)
{
	for (i <- 1 to 20)
	{
		avance(100);
		recule(100);
		droite(1);
	}

	if (getCouleur() == Color.red) setCouleur(Color.black);
	else setCouleur(Color.red);
}
setCouleur(Color.red);
}
