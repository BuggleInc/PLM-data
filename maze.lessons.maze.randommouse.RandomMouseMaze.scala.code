override def run() {
  var alea : Int = 0
  while (!estSurBiscuit) {
    alea = random3()
    if (alea==0 && !estFaceMur()) {avance()}
    if (alea==1) {gauche()}
    else {droite()}
    }
  prendBiscuit()
}