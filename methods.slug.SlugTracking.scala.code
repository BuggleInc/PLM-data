def isFacingTrail():Boolean = {
  	var vuVert: Boolean = false
  	forward();
  	if (estFaceMur()==false) {
      if (getCouleurSol()==Color.green) {
        vuVert=true
      } else {
        isFacingTrail();
      }
    } else {
      left();
      isFacingTrail();
    }
  	backward();
  	return vuVert
}

