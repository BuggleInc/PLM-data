
def faireMotif () {
  for (cpt0 <- 1 to 3) {
  	for (cpt <- 1 to 4) {
      avance(2)
  	  baisseBrosse()
  		leveBrosse()
  		avance()
  		gauche()
  		avance()
  		baisseBrosse()
  		leveBrosse()
  		droite()
  		avance()
  		droite()
  		avance()
  		baisseBrosse()
  		leveBrosse()
  		gauche()
  		avance(2)
  		gauche()
		}
		avance(7)
   }
  retourne()
	avance(21)
	droite()
	avance(7)
  droite()
  
}
 
override def run() {
 for (cpt1 <- 1 to 3) {
   faireMotif()
 } 
 
}

