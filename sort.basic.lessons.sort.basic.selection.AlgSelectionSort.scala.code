def selectionSort() {

	var currentMin = getValeur(1);
	var rank = 1;
	
	for ( i <- 0 to getNombreValeurs - 1)
	{
		for ( j <- i to getNombreValeurs - 1)
		{
			if (plusPetitQue(j, currentMin))
			{
				currentMin = getValeur(j);
				rank = j;
			}						
		}

		copie(i,rank);
		setValeur(i, currentMin);
		currentMin = getValeur(i+1);
		
	}
}
