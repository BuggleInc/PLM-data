def bubbleSort3() {
  var ech = true 
  for (j <- 1 to getNombreValeurs()){
    ech = false
  	for (i <- 0 to getNombreValeurs()-1 -j ){
    	if (plusPetit(i+1,i)){
    	  echange(i,i+1)
      	ech = true
      }
    }
    if (!ech){
      return
    }
  }
}