override def run() {
  ligne4()
  
}

def ligne4(){
  for (i <- 1 to 4){
    
    baisseCrayon()
    avance(30)
  	droite(30)
  	
    for (i <- 1 to 3){
    	avance(30)
    	droite(120)
  	}
  	droite(60)
  	
    for (i <- 1 to 3){
  	  avance(30)
  	  droite(90)
  	}
   	leveCrayon(); droite(90); avance (50); gauche(90)
  	}
  
  gauche(90); avance(200); droite(90)
}