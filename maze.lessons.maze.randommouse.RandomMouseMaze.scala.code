override def run() {
  var plop=0
    while (!estSurBiscuit()){
      plop=random3()
      if (plop==0){gauche()}
      if (plop==1){
        if (!estFaceMur()){avance()}
      }
      if (plop==2){droite()}
    }
  prendBiscuit()
}
