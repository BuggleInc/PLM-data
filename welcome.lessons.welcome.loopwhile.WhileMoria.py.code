while(isFacingWall() == False):
    if(isCarryingBaggle() == False):
        backward()
    if(isOverBaggle() == True):
        pickupBaggle()
    if(isCarryingBaggle()== True):
        forward()
    if(isOverBaggle() == True):
        backward()
        dropBaggle()

    
