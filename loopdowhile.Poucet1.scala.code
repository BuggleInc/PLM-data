var compteBaggle = 0

while (!sortieTrouvee() ) {

	for (i <- 1 to 5) {

    avance()
    
		if(estSurBiscuit())
		{
			compteBaggle = compteBaggle + 1
		}
	}

	if(compteBaggle >= 3)
		gauche();
	else
		droite();

	
	compteBaggle = 0
}