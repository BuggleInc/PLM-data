def solve() {
  while(!isSorted()){
    for(i <- 0 to getStackSize()-1){
      if(getPancakeRadius(i) > getPancakeRadius(i+1)){
        for(j <- -1 to i-1){
          flip(i-j)
        }
      }
    }
  }
}