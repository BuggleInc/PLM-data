        def goAndGet() {
        	for (a<-1 to 7) {avance(); if(estSurBiscuit()) {prendBiscuit();}}
          for(b<-7 to 1) {recule();}
        }

        for (i <- 1 to 7) {
            goAndGet();
            right();
            forward();
            left();
        }
