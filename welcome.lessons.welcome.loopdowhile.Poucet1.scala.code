var baggle : Int = 0
do {
  baggle = 0
  for(couloir <- 1 to 5){
    avance()
    if (estSurBiscuit()){
      baggle = baggle + 1
    }
  }
  baggle match {
    case baggle if baggle >= 3 => gauche()
    case baggle if baggle <= 2 => droite()
  }
  
} while(! sortieTrouvee())
avance()