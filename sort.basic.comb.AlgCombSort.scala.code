def combSort() {
	var len = getValueCount();
	var gap = len;
    var swapped = false;

  do {
		if(gap>1)
    	gap  = gap * 10 / 13;
    var i = 0;
    swapped = false;

    while(i+gap < len){
      if(isSmaller(i+gap,i)){
        swap(i, i+gap);
        i+=1;
        swapped = true;
      }
    }

  } while(gap >= 1 || swapped)


}