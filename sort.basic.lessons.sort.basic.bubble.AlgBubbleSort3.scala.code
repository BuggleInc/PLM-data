def bubbleSort3() {
  var n = getNombreValeurs()
  var echanges = 0
  var stop = false
  for (j <- 0 to n-2){
  	for (i <- 0 to n-2-j){
    		if (plusPetit(i+1,i)){
      		echange(i,i+1);
      		echanges = echanges +1
    				}
 				 }
   	if (echanges==0){stop=true};
   	echanges = 0;
  }
}