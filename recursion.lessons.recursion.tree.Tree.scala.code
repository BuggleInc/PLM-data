def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {

def branche() = { 
	droite(angle);
	avance(length);
	recule(length);
	gauche(angle*2);
	avance(length);
	recule(length);
}
current(step);
avance(length);
branche()

}
