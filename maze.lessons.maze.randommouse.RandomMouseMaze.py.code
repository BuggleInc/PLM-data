
cur = (0, 0)

while cur != (3, 3):
# if cur[0] == 0:
#    x = random.randrange(0, 3)
    x = random3()    
    print(x)
    if x == 0:
        if !isFacingWall():
            left()
    elif x == 1:
        if !isFacingWall():
          right()
    elif x == 2:
        if !isFacingWall():
          forward()
    cur = (getX(), getY())