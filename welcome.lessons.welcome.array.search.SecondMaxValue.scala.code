def max2Value(nums:Array[Int]): Int = {
  var max = Math.max(nums(0),nums(1))
  var dmax = Math.min(nums(0),nums(1))
  for (i <- 2 to nums.length-1){
    if(max<nums(i)){
      dmax=max;
      max=nums(i);}
    else if(dmax<nums(i)){dmax = nums(i)}
  }
  return dmax
  
}