def withoutDoubles(die1:Int, die2:Int, noDoubles:Boolean):Int = {
	if (noDoubles) {
		if (die1==die2 && die1==6) {
			return die1+1
		} else if (die1==die2 && die1!=6) {
			return 2*die1+1
		} else {
			return die1+die2
		}
	} else {
		return die1+die2
	}
}
