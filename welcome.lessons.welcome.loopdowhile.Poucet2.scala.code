do {
  var nbBiscuit:Int = 0;
  while (!crossing()) {
    avance();
    if (estSurBiscuit) {
      nbBiscuit = nbBiscuit + 1;
  	}
  }
   if (nbBiscuit >= 3){
     gauche();
   } else {
     droite();
   }    
}
} while (!sortieTrouvee())

  