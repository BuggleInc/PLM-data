var compteur = 0
avance();
do { 
	
	while (!croisement()){
		avance();
		if (estSurBiscuit()){
			compteur = compteur + 1
		}
	}
	   else 
        if (compteur <= 2){
        	droite(); 
        	compteur = 0;       	
        }
        
        else gauche(); compteur = 0;
	    
} while( !sortieTrouvee())
avance();
