def noTriples(nums:Array[Int]): Boolean = {
  var valeur=nums(0)
  for(i<-0 to nums.length-1){
    if (i+1<=nums.length-1 && nums(i+1)==valeur){
      if (i+2<=nums.length-1 && nums(i+2)==valeur){
        return false
      }
    }else{
      if (i+1)
    }
  }
  return true
}
