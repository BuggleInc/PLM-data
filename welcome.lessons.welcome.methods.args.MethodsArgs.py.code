move(nbPas,versAvant):
    if getDirection() == Direction.NORTH():
        versAvant==True
        pas = getY()
        for i in range(7-pas):
            avance()
    if getDirection()==Direction.SOUTH:
        versAvant==False
        pas = getY()
        for i in range(7-pas):
            recule()       