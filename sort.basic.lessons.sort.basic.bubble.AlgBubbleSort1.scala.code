def bubbleSort()  {
  var swap = true
  while (swap) {
    swap = false
  for (i <- 0 until getNombreValeurs-1) {
      if (plusPetit(j+1,j)) {
        echange(j,j+1) 
        swap = true
      }
  }
}
