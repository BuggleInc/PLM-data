def insertionSort() {
  var L = getNombreValeurs()
  var v = 0
  var cpt = -1
  for (i <- 1 to L-1){
    var I = i-1
    v = getValeur(i)
    while (plusPetit(i,I) && I>0) {
      echange(I,getValeur(I+1))
      if (I!=0){
        I =I-1
      }
    }
    setValeur(I,v)
  }
}