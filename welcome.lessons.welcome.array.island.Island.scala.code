def island(num:Array[Int]): Int = {
	var n = num.length-1;
	var compt = 0;
	var diff= 0;
	for (k <- 0 to n-1) {
		if (num(k+1) > num(k)) {
			diff = num(k+1) - num(k);
			compt = compt + diff;
			diff =0;
		}
}
return compt;
}
