def haveBaggle():Boolean = {var seenBaggle:Boolean=false; 
                            if (isOverBaggle) {seenBaggle=true}
                            for (stepper<-1 to 6) {if (isOverBaggle) {seenBaggle=true}; forward()} 
                            for (stepper1<-1 to 6) {backward()}
                            return seenBaggle}