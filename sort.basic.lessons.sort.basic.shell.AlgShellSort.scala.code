def shellSort()  {
  var gap:Int = getValueCount() / 2
  while(gap >= 1){
    for(i <- 0 to getValueCount() - gap - 1)
      if(getValue(i) > getValue(i + gap))
      	swap(i, i + gap)
    gap /= 2
  }
}