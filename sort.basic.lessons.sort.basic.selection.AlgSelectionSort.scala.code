def selectionSort() {
  val lgr:Int = getNombreValeurs()
  for (i <- 0 to lgr-2) {
    var posMin:Int = i
    for (j <- i+1 to lgr-1) {
      if (plusPetit(j,posMin)) {
        posMin = j
      }
    }
    echange(i,posMin)
  }
}
