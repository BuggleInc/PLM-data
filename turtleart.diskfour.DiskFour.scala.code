override def run()
{
  val couleurs = List(Color.black, Color.red, Color.yellow, Color.magenta);
  
  for (j <- 1 to 4)
  {
    setCouleur(couleurs(j));
  
    for (i <- 1 to 90)
    {
      avance(100);
      gauche(180);
      avance(100);
      gauche(179);
    }
  }
}