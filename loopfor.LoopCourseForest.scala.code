var tours:Int = 0
val posX:Int = getX():Int
val posY:Int = getY():Int

while(tours < 7) {
  
  for (i <- 1 to 4) {
    avance()
  }
  gauche()
  for (i <- 1 to 2) {
    avance()
  }
  droite()
  for (i <- 1 to 4){
    avance()
  }
  droite()
  for (i <- 1 to 2) {
    avance()
  }
  gauche()
  for (i <- 1 to 4) {
    avance()
  }
  gauche()
  
  if (getX() == posX && getY() == posY) {
    tours += 1
  }
  
}