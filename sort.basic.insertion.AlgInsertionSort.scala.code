def insertionSort() {
  for (i<- 1 to getNombreValeurs()-1){
    var v = getValeur(i)
		var k = i
    while (k>0 && !plusPetitQue(k-1,v)){
      
      copy(k-1,k)
      k-=1
    }
		setValeur(k,v)
    
}
}