override def run() {
  var y:Int=0;
  var count:Int=0;
  
  while(y!=getWorldHeight()){
    colonne(y);
    y=y+1;
  }
  def colonne(y:Int){
  var x:Int=0
  if(x!=getWorldHeight()){
    setPos(x,y);
    writeMessage(count);
    x=x+1;
    count=count+1;
  }
    }
}