def has271(nums:Array[Int]): Boolean = {
	var x:Boolean = false
	for (i <- 0 to nums.length-3) {
		if (nums(i) == nums(i+1)-5 && (nums(i)+2 <= nums(i+2) || nums(i)-2 >= nums(i+2))) {x=true}
	}
	return x
}
