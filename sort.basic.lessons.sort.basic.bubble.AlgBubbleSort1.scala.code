def bubbleSort()  {
	var sorted:Boolean = false;
	while (!sorted) {
		for (i <- 1 to getNombreValeurs()-1) {
			sorted = false;
			if (!plusPetit(i,i+1)) {
				sorted = true;
				echange(i,i+1);
			}
		}
	}
}
