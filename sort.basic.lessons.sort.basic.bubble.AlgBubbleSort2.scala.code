def bubbleSort2() {
  var n:Int = getNombreValeurs()
  var i:Int = 0
  
  for (i <- 0 to n - 1) {
    
    for (j <- 0 to n - i - 2) {
      if (plusPetit(j + 1, j)) {
        echange(j + 1, j)
      }
    }
    
    i += 1
  }
}