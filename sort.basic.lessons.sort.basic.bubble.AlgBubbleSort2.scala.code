def bubbleSort2() {
  var length : Int = getValueCount();
  var i : Int = 0;
  var j : Int = 0;
  
	for(i<-0 to length-2)
  {
    for(j<-0 to (length-2-i))
    {
      if(isSmaller(j+1,j))
      {
        swap(j,j+1);
        
      }
    }    
  }
}