override def run() {
  MotifBas(Color.red,Color.blue)
}


def MotifBas(a:Color,b:Color) {
  setCouleurBrosse(a)
  avance;
  baisseBrosse;
  gauche;
  leveBrosse;
  avance;
  gauche;
  avance;
  baisseBrosse;
  leveBrosse
  gauche;
  avance;
  gauche;
  for (i<-1 to 2) {
    avance
  }
  setCouleurBrosse(b)
  baisseBrosse;
  leveBrosse;
  avance;
  gauche;
  avance;
  gauche;
  baisseBrosse;
  leveBrosse;
  avance;
  gauche;
  avance;
  gauche
}