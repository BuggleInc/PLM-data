boolean answerCell(boolean isMorning, boolean isMom, boolean isAsleep) {
	boolean answer = true;
	if (isMorning) if (isMom) answer = true; else answer = false;
	if (isAsleep) answer = false;
	return answer;
}
