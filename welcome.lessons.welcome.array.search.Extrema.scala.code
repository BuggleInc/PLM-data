def extrema(nums:Array[Int]): Int = {
  var n = nums.length
  var max = nums(0)
  var min = nums(0)
  for (pas <- 0 to (n-1))
  {
    if (nums(pas)>max)
    {
      max=nums(pas)
    }
    if (nums(pas)<min)
    {
      min=nums(pas)
    }
  }
  return max-min
}
