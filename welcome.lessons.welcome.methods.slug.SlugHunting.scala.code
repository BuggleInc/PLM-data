def hunt() {
    while(!estSurBiscuit){
      while((isFacingTrail()) && (!estSurBiscuit())){avance()}
      gauche()
      if (!isFacingTrail()){
        droite()
        droite()
      }
    }
  	prendBiscuit()
}

def isFacingTrail():Boolean = {
  if(estFaceMur()){return false}
  avance()
  if (getCouleurSol()==Color.green){
    recule()
    return true
  }
  else {
    recule()
    return false
  }
  
}