override def run() {
def keepHandOnSideWall() {
	if (!(estFaceMur())) {
	gauche();
	if (estFaceMur()) {
		droite();
		avance();
		gauche();
		if (!(estFaceMur())) {
			avance();
			gauche();
			if (!(estFaceMur())) {
				avance();
			}
		}
		
	}
	
}

while (!(estSurBiscuit())) {
keepHandOnSideWall();
}
}
