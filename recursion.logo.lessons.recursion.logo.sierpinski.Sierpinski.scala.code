def sierpinski(level:Int, length:Double) {
  if (level==0){
    avance(length);
    droite(45);
  }else{
  	sierpinski(level-1, length/2)
  	droite(120);
    sierpinski(level-1, length/2)
    droite(120)
    sierpinski(level-1, length/2)
    droite(120);
  }
}