def comp(a:Int): Boolean = {return (a>12 && a<20)}
def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {
  var cr = 0
  if (comp(a)) {cr = cr +1}
  if (comp(b)) {cr = cr +1}
  if (comp(c)) {cr = cr +1}
  if (comp(d)) {cr = cr +1}
  return cr}