def min(l:List[Int]): Int = {
	def lambda(l:list[Int],n:Int): Int = {
		if(l.head < n)
		return lambda(l.tail,n)
		else return lambda(n)
	}
	if(l.tail == Nil)
	return min(l)
	else return min(l.tail)
}
