def cocktailSort() {
  var n = getValueCount();
  var hadInversed = true;
  do {
    hadInversed =false;
    for (i <- 0 to n-2) {
      if (isSmaller(i+i, i)) {
        swap(i, i+1);
        hadInversed = true;
      }
    }
    for (i <- (n-2) to 0 by -1) {
      if (isSmaller(i+1, i)) {
        swap(i, i+1);
        hadInversed = true;
      	}
    	}
  	}
	 	while (hadInversed) 
}