public void run() {
int size = getWorldHeight();
Color[] cell;
cell = new Color[size];

	for(int i = 0; i < size; i++){
		for(int j = 0; j < size-1; j++){
			cell[j] = getGroundColor();
			forward();
		}
		if(i < size - 1){
			left();
			forward();
			right();
		}
	}
}
