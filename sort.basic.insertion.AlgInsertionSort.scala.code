def insertionSort() {
  var n = getValueCount();
  
  for (i <- 1 to n - 1) {
    var v = getValue(i);
    
    for (k <- 1 to i-1) {
      var j = i - k;
      var w = getValue(j)
      if(w > v) {
        setValue(j+1, w);
      }
    }
  }
}