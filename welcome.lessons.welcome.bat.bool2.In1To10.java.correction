$package import plm.universe.bat.BatEntity; import plm.universe.bat.BatWorld; import plm.universe.bat.BatTest; import plm.universe.World; public class in1To10 extends BatEntity {  @SuppressWarnings("unchecked") public void run(BatTest t) {
		t.setResult( in1To10((Integer)t.getParameter(0), (Boolean)t.getParameter(1)) ); }
	boolean in1To10(int n, boolean outsideMode) {
		return (outsideMode && (n <= 1 || n >= 10)) || ((! outsideMode) && (n >= 1 && n <= 10));
	}
 }