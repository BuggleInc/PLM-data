override def run() { 
  for (i<-1 to 3){
    for(i<-1 to 3) {
  	 faireMotif()
   		avance(5)
    }
   gauche()
   avance(5)
   droite()
  }
}

def marquer(){baisseBrosse();leveBrosse()}

def faireV(c:Color){
  setCouleurBrosse(c)
  avance()
  marquer()
  avance()
  gauche()
  avance()
  marquer()
  recule()
  droite()
  avance()
  marquer()
  avance()
  gauche()
  
}

def faireMotif() {
  faireV(Color.YELLOW)
  faireV(Color.RED)
  faireV(Color.BLUE)
  faireV(Color.GREEN)
  }


