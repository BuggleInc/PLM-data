def bubbleSort()  {
	var test:Boolean = true;
	while(test==true)
	{
		test=false;
		for(i <-0 to (getNombreValeurs()-2) by 1){
			if(plusPetit(i+1,i))
			{
				echange(i,i+1);
				//var temp:Int =getValeur(i);
				//setValeur(i,getValeur(i+1));
				//setValeur(i+1,temp);
				test=true;
			}
		}
	}
}
