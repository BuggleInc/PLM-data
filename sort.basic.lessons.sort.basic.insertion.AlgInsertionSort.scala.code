def insertionSort() {
	var stockage=0
  var k=0
  for (i<- 1 to getNombreValeurs()-1) {
    stockage=getValeur(i)
    k=i-1
    while(!plusPetitQue(k,stockage)) {
      copie (k,k+1)
      k-=1
      if (!plusPetitQue(k,stockage))  {setValeur(k,stockage-1)}
    }
  }
}