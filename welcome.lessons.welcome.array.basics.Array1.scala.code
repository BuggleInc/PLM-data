override def run() {
var tab = new Array[Color] (6)
for (i<- 0 to 5 ) {tab(i) = getCouleurSol();avance()} 
baisseBrosse()
for (i<-0 to 4) {gauche();avance();droite();avance()
for(h<- 0 to 5) { setCouleurBrosse(tab(h));avance()}}
}
