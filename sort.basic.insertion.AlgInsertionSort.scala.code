def insertionSort() {
  var v=0

  
  for(i <- 1 to getValueCount()-1){  
    var n = i
    v = getValue(n)
    
    while(n > 0 && !isSmallerThan(n,v)){
      copy(n,n+1)
      n = n-1
    }
   setValue(n,v)
  }
}