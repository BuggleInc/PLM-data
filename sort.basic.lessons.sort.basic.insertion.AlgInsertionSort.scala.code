def insertionSort() {
  var n = getNombreValeurs()
  var v=0
  for (i <- 1 to n-1){
    v=getValeur(i)
    while (getValeur(i-1)>v){
      setValeur(i,getValeur(i-1))
      }
      i=i-1
      
    }
  }
}