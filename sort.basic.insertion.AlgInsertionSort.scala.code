def insertionSort() {
  for (i <- 1 to getValueCount()-1) {
    var tmp=getValue(i);
    var sp=false;
    var tmp2=0;
    for (j <- 1 to i-1) {
      if (!isSmaller(i-j,i)) {
        copy(i-j, i-j+1)
        tmp2=i-j
        sp=true
      }
    }
    if (sp==true) {
      setValue(tmp2,tmp)
    }
  }
}