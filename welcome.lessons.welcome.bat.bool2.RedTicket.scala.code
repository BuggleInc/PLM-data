def redTicket(a:Int, b:Int, c:Int):Int = {
  (a,b,c) match {
    case (a,b,c) if a == 2 && a == b && c ==a => 10
    case (a,b,c) if  a == b && b== c && c ==a  => 5
    case (a,b,c) if a!=b && a!=c => 1
    case (_,_,_) => 0
  }
}