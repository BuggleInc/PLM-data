def min(l:List[Int]): Int = {
  if (l==Nil) {
    return 0 
  } else if (l.tail==Nil) {
    return l.head
  } else {
    if (l.head<(l.tail).head) {
      min(l.head::(l.tail).tail)
    } else {
      min(l.tail)
    }
  }
}
