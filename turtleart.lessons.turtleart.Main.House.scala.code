override def run() {
	avance(100)
	var x = getX()
	var y = getY()
	droite(30)
	avance(100)
	allerVers(x+100,y)
	allerVers(x,y)
	droite (60)
	setPos(x,y+100)
	avance(100)
	gauche(90)
	avance(100)
	setPos(x,y+100)
		
}
