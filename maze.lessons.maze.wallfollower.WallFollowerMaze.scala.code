override def run() {
  
  def keepHandOnSideWall() = {
    gauche()
    if (estFaceMur){
      droite()
      avance()
    }
    else {
      gauche()
      if (!estFaceMur()){
        avance()
      }
      else {
        gauche()
        avance()
      }
    }
  }
  
  keepHandOnSideWall()
}