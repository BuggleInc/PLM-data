def bubbleSort()  {
  
  var x=getValueCount()
  var i = 0
  var echange = true
  
  if(isSmaller(getValue(0), getValue(1))) {
    swap(0, 1)
  }
  
  /*
  while ((echange==true) && (i<x)){
    
    echange = false;
    
    for (j <- 0 to x-i-2){
      
      if (isSmaller(getValue(j+1), getValue(j))){
        
        swap(j,j+1);
        echange = true;
        
      }  
      
    }
    
    i=i+1
    
  }*/
  
}