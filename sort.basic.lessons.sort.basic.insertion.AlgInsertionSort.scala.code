def insertionSort() {
	var stockage=0
  var k=0
  for (i<- 1 to getNombreValeurs()-1) {
    stockage=getValeur(i)
    k=i
    while(!plusPetitQue(k,stockage) && k>0) {
      copie (k-1,k)
      k-=1
    }
    setValeur(k,stockage)
  }
}