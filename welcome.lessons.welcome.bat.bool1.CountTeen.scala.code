def countTeen(a:Int, b:Int,c:Int,d:Int): Int = 
{
/* On dira qu'un nombre est "ado" s'il appartient est entre 13 et 19 (inclus). 
 Étant donné quatre nombres entiers, retournez combien d'entre eux sont ados.*/
var compteur=0

	 if((a>12 && a<20) )
 	{
	compteur=compteur +1
	}

 		if((b>12 && b<20) )
 		{
 		compteur=compteur+1
 		}
 	
 			if((c>12 && c<20) )
 			{
 				compteur=compteur+1
 			}

				if((d>12 && d<20) )
				{
					compteur=compteur+1
				}

     return compteur				
}