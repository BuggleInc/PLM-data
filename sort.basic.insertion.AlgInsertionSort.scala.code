def insertionSort() {
  var j:Int = 0
  var v:Int = 0
  var len:Int = getNombreValeurs()
  
  for (i <- 1 to (len - 1)) {
    j = i
    v = getValeur(j)
    while ((j > 0) && (!plusPetitQue((j-1),v))) {
      copie((j-1),j)
      j -= 1
    }
    setValeur(j,v)
  }
}