override def run() {
  var x=0
  for (x <- 1 to 4){
    maison()
  }
}
def maison() {
  var x=0
  for (x <- 1 to 4){
    avance(30)
    droite(90)
  }
  avance(30)
  droite(30)
  for (x <- 1 to 2){
    avance(30)
    droite(120)
  }
  gauche(90)
  avance(30)
  gauche(90)
  leveCrayon()
  avance(20)
  gauche(90)
  baisseCrayon()
}