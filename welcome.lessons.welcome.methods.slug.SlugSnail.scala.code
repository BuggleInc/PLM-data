 def hunt(c:Color) {
		
	while (!estSurBiscuit()){
    			while(isFacingTrail(c) && !estSurBiscuit() && !estFaceMur()){
				    	avance();
				    	baisseBrosse();
				    	leveBrosse();
				    }
			droite();
			if(isFacingTrail(c) && !estSurBiscuit() && !estFaceMur()){
				    	while (isFacingTrail(c) && !estSurBiscuit() && !estFaceMur()){
				    		avance();
				    		baisseBrosse();
				    		leveBrosse();
				    		
				    	}
			}else{
				    gauche();gauche();
				    if(isFacingTrail(c) && !estSurBiscuit() && !estFaceMur()){
				    		while (isFacingTrail(c) && !estSurBiscuit() && !estFaceMur()){
				    			avance();
				    			baisseBrosse();
				  			leveBrosse();
				    		}
				    			
				    	}
				}
				
			
				
		}
		prendBiscuit();
	}


def isFacingTrail(c:Color):Boolean = {
	if(!estFaceMur()){
		avance();
	    if(getCouleurSol()==c){
	    	 recule();
	    	return true ;
	    }
	    else {
	    	recule();
	    	return false;
	    }

	}
	else {
		return false ;
	}
}


