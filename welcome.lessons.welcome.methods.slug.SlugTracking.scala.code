def isFacingTrail():Boolean = {
	var m:Boolean = estFaceMur()	
     if (m == false) {
    		avance;
    		if (getCouleurSol() == Color.green) {
    			recule;
    			return true;
    		} else {
    			recule;
    			return false;
    		}
    }
}        
