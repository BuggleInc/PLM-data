def inOrderEqual(a:Int, b:Int, c:Int, equalOk:Boolean):Boolean = {
	if (equalOk==false){
		(a,b,c) match{
			case (a<b && b<c) => return true
			case _ => return false
		}
	}else{
		(a,b,c) match{
			case 
		}
	}
}
