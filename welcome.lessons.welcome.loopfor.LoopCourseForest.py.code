for i in range(7):
    forward()
    for cote in range(4):
        forward(4)
        gauche()
        forward(2)
        droite()
        forward(4)
        droite()
        forward(2)
        gauche()
        forward(4)
