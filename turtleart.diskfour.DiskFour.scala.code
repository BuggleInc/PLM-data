override def run() {
  for (i <- 0 to 89) {
    avance(100)
    recule(100)
    droite(1)
  }
  setCouleur(Color.RED)
  for (i <- 0 to 89) {
    avance(100)
    recule(100)
    droite(1)
  }
  setCouleur(Color.YELLOW)
  for (i <- 0 to 89) {
    avance(100)
    recule(100)
    droite(1)
  }
  setCouleur(Color.MAGENTA)
  for (i <- 0 to 89) {
    avance(100)
    recule(100)
    droite(1)
  }
}