def indexOfMaxValue(nums:Array[Int]):Int = {
  var max:Int = nums(0)
  var indexMax:Int = 0
  for(i <- 0 to nums.length - 1)
  	if(nums(i) > max){
      max = nums(i)
      indexMax = i
    }
  return indexMax
}