var diag = 0;
override def run() {var count:Int=0; for (i<-0 to 6) {for (j<-0 to diag) {setPos((diag-j), j); writeMessage(count); count=count+1}; diag=diag+1}; 
diag=0; count=48;
for (i<-0 to 5) {for (j<-0 to diag) {setPos((6-(diag-j)), (6-j)); writeMessage(count); count=count-1}; diag=diag+1}
}
