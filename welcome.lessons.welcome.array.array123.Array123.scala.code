def array123(nums:Array[Int]): Boolean = {
  var place:Int=0
	while ((nums.length-place-2)>0){
    if (nums(place)==1 && nums(place+1)==2 && nums(place+2)==3){
      return true
    }else{
      place=place+1
    }
  }
}