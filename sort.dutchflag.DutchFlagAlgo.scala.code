def solve() {
  while (! isSorted()) {
    for (x <- 0 to getSize()-1) {
      if (getColor(x)==BLUE) {
        swap(x,0)
      } else { 
        if (getColor(x)==RED) {
          swap(x,getSize()-1)
        }
      }
    }
  }
}
