def step(rule:Array[Char], colors:Array[Color]) {
	for(i<- 0 to colors.length-1){
	if(getCouleurSol()==colors(i)){
		setCouleurBrosse(colors(i%(colors.length-1)+1))
		baisseBrosse
		leveBrosse
		if (rule(i)=='R'){
			droite
		}else{
			gauche
		}
		avance()
}
	}
}
