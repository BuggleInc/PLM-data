override def run() {
  quarter()
  setColor(Color.red)
  quarter()
  setColor(Color.orange)
  quarter()
  setColor(Color.magenta)
  quarter()
}

def quarter() {
  for (i<-1 to 90) {
    forward(100)
    backward(100)
    right(1)
  }
}