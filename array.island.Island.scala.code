def island(num:Array[Int]): Int = {
  var i:Int = 0
  var previousLevel: Int = 0
  var levels: Int = 0
  while(i < num.length){
    if(num(i) - previousLevel >= 1){
      levels = levels + 1
    }
    previousLevel = num(i)
    i = i + 1
  }
  
  
  return levels
}