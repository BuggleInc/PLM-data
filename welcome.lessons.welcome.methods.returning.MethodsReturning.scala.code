def haveBaggle():Boolean = {
	var vuBiscuit: Boolean = false
	while (!vuBiscuit)
	{ for (i <- 1 to 6)
		{avance(); if (estSurBiscuit()) {vuBiscuit = true}
					else {vuBiscuit = false}}
		recule(6); droite();avance();gauche()
}