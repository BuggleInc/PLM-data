def insertionSort() {
	val lgr=getNombreValeurs()
  for(pivot<-1 to lgr-1){
    var vtemp=getValeur(pivot)
    var ptemp=pivot
    while(ptemp>0){if(getValeur(ptemp)>=vtemp){copie(ptemp-1,ptemp)};ptemp-=1}
    setValeur(vtemp,ptemp)
  }}