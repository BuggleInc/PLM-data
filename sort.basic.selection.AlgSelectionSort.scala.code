def selectionSort() {
  for (i<- 1 to getNombreValeurs()-1){
    var k = getValeur(i)
    for (j<- i to getNombreValeurs()-1){
      
      if (plusPetit(j,k)){
        k = j
      } 
      
    }
    swap(k,i)
  }
}