def bubbleSort()  {
	var est_trie:Boolean = true
	do {
		for (i<-0 to getNombreValeurs() - 2) {
			if (plusPetit(i+1,i)) {
				echange (i,i+1)
				est_trie = est_trie && true
			}
			est_trie = false
		}	
	} while (est_trie)
}
