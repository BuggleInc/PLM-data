override def run() {
  def carre() {
    avance(100)
    droite(90)
    avance(100)
    droite(90)
    avance(100)
    droite(90)
    avance(100)
  }
  
  carre()
  droite(90)
  droite(90)
  carre()
  droite(90)
  droite(90)
  carre()
  gauche(90)
  gauche(90)
  carre()
  droite(90)
  droite(90)
}