def isFacingTrail():Boolean = {
  if (estFaceMur()) {return false}
  else if (getCouleurSol() != Color.green) {
      return false
  recule()}
  else if (getCouleurSol() == Color.green) {
    return true
  recule()}
}