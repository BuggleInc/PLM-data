override def run() {
		var depart:Color = getCouleurSol()
	var hauteur = 0
		do{
		avance()
		hauteur = hauteur + 1
	}while (getCouleurSol!=depart)
	var tab = new Array[Color](hauteur)
	for(i <- 0 to hauteur-1){
		tab(i) = getCouleurSol();
		avance()
		}
		for(c <- 1 to hauteur - 1){
			gauche()
			avance()
			droite()
		for(i <- 0 to hauteur-1){
	var decal = litMessage().toInt
	setCouleurBrosse(tab(i%decal))
	baisseBrosse()
	leveBrosse()
	avance()}
		}
}
