override def run() {
  for (ligne <- 1 to 3) {
    faireLigne();
  }
  droite();
  longeur();
}

def faireV() {
  avance();
  avance();
  baisseBrosse();
  leveBrosse();
  avance()
  gauche()
  avance();
  baisseBrosse();
  leveBrosse();
  recule();
  droite();
  avance();
  baisseBrosse();
  leveBrosse();
  avance();
  avance();
}

def faireMotif() {
  for (cote <- 1 to 4) {
    faireV();
    gauche();
  };
  for (pas <- 1 to 6) {
    avance();
  };
}

def faireLigne() {
  for (bouts <- 1 to 3) {
    faireMotif();
  };
  retourne();
  longeur();
  gauche();
  for (pas <- 1 to 6) {
    avance();
  };
  droite();
}

def longeur() {
  for (pas <- 1 to 18){
    avance();
  }
}