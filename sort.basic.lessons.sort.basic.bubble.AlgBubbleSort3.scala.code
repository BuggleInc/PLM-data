def bubbleSort3() {
	var sorted : Boolean = false;
	for (i <- getNombreValeurs()-2 to 0 by -1) {
		sorted = true;
		for (j <- 0 to i) {
			if (!plusPetit(j,j+1)) {
				echange(j,j+1);
				sorted = false;
			}
		}
		if (sorted) {
			return;
		}
	}
}
