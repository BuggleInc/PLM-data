retourne();
while (estFaceMur() == false) {

  if (!estSurBiscuit()) {
    avance();
  } else if (estSurBiscuit()) {
  
  	prendBiscuit();
  	retourne();
    
  }
  if (!estSurBiscuit()) {
    avance();
  } else if (estSurBiscuit()) {
    recule();
  poseBiscuit();
  
  retourne();
  avance(); 
  }
   
  
}

droite();
avance();
gauche();
avance();

//A corriger