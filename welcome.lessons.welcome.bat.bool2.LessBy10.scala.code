def lessBy10(a:Int, b:Int, c:Int):Boolean = {
	var H=0;
	var P=0;
	var U=0;
	H=Math.abs(a-b);
	P=Math.abs(a-c);
	U=Math.abs(a-c);
	if (H>=10 | P>=10 | U>=10) {
		return true;
	}
	else return false;
}
