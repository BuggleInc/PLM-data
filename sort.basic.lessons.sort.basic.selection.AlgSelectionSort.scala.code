def selectionSort() {
  var max : Int = 0
  var imax : Int = 0
  var temp : Int =0
  for(i<-0 to getNombreValeurs()-2){
    max=getValeur(i);
    imax = i;
    for(k <-i to getNombreValeurs()-1){
      if(plusPetitQue(k,max)){max = getValeur(k);imax=k}
    }
    echange(i,imax)
    
  }
  
}