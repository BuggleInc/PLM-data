def array123(nums:Array[Int]): Boolean = {
	for (i <- 0 to nums.length - 1) {
    if (i >= nums.length - 2)
    	return false
    
    if (nums(i) == 1)
      return (nums(i + 1) == 2 && nums(i + 2) == 3)
  }
  
  return false
}