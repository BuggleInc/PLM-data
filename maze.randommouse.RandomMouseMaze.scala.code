override def run() {
  var r = 0;
  while(!estSurBiscuit()){
    r = radom3();
    r match {
      case 0 => if(!faceMur()){avance()};
      case 1 => droite();
      case 2 => gauche();
    }
  }
  prendBiscuit();
}