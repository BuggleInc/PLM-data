def bubbleSort()  {
  sorting:Boolean = true;
  
  while (sorting == true) {
    sorting = true;
    
  	for (i <- 0 to getValueCount()-1 ) {
      
      if (isSmaller(i, i+1)) {
        swap(i, i+1);
        sorting = false;
      }
    }
  }
}
