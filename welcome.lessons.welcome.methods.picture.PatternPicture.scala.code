override def run() {
  for (k<-1 to 2) {
  	for (i<-1 to 2) {
  		avance();
 		 	forme(Color.BLUE);
  		forme(Color.GREEN);
  		forme(Color.YELLOW);
  		forme(Color.RED);
  		avance();avance();
    }
  }
}




def forme(c:Color) {
  setCouleurBrosse(c:Color);
  avance();
  baisseBrosse();leveBrosse();avance();gauche();avance();baisseBrosse();leveBrosse();

	


}