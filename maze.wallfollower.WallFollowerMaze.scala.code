def keepHandOnSideWall(){
  var continuer:Boolean = true
  if (!estFaceMur()){
    gauche()
    if (estFaceMur()){
      droite()
      avance()
    }else{avance()}
  }else{
    while(continuer){
      gauche()
      if (!estFaceMur()){
        avance()
        continuer = false
    	}
  	}
	}
}

override def run() {
  while (!estSurBiscuit()){
    keepHandOnSideWall
  }
  prendBiscuit()
}