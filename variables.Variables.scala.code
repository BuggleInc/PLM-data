// tant qu'on n'est pas sur le biscuit on avance

var pasavances:Int = 0
while (!(estSurBiscuit() || porteBiscuit())) 
{
    avance()
    pasavances += 1
}
//si on est arriv? l? c'est qu'on est sur le biscuit
if(estSurBiscuit())
    prendBiscuit() // une ligne, pas besoin d'accolades

while (pasavances > 0) 
{
    recule()
    pasavances -= 1
}
poseBiscuit()