while(!isFacingWall()) {
  left()
  left()

  while(!isOverBaggle()) {
		forward()
  }

  pickupBaggle()

  left()
  left()

  while(!isOverBaggle() && !isFacingWall()) {
		forward()
  }

  if(!isFacingWall()) {
    backward()

    dropBaggle()

    backward()
  }
}

right()
forward()
left()
forward()