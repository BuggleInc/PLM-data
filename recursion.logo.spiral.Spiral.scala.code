def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {
  var x =steps
  if (x != 0){
    avance(length)
    gauche(angle)
    x=x-1
    spiral(x,angle,length + increment,increment)
  }else{
  }
}