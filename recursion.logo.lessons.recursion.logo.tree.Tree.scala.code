def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if (steps == 0){
    
  }
  else {
    current(steps)
    avance(length)
    droite(angle)
    avance(length)
    recule(length)
    gauche(2*angle)
    avance(length)
    recule(length)
    tree(steps-1, shrink*length,angle, shrink)
   
  }
  
}