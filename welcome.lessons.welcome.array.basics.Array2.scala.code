override def run() {
	var T = new Array[Color](getMondeLargeur())
  
  for(i<- 0 to T.length-2){
  	T(i)=getCouleurSol()
  	avance()
  }
  
  for(l<- 0 to T.length-2){
    recule()
  }
  for (j <- 0 to T.length-2){ 
    gauche()
    avance()
    droite()
    for(l<- 0 to T.length-2){
    recule()
    }
  
    	for(k <- 0 to T.length-1){
    		setCouleurBrosse(T(k))
    		baisseBrosse()
      	leveBrosse()
    		avance()      
    	}    
  }
}