def indexOfMaxValue(nums:Array[Int]):Int = {
  var indexMaxi:Int = 0
  var maxi:Int = nums(0)
  var longueurTab:Int = nums.length
  for (indice <- 0 to longueurTab-1) {
    if (nums(indice) > maxi) {
      maxi = nums(indice)
      indexMaxi = indice
    }
  }
  return indexMaxi
}