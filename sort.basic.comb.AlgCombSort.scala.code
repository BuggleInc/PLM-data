def combSort() {
	var len = getValueCount();
	var gap = len;

  do {
		if(gap>1)
    	gap : Int = gap / 1.3;
    var i = 0;
    var swapped = false

    while(i+gap < len){
      if(isSmaller(i+gap,i)){
        swap(i, i+gap);
        i+=1;
        swapped = true;
      }
    }

  } while(gap >= 1 || swapped)


}