override def run() {
  var hasard=0
	hasard=random3()
  while (!estSurBiscuit()){
    if (hasard==0){
      if (est faceMur()){
        hasard=random3()
      }else{
        avance()
      }else{
        if(hasard==1){
          gauche()
          if(estFaceMur()){
            hasard=random()
          }else{
            avance
          }
        }
        if(hasard==2)
      }
    }
  }
    
  }	
}