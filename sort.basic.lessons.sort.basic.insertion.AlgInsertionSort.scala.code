def insertionSort() { 
  var v= 0
  for (i<-1 to getValueCount()-1) {
    v=getValeur(i)
    var j = i
    while (j>0 && getValeur(j-1)>v) {
      	copy(j-1,j)
        j=j-1
    }
    setValue(j,v)
  }
}