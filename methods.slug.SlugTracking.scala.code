def isFacingTrail():Boolean = {
  	var vuVert: Boolean = false
  	if (estFaceMur()==false) {
      forward();
      if (getCouleurSol()==Color.green) {
        vuVert=true
      } else {
        isFacingTrail();
      }
    } else {
      left();
      forward();
      isFacingTrail();
    }
  	backward();
  	return vuVert
}

