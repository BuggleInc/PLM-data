override def run() {
	var hauteur = getMondeHauteur();
	var hau = hauteur -1;
	var largeur = getMondeLargeur();
	var lar = largeur-2;
	var tab = new Array[Color] (hauteur)
	for (i<- 0 to hau-1 ) {
		tab(i) = getCouleurSol();
		}
		return tab}

