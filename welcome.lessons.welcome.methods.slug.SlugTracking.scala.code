def isFacingTrail():Boolean = {
   while (!estSurBiscuit())
   	{ avance()
   		if (estFaceMur()) {droite()
   			if (getCouleurSol() != Color.green)
   		 		{recule(); droite()}}}
   return true			
}        
