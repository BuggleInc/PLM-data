def shellSort()  {
	var gap:Int=(getNombreValeurs()/2).toInt;
	while(gap>0){
		for( i<-getNombreValeurs()-1 to 1){
			var v:Int= getValeur(i);
			var j:Int=i;
			while(j>0 && plusPetitQue(j-gap,v)==false){
				copie(j-gap,j);	
				j=j-gap;	
			}
			setValeur(j,v);
		}
		gap=gap-1;
	}
}
