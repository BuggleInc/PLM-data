def bubbleSort2()  {
  
  var count = 1
  var continue = true
  while (continue){
      continue = false
      for (i <- 1 to getValueCount() - count){
          if (isSmaller(i,i-1)){
            swap(i, i-1)
            continue = true
          } 
      }
    count += 1
  }
}