def teenSum(a:Int, b:Int):Int = {
	def ado(d:Int): Boolean = {
		if ( d > 12 && d < 20) {
		return true
		}else{
			return false
		}
	}
	if (!ado(a) && !ado(b)) {
		return a+b
	}else {
		return 19
	}
}
