def lessBy10(a:Int, b:Int, c:Int):Boolean = {
  var x=Math.min(a,b)
  var y=Math.min(x,c)
  if (y - 10 < a) {
    return true
  }
   if (y - 10 < b) {
    return true
  } if (y - 10 < c) {
    return true
  }
  return false }