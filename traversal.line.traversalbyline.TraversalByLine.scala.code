override def run() {
  var compt:Int=0;
  gauche;
  while (!posiFinal) {
   ecritMessage(compt);   
   if (!estDosMur) {
     setX(getX()+1);
     compt=compt+1
   }
    else {setY(0)
          setY(getX+1);
         compt=compt+1}
  } ;
  ecritMessage(48)}

def posiFinal():Boolean = {
  return (estDosMur && estDroiteMur)
    
}

def estDroiteMur():Boolean = {
   var a:Boolean=false ;
   droite;
   a=estFaceMur
  gauche ;
  return a
}