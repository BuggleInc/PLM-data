def insertionSort() {
  var memoire = 0
  var longueur = getNombreValeurs()-1
  for (i <- 1 to longueur){
    memoire=getValeur(i)
    var k=1
    while (i-k >= 0 && getValeur(i-k)>memoire){
      setValeur(i-k+1,getValeur(i-k))
      k=k+1
    }
    setValeur(i-k+1,memoire)
  }
}