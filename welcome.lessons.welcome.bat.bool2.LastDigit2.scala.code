def lastDigit(a:Int, b:Int, c:Int):Boolean = {
var resteAB= a%b
var resteAC=a%c
var resteBC= b%c

	if ((resteAB == resteAC || resteAB == resteBC) && resteBC == resteAC) return true
	else return false
}