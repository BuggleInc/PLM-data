override def run() {
  
def suivre_mur() {
  gauche();
  while(estFaceMur()){droite()}
  avance();
  }
  

def nord () {
  if (!estFaceMur){avance()}
  else {droite()
        if(!estFaceMur()){avance();gauche()}}
  
}

do {nord()}
  while (!(getY==0))
  gauche();
  gauche();
  do{suivre_mur()} while (!estSurBiscuit)
  prendBiscuit()
}
