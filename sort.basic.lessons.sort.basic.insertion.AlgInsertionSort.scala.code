def insertionSort() {
  var v=getValeur(0)
  var j =0
  if (getValeur(0)>getValeur(1)) {
    echange(0,1)
  }
  for (i <- 2 to getNombreValeurs()-1) {
    v=getValeur(i)
    j=i-1
    while (getValeur(j)>v && j!=0) {
      setValeur(j+1,getValeur(j))
      j-=1
    }
    setValeur(j,v)
  }
}
