override def run() {
	def PositionFinale():Boolean {
		(estFaceMur && getDirection==Direction.est)
	}
	baisseBrosse
	while (!PositionFinale){
		avance
		if (estFaceMur) {
			gauche
			avance
			droite
			recule(6)
		}
	}
}