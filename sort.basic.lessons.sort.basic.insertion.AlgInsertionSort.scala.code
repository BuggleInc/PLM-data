def insertionSort() {

	for(i<- 1 to getNombreValeurs()-1) 
	{
		val valeur = getValeur(i);
		var j=i;
		while((j>0) && (plusPetit(valeur,j-1)))
		{	
			copie(j-1,j);
			j-=1
		}
	setValeur(j,valeur);
	}	
}
