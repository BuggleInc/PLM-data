boolean squirrelPlay(int temp, boolean isSummer) {
	if (temp<60) {
		return false;
	} else if (isSummer) {
		if (temp<=100) {
			return true;
		} else return false;
	} else if (temp<=90=) {
		return true;
	} else return false;
}
