def bubbleSort2() {
  var n:Int = getNombreValeurs - 1
  for(i <- 0 to getNombreValeurs - 2) {
    n -= i
    for(j <- 1 to n) {
      if(getValeur(i - 1) > getValeur(i))
        echange(i - 1, i)
    }
  }
}