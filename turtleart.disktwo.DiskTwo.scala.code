override def run() {
  for(i <- 1 to 18) {
    for(j <- 1 to 20) {
      if(i % 2 == 0) {
        setColor(Color.red)
      }
      else {
        setColor(Color.black)
      }

      forward(100)
      backward(100)
      right(1)
    }
  }
}