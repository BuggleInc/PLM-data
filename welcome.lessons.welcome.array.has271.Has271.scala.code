def has271(nums:Array[Int]): Boolean = {
	var flag:Boolean=false;
	if (nums.length < 3) return false;
	else {
		for (i<- 0 to nums.length-3) {
			if ((nums(i) == (nums(i+1)+5)) && (nums(i) == (nums (i+2)-1))) flag = true;
		}
	return flag;}
}
