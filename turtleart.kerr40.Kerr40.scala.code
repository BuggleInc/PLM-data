override def run():Unit= {
  petale()
}

def v(){
  avance(30)
  gauche(90)
  avance(30)
}

def petale(){
  v()
  droite(45)
  v()
  
  droite(90)
  avance(30)
  droite(90)
  
  v()
  droite(45)
  v()
  droite(45)
  v()
  droite(45)
  
  droite(45)
  avance(30)
  droite(90)
  
  v()
  droite(45)
  v()
}