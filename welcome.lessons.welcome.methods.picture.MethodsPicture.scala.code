override def run() {for (compteur <- 1 to 3){
  Motif()
}}

def Motif(){
  for (compteur <- 1 to 3){
    faireMotif()
  }
  recule(21)
  gauche()
  avance(7)
  droite()
}
def faireMotif(){
  for (compteur <- 1 to 4){
    faireV(c)
   
  }
  avance(7)
}

def brosse(){
  baisseBrosse()
  leveBrosse()
}
def faireV(c:Color){
  setCouleurBrosse(c)
  avance(2)
  brosse()
  avance()
  gauche()
  avance()
  brosse()
  recule()
  droite()
  avance()
  brosse()
  avance(2)
  gauche()
}