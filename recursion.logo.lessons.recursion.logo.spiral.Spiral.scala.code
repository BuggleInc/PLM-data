def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {
  if (steps==0) return
  /*var sum : Double = 0.0
  while(sum!=360){
    avance(length)
    gauche(angle)
    sum= sum+angle
  }*/
  avance(length)
  gauche(angle)
  return spiral(steps-1, angle,length+increment,increment)
}