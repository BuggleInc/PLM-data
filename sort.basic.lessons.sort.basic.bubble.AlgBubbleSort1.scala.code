def bubbleSort()  {
  
  var n:Int = getNombreValeurs()
  var echangeCount:Int = 0
  
  while (echangeCount != 0) {
    echangeCount = 0
    
    for (i <- 0 to n - 2) {
      if (plusPetit(i + 1, i)) {
        echange(i + 1, i)
        
        echangeCount += 1
      }
    }
  }
}