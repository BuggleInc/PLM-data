def insertionSort() {
  var v = getValeur(0)
  val n = getNombreValeurs()
  var i = 0
  for (j <- 1 to (n-1)) {
    v = getValeur(j)
    i = j-1
    while (i>=0  && !plusPetitQue(i,v)) {
      setValeur(i+1,getValeur(i))
      i -= 1
    }
    setValeur(i+1,v)
  }
}