override def run() 
{
  baisseBrosse()
  
  def positionFinale() {
    var dir:Direction = getDirection()
    if(estFaceMur())
    {
    	return true
    }
    else if(dir == Direction.EAST)
    {
      gauche()
      if(estFaceMur())
    		return true
      droite()
    }
    else if(dir == Direction.WEST)
    {
      droite()
      if(estFaceMur())
    		return true
      gauche()
    }
    else return false
  }

}