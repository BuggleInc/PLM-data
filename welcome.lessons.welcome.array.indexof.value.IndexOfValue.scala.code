def indexOfMaxValue(nums:Array[Int] ,value:Int): Int = {
    		var u:Int = nums.length;
    		var i:Int = 0; var j:Int = -1;
    		while (i<u && j<0) {
    			if (nums(i)==value) j = i;
    			else i = i+1;
    		}
    		return j;
}
