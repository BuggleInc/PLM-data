var x = 0;
var y = 0;
 
for (y <- 1 to 7*4) { 
   	for (x <- 1 to 16*4) { 
		avance(4);
		gauche();
		avance(2);
		droite();
		avance(4);
		droite();
		avance(2);
		gauche();
		avance(4);
		gauche();
		if (x=16*4 || x=16*4*2 || x=16*4*3 || x=16*4*4 || x=16*4*5 || x=16*4*6 || x=16*4*7) {
			y=y+1;
		}
   	}
}
