override def run() {
  while (!estSurBiscuit()) {
    keepHandOnSideWall()
  }
}


def keepHandOnSideWall() = {
  if (estFaceMur) {
  	gauche();
  	if (estFaceMur()) {
    	droite();
    	avance();
  	}
  }
}