def bubbleSort3() 
{
	var modifie=false
	for(k<-0 to getNombreValeurs()-1)
	{
		modifie=false
		for(i<-0 to getNombreValeurs()-2-k)
		{
			if(getValeur(i)>getValeur(i+1))
			{
				echange(i,i+1)
				modifie=true
			}
		}
		if(!modifie)
			return
	}
}
