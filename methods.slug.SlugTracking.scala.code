def isFacingTrail():Boolean = {
    while (!estSurBiscuit()){
      if (getCouleurSol()==Color.green){
        avance(); return(true)
      }else{
        gauche();avance();return(false)
      }
    }
}