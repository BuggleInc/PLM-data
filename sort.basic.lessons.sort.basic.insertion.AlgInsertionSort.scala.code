def insertionSort() {
  var n = getNombreValeurs()
  var v = 8
  var j=0
  for (i <- 1 to (n-1)) {
    v = getValeur(i)
    j=i-1
    while ((j>=0)&&((getValeur(j)>v))) {
       setValeur(j+1,getValeur(j))
       j=j-1      
    }
    setValeur(j+1,v)
  }
}
