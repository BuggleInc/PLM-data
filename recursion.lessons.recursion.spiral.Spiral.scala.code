def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {	
	 
	var incre = increment
	var x=steps+incre
	
	avance(x)
	gauche(angle)
	var taille = length+incre
	
	if (length == 75){
	 	return
	 }
	 else
	 
	 {
	 	spiral(x, 90, taille, 5)
	 }
}

