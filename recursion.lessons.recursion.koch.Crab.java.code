void crab(int levels, double length) {
	if (levels == 0) {
		forward(length);
		return;
	}
	left(45.0);
	crab(levels - 1, Math.sqrt(length * length));
	right(90.0);
	crab(levels - 1, Math.sqrt(length * length));
	left(45.0);
}
