def cote(c:Color) {
  avance();baisseBrosse(); leveBrosse(); gauche(); avance(); droite(); baisseBrosse(); leveBrosse(); avance(); droite(); avance();baisseBrosse(); leveBrosse(); gauche(); avance();gauche()
}

def carre() {
  for (compteur <- 1 to 4) {cote(Color.YELLOW);cote(Color.RED);cote(Color.BLUE);cote(Color.GREEN)}
}

override def run() {
  for (compteur <- 1 to 3) {
    for ( compteur1 <- 1 to 3){
      carre(); avance(); avance(); avance(); avance(); avance()
    }
  }
}