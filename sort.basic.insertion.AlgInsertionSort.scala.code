def insertionSort() {

  for(i<- 1 to getNombreValeurs()-1){
    var v = getValeur(i)
    var j=i
    while(getValeur(j-1)>v){
 			setValeur(j-1,getValeur(j))
      j=j-1
    }
    setValeur(j,v)
    
  }
}