def rechercheObstacle() {
  retourne()
  if (estFaceMur) {
      droite();avance();droite();avance()
    }
  while (!estFaceMur) {
    if (estFaceMur) {
      droite();avance();droite();retourne();avance()
    }
    while (!estSurBiscuit) {
      if (estFaceMur) {
      droite();avance();droite();retourne();avance()
    }
      avance()
    }
    if (estSurBiscuit) {
      prendBiscuit(); retourne(); deposeBaggleLoin()
    } 
    if (estFaceMur) {
      droite();avance();droite();retourne();avance()
    }
    

  }
  if (estFaceMur) {
      droite();avance();droite();retourne();avance()
    }
  
}


def deposeBaggleLoin() {
  while (!estSurBiscuit) {
    avance()
  }
  recule()
  poseBiscuit()
  retourne()
  avance()
}

rechercheObstacle()