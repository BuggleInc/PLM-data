def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {
  (steps,angle,length,increment) match {
    case (steps,angle,length,increment) if steps>0 => spiral(steps-1,angle,length+increment,increment)
  }
}