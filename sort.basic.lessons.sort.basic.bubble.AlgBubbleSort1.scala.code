def bubbleSort()  {
  var count= -1
  var n= getNombreValeurs()
  
  while (count != 0){
    count= 0
    for (i <- 0  to n-2){
      if (getValeur(i)>getValeur(i+1)){
        echange(i,i+1)
        count=count+1
      }
    }
  }
}
