def bubbleSort()  {
	var inverse = true;
	
	do{
		var i = 0;
		for(i <- 1 to getNombreValeurs()) {
	         if(plusPetit(i+1,i)) {
			echange(i+1,i);
	         }
	         else {
	         	var inverse = false;
	         }
		}
      }while( inverse )
      
}
