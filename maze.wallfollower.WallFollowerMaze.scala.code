override def run() {var x:Int=0;
                    while (!isOverBaggle()) {if (isFacingWall()) {right(); forward()}
                                             else {right(); if (isFacingWall()) {right();                                                            forward()}}};
pickupBaggle()
    // Your code here 
}