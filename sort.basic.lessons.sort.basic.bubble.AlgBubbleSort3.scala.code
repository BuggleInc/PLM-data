def bubbleSort3() {

	for ( i <- 0 to getNombreValeurs -2) {
		for (j <- 0 to (getNombreValeurs -2-i)) {
			var inverse = false
			if (!plusPetit(j,j+1)) {
				echange(j,j+1)
				inverse = true
			}
			if (inverse==false) {
				return
			}
		}
	}

}
