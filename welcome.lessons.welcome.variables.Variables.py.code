steps = 0

while(isCarryingBaggle()==True):
    backward(steps)
    while(isOverBaggle()==False):
        forward()
        steps = steps + 1
            if(isOverBaggle()):
                    pickupBaggle()


    