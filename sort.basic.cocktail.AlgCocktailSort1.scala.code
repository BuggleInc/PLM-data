def cocktailSort() {
  var l=getValueCount()
  var i=0
  var k=0
  for (i <- 0 to (l-2)) {
    if (isSmaller(i,i+1)==false){
      swap(i,i+1)
    }
    for (i <- (l-2) to 0 by -1){
      if (isSmaller(i+1,i)==true){
        swap(i,i+1)
      }
    }
  }
  }
}