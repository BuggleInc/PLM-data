def noTriples(nums:Array[Int]): Boolean = {
  var res=false
  val taille=nums.length
  
  for (i<-0 to (taille-3)){
    if (!((nums(i))==(nums(i+1))==(nums(i+2)))){
      res=true
    }
  }
  return res
  
}
        