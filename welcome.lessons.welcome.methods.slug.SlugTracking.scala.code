def isFacingTrail():Boolean = {
  if ( estFaceMur() ){
    gauche();
  } else {
    avance();
    if ( getCouleurSol() == Color.green ){
      recule();
      return true;
    } else { 
     return false;
    }
  }
}