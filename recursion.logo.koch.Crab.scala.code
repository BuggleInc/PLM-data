def crab(levels:Int, length:Double) {
  
    if(levels == 0){
    
    	avance(length);
    
  	} else{
  
      crab(levels-1, length*0.14);
      gauche(120.00);

      crab(levels-1, length*0.14);
      right(60.00);  
      
   	}
  
}