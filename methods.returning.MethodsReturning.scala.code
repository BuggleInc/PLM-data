def haveBaggle():Boolean = {
  var seenBaggle=false;
  for (i <- 1 to 6) {
    for (j <- 1 to 6) {
      forward();
      if (isOverBaggle) {
      seenBaggle=true;
      }
    }
    if (seenBaggle) {
      return seenBaggle
    }
  }
  return seenBaggle
}