def golomb(num:Int): Int = {
	var T=new Array [Int] (1000);
	T(0)=0;
	T(1)=1;
	T(2)=2;
	T(3)=2;
	var c=0;
	for (i <- 3 to num+1) {
		c=T(i);
		for (l <- i+1 to i+c) {
			T(l)=i;
		}
	}
return T(num);
}
