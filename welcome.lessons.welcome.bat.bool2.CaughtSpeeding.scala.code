def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
  if (isBirthday == true && speed < 66 || isBirthday == false && speed <61 ) {
    return 0
  } else if (isBirthday == true && speed >= 66 && speed <= 86 || isBirthday==false && speed >=61 && speed <= 81) {
    return 1
  } else return 2
}