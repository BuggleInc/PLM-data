def insertionSort() {
	for(pointeur<-1 to getNombreValeurs()-1) {
		var buffer = getValeur(pointeur)
		var i=pointeur-1
		while ((i>0)&&(plusPetit(buffer,getValeur(i)))) {
			setValeur(i+1,getValeur(i))
		}
		setValeur(i,buffer)
	}
}
