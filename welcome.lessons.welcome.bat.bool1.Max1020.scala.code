def max1020(a:Int, b:Int):Int = {
	var c=0;
	if ((a<=20 && a>=10) && (b>20 && b<10)) {
		return a;
	}
	else if ((a<10 && a>20) && (b>=10 && b<=20)) {
		return b;
	}
	else if ((a>=10 && a<=20) && (b>=10 && b<=20)) {
		c=Math.max(a, b);
		return c;
	}
}
