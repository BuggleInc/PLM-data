def gnomeSort() {
  var len=getValueCount()-1;
  var i=1
  while (i!=len) {
    if (i==0) {
      if (!isSmaller(0,1)) {
        swap(0,1)
      }
      i+=1
    }
    else if (i!=len) {
      if (!isSmaller(i,i+1)) {
        swap(i,i+1)
        i-=1
      }
      else {
        i+=1
      }
    }
  }
}