def squirrelPlay(a:Int, isSummer:Boolean):Boolean = {

var bool = false;

	if(a >= 60)
	{
		if(a == 100 && !isSummer)
		{
			bool = true;
		}
		if(a == 90 && isSummer)
		{
			bool = true;
		}
		
	}

	return bool;
}