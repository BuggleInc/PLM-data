def cocktailSort() {
var n = getNombreValeurs()-2;
var inv = true;
do {
	inv = false;
	for (i <- 0 to n) {
		if (plusPetit(i+1,i)) {
			echange(i,i+1);
			inv = true;
		}
	}
	for (i <- n to 0 by -1) {
		if (plusPetit(i+1,i)) {
			echange(i,i+1);
			inv = true;
		} 
}
} while (inv)
}



