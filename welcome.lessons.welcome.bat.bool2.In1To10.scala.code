def in1To10(a:Int, outsideMode:Boolean):Boolean = {

	var bool = false;
	
	if(outsideMode)
	{
		if(a < 1 && a > 10)
		{
		bool = true;
		}
	}
	else
	{
		if(a >= 1 && a <= 10)
		{
		bool = true;
		}
	}

		return bool;
	
}
