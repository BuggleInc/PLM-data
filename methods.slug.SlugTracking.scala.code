def isFacingTrail():Boolean = {
  	var couleur = Color.black
    while (estFaceMur()) {
      droite()
    }
    avance()
    couleur = getCouleurSol()
    recule()
    return (couleur == Color.green)
}