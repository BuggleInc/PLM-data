def insertionSort() {
  var n:Int = getValueCount();
  var v:Int = 0;
  var j:Int = 0;
  for (i<-1 to n-2) {
    v = getValue(i);
    j = i;
    while (isSmaller(j-1,v)) {
      setValue(getValue(j-1),j);
      j =  j-1;
    }
    setValue(j,v)
  }
}