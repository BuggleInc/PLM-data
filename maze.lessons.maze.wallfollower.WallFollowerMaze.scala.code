override def run() {
  while (!estSurBiscuit()) {
    keepHandOnSideWall()
  }
  prendBiscuit();
}


def keepHandOnSideWall() = {
  if (!estFaceMur) {
  	gauche();
    if (!estFaceMur()) {
      avance();
    }
  }
  if (estFaceMur()) {
    	droite();
    	avance();
    	gauche();
  }
  if (!estFaceMur()) {
  }
 
}