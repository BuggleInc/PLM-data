override def run() {
  while(!estSurBiscuit()){
  	while(getDirection!=Direction.nord)
    	gauche()
    if (estFaceMur())
    	longeGch()
    else(avance())
	}
  prendBiscuit()
}

def longeGch(){
  gauche()
  if(!estFaceMur())
    avance()
  else {
    droite()
    while(estFaceMur())
      droite()
    avance()
       }
}
