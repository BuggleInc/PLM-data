def extrema(nums:Array[Int]): Int = {
  var min : Int=nums(0)
  var max : Int=nums(0)
  var l : Int=nums.length
  for (j<- 0 to l-1){
    if (nums(j)>=max){
      max=nums(j)
    }
    if (nums(j)<=min){
      min=nums(j)
    }
  }
  return max-min
}
