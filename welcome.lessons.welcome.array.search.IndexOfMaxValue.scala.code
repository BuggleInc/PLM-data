def indexOfMaxValue(nums:Array[Int]):Int = {
  var max:Int=nums(0)
  val n:Int =nums.length
  var indice:Int=0
  for (i<-1 to n-1){
    if (nums(i)>max){
      max=nums(i)
      return indice =i
    }
  }
  return 0
}