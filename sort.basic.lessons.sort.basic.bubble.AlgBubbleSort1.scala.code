def bubbleSort()  {
var swap=false
val p=getNombreValeurs()
do{
	for (i<-0 to p-2){
		if (plusPetit(i,i+1)==true){
			echange(i+1,i)
			swap=true
		}
		for (i<-0 to p-2){
		if (plusPetit(i,i+1)==false){
			echange(i+1,i)
			swap=true
		}
	}
}while (swap==true)
}
