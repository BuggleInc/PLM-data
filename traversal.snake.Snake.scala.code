override def run() {
}

def testFinal():Boolean={
  fin=false
  if(getDirection()==Direction.EST && estFaceMur()==True){
    gauche()
    if(estFaceMur()==true){
      fin=true
    }
    droite()
  }
  return fin
}

def pas