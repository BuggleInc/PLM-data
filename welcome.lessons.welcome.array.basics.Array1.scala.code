override def run() {
	var j=0;
	var T = new Array[Color](getMondeHauteur());
	var haut=getMondeHauteur();
	var i=0;
	    
	   
	
	
	    for(i<-0 to haut-1){
		   
		      T(i)= getCouleurSol();
		      avance();
	}
	    recule(haut);
	
	for (j <-0 to haut-2){
		 
		 avance(); gauche(); avance(); droite();
		 
		 for(i<-0 to haut-1) { 
			
			setCouleurBrosse(T(i));
			baisseBrosse(); 
			leveBrosse();
			avance();
		}	
	}
}
