def bubbleSort()  {
  var len=getValueCount()
  var bool=false
  var swapp=false
  while(bool==false){
    swapp=false
  	for(i<-0 to len-2){
      if(!isSmaller(i,i+1)){
        swap(i,i+1)
        swapp=true
      
      }
      
      }
    if(swapp==false ) {
        bool==true
    }
  }
}
