def teen(a:Int): Boolean= {
	if (a>=13 && a<=19) {
		return true
	}
	else return false
}

def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {
	var x:Int=0;
	var T: array(a,b,c,d);
	
	for (y<-0 to 3) {
		if (teen(t(y))==true) {
			x=x+1
		}
	}
	return x
}