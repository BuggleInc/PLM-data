override def run() {
  while (!estSurBiscuit()) {
    keepHandOnSideWall()
  }
  prendBiscuit();
}


def keepHandOnSideWall() = {
  while (!estFaceMur) {
  	gauche();
  	if (estFaceMur()) {
    	droite();
    	avance();
  	}
  }
}