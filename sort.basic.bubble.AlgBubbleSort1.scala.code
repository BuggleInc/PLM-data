def bubbleSort()  {
  var listeTriee:Boolean=true
  var x=0
  x=getNombreValeurs
  while (listeTriee) {
    for (i <- 0 to x-2) {
       if (plusPetit(i+1,i)) {
         echange(i+1,i)
       } else {
         listeTriee=true
       }  
    }
  }
}