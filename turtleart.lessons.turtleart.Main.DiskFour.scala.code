override def run() {
	quartCercle(Color.black)
	quartCercle(Color.red)
	quartCercle(Color.orange)
	quartCercle(Color.magenta)
}

def quartCercle(couleur:Color)
{
	setCouleur(couleur)
	for (c <- 1 to 90)
	{
		avance(100); 
		recule(100); 
		droite (1);
	}
}
