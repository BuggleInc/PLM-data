def bubbleSort2()  {
  for(i<- 0 to getValueCount())
  	traverse(i)
}


def traverse(depart:Int) : Boolean = {
   var swapped = false;
   for (i <- depart to getValueCount() - 2){
     if(isSmaller(i+1, i)){
   		 swap(i,i+1);
       swapped = true;
	   }
   }
   return swapped;
}