def withoutDoubles(die1:Int, die2:Int, noDoubles:Boolean):Int = {
  if(noDoubles == true)
  	if(die1 == die2)
  		if(die1 == 6){return die2 + 1}
  		if(die2 == 6){return die1 + 1}
  		else(return die1 + die2 +1)
  	else(return die2 + die1)
  else(return die2 + die1)
}