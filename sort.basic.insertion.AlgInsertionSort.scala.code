def insertionSort() {
  
  for(i<- 1 to getValueCount()-1){
  	backMinus(i);
  }
  
}


def backMinus(indSorted:Int){
  var v = getValue(indSorted)
  for(i <- 1 to indSorted -1 by -1){
    if(isSmaller(v, getValue(i-1))){
      swap(i, i-1);
    }
    else{
      setValue(i-1, v);
      return;
    }
  }
  
}