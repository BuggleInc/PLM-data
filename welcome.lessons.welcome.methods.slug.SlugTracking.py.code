def isFacingTrail():
    if not isFacingWall():
        forward()
        getGroundColor()
        if Color != Color.green:
            backward()
            return False
        else:
            backward()
            return True
    else: 
        left()
        return False

while not isOverBaggle():
    isFacingTrail()
    if isFacingTrail():
        forward()

    
    
