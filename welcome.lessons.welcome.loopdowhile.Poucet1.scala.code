while (sortieTrouvee() == false) {
  var baggles: Int = 0
  for (i <- 1 to 5) {
    avance()
    if (estSurBiscuit == true) {baggles = baggles + 1}
  }
  if (baggles >= 3) {gauche()} else {droite}
}
avance()