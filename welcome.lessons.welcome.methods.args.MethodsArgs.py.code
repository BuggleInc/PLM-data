forward = False
def move(stepCount,forward):
    if forward:
        forward(stepCount)
    elif not forward:
        backward(stepCount)

stepCount = getX()
if getDirection() == Direction.NORTH:
    forward = True
elif getDirection() == Direction.SOUTH:
    forward = False

move(stepCount,forward)