def bubbleSort2() 
{
	var modifie=false
	var i=0
	do
	{
		modifie=false
		for(k<- 1 to getNombreValeurs()-2-i)
		{
			if(getValeur(k)>getValeur(k+1))
			{
				echange(k,k+1)
				modifie=true
			}
		}
		i=i+1
	}
	while(modifie)
}
