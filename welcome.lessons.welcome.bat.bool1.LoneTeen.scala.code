def loneTeen(a:Int, b:Int):Boolean = {
  if ((ado(a) && !ado(b)) || ( ado(b) && !ado(a))) {
    return true
  } else {return false}
}

def ado(a:Int) : Boolean = {
  if (a<=19 && a>=13) {
    return true
  } else  {return false}
}
