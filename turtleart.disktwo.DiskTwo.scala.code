override def run() {
  for (i <- 0 to 8) {
    setCouleur(Color.BLACK)
    for (j <- 0 to 19) {
      avance(100)
      recule(100)
      droite(1)
    }
    setCouleur(Color.RED)
    for (j <- 0 to 19) {
    avance(100)
    recule(100)
    droite(1)
  }
  }
}