"""Il faut faire la valeur absolue de la difference de tous les nombres
entre eux et retourner vrai si une de ces valeurs absolue est superieure
ou egale à 10"""

def lessBy10(a, b, c):
    import math
    if math.fabs(a-b)>=10 or math.fabs(a-c)>=10 or math.fabs(b-c)>=10:
        return True
    else:
        return False