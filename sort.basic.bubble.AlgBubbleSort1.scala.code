def bubbleSort()  {
  var ech: Boolean = false
  for (i<- 0 to (getNombreValeurs()-2)) {
    while (ech == false) {
      while (plusPetit(i+1, i)) {
      	echange(i,i+1);
      }
      ech = true
    }
 	}
}