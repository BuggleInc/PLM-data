def max2Value(nums:Array[Int]): Int = {
  var max:Int = nums(0)
  var maxSec:Int = nums(0)
  
  
  for (n <- nums){
    if (n > max){
      max = n
    }else if(n > maxSec){
      maxSec = n
    }
    
    }
  
  return maxSec
  }


