def combSort()  {
  
  var tri = false
  var compt = 0
  var a = 0
  var b = 0
  var long = getNombreValeurs()-1
  var ecart = long
  
  while (ecart > 1)
  {
    tri = true
    compt += 1
    for (i <- 0 to long/*-compt*/-ecart)
    {
      a = getValeur(i)
      b = getValeur(i+ecart)
      if (a > b)
      {
        setValeur(i,b)
        setValeur(i+ecart,a)
        tri = false
      }
    }
		ecart = (ecart.toDouble / 1.3).toInt
  }
  return
}