var diag = 0;

def tag(i:Int){
  ecritMessage(i)
}

override def run() {
  var nb=0
  var x=0
  for(y<-0 to getMondeHauteur()-1){
    setPos(0,y)
    tag(nb)
    nb=nb+1
    while(y>0 && x<=getMondeLargeur()-1){
      x=getX()
      setPos(x+1, y-1)
      tag(nb)
      nb=nb+1
    }
  }
}