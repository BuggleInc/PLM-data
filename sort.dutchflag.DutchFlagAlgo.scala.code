def solve() {
  var afterBlue:Int = 0
  var beforeWhite:Int = getSize()-1
  var afterWhite:Int = getSize()-1
  while (afterBlue<beforeWhite) {
    for (x <- 0 to getSize()-1) {
      if (getColor(x)==BLUE) {
        swap(x,afterBlue)
      } else { 
        if (getColor(x)==RED) {
          swap(x,afterWhite)
        } else {
          swap(x,beforeWhite)
        }
      }
    }
  }
}
