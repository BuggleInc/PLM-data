def combSort() {
  var ecart:Int = getNombreValeurs()
  var echanges:Boolean = true
  while ((echanges)||(ecart>1)){
  	if (ecart>1){
      ecart = (ecart.toDouble/1.3).toInt
    }
    var i:Int = 0
    while (i+ecart<getNombreValeurs()){
      if (getValeur(i)>getValeur(i+ecart)){
        echange(i,(i+ecart))
      }
      i = i+1
    }
  }
}