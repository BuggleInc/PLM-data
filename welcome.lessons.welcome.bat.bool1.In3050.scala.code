def in3050(a:Int, b:Int):Boolean = {
  if (( test1(a) && test1(b) ) || ( test2(a) && test2(b))){
    return true;
  } else {
    return false;
  }
}
  
def test1(a:Int):Boolean ={
  if ( a <=40 && a >= 30 ){
    return true;
  } else {
    return false;
  }
}
  
def test2(a:Int):Boolean ={
  if ( a <=50 && a >= 40 ){
    return true;
  } else {
    return false;
  }
}