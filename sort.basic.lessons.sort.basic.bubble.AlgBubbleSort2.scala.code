def bubbleSort2() {
  var nontri:Boolean= true 
  for (j<-1 to getNombreValeurs()){
    var compteur:Int=0
    for (i<-0 to getNombreValeurs()-2){
      if (plusPetit( i+1, i)) {
        echange(i, (i+1))
        compteur+=1}}
  }
}