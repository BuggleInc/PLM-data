var diag = 0;
override def run() {
  var cpt=0
  ecritMessage(cpt)
  while(diag < getMondeLargeur()+getMondeHauteur-1) {
    if(!estFaceMur()) {
      setPos(getX()+1,getY()-1)
    }
    else {
      setPos(0,getY()+1)
    }
    cpt=cpt+1
    ecritMessage(cpt)
    diag = diag +1
  }
}