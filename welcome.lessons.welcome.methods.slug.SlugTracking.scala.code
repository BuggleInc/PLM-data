def isFacingTrail():Boolean = {
  while (!estSurBiscuit()) {
    avance()
    if (getCouleurSol()==Color.green) {
      recule()
      baisseBrosse()
      avance()
      leveBrosse()
    } else { 
      recule()
      droite()
    }
  }
  prendBiscuit()
}