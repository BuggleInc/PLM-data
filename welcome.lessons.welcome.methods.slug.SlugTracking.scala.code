def isFacingTrail():Boolean = {
  var vert: Boolean = false;
	while (!estSurBagel) {
  	avance()
  	if (estFaceMur()) {
    	droite()
  	}
  	else if (getCouleurSol() == Color.green) {
      vert = true
     	avance()}
  	else {
    	recule()
    	droite()
  	}
    	}

  return vert
  }
