override def run() {
	var hauteur: Int = getMondeHauteur();
	var largeur: Int = getMondeLargeur();
	 var res = new Array[Int] (hauteur)

	def lireColonne(){
		var n: Int = 0;
		for(n <- 1 to hauteur){
			TCouleur(n) = getCouleurSol();
			avance();
		}
	}
	lireColonne()
}
