def makes10(a:Int, b:Int):Boolean =
{
	if(a==10 || b==10)   // Quand a et b sont des parametres de fonctions il faut mettre == pck on a pas le droit de changer les parametres
	{
	return true 
	}

		if(a!=10 && b!=10)
		{
		return false
		}

				if(a+b==10)
				{
				return true
				}

					
		    
}