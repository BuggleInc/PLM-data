public void run() {
	for (int x = 0; x < getWorldWidth(); x++) {
		for (int y = 0; y < getWorldHeight() - x; y++){
			setPos(x,y);
			int message = (int) ((((x + y) * (x + y + 1)) / 2) + x);
			writeMessage(message);
		}
	}
	for (int y = 1; y < getWorldHeight(); y++){
		for (int x = getWorldWidth() - y; x < getWorldWidth(); x++){
			setPos(x,y);
			int a = x - 1;
			int b = y + 1;
			int message = (int) ((((x + y) * (x + y + 1)) / 2) + x -1);
			writeMessage(message);
		}
	}
}
