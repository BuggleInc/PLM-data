while not estFaceMur():
    longitude = getX()
    setX(longitude + 1)


for i in range(8):
    altitude = getY()
    longitude = getX()
    gauche()
    if estFaceMur():
        droite()
        setX(longitude + 3)
    else : 
        setY(altitude - 1)
        droite()
        setX(longitude + 1)



