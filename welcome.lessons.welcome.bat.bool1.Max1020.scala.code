def max1020(a:Int, b:Int):Int = {
  var max = 0
  max=Math.max(a,b):Int;
  
  if ((a >= 10 && a <= 20) && (b >= 10 && b <= 20))
  {
  max=Math.max(a,b)
  return max ;
  }
  
  if ((a <= 10 && a >= 20) || (b <= 10 && b >= 20))
  {
  return 0;
  }
						   }	