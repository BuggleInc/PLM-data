while (!estFaceMur()) {
  setDirection(Direction.EST)
  while ((!estSurBiscuit()) || (!estFaceMur())) {
    avance()
  }
  prendBiscuit()
  setDirection(Direction.OUEST)
  while (!estSurBiscuit()) {
    avance()
  }
  recule()
  poseBiscuit()
  retourne()
}