def gnomeSort() { val n = getNombreValeurs()
                 var k = 0
                 while (k < n ) { if (plusPetit(k,k+1)) { k=k+1 }
                                 else if (k !=0) { echange(k,k+1)
                                                 k=k-1}
                                
                                
                                
                                }
                 
}