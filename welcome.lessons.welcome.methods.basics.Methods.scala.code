def goAndGet() {
  var pas=0;
  while (!estSurBiscuit()) {
    avance();
    pas=pas+1;
  }
  prendBiscuit();
  recule(pas);  
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}