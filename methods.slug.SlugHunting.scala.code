def hunt() {
  while(!estSurBiscuit()) {  
  	if (isFacingTrail()==true) {
    	avance()
    	}
  	else {
      gauche()
    	if (isFacingTrail()==true) {
      	avance()
      else {
        droite();droite()
        avance();;
      }
    }
    }
}}

def isFacingTrail():Boolean = {
    
  if (estFaceMur) {
    return false
  }
  avance()
  if (getCouleurSol()==Color.green){
      recule()
      return true}
  recule
    return false
    
}