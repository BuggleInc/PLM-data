def hunt() {
	if (isFacingTrail==true) {
		baisseBrosse()
	}
	while (isFacingTrail==false) {
		leveBrosse()
		gauche()
		avance()
		
	}

}
def isFacingTrail():Boolean = {
	var estVert = false;
	if (!estFaceMur) {
		avance()

		if (getCouleurSol()==Color.green) {
			estVert = true;
		}
		recule()

	}
	return estVert;
}        