def cocktailSort() {
	var swapped=true
	{ for(i<-0 getNombreValeurs()-2) {
		if(plusPetit(i,i+1)){
			echange(i+1,i)
			}
	  for(i<-setNombreValeurs() to 0 by -1) {
	  	if(plusPetit(i,i+1) {
	  		echange(i+1,i)
	  				} 		
	  			}
	  		}
		}
	while 	
	}

