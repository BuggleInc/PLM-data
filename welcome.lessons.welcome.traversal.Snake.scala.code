override def run() {
	def final():Boolean = {
		if(estfaceMur  ) {
		getDirection()
		if (getDirection()==direction.EST){
			gauche() 
			if(estFaceMur) { droite(); return true }
			else { return false}
		}
		else { droite()
		if(estFaceMur) { droite();return true }
		else {return false}
		}
	}
}

else{return false}
}

while(final==false) {
	while(!estFaceMur){
		baisseBrosse();avance()
	}
	if(getDirection()==Dorection.EST
}
