override def run() {
  while(!estFaceMur){
      avance()
    }
  var virages = 0
  gauche()
  while(!estSurBiscuit){
    while (estFaceMur){
      gauche();
      virages = virages + 1
      }
    avance()
    droite() 
    virages = virages - 1
    while(!estFaceMur){
      avance();droite() 
      virages = virages - 1
    }
    if (getDirection == Direction.NORTH && virages == 0){
      while(!estFaceMur){
        avance()
      }
    }
  }
  prendBiscuit
}
