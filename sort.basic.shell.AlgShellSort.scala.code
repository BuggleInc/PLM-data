def shellSort()  {
  var ecart=((getNombreValeurs().toDouble)/2).toInt
  
   for(i<- 1 to getNombreValeurs()-1){
    var v = getValeur(i)
    var j=i
    while((j-ecart)>0 && getValeur(j-ecart)>v ){
 			copie(j-ecart,j)
      j=j-ecart
    }
    setValeur(j,v);
    ecart=(ecart.toDouble /2)to.Int;
	}
}