def insertionSort() {
  for (i <- 1 to getNombreValeurs()-1) {
    var v = getValeur(i)
    for (j <- 0 to i-1) {
      if (i-j > v) {
        copie(i-j,i)
        derniereCase=i-j
      }
      copie(v,i-j)
    }
  }
}