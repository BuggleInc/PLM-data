def keepHandOnSideWall(){
  if(getDirection()==Direction.NORTH && !estFaceMur()){
    avance();
    return
  }
  gauche();
  while(estFaceMur()){
    droite();
  }
  avance();
}

override def run() {
  while(!estSurBiscuit()){
    keepHandOnSideWall();
  }
  prendBiscuit();
}