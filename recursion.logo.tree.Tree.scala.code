def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if(steps==0) { return; }
  avance(length)
  droite(angle)
  tree(steps-1,length,angle,shrink*length)
  recule(length)
  gauche()
}