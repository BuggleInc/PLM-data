def hunt() {
   while(!estSurBiscuit){
      if(isFacingTrail){
        baisseBrosse ; avance ; leveBrosse ;
      }
      else gauche ;
    }
 	 prendBiscuit ;
}

def isFacingTrail():Boolean = {
  	var surLaTrace = false ;
    if(estFaceMur) return false;
  
  	avance ;
  	surLaTrace = (Color.green == getCouleurSol)
  	recule ;
  	return surLaTrace ;
}