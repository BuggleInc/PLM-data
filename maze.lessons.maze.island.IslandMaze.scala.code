override def run() {
var c:Int = 0
while(getY() != 0) {
	setDirection(Direction.NORTH)
	setDirection() match {
		case Direction.EST
	}
	avance()
}

while(!estSurBiscuit) {
		droite()
		while(estFaceMur) {
			gauche()
		}
		avance()
	}
	prendBiscuit()
}
