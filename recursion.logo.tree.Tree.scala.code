def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if (steps <= 0)
  {
    // TODO revenir ? la position initiale
  }
  else
  {
    current(steps)
   	avance(length)
    droite(angle)
    tree(3, length*shrink, angle,shrink)
    gauche(angle)
    gauche(angle)
    tree(3, length*shrink, angle,shrink)
    // TODO recule
  }
}