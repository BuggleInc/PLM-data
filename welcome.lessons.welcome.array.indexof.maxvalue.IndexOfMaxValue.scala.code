def indexOfMaxValue(nums:Array[Int]):Int = 
{
 var indice_valeur_max = 0
 
 for(i<-0 to (nums.length-1))
 {
 	if(nums(i)>nums(indice_valeur_max))
 	{
 		indice_valeur_max=i	 // Quand on fait " == " c'est une affectation de la droite vers la gauche
 	}
 }
 
return indice_valeur_max

}
