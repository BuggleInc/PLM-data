def selectionSort() {
  for (i<- 0 to getNombreValeurs()-2){
    var k = getValeur(i)
    for (j<- i+1 to getNombreValeurs()-2){
      
      if (plusPetit(j,k)){
        k = j
      } 
      
    }
    swap(k,i)
  }
}