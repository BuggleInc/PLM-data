override def run() {
  faireCoteGauche(Color.YELLOW)
}

def faireCoteGauche(c:Color) {
  gauche();avance();
  setCouleurBrosse(c)
  baisseBrosse()
	leveBrosse()
  recule();
  droite();
  avance();
  baisseBrosse()
	leveBrosse();
  recule(2); 
}

