def step(rule:Array[Char], colors:Array[Color]) {
var x=Color.white
for (i <-0 to colors.length-2){
	x==getCouleurSol();
	while(Array(i)!=x){
		
	}
	if (rule(i)=='R'){
		gauche()
	}
	else {
		droite();
	}
	ChangeColor(colors(i+1))
	avance();
}
}

def ChangeColor(c:Color){
	setCouleurBrosse(c);
	baisseBrosse();
	leveBrosse();
	
}
