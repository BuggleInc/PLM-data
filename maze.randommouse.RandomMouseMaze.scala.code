override def run() {
  var rng:Int = random3()
  while(!estSurBiscuit){
    if (rng == 0){
      if (!estFaceMur){
        avance()
      } else if (rng == 1){
        droite()
      } else {
        gauche()
      }
    }
    rng = random3()
  }
  prendBiscuit()
}