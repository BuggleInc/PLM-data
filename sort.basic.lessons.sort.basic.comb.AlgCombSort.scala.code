def combSort() {
  var ecart:Int = getNombreValeurs()
  var inv:Boolean = false
  do{
    if (ecart>1){
      ecart = (ecart.asInstanceOf[Double] / 1.3).asInstanceOf[Int]
    }
    var i:Int =0
    inv=true
    while (i + ecart < getNombreValeurs()){
      if (plusPetit(i + ecart,i)){echange(i+ecart,i);inv=false}
      i+=1
    }
  }while(ecart > 1 || inv)
}