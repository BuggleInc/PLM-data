def polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double) {
if (levels==1)
{for(i<-1 to sides) {
	avance(length)
	droite(360/sides)
		}
}


else {
	for(j<-1 to sides)
	{
	avance(length)
	gauche(360-(360/sides))
	polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double)
	droite(180)
	}

}

}
