var int i= 0
def bubbleSort()  {
	var faitechange:Boolean=true
while(faitechange==true)
{
faitechange=false
  for(i<-0 to getNombreValeurs()-2)
   if plusPetit(i+1,i)
    {echange(i+1,i)
     faitechange=true}}
}
