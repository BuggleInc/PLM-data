def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {	
	
	if(steps !=0){
		var x = increment
		var longueur = length + x
		var pas=steps-1
		
		gauche(angle)
	//	if (steps>1){
			avance(longueur)
	//	}
		spiral(pas, angle, longueur, increment)
	}
}

