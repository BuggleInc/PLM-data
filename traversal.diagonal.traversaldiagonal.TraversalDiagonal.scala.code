var diag = 0;
override def run() {
  var max = (getMondeHauteur*getMondeLargeur) - 1;
  var cpt = 0;
  
  while(cpt <= max){
    ecritMessage(cpt);
    
    if(getY == 0){
      diag += 1;
      setPos(0, diag);
    } else setPos(getX + 1, getY - 1);
    
  }
}

