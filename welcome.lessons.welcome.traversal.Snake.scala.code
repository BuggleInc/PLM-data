override def run() {
	baisseBrosse()
  while (pos() == false){
    serpent()
  }
  leveBrosse
}

def serpent(){
    if (!estFaceMur){
      	avance()
    	}
    if (estFaceMur){
      if (getDirection() == Direction.EST){
        gauche()
        avance()
        gauche()
      }
      if (getDirection() == Direction.OUEST){
        droite()
        avance()
        droite()
      }
    }
}

def pos(): Boolean ={ 
  if (estFaceMur){
    if (getDirection() == Direction.EST){
      gauche()
      if (estFaceMur()){
      	droite()
        return true
      }
    }
    if (getDirection() == Direction.OUEST){
      droite()
      if (estFaceMur()){
       	gauche()
        return true
      }
    }
    else {
      return false
    }
  }
  else{
    return false
  }
}