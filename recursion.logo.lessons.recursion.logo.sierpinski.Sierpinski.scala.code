def sierpinski(level:Int, length:Double) {
  if (level==0){
    avance(length)
    return
  }
  baisseCrayon()
  sierpinski(level-1,length/2)
  droite(120)
  sierpinski(level-1,length/2)
  droite(120)
  sierpinski(level-1,length/2)
  droite(120)
  avance(length)
  leveCrayon()
}
