override def run() {
  
  while(!estSurBiscuit()){
   var dir=getDirection()
   setDirection(Direction.NORTH)
    if (!estFaceMur()){
      avance()
    }
    else{
      setDirection(dir)   
    	keepHandOnSideWall()
    }
  }
  prendBiscuit()
}


def keepHandOnSideWall(){
 // gauche()
  if (estFaceMur()){
    droite()
    if (estFaceMur()){
      droite()
      if (estFaceMur())
      	droite()
    }
  avance()
  }
  else{
    avance()
  }
}