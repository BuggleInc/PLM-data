def haveBaggle():Boolean = {
  var vuBaggle = false
  for(i<-1 to 6) {
    if(estSurBiscuit()) {
      vuBaggle = true
      avance()
    }
    else {
      avance()
    }
  }
  for(i<-1 to 6) {
    recule()
  }
  return vuBaggle
}