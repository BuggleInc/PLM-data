override def run() {override def run() {
  for (i<-1 to 4) {
  avance(30)
  droite(30)
  avance(30)
  droite(120)
  avance(30)
  droite(120)
  avance(30)
	droite(180)
  avance(30)
  droite(90)
  avance(30)
  droite(90)
  avance(30)
  droite(90)
  droite(90)
  leveCrayon()
  avance(50)
  gauche(90)
  baisseCrayon()
  }
  
  leveCrayon()
  gauche(90)
  avance(200)
  droite(90)
  avance(75)
  baisseCrayon()
}
}
