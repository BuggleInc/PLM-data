override def run() {
  while(!estSurBiscuit()){
    keepHandOnSideWall()
  }
  prendBiscuit()
}


def keepHandOnSideWall(){
  gauche()
  if (estFaceMur()){
    droite()
    if (estFaceMur()){
      droite()
      if (estFaceMur())
      	droite()
    }
  avance()
  }
  else{
    avance()
  }
}
