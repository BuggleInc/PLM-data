color = getCouleurSol()
def isFacingTrail(color):
       if estFaceMur():
            return False
       if coul == color:
           return True

def hunt(color):
    if estSurBiscuit():
        prendBiscuit()
    elif isFacingTrail(color) == True:
        baisseBrosse()
        avance()
        leveBrosse()
    else:
        left()
        
forward()

backward()
while not porteBiscuit():
    hunt(color)