def positionFinale():Boolean = {
		if ((estFaceMur()) && (getDirection == Direction.EST)){
			gauche()
			if (estFaceMur()){
				droite()
				return true
			} else { return false gauche() avance() gauch() } 
		} 
		if ((estFaceMur()) && (getDirection == Direction.EST)){
		return false
		droite() avance() droite()
	}
override def run() {
baisseBrosse()
	while (positionFinale() == false){
		while (!estFaceMur()){
				avance() 
		}
  	}
}
