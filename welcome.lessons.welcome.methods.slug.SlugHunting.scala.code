def hunt() {
  while (!estSurBiscuit()) {
    
    // On baisse la brosse pour effacer les traces
    baisseBrosse()
    
    if (isFacingTrail())
    	avance()
    else {
      
    }
  }
}

def isFacingTrail():Boolean = {
  if (estFaceMur())
  	return false
  
  // On avance d'une case
  avance()
  
  var result:Boolean = (getCouleurSol() == Color.green)
  
  recule()
  
  return result
}
