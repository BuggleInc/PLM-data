def posNeg(a:Int, b:Int, negative:Boolean):Boolean = {
  (negative,a,b) match {
    case (false,<0, >0) => return true
    case (false, >0,<0) => return true
    case (false,<0,<0) => return true
    case _ => return false
  }
}