def max1020(a:Int, b:Int):Int = {
	
def condition(x:Int):Boolean=
{return (x >= 10 && x <= 20)}

if (!condition (a) && !condition (b)) {return 0}
 else if (condition (a) && !condition (b)) {return a}
else if (!condition (a) && condition (b)) {return b}
else {return Math.max(a,b)}
}