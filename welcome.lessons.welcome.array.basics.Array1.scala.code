override def run() { def analyseColor() = {var T = new Array[Color](getMondeHauteur())
                                          for (i <- 0 to T.length-1) { T.(i) = getColor()
                                                                 		avance()}
                                          }
                    def reproduire() = {}
                    analyseColor()
}