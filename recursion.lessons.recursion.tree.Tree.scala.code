def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {

droite(angle);
avance(length);
recule(length);

droite(angle);
avance(length);
recule(length);



tree(steps-1, length*shrink, angle:Double, shrink)



}
