def insertionSort() {
  var length : Int = getValueCount();

  
  for(i<-1 to length-1)
  {
    var v = getValue(i);
    var j=i;
    while(j>0 && !isSmallerThan(j-1,v))
    {
      swap(j-1,j);
      j-=1;
    }
    setValue(j,v);
  }
}