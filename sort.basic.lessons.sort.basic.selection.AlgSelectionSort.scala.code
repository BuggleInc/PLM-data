def selectionSort() {
  var min = 0
  for (i <- 0 to getNombreValeurs()-1) {
    min=i
    for (j <- i to getNombreValeurs()-1) {
      if (getValeur(j)<getValeur(min)) {
        min=j
      }
    }
    echange(i,min)
  }
}