def teenSum(a:Int, b:Int):Int = 
{
/*Étant donné deux nombres entiers (a et b), renvoyez leur somme. 
 Cependant, les valeurs ados (dans l'intervalle [13;19]) 
 sont particulièrement chanceuses. Donc, si l'un des nombres est ado, renvoyez simplement 19. */

 (a,b) match
 {
 	case(a,b) if ( (a>12 && a<20) || (b>12 && b<20) ) => return 19
 	case _ 									=> return a+b
 }
}