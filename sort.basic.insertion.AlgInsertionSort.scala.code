def insertionSort()
{
  var n = getNombreValeurs()-1;
  var min = 0;
  
  for (i <- 0 to n)
  {
    min = 0;
    for (j <- i to n)
    {
      if (plusPetit(j,min)) min = j;
    }
    echange(i, min);
  }
}