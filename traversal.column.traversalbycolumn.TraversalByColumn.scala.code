override def run() {
  var n = 0
  ecritMessage("0")
  while (!positionFinale())
      {if (getY()==6)
           {setPos(getX()+1,0);}
       else
           {setPos(getX(),getY()+1);}
       n+=1;
       ecritMessage(n)
       
        
        
        
        
        
        
        
      }
}








def positionFinale():Boolean= {
  if ((getMondeHauteur(),getMondeLargeur())==(getX(),getY()))
      {return(true);};
  return(false);}