def bubbleSort3() {
  var fini = false;
  var n = 2 ;
  for(j<- 0 to getNombreValeurs()-2 ){
    fini = true;
    for(i<- 0 to getNombreValeurs() - n){
      if (!plusPetit(i,i+1)){
        echange(i,i+1);
        fini = false
      }
    }
    if(!fini)
			return    	
    n += 1;
  } 
}