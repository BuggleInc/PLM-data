def bubbleSort()  {
  var tri:Boolean = true
  while (tri) {
    tri=false
  for(i<-0 to getNombreValeurs()-2) {
      if(getValeur(i+1)<getValeur(i)) {
        echange(i,i+1)
        tri=true
      }
    }
  }
}