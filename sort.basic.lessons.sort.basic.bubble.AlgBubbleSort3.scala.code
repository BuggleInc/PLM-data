def bubbleSort3() {
  var trie = false
  var c:Int = 0
  for (j <- getNombreValeurs()-1 to 0 by -1){
    trie = true
    for (i <-0 to j-1){
    
      if (getValeur(i) > getValeur(i+1)){
        echange(i,i+1)
        trie = false

      }
    }
    if (trie) return
  }
  
}
