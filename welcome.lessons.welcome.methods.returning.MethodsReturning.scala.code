
def testcol():Boolean= {
	for  (x<-0 to 6) {
		if (estSurBiscuit) {
			setY(7);
			return true;
		}
	else {return false};
	avance();
	}
}

def haveBaggle():Boolean = {
	
	avance();
	/*for(x<-0 to 6) {
		testcol();
		writeMessage(testcol());
		
		droite();
		avance();
		gauche();
	} */
}