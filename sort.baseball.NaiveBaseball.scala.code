var mem = 0
var pos1 = 0
var pos2 = 0
override def run() {
  while(!estTrie) {
    mem=getTrouBase()+1
    while (mem>4) {
      mem=mem%4
    }
    pos1 = getCouleurJoueur(mem,0)-mem
    pos2 = getCouleurJoueur(mem,1)-mem
    if (pos1 < pos2) {
      deplace(mem,0)
    }
  }
}