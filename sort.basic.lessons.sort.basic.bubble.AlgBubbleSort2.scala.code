def bubbleSort2()
{
  var stop = false
  var max = getNombreValeurs() - 2
 
  while (!stop)
  {
    stop = true
    for (i <- 0 to max)
    {
      if (plusPetit(i + 1, i))
      {
    		echange(i, i + 1)
        stop = false
      }
    }
    max += 1
  }
}