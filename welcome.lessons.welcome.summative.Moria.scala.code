while ( !estFaceMur() || estSurBiscuit() ){
  retourne();
  while ( !estSurBiscuit() && !estFaceMur() ){
    avance();
  }
  prendBiscuit();
  retourne();
  while ( !estSurBiscuit() && !estFaceMur() ){
    avance();
  }
  recule();
  poseBiscuit();
  recule();
  if ( estFaceMur() && !estSurBiscuit() ){
    droite();
    avance();
    gauche();
    avance();
  }
}
