override def run():Unit= {
  for (i<- 0 to 7){
    petale()
  }
}

def v(){
  avance(30)
  gauche(90)
  avance(30)
}

def v2(){
  avance(30)
  droite(90)
  avance(30)
}

def petale(){
  v()
  droite(45)
  v()
  
  droite(90)
  avance(30)
  droite(90)
  avance(30)
  gauche(45)
  
  v2()
  gauche(45)
  v2()
  
  gauche(45)
  avance(30)
  droite(90)
  avance(30)
  droite(90)
  
  v()
  droite(45)
  v()
}