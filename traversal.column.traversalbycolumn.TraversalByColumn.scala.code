override def run() {
  var c=0;
  var x=0
  var y=1;
  while (readMessage()!="48"){
    for (i<- 1 to 7){
      writeMessage(c);
      y= y+1;
      setPos(x,y);
      c=c+1
    }
    y=1;
    x=x+1;
    setPos(x,y);

    
  }
}