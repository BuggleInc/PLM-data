def bubbleSort()  {
  var modif = true;
  while(modif)
  	modif = traverse();
}


def traverse() : Boolean = {
   var swapped = false;
   for (i <- 0 to getValueCount() - 2){
     if(isSmaller(i, i+1)){
   		 swap(i+1,i);
       swapped = true;
	   }
   }
   return swapped;
   
}