def isFacingTrail():
   # write your code here
    forward()
    if estFaceMur()==1:
        backward()
        return 0
    if estDosMur()==1:
        backward()
        return 0
    if getCouleurSol()==Color.green:
        backward()
        return 1
    backward()
    return 0

forward()