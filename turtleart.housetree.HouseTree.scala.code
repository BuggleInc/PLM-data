override def run() {
  for (house <- 0 to 3){
  	for (round <- 0 to 4){
  	  forward(30)
 	   right(90)
	  }
	  left(60)
 	 for (rounds <- 0 to 1){
 	   forward(30)
  	  right(120)
 	 }
    brushUp
    right(90)
 	 backward(30)
    right(90)
    forward(20)
    left(90)
    brushDown
}
  }
