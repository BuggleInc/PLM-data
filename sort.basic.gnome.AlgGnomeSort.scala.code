def gnomeSort() {
  
  var n : Int =getValueCount()
  
  for(it<-1 to n-1){
    var element : Int = it
    
    while(element >0 && isSmaller(element-1,element)){
      element-=1
    }
    swap(it,element+1)
  }
}