def haveBaggle():Boolean = {
  var seenBaggle:Boolean = false
  
  for (stepper1 <- 1 to 6) {
    if (isOverBaggle()) {
      seenBaggle = true
    }
    
    forward();
  }
  
  for (stepper2 <- 1 to 6) {
    backward()
  }
  
  return seenBaggle;
}
