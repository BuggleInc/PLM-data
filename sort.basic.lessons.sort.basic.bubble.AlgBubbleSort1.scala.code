def bubbleSort()  {
  val n = getNombreValeurs()
  var swap:Boolean = false
  while (!swap) {
    swap = false
    for (i <- 0 to n-2) {
    	if (plusPetitQue(i+1,i)) {
     		echange(i-1,i);
     		swap = true
    	}
  	}
  }
}