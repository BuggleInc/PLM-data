def haveBaggle():Boolean = {
  var vuBiscuit : Boolean = false ;
  for (ligne <- 1 to 6) {if (estSurBiscuit) {vuBiscuit = true} ;
  avance() ;
  };
  for (ligne <- 1 to 6) {recule();};
  return vuBiscuit;
}