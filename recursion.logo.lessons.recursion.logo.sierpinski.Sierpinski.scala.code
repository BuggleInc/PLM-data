def sierpinski(level:Int, length:Double) {
  if(level == 0){
    avance(length)
    droite(120.0)
    avance(length)
    droite(120.0)
    avance(length)
    droite(120.0)
  }else{
    sierpinski(level-1, length/2.0)
    avance(length)
    droite(120.0)
    sierpinski(level-1,length/2.0)
  }
}