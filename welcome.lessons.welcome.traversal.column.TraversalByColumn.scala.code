override def run() {
  var cpt:Int =0
  var x:Int = 0
  var y:Int =0
  while(!estFinal) {
    cpt = cpt+1;
    if (!estFaceMur) {
      setPos(x,y)
      y=y+1
      ecritMessage(cpt)
    } else {
      y=y+1
      setPos(0,y)
      ecritMessage(cpt)
    }    
    
  }
}

def estFinal () :Boolean={
  if (getX()==getMondeLargeur() && getY==getMondeHauteur()) {
    return true
  } else return false
}