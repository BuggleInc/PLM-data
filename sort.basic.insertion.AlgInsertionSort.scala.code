def insertionSort() {
  var ok = false
  var v = 0
  var k = 0
  for(i<- 0 to getNombreValeurs()) {
    v=getValeur(i)
    k=0
    ok=false
    while(ok==false) {
     if(plusPetit(i+k-1,i+k)) {
        ok= true
      }
      else {
      copie(i-1+k, i+k)
        setValeur(i+k-1,v)
      
    }
      k=k+1
  }
}
}