def bubbleSort2() {
  var temp:Int =0;
  var nbDeTour:Int =0;
	for(i <- 0 to (getNombreValeurs()-(1+nbDeTour))){
    nbDeTour=nbDeTour+1;
  	for(j <- getNombreValeurs()-2 to 0 by -1){
  	  	if(getValeur(j)>getValeur(j+1)){
        	temp=getValeur(j);
      	  setValeur(j,getValeur(j+1));
      	  setValeur(j+1,temp);
    	  }
  	}
  }

}