def island(num:Array[Int]): Int = {
  var Etage=0     
  for (k <- 1 to num.size by 2){   
    if (num(k) <= num(k+1)){    
      
      Etage = Etage + 1 
      
    }
    
  }
        return Etage
}