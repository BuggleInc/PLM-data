def selectionSort() {
  for(i<- 0 to getNombreValeurs()-2){
    var petit = 0
    for(o<- i to getNombreValeurs()-1){
      if(getValeur(o)<getValeur(i) && getValeur(o)<getValeur(petit)){
        petit=o
      }
    }
    echange(petit,i)
  }
}