def bubbleSort()  {
	  var x=getNombreValeurs();
	  var estTrie=false;
	  while (!estTrie) {
	  	estTrie=true;
	  	for (i<-0 to x-2) {
	  		if (!plusPetit(i, i+1)) {
	  			echange(i, i+1);
	  			estTrie=false;
	  		}
	  	}
	}
}
