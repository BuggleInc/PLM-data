def faireV(c:Color) {
  setCouleurBrosse(c)
  avance()
  baisseBrosse()
  leveBrosse()
  avance()
  gauche()
  avance()
  baisseBrosse()
  leveBrosse()
  recule()
  droite()
  avance()
  baisseBrosse()
  leveBrosse()
  avance()
  gauche()  
}

def deplace(a: Int) {
  if (a%3 == 1 || a%3 == 2 || a%3 == 3 || a%3 == 4 || a%3 == 5 || a%3 == 6 || a%3 == 7 || a%3 == 8) {
    avance(); avance(); avance(); avance(); avance() 
  } else {
    avance(); avance(); avance(); avance(); avance() 
    gauche()
    avance(); avance(); avance(); avance(); avance()
    droite()
  }
}

def motif() {
  faireV(Color.YELLOW)
  faireV(Color.RED)
  faireV(Color.BLUE)
  faireV(Color.GREEN)
}

override def run() {
  for (i <- 1 to 81) {
    motif()
    deplace(i)
  }
}