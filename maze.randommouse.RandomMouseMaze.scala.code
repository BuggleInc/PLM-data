override def run() {
  	var rand = 0
    while(!estSurBiscuit()) {
      rand = random3()
      if (rand == 0 && !estFaceMur()) {
        avance()
      } else if (rand == 1) {
        droite()
      } else {
        gauche()
      }
    }
  prendBiscuit()
}