def occurrences(nums:Array[Int],lookingFor:Int):Int = {
  var occu:Int= 0
  var len:Int=nums.length-1
  for (i <- 0 to len ) {
    if ( lookingFor== nums(i) ) {
      occu=occu+1
    }
  }
  return occu
}
