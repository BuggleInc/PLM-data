def haveBaggle():Boolean = {
  var seenBaggle=false;
  for (i <- 1 to 6) {
    if (isOverBaggle) {
      seenBaggle=true;
    }
    for (j <- 1 to 6) {
      forward();
      if (isOverBaggle) {
      seenBaggle=true;
      }
    }
    for ( h <- 1 to 6) {
      backward();
    }
    if (seenBaggle) {
      return seenBaggle
    }
  }
  return seenBaggle
}