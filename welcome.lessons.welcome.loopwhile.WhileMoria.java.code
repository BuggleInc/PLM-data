public void run() {	
	while (isOverBaggle() == false) {
		setDirection(Direction.EAST);
		forward();
		while (isOverBaggle() == isCarryingBaggle()) 
		pickupBaggle();
		setDirection(Direction.WEST);
		forward(); 
		while (isOverBaggle() == isCarryingBaggle() == true)
		dropBaggle();								
						
					}
						}