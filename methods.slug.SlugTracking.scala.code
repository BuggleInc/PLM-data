def isFacingTrail():Boolean = {
			forward()
   		if(isFacingWall() == true){
      return false
      }
  
      else if (getGroundColor() == Color.green){
      return true
    }
    
  	else {
    backward()
    left()
      return false
  }
}