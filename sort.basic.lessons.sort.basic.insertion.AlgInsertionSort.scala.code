def insertionSort() {
	for (i <- 1 to getNombreValeurs() - 1) {
		var valeur = getValeur(i)
		var j = i - 1
		while (j >= 0 && plusPetit(valeur,j)) {
			copie(j,j+1)
			j = j - 1
		}
		setValeur(valeur,j)
	}
}
