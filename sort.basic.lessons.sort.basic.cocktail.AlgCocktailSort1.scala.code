def cocktailSort() {
  var a=true
  for (i <- getValueCount() to 0 by -1){
    if (a==false){
      return
    }
    a=false
        for (j <- 1 to i-1){
    if (isSmaller(j,j+1)){
      swap(j-1,j)
		}
}
  for (j <- 1 to i-1){
    if (!isSmaller(j,j+1)){
      swap(j,j+1)
      a=true
		}
}
}
}