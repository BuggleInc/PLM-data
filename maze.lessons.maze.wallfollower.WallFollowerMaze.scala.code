override def run() {
  while(!estSurBiscuit){
    while(!estFaceMur() && !estSurBiscuit()){
      MainAuMur();
    }
    while(estFaceMur() && !estSurBiscuit()){
      left();
    }
    if (!estSurBiscuit()){
      avance();
    }
  }
  
  prendBiscuit();
}

def MainAuMur(){
  left();
  if (estFaceMur()){
    right();
    avance();
  } else {
    avance();
  }
  
}