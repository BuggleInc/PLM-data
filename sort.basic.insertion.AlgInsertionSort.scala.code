def insertionSort() {
  for(i<-1 to getNombreValeurs()-1)
  	{
      var k = i
      var v = getValeur(i)
      
      while((k>0)&&(!plusPetitQue(k-1,v)))
      	{
					copie(k-1,k) ;
          k=k-1
        }
      setValeur(k, v)
    }
}