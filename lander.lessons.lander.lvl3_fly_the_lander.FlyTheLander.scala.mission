<html><head>  <style type="text/css">
    body { font-family: tahoma, "Times New Roman", serif; font-size:10px; margin:10px; }
    code { background:#EEEEEE; }
    pre { background: #EEEEEE;
          margin: 5px;
          padding: 6px;
          border: 1px inset;
          width: 640px;
          overflow: auto;
          text-align: left;
          font-family: "Courrier New", "Courrier", monospace; }
   .comment { background:#EEEEEE;
              font-family: "Times New Roman", serif;
              color:#00AA00;
              font-style: italic; }
  </style>
</head><body>
<!-- Please don't translate this file but l10n/missions/plm.pot (see https://github.com/oster/PLM/wiki/Working-on-translations) -->
<h1>Aux commandes du module</h1>

<p>Apr?s 30 jours ? regarder des cailloux en mangeant des c?r?ales, vous
d?cidez qu'il est temps d'aller explorer d'autres coins de cette plan?te.</p>

<p>Vous volez un moment, avant de r?aliser soudain que vous ?tes presque ?
court d'essence. Le pilote automatique est malheureusement inutile dans ce
genre de situation p?rilleuse, et vous allez devoir vous poser en mode
manuel. Votre mission et de vous <b>poser sur une zone plate, avec une
vitesse verticale inf?rieure ? 10 m/s, une vitesse horizontale inf?rieure ?
5 m/s et un angle de 0Åã</b>.</p>

<p>Vous devez d?finir deux fonctions : <code>initialize()</code> et <code>step()</code>. <code>initialize()</code> est appel?e au moment o? l'on
appuie sur le bouton Å·Ex?cuterÅ‚. Vous pouvez l'utiliser pour choisir une
zone plate. Comme avant, <code>step()</code> est appel?e tous les dixi?mes
de seconde pour vous permettre d'ajuster la pouss?e (avec la fonction
<code>setPousseedesiree(poussee:Integer)</code>). Vous pouvez ?galement contr?ler
l'angle du module avec la fonction <code>setAngleDesire(angle:Double)</code>. Un
angle de OÅã d?note une position verticale, avec le sommet du module dirig?
vers le haut. C'est l'angle avec lequel vous devez vous poser. Un angle
positif indique que le module penche vers la gauche tandis qu'un angle
n?gatif indique au contraire qu'il penche ? droite. Il est impossible de se
pencher ? plus de Å}90Åã, et on ne peut pas pencher le module de plus de Å}5Åã
en une fois.</p>

<p>Comme dans les exercices pr?c?dents, <code>getSol():List[Point]</code> retourne les points qui
composent l'horizon. Le manuel de r?f?rence de votre module est disponible
dans la documentation (Aide/? propos de ce monde).</p>

<p>Comme d'habitude, votre programme doit se poser sans heurt dans toutes les
situations que nous avons rencontr? jusqu'? pr?sent, ainsi que dans ce
nouveau cas.</p>

<p>Bonne chance. Essayez de ne pas vous scratcher ;)</p>
</body></html>