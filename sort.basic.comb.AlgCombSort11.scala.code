def combSort11() {
  var ecart:Int = getNombreValeurs()
  var fini:Boolean=false
  var nop:Int=0
  
  do{
    if(ecart>1){
      ecart = (ecart.toDouble / 1.3).toInt}
    if(ecart==9 || ecart ==10){
      ecart=11
    }
    nop=0
    fini=false
    while(nop+ecart<getNombreValeurs()){
      if(plusPetit(nop+ecart,nop)){
        echange(nop,nop+ecart)
        fini=true
      }
      nop+=1
    }
  }
    while(ecart>1 || fini==true)
    }