def insertionSort() {
  for(i <- 0 to getNombreValeurs()-1){
    val v= getValeur(i)
    var j = i
    while(j>0 && !plusPetitQue(j-1, v)){
      copie(j-1,i)
      j-=1
    }
    setValeur(j, v)
  }
}