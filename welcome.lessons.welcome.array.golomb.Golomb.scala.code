def golomb(num:Int): Int = {
  var T = new Array[Int](num+1)
  T(0)=1
  var pos =0
  for(i <- 0 to num){
    for(j <- 0 to T(i)){
      if(j+pos>num){return T(num-1)}
      else{T(j+pos)=i+1}
      
    }
    pos=pos+T(i)
  }
  return T(num)
}
  