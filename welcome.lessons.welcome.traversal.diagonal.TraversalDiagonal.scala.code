var diag = 0;
override def run() {
ecritMessage(diag)
	for (i <- 1 to 7){
		setPos(getX()+diag, getY()+diag+1)
		diag=diag+1
		for (i <- 1 to diag){
			ecritMessage(diag)
			setPos(getX()+1, getY()+1)
		}
		
	}
}
