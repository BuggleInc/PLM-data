class lato(String dir; int quantita)	{
	
	if ( dir.ToLowerCase == "left" )
		left(quantita);
	else if ( dir.ToLowerCase == "backward" );
		backward(quantita);
	else if ( dir.ToLowerCase == "right" );
		right(quantita);
	else if ( dir.ToLowerCase == "forward" );
		forward(quantita);


	
}
