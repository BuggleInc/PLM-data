override def run() {
  for (house <- 1 to 4){
  avance(30);
  right(30);
  for (i <- 1 to 3){
    avance(30); right(120)
  }
  right(60);
  for (j <- 1 to 3){
    avance(30); droite(90)
  }
  setPos(getX()+20,getY()) 
  }
}