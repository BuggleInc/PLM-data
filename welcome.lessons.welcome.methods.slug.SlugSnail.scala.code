 def hunt(c:Color) {
 	def isFacingTrail():Boolean = {
	var solEstOrange:Boolean = false;
	var solEstPink:Boolean = false;
	if(!estFaceMur) {
		avance();
		if(getCouleurSol() == Color.orange) {
			solEstOrange = true;
		if(getCouleurSol() == Color.pink) {
			solEstPink = true
		}
		}
		recule();
	}
	return solEstOrange;
	return solEstPink
    // Write your code here
}        


def hunt() {
while(!estSurBiscuit) {
	
	if (isFacingTrail)
	{
		avance()
		baisseBrosse()
		leveBrosse()
	} 
	else 
	{
		gauche()
		if(!isFacingTrail) 
			{
				droite()
				droite()
			}
	}
}
prendBiscuit()
}
     // Write your code here
 }

 // here comes your isFacingTrail method   
