def insertionSort() {
	for (i <- 1 to getNombreValeurs()-1) {
		var v:Int = getValeur(i)
		var j = i-1
		while (j>0 && getValeur(j)>v) {
			 setValeur(j,getValeur(j -1));
			 j=j-1;
		}
		setValeur(j,v)
	}
}
