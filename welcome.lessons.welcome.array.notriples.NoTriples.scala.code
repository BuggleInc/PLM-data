def noTriples(nums:Array[Int]): Boolean = {
	var T = new Array[Int] (nums.length);
	for (i <- 0 to T.length-1) {
		T(i)=0;
	}
	for (i <- 0 to nums.length-1) {
		T(nums(i))=T(nums(i))+1;
	}
	for (i <- 0 to nums.length-1) {
		if (T(i)>=3) {
			return true;
		}
	}
	return false;
}
