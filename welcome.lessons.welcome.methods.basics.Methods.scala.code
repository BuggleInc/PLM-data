        def goAndGet() {
        	for (a<-1 to 6) {avance(); if(estSurBiscuit()) {prendBiscuit();)}}
          for(b<-1 to 6) {recule(); if (estFaceMur()) {poseBiscuit();}}
        }

        for (i <- 1 to 7) {
            goAndGet();
            right();
            forward();
            left();
        }
