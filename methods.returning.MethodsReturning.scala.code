def haveBaggle():Boolean = {
  
  var seenBaggle:Boolean = false

  for (k <- 1 to 6){
	  if(isOverBaggle()){
   	 seenBaggle = true
 	 }
    forward()
	}
  
  for (i <- 1 to 6){
    backward()
  }
  
  return(seenBaggle)
}