def posNeg(a:Int, b:Int, negative:Boolean):Boolean = {
  if ((a*b)<0 && !negative) {
    return true
  } else if (negative){
    	if (a<0 && b<0) {
        return true
      } else {
        return false
      }
  } else {
    return false
  }
}