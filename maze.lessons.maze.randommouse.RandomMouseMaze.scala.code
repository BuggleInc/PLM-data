override def run() {
  while (!estSurBiscuit()){
    if (!estFaceMur){
    var rand = random3()
    rand match{
      case 0 => avance()
      case 1 => droite()
      case 2 => gauche()
    }
  } else { gauche()}
  prendBiscuit()
    // Your code here 
  }}