def bubbleSort2()  {
  var swapped = false;
  
  do {
    swapped = false;
    for (i <- getValueCount()-2 to 0) {
      for (j <- 0 to i) {
        if (isSmaller(j+1, j)) {
          swap(j, j+1);
        }
      }
    }
    
  } while (swapped);
}