var nbBaggle = 0

while (!sortieTrouvee()) {
  nbBaggle = 0
do  {
    avance()
    if (estSurBiscuit()) {
      nbBaggle = nbBaggle + 1
    }
  } while (!crossing())
  if (nbBaggle >= 3) {
    gauche()
  } else {
    droite()
  }
}

if (sortieTrouvee()) {
  avance()
}