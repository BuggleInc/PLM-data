def in3050(a:Int, b:Int):Boolean = {
  def in3040(a:Int):Boolean={
    return (a>=30 && a<=40)
  }
  def in4050(a:Int):Boolean={
    return (a>=40 && a<=50)
  }
  (in3040(a)&&in3040(b) || in4050(a)&&in4050(b))
}