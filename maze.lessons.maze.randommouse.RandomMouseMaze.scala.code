override def run() {
  var x:Int = 0;
  while ( !estSurBiscuit() ){
    x = random3();
    if ( x == 0 && !estFaceMur() ){
      avance();
    } else if ( x == 1 ){
      gauche();
    } else {
      droite();
    }
  }
  prendBiscuit();
}
