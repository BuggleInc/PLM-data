def hunt(c:Color) {
   while(!estSurBiscuit){
      if(isFacingTrail(c)){
        baisseBrosse ; avance ; leveBrosse ;
      }
      else gauche ;
    }
 	 prendBiscuit ;
 }



def isFacingTrail(c:Color):Boolean = {
  	var surLaTrace = false ;
    if(estFaceMur) return false;
  
  	avance ;
  	surLaTrace = (c == getCouleurSol)
  	recule ;
  	return surLaTrace ;
}