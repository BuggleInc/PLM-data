def solve() {
  val lgr:Int = getTaillePile()
  var i:Int = 0
  while(!estTriee()) {
    var largeur:Int = getRayonCrepe(i)
    var k:Int = 0
    while(getRayonCrepe(k)<largeur) {
      k += 1
    }
    retourne(1)
    retourne(k+1)
    i += 1
  }
}