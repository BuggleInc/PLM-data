def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
if(steps==0){
	
}
else {
	
	avance(length)
	droite(angle)
	current(steps)
	tree(steps-1,shrink*length,angle,shrink-1)
	gauche(angle)
	avance(length)
	tree(steps,length,angle,shrink)
	recule(steps+1)
	
	
	

}
}
	
