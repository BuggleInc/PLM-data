def nearHundred(n:Int): Boolean = {
  (n) match {
    case (Math.abs(n-100)>=10) => return false
    case (Math.abs(n-200)>=10) => return false
    case (Math.abs(n-100)<10) => return true
    case (Math.abs(n-100)<10) => return false
    case(_) => return false
  }