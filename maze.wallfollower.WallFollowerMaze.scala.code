override def run() {
  while(!estSurBiscuit()) {
		droite()
    if(estFaceMur()) {
    	gauche()
    	if(!estFaceMur())
      	avance()
      else
      	gauche()
  	} else
    	avance()
  }
  prendBiscuit()
}