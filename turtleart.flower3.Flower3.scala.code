override def run():Unit= {
  for (i<-1 to 8) {
    droite(45)
    avance(20) //1
    gauche(90)
    avance(20) //2
    droite(45)
    avance(20) //3
    droite(90)
    avance(20) //4
    gauche(90)
    avance(20) //5
    gauche(90)
    avance(20) //6
    avance(20) //7
    gauche(90)
    avance(20) //8
    gauche(90)
    avance(20) //9
    droite(90)
    avance(20) //10
    droite(45)
    avance(20) //11
    gauche(90)
    avance(20) //12
    gauche(45)
  }
}