premiereFois = True
while premiereFois or not croisement() :
    premiereFois = False
    while sortieTrouvee() == False :
        b=0
        for i in range (4):
            forward()
            if estSurBiscuit() == True :
                b = b+1
            forward()
        if b <= 2 :
            droite()
        if b >= 3 :
            gauche()
    forward()