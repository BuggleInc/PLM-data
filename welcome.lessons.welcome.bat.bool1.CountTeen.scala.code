def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {
  var s:Int = 0
  if (ado(a)){s+=1}
  if (ado(b)){s+=1}
  if (ado(c)){s+=1}
  if (ado(d)){s+=1}
  return s
}
def ado(a:Int):Boolean ={
  return (13 <= a && a <= 19)
}
