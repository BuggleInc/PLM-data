def polygone(n:Int,x:Int) {
  for (i <- 1 to n) {
    avance(x);droite(360/n)
  }
}


override def run() {
  polygone(8,50);avance(25);droite(45)
  for (i <- 1 to 8) {
    avance(25);gauche(45);avance(25);droite(90)
  }
  avance(25);
  for(i <- 1 to 8) {
  droite(45);avance(25);droite(45);avance(25);recule(25);gauche(135);avance(25);droite(90)
  }
}