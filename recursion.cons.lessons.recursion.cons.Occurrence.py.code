def occurences(list, val):
    if list==[]:
        return 0
    if list[0]=!val:
        list=list[1:]
        return occurences(list, val)
    else :
        i=i+1
        list=list[1:]
        
        