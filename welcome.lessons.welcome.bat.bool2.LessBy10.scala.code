def lessBy10(a:Int, b:Int, c:Int):Boolean = {
  var d : Int = Math.abs(b-a)
  var e : Int = Math.abs(c-a)
  var f : Int = Math.abs(b-c)
  if (d>=10 || e>=10 || f>=10){
    return true
  }
  else{
    return false
  }
}
