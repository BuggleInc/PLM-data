def ado(n:Int):Boolean = {
	if(n<=19 && n>=13)
		return true
	else
		return false
}
def loneTeen(a:Int, b:Int):Boolean = {
	if(ado(a) | ado(b))
	{
		if(ado(a) && ado(b))
			return false
		else
			return true
	}
	else
		return false
}
