def insertionSort() {
  for(i<- 1 to getNombreValeurs()-1){
    var v = getValeur(i)
    for(j<- i-1 to 0 by -1){
      if(getValeur(j)>v){
        setValeur(j+1,getValeur(j))
      }
    }
    
  }
}