var diag = 0;
//var a : Int = 0
//var y : Int = 0
var counter : Int = 0;
var finalpos : Boolean = true;
override def run() {
    //while( diag <= 7){
      for(y <- 0 to 6){
        for (a <- 0 to y){
          setPos(a,y-a)
          writeMessage(counter)
          counter = counter + 1
          diag = diag + 1
        }
      //}
 
    }
  for (x<-0 to 5){
      setPos(x+1,6-x)
      writeMessage(counter)
      counter = counter + 1
    
  }
}