def getLandingZone():Segment = 
{	
	
	
	
	/*for(i <- 0 to getSol().length-1)
	{
		if(getSol()(i).x == getSol()(i+1).x )
			return Segment( Point(getSol()(i).x, getSol()(i).y), Point(getSol()(i+1).x, getSol()(i+1).y) )
	}*/
}
