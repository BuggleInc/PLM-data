def insertionSort() {
  var v=getValeur(0)
  for (i <- 1 to getNombreValeurs()-1){
    v=getValeur(i)
    for (j <- i-1 to 1 by -1) {
      if (getValeur(j)>v) {
        setValeur(j+1,getValeur(j))
      }
      else {setValeur(j,v)}
    }
  }
}