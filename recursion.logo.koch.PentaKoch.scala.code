def pentaKoch(levels:Int, length:Double) {
  pentagonSide(levels, length)
  gauche(108)
  pentagonSide(levels, length)
  droite(108)
  pentagonSide(levels, length)
  droite(108)
  pentagonSide(levels, length)
  droite(108)
  pentagonSide(levels, length)
  gauche(108)
}

def pentagonSide(levels:Int, length:Double) {
  if (levels > 0) {
    
  }
}