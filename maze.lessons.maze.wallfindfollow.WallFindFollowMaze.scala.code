override def run() {
  while(!estSurBiscuit){
    while(!estFaceMur() && !estSurBiscuit()){
      MainAuMur();
    }
    if(estFaceMur() && !estSurBiscuit()){
      left();
    }
    if(estFaceMur() && !estSurBiscuit()){
      retourne();
    }
    if(estFaceMur() && !estSurBiscuit()){
      right();
    }
    if (!estSurBiscuit()){
      avance();
    }
  }
  
  prendBiscuit();
}

def MainAuMur(){
  left();
  if (estFaceMur()){
    right();
    avance();
  } else {
    avance();
  }
  
}