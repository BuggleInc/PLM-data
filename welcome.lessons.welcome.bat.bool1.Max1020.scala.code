def max1020(a:Int, b:Int):Int = {
  var max:Int = Math.max(a, b)
  var min:Int = Math.min(a, b)
  
  if (max > 20 && min < 10)
		return 0
  else if (max > 20)
  	return min
  else if (min < 10)
  	return max
}