override def run() {
  while (!estSurBiscuit) {
    while (estFaceMur) {
      gauche()
    }
    if (keepHandOnSideWall && !estFaceMur) {
      avance()
    }
  }
  prendBiscuit()
}

def keepHandOnSideWall(): Boolean = {
  gauche()
  if (estFaceMur) {
    droite()
    return true
  }
  droite()
  return false
}