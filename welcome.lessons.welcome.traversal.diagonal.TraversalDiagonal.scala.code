override def run() {
	var cpt = 0;
	var L = getMondeHauteur();
	for (XY <- 0 to 2*L-1) {
		for (x <- 0 to XY) {
			if (x<L && XY-x<L) {
				setPos(x,XY-x)
				ecritMessage(cpt);
				cpt=cpt+1;
			}
			
		}
	}	
}
