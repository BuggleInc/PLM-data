def extrema(nums:Array[Int]): Int = {
var n = nums.length;
var valemax = nums(0);
var valemin = nums(0);

for (k <- 0 to n-1) {
	if (valemax < nums(k)) {
		valemax = nums(k);
	}
	if (valemin > nums(k)) {
		valemin = nums(k);
}
return (valemax-valemin);
}
