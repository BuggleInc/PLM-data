def hunt(c:Color) {
     while (!estSurBiscuit()) {
      if (isFacingTrail()) {
        avance()
        baisseBrosse()
        leveBrosse()
      } else {
        gauche()
      }
    }
  if (estSurBiscuit()) {
    prendBiscuit()
  }
 }

 // here comes your isFacingTrail method