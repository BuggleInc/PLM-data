def cocktailSort() {
	var ech = false;
	do {
		ech = false;
		for(i<-0 to getNombreValeurs()-2) {
			if(plusPetit(i+1, i)) {
				echange(i, i+1);
				ech = true;
			}
		}
		for(j<-getNombreValeurs()-2 to 0) {
			if(plusPetit(j+1, j)) {
				echange(j, j+1);
				ech = true;
			}
		}
	}while(ech)
}
