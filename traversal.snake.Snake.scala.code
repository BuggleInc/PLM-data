override def run() {
}

def positionFinale():Boolean{
  if (estFaceMur())
  return true
  if (getDirection()==Direction.EST)
  	gauche()
  	if (estFaceMur())
  		return true
  if (getDirection()==Direction.OUEST)
  	gauche()
  	if (estFaceMur())
  		return true
}