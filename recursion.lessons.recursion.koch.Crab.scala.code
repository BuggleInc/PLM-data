def crab(levels:Int, length:Double) {
if (levels==0){
	//avance(length)
	return
}
gauche(45)
avance((length)/Math.sqrt(2))
droite(90)
avance((length)/Math.sqrt(2))
crab(levels-1, (length)/Math.sqrt(2))
}
