def insertionSort() {
  var v = 0
  var k = 0
  for (i<- 1 to getNombreValeurs()-1){
    v = getValeur(i)
    k = i
    while( plusPetit(v,k) && k>1 ){
      
      copy(k-1,k)
      k-=1
    }
    setValeur(k,v)
    
    
  }
}