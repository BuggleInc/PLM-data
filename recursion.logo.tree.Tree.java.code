public void tree(int steps, double length, double angle, double shrink)  {
	if(steps==0){}
  else if(steps==1){avance(length);}
  else
    {
    avance(length);
    droite(angle);
    tree(steps-1,length-shrink,angle,shrink);
    tree(steps-1,length-shrink,-angle,shrink);
  }
}