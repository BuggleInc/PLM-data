def growFlowers() {
  flower(Color.RED);
  avance();
  avance();
  flower(Color.CYAN);
  for (i<-1 to 6) {
    recule();
  }
  droite();
  avance();
  avance();
  gauche();
  avance();
  avance();
  flower(Color.YELLOW);
  for(k<-1 to 4) {
    recule();
  }
  droite();
  avance();
  avance();
  gauche();
  flower(Color.PINK);
  avance();
  avance();
  flower(Color.GREEN);
}

def flower(c:Color) {
  setCouleurBrosse(c:Color);
  baisseBrosse();
  avance();avance();avance();
  recule();
  gauche();
  avance();
  recule();
  recule();
  avance();
  setCouleurBrosse(Color.YELLOW)
  droite();
  leveBrosse();
  avance();
  
}