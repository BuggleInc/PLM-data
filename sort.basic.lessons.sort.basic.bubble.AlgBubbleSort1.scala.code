def bubbleSort() 
{
  var sortie:Boolean = false
  
  while(!sortie)
  {
    sortie = true
    
    for(i <- 0 to getNombreValeurs())
    {
      if(plusPetit(i+1,i))
      {
        echange(i,i+1)
        sortie = false
      }
    }
    
  }
}