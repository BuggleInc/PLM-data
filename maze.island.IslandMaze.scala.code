override def run() {
  while (!estSurBiscuit()) {
    while (getDirection != Direction.NORTH) {
      gauche()
    }
    while (!estFaceMur) {
      avance()
    }
    while (estFaceMur()) {
      keepHandOnSideWall()
    }
  }
}

def keepHandOnSideWall() = {
  gauche()
  while (estFaceMur()) {
    droite()
  }
  avance()
}