def carre() {
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
}

override def run() {
  carre()
  droite(90); droite(90)
  carre()
  droite(90); droite(90)
  carre()
  droite(90); droite(90)
  carre()
  droite(90); droite(90)
}