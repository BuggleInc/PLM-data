override def run() {
  var compt = 0
  val n = getMondeLargeur()
  for (i <- 0 to n-1) {
    for (j <- 0 to n-1) {
      ecritMessage(compt)
      if (i%2==0) setPos(j,i)
      else setPos(n-1-i,j)
      compt += 1
    }
  }
}