override def run() {
  while (!estSurBiscuit()){
    //var nb : Int = random3() 
    random3() match {
      case 0 => if (!estFaceMur()){avance()}
      case 1 => droite()
      case 2 => gauche()
    }
      
  }
  prendBiscuit()
}