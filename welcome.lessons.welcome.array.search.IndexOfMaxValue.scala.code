def indexOfMaxValue(nums:Array[Int]):Int = {
  var L = nums.length
  var indice = 0
  var max = nums(0)
  for (i <- 1 to L-1){
    if (max<nums(i)){
      max = nums(i)
      indice = i
    }
  }
  return indice
}
