def goAndGet() {
  var cpt =0
  while(!estSurBiscuit) {
    avance()
    cpt = cpt + 1
    }
  prendBiscuit()
  for(cpt1<- 1 to cpt) {
    recule()
  }
  poseBiscuit()
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}