public void run() {
  int nbBiscuit = 0;
  int nbCase = 0;
    
  do{
    
    if(estSurBiscuit()&&crossing()){
      nbBiscuit++;
    }
    
    avance();

    if(nbBiscuit >=3 && crossing()){
      gauche();
    } else if(nbBiscuit <=2 && crossing()){
      droite();
    }
    
    
    
  }while(!sortieTrouvee()); 
  
}