def insertionSort() {
  for (i <- 1 to getNombreValeurs()-1){
    var tmp:Int = getValeur(i)
    var j:Int = i
    while ((j>0)||(getValeur(j)<getValeur(j-1))){
      setValeur(j,getValeur(j-1))
      j = j-1
    }
    setValeur(j,tmp)
  }
}