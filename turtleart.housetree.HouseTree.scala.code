override def run() {
  for (nbMais <- 1 to 4) {
    for (compt1 <- 1 to 5) {
      avance(30)
      droite(90)
    }
    for (compt2 <- 1 to 2) {
      avance(30)
      gauche(120)
    }
    avance(30)
    gauche(30)
    avance(30)
    gauche(90)
    leveCrayon()
    avance(50)
    baisseCrayon()
    gauche(90)
  }
}