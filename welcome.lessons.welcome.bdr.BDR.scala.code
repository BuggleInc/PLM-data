val fini=0;

if (litMessage() == "R")
{
  droite();
  avance();
  
}
	else if (litMessage() == "L")
	{
 	gauche();
  	avance();
  	
	}

		else if (litMessage() == "I") 
			{
 			 retourne();
  			avance();

			}
				else {
  					fini = true;
					}
