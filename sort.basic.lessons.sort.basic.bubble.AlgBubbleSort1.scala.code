def bubbleSort()  {
  var sorted:Boolean = false
  while(!sorted){
    sorted = true
  	for(i <- 0 to getValueCount() - 2){
      if(getValue(i) > getValue(i + 1)){
        swap(i, i + 1)
        sorted = false
      }
    }
  }
}
