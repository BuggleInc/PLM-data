def gnomeSort() {

  var i=0

  while(i<getValueCount()){

    if((i==0)||(isSmaller(i,i-1))){i+=1;}
    else{
      swap(i,i-1);
      i=i-1;

    }
  }
}


    /*while pos < length(a):
        if (pos == 0 or a[pos] >= a[pos-1]):
            pos := pos + 1
        else:
            swap a[pos] and a[pos-1]
            pos := pos - 1*/