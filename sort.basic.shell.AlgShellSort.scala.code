def shellSort()  {
var ecart =(getNombreValeurs()/2).toInt
  for(i<- 1 to (getNombreValeurs()-1))
  	{var v=getValeur(i)
   	while(i-ecart>0 && getValeur(i-ecart)>v){
     	copie(i-ecart,i-2*ecart)
     	ecart=ecart-ecart}
   	setValeur(ecart,v)}
}