override def run() {
	for (compt1 <- 1 to 5) {
    avance(100)
    droite(90)
  }
  for (compt2 <- 1 to 2) {
    avance(100)
    gauche(120)
  }
  avance(100)
  gauche(30)
  avance(100)
  gauche(180)
}
