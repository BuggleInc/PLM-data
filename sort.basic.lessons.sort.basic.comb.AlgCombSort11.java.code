public void combSort11() {
  double ecart = getValueCount(); 
  boolean swap = true;
  do{
    if(ecart >1){
      ecart = (ecart.toDouble()/1.3).toInt();
    	if((gap == 9)||(gap == 10)){
      	gap = 11;
   	 }
    }
		
    swap = false;
    for(int j = 0; j < getValueCount()-gap;j++){
      if(isSmaller(j+gap,j)){
        swap(j+gap,j);
        swap = true;
      }
      
    }
  }while((ecart > 1)||swap);
}