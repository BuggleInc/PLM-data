override def run() {
  var etat = 0
  while (!estSurBiscuit()) {
    etat match {
      case 0 => etat=courrirNord()
      case 1 => etat=suivreGauche()
      case _ => println("error")
    }
  }
  prendBiscuit()
}

def courrirNord() {
  setDirection(Direction.NORTH)
  if (!estFaceMur()) { 
    avance() 
    0
  } else {
		while (!estFaceMur()) {
      gauche()
    }
    droite()
    1
  }
}

def suivreGauche() {
  if (getDirection()==Direction.NORTH && !estFaceMur()) {
    0
  }
  gauche()
  if (!estFaceMur()) {
    avance()
  } else  {
    while (estFaceMur()) {
      droite()
    }
    avance()
  }
  1
}