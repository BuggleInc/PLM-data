override def run() {
  while(getY()!=0){
    while(!estFaceMur()){
      avance()
    }
    droite()
    if (!estFaceMur()){
      avance();gauche()
    } else {
      retourne();avance();droite()
    }
  }
  while (!estSurBiscuit()){
    keepHandOnSideWall()
  }
  prendBiscuit()
  
}

def keepHandOnSideWall(){
  gauche()
  if (!estFaceMur()){
    avance()
  } else {
    while(estFaceMur()){
      droite()
    }
    avance()
  }
}