 def hunt(c:Color) {
 	while(!estSurBiscuit) {
 		leveBrosse();
 		isFacingTrail()
 		     if (isFacingTrail == true ) {
 		     	avance();
 		     	baisseBrosse();
 		     else  (isFacingTrail == false ) {
 		     	droite()}
 		     	
 		     }
 		     
 	}
prendBiscuit()
 	
 }

def isFacingTrail():Boolean = {
     if (estFaceMur) {
     	return false
     }
    avance();getCouleurSol();
    if (getCouleurSol() == Color.green) {
    	recule();
    return true
} else { recule();
return false
}
}       
