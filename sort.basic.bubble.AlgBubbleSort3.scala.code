def bubbleSort3() {
  var k = false;
  val len = getValueCount();

  for(j<- 0 to len-2){
    k = false ;
    for(i<- 0 to len-2 - j){
      if(!isSmaller(i, i+1)){
        swap(i,i+1);
        k = true ;
      } 
    }
    if(!k) { 
      return 
    }
  }
}