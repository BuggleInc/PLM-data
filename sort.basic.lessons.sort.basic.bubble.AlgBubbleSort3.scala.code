def bubbleSort3() {
  var nb : Int = getNombreValeurs()
  var bool : Boolean = false
  for (i<-nb-1 to 0 by -1) {
    bool = false
    for (j<-0 to i-1) {
    	if (plusPetit(j+1,j)) {
        echange(j,j+1)
        bool = true
    	} 
  	}
    if (bool == false) {return}
  }
}
