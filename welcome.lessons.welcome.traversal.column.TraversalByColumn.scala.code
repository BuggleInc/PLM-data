override def run() {
	retourne();
	var hauteur=getMondeHauteur();
	var largeur=getMondeLargeur();
	var num =0 ;
	var poid=1
	while ( !(getX()==largeur) && !(getY()==hauteur))
{
	for ( i<- 0 to  48 )
	{
	
	ecritMessage(num)
	while (!estFaceMur)
	{
		avance();	
	}
	if (estFaceMur)
	{
		setPos(poid,0)
	poid==poid+1
	}
	}
}
}