def squirrelPlay(temp:Int, isSummer:Boolean):Boolean = {
  if  (isSummer){
    return estDans(temp,60,100)
  }
  else{
    return estDans(temp,60,90)
  }
}




def estDans(a:Int,b:Int,c:Int):Boolean={
  return (a>=b)&&(a<=c) 
}