x=0
y=8
setCouleurBrosse(Color.lightGray)
baisseBrosse()
while(not(getX()==14 or getY()==0)==False):
    while(estFaceMur()==False):
        x=x+1
        setPos(x,y)
        baisseBrosse()
        leveBrosse()
    gauche()
    y=y-1
    setY(y)
    droite()