def noTriples(nums:Array[Int]): Boolean = {
	if(nums.length > 2) {
		for(c <- 0 to nums.length - 2) {
			if(nums(c) == nums(c+1) && nums(c+1) == nums(c+2)) {
				return false
			}
		}
	}
	return true
}
