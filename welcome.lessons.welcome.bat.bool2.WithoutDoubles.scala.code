def withoutDoubles(die1:Int, die2:Int, noDoubles:Boolean):Int = {
  var d?1:Int=die1
  var d?2:Int=die2
  if (noDoubles && d?1==d?2) {
    if (d?1==6) {d?1=1}
    else {d?1=d?1+1}}
  return (d?1+d?2)
}
