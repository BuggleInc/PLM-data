def insertionSort() {
  var v = 0
  var k = 1
    for (i <- 2 to getNombreValeurs()-1) {
      v = getValeur(i)
      k = i
      while ((k>1)&&(plusPetit(v, (k-1)))) {
        setValeur(k-1, getValeur(k));
        k=k-1
      }
      k=k+1;
      setValeur(k,v)
    }
}