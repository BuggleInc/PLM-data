override def run() {
	for (c <- 1 to 8)
	{
		gauche(45)
		recule(25)
		faireMotif();
	}
}

def faireMotif()
{
	droite(45)
	avance(25);
	droite(45);
	avance(25);
	droite(45);
	avance(25);
	gauche(45);avance(25);
	droite(45); avance(25);recule(25);
	droite(90); recule(25); droite(90);recule (25);gauche(135);
	
}
