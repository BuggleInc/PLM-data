public void run() { 
	int nBaggles = 0, nOrange = 0;

	while (true) {
		forward();
		if (isOverBaggle())
			nBaggles++;
		if (getGroundColor() == Color.orange)
			nOrange++;
		if (nBaggles * 2 == nOrange +1)
			break;
	}
}
