premiereFois = True
while premiereFois or not croisement() :
    premiereFois = False
    b=1
    while sortieTrouvee() == False :
        for i in range (5):
            if estSurBiscuit() == True :
                b = b+1
            forward()
        if b <= 2 :
            droite()
        if b >= 3 :
            gauche()
    forward()