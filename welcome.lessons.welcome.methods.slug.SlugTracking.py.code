def isFacingTrail():
   # write your code here
    while isOverBaggle != True:
        if (getGroundColor() == Color.gray):
            if (isFacingWall()):
                lefty()
            else:
                forward()
        elif (getGroundColor() == Color.green):
            brushDown()
            brushUp()
            forward()

isFacingTrail()