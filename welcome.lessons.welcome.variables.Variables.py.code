count=0
while isOverBaggle()!=1:
    count=count+1
    forward()
    while isOverBaggle()==1:
        pickupBaggle()
        setDirection(Direction.SOUTH)
        forward(count)
        dropBaggle()
        setDirection(NORTH)
