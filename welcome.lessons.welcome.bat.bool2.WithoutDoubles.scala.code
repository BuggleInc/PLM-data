def withoutDoubles(die1:Int, die2:Int, noDoubles:Boolean):Int = {
  
  if ((noDoubles) & ((die1 == die2) & (die1 != 6))) {die1 = die1 + 1 ; return (die1 + die2)}
  
  else if ((noDoubles) & (die1 == die2) & (die1 == 6)) {die1 = 1 ; return die1 + die2}
  
  else {return die1 + die2}

}