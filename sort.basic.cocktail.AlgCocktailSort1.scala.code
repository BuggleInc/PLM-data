def cocktailSort() {
  var n: Int=getNombreValeurs()-2
do {  
  cond=false
  for (i<- 0 to n){
    if (plusPetit(i+1,i)){
      echange(i,i+1);cond=true}}
  for (i<- n to 0 by -1){
    if (plusPetit(i+1,i)){
      echange(i,i+1);cond=true
    }
  }
}while (cond)