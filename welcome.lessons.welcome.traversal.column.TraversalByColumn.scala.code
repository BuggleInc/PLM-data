def fini():Boolean = {
  if(getX()==6 && getY()==6){
    return true
  }
  return false
}

override def run() {
  
  var nb:Int = 0;
  
  while(!fini()){
    
    ecritMessage(nb);
    
    nb += 1;
    
    setPos(nb,1);
  }
  
}