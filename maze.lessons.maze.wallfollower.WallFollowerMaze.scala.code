override def run() {
	while (!estSurBiscuit) {
		while ((murAGauche) && (!estFaceMur)) {
			avance();
		}
		gauche()
		if (!estFaceMur) {
			avance()
		}
	}
	
}
def murAGauche():Boolean ={
	gauche();
	if (!estFaceMur) {
		droite()
		return false
	} else {
		droite()
		return true
	}
}
