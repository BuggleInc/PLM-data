def insertionSort() {
  var v:Int = 0
  for (i <- 1 to getNombreValeurs()-1) {
    v = getValeur(i)
    for (j <- 1 to i-1) {
      if (v < getValeur(i-j)) {
        copie(i-j,i-j+1)
      } else {
        setValeur(i-j,v)
      }
    }
  }
}