def selectionSort() {
  var xmin=getValeur(0)
  var k=0
  while(k!=getNombreValeurs()-1){
    for (i<- 1 to getNombreValeurs()-1){
      if(getValeur(i)<xmin){
        xmin=getValeur(i)
      }
    }
    setValeur(k,xmin)
    k=k+1
  }
}