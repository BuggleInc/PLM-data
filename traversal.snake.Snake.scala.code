override def run(def fonce(){while (!isFacingWall) {forward()}}                                              def ok {brushDown();
              for (i<-1 to 3) {fonce(); left(); forward(); left(); fonce(); right(); forward(); right()}; fonce()}                   
