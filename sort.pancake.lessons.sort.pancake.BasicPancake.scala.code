def solve() {
  var max = 0 ;
  for (i<-0 until getStackSize()){
    max=0
    for(j<-0 to getStackSize()-i-1){
      if(getPancakeRadius(j)>getPancakeRadius(max)){
        max = j
      }
    }
    flip(max)
    flip(getStackSize()-i)
  }
}