def cocktailSort() {
	var swap = false ;

	Do{
	for(i <- 0 to getNombreValeurs()-2  ){
		if (plusPetit(i,i+1)){
			echange(i,i+1);
			swap=true;
		}
		
		
	}
	for(j <- getNombreValeurs()-2 to 0  by -1){
		if (plusPetit(j+1,j)){
			echange(j,j+1);
			swap=true;
		}
		}
} while(swap == true)
