override def run() {
def faireV() {avance();baisseBrosse(); leveBrosse(); gauche(); avance(); droite()
	; avance();baisseBrosse(); leveBrosse(); avance(); droite(); avance(); baisseBrosse(); leveBrosse()
	;gauche(); avance(); gauche()}

def faireCase() {for (i<-1 to 4) {faireV(); }}


def faireLigne() {for (c<-1 to 9) {faireCase(); for (a<-1 to 5) {avance()} }}

for (d<-1 to 9) {faireLigne(); gauche() ;for(e<-1 to 5) {avance()}; gauche(); 
for (b<-1 to 10) {avance}; droite(); droite()}
}
