def golomb(num:Int): Int = {
  var suite = new Array[Int](num+1)
  suite(1) = 1
  suite(2) = 2
  for (i <- 3 to num-1) {
    suite(i+1) = 1 + suite(i+1-suite(suite(i)))
  }
  suite(num) = 1 + suite(num-suite(suite(num-1)))
  return suite(num)
}