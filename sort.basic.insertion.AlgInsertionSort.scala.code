def insertionSort() {
  var k = 0
  var v = 0
  for (i<- 1 to getNombreValeurs() -1) {
    k=i
    v=getValeur(i)
    while((k > 0) && getValue(k-1)>v) {
      copie(k-1,k)
      k-=1
    }
    setValeur(k,v)
  }
}