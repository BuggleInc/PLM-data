def insertionSort() {
  
  for(i<- 1 to getValueCount()-1){
  	backMinus(i);
  }
  
}


def backMinus(indSorted:Int){
  var v = getValue(indSorted+1)
  for(i <- 0 to indSorted){
    if(isSmaller(v, getValue(i-1))){
      swap(i, i-1);
    }
    else{
      setValue(i, v);
      return;
    }
  }
  
}