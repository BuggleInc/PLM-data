override def run() {
  
  for (x <- 1 to 12) {
  
  	for (c <- 1 to 5) {
    
    	avance(50)
    	droite(72)
    	avance(50)
    	gauche(144)
    
  	}
    
    avance(50)
    droite(72)
    avance(50)
    
 	}
  
}