def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  
  var x = getX();
  var y = getY();
  
  if(steps == 0){
    
    allerVers(x, y);
    
  } else{
    
    avance(length);
    droite(angle);
    current(steps);
    tree(steps-1, length*shrink, angle, shrink);
    recule(length/shrink);
    
  }
  
}