def hunt() {
	while (!isFacingTrail) {
		leveBrosse()
		gauche()	
	}
	if (isFacingTrail) {
		avance()
		baisseBrosse()
	}
}
def isFacingTrail():Boolean = {
	var estVert = false;
	if (!estFaceMur) {
		avance()
		if (getCouleurSol()==Color.green) {
			estVert = true;
		}
		leveBrosse()
		recule()

	}
	return estVert;
}        