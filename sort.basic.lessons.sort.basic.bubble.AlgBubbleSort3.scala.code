def bubbleSort3() {
  var switch:Boolean=true

	while(switch){
    switch=false
    for(i<- 1 to getNombreValeurs()-1){
      if (plusPetit(i+1,i)){
        echange(i+1,i)
        switch=true
      }
      if (!switch){return}
    }
  }
   
}