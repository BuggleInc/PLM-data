def shellSort()  {
  var ecart:Int= getValueCount()/2
  while(ecart>0){
    insertionSort();
    ecart=ecart/2;
  }

def insertionSort(){
  for(i<-0 to getValueCount()-ecart){
    var x:Int=getValue(i);
    var j:Int=i;
    while(j>=ecart && getValue(j-ecart)>x){
      setValue(j+ecart,getValue(j))
      j=j-ecart
    }
    setValue(j+ecart,x)
  }
}
}