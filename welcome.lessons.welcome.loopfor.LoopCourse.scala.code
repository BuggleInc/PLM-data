for (compteur <- 1 to 4) {
  droite()
  for (compteur2 <- 1 to 8) {
    avance()
  }
  
  droite()
  for (compteur2 <- 1 to 8) {
    avance()
  }
  
  droite()
  for (compteur2 <- 1 to 8) {
    avance()
  }
  
  droite()
  for (compteur2 <- 1 to 8) {
    avance()
  }
}