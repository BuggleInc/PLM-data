def dragon(order:Int, x:Double, y:Double, z:Double, t:Double) {
	var u:Double =0
	var v:Double =0
	order match {
		case 1 => setPos(x,y)
				allerVers (z,t)
		case _ =>
		u = (x + z)/2 + (t - y)/2
		v = (y + t)/2 - (z - x)/2

		
	}
}
