def keepHandOnSideWall = {gauche(); if (!estFaceMur) {avance();gauche();avance()} else {while(estFaceMur()) {droite};avance()} }

override def run() { 
  
  
  while (!estSurBiscuit()) {keepHandOnSideWall}; prendBiscuit()
}