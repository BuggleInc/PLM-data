override def run() {
var Val = 0
	
	while (!(getX() == getMondeLargeur()-1 && getY() == getMondeHauteur()-1)) {
	
		for (i<- 0 to 5) {
				ecritMessage(Val)
				setPos(getX(),getY()+1)
				Val=Val+1
		}
			ecritMessage(Val)
			setPos(getX()+1,0)
			
		
	}
}
