override def run() {
	var x:Double = 0
	var y:Double = 0

	
	
	for (i <- 1 to 4)
	{
		for(c <- 1 to 4)
	{
		avance(100)
		droite(90)
	}
	avance (100)
	droite (30)
	for (c <- 1 to 2)
	{
		avance (100)
		droite(120)
	}
	droite(90)
	x = getX
	y = getY
	setPos(x+20,y)
	}
}
