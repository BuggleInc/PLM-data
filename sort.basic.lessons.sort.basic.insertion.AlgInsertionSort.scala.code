def insertionSort() {
  val n=getNombreValeurs()
  var x = 0
  var save = 0
  
  for(i<-1 to n-1)
   {
     save = getValeur(i)
     var cpt = i
     while(cpt<0 && getValeur(cpt-1)<save)
     {
       copie(cpt-1,cpt)
       cpt -= 1
     }
   }
}