var nbtr:Int=10
var dr:Int=1
var ht:Int=1
var gche:Int=1
var bas:Int=1

for (nbtr <- 1 to 10){
  for(dr <- 1 to 8){avance()}
  gauche()
  for(ht <- 1 to 8){avance()}
  gauche()
  for(gche <- 1 to 8){avance()}
  gauche()
  for(bas <- 1 to 8){avance()}
  gauche()
}
