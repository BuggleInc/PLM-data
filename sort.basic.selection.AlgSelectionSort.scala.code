def selectionSort() {
  var lgr=getNombreValeurs()
  var idx=0
  var min=getValeur(9)
  for (i<-0 to lgr-1) {
    for (j <- i to lgr-1) {
      if (plusPetitQue(j, min)) {idx=j;min=getValeur(j)}
    }
    echange(i,idx)
  }
}