override def run() {
	faireLigne()
	monterLigne()
	
	avance(30)
	droite(30)
	for(i<-1 to 3) {
			avance(30)
			droite(120)
	}
	droite(60)
	for(i<-1 to 3) {
		avance(30)
		droite(90)
	}
	leveCrayon()
	droite(90)
	avance(150)
	gauche(90)
	baisseCrayon()
	avance(30)
	droite(30)
	for(i<-1 to 3) {
			avance(30)
			droite(120)
	}
	droite(60)
	for(i<-1 to 3) {
		avance(30)
		droite(90)
	}
	
	monterLigne()
	faireLigne()
	monterLigne()
}

def faireLigne() {
	for (c<-1 to 4) { //Début ligne maisons
		avance(30)
		droite(30)
		for(i<-1 to 3) {
			avance(30)
			droite(120)
		}
		droite(60)
		for(i<-1 to 3) {
			avance(30)
			droite(90)
		}
		leveCrayon()
		droite(90)
		avance(50)
		gauche(90)
		baisseCrayon()
	}
}

def monterLigne() {
	leveCrayon()
	droite(90)
	recule(200)
	gauche(90)
	avance(75)
	baisseCrayon()
}
