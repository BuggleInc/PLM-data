override def run() {

  while(!estSurBiscuit())
  	{
      var random = random3()
      if(random==0 && !estFaceMur())
      	{
          avance
        }
      if(random==1)
      	{
          gauche()
        }
      if(random==2)
      	{
          droite()
        }

    }
  prendBiscuit()
}