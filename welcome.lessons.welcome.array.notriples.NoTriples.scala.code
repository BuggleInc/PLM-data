def noTriples(nums:Array[Int]): Boolean = {
	var noTriplet = true
  
  for (i <- 0 to nums - 1 - 2)
  	if (nums[i] == nums[i + 1] && nums[i + 1] == nums[1 + 2])
  		noTriplet = false
  
  return noTriplet
}