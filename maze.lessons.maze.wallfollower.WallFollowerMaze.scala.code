override def run() {
    def keepHandOnRightWall() = {
    	if (estFaceMur) {droite()} 
    	else if (!(estFaceMur)) {
    }
    while (!(estSurBiscuit)) {
    	keepHandOnRightWall();
    }
    prendBiscuit();
}
