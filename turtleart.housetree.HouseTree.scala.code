override def run() {
  var x=getX()
  var y=getY()
  setX(x+20)
  for {j <- 1 to 4) {
    for(i <- 1 to 4) {
      avance(30)
      droite(90)
    }
    avance(30)
    droite(30)
    avance(30)
    droite(120)
    avance(30)
    droite(30)
    avance(30)
    droite(90)
    avance(30)
    droite(90)
    x+=20
    y=getY()
    allerVers(x,y)
  }
}