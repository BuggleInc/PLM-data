var nbBiscuits = 0;

while (!sortieTrouvee())
{
  nbBiscuits = 0;

  for (i <- 1 to 5)
  {
    avance();
    if (estSurBiscuit()) nbBiscuits+=1;
  }

  if (nbBiscuits >= 3) gauche();
  else droite();
}

avance();