def bubbleSort3() {
  var n:Int =getValueCount();
  var finished:Boolean = true;
  for (i<-0 to n-2){
    finished = true;
    for(j<-i+1 to n-1) {
      if (!isSmaller(i,j)) {
        swap(i,j);
        finished = false;
      }
    }
    if (finished) {
      return
    }
  }
}