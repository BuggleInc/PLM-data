override def run() {
	var hauteur: Int = getMondeHauteur();
	var largeur: Int = getMondeLargeur();
	var valeur: Int = 0;

	for(n1 <- 0 to largeur - 1){
		for(n2 <- 0 to hauteur - 1){
			setPos(n2, n1);
			ecritMessage(valeur);
			valeur = valeur + 1;
		}
	}
	
}
