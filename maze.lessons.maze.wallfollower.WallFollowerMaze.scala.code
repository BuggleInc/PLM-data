override def run() {
    def keepHandOnRightWall() = {
    while (!(estFaceMur)) {droite(); if (!estFaceMur) {avance}}
    if (estFaceMur) {gauche(); if (!estFaceMur) {avance}}
    }
    while (!(estSurBiscuit)) {
    	keepHandOnRightWall();
    	if (!estFaceMur) {avance()};
    }
    prendBiscuit();
}
