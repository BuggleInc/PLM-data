override def run() {
	def marquer() {
		baisseBrosse();
		leveBrosse();
	}
	def faireV(c:Color) {
		avance();
		marquer();
		avance();
		gauche();
		avance();
		marquer();
		recule();
		droite();
		avance();
		marquer();
		avance();
		gauche();
		setCouleurBrosse(c);
	}
			for (i<- 1 to 4) {
				if (i==1)
					faireV(Color.YELLOW);
				if (i==2)
					faireV(Color.RED);
				if (i==3)
					faireV(Color.BLUE);
				if (i==4)
					faireV(Color.GREEN);
			}
}
