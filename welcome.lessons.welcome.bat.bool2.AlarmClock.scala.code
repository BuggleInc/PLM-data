def alarmClock(day:Int, vacation:Boolean): String = {
  	day match {
      case 0|6 if (vacation) => return ("off")
      case 0|6 => return "10:00"
      case _ if (vacation) => return "10:00"
      case _ => return  "7:00"
    }}
