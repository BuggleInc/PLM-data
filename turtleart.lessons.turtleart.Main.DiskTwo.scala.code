override def run() {

	def quartier(c:Color) {
		setCouleur(c)
		for(i<-1 to 40) {
			avance(100)
			recule(100)
			droite(20.0/40)
		}
	}

	for (i<-1 to 9) {
		quartier(Color.black)
		quartier(Color.red)
	}

}
