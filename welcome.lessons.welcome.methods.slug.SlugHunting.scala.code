def hunt() {
  
  
  	if (!isFacingWall){
    	forward()
    }
  
    while (!isFacingTrail()){
      if (isOverBaggle()){
      	pickupBaggle()
      return
    	}
      
      left()
    }
  
  	brushDown()
  	brushUp()
  	hunt()
}



def isFacingTrail():Boolean = {
  	if (isFacingWall()){
      return false
    }
    forward()
  	if (getGroundColor() == Color.green){
      backward()
      return true
    }
  	backward()
  	return false
}