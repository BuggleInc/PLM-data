def selectionSort() {
  var j:Int = 0
  for (k <- 0 to getNombreValeurs() - 2) {
    j = k
    for (i <- k to getNombreValeurs() - 1) {
      if (getValeur(i) < getValeur(j)) {j = i}
    }
    echange(k,j)
  }
}