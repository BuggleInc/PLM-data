override def run() {
  var cpt:Int = 0
  var posX:Int = 0
  var posY:Int = 0
  while (!estPosFinale()) {
    setPos(posX, posY)
    ecritMessage(cpt)
    if (getY() == getMondeHauteur()-1) {
      posX += 1
      posY = 0
    } else {
      posY += 1
    }
    cpt += 1
  }
}

def estPosFinale():Boolean = {
  if (getX() == (getMondeLargeur()-1) && getY() == (getMondeHauteur-1)) {
    return true
  } else {
    return false
  }
}