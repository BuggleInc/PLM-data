def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
	
	
	if (steps==0) {
			return  } 
	
	else { 	avance(length); 
			droite(angle);
			avance(shrink*length)
			droite(angle);
			avance(shrink*length); 
			droite(angle);
			avance(shrink*length); 
			recule(length)
			gauche(2*angle)
			avance(shrink*angle)
			
			
			
			}


}

