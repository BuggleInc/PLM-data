public void insertionSort() {
	
	for(int i=1; i<getNombreValeurs(); i++){
		int temp = getValeur(i);
		int j=i;
		
		while(j>0 && temp < getValeur(j-1)){
			copie(j-1, j);
			j--;
		}

		setValeur(j, temp);
	}

}
