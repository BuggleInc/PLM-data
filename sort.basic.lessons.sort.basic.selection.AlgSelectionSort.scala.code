def selectionSort() {
	for (i <- 0 to getNombreValeurs()-2) { 
		val minpos=i
		for (j <- i to getNombreValeurs()-1) {
			if (plusPetit(j,minpos)) { 
				copie(j,minpos)
			}
		}
		echange(i,minpos)
	}
}
