def sierpinski(level:Int, length:Double) {
  for (i<- 0 to level-1){
  if (level != 0){
    sierpinski(level-1,length/2)
	}else{
    forward(length)
  	right(120)
  	forward(length)
  	right(120)
  	forward(length)
  	right(120)
  }
}
}