def solve() {
  var taille:Int = getTaille()
  var color:Color = getCouleur(0)
  var apresBleu:Int = 0 // Entre 0 et apresBleu : que des bleus ;
  var avantBlanc:Int = taille-1 // Entre avantBlanc et blancRouge : que des BLANC ;
  var blancRouge:Int = taille-1 // Apres blancRouge : que des ROUGE ;
  // cellules non triees : entre apresBleu et avantBlanc.
  // regardez la couleur de la case apresBleu et effectuez les echanges.
  // La boucle stop quand apresBleu > avantBlanc

  while (apresBleu > avantBlanc) {
    if (getCouleur(apresBleu)==ROUGE) {
      echange(apresBleu,avantBlanc)
      avantBlanc-=1
      blancRouge-=1
    }
    
    if (getCouleur(apresBleu)==BLANC) {
      echange(apresBleu,avantBlanc)
      avantBlanc-=1
    }
    
    for (k <- avantBlanc to apresBleu by -1) {
       if (getCouleur(k)==BLEU) {
          echange(apresBleu,k)
      }
    }
    
  }
  
}

/*
echange(x,y) 
getCouleur(ligne) : Int

*/