def selectionSort() {
  var longueur:Int = getNombreValeurs()
	for(i <- 0 to longueur) {
    var min:Int = i;
  
  	for(j <- i + 0 to longueur) {
    	if(getValeur(j) < getValeur(min))
    		min = j;
    }
    echange(i, min)
  }
}