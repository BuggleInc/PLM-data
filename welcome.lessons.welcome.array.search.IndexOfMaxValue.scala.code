def indexOfMaxValue(nums:Array[Int]):Int = {
  var max = nums(0)
  var indexOfMax = 0
  for (i <- 0 to nums.length-1){
    if (max<nums(i)){
      max=nums(i)
      indexOfMax=i
    }
  }
  return indexOfMax
}
