def bubbleSort()  {
	var test:Boolean=true;
	do{
		test=false;
		for(i<-0 to getNombreValeurs()-2)
        	{
        		if(plusPetit(i+1,i)){
        			echange(i+1,i);
        			test=true;
        		}
        	}
	}while(test)
}
