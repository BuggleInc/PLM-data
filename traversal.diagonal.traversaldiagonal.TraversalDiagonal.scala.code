  var diag = 0;
  var cpt = 0;

def fini():Boolean={
  return cpt == (getMondeHauteur()+getMondeLargeur()-1) 
}

def traceDiag(){
  if (diag<8){
    var x = diag
    var y = 0
    while(y != diag ){
      setPos(y,x);
      ecritMessage(cpt)
      cpt += 1;
      x -= 1;
      y += 1;
    }
  }
}

override def run() {
  while(!fini()){
    traceDiag();
    diag += 1;
  }
}