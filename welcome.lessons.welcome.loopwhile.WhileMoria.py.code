while not isBackingWall():
    if isCarryingBaggle():
        while not isOverBaggle():
            forward()
        backward()
        dropBaggle()
        backward()
    else:
        while not (isOverBaggle()) or (isBackingWall()):
            backward()
        pickupBaggle()
        