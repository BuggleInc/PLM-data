def combSort11() {
  var used = true;
  val len = getValueCount();
  var gap = len;
  var tempGap = gap;
  var i = 0;
  do{
    used = false;
    if (gap > 1) {
      gap = (gap.toDouble / 1.3).toInt
    }
    i = 0;
    tempGap = if(gap==9 || gap==10) 11 ; else gap; 
    while (i+tempGap < len){
      if(!isSmaller(i, i+tempGap)){
        swap(i,i+tempGap);
        used = true ;
      }
      i+=1;
    }
  }while (gap > 1 || used)
}