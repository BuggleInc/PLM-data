def insertionSort() {
  var v : Int =0
  var k : Int =0
  for(i<-1 to getNombreValeurs()-1){
    v = getValeur(i)
    k=i
    while(getValeur(k)>v && k>0){setValeur(k+1,getValeur(k));k+= -1;}    
    setValeur(k,v)
  }
}
