def insertionSort() {
	var l:Int =getNombreValeurs()
	for (i<- 0 to l-1) {
		var buffer:Int = getValeur(i)
		var k = i
		while((k<l-1)&&(getValeur(k+1)>buffer)) {
			setValeur(k+1,getValeur(k))
			k+=1
		}
		setValeur(k,buffer)
	}
}
