for (compteur <- 1 to 28) {
		for (compteur1 <- 1 to 4)
  			avance()
  	gauche()
  	for (compteur2 <- 1 to 2)
  			avance()
  	droite()
  	for (compteur2 <- 1 to 4)
  			avance()
  	droite()
  	for (compteur2 <- 1 to 2)
  			avance()
  	gauche()
  	for (compteur2 <- 1 to 4)
  			avance()
  	gauche()
  	
}