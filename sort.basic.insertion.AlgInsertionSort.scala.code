def insertionSort() {
  for (i<- 1 to getNombreValeurs()-1){
    var v = getValeur(i)
    var j = i
    do{
      copy(j-1,j)
      j-=1
      
    }while (plusPetit(v,getValeur(j-1)) && j>1)
		setValeur(j-1,v)
    
  }
}