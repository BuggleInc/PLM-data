int x,y;
universe.Direction d;

x=getX();
y=getY();
d=getDirection();

setBrushColor(Color.blue);
brushDown();
left();
forward(4); //Done with top.
left();
forward(4); //Done with left side.
left();
forward(4); //Donw with bottom.
left();
forward(2);
left();
forward(); //Done with right side.

//Now, back to start.
brushUp();
setPos(x,y);
setDirection(d);