def bubbleSort()  {
	var n= getNombreValeurs()
	var h:Boolean =false
		while (h==false) {
			for (k <- 1 to n-2) {
				if (plusPetit(k+1,k)) {
					echange(k+1,k)
					h=false;
				}
			}
		}
	
		
}
