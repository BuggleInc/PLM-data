def gnomeSort() {
  var len = getValueCount();
  var end = false;
  var nain = 0;
  
  while(!end || nain < len){
    if(getValue(nain) > getValue(nain+1)){
      swap(nain, nain+1);
      if(nain > 0){
        nain-1;
      } else {
        nain +1;
      }
    }
  }
}