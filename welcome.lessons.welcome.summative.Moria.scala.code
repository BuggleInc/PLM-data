right(); right()
while(getX() < 19){
  if(isFacingWall()){
    right(); forward(); left(); forward()
  }
  else if(isCarryingBaggle()){
    forward()
    if(isOverBaggle()){
    	right(); right(); forward(); dropBaggle()
  	}
  }
  else{
    forward()
    if(isOverBaggle()){
    	pickupBaggle(); right(); right()
    }
  }
}