def shareDigit(a:Int, b:Int):Boolean = {
  var aL = a / 10;
  var aR = a % 10;
  var bL = b / 10;
  var bR = b % 10;
  
  if (aL == bL || aL == bR || aR == bL || aR == bR)
    return true;
  else
    return false;
}