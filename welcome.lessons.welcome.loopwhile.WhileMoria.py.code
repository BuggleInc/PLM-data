back()
while isFacingWall() != True:
    while isFacingWall() == True:
        left()
    while isOverBaggle() != True:
        forward()
    while isOverBaggle() == True:
        pickupBaggle()
        back()
    while isCarryingBaggle() == True:
        if (isOverBaggle()):
            back()
            forward(1)
            dropBaggle()
            forward()
        else:
            forward()
    
