def haveBaggle():Boolean = {
  var seenBaggle = false
  for (stepper <- 1 to 6) {
    while (isOverBaggle() == false){
      forward
    }
    if (isOVerBaggle() == true) {
      (return seenBaggle = true)
    }
  }
}