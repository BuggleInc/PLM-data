def insertionSort() {
	val n = getNombreValeurs()
  for ( k <- 1 to n-1){
    var v = getValeur(k)
    var i = k
    while(i>0 && getValeur(i-1)>v) {
      setValeur(i,getValeur(i-1))
      i -= 1
    }
    setValeur(i,v)
  }
}
      
