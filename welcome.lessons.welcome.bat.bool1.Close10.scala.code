def close10(a:Int, b:Int): Int = {
	var Anub = Math.abs(a - 10)
	var Bnub = Math.abs(b - 10)

	if (Anub = Bnub ) { 
		return 0
	} else if { Anub<Bnub ) {
		return Anub
	} else if ( Anub> Bnub ) {
		return Bnub
	}
}
