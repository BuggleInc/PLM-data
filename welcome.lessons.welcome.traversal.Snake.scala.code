override def run() {
	while (!positionFinale() {
		if (estFaceMur()) {
			if (getDirection() == Direction.WEST) {
				droite();
				avance();
				droite();
			} else {
				gauche();
				avance();
				gauche();
			}
		} else {
			avance();
		}
	}
}
