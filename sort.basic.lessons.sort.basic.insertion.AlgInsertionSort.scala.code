def insertionSort() {
	for(i<-1 to getNombreValeurs()-1){
		var v:Int= getValeur(i);
		var j:Int=i;
		while(j>0 && plusPetitQue(j-1,v)==false){
			copie(j-1,j);	
			j=j-1;	
		}
		setValeur(j,v);
	}
}
