override def run() {
  setCouleurBrosse(Color.RED)
  baisseBrosse()
  b=test()
  var cpt:Int = 0
  while(test() == false) {
    if (cpt==6) {
      if (getDirection()==Direction.EST) {
        gauche()
        avance()
        gauche()
      }
      else {
        droite()
        avance()
        droite()
      }
    }
    else {
      avance()
    }
  }
}

def test():Boolean = {
  var b:Boolean = false
  if (estFaceMur()) {
    if (getDirection()==Direction.EST) {
      gauche()
      if (estFaceMur) {
        droite()
        b=true
      }
      else {
        b=false
      }
    }
    else {
      droite()
      if (estFaceMur()) {
        gauche()
        b=true
      }
      else {
        b=false
      }
    }
  }
  else {
  	b=false
  }
  return b
}