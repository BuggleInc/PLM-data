def cocktailSort()  {
	  var x=getNombreValeurs();
	  var estTrie=false;
	  var nombreTrie=0
	  while (!estTrie) {
	  	estTrie=true;
	  	for (i<-0 to x-2-nombreTrie) {
	  		if (!plusPetit(i, i+1)) {
	  			echange(i, i+1);
	  			estTrie=false;
	  		}
	  	}
	  	for (i<-0 to x-2-nombreTrie) {
	  		if (!plusPetit(x-2-nombreTrie-1,x-2-nombreTrie)) {
	  			echange(x-2-nombreTrie,x-2-nombreTrie+1);
	  			estTrie=false;
	  		}
	  	}
	  	nombreTrie=nombreTrie+1;
	}
} 