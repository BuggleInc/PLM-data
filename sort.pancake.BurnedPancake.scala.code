def solve() {
  def solve() {
 
	var fin = getStackSize()
	var pk = 0
  
  while(!isSorted()){
    
    pk=0
    
    for (i <- 0 to fin-1){
      if ((getPancakeRadius(pk)<getPancakeRadius(i))){pk=i}
    }
    
    flip(pk+1)
    flip(fin)
    fin-=1
    
  }
  
}
}