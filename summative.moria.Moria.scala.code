while (!isBackingWall) 
{def fonce() {while ((!isOverBaggle)&&(!isBackingWall)) {backward()}}
def retraite() {while (!isOverBaggle) {forward()}}
def caselibre() {while (isOverBaggle) {backward()}}
def pickbaggle() {pickupBaggle(); retraite(); caselibre(); dropBaggle; backward()};
 fonce(); if (!isBackingWall) {pickbaggle()}; if (!isBackingWall) {backward()}}