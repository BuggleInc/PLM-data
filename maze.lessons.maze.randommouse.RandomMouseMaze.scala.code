override def run() {
  var nb:Int=0
	while (!estSurBiscuit()){
    nb=random3()
    nb match{
      case nb if nb==0 => gauche()
      case nb if nb==1 => droite()
      case _ => if (!estFaceMur()){avance()}
    }
  }
  prendBiscuit()
}