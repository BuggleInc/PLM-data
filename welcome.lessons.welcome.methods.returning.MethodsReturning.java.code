boolean haveBaggle() {
	boolean seenBaggle = false;
	while (!isFacingWall()){
	int progressY = 0;
		while (!isFacingWall()){
			if (isOverBaggle()){
				seenBaggle = true;
			}
			forward();
			progressY++;
		}
		while (progressY != 0) {
			backward();
			progressY--;
		}
	}
	return seenBaggle;
}
