override def run():Boolean ={
  def keepHandOnSideWall(){
    droite()
    if(estFaceMur()){ 
      gauche()
      return true}
    else{
      gauche()
      return false}
  }
  
  while(!estSurBiscuit()){
    if(keepHandOnSideWall()){avance()}
    droite()
    avance()
    
  }
  prendBiscuit()
}