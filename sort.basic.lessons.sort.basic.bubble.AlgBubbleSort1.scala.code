def bubbleSort()  {
  val n = getNombreValeurs()
  var swap:Boolean = true
  while (swap){
    swap = false
    for (i <- 0 to n-2) {
    	if (plusPetitQue(i+1,i)) {
     		echange(i,i+1);
     		swap = true
    	}
  	}
  }
}