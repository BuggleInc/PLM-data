def bubbleSort()  { val a = getNombreValeurs() - 1
  for (i <- 0 to a) {
                      for (j <- i to a) { 
                         if (plusPetit(i,i+1)) { echange(i,i+1)
                           }
                        
                       } 
                     }
}