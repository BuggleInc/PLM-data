RecList last(RecList seq) {
	RecList butLast = new RecList();
	butLast(butLast, seq);
}

RecList butLast(RecList butLast, RecList seq){
	if (seq.tail == null) return butLast;	
	butLast = cons(seq.head, butLast);
	return butLast(butLast, seq.tail);
}
