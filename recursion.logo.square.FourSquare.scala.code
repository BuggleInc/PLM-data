override def run() {
  avance(100.00) 
  droite()
  avance(100:Double)
  
  for (i<-0 to 3){
    if (i%2==1){avance(100.00) droite()}}
  droite() 
  avance(200:Double)
  reculer (100:Double)
  droite()
  avance(100:Double)
  recule(200:Double)
  
	}

