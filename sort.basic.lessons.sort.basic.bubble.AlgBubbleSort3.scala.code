def bubbleSort3(){	
	var i:Int = getNombreValeurs() ;
	var hasSwap:Boolean = false ;
	
	while(i !=0)
	{
		for (j <- 1 to i-1 by 1)
			{
				if (plusPetit(j, j-1))
				{
					echange(j,j-1);
					hasSwap = true;
				}
			}
		if (!hasSwap)
			return;
		i = i-1;
	}
	
}