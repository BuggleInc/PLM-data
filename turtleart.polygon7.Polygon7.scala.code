override def run() {  
  avance(80:Double)
  droite(45:Double)
  avance(80:Double)
  droite(45:Double)
  avance(80:Double)
  droite(45:Double)
  avance(80:Double)
  droite(45:Double)
  avance(80:Double)
  droite(45:Double)
  avance(80:Double)
  droite(45:Double)
   avance(80:Double)
  droite(45:Double)
                   }