def insertionSort() {

  for(i<- 1 to getNombreValeurs()-1){
    var v = getValeur(i)
    var j=i
    while(v<getValeur(j-1)){
 			setValeur(j,getValeur(j-1))
      if(j!=0){
      	j=j-1
      }
    }
    setValeur(j,v)
    
  }
}