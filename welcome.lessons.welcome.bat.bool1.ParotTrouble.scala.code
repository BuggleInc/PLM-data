def parotTrouble(talking:Boolean, hour:Int):Boolean = {
  if (talking){
    hour match {
      case h if h<=7 => return true
      case h if h>=20 => return true
      case _ => return false
    }
    else {return false}
  }
}