override def run() {
	var sommeAngle:Int = 0
	def isDirectionFree(dir: Direction) : Boolean = {
		setDirection(dir);
		if (!estFaceMur) {
			true;
		} else {
			false;
		}
	}
	while (!estFaceMur()) {
		avance();
	}
	
}
