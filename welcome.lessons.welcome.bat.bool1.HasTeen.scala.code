def hasTeen(a:Int, b:Int, c:Int): Boolean = {
	(a,b,c) match {
		case (a,_,_) if (a>=13 && a<=19) => return true
		case (_,b,_) if (b>=13 && b<=19) => return true
		case (_,_,c) if (c>=13 && c<=19) => return true
		case (_,_,_)				   => return false
	}
}