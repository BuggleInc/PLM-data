override def run():Unit= {
	avance(30)
  avgauche()
  droite(45)
  avance(30)
  avgauche()
  avdroite()
  avdroite()
  gauche(45)
  avance(30)
  avdroite()
  gauche(45)
  avance(30)
  avdroite()
  gauche(45)
  avdroite()
  
  
  
}

def avdroite(){
  droite(90)
  avance(30)
}
def avgauche(){
  gauche(90)
  avance(30)
}