def teaParty(tea:Int, candy:Int): Int = {
(tea, candy) match {
	case (a,b) if (candy>=2*tea || tea>=2*candy) => return 2
	case (a,b) if (tea<5 && candy<5) => return 0
	case (a,b) if (tea>=5 && candy>=5) => return 1
	case (_,_) => return 0
}
}
