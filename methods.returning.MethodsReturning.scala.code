def haveBaggle():Boolean = {
  var seenBaggle = false
  for (stepper <- 1 to 6) {
    if (isOverBaggle == true) {
      seenBaggle = true
      forward()
    }
    else {
      forward()
    }
  return seenBaggle
  }
}