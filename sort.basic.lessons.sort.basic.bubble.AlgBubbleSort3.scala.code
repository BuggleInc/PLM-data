def bubbleSort3()  {
  var compt = getValueCount()
  var swapped=false;
  for (i<-getValueCount()-1 to 0 by -1;j<-0 to i-1 ) {
    	swapped=false
    
      
      if (!isSmaller(i,i+1))  		  
          {
            swap(i,i+1);swapped=true;
          }
      
    
    if (!swapped) {return}
    
  }
}