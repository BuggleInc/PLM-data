def selectionSort() {
  var i:Int=0
  
  for(i<-0 to getValueCount()-2){
    var m=i
    for(j<-i+1 to getValueCount()-1){
      if(getValue(j)<=m){
        m=getValue(j)
      }
    }
    swap(m,j);
  }
}
