def insertionSort() {
  
  for (i <- 1 to getNombreValeurs()-1) {
    
    var v:Int = getValeur(i)
    for (j <- 1 to v) {
      
      if (plusPetit(v, i-1)) {
        
        copie(j, j-1)
        
      }
      
    }
    
  }
  
  
}