def haveBaggle():
    seenBaggle = False
    steps = 0
    while steps <= 6:
        forward()
        steps = steps + 1
        if isOverBaggle() == True:
            seenBaggle = True
            return seenBaggle
    while steps >= 0:
        backward()
        steps = steps -1
    return seenBaggle