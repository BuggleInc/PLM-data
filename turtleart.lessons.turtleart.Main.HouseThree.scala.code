override def run() {
	
	avance(30:Double)
		droite(30:Double)
	for(i <- 1 to 3){
		avance(30:Double)
		droite(120:Double)
	}
	gauche(30:Double)
		droite(90:Double)
	
	for (i <- 1 to 3){
		avance(30:Double)
		droite(90:Double)
	}
	droite(90:Double)
	leveCrayon()
	avance(20:Double)
	baisseCrayon()
	gauche(90:Double)
}

