def insertionSort() {
  
  for (i <- 1 to getNombreValeurs()-1) {
    
    for (c <- i to 1 by -1) {
      
      while(plusPetit(c,c-1)) {
        
        echange(c,c-1)
        
      }
      
    }
    
  }
  
}
