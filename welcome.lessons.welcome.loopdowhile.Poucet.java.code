public void run() { 
	int bagels;
	while(!exitReached()){
		bagels = 0;
		do
			forward();
			if(isOverBagel())
				bagels++;
		while(!crossing());
		if (bagels <= 2){
			right();
		}else {
			left();
		}
	}
	
}
