def combSort() {
	var gap:Int = getValueCount();
	if(gap>1) gap=gap/1.3;
	i=0;
	while(i+gap<getValueCount()){
		if(isSmaller(i+1,i))
			swap(i,i+1);
	}
}



o
   if gap>1 then
     gap = gap / 1.3
   i = O
   while i+gap < len do:
     if i and i+gap must be swapped, do it
     increase i by one
while the gap is bigger than 1 or 
the last traversal swapped at least one pair