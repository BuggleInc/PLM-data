def cocktailSort() {
  for (i<-0 to getNombreValeurs()-2) {
    if (getValeur(i+1)<getValeur(i)) {
      echange(i,i+1)
    }
  }
  for (i<- getNombreValeurs()-2 to 0 by -1) {
    if (getValeur(i)<getValeur(i-2)) {
      echange(i,i-1)
    }
  }
}