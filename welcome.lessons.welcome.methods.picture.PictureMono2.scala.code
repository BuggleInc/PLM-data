

def motif() {for (i<-1 to 4) {avance(2);faireV()} 
}

def marquer()= {baisseBrosse();leveBrosse()}

def faireV()={
  marquer();
  gauche();
  avance();
  droite();
  avance();
  marquer();
  avance();
  droite();
  avance();
  marquer();
  gauche();
  avance(2);
  gauche()
  }





override def run() {for (i<-1 to 3) {for (i<-1 to 3) {motif();avance(8)};gauche();avance(8)}
}
