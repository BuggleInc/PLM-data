def fleur(c:Color) = {
  setCouleurBrosse(c)
  for (i <- 1 to 3) {avance()}
  recule()
  gauche()
  for (i <- 1 to 3) {avance()}
  recule()
  droite()
  setCouleurBrosse(Color.YELLOW)
  leveBrosse()
  recule()
}

def growFlowers() = {
  fleur(Color.RED)
}
