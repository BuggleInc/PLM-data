def cocktailSort() {
val n :Int = getNombreValeurs()
for (i <- 0 to n-2) {
	
 	if (plusPetit(k+1,k)) {
 		echange(k+1,k)}
for (k <- n-2 to 0 by -1) {
	
 	if (plusPetit(k+1,k)) {
 		echange(k+1,k)}  
}
}
}
