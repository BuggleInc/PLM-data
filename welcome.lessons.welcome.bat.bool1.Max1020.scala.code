def max1020(a:Int, b:Int):Int = {
  if (dv(a) && dv(b)) {return Math.max(a,b)}
  else if (dv(a)) {return a}
  else if (dv(b)) {return b}
  else {return 0}
}


def dv(a:Int):Boolean= {
  if (a<=20 && a>=10) {return true}
  else  {return false}
}