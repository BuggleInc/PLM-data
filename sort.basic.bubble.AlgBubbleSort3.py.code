array_len = getNombreValeurs()
for i in range(array_len - 2, -1, -1):
    changed = False
    for j in range(0, i+1):
      if plusPetit(j+1, j):
        echange(j+1, j)
        changed = True
    if not changed:
  		break