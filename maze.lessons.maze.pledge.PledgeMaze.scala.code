override def run() {
  gauche()
  var sommeAngle = 1
  while(!estSurBiscuit()){
    if(isDirectionFree(Direction.NORTH)&&sommeAngle==0){
      avance()
    } else {
      while(sommeAngle!=0){
        sommeAngle = KeepHandOnSideWall(sommeAngle)
    }
  }
  }
  droite()
  prendBiscuit()
}

    
    
    
// def eviteMur(i:Int){
//  while(getDirection()!=Direction.NORTH){
//    KeepHandOnSideWall(i)
//}
//} 

def isDirectionFree(dir:Direction): Boolean = {
  setDirection(dir)
  if(estFaceMur()){
    return false
  } else {
    return true
  }
}

def KeepHandOnSideWall(i:Int):Int={
  if(estFaceMur()==false){
    avance()
    gauche()
    return i+1
  } else{
    droite()
    return  i-1
  }
}