def polygone(n:Int,x:Int) {
  for (i <- 1 to n) {
    avance(x);droite(360/n)
  }
}


override def run() {
  polygone(8,50);avance(25);droite(45)
  for (i <- 1 to 4) {
    avance(50);gauche(45);avance(50);droite(90);avance(50)
  }
}