override def run() {
 
  def maison() {
    for(i <- 1 to 4) {
      forward(30)
      right(90)
    }
    forward(30)
    right(30)
    for(j <- 1 to 3) {
      forward(30)
      right(120)
    }
    left(30)
    backward(30)
    right(90)
    forward(30)
  }
  
	for(k <- 1 to 4) {
    maison()
    penUp()
    forward(20)
    left(90)
    penDown()
  }
}