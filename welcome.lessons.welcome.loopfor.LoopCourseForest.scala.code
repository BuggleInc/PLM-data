var nbTours = 0;

for(t<-1 to 10){
  
  for(i<-1 to 4){
		avance(4);
    gauche();
    avance(2);
    droite();
    avance(4);
    droite();
    avance(2);
    gauche();
    avance(4);
    gauche();
  }
  
  nbTours+=1;
  
}