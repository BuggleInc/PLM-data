def bubbleSort2() {
  var swapped= false
  var c:Int=getValueCount()
  do {
    swapped=false;
    for (i<-0 to c-1){
      if (!isSmaller(i,i+1)) {
        swap(i,i+1);
        swapped= true
        
      }
      c=c-1
    }
    
  }while (swapped);
}