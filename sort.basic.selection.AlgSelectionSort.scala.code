def selectionSort() {
  var n:Int = getNombreValeurs()
  for (i <- 0 to  -1){
    var indMin:Int = i
    var min:Int = getValeur(i)
    for (j <- i to n -1){
      if (getValeur(j)<min){
        indMin = j
        min = getValeur(indMin)
      }
    }
    echange(i,indMin)
  }
}