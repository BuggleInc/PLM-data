override def run() {
  var c = 0;
  while ((c == 0) && (getDirection() == Direction.NORTH)){
    avance();
  }

  while(((c%4) != 0) && !estSurBiscuit()){
  	gauche();
    c -=1 ;
 		while(estFaceMur())
  	{
    	droite();
      c += 1
  	}
  	avance();
  }
  
}

def run2(){
  while(!estSurBiscuit()){
    fun();
  }
}

def fun(){
  gauche();
  while(estFaceMur())
  {
    droite();
  }
  avance();
}