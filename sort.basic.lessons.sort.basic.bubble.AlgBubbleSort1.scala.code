def bubbleSort()  {
  var swap = true
  while (swap) {
    swap = false
  	for (i <- 0 until getNombreValeurs-1) {
      if (plusPetit(i+1, i)) {
        echange(i, i+1) 
        swap = true
      }
  	}
  }
}
