def bubbleSort()  {
  var i=0
  while (i != getNombreValeurs() ){
    i+=1
    var c=0
    var nombre=getValeur(0)
    while (c != getNombreValeurs()-1){
      if (plusPetit(nombre,c)){
        c+=1
        echange(c,c+1)
      } 
      else {
        nombre=getValeur(c)
      }
    }
  }
}