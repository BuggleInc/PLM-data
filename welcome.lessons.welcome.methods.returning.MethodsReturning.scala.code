Boolean vubiscuit = false

def haveBaggle():Boolean = {
  for (i <- 1 to 7) {
    for (j <- i to 7) {
      vubiscuit();
      avance();
    }
  recule(6);
  if (vubiscuit()==true) {
    return true
  }
  }
}

def vubiscuit() :Boolean = {
  if (estSurBiscuit()) {
    return true;
  }
}