def keepHandOnSideWall() {
  while (estDosMur()){gauche()}
  recule();droite()
  
}

override def run() {
  while(!estSurBiscuit()){
    keepHandOnSideWall()
  }
  prendBiscuit()
 while(estDosMur()){gauche()}
}