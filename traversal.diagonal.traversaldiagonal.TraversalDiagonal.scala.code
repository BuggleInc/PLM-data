override def run() {
  var diag = 0
  var count = 0
  ecritMessage(count)
  while (diag!=12){
    count=count+1
    ecritMessage(count)
    if (getX()==diag && getY()==0){
      diag=diag+1
      setX(0);setY(diag)
      
    }
    else{
      setX(getX()+1);setY(getY()-1)
    }
  }
}

