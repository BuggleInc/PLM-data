def bubbleSort()  {

	var lgr = getNombreValeurs();
	var tri = true;
	
	while(tri) {
		tri = false;
		for(i<-0 to lgr-2) {
			if(plusPetit(i:Int, i+1:Int) == false) {
				echange(i, i+1);
				 tri = true;
			}
		}
		
	}
	
}
