var baggle = 0
do {
    baggle=0
    do {
     avance()
     if (estSurBiscuit()){
      baggle=baggle + 1
     }
    }while (!crossing())
    if (baggle>=3){
      gauche()
    } 
    else { 
      droite()
    }
}while (!sortieTrouvee())
avance()
