def cocktailSort() {
  
  for(i <- 0 to (getValueCount() / 2); j <- (getValueCount() / 2) to getValueCount()-1) {
    
     if (isSmaller(i+1,i))
        swap(i,i+1);
     if (isSmaller(j+1,j))
        swap(j,j+1);
  }
      
}