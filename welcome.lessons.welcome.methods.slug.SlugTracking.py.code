def isFacingTrail():
   # write your code here
    while isOverBaggle != True:
        if (isFacingWall() == True):
            left()
        else:
            brushDown()
            brushUp()
            forward()
        if (getGroundColor() != Color.green):
            backward()
            left()
'''        else:
            brushDown()
            brushUp()
            forward()'''

isFacingTrail()