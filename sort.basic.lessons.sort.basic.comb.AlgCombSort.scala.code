def combSort() {
	val n:Int = getNombreValeurs()
	var yaw:Int = n
	var swapped:Boolean = true

	do {
		if (yaw>1)
			yaw = (yaw.toDouble/1.3).toInt
		var i:Int = 0
		swapped = false
		while (i+yaw<n) {
			if (plusPetit(i+yaw, i)) {echange(i, i+yaw); swapped = true}
			i+=1
		}
	} while (yaw>1 || swapped)
}
