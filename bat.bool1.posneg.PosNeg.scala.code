def posNeg(a:Int, b:Int, negative:Boolean):Boolean = {
  (a,b,negative) match {
    case (-1,1,false) => return true
    case (1,-1,false) => return true
    case(-1,-1,true) => return true
    case _ => return false
  }
}