def icyHot(temp1:Int, temp2:Int):Boolean = {
	(temp1,temp2) match {
		case (temp1,temp2) if {temp1<0 && temp2>100} => return true
		case (temp1,temp2) if {temp2<0 && temp1>100} => return true
		case (_,_) => return false
	}
}
