def cocktailSort() {
  var term = false
  var n = getValueCount()-2
  
  do{
    term = false
    
    for(m <- 0 to getValueCount()-2)
    for(i <- 0 to getValueCount()-2-m){
      if(isSmaller(i+1,i)){
        swap(i,i+1)
        term = true
      }
    }
    for(o <- 0 to getValueCount()-2)
    for(j <- 0 to getValueCount()-2-o){

      if (isSmaller(n+1,n)){
        swap(n,n+1)
        term = true
        n = n-1
      }
    }
    
    
  } while (term == true)
}