override def run() {
  cercle(Color.black)
  cercle(Color.red)
  cercle(Color.orange)
  cercle(Color.magenta)
}

def cercle(c:Color) {
  setCouleur(c)
  for (i <- 0 to 89){
    avance(100)
    recule(100)
    droite(1)
  }
}