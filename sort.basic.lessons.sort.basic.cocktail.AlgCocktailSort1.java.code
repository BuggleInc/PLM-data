public void cocktailSort() {
	boolean swapped;
	do {
		swapped = false;
		int i = 0;
		while(i < getValueCount() - 2) {
			System.out.println("-> i:  " + i);
			if (isSmaller(i + 1, i)) {
				swap(i, i+1);
				swapped = true;
			}
			i++;
		}
		while(i > 0) {
			if (isSmaller(i + 1, i)) {
				swap(i, i + 1);
				swapped = true;
			}
			i--;
		}
	} while(swapped);
}

