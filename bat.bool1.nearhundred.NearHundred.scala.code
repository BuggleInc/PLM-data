def nearHundred(n:Int): Boolean = {
  var diff : Int = Math.abs(n-100); var diffbis : Int = Math.abs(diff-100)
  if (diff<=10) {return true}
  else {if (diffbis <= 10 ){return true}
       else return false}
}