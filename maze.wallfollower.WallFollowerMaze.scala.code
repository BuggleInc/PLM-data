override def run() {
  //while(!isOverBaggle()){
  for(i <- 1 to 100){
      keepHandOnSideWall()

}

def keepHandOnSideWall(){
  
  if(isFacingWall()){
    
    //Si mur devant et ? gauche
    right()
    if(!isFacingWall()){
      forward()
        }
    	}
  }
}