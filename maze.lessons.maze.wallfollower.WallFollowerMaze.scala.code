override def run() {
  while (!estSurBiscuit()) {
    keepHandOnSideWall()
  }
  prendBiscuit();
}


def keepHandOnSideWall() = {
  if (!estFaceMur) {
  	droite();
    avance();
  }
  if (estFaceMur()) {
    	gauche();
  }
 
}