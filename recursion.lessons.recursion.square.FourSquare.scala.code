override def run() {
	for(i<-1 to 2){
	avance(100)
	droite(90)
	avance(100)
	droite(90)
	avance(100)
	droite(90)
	avance(100)
	recule(100)
	gauche(90)
	avance(100)
	droite(90)
	avance(100)
	droite(90)
	avance(100)
	
	}
	gauche(90)
}
