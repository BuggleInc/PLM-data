def selectionSort() {
  for(i<- 0 to getNombreValeurs()-2){
    var petit = i
    for(o<- i to getNombreValeurs()-1){
      if(getValeur(o)<getValeur(petit)){
        petit=o
      }
    }
    echange(petit,i)
  }
}