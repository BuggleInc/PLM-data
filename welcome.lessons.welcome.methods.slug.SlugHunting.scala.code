def hunt() {
    while (!estSurBiscuit) {
    		isFacingTrail(); 
    	}
    prendBiscuit();
}




def isFacingTrail():Boolean = { 
	 if ( !estFaceMur() ) { avance(); }
	 else { gauche(); avance(); }
	if (getCouleurSol() == Color.green) {
		 recule();
		 return true
	} else {
           recule();
		 return false
		}
		
	} 
           