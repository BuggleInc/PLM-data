def extrema(nums:Array[Int]): Int = {
  var max=nums(0)
  var min=nums(0)
  var n =nums.length
  for ( i <- 0 to n-1){
    if (max<nums(i)){
      max=nums(i)
    }
   	else if (min>nums(i)){
      min=nums(i)
    }
  }
  return ( max-min)
}