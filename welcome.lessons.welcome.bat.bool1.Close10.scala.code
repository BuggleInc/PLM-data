def close10(a:Int, b:Int): Int = {

var compteurA = Math.abs(a - 10)
var compteurB = Math.abs(b - 10)
var c = 0

	if (compteurA <= compteurB) {
		return a
	} else {
		if (compteurB <= compteurA) {
			return b
		} else {
			if (compteurA == compteurB) {
				return c
			} else { 
				return c
				} 
			}
		}
	}
