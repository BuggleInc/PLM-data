def indexOfMaxValue(nums:Array[Int]):Int = { 
  var max = nums(0)
  var k =0
                                            for (i<-0 to nums.length-1){if (nums(i)> max){max= nums(i)
                                                                                        k = i}}
  return k
}