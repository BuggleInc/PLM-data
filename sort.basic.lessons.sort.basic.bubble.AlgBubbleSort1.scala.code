def bubbleSort()  {
  var nb=getNombreValeurs()
  var k=0
  while (k!=nb-1){
    for (i<-0 to nb-1){
      if (plusPetit(getValeur(k),getValeur(i))){
        echange(i,k)
        k=i
      } else {k=i+1}
    }
  }
}