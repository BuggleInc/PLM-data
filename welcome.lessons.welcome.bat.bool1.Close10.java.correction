$package import plm.universe.bat.BatEntity; import plm.universe.bat.BatWorld; import plm.universe.bat.BatTest; import plm.universe.World; public class close10 extends BatEntity {  public void run(BatTest t) {
		t.setResult(close10((Integer)t.getParameter(0),(Integer)t.getParameter(1))); }
	int close10(int a, int b) {
		if (Math.abs(a-10)==Math.abs(b-10))
			return 0;
		if (Math.abs(a-10)<Math.abs(b-10))
			return a;
		return b;
	}
 }