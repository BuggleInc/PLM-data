def bubbleSort()  {
  var ok :Boolean = false
  do{
    ok = true
    for (i<-0 to getNombreValeur()-2){
      if (getValeur(i)>getValeur(i+1)){
        echange(i,i+1)
        ok = false
      }
      
    }
    
  } while (!ok)
}