override def run():Unit= {
  gauche(45);avance(20);droite(45);avance(20);gauche(90);avance(20);droite(90)
  for (i<- 1 to 8) { droite(45);avance(20);gauche(45);avance(20);droite(45)};
  droite(45);avance(20);droite(45);avance(20);droite(45);avance(20);recule(20);
  for (i <- 1 to 8) {gauche(135);avance(20);droite(135);avance(20);droite(45);avance(20);recule(20);}
  gauche(45);recule(20);gauche(135)
}