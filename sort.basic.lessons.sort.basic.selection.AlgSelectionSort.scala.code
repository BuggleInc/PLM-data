def selectionSort() {
  for (i <- 0 to getValueCount()-2) {
			var min = i;	

			
			for (j <- i + 1 to getValueCount()-1) {
				if (isSmaller(j,min))
					min = j;				
			}

			
			swap(min,i);
		}
}