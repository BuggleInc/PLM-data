def bubbleSort2() {
  

  var taille = getNombreValeurs();
  for(i<-0 to taille - 1)
{
    for(j<-1 to taille - 1)
    {
      
      if (getValeur(j)<getValeur(j-1))
          {
            echange(j,j-1)
            
          }
         
    }
  taille = taille - 1
  }

}
