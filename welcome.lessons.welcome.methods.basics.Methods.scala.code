def goAndGet() {
  var count: Int = 0
  while(!isOverBaggle()){
    count = count + 1
    forward()
  }
  pickupBaggle()
  
  for (c <- 0 to count){
    backward()
  }
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}