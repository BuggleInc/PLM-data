def insertionSort() {
  var v:Int = 0
  var j:Int = 0
  var k:Int = 0
  
	for (i <- 1 to getNombreValeurs() - 1) {
		v = getValeur(i)
    k = i
    j = i
    
    while (j > 0 && getValeur(j - 1) > v) {
      echange(j, j - 1)
    	j -= 1
    }
    echange(j, k)
	}
}