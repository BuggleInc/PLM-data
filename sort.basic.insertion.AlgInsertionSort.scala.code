def insertionSort() {
  for(i<-1 to (getNombreValeurs-1))
  	{
      var v = getValeur(i)
      var k = i-1
      while(!plusPetitQue(k,v) && k!=0)
      	{
					copie(k,k+1)
          k=k-1
        }
      setValeur(k+1,v)
    }
}