override def run() {
	nord;
	while (!estSurBiscuit()) {
	keepHandOnSideWall()}
prendBiscuit()
}

	 
def keepHandOnSideWall() {
	gauche()
	if (!estFaceMur()) {avance()} else 
	{droite(); if (!estFaceMur()) {
		avance()} else {droite()}
	}
	}

def nord {
	while(!estFaceMur()) {avance()}
	if (estFaceMur()) {droite(); avance(); gauche()}
}
