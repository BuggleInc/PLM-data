def hunt(c:Color) {
  while(!estSurBiscuit()){
    isFacingTrial(c)
  isFacingTrial(c) match {
    case true => baisseBrosse();leveBrosse()
    case false => gauche()
  }
  } 
  prendBiscuit()   
 }

def isFacingTrial(b:Color): Boolean={
if( estFaceMur()){
  return(false)
}
  avance()
  if (getCouleurSol()==b){
  recule()
    return(true)
}else{
    recule()
  return false
}
}