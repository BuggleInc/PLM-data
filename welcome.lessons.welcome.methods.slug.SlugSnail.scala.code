def hunt(c : Color) {
	while(estSurBiscuit == false) {
		if (isFacingTrail(c : Color) == true) {
    			baisseBrosse();
    			avance();
    			leveBrosse();
    		} else {
    			gauche();
    			if (isFacingTrail(c : Color) == false) {
    				droite();droite();
    			}
    		}
	}
	prendBiscuit();
}
 
def isFacingTrail(c : Color):Boolean = {
	var couleur : String = "non"
	if (estFaceMur()==false) {
		avance();
		if (getCouleurSol()== c) {
			couleur = "oui";
		} else { 
			couleur = "non";
		}
		recule();
		if (couleur == "oui") {
			return true;
		} else {
			return false;
		}
    } else {
    	return false;
    } 
}        
