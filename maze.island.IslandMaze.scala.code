override def run() {
  while (!estSurBiscuit()) {
    setDirection(Direction.NORTH)
    while (!estFaceMur()) {
      avance()
    }
    if (estFaceMur()) {
      keepHandOnSideWall()
    }
  }
}

def keepHandOnSideWall() = {
  if (!estFaceMur()) {
    gauche()
  }
  while (estFaceMur()) {
    droite()
  }
  avance()
}