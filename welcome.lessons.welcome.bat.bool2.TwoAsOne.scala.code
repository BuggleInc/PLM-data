def twoAsOne(a:Int, b:Int, c:Int):Boolean = {
	(a,b,c) match{
		case(a,b,c) if(a == (b+c)) => return true
		case (a,b,c) if(b == (a+c)) => return true
		case (a,b,c) if(c == (a+b)) => return true
		case _ => return false
	}
}
