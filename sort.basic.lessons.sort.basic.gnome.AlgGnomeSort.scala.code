def gnomeSort() {

	var pos = 1;
	
 do
 {
	if(pos > 0)
	{
		if(plusPetit(pos - 1, pos))
		{
			pos = pos + 1;
		}else
		{
			echange(pos - 1, pos);
			pos = pos - 1;
		}
	}else
	{
		pos = pos + 1;
	}

 	
 }while(pos != getNombreValeurs() )
}
