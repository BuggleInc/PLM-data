def answerCell(isMorning:Boolean, isMom:Boolean, isAsleep:Boolean):Boolean = {
  (isMorning, isMom, is Asleep) match{
    case (_,_,true) => return false
    case (false, _,_) => return true
    case (true, true, false) => return true
    case (_,_,_) => return false
  }
}