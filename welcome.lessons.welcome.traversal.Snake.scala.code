override def run() {
  baisseBrosse()
  while(getX!=6 && getY!=0){
    if(estFaceMur()){
      if(getDirection() == Direction.EST){
        gauche()
        avance()
        droite()
      }
      else{
        droite()
        avance()
        gauche()
      }
    }
    else{ avance(6)}
  }
}