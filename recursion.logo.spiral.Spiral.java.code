public void spiral(int steps, int angle, int length, int increment)    {
  if(steps==0){}
  else if(length%increment==0)
    {
    spiral(steps-1,angle,length+1,increment);
  }
  else{
    avance(length);
    gauche(angle);
    spiral(steps-1,angle,length+1,increment);
    }
}