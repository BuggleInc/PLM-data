def inOrderEqual(a:Int, b:Int, c:Int, equalOk:Boolean):Boolean = {
	if(equalOK && a <= b && b <= c) {
		return true
		}
	else return false
	
	if(equalOK && a < b && b < c) {
		return true
	}
	else return false
}
	
