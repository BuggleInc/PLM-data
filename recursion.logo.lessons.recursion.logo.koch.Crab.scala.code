def crab(levels:Int, length:Double) {
  if(levels!= 0){
    crab(levels-1, length/sqrt(2))
  	gauche(60)
  	crab(levels-1, length/sqrt(2))
    droite(120)
    crab(levels-1, length/sqrt(2))
    gauche(60)
    crab(levels-1, length/sqrt(2))
  }
  if(levels == 0){
    avance(length)
  }
}
