override def run() {
	for(x <- 1 to getMondeHauteur(); y <- 1 to getMondeLargeur()) {
		if(aBiscuit(x,y)) {
			setIndications(x, y, 0);
		}
	}
}

def setIndications(x, y, valeur) {
	setIndication(x,y, valeur);
	if(!aMurNord(x,y)) {
		setIndications(x,y+1,valeur+1);
	}
	if(!aMurOuest(x,y)) {
		setIndications(x+1,y,valeur+1);
	}
}
