def alarmClock(day:Int, vacation:Boolean): String = {
  if (vacation && day>=1 && day<=5){
    return "10:00"
  }else if (!vacation && day>=1 && day<=5){
    return "7:00"
  }else if(vacation && (day==0 or day==6)){
    return "off"
  }else{
    return "10:00"
  }
}