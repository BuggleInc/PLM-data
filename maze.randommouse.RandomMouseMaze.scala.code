override def run() {
    while (isOverBaggle == false){
      if (isFacingWall==true){
        for (a <- 0 to random3()){
          left()
        }
        
      }
      else {
          for (a <- 0 to random3()){
            while (isFacingWall==false){
              forward()
        }
        }
      }
    }
  pickupBaggle()
}