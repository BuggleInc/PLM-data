override def run() {
  
  while(!isOverBaggle()){ 
    var X = random3();

    if(X==0)
      left();
    else if(X==1)
      right();
    else
    	if(!isFacingWall())
	      forward();
	}
  
  pickupBaggle();
  
}