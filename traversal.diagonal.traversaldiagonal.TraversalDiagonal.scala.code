var diag = 0;
var count:Int = 0

override def run() {
  
  avancer()
    
  }
  
  
def avancer(){
  
  for (y <- 0 to 6){
    for (x <- 0 to y){
      setPos(x,y-x);
      ecritMessage(count);
      count+=1;
    }
    
    
    
 
  }
}