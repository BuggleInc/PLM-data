def goAndGet() {
  var pas:Int = 0
  while (!estSurBiscuit()) {
    avance()
    pas += 1
  }
  prendBiscuit()
  for (i <- 1 to pas) {
    recule()
  }
  poseBiscuit()
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}