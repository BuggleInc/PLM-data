def carre(){
  for (i <- 0 to 3){
    avance(20)
    droite(90)
  }
}

override def run():Unit= {
  avance(20)
  gauche(45)
  carre()
  leveCrayon()
  droite(45)
  avance(28)
  baisseCrayon()
  avance(20)
  gauche(90)
  avance(20)
  droite(45)
  avance(20)
  droite(135)
  avance(20)
  gauche(45)
  avance(20)
  droite(90)
  avance(20)
  gauche(45)
  avance(20)
  droite(135)
  avance(20)
  droite(45)
  avance(20)
}