override def run() {
}
def marque() = {
  baisseBrosse()
  leveBrosse()
}

def diag(c:Color) = {
  setCouleurBrosse(c)
  avance()
  marque()
  gauche();avance();gauche();avance();
  marque()
  gauche();avance();gauche()
}

def motif() = {
  diag(Color.RED)
  
}