def testg():Boolean= {
	var x:Boolean=false;

	gauche();
	if (estFaceMur) x= true;
	droite();
	return x
}

override def run() {
	baisseBrosse();
	while (!estFaceMur && getDirection!=Direction.EST && testg== false) {
		avance(getMondeHauteur-1)
		gauche();
		avance();
		droite();
		recule(getMondeHauteur-1);
		gauche();
		avance();
		droite();
	}
}
