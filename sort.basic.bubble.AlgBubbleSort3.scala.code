def bubbleSort3() {
  var swapped:Boolean=true
  var len:Int=getValueCount();
  while (swapped) {
    swapped=false
    for(i <- len-2 to 0 by -1) {
      for (j <- 0 to i) {
        if (!isSmaller(j,j+1)) {
          swap(j,j+1)
          swapped=true
        }
      }
    }
  }
}