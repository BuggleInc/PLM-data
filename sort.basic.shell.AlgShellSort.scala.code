def shellSort()  {
  var lgr=getNombreValeurs()
  var ecart=lgr/2
  
  while (ecart>0){
    
  }
/*  
  for (i<- 1 to getNombreValeurs()-1){
    var v=getValeur(i)
    var k=i
    while (k>=1 && !plusPetitQue(k-1,v)){
      copie(k-1, k)
      k-=1
    }
    setValeur(k, v)
  }  */
}