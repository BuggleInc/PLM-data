def hunt() {
def isFacingTrail():Boolean = {
	if (estFaceMur) {
		return false
	}
    
avance(); getCouleurSol(); 
    if (getCouleurSol() == Color.green) { recule();
    	return true
    } else{ recule();
    	return false
    }
}      

 do {
 	avance();
 } while (isFacingTrail() == true)
 }
}

// Copy your isFacingTrail() here
