def bubbleSort()  {
  var inutile = true;
  while(traverse())
  	inutile = true;
}


def traverse() : Boolean = {
   var swapped = false;
   for (i <- 0 to getValueCount() - 2){
     if(isSmaller(i+1, i)){
   		 swap(i,i+1);
       swapped = true;
	   }
   }
   return swapped;
}