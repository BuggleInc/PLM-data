while not sortieTrouvee() :
    seen = 0
    within = False
    
    while not within or not croisement():
        within = True
        forward()
        if isOverBaggle():
            seen += 1
    
    if seen > 2:
        gauche()
    else:
        droite()
avance()