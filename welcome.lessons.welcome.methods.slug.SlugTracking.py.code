def isFacingTrail():
    forward()
    if getCouleurSol()==Color.green:
        return(False)
        
    
 if isFacingWall():
      return False
   else:
      forward()
      res = (getGroundColor() == Color.green)
      backward()
      return res