override def run() {
  while(!estSurBiscuit){
    var rng:Int = random3()
    if (rng == 0){
      if (!estFaceMur){
        avance()
      } else if (rng == 1){
        droite()
      } else {
        gauche()
      }
    }
  }
}