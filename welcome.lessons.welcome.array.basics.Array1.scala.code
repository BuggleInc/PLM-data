override def run() {
var T = new Array[Color](getMondeHauteur)
for (i<-0 to T.length-1) {
	T(i)=getCouleurSol
	avance
	gauche
	avance
	droite
} // récupère le motif
for (nb<-1 to getMondeLargeur) {
	for (nb2 <-0 to T.length-1) {
		avance
		setCouleurBrosse(T(nb2))
		baisseBrosse
		leveBrosse
	}
}//le dessine
}