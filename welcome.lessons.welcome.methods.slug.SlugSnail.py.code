def isFacingTrail(color):
   # write your code here
    if estFaceMur():
        return False
    else:
        forward()
        res = (getCouleurSol() == color)
        backward()
        return res
        
def hunt(color):
    while estSurBiscuit() != True:
        if isFacingTrail(color) :
            forward()
            baisseBrosse()
            leveBrosse()
        else:
            left()
    prendBiscuit()
