def haveBaggle():
    seenBaggle = False
    #steps = 0
    while isFacingWall() == False:
        forward()
        if isOverBaggle() == True:
            seenBaggle = True
        #steps = steps + 1
    backward(6)
    return seenBaggle
    
