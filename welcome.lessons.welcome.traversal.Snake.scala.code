override def run() {
for(i<-0 to 6 ) 
baisseBrosse();
getDirection
 while(!estFaceMur) {  avance(6); 
if(getDirection()==Direction.EST) { gauche(); avance(); gauche();}
else  { droite();avance();droite();}
{}

 } 
}
