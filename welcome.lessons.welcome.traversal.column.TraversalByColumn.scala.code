def fin():Boolean = 
{
  if (!estDosMur())
  	return false
  
  var ok = false
  droite()
  
  if (estFaceMur())
  	ok = true
  
  gauche()
  return true
}

override def run() {
  var idCase = 0
  
  while (!fin())
  {
  	ecritMessage("" + idCase)
  	setPos(getX(), getY() - 1)
  }
}