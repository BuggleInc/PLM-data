def haveBaggle():Boolean = {
  var vuBiscuit : Boolean = false
  for (nbPas <- 1 to 6) {
    if (estSurBiscuit())
      vuBiscuit = true
  }
  for (compteur <- 1 to 6) {
    recule()
  }
  return vuBiscuit
}
