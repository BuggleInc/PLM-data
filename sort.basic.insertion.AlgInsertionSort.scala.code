def insertionSort() {
  var len = getNombreValeurs()
  for (i <- 1 to len-1) {
    var v = getValeur(i)
    var j = 0
    var echanger = false
    while(i-j>=0 && echanger == false) {
      if (!plusPetitQue(i-j,v)) {
        copie(i-j,i)
        echanger = true
      };j+=1
    }
  }

}