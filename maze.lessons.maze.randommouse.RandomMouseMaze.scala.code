override def run() {
    var r = 0;
    while (!(estSurBiscuit())) {
    	r = random3();
    	if (r==0) {
    		if (!(estFaceMur)) {
    			avance();
    		}
    	}
    	else if (r==1) {
    		gauche();
    	} else {
    		droite();
    	}
    }
    prendBiscuit();
}
