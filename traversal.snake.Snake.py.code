def gagne(x,y) :
  return x==0 and y==0 


def virage() :
  left()
  avance()
  left()

while not gagne(getX(),getY()) :
	
  	while not estFaceMur() :
      avance()
    if estFaceMur() :
      virage()
    