def posNeg(a:Int, b:Int, negative:Boolean):Boolean = 
{
	if((a<0 && b>0)||((b<0 && a>0)))
	{
		return true
	}

return negative
}


/*
 (a,b) match {
 case (a,b) if { a<0 || b>0
 
 */







/* Étant donné deux entiers, retournez vrai si l'un d'entre eux est positif et l'autre négatif.
Sauf si le paramètre négative est vrai, auquel cas il faut que les deux nombres soient négatifs.*/

			