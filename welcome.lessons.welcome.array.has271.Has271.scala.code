def has271(nums:Array[Int]): Boolean = {
  var l:Int=nums.length
  for (j<-0 to l-3){
    if ((nums(j)+5==nums(j+1)) && (nums(j+2)>=nums(j)-3) && (nums(j+2)<=nums(j)+1)){
      return true
    }
  }
  return false
}
