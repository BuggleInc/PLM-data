def Moria() = {
  gauche(); gauche(); avance();
  while (!estFaceMur) {
    avance();
    if (estSurBiscuit) {
      prendBiscuit();
      gauche(); gauche();
      while (!estSurBiscuit()) {
        avance()
      }
      recule();
      gauche(); gauche();
      poseBiscuit();
    }
  }
}