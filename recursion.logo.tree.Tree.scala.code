def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  while(steps=!){avance(length:Double)
            right(angle:Double)
            length=length*shrink
            steps=steps-1}
}