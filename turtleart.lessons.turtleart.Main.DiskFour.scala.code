override def run() {
	for (c<-1 to 4) {
		c match {
			case 1 => setCouleur(Color.black)
			case 2 => setCouleur(Color.red)
			case 3 => setCouleur(Color.orange)
			case 4 => setCouleur(Color.magenta)
		}
		for (i<-1 to 90) {
			avance(100)
			recule(100)
			droite(1)
		}
	}
}
