override def run() {
baisseBrosse()
var tab = new Array[Color] (6)
for (i<- 0 to 5 ) {tab(i) = getCouleurSol();avance()} 
for (h<-0 to 5 ) {gauche();avance();droite();
for (j<- 0 to 5 ) { setCouleurBrosse(tab(j-h));avance()}}
}
