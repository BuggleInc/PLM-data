def cigarParty(cigars:Int, isWeekend:Boolean):Boolean = {
	(cigars,isWeekend) match {
		case (x,y) if (isWeekend==false && cigars>=40 && cigars<=60) => return true
		case (x,y) if (isWeekend==true && cigars>=40) => return true
		case (_,_) => return false
	}
}
