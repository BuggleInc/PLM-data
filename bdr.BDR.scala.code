while(estSurMessage())
{
  var lettre = litMessage();
  if (lettre=="R") droite();
  else if (lettre=="L") gauche();
  else if (lettre=="I") retourne
  else if (lettre=="A") avance
  else if (lettre=="B")
  {
    avance();
    avance();
  }
}