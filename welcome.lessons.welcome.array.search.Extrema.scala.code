def extrema(nums:Array[Int]): Int = {
  var max=nums(0)
  for (i<- 1 to nums.length-1) {
    if (nums(i)>max) {max=nums(i)}
  }
  var min=nums(0)
  for (i<- 1 to nums.length-1) {
    if (nums(i)<max) {max=nums(i)}
  }
  return max-min
}