def cocktailSort()  { 
                     val n = getNombreValeurs() -2
                     for (j <- 0 to n) {
                     for (i <- 0 to n) { if (plusPetit(i,i+1)) {echange(i,i+1)}
                                       }
                                             
                                }
                   }