var sommeAngle = 0

def keepHandOnSideWall = {gauche();sommeAngle+=1; if (!estFaceMur) {avance()} else {while(estFaceMur()) {droite;sommeAngle-=1};avance()} }

override def run() { 
 
  
  
  while(getY!=0) { keepHandOnSideWall;setDirection(Direction.NORTH);while (!estFaceMur) {
    avance()} }
  
 while (!estSurBiscuit()) {keepHandOnSideWall}; prendBiscuit()
}