def bubbleSort()  {
  var notSorted = true;
  while (notSorted) {
    notSorted = false;
    for (i <- 0 to getNombreValeurs()-2) {
      if (plusPetit(i+1,i)) {
        echange(i,i+1);
        notSorted=true;
      }
    }
  }
}