def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {

for(i <- 0 to steps)
{
spiral(steps-1*i, angle:Int, length+3*i, increment:Int);
gauche(90);
avance(steps);
}


spiral(5, 90, 0, 3);

}
