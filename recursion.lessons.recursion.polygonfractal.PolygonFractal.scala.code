def polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double) 
{
	if(levels==1)
	{
		avance(length)
		droite(180-(360/sides))
		avance(length)
		droite(180-(360/sides))
		avance(length)
		droite(180-(360/sides))
		avance(length)
		droite(180-(360/sides))
		avance(length)
		droite(180-(360/sides))		
	}
}
