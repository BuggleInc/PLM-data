override def run() { 
  doTheThing()
}

def doTheThing() {
  baisseBrosse()
  pasDeSerpent()
  while (!estFaceMur()) {
    avance()
    gauche()
    if (estFaceMur())
    	gauche(); gauche()
    pasDeSerpent()   
  }
  
}
def pasDeSerpent() {
  while (!estFaceMur()) {
    avance()
  }
  setDirection(Direction.NORTH)
}