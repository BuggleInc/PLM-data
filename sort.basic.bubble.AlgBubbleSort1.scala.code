def bubbleSort()  {
  var B : Boolean = true
  var n = getNombreValeurs()
  var rg = 0
	for (i <- 0 to n-2 ){
      rg=i
      while (rg>0) {
        if (plusPetit(rg+1,rg)) {
      		echange(rg,rg+1)
          rg-=1
        }
   
      }
   }
}