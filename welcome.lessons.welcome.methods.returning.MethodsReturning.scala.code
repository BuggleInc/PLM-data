def haveBaggle():Boolean = {
  var vuBiscuit:Boolean = false
  for (compteur <- 1 to 6) {
    if (estSurBiscuit) {
      
      vuBiscuit = true
    	avance()
     	} else {
       avance()
     	}
	}
  for (i <- 1 to 6) {
    recule()
  }
  return vuBiscuit
}