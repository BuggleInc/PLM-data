var i:Int = 0;

while(!isFacingWall()) {
	forward();
}

if(isFacingWall() && getDirection() == Direction.EAST) {
	for(i <- 1 to 8) {
		left();
		forward();
		right();
		forward();
	}
	
	for(i <- 1 to 3) {
		forward();	
	}
}

