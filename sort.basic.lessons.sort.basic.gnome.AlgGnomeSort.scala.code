def gnomeSort() {
  var length : Int = getValueCount();
  var i : Int = 0;
  
  while(i<length)
  {
    if(isSmaller(i+1,i))
    {
      Swap(i,i+1);
      i-=1;
    }
    else
    {
      i+=1;
    }
  }
}