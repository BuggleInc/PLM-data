override def run() {
  var a:Int = 0
  while (!estSurBiscuit()) {
    a=random3()
    if ((a==0) && (!estFaceMur)) {
      avance()
    } else if (a==1) {
      droite()
    } else if (a==2) {
      gauche()
    }
  }
  prendBiscuit()
}