public void run() {
	left();
	left();	
	while (isFacingWall() == false) {
		while (isOverBaggle() == false) {forward();}
			pickupBaggle();
			
			while (isCarryingBaggle()== true && isOverBaggle() == false) {
			backward();}
			forward();
			dropBaggle();
			
			forward();
			
		}
	
}
	/* isFacingWall() isOverBaggle(), isCarryingBaggle(), pickupBaggle() or dropBaggle().*/