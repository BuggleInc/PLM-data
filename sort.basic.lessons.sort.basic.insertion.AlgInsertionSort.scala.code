def insertionSort() {
  var valeur = 0;
  var test = 0;
  for (i <- 1 to getNombreValeurs-1){
   	valeur = getValeur(i);
    test = i-1;
   	while (test>0 && getValeur(test)>valeur){
      	setValeur(test,getValeur(test-1));
      	test=test-1;
    }
    setValeur(test+1,valeur)
  }
}