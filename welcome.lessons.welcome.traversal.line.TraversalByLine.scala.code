override def run() {
  droite()
  while( ! positionFinale()){
    ecritMessage( ""+(getY() + getX()*7))
    parcours()
  }
  ecritMessage("48")
  
}


def positionFinale(): Boolean ={
  return getMondeHauteur()-1 == getY() && getMondeLargeur()-1 == getX() 
}

def parcours(){
  if (estFaceMur()){
    setPos(0 , getY()+1)
  } else{setPos(getX()+1, getY())}
}