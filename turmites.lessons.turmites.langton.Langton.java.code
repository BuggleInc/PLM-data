public void step() {
	Color c=getGroundColor();
	if(c.equals(Color.black)){
		setBrushColor(Color.white);
		brushDown();
		brushUp();
	}else{
		setBrushColor(Color.black);
		brushDown();
		brushUp();
	}
}
