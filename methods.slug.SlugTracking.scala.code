def isFacingTrail():Boolean = {
    // Write your code here
 brushDown()
  do {

    forward()
    backward()
    if (getGroundColor() != Color.green) {
      left()
    }
    if (isFacingWall()){
      left()
    }
  }while (isOverBaggle() == false)
  return true
}