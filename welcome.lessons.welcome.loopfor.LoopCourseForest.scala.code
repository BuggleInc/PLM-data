for (i <- 1 to 28) {
	for (tmp1 <- 1 to 4) { avance(); }
	gauche();
	for (tmp2 <- 1 to 2) { avance(); }
	droite();
	for (tmp3 <- 1 to 4) { avance(); }
	droite();
	for (tmp4 <- 1 to 2) { avance(); }
	gauche();
	for (tmp5 <- 1 to 4) { avance(); }
	gauche();
}

