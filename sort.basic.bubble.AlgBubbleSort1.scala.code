var isover : Boolean = true
def bubbleSort()  {
  while (isover==true){
    isover = false
    for (i <- 0 to getValueCount()-2 ){
      if (isSmaller(i, i+1) == false){
        swap(i, i+1)
        isover = true
      }
    }
  }
}