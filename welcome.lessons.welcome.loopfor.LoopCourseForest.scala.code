for (i <- 1 to 28) {
  
  for (k <- 1 to 4) {
    avance();
  };
  
  gauche();
  for (j <- 1 to 2) {
    avance();
  };
  
  droite();
  for (k <- 1 to 4) {
    avance();
  };
  
  droite();
  for (l <- 1 to 2) {
    avance();
  };
  
  gauche();
  for (m <- 1 to 4) {
    avance();
  };
  gauche();
}