def gnomeSort() {
	val n:Int = getNombreValeurs()

	def Sort(pos:Int) {
		pos match {
			case 0 => Sort(pos+1); return
			case n => return
			case _ =>
		}
		if (plusPetit(pos, pos-1))
			{echange(pos, pos-1); Sort(pos-1); return}
		else {Sort(pos+1); return}
		
	}
	
	Sort(0)
}
