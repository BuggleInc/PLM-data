def hunt() {
    while (!isFacingTrail()){
      gauche();
    }
		avance();
    leveBrosse();
    }
}

def isFacingTrail():Boolean = {
  if (estFaceMur()){
    return false;
  }
  avance();
  if (getCouleurSol() == Color.green) {
    recule();
    return true;
  }
  recule();
  return false
}
