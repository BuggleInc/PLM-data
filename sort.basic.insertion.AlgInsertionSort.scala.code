def insertionSort() {
  var v=0
  for (i<-1 to getNombreValeurs()-1) {
    v=i
    copie(i-1:Int,i:Int)
    for (k<-i-1 to 0 by -1) {
      if (getValeur(k:Int)>getValeur(v:Int)) {
        copie(k-1:Int,k:Int)
      }
    }
    copie(v:Int,getNombreValeurs()-2)
  }
}