def insertionSort() {
  var v = 0
  for (i<- 1 to getNombreValeurs()-1){
    v = getValeur(i)
    var j = i-1
    do{
      copie(j,j+1)
      j-=1
    }while(getValeur(j)>v && j>0)
    setValeur(j+1,v)
  }
}