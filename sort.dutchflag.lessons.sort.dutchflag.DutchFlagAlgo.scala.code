def solve() {
  for (i<-0 until getSize()){
    for(j<-i until getSize()){
      if ((getColor(j)==BLUE && getColor(i)== WHITE) || (getColor(j)==BLUE && getColor(i)==RED) || (getColor(j)==WHITE && getColor(i)==RED)){
        swap(i,j)
      }
    }
  }
}
