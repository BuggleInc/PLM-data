override def run() {
	var T = Array(Color.red, Color.cyan, Color.green, Color.magenta, Color.orange, Color.pink)
	avance()
	gauche()
	avance()
	droite()
	for (i <- 1 to 6){
		setCouleurBrosse(T(i))
		baisseBrosse()
		leveBrosse()
	}
}
