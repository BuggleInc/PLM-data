def gnomeSort() 
{
	var pos=getNombreValeurs()-1
	while(pos!=0)
	{
		if(pos==getNombreValeurs()-1)
			pos=pos-1
		else
		{
			if(plusPetit(pos-1,pos+1))
				pos=pos+1
			else
			{
				echange(pos-1,pos+1)
					pos=pos-1
			}
		}
	}
}
