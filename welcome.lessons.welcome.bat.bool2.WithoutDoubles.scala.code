def withoutDoubles(die1:Int, die2:Int, noDoubles:Boolean):Int = {
  if (!noDoubles){return die1 + die2}
  else if (die1 == 6 && die2 == 6){return 7}
  else if (die1==die2){return die1*2+1}
  else {return die1+die2}
  
}