def hunt(c:Color) {
  while(!estSurBiscuit()){
    if (isFacingTrail()){
      avance()
      
    }
    else {
      gauche()
      if (isFacingTrail()){
        avance()
    baisseBrosse()
    leveBrosse()
      }
    }
  }
  prendBiscuit()
}

def isFacingTrail():Boolean = {
  var k=false
  var c=getCouleurSol()
  if (!estFaceMur()){
    avance()
    if (getCouleurSol()==c){
      k=true
    }
    recule()
  }
  return k 
}