def insertionSort() {
  
  val lgr = getNombreValeurs()
  var continue = true
  var a = 1
  
  for (i <- 1 to lgr-1){
    var stockeur = getValeur(i)
    a = 1
    
    while(plusPetit(i,i-a)){
 			
      copie(i-a,i-a+1)
      a+=1
      
    }
    setValeur(i-a, stockeur)
  }
}