var nbBaggle=0
avance();
avance();
avance();
avance();
avance();
while (!sortieTrouvee()) {
  avance();
  if (nbBaggle<3) {
    droite();
  } else {
    gauche();
    for (i<-0 to 3) {
      avance();
    }
  }
}
