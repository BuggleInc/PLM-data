def indexOfValue(nums:Array[Int] ,lookingFor:Int): Int = {
  val n:Int=nums.length
  var cpt:Int =0
 
  for (i<-0 to n-1){
    if (lookingFor == nums(i)){
      cpt = cpt+1
      return i
    }
  }
  if (cpt == 0){
    return -1
  }
}