override def run() {
  ecritMessage("0")
  var cpt = 0
  while (!posFinale()) {
    	posSuivante()
    	cpt += 1
    	ecritMessage(cpt)
    	
  }
}


def posFinale() : Boolean = {
  return ((getMondeLargeur() == getX()) && (getY() == getMondeHauteur()))
}



def posSuivante() {
  if (getY() == getMondeHauteur()) {
    	setPos((getX()+1), 0)
  }
  else {
  		setPos(getX(), (getY()+ 1))
  }
}