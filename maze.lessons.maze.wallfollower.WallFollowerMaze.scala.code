override def run() {
  while(!estSurBiscuit()){
    keepHandOnSideWall()
  }
  prendBiscuit()
}


def keepHandOnSideWall(){
  if (!estFaceMur()){
    gauche()
    if (estFaceMur()){
      droite()
      if (estFaceMur()){
        droite()
        if (estFaceMur())
        	droite()
      }
    }
    avance()
  }
  else{
    gauche()
    if (!estFaceMur())
    	avance()
  }
}