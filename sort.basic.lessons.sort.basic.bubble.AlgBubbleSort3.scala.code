def bubbleSort()  {
  var sorting:Boolean = true;
  var a = 2;
  
  while (sorting == true) {
    sorting = false;
    
  	for (i <- 0 to getValueCount()-a ) {
      
      if (! (isSmaller(i, i+1)))  {
        swap(i, i+1);
        sorting = true;
      }
    }
    
    a += 1;
  }
}