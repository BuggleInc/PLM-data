def spiral(steps:Int, angle:Int, length:Int, increment:Int)   { steps match { 
                          case 0 => avance (0.0)
                            case _ => {avance (steps) ; 
                                       gauche(angle) ; 
                                       spiral (steps-1, angle, length+3, increment+1)}
}
                                                                        

}