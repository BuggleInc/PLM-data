def extrema(nums:Array[Int]): Int = {


	var maxVal = nums(0);
	var minVal = nums(0);
	
	for(i <- 0 to nums.length - 1)
	{
		if(nums(i) > maxVal)
		{
			maxVal = nums(i);
		}
		if(nums(i) < minVal)
		{
			minVal = nums(i);
		}
	}
	return maxVal - minVal;
}
