def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
	if (steps != 1){
	avance (length);
	droite(angle);
	tree(steps-1, length*shrink, angle, shrink)
	}else{
		leveCrayon();
		gauche(angle);
		recule(length);
		gauche(angle);
		baisseCrayon()
		avance(length);
	}
	
}
