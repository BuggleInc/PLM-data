def sierpinski(level:Int, length:Double) {
  if (levels==0){
    avance(length)
  droite(60)
  avance(length)
  droite(60)
  avance(length)
  droite(120)
  }
  avance(length)
  droite(60)
  avance(length)
  droite(60)
  avance(length)
  droite(60)
  sierpinski(level-1,length/2)
  
}