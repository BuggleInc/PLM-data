nDiag=0
n=0
while getX() != getWorldWidth()-1 or getY() != getWorldHeight()-1:
    if nDiag <getWorldHeight()-1:
        for i in range(nDiag):
            setPos(getX()+i,getY()-i)
            writeMessage(n)
            n+=1
        nDiag += 1
        setPos(getX()-nDiag+1,getY()+nDiag)
        