def bubbleSort()  {
  val n = getNombreValeurs()
  var swap:Boolean = false
  while (!swap) {
    swap = false
    for (i <- 0 to n-2) {
    	if (plusPetitQue(i,i+1)) {
     		echange(i,i+1);
     		swap = true
    	}
  	}
  }
}