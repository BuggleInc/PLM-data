def withoutDoubles(die1:Int, die2:Int, noDoubles:Boolean):Int = {
	if ((die1==die2) && (noDoubles)) {
		var die11 = die1
		var die22 = die2
		if (die1==6) { 
			die11 = 1
		}
		if (die2==6) {
			die22 = 1
		}
		return (2*die11 + 1);
	}
	return (die1 + die2);
}