def cocktailSort2() {
  var swapped= false
  var j=getValueCount()-2
  var k=getValueCount()-2
  do {
    swapped=false
    for (i <- 0 to j)
      if (! isSmaller(i,i+1)){
      swap(i,i+1)
        swapped=true;
        
    }
    j=j-1
    for (i<- k to 0 by -1)
      if (! isSmaller(i,i+1)){
        swap(i,i+1)
        swapped=true;
       
      }
   k=k+1 
   
  }  while (swapped);
}
