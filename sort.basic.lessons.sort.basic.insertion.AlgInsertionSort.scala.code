def insertionSort() {
  for(i <- 0 to getNombreValeurs()-1){
    var v = getValeur(i);
    var j = i;
    while(plusPetit(v,j-1) && j!=0){
      copie(j-1,j);
      j -= 1;
    }
    setValeur(j,v);
  }
}