override def run() {
}

def doTheSnakyThing() {
  var compt:Int = 0
  
  pasDeSerpent()
  while ((getX(), getY()) != (getMondeLargeur() - 1, getMondeHauteur() - 1)) {
    avance()
    if (estFaceMur()) {
    	gauche()
    	gauche()
    }
    pasDeSerpent()   
  }
  droite()
}

def pasDeSerpent() {
  while (!estFaceMur()) {
    avance()
  }
  setDirection(Direction.NORTH)
}