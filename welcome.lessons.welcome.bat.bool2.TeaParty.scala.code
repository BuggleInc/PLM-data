def teaParty(tea:Int, candy:Int): Int = {
  (tea, candy) match{
    case(_,i) if i<5 => return 0
    case(i,_) if i<5 => return 0
    case(i,j) if 2*i >= j => return 2
    case(j,i) if 2*i >= j => return 2
    case(_,_) => return 1
  }
}