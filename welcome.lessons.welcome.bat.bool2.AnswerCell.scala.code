def answerCell(isMorning:Boolean, isMom:Boolean, isAsleep:Boolean):Boolean = {
	if (!isAsleep && !isMorning) {
		return true
	} else {
		if (!isAsleep && isMom) {
			return true
		} else {
			return false
		}
	}
}