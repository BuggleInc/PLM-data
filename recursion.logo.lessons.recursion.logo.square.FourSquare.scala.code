override def run() {
  FaitCarre1()
  for (i<-1 to 3){
    FaitCarre2()
  }
}

def FaitCarre1(){
  baisseCrayon()
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
  leveCrayon()
  droite(180)
  avance(100)
  droite(90) 
}

def FaitCarre2(){
  baisseCrayon()
  
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
  droite(180)
  avance(100)
  leveCrayon()
  droite(90) 
}
