public void run() {
	int count = 0;
	int initY = 0;
	while (initY < getWorldHeight()) {
		int x = 0;
		int y = initY;
		while (x <= getWorldWidth() && y >= 0){
			setPos(x,y);
			writeMessage(count);
			x++;
			y--;
			count++;
		}
		initY++;
	}

	int initX = 1;
	while (initX < getWorldWidth()){
		int x = initX;
		int y = getWorldHeight() - 1;
		writeMessage(y);
		while(x <= getWorldWidth() && y >= getWorldHeight() - x + 1){
			setPos(x,y);
			//writeMessage(x + " " + count);
			x++;
			y--;
			count++;
		}
		initX++;
	}
}