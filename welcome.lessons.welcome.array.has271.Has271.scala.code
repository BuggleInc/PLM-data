def has271(nums:Array[Int]): Boolean = {
  
  var truc:Boolean =false
  
  for(i<- 0 to nums.length-3){
    
		if (nums(i)+5==nums(i+1) && Math.abs(nums(i+2)-nums(i)+1)<=2){truc=true}
    
  }
  
  return truc
}