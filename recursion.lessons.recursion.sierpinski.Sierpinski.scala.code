def sierpinski(level:Int, length:Double) {

	if (level!=0) {
		sierpinski(level-1, length/2)
		avance(length/2)
		sierpinski(level-1, length/2)
		droite(60)
		avance(length/2)
		droite(45)
		sierpinski(level-1, length/2)
	} else {
		for (i<-1 to 3) {
			avance(length)
			droite(120)
		}
	}
	
}
