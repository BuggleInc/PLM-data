def bubbleSort()  
{
var estRange = false
while(estRange == 1)
{
	estRange = true
	for( i<- 0 to getNombreValeurs()-2 )
	{
		if(getValeur(i) > getValeur(i+1))
		{
			echange(i,i+1)
			estRange = false
		}
	}
}
}
