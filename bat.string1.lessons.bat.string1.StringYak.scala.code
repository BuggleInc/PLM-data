def stringYak(str:String):String = {
  
  var sortie:String = "";
  
    var i:Int = 0;
    while(i<=str.length-3){
			if(str.charAt(i)=='y' && str.charAt(i+1)=='k'){
        i+=2;
      }
      i+=1;
    }
  
  return sortie;
}