override def run() {
 baisseBrosse()
 while (arrive() == false) 
 {
 	if (!estFaceMur())
 	  {avance()}
 	 		
 }
}


def arrive() {
	if (estFaceMur())
		{ if (getDirection() == Direction.EST)
			  {gauche()
			    if (estFaceMur())
			     { return true
			   droite()}}
			else { if (getDirection == Direction.OUEST)
					 {droite()
					 estFaceMur()
					 gauche}}
		}
	else return false
}
