override def run() {
  var reached=false;
  while (getY()!=0 && reached!=true) {
    if (isFacingWall()) {
      right()
      if (!isFacingWall()) {
        forward();left()
      }
      else {
        left();left();forward();right()
      }
    }
    else {
      forward()
    }
  }
  reached=true
  while (!isOverBaggle()) {
    left()
    if (!isFacingWall()) {
      forward()
    }
    else {
      right();
      if (isFacingWall()) {
        right()
        if (isFacingWall()) {
          right()
          forward()
        }
        else {
          forward()
        }
      }
      else {
        forward()
      }
    }
  }
  pickupBaggle()
}