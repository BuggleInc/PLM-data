def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
	if( steps==0){
		return;
	}
	else{
		avance(length);
		droite(angle);
		tree(steps,length,angle,3);
		gauche(2*angle);
		tree(steps,length*shrink,angle,3);
				
		}
}
