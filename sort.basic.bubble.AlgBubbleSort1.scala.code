def bubbleSort()  {
  while(traverse())
  	var inutile =  true
}


def traverse() = {
   swapped = false
   for (i <- 0 to getValueCount() - 2){
     if(isSmaller(i, i+1)){
   		 swap(i,i+1);
       swapped = true;
   }
   return swapped;
  
}