def solve() {
  val n = getTaillePile()
  var max = 0
  var idMin = 0
  for(i <- n-1 to 0 by -1) {
    idMin = i
    max = getRayonCrepe(i)
    for(j <- n-1 to i by -1) {
      if(max<getRayonCrepe(j)) {
        idMin = j
        max = getRayonCrepe(j)
      }
    }
    retourne(idMin+1)
  }
}
