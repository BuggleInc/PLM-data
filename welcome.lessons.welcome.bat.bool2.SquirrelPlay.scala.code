def squirrelPlay(temp:Int, isSummer:Boolean):Boolean = {
	var x:Boolean=false;

	if (isSummer==false) {
		if (temp>=60&&temp<=90) {
			x=true
		}
	}
	else {
		if (temp>=60&&temp<=90) {
			x= true
		}
	}
	return x
}
