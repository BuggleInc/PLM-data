def bubbleSort2() {
  val n = getNombreValeurs()
  var i = n - 1
  var nbPassage = 0
  while (nbPassage != n) {
    i += 1
    for (j <- 0 to i - 1) {
      if (getValeur(j+1) < getValeur(j)) {
        echange(j+1,j)
      }
    }
    nbPassage += 1
  }
}