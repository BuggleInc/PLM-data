def bubbleSort3(){	
	var i:Int = getNombreValeurs() ;
	var hasSwap:Boolean = false ;
	
	while(i !=0)
	{
		for (j <- i-1 to 1 by -1)
			{
				hasSwap = false;
				if (plusPetit(j,j-1))
				{
					echange(j-1,j);
					hasSwap = true;
				}
			}
		i = i-1;
		if (!hasSwap)
			return;
		
	}
	
}