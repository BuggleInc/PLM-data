def gnomeSort() {
  var nain=0
  while(nain!=getNombreValeurs()-1) {
    if (plusPetit(nain,nain+1)) {nain+=1}
    else if (nain!=0) {echange(nain,nain+1);nain+=1}
    else {echange(nain,nain+1);nain-=1}
  }
}