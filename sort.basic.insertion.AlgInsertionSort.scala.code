def insertionSort() {
  var j=1
  var v=0
  var a= getNombreValeurs()
  for ( i<-1 to a-1){
    v=getValeur(i);j=i-1
    while(j>=0 && getValeur(j-1)>v){
      setValeur(j,getValeur(j-1))
      j-=1
    }
  setValeur(j,v)  
  }}
  