override def run() {var x:Int=0; var y:Int=0; var i:Int=0; var j:Int=0; x=getWorldWidth(); y=getWorldHeight(); var count:Int=0; for (i<-0 to (x-1)) {for (j<-0 to (y-1)) {writeMessage(count); setPos(i,j); count=count+1}}
}