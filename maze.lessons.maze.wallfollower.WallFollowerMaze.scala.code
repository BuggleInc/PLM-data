override def run() {
    def keepHandOnRightWall() = {
	droite();
	if (estFaceMur) {gauche()} else {avance()}
    }
    while (!(estSurBiscuit)) {
    	keepHandOnRightWall();
    	if (!estFaceMur) {avance()};
    }
    prendBiscuit();
}
