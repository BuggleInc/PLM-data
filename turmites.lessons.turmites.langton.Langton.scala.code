def step() {
  if (getCouleurSol()==Color.white) {
    baisseBrosse()
    setCouleurBrosse(Color.black);droite();avance()
    leveBrosse()
  }
  if (getCouleurSol()==Color.black) {
    baisseBrosse()
    setCouleurBrosse(Color.white);gauche();avance()
    leveBrosse()
  }
}