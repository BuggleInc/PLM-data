public void run() {
int hauteur=getMondeHauteur();
Color[] T=new Color[hauteur];
	for (nCase=0; nCase<hauteur; nCase=nCase+1) {
		Color couleur=getCouleurSol();
		T[nCase]=couleur;
		if (nCase<hauteur-1) {
			avance();
		}
	}
}
