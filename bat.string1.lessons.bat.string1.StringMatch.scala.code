def stringMatch(a:String, b:String):Int = {
  var n = Math.min(a.length,b.length)
  var c = 0 
  var b1:Boolean = false
  var b2:Boolean = false
  for (i <- 0 to n-1) {
    b2 = b1 && a.charAt(i) == b.charAt(i)
    b1 = a.charAt(i) == b.charAt(i)
    if (b2) {c=c+1}
  }
  return c }
