override def run() {
  makePolygone(20, 30)
}

def makePolygone(nbSides:Int, length:Int) {
  var angle = 360.0 / nbSides.toDouble
  
  for(i <- 1 to nbSides) {
    forward(length)
    right(angle)
  }
}