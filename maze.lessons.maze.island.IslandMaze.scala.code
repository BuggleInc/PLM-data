override def run() {
  var northfound=false
  while (!estSurBiscuit()) {
    if (getY()==0) {northfound==true}
    if (!northfound) {setDirection(Direction.NORTH); while (!estFaceMur()) {avance()}}
    println("test0")
    if (estFaceMur()) { println("test1")
      if (keepHandOnSideWall()) {
        println("test3")
        droite()
        if (estFaceMur()) {droite()}
        else {avance()}
      }
      else {
        println("test4")
        gauche()
        avance()
      }
    }
    else if (!estFaceMur()){ println("test2")
      if (keepHandOnSideWall()) {
        avance()
      }
      else {gauche();avance()}
    }
  }
  def keepHandOnSideWall(): Boolean = {
    gauche()
    if (estFaceMur()){droite();return true}
    else {droite();return false}
  }
  
  def findNorth() {
    getDirection() match {
      case Direction.NORD => if (!estFaceMur()) {avance()}
      case Direction.OUEST => droite()
      case Direction.SUD => retourne()
      case Direction.EST => gauche()
    }
  }
  
  prendBiscuit()
  
}