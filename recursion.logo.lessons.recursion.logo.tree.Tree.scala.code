def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if (steps ==0) {return}
  
  var pas = steps
  current(steps)
  avance(length)
  droite(angle)
  tree(steps-1,length*shrink,angle,shrink)
  gauche(angle)
  recule(length)
  pas+=1
  gauche(angle)
  tree(pas-1,length*shrink,angle,shrink)
  droite(angle)
  recule(length)
}