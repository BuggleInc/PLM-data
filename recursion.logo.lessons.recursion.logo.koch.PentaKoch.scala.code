def pentaKoch(levels:Int, length:Double) {
  if(levels!=0){
    squareSide(levels-1, length*0.4)
    gauche(108)
    squareSide(levels-1, lengt*0.4)
    droite(72)
    squareSide(levels-1, length*0.4)
    droite(72)
    squareSide(levels-1, length*0.4)
    droite(72)
    squareSide(levels-1, length*0.4)
    gauche(108)
    squareSide(levels-1, length*0.4)
    

  } else {
    avance(length)
  }
}