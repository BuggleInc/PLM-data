def sierpinski(level:Int, length:Double) {
	
		forward(length);
		right(120);
		sierpinski(level+1,length/2);	
		forward(length);
		right(120);
		sierpinski(level+1,length/2);	
		forward(length);
		right(120);
		sierpinski(level+1,length/2);	
		
}
