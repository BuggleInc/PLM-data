override def run() {
  while (!isOverBaggle()) {
    do {
	    keepWallOnLeft()
    } while (getDirection() != Direction.NORTH)
    
  	while (! isFacingWall()) {
    	forward()
  	}
    
  }
  pickupBaggle()
}


def keepWallOnLeft() {
  var a:Boolean = false
  left()
  a = isFacingWall()
  
  if (a) {
    right()
    if (!isFacingWall())
    	forward()
    else
    	right()
  } else {
    forward()
  }
}

def headNorth() {
  while (getDirection() != Direction.NORTH) {
    right()
  }
    
  while (! isFacingWall()) {
    forward()
  }
}