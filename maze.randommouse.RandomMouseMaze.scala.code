override def run() {
  var rng:Int = random3()
  while(!estSurBiscuit){
    rng = random3()
    if (rng == 0){
      if (!estFaceMur){
        avance()
      } else if (rng == 1){
        droite()
      } else if (rng == 2){
        gauche()
      }
    }
  }
  prendBiscuit()
}