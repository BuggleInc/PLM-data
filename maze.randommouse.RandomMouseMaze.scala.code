override def run() {
    while (!estSurBiscuit) {
      if (!estFaceMur) {
        avance()
        if (random3()==0) {
          avance()
        } else if (random3()==1) {
          droite();avance()
        } else {
          gauche();avance()
        }
      } else {droite()}
    }
} 