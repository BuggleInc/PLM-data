def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {
var comp = 0;	
comp = test2(a,comp)
comp = test2(b,comp)
comp = test2(c,comp)
comp = test2(d,comp)

return comp 
}

def test(a:Int):Boolean =
{
	if ( 12 < a && a < 20)
	{
		true
	}
	else 
	false
}

def test2 (a:Int, comp:Int): Int = 
{
	if (test(a))
	{
		comp = comp +1 
	}

	return comp
}
