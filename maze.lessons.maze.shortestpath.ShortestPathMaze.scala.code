override def run() {
	for (x <- 0 to getMondeLargeur()-1)
		for (y <- 0 to getMondeHauteur()-1)
			{if (aBiscuit(x,y))
				{val x0=x
				val y0=y
				setIndication(x0,y0,0)}
			}
	for (x <- (x0-1) to (x0+1) by 2)
		for (y <- y0-1 to y0+1 by 2)
			{setIndication(x,y,1)}






}
