def bubbleSort()  {
  var length : Int = getValueCount();
  var i : Int = 0;
  var tri : Boolean = true;
  
  while(tri)
	{
  	tri = false;
    for(i<-0 to length-2)
    {
      if(isSmaller(i+1,i))
      {
        swap(i,i+1);
        tri=true;
      }
    }
  }
    
}
