def averageValue(nums:Array[Int]): Int = {
  var rep = 0
  var somme = 0
  var last = nums.length -1
  for (i <- 0 to last){
    somme = somme + nums(i)
  }
  rep = somme/(nums.length+1) 
  return rep
}
