def answerCell(isMorning:Boolean, isMom:Boolean, isAsleep:Boolean):Boolean = {
  if (isMorning && isMom) {
    return true
  } else if (!isMorning){
    return true
  } else if (isAsleep){
    return false
  } else {
    return false
  }
}