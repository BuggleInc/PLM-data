override def run() {
  penDown();
	carre();
  forward(100);
  right(30);
  triangle();
  left(30);
  backward(100);
}

def carre() {
  for (i<-1 to 4) {
    forward(100);
    right(90);
  }
}

def triangle() {
  for(i<-1 to 3) {
    forward(100);
    right(120);
  }
}