def haveBaggle():Boolean = {
  var vuBiscuit=false
  for ( compteur1 <- 1 to 7) {
    for (compteur <- 1 to 6) {
      if (estSurBiscuit()) {
        vuBiscuit==true
      }
      avance()
    }
  for (compteure <- 1 to 6) {
    recule()
  }
     return vuBiscuit
  }
  droite()
  avance()
  gauche()
}