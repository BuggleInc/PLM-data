
override def run() {
  carre()
  carre()
  droite(180)
  avance(100)
  carre()
  carre()
  droite(180)
}

def carre(){
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
  droite(90)
  avance(100)
}

 