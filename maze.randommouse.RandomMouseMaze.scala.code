override def run() {
  
  while (!isOverBaggle()) {
    random3() match {
      case 0 => if(!isFacingWall()) forward();
      case 1 => left();
      case 2 => right();
    }
  }
  
  pickupBaggle();
}