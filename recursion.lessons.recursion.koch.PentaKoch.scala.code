def pentaKoch(levels:Int, length:Double) {
	if(levels==0){
		avance(length)
	else{
		pentaKoch(levels-1,length*0.4)
		gauche(120)
		pentaKoch(levels-1,length*0.4)
		droite(60)
		pentaKoch(levels-1,length*0.4)
		droite(60)
		droite(60)
		gauche(120)
		pentaKoch(levels-1,length*0.4)
	}
}
