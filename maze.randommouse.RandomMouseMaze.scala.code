override def run() {
  while (!estSurBiscuit) {
    if (!estFaceMur()) {
      avance()
    }
    if (estSurBiscuit()) { prendBiscuit(); return }
    if (random3()==1) {
      gauche()
    } else { droite() }
  }
}