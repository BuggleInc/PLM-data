def insertionSort() {
  var v=0
  for (i<-1 to getNombreValeurs()-2) {
    v=i
    for (k<-i-1 to 0 by -1) {
      if (plusPetit(k-1:Int,v:Int)) {
        copie(k:Int,k+1:Int)
      }
    }
    copie(v:Int,getNombreValeurs()-1)
  }
}