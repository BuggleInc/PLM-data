var cpt:Int=0;
while(!sortieTrouvee()){
	
		
	do{
	
		if(estSurBiscuit()){
			cpt=cpt+1;
		}
		if(croisement() && cpt==3){
			gauche()
			avance()
			droite()
			cpt=0
		}
		else if(croisement() && cpt==2 ){
			droite()
			avance()
			gauche()
			cpt=0
		}
		avance()
		
		
	
	}
	while(!croisement())
}
