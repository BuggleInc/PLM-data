def insertionSort() {
		for (i <- 1 to getNombreValeurs()-1) {
			var valeur = getValeur(i);
			var j = i;
			while ((j > 0) && (!plusPetitQue(j-1,valeur))) {
				copie(j-1,j);
				j-=1;
			}
			setValeur(j,valeur);
		}
}