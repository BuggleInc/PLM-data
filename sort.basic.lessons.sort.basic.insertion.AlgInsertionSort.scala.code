def insertionSort() {
  val taille=getNombreValeurs()
  var temp=0
  var minj=0
  for (i<-2 to taille-1) {
    temp=getValeur(i)
    
    for (j<-i to 1 by -1){
      if (getValeur(j-1)>temp) {
        copie(j-1,j)
      	minj=j
      }
    }
		setValeur(minj,temp)
    minj=i
  }
}