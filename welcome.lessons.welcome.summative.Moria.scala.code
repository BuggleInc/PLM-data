while ( !estFaceMur || estSurBiscuit() ){
  retourne();
  while ( !estSurBiscuit() ){
    if ( !estFaceMur() ){
      avance();
    }
  }
  prendBiscuit();
  retourne();
  while ( !estSurBiscuit() ){
    if ( !estFaceMur() ){
    avance();
    }
  }
  recule();
  poseBiscuit();
  recule();
  if ( estFaceMur() && !estSurBiscuit() ){
    droite();
    avance();
    gauche();
    avance();
  }
}
