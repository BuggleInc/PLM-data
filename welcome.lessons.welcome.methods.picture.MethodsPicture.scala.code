override def run() {
  for (i <- 1 to 9) {
    faireMotif()
  }
}

def faireMotif() {
 	faireV(Color.yellow);
 	gauche();
 	faireV(Color.red);
 	gauche();
 	faireV(Color.blue);
 	gauche();
 	faireV(Color.green);;
 	gauche();
  for (compteur <- 1 to 5) {
    avance();
  }
}

def faireV(c:Color) {
  setCouleurBrosse(c)
  avance()
  baisseBrosse()
  leveBrosse()
  avance()
  gauche()
  avance()
  baisseBrosse()
  leveBrosse()
  recule()
  droite()
  avance()
  baisseBrosse()
  leveBrosse()
  avance()
}