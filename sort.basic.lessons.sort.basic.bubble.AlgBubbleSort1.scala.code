public void bubbleSort()  {
	var swapped = true
	while ( swapped) {
		swapped=false
		for (i<-0 to GetNombreValeurs-2)
		 if (Pluspetit(getValeur(i-1),getValeur(i))   {
		 	                     echange(getValeur(i),getValeur(i-1))
		 	                     swapped=true
		 	                                           }           
		         
	                }	          
}
