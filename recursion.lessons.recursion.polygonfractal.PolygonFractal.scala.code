def polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double) {
	if (levels == 0)
	{
		avance(length)
		droite(72)
		return
	}
	else if (sides != 0)
	{
		polygonFractal(levels - 1, 1, length, shrink)
		polygonFractal(levels, sides - 1, length, shrink)
	}
	else 
	{
		
	}

		
}
