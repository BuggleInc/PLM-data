override def run() {
  var cpt: Int = 0
  ecritMessage(cpt);
  while (getMondeHauteur()-1 != getY() && getMondeLargeur() != getX()) {
    cpt += 1;
    if (getY() == getMondeHauteur()-1) {
      setPos(getX()+1,0); ecritMessage(cpt)
    } else {
      setPos(getX(),getY+1); ecritMessage(cpt)
    }
  }
}