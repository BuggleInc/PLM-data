def insertionSort() {
  var v : Int =0
  var k : Int =0
  for(i<-1 to getNombreValeurs()-1){
    v = getValeur(i)
    k=i
    while( k>=1 &&getValeur(k-1)>v){setValeur(k,getValeur(k-1));k+= -1;}
    setValeur(k,v);
  }
}
