def cocktailSort2() {
  for(j <- getNombreValeurs-2 to 0 by -1){
  	for(i <- 0 to j-2) {
   	  if (!plusPetit(i,i+1)){
      	echange(i,i+1)
    	}
  	}
  	for(k <- j-2 to 1 by -1) {
    	if (!plusPetit(k-1,k)){
    	  echange(k-1,k)
    	}
  	}
  }
}
