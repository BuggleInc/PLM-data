def extrema(nums:Array[Int]): Int = {
	var max=nums(0)
	var min=nums(0)
	var sol=max+min
	for (i <- 0 to nums.length-1) {
		if(max<nums(i)) {
			max=nums(i)
		} else if (min>nums(i)) {
			min=nums(i)	
		}
	}
	return sol

}
