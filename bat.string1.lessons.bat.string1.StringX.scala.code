def stringX(str:String):String = 
{
	var chaine = ""

	for(i <- 0 to str.length-1)
	{
		if(i==0 && str(i)=="x")
			chaine += str(i)

		if(i==str.length-1 && str(i)=="x")
			chaine += str(i)

		if(str(i)!="x")
			chaine += str(i)
			
	}

	return chaine
}
