compteur = int(0)
for compteur in compteur:
    if estSurBiscuit() == True:
        prendBiscuit()
        poseBiscuit()
    else: 
        avance()
        if estSurBiscuit() == True:
            prendBiscuit()
