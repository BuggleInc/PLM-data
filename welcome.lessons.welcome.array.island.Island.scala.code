def island(map:Array[Int]): Int = {
var cliffs = 0;
var levels = 0;

	for(i <- 1 to map.length - 1)
	{

		
			if(map(i-1) != map(i))
			{
				cliffs = cliffs + 1;
			}
		
	
	}
	
		println("Falaises");
		println(cliffs);

			levels = (cliffs+1) / 2 ;

		println("Niveaux");
		println(levels);

	
	
		
	return levels;
}

