def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {
var nb = 0
(a,b,c,d) match {
	case (a,_,_,_) if (a>=13 && a<=19) => nb = nb + 1
	case (_,b,_,_) if (b>=13 && b<=19) => nb = nb + 1
	case (_,_,c,_) if (c>=13 && c<=19) => nb = nb + 1
	case (_,_,_,d) if (d>=13 && d<=19) => nb = nb + 1
}
return nb}