def insertionSort() {var l=0
  for (i<- 1 to getNombreValeurs()-1)
                     {var v=getValeur(i)
                     for (k<- getNombreValeurs()-1 to 1 by -1)
                     {if (plusPetit(k,getValeur(k-1)))
                      {copie(k-1, k)
                       l=k}
                       copie(v,l) }
                     }
}