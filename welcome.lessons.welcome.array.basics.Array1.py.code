def mark(color):
    setBrushColor(L[b])
def array():
    n = 0
    brushDown()
    for b in range(len(L)):
        mark(color)
        forward()
                
def storeArray():
    global L
    L = []
    for a in range(getWorldHeight()):
        L.append(getGroundColor())
        forward()
    L.append(getGroundColor())

storeArray()
for c in range(getWorldHeight()-1):
    left()
    forward()
    right()
    array()

    