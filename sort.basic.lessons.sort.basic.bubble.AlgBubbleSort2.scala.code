def bubbleSort2() {
  var swapped=false
  do {
    swapped= false;
    var j=2;
    for (i <- 0 to getValueCount()-j)
      if(isSmaller(i+1,i)) {
        swap(i,i+1);
        swapped= true;
        j=j+1;
      }
  } while(swapped);
}