def hunt(c:Color) {
     do {
      while ( ! isFacingTrail()) {
      gauche() 
      }
      avance()
      
    } while( ! estSurBiscuit())
  prendBiscuit()
 }


def isFacingTrail(c:Color):Boolean = {
    if (estFaceMur()){return false}
  	var vert = false
  	avance()
    if (getCouleurSol() == c) {vert = true}
  	recule()
  	return vert
  
}