def plusDeBiscuit():Boolean = { //Renvoie true s'il n'y a plus de biscuit, false sinon.
                                //Positionne la buggle sur le prochaine biscuit, ou au bout du couloir.
  while (!estSurBiscuit && !estFaceMur) {
    recule()
  }
  return !estSurBiscuit
}

def run() {
  while (!plusDeBiscuit()) {
    prendBiscuit()
    while (!estSurBiscuit) {
      avance()
    }
    recule()
    poseBiscuit()
    recule()
  }
  gauche()
  avance()
  gauche()
  avance()
}