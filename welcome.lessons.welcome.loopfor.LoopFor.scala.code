var worldSize = getWorldHeight()

for (i <- 1 to worldSize){
  
	for (j <- 2 to worldSize){
    if (isOverBaggle()){
      pickupBaggle()
      for (k <- 2 to j){
      	backward()
      }
      
      return
    }
  }
}