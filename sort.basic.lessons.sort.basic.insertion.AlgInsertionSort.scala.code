def insertionSort() {
	var v=0;
	var count=0;
	for (i <- 1 to getNombreValeurs()-1) {
		v=getValeur(i);
		count=i;
		while ((count>0) && (getValeur(count-1)>v)) {
			setValeur(count,getValeur(count-1));
			count=count-1
			}
		setValeur(count, v);
	}
}
