setDirection(Direction.EST)
while estFaceMur() is False:
    avance()
for i in range(8):
    setDirection(Direction.NORD)
    avance()
    setDirection(Direction.EST)
    avance()
setDirection(Direction.EST)
avance(3)