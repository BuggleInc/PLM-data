def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
  var speed2:Int = speed
  if (isBirthday) {speed2 = speed2 -5}
  if (speed2 <= 60){return 0}
  else if (speed2 <= 80){
    return 1}
  else {
    return 2}}