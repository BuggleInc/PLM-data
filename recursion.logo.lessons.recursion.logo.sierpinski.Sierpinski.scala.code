def sierpinski(level:Int, length:Double) {
  if(level!=0){
    avance(length)
    droite(60)
    avance(length)
    droite(60)
    avance(length)
    droite(60)
    avance(length)
    sierpinski(level-1,length)
  }
}