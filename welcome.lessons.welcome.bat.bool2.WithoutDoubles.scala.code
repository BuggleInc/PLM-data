def withoutDoubles(die1:Int, die2:Int, noDoubles:Boolean):Int = {
	if (noDoubles==false) {
		return die1+die2 
	} else {
		if (die1==6) {
			die1=1
		} else {
			die1=die1+1
		}
		return die1+die2
	}
}
