def selectionSort() {
  for(i<- 0 to getNombreValeurs()-1){
    var n=getValeur(i)
    var o=i
    for(j<- i to getNombreValeurs()-1){
      if (getValeur(j)<n){
        n=getValeur(j)
        o=j
      }
    }
    echange(i,o)
  }
}