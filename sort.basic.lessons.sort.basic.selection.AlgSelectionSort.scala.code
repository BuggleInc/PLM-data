def selectionSort() {
  var smallest = getValue(0)
  var mem = 0
  for (i<-0 until getValueCount()){
    smallest = getValue(i)
    mem = i
    for (j<-i until getValueCount()){
      if (getValue(j)<smallest){
        smallest=getValue(j)
        mem = j
      }
    }
    swap(i,mem)
  }
}