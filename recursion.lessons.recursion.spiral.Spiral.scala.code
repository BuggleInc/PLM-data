def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {
 if(length==0){
 	
 } 
 else {
 	avance(0)
 	gauche(90)
   spiral(steps=steps-1, angle , length=length + increment, increment=increment)
 }
}
