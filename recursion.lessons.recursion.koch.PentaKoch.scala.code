def pentaKoch(levels:Int, length:Double) {
	if(levels == 0){
		avance(length);
		return;
	}
	pentaKoch(levels - 1, length * 0.4);
	gauche(90 + 18);
	pentaKoch(levels - 1, length * 0.4);
	droite(36*2);
	pentaKoch(levels - 1, length * 0.4);
	droite(36*2);
	pentaKoch(levels - 1, length * 0.4);
	droite(36 * 2);
	pentaKoch(levels - 1, length * 0.4);
	g(90 + 18);
	pentaKoch(levels - 1, length * 0.4);
}
