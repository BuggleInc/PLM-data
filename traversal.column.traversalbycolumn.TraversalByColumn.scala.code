override def run() {
  while (!fonctionTest()) {
    for (i <- 0 to 6) {
      for (j <- 0 to 6) {
        setPos(i,j);
      }
    }
  }
}


def fonctionTest() : Boolean = {
  if (getX() <= getMondeHauteur() && getY() >= getMondeLargeur()) {
    return true    
  }
  else {
    return false
  }
}