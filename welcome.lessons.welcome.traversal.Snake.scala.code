override def run() {
// Fonction qui test si le Buggle est dans un angle (position finale)
def fonctionTest() {
	var test2 = estFaceMur
	var test1 = getDirection()==Direction.EST
	(test1,test2) match {
		case (true,true) => gauche(); if (estFaceMur) {return true}
		case (false,true)=> droite(); if (estFaceMur) {return true}
		case (_,_)	  => return false
	}
}

baisseBrosse()
while (fonctionTest()==false)
	{if (estFaceMur)
		{if (getDirection()==Direction.EST)
			{gauche()
			avance()
			gauche()
			avance()}
		else {droite()
			avance()
			droite()
			avance()}
		}
	else {avance()}
	}
}





