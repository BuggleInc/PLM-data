def bubbleSort()  {
  var a:Boolean = false
  while (a == false) {
    a = true
    for (i <- 0 to getNombreValeurs()-2) {
      if (getValeur(i)>getValeur(i+1)) {
        a = false
        echange(i,i+1)
      }
    }
  }
}