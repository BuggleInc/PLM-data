def insertionSort() {
  var n = getNombreValeurs()
  var v = getValeur(0)
  var j = 1
  for (i <- 1 to n-1) {
    v = getValeur (i)
    while ( i-j >=0 && plusPetit(i,i-j)) {
        copie(i-j,i-j+1)
        j += 1 
      } 
    setValeur(i-j,v)
    j=1
  }
}
