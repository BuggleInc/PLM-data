override def run() {
 	var nbPas : Double = 100.00;
  var angle : Double = 90.00;
  
  avance(nbPas);
  droite(angle);
  avance(nbPas);
  droite(angle);
  avance(nbPas);
	droite(angle);
  avance(nbPas);
  droite(2*angle);
  
  
}
