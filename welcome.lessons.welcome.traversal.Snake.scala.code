override def run() {
  baisseBrosse;
  while (!finTest{
    if (!estFaceMur){
      avance;
    }else{droite;
         if (estFaceMur){
           gauche;gauche;avance;gauche;
         }else{avance;droite;}
         }
  }
}

def finTest(Boolean)={
  if (getDirection==Direction.OUEST&&estFaceMur){
    return true
  }else{return false}
}
