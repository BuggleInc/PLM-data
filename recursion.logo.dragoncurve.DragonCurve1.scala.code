def dragon(order:Int, x:Double, y:Double, z:Double, t:Double) {
  if(order==1){
    allerVers(z,t)
  }
  else{
    dragon(order-1,x,y,(x + z)/2 + (t - y)/2,(y + t)/2 - (z - x)/2)
    setPos(z,t)
    dragon(order-1,z,t,(x + z)/2 + (t - y)/2,(y + t)/2 - (z - x)/2)
  }
}