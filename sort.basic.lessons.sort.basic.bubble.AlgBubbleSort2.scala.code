def bubbleSort2() {
  var swapped= false
  var c:Int=getValueCount()-1
  do {
    swapped=false;
    for (i<-0 to c){
      if (!isSmaller(i,i+1)) {
        swap(i,i+1);
        swapped= true
        
      }
      c=c-1
      if (c==0) {
        return {}
      }
    }
    
  }while (swapped);
}