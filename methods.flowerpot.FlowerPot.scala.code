def growFlowers() {
  
}

def motif1(c : Color) {
  
  var n = 0
  
  setCouleurBrosse(c);
  for (n <- 1 to 2) {
    avance();
  }
  recule();
  gauche();
  avance();
  for (n <- 1 to 2) {
    recule();
  }
  avance();
  
  setColuleurBrosse(Color.YELLOW);
  gauche();
  
}
motif1(Color.RED)