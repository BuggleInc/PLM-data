def insertionSort() {
  var v=0
  
  for (i<-1 to getNombreValeurs()-1) {
    v=getValeur(i)
    var place = i
    while(place>0 && !plusPetitQue(place-1,v)) {
      copie(place-1,place)
      place-=1
    }
    setValeur(place,v)
  }
}
