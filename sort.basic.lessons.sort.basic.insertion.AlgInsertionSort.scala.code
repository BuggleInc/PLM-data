def insertionSort() {
  var n = getNombreValeurs();
  for ( i <- 1 to n-1 ){
    var v = getValeur(i);
    var j = 0;
    while ( i-j-1>=0 && !plusPetitQue(i-j-1,v) ){
      copie(i-j-1,i-j);
      j += 1;
    }
    setValeur(i-j,v);
  }
}