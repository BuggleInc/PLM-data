def selectionSort() {
  var min = 0;
  var va = 0;
  for(i<-0 to getNombreValeurs()){
    min = 0;
    for(j<-i to getNombreValeurs()){
      va = getValeur(j);
      if(va<min){
        min = va;
      }
    }
    setValeur(i,va);
  }
}