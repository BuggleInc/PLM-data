def cigarParty(cigars:Int, isWeekend:Boolean):Boolean = {
  (cigars , isWeekend) match {
    case (cigars , isWeekend) if isWeekend == false && cigars >= 40 && cigars <= 60 => true
    case (cigars , isWeekend) if isWeekend == true && cigars >= 40 => true
    case (_,_) => false
  }
}