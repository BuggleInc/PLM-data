def insertionSort() {
  var stock=getValeur(0)
  for (i<-1 to getNombreValeurs()-1){
    stock=getValeur(i)
    var j = i
    for (a<-i-1 to 0 by -1){
      if (getValeur(a)<stock){
        copie(a,a-1)
      }
    }
  }
}