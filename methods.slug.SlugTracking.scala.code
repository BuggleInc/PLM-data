def isFacingTrail():Boolean = {
  	var vuVert: Boolean = false
  	if (estFaceMur()==false) {
      forward();
      if (getCouleurSol()==Color.green) {
        vuVert=true
      } else {
        right();
        isFacingTrail();
      }
    } else {
      right();
      isFacingTrail();
    }
  	backward();
  	return vuVert
}

