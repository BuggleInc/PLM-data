def arrayFront9(nums:Array[Int]): Boolean = {
	var found = false
	var fin = 3
	if(nums.length < fin){
		fin = nums.length - 1
	}
	for(i <- 0 to fin){
		if(nums(i) == 9){
			found = true
		}
	}
	return found
}
