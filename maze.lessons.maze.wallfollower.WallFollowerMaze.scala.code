override def run() {
	while(!estSurBiscuit())
  keepHandOnSideWall()
}

def keepHandOnSideWall(){
  if (estDosMur()){
  gauche()
  recule()
    gauche()}
  else if (estFaceMur()){
    droite()
    avance()}
  else{
  recule()
    droite()}
  
}