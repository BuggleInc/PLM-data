#estSurBiscuit(), porteBiscuit(), prendBiscuit() et poseBiscuit(). 
setDirection(Direction.EAST)
while (estDosMur()==False or porteBiscuit()==False) and estFaceMur==False:
    recule()
    while not estSurBiscuit():
        recule()
    prendBiscuit()
    while not estSurBiscuit():
        avance()
        
