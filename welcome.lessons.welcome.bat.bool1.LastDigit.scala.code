def lastDigit(a:Int, b:Int):Boolean = 
{
 /* Étant donné deux nombres entiers positifs, retournez vrai s'ils ont le même dernier chiffre,   
  comme 27 et 57. Remarquez que l'opérateur % (modulo) calcule le reste   
  de la division entière (donc 17 % 10 vaut 7).*/

  (a%10,b%10) match
  {
  	case(x,y) if (x==y) => return true
  	case(x,y)           => return false
  }
}