def bubbleSort3() {
	var y:Int = 0
  var x:Boolean = false
	if(!x){
    x=true
  	for(i<- 0 to getNombreValeurs()-2-y){
      if(plusPetit(i+1,i)){
        echange(i,i+1)
        x=false
       }
    y += 1
    }
  }
  else(return)
}
