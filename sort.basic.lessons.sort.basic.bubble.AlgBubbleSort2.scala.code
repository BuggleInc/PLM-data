def bubbleSort2()  
{
	var taille:Int = getNombreValeurs();
	var i:Int = 0;
	var j:Int = 0;

	for (i <- taille - 2 to 0)
	{
		for (j <- 0 to i)
		{
			if (!plusPetit(j, j + 1)) 
					echange(j, j + 1);
		}
	} 
}
