def hunt(c:Color) {
def isFacingTrail(c:Color):Boolean = {if (isFacingWall()) {return false}
                                   else {forward(); if (getGroundColor()==c) {backward(); return true} else {backward(); return false}}}
                               while (!isOverBaggle()) {if (!isFacingTrail(c)) {left(); if (!isFacingTrail(c)) {right(); right()}}; forward()}
    // Write your code here


pickupBaggle()}
