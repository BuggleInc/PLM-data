def teaParty(tea:Int, candy:Int): Int = {
  var outcome: Int = 0;
  if (tea < 5 || candy < 5) {
    outcome = 0;
  } else if (tea >= 2*candy || candy >= 2*tea) {
    outcome = 2;
  } else {
    outcome = 1;
  }
  return outcome;
}