
def bubbleSort()  { val a = getNombreValeurs()
  for (i <- 0 to a) {
                      for (j <- i+1 to a) { 
                         if (plusPetit(j,i)) { echange(i,j)
                           }
                        
                       } 
                     }
}
