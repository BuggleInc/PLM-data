def selectionSort() {
  var N:Int=0
  var lgr:Int = getNombreValeurs()
  var M:Int=0
	var min:Int = 0
  for (N <- 0 to lgr-1){
    min = N
    for (M <- N to lgr){
      if (getValeur(min)<getValeur(M)){
        min = M
      }
    }
    echange(N,min)
  }
  
}