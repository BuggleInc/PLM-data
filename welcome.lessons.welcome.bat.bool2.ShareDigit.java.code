boolean shareDigit(int a, int b) {
	for (int i : digits(a))
		for(int j : digits(b))
			if(i == j) return true;

	return false;
	
}

ArrayList[Int] digits(int a){
	return new ArrayList(n/10, n%10);
}
