override def run() {
  for (i<- 1 to 4) {
    carre()
    droite(90.)
  }
  
}

def carre() {
  avance(100)
  droite(90.)
  avance(100)
  droite(90.)
  avance(100)
  droite(90.)
  avance(100)
  droite(90.)
}