def hunt(c:Color) {
     
  while(!estSurBiscuit())
  {
    if(isFacingTrail(c))
  		{
        baisseBrosse()
        leveBrosse()
        avance()
      }
  else
  	{
      while(!isFacingTrail(c))
      	{
          droite()
          
        }
      
    }
  }
  baisseBrosse()
   leveBrosse()
  prendBiscuit()
 }





def isFacingTrail(c:Color):Boolean = {
    var bool = false
  	if(!estFaceMur())
      {
        	avance()
        if(getCouleurSol()==c)
          {
            bool = true
          }
        recule()
      }
  return bool
  
}