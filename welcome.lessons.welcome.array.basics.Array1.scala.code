override def run() {
  var x=0
  x=getMondeHauteur()
  var T = new Array[Color](x)
  for (c <- 1 to x){
    T(c)=getCouleurSol()
    avance()
  }
  avance()
  for (i <- 1 to x){
    gauche()
    avance()
    droite()
    avance()
    for (j <- 0 to (x-1)){
      setCouleurBrosse(T(j))
      baisseBrosse()
      leveBrosse()
    }
  }
}
