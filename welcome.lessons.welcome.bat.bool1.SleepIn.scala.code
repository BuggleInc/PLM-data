def sleepIn(weekday:Boolean, vacation:Boolean): Boolean = {

if (weekday && vacation)
{
	 true
}

if (weekday && !vacation)
{
	false
}

if (vacation && !weekday)
{
	true
}

if (!weekday && !vacation)
{
	true
}

}