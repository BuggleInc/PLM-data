def insertionSort() {
  var len = getValueCount();
  
 
  
 	for(i <- 1 to len-1){
    
    var v = getValue(i);
    
    for(j <- i-1 to 1 by -1){
      
      if(v < j){
        copy(j, j+1);
      }
      else{
        setValue(j,v);
      }
    }
    
    
    
  } 
  
 
  
}