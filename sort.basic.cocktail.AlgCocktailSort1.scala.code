def cocktailSort() {
  for (i <- 0 to getNombreValeurs()){
    for (j <- 1 to getNombreValeurs()-i -2){
      
      if (plusPetit(j+1,j)){
        echange(j,j+1)
        plusPetit(j+1,j)

      }
      
    }
  }
}
