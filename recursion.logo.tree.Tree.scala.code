def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if(steps==0) { return; }
  avance(length)
  droite(angle)
  avance(length)
  recule(length)
  tree(steps-1,length,angle,shrink*length)
  tree(steps-1,length,-2*angle,shrink*length)
}