def getLandingZone():
    lastPoint = getGround()[0]
    for point in getGround():
        if (point != lastPoint):
            if lastPoint.y() == point.y():
                return (lastPoint,point)
        lastPoint = point

