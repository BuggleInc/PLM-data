var toggle = false

while (!isFacingWall()){
  forward()
  
  if (isOverBaggle()){
    
    if (isCarryingBaggle()){
      backward()
      dropBaggle()
      
    }else{
      pickupBaggle()
    }
  }
}