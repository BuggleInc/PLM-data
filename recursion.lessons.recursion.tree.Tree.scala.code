def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
	avance(length);
	droite(angle);
	tree(3, shrink*length,angle,shrink);
	gauche(2*angle)
	tree(3,shrink*length,angle,shrink);
	droite(angle)
	recule(length)
	
}
