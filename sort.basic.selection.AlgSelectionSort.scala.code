def selectionSort() {
  var n=getValueCount()
  
  for(i <- 0 to n-2){
    
    var min : Int = getValue(i)
    var minId : Int = i
    
    for(j <- i+1 to n-1){
      if(getValue(j)<min){
        min=getValue(j)
        minId=j
      }
    }
    swap(i,minId)
  }
}