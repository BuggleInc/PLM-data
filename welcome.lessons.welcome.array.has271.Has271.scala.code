def has271(nums:Array[Int]): Boolean = {
  for (i<- 0 to nums.length-3) {
    var el : Int = nums(i) 
    if (nums(i+1)== el+5 && nums(i+2) >= el-3 && nums(i+2) <=el+1) return true
  }
  
  return false
}