def insertionSort() {
  var n: Int=getNombreValeurs()-1
  for (i<- 1 to n-1){
    var v: Int=getValeur(i)
    var pos: Int=0
    for (j<- 0 to i){
      if (!plusPetitQue(j,v)){
        pos+=1
      }}
    var vali: Int=getValeur(pos)
    for (j<- pos to i ){
      setValeur(j,vali)
      vali=getValeur(j+1)
    }
    
    
  }
}