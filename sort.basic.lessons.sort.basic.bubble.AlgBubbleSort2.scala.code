def bubbleSort2() {
	val n:Int = getNombreValeurs()
	var sorted:Boolean = false
	var accu:Int = 0
	while (!sorted) {
		sorted = true
		for (i <- 0 to (n-2-accu)) {
			if (plusPetit(i+1, i)) { echange(i, i+1); sorted = false}	
		}
	accu+=1
	}
}
