override def run() {
    while (!isOverBaggle()){
      random3() match{
        case 0 => left();forward()
        case 1 => right();forward()
        case 2 => 
        	if (!isFacingWall()){
            forward()
          }
      }
    }
  
  	pickupBaggle()
}