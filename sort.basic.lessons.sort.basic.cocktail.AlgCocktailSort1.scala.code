def cocktailSort() {
  var test=false
  while (!test){
    test = true
    for (i <- 1 to getNombreValeurs()-2){
      if(plusPetit(i+1,i)){
        test = false
        echange(i,i+1)
      }
    }
    var i=getNombreValeurs()
    while(i!=1){
      i-=1
      if (plusPetit(i,i-1)){
        test = false
        echange(i-1,i)
    }
    }
  }
}