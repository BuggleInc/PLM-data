override def run() {
  baisseBrosse;
  while (!finTest){
    if (!estFaceMur){
      avance;
    }else{droite;
         if (estFaceMur){
           gauche;gauche;avance;gauche;
         }else{avance;droite;}
         }
  }
}

def finTest={
  var a:Boolean=false
  if (getDirection==Direction.OUEST&&estFaceMur){
    return true
  }else{return false}
	return false
}
