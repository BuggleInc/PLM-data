def golomb(num:Int): Int = {
  var suite = new Array[Int](num)
  suite(0) = 1
  suite(1) = 2
  for (i <- 1 to num-1) {
    var a:Int = 0
    for (j <- 0 to i-1) {
      if (suite(j) == i) {a = a + 1}
    }
    suite(i) = a
  }
  return suite(num-1)
}