L=[getCouleurSol()]
for i in range(getWorldHeight()-1):
    avance()
    L.append(getCouleurSol())
backward(getWorldHeight()-1)
for i in range(getMondeLargeur()-1):
    left()
    forward()
    right()
    M=int(litMessage())
    baisseBrosse()
    setCouleurBrosse(L[0+M])
    leveBrosse()
    for j in range(getWorldHeight()-1):
        setCouleurBrosse(L[(j+M+1)%len(L)])
        forward()
        baisseBrosse()
        leveBrosse()
    backward(getWorldHeight()-1)