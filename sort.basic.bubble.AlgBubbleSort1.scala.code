def bubbleSort()  {
  var i: Int = 0
  var j: Int = 0
  for (compteur <- 1 to getNombreValeurs()-2) {
    i = getValeur(compteur)
    j = getValeur(compteur + 1)
    if (plusPetit(j,i)) {
      echange(i,j)
    }
  }
}