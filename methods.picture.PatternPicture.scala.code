override def run() {
  def Motif(c:Color){
    setCouleurBrosse(c)
    avance()
    baisseBrosse();leveBrosse()
    recule()
    gauche();avance()
    baisseBrosse();leveBrosse()
    recule();droite();avance(3);gauche()
  }
for (i<-1 to 4){
  Motif(Color.RED)
  Motif(Color.BLUE)
  Motif(Color.GREEN)
  Motif(Color.YELLOW)
}
}