public void shellSort()  {

	int gap=(int)(getNombreValeurs()/2);

	while(gap>0){

		for(int i=1; i<getNombreValeurs(); i++){
			int temp = getValeur(i);
			int j=i-gap;
			
			while(j>0 && temp < getValeur(j-gap)){
				copie(j-gap, j);
				j-=gap;
			}
	
			setValeur(j, temp);
		}
		
	}
}
