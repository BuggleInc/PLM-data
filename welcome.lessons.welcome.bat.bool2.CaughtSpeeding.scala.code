def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
	if (isBirthday) {
		speed = speed + 5
	}
	if (speed < 60 && !isBirthday) {
		return 0;
	}
	if ( speed <= 80 && 61 <= speed && !isBirthday ) {
		return 1;
	}
	if ( 81 <= speed && !isBirthday ) {
		return 2;
	}
}