def combSort() {
  var eca = getNombreValeurs()
  var t = true
while ((eca > 1) || t == true){
    t = true
    if (eca >1){
      eca = (eca.toDouble / 1.3).toInt

    }
    
    var i = 0
    while (i+eca < getNombreValeurs()){
      if (plusPetit(i+eca,i)){
        swap(i,i+eca)
        t = false
      }
      i+=1
    }
    
    
    
  }
}