def selectionSort() {
  for(i<-0 to getNombreValeurs()-2){
    var minimum=getValeur(i)
    var n=i
    for (j<-i+1 to getNombreValeurs()){
      if (getValeur(j)<minimum){
        minimum=getValeur(j)
        n=j
      }
    }
    echange(i,n)
  }
}