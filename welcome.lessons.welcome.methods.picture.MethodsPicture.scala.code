override def run() {
  for (cp1 <- 1 to 3){
    faireV(c)
  }
}

def faireV(c:Color){
  for (cpt <- 1 to 3){
    for (cp2 <- 1 to 4){
      avance()
      baisseBrosse()
      leveBrosse()
      avance()
      gauche()
      avance()
      setCouleurBrosse(c)
      baisseBrosse()
      leveBrosse()
      droite()
      avance()
      droite()
      avance()
      setCouleurBrosse(c)
      baisseBrosse()
      leveBrosse()
      gauche()
      avance(2)
      gauche()
      setCouleurBrosse(c)
  
    }
    avance(5)
  }
  retourne()
  avance(21)
  droite()
  avance(7)
  droite()
} 