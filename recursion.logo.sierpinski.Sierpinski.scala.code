def sierpinski(level:Int, length:Double) {
  var i = level+1
  var y = length*2
  if ( i !=0){
    for (i <- 1 to 3){
    sierpinski(level-1, y/2)
    droite(120)
    }
  } else  {
    avance(length)
  }
}