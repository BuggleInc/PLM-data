public void run() {void makepattern() {forward(); forward(); brushdown(); brushup(); left(); forward(); right(); forward(); brushdown(); brushup(); forward(); right(); forward(); brushdown(); brush(up); right(); forward(); forward(); left()}
                   for (stepper<-1 to 4) {makepattern()}}