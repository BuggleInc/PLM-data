def cigarParty(cigars:Int, isWeekend:Boolean):Boolean = {
  (cigars, isWeekend) match{
    case (cig, false) if cig >= 40 && cig <= 60 => return true
    case (_,false) => return false
    case (cig, true) if cig >=40 => return true
    case (_,_) => return false
  }
}