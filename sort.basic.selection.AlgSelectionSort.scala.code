def selectionSort() {
  var xmin=0
  
  for (k <- 0 to getNombreValeurs()-1){
    for (i<- k to getNombreValeurs()-1){
      if(getValeur(i)<getValeur(xmin)){
        xmin = i
      }
    }
    if (k != xmin){
      echange(k,xmin)
    }
    xmin=k
  }
}
