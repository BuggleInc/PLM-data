override def run() {
  for (compt1 <- 1 to 9) {
    for (compt <- 1 to 20) {
      avance(100)
      recule(100)
      droite(1)
    }
    setCouleur(Color.red)
    for (compt <- 1 to 20) {
      avance(100)
      recule(100)
      droite(1)
    }
  }
}