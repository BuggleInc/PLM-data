def bubbleSort3() 
{
  var inverse=false
  for (i<- getNombreValeurs()-2 to 0 by -1)
  	{
      for (j<-0 to i)
      	{
      		if (plusPetit(j+1,j))
      			{
          		echange(j+1,j)
              inverse=true
        		}
    		}
      if (!inverse)
      	{
          return
        }
    }
}