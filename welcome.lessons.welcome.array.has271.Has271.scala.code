def has271(nums:Array[Int]): Boolean = {
	var x:Boolean = false
	for (i <- 0 to nums.length-3) {
		if (nums(i) == nums(i+1)-5 && (nums(i) == nums(i+2)+1 || nums(i) == nums(i+2)-1)) {x=true}
	}
	return x
}
