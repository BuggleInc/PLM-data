def golomb(num:Int): Int = {
  var sequence = new Array[Int](num)
  sequence(0) = 1
  
	for (i <- 0 to num - 1){
    for(j <- 1 to sequence(i)){
      System.out.println(j)
     // sequence(j) = i
    }
  }
  
  return 0
}