def insertionSort() {
  var N = getNombreValeurs()
  for(i <- 1 to N-1) {
    var v = getValeur(i)
    var j = i
    while(j>0 && !plusPetitQue(j-1,v) ) {
      copie(j-1,j)
      j-=1
    };setValeur(j,v)
  }
}