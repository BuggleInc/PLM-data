def dateFashion(you:Int, date:Int):Int = {
	(you,date) match {
		case (x,_) if (x<=2)		=> return 0
		case (_,y) if (y<=2)		=> return 0
		case (x,y) if (x>=8 || y>=8)	=> return 2
		case (_,_)				=> return 1
	}
}