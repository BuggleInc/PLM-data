def indexOfMaxValue(nums:Array[Int]):Int = {
  var max:Int = 0
  var posMax:Int = 0
  for(i<- 0 to nums.length-1){
    if(nums((nums.length-1)-i)>max){max=nums((nums.length-1)-i);posMax=(nums.length-1)-i}
  }
  return(posMax)
}