def insertionSort() {
  var v=0
  var n=getValueCount()-1
  
  for(i <- 1 to getValueCount()-1){
    v = getValue(i)
    
    for(j <- 1 to i){
      
      if(isSmaller(v,i-j)){
        copy(i-j-1,i-j)
      }
      else{setValue(i-j,v)
        
      }
    }
    
  }
}