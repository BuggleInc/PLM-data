override def run() {
def posfinale() {
	if (estFaceMur() && (getDirection() == Direction.EST)) {
		return true;
	}
	return false;
}
def serpent() {
	if (posfinale()) {
		droite()
		avance();
		droite();
	} else {
		avance();
	}
}

}
