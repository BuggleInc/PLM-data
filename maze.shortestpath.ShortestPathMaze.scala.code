def findBaggle() : (Int, Int) = {
  var (h, l) = (getMondeHauteur(), getMondeLargeur());
  for (i <- 0 to (h-1)) {
    for (j <- 0 to (l-1)) {
      if (aBiscuit(i,j)) {
        return (i, j);
      }
    }
  }
}

override def run() {
	println(findBaggle());
}