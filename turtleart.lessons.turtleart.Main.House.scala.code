override def run() {
 avance(100);
 toit();
 gauche(30);
 base();
 setPos(getX()-100, getY()+100);
}
def toit() {
	droite(30);
	for (i <- 0 to 2) {
		avance(100);
		droite(120);
 }
}
def base() {
	recule (100);
	droite(90);
	avance(100);
	gauche(90);
	avance(100);
}
