override def run() {for (j <- 1 to 4){for (i <- 1 to 4 ) {avance(30);droite(90)}
                   avance(30)
                   droite(30)
                   for ( i <- 1 to 3) {avance(30);droite(120)}
                   gauche(30)
                   recule(30)
                   droite(90)
                   leveCrayon()
                   avance(50)
                    gauche(90)
                   baisseCrayon()}}