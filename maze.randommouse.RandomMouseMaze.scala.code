override def run() {
  var r:Int =0
    while (! isOverBaggle()) {
      r=random3()
      if (r==0 && ! isFacingWall()) {forward()} else {}
      if (r==1) {left()}
      if (r==2) {right()}
    }
  pickupBaggle()
}