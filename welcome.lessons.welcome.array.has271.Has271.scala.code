def has271(nums:Array[Int]): Boolean = {
  for(i <- 0 to nums.length){
    if(i < nums.length - 2){
      if((nums(i+1) == nums(i)+5) && ((Math.abs(nums(i+2)) == Math.abs(nums(i)-1)) || Math.abs(nums(i+2)) == Math.abs(nums(i)+1))){
        return true
      }
    }
  }
  return false
}