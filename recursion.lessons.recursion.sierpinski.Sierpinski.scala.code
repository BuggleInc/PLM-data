def sierpinski(level:Int, length:Double) {
	if(level==0){
		for(i<-1 to 2){
			forward(length);
			right(120);
		}
	}
	else{
		for(i<-1 to 2){
			sierpinski(level-1,length/2);
			forward(length);
			right(120);
		}
	}	
}
