def bubbleSort()  { for (i <- 0 to 9) {
                      for (j <- i to 9) { 
                         if (plusPetit(i,j)) { echange(i,j)
                           }
                        
                    } 
        }
}
