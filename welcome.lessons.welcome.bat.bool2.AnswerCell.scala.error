answerCell:3: error: missing arguments for method && in class Boolean;
follow this method with `_' if you want to treat it as a partially applied function
	case (x,y,z) if (isMorning==true && isMom=true) => return true
                                         ^