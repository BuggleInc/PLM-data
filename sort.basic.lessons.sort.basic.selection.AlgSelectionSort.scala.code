def selectionSort() {
  var mem = 0
  for (i<-0 until getValueCount()-1){
    for (j<-i until getValueCount()){
      if (getValue(j)<getValue(mem)){
        mem = j
      }
    }
    swap(i,mem)
    mem = i+1
  }
}