override def run() {
  while(!isOverBaggle()){
      keepHandOnSideWall()

}

def keepHandOnSideWall(){
  
  if(isFacingWall()){
    
    //Si mur devant et ? gauche
    right()
    if(!isFacingWall()){
      forward()
        }
    	}
  }
}