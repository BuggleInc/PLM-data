def isFacingTrail():Boolean = {
  var resu:Boolean =false
    if (estFaceMur==false) {
      avance()
      if (getCouleurSol()==Color.green) {
        resu=true
      }
      recule()
    }
  return resu
}