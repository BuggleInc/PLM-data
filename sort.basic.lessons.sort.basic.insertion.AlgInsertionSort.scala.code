def insertionSort() {
  for (i <- 0 to getNombreValeurs() - 1){
    var v = getValeur(i);
    var j = i -1;
    while (getValeur(j) > getValeur(j+1) && j >= 0){
      copie(j, j+1);
      j -= 1;
    }
    setValeur(j, v)
  }
}