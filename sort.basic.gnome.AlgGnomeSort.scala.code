def gnomeSort() {
  
  var pot = 0
  
  while (pot < getNombreValeurs()-1)
      {if (plusPetit(pot+1,pot))
           {echange(pot+1,pot)
            pot-=1
            if (pot == -1)
                {pot=1}}
       else 
           {pot+=1}
     
}