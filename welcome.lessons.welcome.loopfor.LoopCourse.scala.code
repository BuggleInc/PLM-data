for (compteur <- 1 to 10) {
  for (compteur2 <- 1 to 8) {
    avance()
  }
  gauche()
  
  for (compteur2 <- 1 to 8) {
    avance()
  }
  gauche()
  
  for (compteur2 <- 1 to 8) {
    avance()
  }
  gauche()
  
  for (compteur2 <- 1 to 8) {
    avance()
  }
  gauche()
}