def bubbleSort()  {
  
  var x=getValueCount();
  
  for (i <- 0 to x){
    
    for (j <- 0 to x-i){
      
      if (isSmaller((getValue(j+1)), getValue(j))){
        
        swap(j,j+1);
        
      }
      
    }
    
  }
  
}