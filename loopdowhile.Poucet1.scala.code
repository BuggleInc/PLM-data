var exit=false
while (!exit) {
  var baggle = 0
  for (stepper <- 1 to 5) {
    forward() ;
    if (isOverBaggle) {
      baggle = baggle + 1
    }
  }
  if (baggle>=3) {
    left() ;
  }
  else right() ;  
    if (exitReached()) {
    exit=true
  } 
}
forward()