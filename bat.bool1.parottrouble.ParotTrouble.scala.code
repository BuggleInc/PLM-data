def parotTrouble(talking:Boolean, hour:Int):Boolean = {
  if (talking && ((hour<7) && (0<=hour))) {
    return true
  } else if (talking && ((hour<=23) && (20<hour))) {
    return true
  } else {
    return false
  }
  }