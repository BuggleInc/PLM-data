def getLandingZone():Segment = {
var debut:Point = Point(0,0)
var fin:Point = Point(0,0)
	 for (i <- 0 to getSol().length) {
	 	if (y(getSol(i))==y(getSol(i+1))) {
	 		debut=getSol(i)
	 		fin=getSol(i+1)
	 	}
	 }
	 return Segment(debut,fin)
}
