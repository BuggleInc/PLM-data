def noTriples(nums:Array[Int]): Boolean = {
  var longueurTab:Int = nums.length
  for (i <- 0 to longueurTab-3) {
    if (nums(i+1)==nums(i) && nums(i+2)==nums(i)) {
      return false
    }
  }
  return true
}