def bubbleSort3() {
	var swap=true
	for (i<-getNombreValeurs()-2 to 0 by -1) {
		while (swap) {
			swap=false
			for (j<-0 to i) {
				if (!plusPetit(j,j+1)) {
					echange(j,j+1);
					swap=true
				}
			}
		}
	}
}
