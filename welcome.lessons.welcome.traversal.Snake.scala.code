override def run() {
  var test:Boolean = false
  //baisseBrosse()
  while (!test) {
    if (estFaceMur == true) {
      if (getDirection() == Direction.EST) {
        if (estFaceMur == true) {test = true}
        gauche(); avance(); gauche()
      }
      if (getDirection() == Direction.OUEST) {
        droite(); avance(); droite()
      }
    } else {
      avance()
    }
  }
  //leveBrosse()
}