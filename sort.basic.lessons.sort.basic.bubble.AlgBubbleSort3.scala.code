def bubbleSort3() {
  var taille=getNombreValeurs()
	var trie=false
  
 	for (j<-0 to taille-2){
   for (k<-0 to taille-j-2){
    if (!plusPetit(k,k+1)){
       echange(k,k+1)
			 trie=true    
     } 
    }
    if (!trie){
      return 
    }
    }
  
}
