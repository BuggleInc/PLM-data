def goAndGet() {
  var i = 0;
  while(!isOverBaggle()) {
    forward();
    i += 1;
  }
  
  pickupBaggle();
  
  for(j <- i to 1 by -1) {
    backward();
  }
  
  dropBaggle();
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}