public void run() { 
	while(!isFacingWall() && !isOverBaggle() ){
		backward();
			if (isOverBaggle()){
				pickupBaggle();
				while(isCarryingBaggle() && !isOverBaggle()){
					forward();
				}
				if(isOverBaggle()){
					backward();
					dropBaggle();
					backward();
				}
			}

	}
					if (isFacingWall()){
						left();
						forward();
						right();
						forward();
					}
}
