def hunt() {while (!estSurBiscuit) {if (isFacingTrail()) {avance()} else {gauche();if (isFacingTrail()) {avance()} else {gauche();gauche();avance()}}}
    // Write your code here
}

// Copy your isFacingTrail() here
def isFacingTrail():Boolean = {if (estFaceMur()) {return false} else {avance();var p = getCouleurSol();recule();return (p == Color.green)}
    // Write your code here
}
