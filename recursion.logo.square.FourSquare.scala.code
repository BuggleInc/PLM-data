override def run() {
  for(i <- 0 until 4){
    avance(100)
    gauche(100)
  }
  for(i <- 0 until 4){
    recule(100)
    gauche(100)
  }
  for(i <- 0 until 4){
    avance(100)
    droite(100)
  }
  for(i <- 0 until 4){
    recule(100)
    droite(100)
  }
}