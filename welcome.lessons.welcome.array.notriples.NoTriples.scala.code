def noTriples(nums:Array[Int]): Boolean = {
  var valeur=nums(0)
  for(i<-2 to nums.length-1){
    if (nums(i)==valeur && nums(i-1)==valeur && nums(i-2)==valeur){
      return false
    } 
  }
  return true
}