def dogHouse() {
     baisseBrosse(); 
     /*
     for(i<-0 to 1) { avance(); }
	gauche();
	for(i<-0 to 1) { avance(); }
	gauche();
	for(i<-0 to 1) { avance(); }
	*/
	for(i<-0 to 3) {
		for(i<- 0 to 1) {
			avance();
		}
		gauche();
	}
}




