override def run() {
  
  while (!estSurBiscuit()){
    var hasard=random3()
    if (hasard==0){
      droite()
      if (!estFaceMur){
	      avance()
      }  
    }
    else if (hasard==1){
      if (!estFaceMur){
	      avance()
      } 
    }
    else{
      gauche()
      if (!estFaceMur){
	      avance()
      } 
    }
  }
  prendBiscuit()
}