override def run() {
	var T = new Array[Color](getMondeHauteur());
	T(0) = getCouleurSol();
	for(w<- 1 to T.length-1) {
		T(w) = getCouleurSol();
		avance();
	}
	for(x<-1 to T.length-1) {
		recule();
	}
	def Beta() {
		gauche();
		avance();
		droite();
		val decalage = litMessage().toInt
		for ( w<- 0 to T.length - 1 ) {
			var o:Int = w + decalage % T.length
			var c:Color = T(o)
			setCouleurBrosse(c);
			baisseBrosse();
			leveBrosse();
			avance();
								}
	
			}
	Beta();

}
