override def run() {
  for (i<- 1 to 9) {
  	quart(Color.BLACK); quart(Color.RED);
	}
}


def quart(c:Color) {
  for (i<- 1 to 30) {
    ligne(c:Color); right(1);
  }
}

def ligne(c:Color) {
  setCouleur(c);forward(100); backward(100);
}