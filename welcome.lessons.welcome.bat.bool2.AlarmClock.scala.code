def alarmClock(day:Int, vacation:Boolean): String = {
  if (vacation)
  {
    if (0==day || 6==day)
    	return "off"
    else
    	return "10:00"
  }
  else
  {
    if (0==day || 6==day)
    	return "10:00"
    else
    	return "7:00"
  }
}