def teaParty(tea:Int, candy:Int): Int = 
{

(tea,candy) match
{
	case(a,b) if(a<5 || b<5) => return 0
	
	case(a,b) if(a>=2*b || b>=2*a) => return 2
	case(a,b) if(a>=5 && b>=5) => return 1 
}
}
// Qd tu ne vois vraiment pas d'erreurs entre le monde et l'objectif, change l'ordre de tes instructions.