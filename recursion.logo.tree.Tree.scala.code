
def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
    if (steps==0){
    }else{
        current(steps)
        avance(length)
        gauche(angle)
        tree(steps-1, length*shrink, angle, shrink)
        droite(angle*2)
        tree(steps-1, length*shrink, angle, shrink)
        gauche(angle)
        current(steps)
        recule(length)
    }
}

