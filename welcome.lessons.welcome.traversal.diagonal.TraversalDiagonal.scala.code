var diag = 0;

override def run() {
	var cpt : Int =0
	ecritMessage(cpt);
	setPos(getX(),getY()+1);
	while(!posF)
	 if(!estFaceMur){
	 setPos(getX()+1,getY()-1);
	 }
	 if(estFaceMur){
	 setPos(getY(),getX()+1)
	
	 }
	 

	
}



def posF():Boolean ={
	if( getX()== (getMondeLargeur()-1)  && getY()==(getMondeHauteur()-1)){
		return true
	}else{
		return false
	}
}
