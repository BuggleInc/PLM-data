def solve() = {
  def danslOrdre(x:Int, y:Int) = {

    if (getColor(x) == getColor(y)) {
      true;
    } 
    else{
      if (getColor(x) == "BLUE") {
        	true;
      } 
      else if (getColor(x) == "WHITE") {
        if (getColor(y) == "BLUE") {
          !true;
        }else{
          true;
        }
          
      } else {
     		!true;
      }
    }

  }

  for (i <- 1 until getSize()) {
    if (!(danslOdre(i, i+1))){
      swap(i, i+1);
    }
    
  }
  
  
}

