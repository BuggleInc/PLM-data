def alarmClock(day:Int, vacation:Boolean): String = {
  (day, vacation) match {
    case (0|6, true) => "off"
    case ( _, true) => "10:00"
    case (0|6, _) => "10:00"
    case (_,_) => "7:00"
  }
}