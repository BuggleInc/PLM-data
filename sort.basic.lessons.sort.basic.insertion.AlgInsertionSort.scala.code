def insertionSort() {
  for (i<-1 to getNombreValeurs()-1){
    var j=i
    var memory=getValeur(i)
    while (j>=1 && !plusPetitQue(j-1, memory)){
      copie(j-1, j)
      j=j-1
    }
    setValeur(j, memory)
    
  }
}