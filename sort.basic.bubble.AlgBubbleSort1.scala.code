def bubbleSort()  {
  var n : Int =getValueCount()
  
  var i : Int = n
  
  while(i >= 1 ){
    
    var j : Int = 0
    
    while(j<i-1 && isSmaller(j+1,j)){
      j+=1
    	swap(j,j+1)
    }
    
    i-=1
  }
}