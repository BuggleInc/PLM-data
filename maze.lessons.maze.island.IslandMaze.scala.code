override def run() {
  while(!estSurBiscuit()){
       setDirection(Direction.NORTH)
    if (getDirection()==Direction.NORTH && !estFaceMur()){
      avance()
    }
    else{
   
    	keepHandOnSideWall()
    }
  }
  prendBiscuit()
}


def keepHandOnSideWall(){
  gauche()
  if (estFaceMur()){
    droite()
    if (estFaceMur()){
      droite()
      if (estFaceMur())
      	droite()
    }
  avance()
  }
  else{
    avance()
  }
}