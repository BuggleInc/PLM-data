def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
	if(isBirthday==true){if(speed>60){if (speed<81){return 1}else{return 2}}else{return 0}
	}else{if(speed>65){if (speed<86){return 1}else{return 2}}else{return 0}
	
	
}
}