override def run() {
  for (f <- 1 to 4){
    maison();
    leveCrayon();
    droite(90);
    avance(50);
    gauche(90);
    
  }
  
}  
  
def maison(){
  
  for (i<- 1 to 5){
    avance(100);
    droite(90);
  }
  gauche(60)
  
  for (a <- 1 to 3){
    avance(100);
    droite(120);
  }
  gauche(30)
  recule(100);

}