def step() {
  if (getGroundColor() == Color.white) {
    mark(Color.black)
    right(); forward()
  }
  
  if (getGroundColor() == Color.black) {
    mark(Color.white)
    left(); forward()
  }
}


def mark(c:Color) {
  setBrushColor(c);
  brushDown();
  brushUp();
}