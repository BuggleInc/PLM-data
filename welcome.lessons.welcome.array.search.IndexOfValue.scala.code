def indexOfValue(nums:Array[Int] ,lookingFor:Int): Int = {
  var rep = -1
  var last = nums.length -1
  for (i <- 0 to last){
    if (nums(i) == lookingFor){
      //while (rep== -1){
        rep = i
      // }
    }
  } 
  return rep
}