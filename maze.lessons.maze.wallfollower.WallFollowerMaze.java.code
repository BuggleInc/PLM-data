public void run() {
	while(!isOverBaggle()) {
		do{
			forward();
		}while(keepHandOnSideWall() && !isFacingWall());
		left();
		if(!isFacingWall()){
			forward();
		}
	}
}

boolean keepHandOnSideWall(){
	right();
	boolean isFacingWall = isFacingWall();
	left();
	return isFacingWall;
}
