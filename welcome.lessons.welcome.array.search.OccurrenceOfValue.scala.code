def occurrences(nums:Array[Int],lookingFor:Int):Int = {
  var longueur:Int = nums.length
  var occur:Int = 0
  for (i <- 0 to longueur-1) {
    if (nums(i) == lookingFor) {
      occur += 1
    }
  }
  return occur
}
