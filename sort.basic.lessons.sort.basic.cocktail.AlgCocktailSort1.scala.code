def cocktailSort() {
val n :Int = getNombreValeurs()
for (i <- 0 to n-2) {
	
 	if (plusPetit(i+1,i)) {
 		echange(i+1,i)}
for (k <- n-2 to 0 by -1) {
	
 	if (plusPetit(k+1,k)) {
 		echange(k+1,k)}  
}
}
}
