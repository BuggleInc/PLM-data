override def run() 
{

	for (n<-1 to 4)
	{
		avance(100)
		droite(90)
	}
	avance(100)
	droite(30)
	for (n<-1 to 2)
	{
		avance(100)
		droite(120)
	}
	leveCrayon()
	avance(100)
	droite(120)
	gauche(30)
	recule(100)
	baisseCrayon()
}
