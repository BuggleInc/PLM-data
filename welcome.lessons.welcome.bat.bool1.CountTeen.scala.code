def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {
  var nbrs = List(a, b, c, d)
  var cpt:Int = 0
  
  for (stepper1 <- 0 to 3) {
    if (13 <= nbrs(stepper1) && nbrs(stepper1) <= 19)
    	cpt += 1
  }
  
  return cpt
}