def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
  if (IsBirthday==true){
    speed match{
      case speed < 66 => return 0
      case speed > 66 && speed < 86 => return 1
      case speed > 85 => return 2
    }}
  else {speed match{
    case speed < 61 => return 0
    case speed > 60 && speed < 81 => return 1
    case speed > 80 => return 2
  }}}
  
  