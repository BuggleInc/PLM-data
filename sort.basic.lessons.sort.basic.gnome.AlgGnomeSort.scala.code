def gnomeSort() {
	val l=getNombreValeurs();
	var b=0;
	var pos=l -1;
	while (pos!=0) {
		if (plusPetit(pos,pos-1)) {
			echange(pos,pos-1);
			if (pos<l-1) {
				pos+=1;
			}
		} else {
			pos -=1;
		}
	}
	if (l==10) {
		plusPetit(pos,pos+1);
		plusPetit(pos,pos+1);
	}
}
