def insertionSort() {
  var memoire = 0
  var longueur = getNombreValeurs()-1
  for (i <- 1 to longueur){
    memoire=getValeur(i)
    var n=1
    while (i-n >= 0 && getValeur(i-n)>memoire){
      n=n+1
    }
    for (k <- 0 to n-1){
      echange(i-k+1,i-k)
    }
  }
}