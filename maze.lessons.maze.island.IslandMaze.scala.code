override def run() {
  while (!estSurBiscuit()) {
    while (!estFaceMur()) {
      avance()
    }
    keepHandOnSideWall()
    if (getDirection() == Direction.OUEST) {
      droite()
    }    
  }
  prendBiscuit()
}

def keepHandOnSideWall() = {
    while (estFaceMur()) {
      droite()
    }
  	avance()
  	gauche()
}