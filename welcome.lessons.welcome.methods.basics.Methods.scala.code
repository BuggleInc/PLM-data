        def goAndGet() {
       var fin = false;
      var cpt=0
       while(!fin){
      while(!estSurBiscuit()){
      	avance()
      	cpt=cpt+1
      }
      if(estSurBiscuit()){
      	prendBiscuit()
      }
      for(i <- cpt to 0 by -1){
      recule()
       }
       fin=true
        }
        }
        for (i <- 1 to 7) {
            goAndGet();
            right();
           // forward();
           avance()
            left();
        }
