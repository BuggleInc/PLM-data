override def run() {
    round(Color.RED)
    round(Color.YELLOW)
  	round(Color.GREEN)
  round(Color.BLUE)
  forward(4)
}  
  
  
  
def round(c:Color) {
  setBrushColor(c)
  left()
  forward()
  brushDown()
  brushUp()
  right()
  forward()
  right()
  forward()
  brushDown()
  brushUp()
  left()
  forward(2)
  left()
}