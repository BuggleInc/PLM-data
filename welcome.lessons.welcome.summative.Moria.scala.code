def Moria() = {
  while (!estFaceMur) {
    gauche(); gauche(); avance();
    while(!estSurBiscuit()) {
     	avance();
    }
    if (estSurBiscuit) {
      prendBiscuit();
      gauche(); gauche();
      while (!estSurBiscuit()) {
        avance()
      }
      recule();
      poseBiscuit();
    }
  }
}