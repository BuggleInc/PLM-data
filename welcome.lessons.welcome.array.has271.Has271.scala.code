def has271(nums:Array[Int]): Boolean = {
	var l = nums.length -3
	var rep = false
	var inter = nums(0)
	for (i<- 0 to l ) {
		inter = nums(i) ;
		if (nums(i+1) == (inter + 5)) {
			if (nums(i+2) == (inter - 1)) {
				rep = true
			}
		}
	 
		
	}
	return rep
	
}
