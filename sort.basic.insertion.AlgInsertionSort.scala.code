def insertionSort() {
  var length:Int = getNombreValeurs()
  var v:Int = 0
  var a:Int = 0
  for (i <- 1 to length-1){
    v = getValeur(i)
    for (j <- 1 to i){
      if (!plusPetit(i-j,i-j+1)){
        copie(i-j,i-j+1)
        a = i-j
      }
    }
    setValeur(a,v)
  }
}