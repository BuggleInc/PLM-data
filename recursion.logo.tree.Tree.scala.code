def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  var r=length/shrink
  var y=steps+1
  while(y!=0){r=r*shrink
    				avance(r*shrink:Double)
            	current(y)
            	right(angle:Double)
           	  y=y-1}
  left(angle:Double)
}