def bubbleSort()  {
  var len=getNombreValeurs()
  var sp=true
  while (sp==true) {
    sp=false
    for (i <- 0 to len-2) {
      if (!isSmaller(i,i+1)) {
        swap(i,i+1)
        sp=true
      }
    }
  }
}