while (! isFacingWall() ) {
  if (! isOverBaggle) {
    setDirection(Direction.EAST)
    forward()
  }
  else {
    pickupBaggle()
    back()
    while(! isOverBaggle() ) {
      forward()
    }
    backward()
    dropBaggle()
    back()
    forward()
  }
}
right()
forward()
left()
forward(2)
