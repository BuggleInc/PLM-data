override def run() {

	var cpt:Int = 0;
	var x:Int = 0;
	var y:Int = 0;
	setPos(3, 2);
	while (positionFinale)
	{
		ecritMessage(cpt);
		calculPos(x, y);
		setPos(x, y);
	}
}

def positionFinale():Boolean = 
{
	return (getX() == getMondeLargeur()) && (getY() == getMondeHauteur());
}

def calculPos(x:Int, y:Int)
{
	
}
