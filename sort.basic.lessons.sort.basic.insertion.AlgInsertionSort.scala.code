def insertionSort() {
val l= getNombreValeurs()
var swapped = -1
	for (i<-1 to l-1) {
		var v=getValeur(i)
		for(k<-i to 0 by -1) {
			if (!plusPetitQue(k-1,v)) {copie(k-1,k); swapped=k}
		}
		setValeur(swapped,v)

}}
