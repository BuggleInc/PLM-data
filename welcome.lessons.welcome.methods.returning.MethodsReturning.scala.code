def haveBaggle():Boolean = {
	var vuBiscuit: Boolean = true
	while (vuBiscuit==true)
	{ for (i <- 1 to 6)
		{avance(); if (estSurBiscuit()) {vuBiscuit = false}
					else {vuBiscuit = true}}
		recule(6); droite();avance();gauche()}
}