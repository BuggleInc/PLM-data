def lessBy10(a:Int, b:Int, c:Int):Boolean = {
  (a,b,c) match  {
    case (a,b,_) if (Math.abs(a-b)>9)=>true
    case (a,_,c) if (Math.abs(a-c)>9)=>true
    case (_,b,c) if (Math.abs(a-b)>9)=>true
    case (_,_,_)=>false
  }
}