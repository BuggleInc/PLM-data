def gnomeSort() {
var pos:Int=0
while (pos!=getNombreValeurs-1) {
	if (pos==0) {
		pos=pos+1
	} else if(getValeur(pos+1)<getValeur(pos)) {
		echange(pos,pos+1)
		pos=pos-1
	} else {
		pos=pos+1
	}
	
}
}
