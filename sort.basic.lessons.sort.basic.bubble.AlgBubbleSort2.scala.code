def bubbleSort2()  {
  
  var count = 20
  var continue = true
  while (continue){
      continue = false
      for (i <- 1 to getValueCount()-1 - count){
          if (isSmaller(i,i-1)){
            swap(i, i-1)
            continue = true
          } 
      }
    count += 1
  }
}