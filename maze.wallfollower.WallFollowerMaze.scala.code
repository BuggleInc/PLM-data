override def run() {
  while(!estSurBiscuit()){
    keepHandOnSideWall()
  }
  prendBiscuit()
}

def keepHandOnSideWall(){
  if (estFaceMur()){
  	while (estFaceMur()){
    	gauche()
  	}
    avance()
  }
  
  
  verifierMurGauche() match{
      case true => avance()
      case false => gauche();avance()
    
    
}
}

def verifierMurGauche():Boolean={
  gauche()
  estFaceMur() match{
    case true => droite();return true
    case false => droite();return false
  }
  
}