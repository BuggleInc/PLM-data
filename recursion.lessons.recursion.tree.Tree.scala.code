def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
if (steps==0) return
else {
avance(length)
current(steps)
gauche(angle)
avance(length)
recule(length)
droite(2*angle)
avance(length)
gauche(angle)
current(steps)
tree(steps-1,length-shrink,angle,shrink)

}
}