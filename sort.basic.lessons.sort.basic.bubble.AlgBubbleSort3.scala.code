def bubbleSort3() {
  var change = true
  var L = getNombreValeurs()
  for (i<- L-2 to 0 by -1){
    change = false
    L= L-1
    	for (j<- 0 to i) {
      	if (getValeur(j) > getValeur(j+1)) {
        	echange(j,j+1)
          change = true
          
      	}
 	   	}
    if (change == false) {
      return
    }
  	}
    
	}
