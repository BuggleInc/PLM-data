def maxMod5(a:Int, b:Int):Int = {

	var result = 0;
	
	if(a % 5 == b % 5)
	{
		result = Math.min(a,b);
	}
	else if(a == b)
	{
		result = 0;
	}
	else
	{
		result = Math.max(a,b);
	}

		return result;
}