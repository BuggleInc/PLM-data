def insertionSort() {
  var n = getNombreValeurs()
  var v = getValeur(0)
  var j = 1
  for (i <- 1 to n-1) {
    v = getValeur (i)
    do {
      if (plusPetit(i,i-j)) {
        copie(i-j,i-j+1)
        j += 1
      }
    }
    while (plusPetit(i,i-j) && j<i)
    setValeur(i-j,v);j=1
  }
}