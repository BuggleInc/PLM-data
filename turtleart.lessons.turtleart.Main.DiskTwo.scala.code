override def run() {

for (i <- 1 to 9)
	{	setCouleur(Color.black)
		for (j <- 1 to 30)
			{avance(100)
			recule(100)
			droite(20.00/3)}
		setCouleur(Color.red)
		for (j <- 1 to 30)
			{avance(100)
			recule(100)
			droite(20.00/3)}
		}
}
