$package import plm.universe.bat.BatEntity; import plm.universe.bat.BatWorld; import plm.universe.bat.BatTest; import plm.universe.World; public class parotTrouble extends BatEntity {  @SuppressWarnings("unchecked") public void run(BatTest t) {
		t.setResult( parotTrouble((Boolean)t.getParameter(0),(Integer)t.getParameter(1)) );		 }
	boolean parotTrouble(boolean talking, int hour) {
		return (talking && (hour<7||hour>20));	
	}
 }