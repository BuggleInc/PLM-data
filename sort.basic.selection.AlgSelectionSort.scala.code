def selectionSort() {
  val n = getNombreValeurs()
  var min = getValeur(0)
  var idMin = 0
  for(i <- 0 to n-1) {
    for(j <- i to n-1) {
      if (plusPetitQue(j,min)) {
        idMin = j
        min = getValeur(j)
      }
    }
    min=getValeur(i)
    echange(i,idMin)
    idMin = i+1
  }
}