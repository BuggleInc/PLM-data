def noTriples(nums:Array[Int]): Boolean = {
  var un =nums(0)
  var deux = nums(1)
  for ( i <- 2 to nums.length - 1){
    if (un == deux){
      if (deux == nums(i)){
        return true;
      }
      else{
        un = deux;
        deux = nums(i);
      }
    }
  }
  return false
}