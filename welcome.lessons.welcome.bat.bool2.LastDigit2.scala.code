def lastDigit(a:Int, b:Int, c:Int):Boolean = {
 var modA = a % 10;
 var modB = b % 10;
 var modC = c % 10;
   
  if (modA == modB || modA == modC || modB == modC)
    return true;
  else
    return false;
}