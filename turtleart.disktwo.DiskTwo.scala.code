override def run() {
  for(i <- 1 to 9){
    for(i <- 1 to 360/18){
      forward(100)
      backward(100)
      right(1)
    }
   setColor(Color.red)
   for(i <- 1 to 360/18){
      forward(100)
      backward(100)
      right(1)
    }
   setColor(Color.black)
  }
}