def bubbleSort2() {
  var nbValue = getValueCount();
  var fin = false;
  while(!fin) {
    fin = true;
    
    var nbLoop = 0;
    for(i <- 0 to nbValue-1-nbLoop) {
      if( getValue(i) > getValue(i+1)) {
        swap(i,i+1);
        fin = false;
      }
      nbLoop = nbLoop + 1;
    }
  }
}