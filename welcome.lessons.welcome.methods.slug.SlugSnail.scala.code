def hunt(c:Color) {
  var Color=getCouleurSol()
    while (isFacingTrail(Color)) {
      baisseBrosse()
      avance()     
  	if (estSurBiscuit()) {
      prendBiscuit()
      
    }
  }
}


def isFacingTrail(c:Color):Boolean = {
  if (estFaceMur()) {
    return false 
  }
  var estVert= false
  avance()
  if ( getCouleurSol() == c:Color ) {
    estVert = true
  } else estVert = false
  recule()
  return estVert 
}