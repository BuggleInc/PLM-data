override def run() {
  while(!estSurBiscuit()){
    if (getDirection()==Direction.NORTH && !estFaceMur()){
      avance()
    }
    else{
      setDirection(Direction.NORTH)
    	keepHandOnSideWall()
    }
  }
  prendBiscuit()
}


def keepHandOnSideWall(){
  gauche()
  if (estFaceMur()){
    droite()
    if (estFaceMur()){
      droite()
      if (estFaceMur())
      	droite()
    }
  avance()
  }
  else{
    avance()
  }
}