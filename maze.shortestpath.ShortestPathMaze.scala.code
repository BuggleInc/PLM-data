// gives the position of the Baggle's place
def findBaggle() : (Int, Int) = {
  var h = getMondeHauteur();
  var l = getMondeLargeur();
  for (i <- 0 to (h-1)) {
    for (j <- 0 to (l-1)) {
      if (aBiscuit(i,j)) {
        return(i,j)
      }
    }
  }
}

def drawLines() = {
  var (x, y) = findBaggle();
  
}


override def run() {
  findBaggle();
}