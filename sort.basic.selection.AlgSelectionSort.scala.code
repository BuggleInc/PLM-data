def selectionSort() {
  for(i<- 0 to getNombreValeurs()-2){
    var n=getValeur(i)
    var o=i
    for(j<- i to getNombreValeurs()){
      if (getValeur(j)<n){
        n=getValeur(j)
        o=j
      }
    }
    echange(i,o)
  }
}