public void run() {
	colByCol();
}

void colByCol(){
int width = getWorldWidth();
int height = getWorldHeight();
int gx = getX();
int gy = getY();
int count = 0;

	for(int x = gx; x < width; x++){
		for(int y = gy; y < height; y++){
			if(!finalPos()){
				setPos(x, y);
				count++;
				writeMessage(count - 1);
			}			
		}
	}
}

boolean finalPos(){
	if(getX() == getWorldWidth() && getY() == getWorldHeight()){
		return true;
	}

	return false;
}//end finalPos
