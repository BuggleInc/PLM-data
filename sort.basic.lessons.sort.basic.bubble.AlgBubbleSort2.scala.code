def bubbleSort2() {
  var N:Int = getNombreValeurs();
  while (N > 0) {
    for (k <- 0 to getNombreValeurs()-2) {
      if (plusPetit(k+1, k)) {
        echange(k, k+1);
      }
    }
    N = N-1;
  }
}