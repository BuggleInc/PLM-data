override def run() {
	for(h<-0 to 8) {
	for (i<- 0 to 19) {
		avance(100)
		recule(100)
		droite(1)
	}
	setCouleur(Color.red)
	for (i<- 0 to 19) {
		avance(100)
		recule(100)
		droite(1)
	}
	setCouleur(Color.black)
	}
}
