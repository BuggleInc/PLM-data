def cocktailSort() {
  var finished:Boolean= true;
  var n:Int = getValueCount();
  do{
    finished = true;
    for (i<-0 to n-2) {
      if (!isSmaller(i,i+1)) {
        swap(i,i+1);
        finished = false;
      }
    }
    for (i<- n-2 to 0 by -1) {
      if (!isSmaller(i,i+1)) {
        swap(i,i+1);
        finished = false;
    	}
  	}
	} while (!finished)
}