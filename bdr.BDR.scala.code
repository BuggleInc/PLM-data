var x = false
while (estSurMessage()) {
  x=false;

  if (litMessage()=="R") {
    droite();
    avance();
  };
  if (litMessage()=="L") {
    gauche();
    avance();
  };
  if (litMessage()=="I") {
    retourne();
    avance();
  };
  if (litMessage()=="A") {
    avance();
  };
  if (litMessage()=="B") {
    avance(2);
  };
  if (litMessage()=="C") {
    avance(3);
  };
  if (litMessage()=="Z") {
    recule();
  };
}