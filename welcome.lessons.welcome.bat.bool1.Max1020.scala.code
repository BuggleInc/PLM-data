def max1020(a:Int, b:Int):Int = {
  if((10 <= a && a <= 20)||(10 <= b && b <= 20)){
    if(a < 10 || 20 < a){return b}
    if(b < 10 || 20 < b){return a}
    else(return Math.max(a,b))
  }
  else(return 0)
}
