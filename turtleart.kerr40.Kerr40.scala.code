override def run():Unit= {
	for(i <- 1 to 36 ) {
    motif()
  }
}

def motif() {
  avance(30)
  gauche(90)
  avance(30)
  droite(45)
  avance(30)
  gauche(90)
  avance(30)
  droite(90)
  avance(30)
  droite(90)
  avance(30)
  gauche(45)
  avance(30)
  droite(90)
  avance(30)
  gauche(45)
  avance(30)
  droite(90)
  avance(30) //10
  gauche(45)
  avance(30)
  droite(90)
  avance(30)
  droite(90)
  avance(30)
  gauche(90)
  avance(30)
  droite(45)
  avance(30)
  gauche(90)
  avance(30)
}