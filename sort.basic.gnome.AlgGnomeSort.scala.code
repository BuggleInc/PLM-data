def gnomeSort() {
  
  for (n <- 0 to getValueCount()-2){
    var k = n
    if (k == 0){k = k+1}
    else if (k == getValueCount-1){ k = k-1}
    
    if (isSmaller(i+1,i)){
      swap(k,k+1)
      k = k-1
    }
    else {k = k+1 }
  }
}