def stringTimes(str:String, n:Int):String =
{
  var chaine = ""
  
  for (i <- 1 to n)
  	chaine += str
  
  return chaine
}

def frontTimes(str:String, n:Int):String =
{
  if (str.length < 3)
  	return stringTimes(str, n)
  else
    return stringTimes(str(0).toString() + str(1) + str(2), n)
}