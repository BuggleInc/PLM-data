def insertionSort() {
var v =0
var j=0
    for (i<-1 to getNombreValeurs()-1){
      j=i-1
      v=getValeur(i)
      while (j>0 && !plusPetit(j,v)){
        copy(j,j+1)
        j=j-1
        setValeur(j+1,v)
        
      }
    }
}
