def combSort() {
  var ecart = getNombreValeurs();
  var n = getNombreValeurs();
  var i = 0
		  
  while (ecart>1) {
 		
   	ecart=(ecart.toDouble/1.25).toInt 
    
 		i=1
  	
 		while (i+ecart<n) {
     	 
 			 if (getValeur(i)>getValeur(i+ecart)) {
 	  		 echange(i,i+ecart)
         
 	  	 }
     i=i+1 
    }
  }
}
