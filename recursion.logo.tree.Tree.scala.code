def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if(steps==0){
    return
  }
  baisseCrayon()
  current(steps)
  avance(length)
  droite(angle)
  subtree(steps,length,angle,shrink)
  gauche(angle)
  gauche(angle)
  subtree(steps,length,angle,shrink)
  recule(length)
}