def bubbleSort()  {
  var len:Int=getValueCount()
  var swapped:Boolean = true
  do {
    swapped=false
    for (i <- 0 to len-2) {
      if (!isSmaller(i,i+1)) {
        swap(i,i+1);
        swapped=true
      }
    }
  } while (swapped)
}