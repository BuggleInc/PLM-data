def insertionSort() {
  var lgr:Int=getNombreValeurs()
  for (i<-1 to lgr-1) {
    var v:Int=i
    for (j <- (1 to i).reverse) {
    if (plusPetit(v,j-1)) { copie(j-1,j) }
 		copie(v,j)
      }
    
  }
}