override def run() {
  while (!estSurBiscuit()){
    while (!(getDirection()==Direction.NORTH)){
      droite()
    }
    if (estFaceMur){
      longerMur()
    } else {
      avance()
    }
  }
  prendBiscuit()
}


def longerMur(){
  gauche()
  if (estFaceMur()){
    droite()
    if (!estFaceMur()){
      avance()
    } else {
      droite()
      if (estFaceMur()){
        droite()
        avance()
      } else {
        avance()
      }
    }
  } else {
    avance()
  }
}