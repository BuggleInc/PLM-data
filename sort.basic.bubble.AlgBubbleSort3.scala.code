def bubbleSort3(){
  var lgr = getNombreValeurs;
  for(k <- 0 to lgr - 2){
    var i = lgr - 2 - k;
    for (j <- 0 to i){
      var fini = true;
      if(plusPetit(i,j)){
        echange(i,j);
        fini = false;
      }
      if(fini) return;
    }
  }
}