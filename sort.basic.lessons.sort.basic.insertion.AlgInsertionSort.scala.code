def insertionSort() {
  var length : Int = getValueCount();
  var i : Int =0;
  var j : Int =0;
  var v : Int =0;
  
  for(i<-1 to length-1)
  {
    v = getValue(i);
    j=i;
    for(j<-i to i by -1)
    {
      if(isSmallerThan(i-1,v))
      {
        setValue(i,v);
      }
    }
  }
}