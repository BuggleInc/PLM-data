override def run():Unit= {
  for (c <- 1 to 8){
    motif()
  }
}

def motif(){
  droite(45);avance(20);gauche(90);avance(20);droite(45);avance(20);droite(90);avance(20);gauche(90);avance(20);gauche(90);avance(20);avance(20);gauche(90);avance(20);gauche(90);avance(20);droite(90);avance(20);droite(45);avance(20);gauche(90);avance(20);droite(45);avance(20);gauche(135);
}