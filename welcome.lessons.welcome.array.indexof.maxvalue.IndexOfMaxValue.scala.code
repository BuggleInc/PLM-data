def indexOfMaxValue(nums:Array[Int]):Int = {
	var z:Int=nums(0);
	var w,t:Int=0;

	for (x<-0 to nums.length-1) {
		if (nums(x)>z) {
			z=nums(x);
		}
	}
	while ( nums(t)!= z) {
		t=t+1
	}
	return t
}
