def bubbleSort3() {
val n = getNombreValeurs()
var swapped = true
for (i<-n-2 to 0 by -1) {
	swapped=false
	for(k<- 0 to i) {
		if (plusPetit(k+1,k)) {
 		echange(k+1,k)
 		swapped=true}}
 	if (!swapped){return}

}
}