override def run() {
  var cpt:Int=0
  for (i <- 0 to 6) {
    for (j <- 0 to 6) {
      if (i%2==0) {
        setPos(j,i)
        writeMessage(cpt)
        cpt+=1
      }
      else {
        setPos(6-j,i)
        writeMessage(cpt)
        cpt+=1
      }
    }
  }
}