def indexOfValue(nums:Array[Int] ,value:Int): Int = {
	var y,z:Int=0;
	val w=-1;
	
	for (x<-0 to nums.length-1) {
		if (value==nums(x)) {
			y=y+1;
			z=z+1;
		}
		if (x==nums.length-1 && value!=nums(x) && z==0) {
			return w
		}
	}
	return y
}
