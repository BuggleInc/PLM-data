def in1To10(n:Int, outsideMode:Boolean):Boolean = {
	if (n<11 && n>0 && outsideMode==false) {
		return true
	} else if (outsideMode==true && (n<2 || 9<n)) {
		return true 
	} else {
		return false
	}
}
