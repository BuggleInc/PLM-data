def dogHouse(); {
     baisseBrosse(); 
     setCouleurBrosse(java.awt.Color.red) ;
     for(i<-0 to 2) { avance(); }
	gauche();
	for(i<-0 to 2) { avance(); }
	gauche();
	for(i<-0 to 2) { avance(); }
}




