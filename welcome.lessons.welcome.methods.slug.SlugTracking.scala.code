def isFacingTrail():Boolean = {
    if (estFaceMur()) return false;
    else {
    		avance();
    		def trace:Boolean = (Color.green == getCouleurSol());
    		recule();
    		return trace;// Write your code here
    }
}        
