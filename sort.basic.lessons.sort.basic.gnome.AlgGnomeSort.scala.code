def gnomeSort() {
  var n = getNombreValeurs()
  var i=0
  while (i!=n){
    if (getValeur(i)>getValeur(i+1) && i!=0){
      echange(i,i+1)
      i=i-1
    }
    else if (getValeur(i)<getValeur(i+1) && i!=n){
      i=i+1
    }
  }
}