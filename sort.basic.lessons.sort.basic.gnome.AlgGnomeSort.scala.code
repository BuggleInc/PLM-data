def gnomeSort() {
	var i:Int = 1
	
	while (i > 0 && i < getNombreValeurs()) {
		if (i-1 < 1)
			i++
		else if (i > getNombreValeurs())
			i--
			
		if (plusPetit(i+1,i-1)) {
			echange(i-1,i+1)
			i--
		} else
			i++
	}
}
