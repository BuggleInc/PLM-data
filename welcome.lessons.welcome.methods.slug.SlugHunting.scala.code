def hunt() {
    while (!isFacingTrail()){
      gauche();
    }
		avance();
  	if (estSurBiscuit()){
      prendBiscuit()
    }
  	baisseBrosse();
    leveBrosse();
}

def isFacingTrail():Boolean = {
  if (estFaceMur()){
    return false;
  }
  avance();
  if (getCouleurSol() == Color.green) {
    recule();
    return true;
  }
  recule();
  return false
}