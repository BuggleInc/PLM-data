while(getCouleurSol()!="Color.green"):
    if(estFaceMur()==True):
        droite()
        avance()
        gauche()
        avance()
    if(estSurBiscuit()==False and estFaceMur()==False):
        setDirection(Direction.EAST)
        avance()
    if(estSurBiscuit()==True):
        prendBiscuit()
        setDirection(Direction.WEST)
    if(estSurBiscuit()==False):
        avance()
    recule()
    poseBiscuit()
    recule()
