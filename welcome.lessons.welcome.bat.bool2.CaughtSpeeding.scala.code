def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
  if (isBirthday)
  	speed = speed - 5
  speed match {
    case speed <= 60 => return 0
    case speed <= 80 => return 1
    case speed > 80 => return 2
  }
}