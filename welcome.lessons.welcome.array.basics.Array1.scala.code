override def run() { 
  var x = getMondeLargeur();
  var y = getMondeHauteur()
  var T = new Array[Color](y);
  for (i<- 1 to (getMondeHauteur())) {
    T(i)=getCouleurSol();
    avance()
  }
  for (i<-1 to y) {
    gauche()
    avance()
    droite()
    avance()
    
    for (j <- 1 to (T.length)) {
      setCouleurCorps(T(j))
      baisseBrosse()
      leveBrosse()
      avance()
    }     
  }
}
  