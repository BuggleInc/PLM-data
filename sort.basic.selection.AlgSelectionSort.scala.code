
def selectionSort() {
  var len = getValueCount();
  for(i <- 0 to len-1){
    var ind = select(i,len);
    swap(ind, i);
    
  }
}

def select(i:Int, len:Int) : Int = {
  var indMini = i;
  for(j<- i to len-1){
    if(getValue(j) < getValue(i))
    	indMini = j;
  }
  return indMini;
  
}