def insertionSort() {
  var ok = false
  var v = 0
  var k = 0
  for(i<- 0 to getNombreValeurs()) {
    v=getValeur(i)
    k=0
    ok=false
    while(ok==false) {
     if(plusPetit(i+k,i+k+1)) {
        ok= true
      }
      else {
      copie(i+k, i+k+1)
        setValeur(i+k,v)
      
    }
      k=k+1
  }
}
}