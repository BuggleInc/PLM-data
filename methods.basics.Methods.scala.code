def goAndGet() {
  var nbPas : Int = 0
  while (!estSurBiscuit()) {
    avance()
    nbPas = nbPas + 1
  }
  prendBiscuit()
  retourne()
  for (compteur <- 1 to nbPas) {
    avance()
  }
  poseBiscuit()
  retourne()
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}