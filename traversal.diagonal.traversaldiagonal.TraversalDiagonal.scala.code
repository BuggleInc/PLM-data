var diag = 0;
//var a : Int = 0
//var y : Int = 0
var counter : Int = 0;
var finalpos : Boolean = true;
override def run() {
    //while( diag <= 7){
      for(y <- 0 to 6){
        for (x <- 0 to y){
          setPos(x,y-x)
          writeMessage(counter)
          counter = counter + 1
          //diag = diag + 1
        }
      //}
 
    }
  for (y <-  0 to 6){
    for (x <- 0 to 6-y){
      setPos(x, 6-y)
      writeMessage(counter)
      counter = counter + 1
    }
  }
      
    
  }
