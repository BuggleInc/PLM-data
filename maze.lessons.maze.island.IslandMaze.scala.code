override def run() {
  while ( !estSurBiscuit() ){
    if ( estFaceMur() ){
      droite();
      droite();
      keepHandOnSideWall();
    } else { 
      avance();
    }
  }
  prendBiscuit();
}

def keepHandOnSideWall() {
  gauche();
  if ( estFaceMur() ){
    droite();
  }
  if ( !estFaceMur() ){
    avance();
  } else {
    droite();
    keepHandOnSideWall();
  }
}