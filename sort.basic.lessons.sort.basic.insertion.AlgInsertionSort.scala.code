def insertionSort() {
  var longueur = getNombreValeurs()
  var v=0

  for (i <- 1 to longueur-1){
    v=getValeur(i)
    var I=i-1
    
    while (plusPetit(i,I) && I>0){
      setValeur(I,getValeur(I+1))
      }
      I=I-1
      
    }
  }
}