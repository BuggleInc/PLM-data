def squirrelPlay(temp:Int, isSummer:Boolean):Boolean = {
  
  (temp, isSummer) match {
    case (i, false) if ((i >= 60) & (i <= 90)) => return true
    case (i, true) if ((i >= 60) & (i <= 100)) => return true
    case (_, _) => return false
  }
}