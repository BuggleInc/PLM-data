def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {
  var res=0
  if (estAdo(a)) {res=res+1}
  if (estAdo(b)) {res=res+1}
  if (estAdo(c)) {res=res+1}
  if (estAdo(d)) {res=res+1}
  return res}

def estAdo (a:Int):Boolean={
  a>=13 && a<=19
}
