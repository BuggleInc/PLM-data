override def run() {
  var T = new Array[Color](getWorldHeight()-1)
  for (i<-0 to getWorldHeight()-1) {    
    T(i)=getCouleurSol()
    avance()}
  for (j <-1 to getWorldHeight()-1) {
    avance()
    for (k <- 0 to getWorldHeight()-1) {
      setCouleurBrosse(T(k))
      baisseBrosse()
      leveBrosse()
      avance()
      }
    gauche()
    avance()
    droite()
  }