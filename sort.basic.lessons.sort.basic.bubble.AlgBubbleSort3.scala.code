def bubbleSort3() {
	
	var length = getNombreValeurs();

	for(i<-length-2 to 0 by -1) {
		var tri = true;
		for(j<-0 to i) {
			
			if(plusPetit(j, j+1) == false) {
				echange(j, j+1);
				tri = false;
			}
			
		}

		if(tri)
			return;
	}
	
}
