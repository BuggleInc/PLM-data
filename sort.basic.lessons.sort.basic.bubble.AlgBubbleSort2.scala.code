def bubbleSort2() {
  var temp:Int =0;
	var nbTour:Int =0;
  
  for(i <- getNombreValeurs to 0 by -1){
    nbTour=nbTour+1;
    for(j <- 0 to (getNombreValeurs-(1+nbTour))){
      if(plusPetit(j+1,j)){
        echange(j+1,j);
      }
    }    
  }
}
