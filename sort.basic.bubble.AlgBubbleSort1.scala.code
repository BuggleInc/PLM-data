def bubbleSort()  {
  var swapped : Boolean = false
  do{
    swapped = false
    for(i <- 0 to length){
      if(isSmallerThan(tab(i + 1), tab(i)){
        swap(tab(i), tab(i +1))
        swapped = true
      }
    }
  } while(swapped)
}