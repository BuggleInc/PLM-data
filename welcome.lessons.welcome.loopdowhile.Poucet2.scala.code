while (!sortieTrouvee()) {
  var baggels:Int = 0
  
  // On parcourt le corridor
  do {
    avance()
    
    if (estSurBiscuit())
    	baggels = baggels + 1
  } while(!crossing())
  
  if (baggels >= 3) {
    gauche()
  }
  else {
    droite()
  }
}

avance()