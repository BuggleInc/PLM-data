override def run() {
	 for (i <- 1 to 8) {
	 	avance(50)
	 	droite(45)
	 }
	 avance(25)
	 for (i<- 1 to 16) {
	 	if (i%2==1) {
	 		droite(45)
	 	} else {
	 		gauche(45)
	 	}
	 	if (i%3==0) {
	 		droite(45)
	 	}
	 	if (i%4==0) {
	 		gauche(45)
	 	}
	 	avance(25)
	 }
}
