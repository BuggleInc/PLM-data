override def run() {
def fonctionTest() : Boolean =  {
	var rep = true
	if (estFaceMur) {
		if (getDirection() == Direction.EST) {
		gauche() ; 
		if (estFaceMur) {
			droite() ; rep = false; 
		} else { avance();gauche()}
	}
	if (getDirection() == Direction.OUEST) { 
		droite();
		if (estFaceMur) {
			gauche() ; rep = false; 
		} else { avance() ; droite()}
	}
	}
	return rep
}
baisseBrosse()
while (fonctionTest()) {
	if (!estFaceMur()) { 
		avance()
	
	}
		
}
leveBrosse()
}
