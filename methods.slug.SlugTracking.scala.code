def isFacingTrail():Boolean = {
  var returnVar = false
	if(!isFacingWall()){
    forward();
    returnVar = (getGroundColor() == Color.green);
    backward();
    return returnVar;
    
  }
  else 
    return false 
}