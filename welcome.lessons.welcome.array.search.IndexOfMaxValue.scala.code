def indexOfMaxValue(nums:Array[Int]):Int = {
  var plusGrandeValeur:Int = nums(0)
  var indiceDuMax:Int = 0
  for (i <- 1 to nums.length -1) {
    if (nums(i) > plusGrandeValeur) {
      plusGrandeValeur = nums(i)
      indiceDuMax = i
    }
  }
  return indiceDuMax
}