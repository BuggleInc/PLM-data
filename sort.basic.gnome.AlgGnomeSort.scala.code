def gnomeSort() {
  val lgr = getNombreValeurs()
  var position = 0
  while (position < lgr - 2) {
    if (plusPetit(position + 1, position + 2) || position == - 1) {
      position = position + 1
    }
    else {
      echange(position+1,position+2)
      position = position - 1
    }
  }
}