do{
  var x:Int = getX()
  var y:Int = getY()
  if(!estSurBiscuit() && !porteBiscuit())
  	setPos(x + 1, y)
  else {
    if(!porteBiscuit())
    	prendBiscuit()
    setPos(x - 1, y)
    if(estSurBiscuit()) {
  		setPos(x, y)
      poseBiscuit()
      setPos(x + 1, y)
  	}
  }
} while(getX() != 19)

setPos(getX(), getY() + 1)
droite()
droite()