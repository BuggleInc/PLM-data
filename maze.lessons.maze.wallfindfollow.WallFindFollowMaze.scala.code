override def run() {
	gauche()
	def murAgauche() : Boolean ={
	var mur :Boolean = false
	gauche()
	if (estFaceMur()) {mur = true}
	droite()
	return mur }

	def keepHandOnSideWall() {
		while (!murAgauche) {gauche()}
		if (estFaceMur()) {droite()}
		else {avance()
			if (!murAgauche) {gauche() ;avance()}
				if (!murAgauche) {gauche();avance()}
		}}
		
	
	
while(!estSurBiscuit()){	
keepHandOnSideWall()
}
prendBiscuit();
}
