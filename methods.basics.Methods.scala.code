def goAndGet() {
  var x=0
  while (!estSurBiscuit()) {
    avance();
    x=x+1;
    if (estSurBiscuit()) {
      prendBiscuit();
    };
   var c=0;
   while (c!=x) {
     recule();
     c=c+1;
   };
     poseBiscuit();
  };
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}