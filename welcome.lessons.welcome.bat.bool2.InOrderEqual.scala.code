def inOrderEqual(a:Int, b:Int, c:Int, equalOk:Boolean):Boolean = {
	var rep = false
	if (iqualOk) {
		rep = ( ( a <= b ) && ( b <= c ) )
	} else { 
		rep =  ( ( a < b ) && ( b < c ) )
	}
	return rep
}
