def bubbleSort()  {

	var taille:Int = getNombreValeurs();
	var bool:Boolean = false;
	var j:Int = 0;

	do
	{
		for (j <- 0 to taille -2)
		{
			bool = false;
			if (!plusPetit(j, j + 1)) 
				{
					echange(j, j + 1);
					bool = true;
				}
		}
	} while(bool);
}
