def bubbleSort3() {
	var swap=false
	for (i<-getNombreValeurs()-2 to 0 by -1) {
		swap=false
		for (j<-0 to i) {
			if (!plusPetit(j,j+1)) {
				echange(j,j+1);
				swap=true
			}
		}
		if (!swap) {
			return
		}
	}
}
