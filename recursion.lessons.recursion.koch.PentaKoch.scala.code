def pentaKoch(levels:Int, length:Double) {
	if(levels==0){
		return
	}
	else{
		avance(length*0.4)
		gauche(108.0)
		avance(length*0.4)
		droite(72.0)
		avance(length*0.4)
		droite(72.0)
		avance(length*0.4)
		droite(72.0)
		avance(length*0.4)
		gauche(108.0)
		avance(length*0.4)
		pentaKoch(levels-1, length)
	}
}
