def hunt() {
  while (!estSurBiscuit) {
    if (!estFaceMur && isFacingTrail()) {
      baisseBrosse();
      avance();
      leveBrosse();
    } else {
      gauche();
    }
  }
  prendBiscuit()
}

// Copy your isFacingTrail() here

def isFacingTrail():Boolean = {
  if (!estFaceMur) {
    avance();
    if (getCouleurSol()==Color.green) {
      recule();
      return true
    }
    recule();
    return false
  }
  return false
}