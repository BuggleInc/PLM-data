def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if (steps==0){
    return
  }
  else{
    avance(length);
    treedroit();
    treegauche();
    recule(length);
  }
}

def tree(steps:Int, length:Double, angle:Double, shrink:Double)  