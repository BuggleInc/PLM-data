def keepHandOnSideWall() {
  if (!estFaceMur) {
    gauche();
    if (estFaceMur()) {
      droite();
      avance();
    }
  } else {
    droite();
  }
}

override def run() {
  while (!estSurBiscuit) {
    keepHandOnSideWall();
  }
  prendBiscuit();
}