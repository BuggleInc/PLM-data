def bubbleSort2() {
  var trie: Boolean=false
  var N:Int=0
  while(trie==false){
    
    for (i<-0 to getNombreValeurs()-2-N){
      if(!plusPetit(i,i+1)){
        echange(i,i+1)
      }
    }
    N=N+1
    if(N==getNombreValeurs()-1){
      trie = true
    }
  }
}