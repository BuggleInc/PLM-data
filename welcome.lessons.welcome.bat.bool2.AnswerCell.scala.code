def answerCell(isMorning:Boolean, isMom:Boolean, isAsleep:Boolean):Boolean = {
  if (isMorning && isMom && !isAsleep)
  {return true}
  if (isMorning && !isMom)
  {return false}
  if (!isMorning && !isAleep)
  {return true}
  if (isAsleep)
  {return false}
  return false}