def insertionSort() {
  
  for (i <- 1 to getNombreValeurs()-1) {
    
    var w = i
    
    while (w > 0 && getValeur(w-1) > getValeur(i)) {
      
      echange(w,w-1)
      w = w - 1
      
    }
    
    setValeur(w,getValeur(i))
    
  }
  
}