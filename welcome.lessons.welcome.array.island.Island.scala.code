def island(num:Array[Int]): Int = {
  var compteur = 1
  var valeur = num(0)
  var n = num.length
  for (i <- 0 to n-1) {
    if (num(i) != valeur) {
      compteur = compteur +1
      valeur = num(i)
    }
  }
  return compteur/2
}