def bubbleSort()  {
  var length : Int = getValueCount();
  var i : Int = 0;
  var j : Int = 0;
  for(i <- 0 to length)
  {
  	for(j <- i to length)
    {
      if(isSmaller(i,j))
      {
        swap(i,j);
      }
    }
    
  }
    
}