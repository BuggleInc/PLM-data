def cocktailSort() {
  for(i <- 0 to getNombreValeurs-2) {
    if (!plusPetit(i,i+1)){
      echange(i,i+1)
    }
  }
  for(k <- getNombreValeurs-2 to 0 by -1) {
    if (!plusPetit(k-1,k)){
      echange(k-1,k)
    } 
  }
}
