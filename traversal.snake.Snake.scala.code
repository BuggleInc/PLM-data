override def run() {
  brushDown();
  while (!estFaceMur) {
    forward();
  if (getDirection() == Direction.EST) {
    if (!estFaceMur) {
      left(); forward();
    }
  }
  if (getDirection() == Direction.OUEST) {
   	if (!estFaceMur) {
      right(); forward();
    }
  }
  }
}