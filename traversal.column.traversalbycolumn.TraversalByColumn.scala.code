override def run() {
  var cpt:Int=0
  ecritMessage(cpt)
  while ((getY()!=getMondeHauteur()-1)&&(getX()!=getMondeLargeur()-1)) {
    if (!estDosMur()) {
      setPos(getX(),getY()+1)
      cpt=cpt+1
    } else {
      setPos(getX()+1,0)
      cpt=cpt+1
    }
    ecritMessage(cpt)
  }
}