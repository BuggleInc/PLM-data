def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {
	spiral(steps,angle,length,increment);
	 avance(length);
	 gauche(angle);
	 for (i<-0 to steps-1 by -1){
	 	spiral(i,angle,length+increment,increment);
	 	avance(length+increment);
	 	gauche(angle);
	 		
	}
}
