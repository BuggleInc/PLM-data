import math

def crab(levels, length):
  if levels == 0:
    avance(length)
  else:
    gauche(45)
    crab(levels-1,length / math.sqrt(2.0))
    droite(90)
    crab(levels-1,length / math.sqrt(2.0))
    gauche(45)