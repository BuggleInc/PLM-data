def max1020(a:Int, b:Int):Int = {
  if ((Math.max (a,b)<10)|| (a>20 && b>20)||(a<10 && b>20) ||(a>20 && b<10)){
    return 0
  }
  if (Math.max(a,b)>20) {
    return Math.min(a,b)
  }
  return Math.max (a,b)
}
