def noTriples(nums:Array[Int]): Boolean = {
  var n = nums.length
  var noTriplet = true
  for (i <- 0 to (n-3))
  {
    if (nums(i)==nums(i+1) && nums(i+1)==nums(i+2))
    {
      noTriplet= false
    }
  }
  return noTriplet
}