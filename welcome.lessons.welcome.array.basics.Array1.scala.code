override def run() {
var hauteur: Int= getMondeHauteur() 
var T = new Array[Color] (hauteur)
for ( k <- 0 to (hauteur-1 )){
	
	T(k)=getCouleurSol()
	avance()
} 
for ( p <- 0 to (hauteur - 1) ) {
	gauche()
	avance()
	droite()
	avance()
	for (o <- 0 to (hauteur - 1)) {
		setCouleurBrosse( T(o))
		baisseBrosse()
		leveBrosse()
		avance()
		}
	}
	recule()
	gauche()
	recule()
	droite()
	
}
