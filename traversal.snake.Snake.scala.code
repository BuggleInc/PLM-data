override def run() {
  var test:Boolean = false
  brushDown();
  while (test ==false) {
    forward();
    if (estFaceMur) {
      if (getDirection() == Direction.EST) {
        left();
        if (!estFaceMur) {
          forward(); left();
        } else {
          test=true
        }
      } else {
        right();
        if (!estFaceMur) {
          forward(); right();
        } else {
          test=true
        }
      }
    }
  }
  right()
  return test
  
}