def dragon(order:Int, x:Double, y:Double, z:Double, t:Double) {
	var u=0.
	var v=0.
	if (order==1) {
		u = (x + z)/2 + (t - y)/2
		v = (y + t)/2 - (z - x)/2
		allerVers(u,v)
	} else {
		 setPos(x,y)
		 dragon(order-1, x/2, y/2, z/2, t/2)
		 setPos(z,t)
		 dragon(order-1, x/2, y/2, z/2, t/2)
	}
}