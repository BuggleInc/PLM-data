while not estFaceMur():
    avance()


for i in range(8):
    altitude = getY()
    longitude = getX()
    gauche()
    if estFaceMur():
        setX(longitude + 1)
    else : 
        gauche()
        if estFaceMur() :
            droite()
        setY(altitude - 1)
        droite()
        setX(longitude + 1)



