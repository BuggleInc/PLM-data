def bubbleSort()  {
var inverse:Boolean=true;
var T: Int=getNombreValeurs()
while(inverse==true){
     for(k<-0 to T-2){
	 
	   if(getValeur(k)>getValeur(k+1)){
	    echange(k+1,k)
	    inverse=true
	}
	
} 
}
}
