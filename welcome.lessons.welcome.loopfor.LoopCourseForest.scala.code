var i:Int = 0;
var j:Int = 0;
var k:Int = 0;

for (i <- 1 to 7; j <- 1 to 4) {
	for(k <- 1 to 4) {
		avance();
	}
	gauche();
	for(k <- 1 to 2) {
		avance();
	}
	droite();
	for(k <- 1 to 4) {
		avance();
	}
	droite();
	for(k <- 1 to 2) {
		avance();
	}
	gauche();
	for(k <- 1 to 4) {
		avance();
	}
	gauche();
}
