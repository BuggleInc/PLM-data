while (!isBackingWall) 
{def fonce() {while ((!isOverBaggle)&&(!isBackingWall)) {backward()}}
def retraite() {while (!isOverBaggle) {forward()}}
def caselibre() {while (isOverBaggle) {backward()}}
 def pickbaggle() {if (isOverBaggle) {pickupBaggle()}; retraite(); caselibre(); if(isCarryingBaggle) {dropBaggle}; backward()};
 fonce(); pickbaggle(); if (!isBackingWall) {backward()}}