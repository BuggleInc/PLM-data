def inOrderEqual(a:Int, b:Int, c:Int, equalOk:Boolean):Boolean = {
	if(a < b | (a <= b && equalOk)){
		if(b < c | (b <= c && equalOk)){
			return true;
		}
		else {
			return false;
		}
	}
	else{
		return false;
	}
}
