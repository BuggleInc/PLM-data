def bubbleSort()  { for (i <- 0 to getNombreValeurs()) {
                      for (j <- i+1 to getNombreValeurs()) { 
                         if (plusPetit(j,i)) { echange(i,j)
                           }
                        
                       } 
                     }
}
