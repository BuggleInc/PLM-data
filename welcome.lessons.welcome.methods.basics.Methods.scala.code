        def goAndGet() {droite();droite();
        	for (a<-1 to 6) {recule(); if(estSurBiscuit()) {prendBiscuit();}}
          for(b<-1 to 6) {do {avance()} while (estFaceMur); poseBiscuit();}
        }

        for (i <- 1 to 7) {
            goAndGet();
            right();
            forward();
            left();
        }
