boolean isFacingTrail() {
boolean greenCell = false;

	do{
    		if(getGroundColor().equals(Color.green)){
    			greenCell = true;
    			forward();
    		}
/*
    		if(isFacingWall()){
    			greenCell = false;
    			backward();
    			backward();
    		}

    		if(isOverBaggle()){
    			greenCell = true;
    		}
*/
	}while(!greenCell);

    return greenCell;
}//end function 
