//quatre pas en avant, deux pas à gauche, quatre pas à droite, deux pas à droite, et quatre pas à gauche

var i = 0
var j = 0

for(i <- 1 to 7)
{
	for(j <- 1 to 4)
	{
		avance()
	}
	gauche()
	for(j <- 1 to 2)
	{
		avance()
	}
	droite()
	for(j <- 1 to 4)
	{
		avance()
	}
	droite()
	for(j <- 1 to 2)
	{
		avance()
	}
	gauche()
	for(j <- 1 to 4)
	{
		avance()
	}
	gauche()
}
