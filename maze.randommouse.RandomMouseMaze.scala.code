override def run() {
  while(!estSurBiscuit){
    var rng:Int = random3()
    rng match{
      case 0 -> {if(estFaceMur){()} else {avance()}}
      case 1 -> droite()
      case 2 -> gauche()
    }
  }
    // Your code here 
}