def isFacingTrail():Boolean = {
  if(!estFaceMur()){
    avance()
    if(getCouleurSol()==Color.green){
      baisseBrosse()
      leveBrosse()
      avance()
    }
    else {gauche()}
  }
  else{gauche()}
}