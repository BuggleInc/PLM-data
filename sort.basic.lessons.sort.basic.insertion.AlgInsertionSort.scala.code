def insertionSort() {
  for (i <- 0 to getNombreValeurs()-1) {
    var v = getValeur(i)
    var lastCase = 0
    for (j <- i-1 to 0 by -1 ) {
      if (plusPetitQue(j,v)) {
        copie(j,j+1)
        lastCase = j
      }
      setValeur(v,lastCase)
    }
  }
}