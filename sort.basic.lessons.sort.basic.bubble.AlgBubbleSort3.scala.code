def bubbleSort3() {
	var lim = getNombreValeurs()-2;
	var ech = false;
	do{
		for(i<-0 to lim) {
			if(plusPetit(i+1, i)) {
				echange(i, i+1);
				ech = true;
			}
		}
		if(ech == false) {
			return;
		}
		lim = lim-1;
	}while(lim >= 0)
}
