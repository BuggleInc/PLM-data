def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {
var comp = 0;	
test
	
}

def test(a:Int):Boolean =
{
	if ( 12 < a && b < 20)
	{
		true
	}
	else 
	false
}

def test2 (a:Int, comp:Int): In
{
	if (test(a))
	{
		comp = comp +1 
	}

	return comp
}
