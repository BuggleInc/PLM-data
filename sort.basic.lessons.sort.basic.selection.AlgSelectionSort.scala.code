def selectionSort() {
  for (i<-0 to getNombreValeurs()-1) {
    var mini=getValeur(i)
    var compt=i
    for(j<-i to getNombreValeurs()-1) {
      if (getValeur(j)<mini) {
        mini=getValeur(j)
        compt=j
      }
    }
    echange(compt,i)
  }
}