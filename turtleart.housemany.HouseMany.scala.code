override def run() {
  def ETMaison(){
  for(x<-0 to 3){avance(30)
                droite(90)}
  avance(30)
  droite(30)
  for(x<-0 to 2){avance(30)
                droite(120)}
  gauche(30)
  recule(100)
  }
  
  ETMaison()
  
}