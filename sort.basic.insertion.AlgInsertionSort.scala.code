def insertionSort() {
  var n = getValueCount();
  
  for (i <- 1 to n - 1) {
    var v = getValue(i);
    
    var j = i;
    while (j > 0) {
      var w = getValue(j-1);
      if (w > v) {
        setValue(j, w);
      }
      
      j -= 1;
    }
    setValue(i,v);
  }
}