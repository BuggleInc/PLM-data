override def run() {
  baisseBrosse()
  def finale():Boolean = {
    var resultat:Boolean = false
    if(getDirection()==Direction.EST){
      gauche();
      resultat = estFaceMur();
      droite()
     }
    else if (getDirection()==Direction.OUEST){
      droite();
      resultat = estFaceMur();
      gauche()
    }
    return resultat
  }
  
  
  def serpent(){
    if(estFaceMur()){
      if(getDirection() == Direction.EST){gauche();avance();gauche();}
      else{droite();avance();droite();}
    }
    else{
      for(i <- 0 to 5){avance();}
    }
  }
  
  
  while(!finale()){serpent()}
  
  
}