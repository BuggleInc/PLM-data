def insertionSort() {

	var v = 0
	var cpt = 1

	for (i<-1 to getNombreValeurs()-1) {
		v=getValeur(i)
		cpt=i
			while (!plusPetitQue(cpt-1, v) && cpt>0) {
			copie(cpt-1, cpt)
			cpt-=1
			}
		setValeur(cpt-1, v)
	}
	
}
