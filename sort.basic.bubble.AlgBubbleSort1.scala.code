def bubbleSort()  {
  var listeTriee:Boolean=true
  var x=0
  x=getNombreValeurs
  while (listeTriee) {
    for (i <- 1 to x-1) {
       if (plusPetit(i+1,i)) {
         echange(i+1,i)
       } else {
         listeTriee=false
       }  
    }
  }
}