def pentaKoch(levels:Int, length:Double) {
	if (levels == 0) 
	{avance(length)
} else {
	avance(length*0.4)
	gauche(110)
	avance(length*0.4)
	droite(70)
	avance(length*0.4)
	droite(70)
	avance(length*0.4)
	droite(70)
	avance(length*0.4)
	gauche(110)
	}
}
