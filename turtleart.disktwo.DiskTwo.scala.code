override def run() {
  for (i <-1 to 9) {
    setColor(Color.black)
  for (i<- 1 to 360/18) {avance(100);recule(100);droite(1)};
  setColor(Color.red);
  for (i<- 1 to 360/18) {avance(100);recule(100);droite(1)}
                   
  }}