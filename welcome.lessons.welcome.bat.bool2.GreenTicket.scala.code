def greenTicket(a:Int, b:Int, c:Int):Int = {
	if ( a != b && b != c && c != a ) {
		return 0
	}
	else if ( a == b && b == c && c == a ) {
		return 20 
	}
	else if ( a == b || b == c || a == c ) {
		return 10
	}
	
		
		
	}

