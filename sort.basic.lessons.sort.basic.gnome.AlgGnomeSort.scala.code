def gnomeSort() {
  var pos = 0
  // ma version commence ? 1 normalement
  while (pos < getValueCount()-1){
  	if (!isSmaller(pos,pos-1)){
  		pos = pos+1
  	}else{
  		swap(pos,pos-1)
  		//if(pos > 1){
 	 			pos = pos-1
  //  }
    if(pos == -1){
      pos=1
    }
   }
	}
}
