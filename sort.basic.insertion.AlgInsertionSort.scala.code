def insertionSort() {
  var n:Int = getNombreValeurs()
  var v:Int = 0
  var j:Int = 0
  for (i <- 1 to n-1) {
    v = getValeur(i)
    j = i-1
    while (j!=(-1) && (!plusPetitQue(j, v))) {
      setValeur(j+1,getValeur(j))
      j = j-1
    }
    setValeur(j+1,v)
  }
}