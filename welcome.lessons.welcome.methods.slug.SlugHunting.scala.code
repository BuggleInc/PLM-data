def hunt() 
{
	while (!estSurBiscuit()) 
	{
		if (isFacingTrail()) 
		{
			baisseBrosse()
			avance()
			leveBrosse()
		} 
		else 
		{
			gauche()
		}
	}
prendBiscuit()
}
def isFacingTrail():Boolean =
{
	if (estFaceMur())
		return false
	forward()
	val vert = getGroundColor() == equals(Color.green)
	backward()
	return vert
}