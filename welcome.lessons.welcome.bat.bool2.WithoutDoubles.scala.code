def withoutDoubles(die1:Int, die2:Int, noDoubles:Boolean):Int = {
  (die1,die2,noDoubles)match{
    case (die1,die2,noDoubles) if die1==die2 && noDoubles && die1==6 => die1+1
    case (die1,die2,noDoubles) if die1==die2 && noDoubles => die1+1+die2
    case (_,_,_) => die1+die2
  }
}