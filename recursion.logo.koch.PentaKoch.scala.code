def pentagonKoch(levels:Int, length:Double) {
  if (levels > 0) {
    pentagonKoch(levels - 1, length*0.4)
    gauche(108)
    pentagonKoch(levels - 1, length*0.4)
    droite(72)
    pentagonKoch(levels - 1, length*0.4)
    droite(72)
    pentagonKoch(levels - 1, length*0.4)
    droite(72)
    pentagonKoch(levels - 1, length*0.4)
    gauche(108)
  } else {
    avance(length)
  }
}