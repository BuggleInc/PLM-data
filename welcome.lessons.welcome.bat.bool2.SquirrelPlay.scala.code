def squirrelPlay(temp:Int, isSummer:Boolean):Boolean = {
  var x=false
  if (isSummer) {
    if (temp<=100 && temp>=60) {
      x=true
    }
  } else if (temp<=90 && temp>=60) {
      x=true
  }
  x
}