def insertionSort() {
  var lgr = getNombreValeurs();
  for ( i<-1 to lgr-1 ){
  	var v=getValeur(i);
  	for ( j<-1 to i){
      if (getValeur(i-j)>v){
        echange(i-j,i-j-1);
      }
      else {setValeur(i-j+1,v)}
    }
  }
}