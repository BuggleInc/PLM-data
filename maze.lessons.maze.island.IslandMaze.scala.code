override def run() {
  while (!estSurBiscuit()) {
    murNord()
    while (murGauche()==false) {
      gauche();
    }
    	keepHandOnSideWall()
  }
  prendBiscuit()
}


def keepHandOnSideWall() { 
    gauche();
  	if (estFaceMur()) {
    droite();
      if (!estFaceMur()) {
        avance();
      } else droite()
    } else {
    	avance();
    }
}

def murGauche() {
  var res = false;
  gauche();
  if (estFaceMur()) {
    res = true
  }
  droite
  return res
}

def murNord() {
  while (!estFaceMur()) {
    avance();
  }
  while(estFaceMur()) {
    keepHandOnSideWall()
  }
}