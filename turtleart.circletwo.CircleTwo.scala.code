override def run()
{
  val rayon = 50;
  
  droite(90);
  leveCrayon();
  avance(rayon);
  baisseCrayon();
  cercle(60);
  
  leveCrayon();
  avance(rayon);
  baisseCrayon();
  cercle(rayon*2);
  
  leveCrayon();
  gauche(180);
  avance(rayon*2);
  droite(90);
}