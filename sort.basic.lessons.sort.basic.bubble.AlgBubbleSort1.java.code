public void bubbleSort()  {
  
  boolean swap = true;
  while(swap){
  swap = false;  
  for(int i = 0 ; i < getNombreValeurs(); i++ )
{
    if(plusPetit(getValeur(i),getValeur(i+1))){
      swap =true;
      echange(i+1,i);
      
    }
    
  } 
  }
}