def greenTicket(a:Int, b:Int, c:Int):Int = {
	var rep = 0 
	if ( a == b && a == c && b == c) {
			rep = 20
		} else {
			if ( (a == b ) || (b == c ) || (a == c ) ) {
			rep = 10
		} else { 
			rep = 0
		}
	}
		
	return rep
}
