while (!isFacingWall) {
  right();right();forward();
  if (isOverBaggle) {
    pickupBaggle()
    while (!isOverBaggle) {
      backward()
    }
    forward()
    dropBaggle()
  }
}
right();forward();left();forward()