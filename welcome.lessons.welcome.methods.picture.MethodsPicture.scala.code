override def run() {
  avance()
  faireMotif(c:Color)
}

def faireV(){
  baisseBrosse()
  leveBrosse()
  avance()
  gauche()
  avance()
  baisseBrosse()
  leveBrosse()
  recule()
  droite()
  avance()
  baisseBrosse()
  leveBrosse()
  
}

def faireMotif(){
  for (compteur <- 1 to 4)
  faireV(c:Color)
  avance()
  gauche()avance()
}