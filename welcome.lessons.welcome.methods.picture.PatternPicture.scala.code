override def run() {

	def motif(x) {
		setCouleurBrosse(x)
		avance()
		baisseBrosse()
		leveBrosse()
		recule()
		gauche()
		avance()
		baisseBrosse()
		leveBrosse()
		recule()
		droite(e)
	}

	def grandMotif(a:Color,b:Color,c:Color,d:Color) {
		motif(a)
		for (i<-1 to 3)
			avance()
		gauche()
		motif(b)
		for (i<-1 to 3)
			avance()
		gauche()
		motif(c)
		for (i<-1 to 3)
			avance()
		gauche()
		motif(d)
		for (i<-1 to 3)
			avance()
		gauche()
	}

	grandMotif(col
}
