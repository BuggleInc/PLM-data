def isFacingTrail():Boolean = {
  	while (!estSurBiscuit) {
    	if (estFaceMur()==true) {
      	left();
    	}
      forward();
  		if (getCouleurSol()==Color.green) {
      	backward(); brushDown(); forward(); brushUp();
    	} else {
      	backward(); left();
    	}
		}
} 
  
