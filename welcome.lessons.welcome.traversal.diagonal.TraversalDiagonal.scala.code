var diag = 0;
override def run() {
  var l:Int = getMondeLargeur()
  var h:Int = getMondeHauteur()
  var nombre:Int = 1
  car cases:Int = 1
  for (i <- 0 to l-1) {
    ecrireMessage(cases)
    cases = cases + 1
    for (j <- 1 to nombre-1) {
      setPos()
      ecrireMessage(cases)
    }
  }
}
