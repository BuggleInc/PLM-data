def findBaggle() : (Int, Int) = {
  var h = getMondeHauteur();
  var l = getMondeLargeur();
  for (i <- 0 to (h-1)) {
    for (j <- 0 to (l-1)) {
      if (aBiscuit(i,j)) {
        var x = i
        var y =i
        return(x,y)
      }
    }
  }
}

override def run() {
	println(findBaggle());
}