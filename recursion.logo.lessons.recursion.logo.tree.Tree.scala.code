def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  
  if (steps != 0) {
    
  	avance(length);
 	 	droite(angle);
    current(steps);
  	tree(steps-1, shrink*length, angle, shrink);
    current(steps);
  	gauche(angle);
  	gauche(angle);
		tree(steps-1, shrink*length, angle, shrink); 
    droite(angle);
    recule(length)
    
  }
  
}