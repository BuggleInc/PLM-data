
def selectionSort() {
  var tampon = getValeur(0)
  var index = 0
  for( i <- 0 to getNombreValeurs()-1){
    for(j <- i to getNombreValeurs()-1){
      if(plusPetitQue(j,tampon)){
      	tampon= getValeur(j)
        index=j}
      	
     
    }
    echange(i,index)
    
  }
}