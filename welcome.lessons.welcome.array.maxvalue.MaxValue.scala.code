def tri(t:Array[Int]) {
	var i,j,k:Int=0;

	for (x<- 0 to t.length-1) {
		for (z<-x+1 to t.length-2) {
			i=t(x);
			j=t(z);
			if (t(i)>=t(j)) {
				k=i;
				i=j;
				j=k;
			}
		}
	}
}

def maxValue(nums:Array[Int]): Int = {
	tri(nums);
	return nums(nums.length-1)
}
