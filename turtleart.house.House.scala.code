override def run() {
  baisseCrayon()
  for(i <- 1 to 4) {
  avance(100)
  droite(90)
  }
  avance(100)
  gauche(90)
  for(i <- 1 to 3) {
    recule(100)
    gauche(120)
  }
  droite(90)
  recule(100)
}