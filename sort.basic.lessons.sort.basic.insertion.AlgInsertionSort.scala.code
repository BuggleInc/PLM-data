def insertionSort() {
	for (i<-1 to getNombreValeurs()-1){
		var x = getValeur(i);
		var j = i ; 
		while (j>0 && getValeur(j-1) > x ){
			setValeur(j,getValeur(j-1));
			j -=1;
		}
		setValeur(j,x) ; 
	}
}

