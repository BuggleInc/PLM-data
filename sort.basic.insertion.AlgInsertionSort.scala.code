def insertionSort() {
  var n = getNombreValeurs()
  var v = getValeur(0)
  var j = 1
  for (i <- 1 to n-2) {
    v = getValeur (i)
    while ( i-j >=0 && !plusPetitQue((i-j),v)) {
        copie(i-j,i-j+1)
        j += 1 
      } 
    setValeur(i-j+1,v)
    j=1
  }
}
