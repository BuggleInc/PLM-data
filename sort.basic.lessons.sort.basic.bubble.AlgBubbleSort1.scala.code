def bubbleSort()  {
	var inverse = true;
	
	do{
		for(i<-0; i<getNombreValeurs(); i++) {
	         if(plusPetit(i+1,i)) {
			echange(i+1,i);
	         }
	         else {
	         	var inverse = false;
	         }
		}
      }while( inverse )
      
}
