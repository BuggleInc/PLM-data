def maxValue(nums:Array[Int]): Int = {
	var k:Int = nums(0)
	for (i<-1 to nums.length-1) {
		if (nums(i)>k) {k=nums(i)}
		}
		return k

def island(num:Array[Int]): Int = {
  var a=0
  var b=0
  var max = maxValue (num)
  var l = num.length
  for (j<-0 to max) {
  
  }
}
