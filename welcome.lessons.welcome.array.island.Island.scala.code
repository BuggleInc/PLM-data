def island(num:Array[Int]): Int = {
  var nbPlateau:Int=0
  var pred:Int=num(0)
  for (i <- 1 to num.length-1){
    if (num(i)>pred)
    	nbPlateau=nbPlateau+1
    pred=num(i)
  }
  return nbPlateau
}