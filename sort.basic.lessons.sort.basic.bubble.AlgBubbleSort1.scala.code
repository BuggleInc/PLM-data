def bubbleSort()  {
    var sorted:Boolean = false;
    do {
        sorted = true;
        for (i <- 0 to getNombreValeurs()-2) {
            if (!plusPetit(i,i+1)) {
                sorted = false;
                echange(i,i+1);
            }
        }
    } while (!sorted);
}
