def bubbleSort3()  {
  var lastEchange: Boolean = false
  for (i <- getNombreValeurs()-2 to 0 by -1) {
    lastEchange = false
    for (j <- 0 to i) {
      if (plusPetit(j + 1,j)) {
        echange(j,j+1)
        lastEchange = true
      }
    }
    if (lastEchange == true) { return }
  }
}