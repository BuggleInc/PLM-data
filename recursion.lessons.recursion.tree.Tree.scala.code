def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
var s= steps
var l=length
	if(s!=1){
		current(s)
		avance(length)
		droite(angle)
		s=s-1
		l=l*shrink
		tree(s,l,angle,shrink)
	}
	else{
		gauche(2*angle)
	}
}
