def max1020(a:Int, b:Int):Int = {
  if ( (a>20 || a <10) && (b>20 || b<10) ){
    return 0;
  } else if ( Math.max(a,b) <= 20 ){
    return Math.max(a,b); 
  } else {
    if ( a == Math.max(a,b) ){
      return b;
    }
    else { 
      return a;
    }
  }
}