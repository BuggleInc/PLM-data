override def run() {
	while (!estSurBiscuit) {
		while ((murAGauche) && (!estFaceMur)) {
			avance();
		}
		gauche()
		if (!estFaceMur) {
			avance()
		}
	}
	
}
def whereWall():Int ={
	gauche();
	if (!estFaceMur) {
		droite()
		return false
	} else {
		droite()
		return true
	}
}
