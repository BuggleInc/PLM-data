def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  
  gauche(angle);
  avance(length);
  tree(steps-1, length*multiplicateur, angle, multiplicateur);
  recule(length);
  
  
  droite(2*angle);
  avance(legngth);
  tree(steps-1, length*multiplicateur, agnle, multiplicateur);
  recule(length);
  gauche(angle);
  
  
}