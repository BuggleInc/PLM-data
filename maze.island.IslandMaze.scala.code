override def run() {
    while(!estSurBiscuit()){
    var possible = true;
    while(possible){
      getDirection() match{
        case Direction.EST => gauche();
        case Direction.WEST => droite();
        case Direction.NORTH => ();
        case Direction.SOUTH => ();
      }
      if(!estFaceMur()){
        avance();
      } else {
        possible = false;
      }
    }
    if(estFaceMur()){
      gauche();
      if(estFaceMur()){
        droite();droite();
      } else {
        avance();
      }
    } else {
      gauche();
      if(estFaceMur()){
        droite();avance();
      } else {
        avance();gauche();
        if(!estFaceMur()){
          avance();
        } else {
          droite();
        }
      }
    }
  }
  prendBiscuit();
}

def vaAuNord(){
  
} 