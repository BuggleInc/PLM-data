def haveBaggle():Boolean ={
	var y:Boolean=false;
	
	for (x<-0 to 5) {
		for (x<-0 to 6) {
			avance();
			if (estSurBiscuit()) {
				setY(6);
				y= true;
			}
		}
		droite();
		avance();
		gauche();
	}
	return y
}
