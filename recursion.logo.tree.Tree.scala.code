def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  avance(length)
  if (steps==0) {
    avance(length)
    recule(length)}
    
  if (steps>0) {
    avance(length)
    droite(angle)
    tree(steps-1,length*shrink,angle,shrink)
    gauche(angle)
    gauche(angle)
	  tree(steps-1,length*shrink,angle,shrink)
    recule(length)
    
    
    

    
  }
}