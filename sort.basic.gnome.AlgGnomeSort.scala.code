def gnomeSort() {
  var pos = 0
  val longueur = getNombreValeurs()
  while (pos != longueur-1) {
    if (getValeur(pos + 1) > getValeur(pos + 2)) {
      echange(pos+1,pos+2)
      if (pos == 0) {
        pos += 1
      } else {
        pos -= 1
      }
    } else {
      pos += 1
    }
  }
}