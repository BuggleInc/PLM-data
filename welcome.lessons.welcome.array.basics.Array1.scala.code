override def run() {
  var T= new Array[color](6)
  for(i<-0 to 5) {
    T(i)=getCouleurSol()
    avance()
  }
  avance()
  for(i<-1 to 5) {
    gauche(); avance(); droite(); avance()
    for(i<-0 to 5) {
      setCouleurBrosse(T(i))
      baisseBrosse()
      leveBrosse()
      avance()
    }
  }
  
}