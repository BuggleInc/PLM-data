var c:Int=0
var seenBaggle:Boolean=false

def haveBaggle():Boolean = {
  for(c<-1 to 6){
    if(isOverBaggle()){
      seenBaggle=true
    }
    forward();
  }
  for(e<-1 to 6){
    backward();
  }
  return seenBaggle
}