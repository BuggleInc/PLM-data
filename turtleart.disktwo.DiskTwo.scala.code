override def run() {
    var c:Color=Color.black;
    for(i<-1 to 9){
      qcircle(Color.red);
      qcircle(Color.black);
      
}
}
    def qcircle(c:Color){
    	setColor(c)
    	for(j<-1 to 360/18){
    		forward(100);
        penUp();
    		backward(100);
        penDown();
    		right(1);
      }
}