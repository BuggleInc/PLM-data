def bubbleSort()  {
  var tri:Boolean = true
  while (tri) {
    tri=false
  for(i<-1 to getNombreValeurs()-1) {
      if(getValeur(i)<getValeur(i-1)) {
        echange(i,i+1)
        tri=true
      }
    }
  }
}