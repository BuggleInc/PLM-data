def island(num:Array[Int]): Int = {
  var ni : Int = 0
  var alt : Int =0
  
  for (el <- num) {
    if (el > alt) {
      ni = ni+ 1
      alt = el
    } else if ( el < alt){
      alt = el
    }
  }
  return ni
}
  