 def hunt(c:Color) {
     // Write your code here
 }









def isFacingTrail():Boolean = { 
	 if ( !estFaceMur() ) { avance(); }
	 else { gauche(); avance(); }
	if (getCouleurSol() == Color.green) {
		 recule();
		 return true
	} else {
           recule();
		 return false
		}
		
	} 