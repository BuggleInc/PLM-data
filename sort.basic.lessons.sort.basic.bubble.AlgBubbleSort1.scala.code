def bubbleSort()  {
  var tri:Boolean = false
  while (!tri) {
    tri=true
  for(i<-1 to getNombreValeurs()-1) {
      if(getValeur(i)<getValeur(i-1)) {
        echange(i,i-1)
        tri=false
      }
    }
  }
}
