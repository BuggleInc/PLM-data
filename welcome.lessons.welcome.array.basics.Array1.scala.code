override def run() {
  colone()
  for (k <- 2 to getMondeLargeur())
  gauche();avance();droite();parcoursColone()


  def colone(){
    var T = new Array[Color](getMondeHauteur())
    for (i <- 0 to lenght.T-1){
      T(i)=getCouleurSol()
      avance()
    }
  }

  def parcoursColone(tableau:Array[Color]){
    for (l<- 0 to lenght.tableau -1){
      avance(); setCouleurBrosse(tableau[l]); baisseBrosse(); leveBrose()
    }
  }