def bubbleSort3() {
    var tri:Boolean = false
  var n=0
  while (!tri) {
    tri=true
  for(i<-1 to getNombreValeurs()-n-1) {
      if(getValeur(i)<getValeur(i-1)) {
        echange(i,i-1)
        tri=false
      }
    }
    n=n+1  }
}