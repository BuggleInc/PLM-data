def faireCarree(n:Double) {
  avance(n)
  droite(90)
  avance(n)
  droite(90)
  avance(n)
  droite(90)
  avance(n)
  
  
}

override def run() {
  faireCarree(100)
  faireCarree(100)
  faireCarree(100)
  faireCarree(100)
}
