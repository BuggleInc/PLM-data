public void run() {
int baggleCount = 0;

	do{
		forward();
		
		if(isOverBaggle()){
			baggleCount++;
		}
		
		if(crossing()){
			if(baggleCount >= 3){
				left();
			}else{
				right();
			}
			baggleCount = 0;
		}

		if(exitReached()){
			forward();
		}
		
	}while(!exitReached()); 
}
