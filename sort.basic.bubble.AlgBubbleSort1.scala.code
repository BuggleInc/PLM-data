def bubbleSort()  {
  var listeTriee:Boolean=false
  var x=0
  x=getNombreValeurs
  do {
    for (i <- 0 to x-2) {
      listeTriee=false
       if (plusPetit(i+1,i)) {
         echange(i+1,i)
         listeTriee=true
       } 
    }
  } while (listeTriee)
}
