def reverse(l:List[Int]): List[Int] = {
	def fonctionInterne(l:List[Int],mem:List[Int]):List[Int] = {
		if(l==Nil){
			return mem;
		}else{
			return fonctionInterne(l.tail,l.head::mem);
		}
	}
	return fonctionInterne(l,Nil);
}
