def has271(nums:Array[Int]): Boolean = {

	var bool:Boolean = false;

	for (i <- 0 to nums.length - 3)
	{
		if (nums(i + 1) == nums(i) + 5 && nums(i + 2) == nums(i) - 1) bool = true;
	}

	return bool;
}
