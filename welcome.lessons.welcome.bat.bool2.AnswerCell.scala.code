def answerCell(isMorning:Boolean, isMom:Boolean, isAsleep:Boolean):Boolean = {
	var rep = false
	if (!(isAsleep)) {
		if (isMorning) {
			if (isMom) {
				rep = true 
			}
		}
	}
	else rep = true
	return rep
}
