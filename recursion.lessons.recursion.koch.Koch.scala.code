def snowFlake (levels:Int, length:Double) {
    snowSide(levels, length);
    right(120);
    setColor(Color.blue);
    snowSide(levels, length);
    right(120);
    setColor(Color.orange);
    snowSide(levels, length);
    right(120);
}
def snowSide(levels:Int, length:Double) {

	def triangle(long:Double) {
		avance(long)
		gauche(60)
		avance(long)
		droite(120)
		avance(long)
		gauche(60)
		avance(long)
		
	}
if (levels== 0) {
	avance(length) ; return
}

triangle(length/(3*levels))


}
