def insertionSort() {
	val lgr=getNombreValeurs()
  copie(2,5)
  for(pivot<-1 to lgr-1){
    var vtemp=getValeur(pivot)
    var ptemp=pivot
    while(ptemp>0){if(getValeur(ptemp)>=vtemp){copie(ptemp,ptemp+1)};ptemp-=1}
    setValeur(vtemp,ptemp)
  }}