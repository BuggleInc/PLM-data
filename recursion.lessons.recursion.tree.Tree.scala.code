def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
	if(steps!=0){
		avance(length-1)
		droite(angle)
		tree(steps-1,length,angle,shrink)
		gauche(2*angle)
		tree(steps-1,length,angle,shrink)
		recule(length)
	}
}

