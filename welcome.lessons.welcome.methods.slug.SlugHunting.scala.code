def isFacingTrail():Boolean = {
  if (estFaceMur()){
    return false
  } else {
  avance()
  if (getCouleurSol()==Color.green){
    recule()
    return true
  }
  recule()
  return false
  }
}

def hunt() {
  baisseBrosse()
  while(isFacingTrail()){
    avance()
    if (!isFacingTrail()){
      gauche()
      if (!isFacingTrail()){
        droite()
        droite()
      }
    }
    if (estSurBiscuit()){
      prendBiscuit()
    }
  }
}