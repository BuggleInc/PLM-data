def arrivee(x,y) :
  return x==6 and y==0 


def virageG() :
  left()
  avance()
  left()

def virageD() :
  right()
  avance()
  right()
  
  



gagne=False

while not gagne :
  
  if estFaceMur() :    
    if getDirection()==Direction.EAST :
      virageG()
  	else:
      virageD()
  
  baisseBrosse()
  avance()
  ecritMessage(getY())
  gagne=arrivee(getX(),getY())
  ecritMessage(gagne)
