override def run() {
	var h:Int = getMondeHauteur()
	var l:Int = getMondeLargeur()
	var c:Int = 0
	ecritMessage(c)
	for (i<- 0 to l-1) {
		for (j <- 1 to h-1) {
		setPos (i,j)
		c = c+1 
		ecritMessage(c)}}
	
	
}
