def bubbleSort2()  
{
	var taille:Int = getNombreValeurs();
	var i:Int = 0;
	var j:Int = 0;

	for (i <- 0 to taille -2)
	{
		for (j <- 0 to taille -2)
		{
			if (!plusPetit(j, j + 1)) 
					echange(j, j + 1);
		}
	} 
}
