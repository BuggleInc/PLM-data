 def hunt(c:Color) {
     // Write your code here
 }

def isFacingTrail():Boolean = {
	var sol: Boolean = false;
	if (!estFaceMur()) {
		avance()
		if (getCouleurSol() == Color.green) {
			recule()
			sol = true;
		}
		else {recule()}
	}
	return sol
}
