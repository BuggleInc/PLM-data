override def run() {
  while (!estSurBiscuit()) {
    while (getDirection != Direction.NORTH) {
      gauche()
    }
    while (!estFaceMur) {
      avance()
    }
    if (estFaceMur()) {
      keepHandOnSideWall()
      keepHandOnSideWall()
      keepHandOnSideWall()
    }
  }
}

def keepHandOnSideWall() = {
  gauche()
  while (estFaceMur()) {
    droite()
  }
  avance()
}