def insertionSort() {
  for (i <- 1 to getNombreValeurs-1){
    var v:Int = getValeur(i)
    var j:Int = 0
      while (plusPetitQue(i-j, v) && j<=i) {
        echange(i-j, i-j+1)
        j+=1
      }
  }
}