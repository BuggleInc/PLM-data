def bubbleSort()  {

	var lgr = getNombreValeurs();
	var tri = false;
	do {
		
		for(i<-0 to lgr-2) {
			if(plusPetit(i:Int, i+1:Int) == false) {
				echange(i, i+1);
				 tri = false;
			} else {
				tri = true;
			}
		}
		
	} while(tri == false)
	
}
