def insertionSort() {
  var v=0
  var n= getNombreValeurs()-1
  for (i<-1 to n){
    v = getValeur(i-1)
    while(!plusPetitQue(n,v) && n >= 0 ){
      copie(n,n+1)
      n= n-1
    }
   setValeur(n,v) 
  }
}