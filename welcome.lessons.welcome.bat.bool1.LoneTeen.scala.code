def loneTeen(a:Int, b:Int):Boolean = {
	def ado(d:Int): Boolean = {
		if ( d > 12 && d < 20) {
		return true
		}else{
			return false
		}
	}
	if (ado(a) && ado(b)) {
		return false
	}else {
		if (!ado(a) && !ado(b)) {
			return false
		}else {
			return true
		}
	}
}
