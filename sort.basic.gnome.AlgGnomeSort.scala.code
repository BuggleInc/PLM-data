def gnomeSort() {
  var pos = 0
  while (pos != getNombreValeurs()-1){
    if (plusPetit(pos+1, pos)){
      echange(pos+1, pos)
      if (pos !=0) {
        pos-=1
      } else {
        pos += 1
      }
    } else {
      pos += 1
    }
  }
}