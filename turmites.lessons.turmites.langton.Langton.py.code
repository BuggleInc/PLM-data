def setGroundColor(col):
  setBrushColor(col)
  brushDown()
  brushUp()

def step():
  col = getGroundColor()
  if col == Color.black:
    setGroundColor(Color.white)
    left()
  else:
    setGroundColor(Color.black)
    right()
  forward()
