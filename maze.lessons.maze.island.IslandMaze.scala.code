override def run() {
  while (!estSurBiscuit()){
    NORD()
    keepHandOnSideWall()
  }
  prendBiscuit()
}


def keepHandOnSideWall(){
  if (!estFaceMur()){
    avance()
  } else {
    gauche()
    if (!estFaceMur()){
      avance()
    } else {
      droite()
      droite()
      if (!estFaceMur()){
        avance()
      } else {
        droite()
        avance()
      }
    }
  }}

  
def NORD(){
  setDirection(Direction.NORTH)
  while (!estFaceMur()){
    if (!estFaceMur()){
      avance()
    }}
      droite()
      if (!estFaceMur()){
        avance()
        gauche()
      } else {
        gauche()
        gauche()
        if (!estFaceMur()){
          avance()
          droite()}
      }
    }
  
