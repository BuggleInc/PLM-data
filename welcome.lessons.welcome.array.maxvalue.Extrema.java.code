int extrema(int[] nums) {
	int max = tab[0];
	int min = tab[0];
	for(int i = 1; i < tab.length; i++){
		if(min > tab[i])
			min = tab[i];
		else{
		if(max < tab[i])
			max = tab[i];
		}	
	}
	return max - min;
}
