def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
 
  if (speed>=61 && speed<=80|| isBirthday==true && speed>=66&& speed<=85){
    return 1
  }
  if (speed <=60 ||isBirthday==true && speed<=65)
    return 0
  }
  ;{
    return 2
  }
}