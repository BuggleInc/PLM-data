def dessineTriangle(length:Double) {
  avance(length)
  droite(120)
  
  avance(length)
  droite(120)
  
  avance(length)
  droite(120)
}

def sierpinski(level:Int, length:Double) {
  if(level == 0) {
    dessineTriangle(length)
    return
  }
}