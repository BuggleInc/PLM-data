def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {

if(steps-increment<=0){
	gauche(angle)
	avance(steps)
}else {
	avance(increment)
	spiral(steps-increment, angle, length, increment)
}


}