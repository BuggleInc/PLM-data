override def run() {
	var compteur = 0;
	while (!estSurBiscuit()) {
		if (compteur == 0) {
			while (!estFaceMur()) {
				avance();
			}
			droite();
			compteur = compteur - 1;
			ecritMessage("Compteur");
		} else if (compteur !=0) {
			if (estFaceMur()) {
				gauche();
				compteur = compteur + 1;
				ecritMessage("Compteur");
				while (estFaceMur()) {
					droite();
				}
				compteur = compteur - 1;
				ecritMessage("Compteur");
				avance();
			} else {
				avance();
			}
		}
	}
}