override def run() {
var  a=360.toDouble/7
  avance(80);droite(a);avance(80);droite(a);avance(80);droite(a);avance(80);droite(a);avance(80);droite(a);avance(80);droite(a);avance(80);droite(a);
    
  }