def indexOfValue(nums:Array[Int] ,lookingFor:Int): Int = {
  var n  = nums.length
  var pos = -1
  for (k<-0 to n-1){
    if (nums(k)==lookingFor){
      pos=k
      return pos
    }else{
      pos=pos
    }
  }
return pos
}
