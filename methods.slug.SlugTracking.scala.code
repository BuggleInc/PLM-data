def isFacingTrail():Boolean = {

   		if(isFacingWall()){
      return false
      }
 			 
      else if (getGroundColor() == Color.green){
      backward()
      return true
    }
    
  	else {
    backward()
    left()
      return false
  }
}