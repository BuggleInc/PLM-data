var finished : Boolean = false

while (finished!= true) {
  while (!isFacingWall()){
  forward()
 }
  if (isFacingWall()){
    left()
    forward()
    right()
    forward()
  }
  else {
    forward(2)
    finished=true
  }
}