def golomb(num:Int): Int = {
  var tab = new Array[Int](num)
  tab(0) = 1
  var flag = 0
  for (k <- 0 to 2) {
    var nb = tab(k)
    for (j <- flag to (flag+nb)) {
      tab(j) = k
    }
    flag = tab(k)
  }
  return tab(num-1)
}