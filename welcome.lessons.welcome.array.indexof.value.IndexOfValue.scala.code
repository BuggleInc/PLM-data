def indexOfValue(nums:Array[Int] ,value:Int): Int = {
	var nombre = 0
	for ( i <- 0 to nums.length)
	{
		if (nums(i)==value )
	{
		nombre=i
	}
	}
	if (nombre ==0)
	{
		return -1  
	}
	else return nombre 
}
