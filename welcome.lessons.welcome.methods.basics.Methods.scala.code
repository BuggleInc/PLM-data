def goAndGet() {
	do {
		avance();
	}while (!estSurBiscuit)
     prendBiscuit();
     
     do { 
     	recule();
       	  
	} while(estFaceMur)
}
for (i <- 1 to 7) {
    goAndGet();
    droite();
   avance();
   gauche();
    
}
