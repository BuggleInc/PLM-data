def cocktailSort2() {

var m: int = 0;
var l: int = 0;
var trie: Int = 0;
while (trie == 0) {
   trie = 1;
 for(i <- 0 to (getNombreValeurs()-l-2))
       {i++;
       	if(getValeur(i+1) < getValeur(i))
        {  
  echange(i+1,i);
  trie = 0;
        }
      }
 
 for(i <- (getNombreValeurs()-m-2) to 0 by -1) {
 	m++;
  if(getValeur(i+1) < getValeur(i)) {
   echange(i+1,i);
   trie = 0;
  }
 }
}
 



}
