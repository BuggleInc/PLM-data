def insertionSort() {

var temp = 0;
var j = 0;
for (i<-1 to getNombreValeurs() -1) {
	temp = getValeur(i);
	j = i;
	while (!plusPetitQue(j-1,temp) && j>=1 ) {
		copie (j-1,j);
		j-= 1;
	}
	setValeur (j+1,temp);
}
}
