def bubbleSort3() {
	var changed = true ; 
	
	while(changed){
		for ( i <- (getNombreValeurs()-2 to 0 by -1)){
				changed = false ; 
				for (j<- 0 to i){
					if(plusPetit(j+1,j)){
						echange(j,j+1);
						changed = true ;
						
					}	 
				}
				 
		}
		 
	}
	 
}
