def hunt() {
	while (isFacingTrail==true) {
		avance()
		baisseBrosse()
	}
	if (isFacingTrail==false) {
		droite()
	}
	gauche()

}
def isFacingTrail():Boolean = {
	var estVert = false;
	if (!estFaceMur) {
		avance()

		if (getCouleurSol()==Color.green) {
			estVert = true;
		}
		recule()

	}
	return estVert;
}        