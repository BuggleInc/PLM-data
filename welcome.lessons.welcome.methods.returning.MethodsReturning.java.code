boolean haveBaggle() {
	boolean seenBaggle=false;
	int i;
	
	for (i=0; i<6; i++) {
		if (isOverBaggle()) {
			seenBaggle=true;
			break;
		}
		forward();
	}
	for ( ; i>0; i--)
		backward();

	return seenBaggle;
}
