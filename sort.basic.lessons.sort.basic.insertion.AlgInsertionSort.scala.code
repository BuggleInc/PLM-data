def insertionSort() {
  var v = 0
  var k = 1
    for (i <- 1 to getNombreValeurs()-1) {
      v = getValeur(i);
      k = i
      while ( (k>0)&&(!plusPetitQue((k-1), v)) ) {
        setValeur(k, getValeur(k-1));
        k=k-1
      }
      setValeur(k,v)
    }
}