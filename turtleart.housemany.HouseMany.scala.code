override def run() {
 var i:Int=0
  
  for(i<- 0 to 3){
    maison()
    droite(90)
    leveCrayon()
    avance(50)
    gauche(90)
  }
}


def maison(){
  var i:Int=0
  var j:Int=0
  for(i<- 0 to 3){
  	avance(30)
  	droite(90)
  }
  avance(30)
  
  
  droite(30)
  for(j <- 0 to 2){
    avance(30)
    droite(120)
  }
  gauche(30)
  recule(30)

  
}