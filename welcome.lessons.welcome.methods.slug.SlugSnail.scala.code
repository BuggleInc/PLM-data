def hunt(c:Color) {
     while (estSurBiscuit()==false) {
    var test1 = isFacingTrail(c)
    if (test1==true) {
      avance()
      baisseBrosse()
      leveBrosse()
      } else {gauche()
             }
    
  }
  prendBiscuit()
 }
}


def isFacingTrail(t:Color):Boolean = { 
  var test = false
  if (estFaceMur()==true) {
    gauche()
  }
  avance()
  if (getCouleurSol()==t) {
    recule()
    test = true
    return test;
    
  } else {
    recule()
    return false;
    
}