def golomb(num:Int): Int = {
  var T = new Array[Int](num+1)
  T(0)=1
  var cpt = 0
  for (i <- 1 to T.length-1){
    T(i)=cpt
    if (cpt<T(i)){
      cpt=cpt+1
    } else {cpt = cpt}  
  }
  return T(num-1)
}