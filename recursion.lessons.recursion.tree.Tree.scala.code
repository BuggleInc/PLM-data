def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {

	current(steps);
	
	avance(length);
	droite(angle);
	avance(length);
	recule(length);
	gauche(2*angle);
	avance(length);
	recule(length);
	



	

}
