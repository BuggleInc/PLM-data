def hunt() {
	if (isFacingTrail==true) {
		avance()
		baisseBrosse()
	}
	while(isFacingTrail==false) {
		recule()
		gauche()
		
		
	}

}
def isFacingTrail():Boolean = {
	var estVert = false;
	if (!estFaceMur) {
		avance()

		if (getCouleurSol()==Color.green) {
			estVert = true;
		}
		recule()

	}
	return estVert;
}        