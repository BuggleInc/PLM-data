def isFacingTrail():
    if not isFacingWall():
        forward()
        if getGroundColor() != "Color.green":
            backward()
            return False
        else:
            backward()
            return True
    else: 
        left()
        return False

while not isOverBaggle():
    isFacingTrail()
    if isFacingTrail():
        forward()

    
    
