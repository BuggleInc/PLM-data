def bubbleSort()  {
  var trier = true
  var longueur = getNombreValeurs()
  
  while(trier) {
    var trier = false
    
    for (i <- 0 to longueur-2 ){
      if (plusPetitQue(i,i+1)){
        echange(i,i+1)
        trier = true
      }
    }
 
  }
}