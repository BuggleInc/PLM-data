override def run() {
	def keepHandOnSideWall() {
		droite()
		if (estFaceMur()){
		gauche()
		 if (!estFaceMur()){avance()}
		else {gauche()}
	}
while(!estSurBiscuit()){	
keepHandOnSideWall()
}
prendBiscuit();
}
