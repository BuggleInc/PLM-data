override def run() {
  def positionFinal() : Boolean = {
    if (getMondeLargeur()==getX() & getMondeHauteur()==getY()){
      return true
    }else return false
  }
  
  def positionSuivante() = {
    if (getY() != getMondeHauteur()-1){
     setPos(getX(),getY()+1)
   }
    else setPos(getX()+1,0)
  }
  
  var cpt =  0
  ecritMessage(cpt)
  while (!positionFinal()){
    positionSuivante()
    ecritMessage(cpt)
    cpt+=1
  }
  
}