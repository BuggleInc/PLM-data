// pour un coté, il faut faire quatre pas en avant, deux pas à gauche, quatre pas à droite, 
//deux pas à droite, et quatre pas à gauche
for (compteur1 <- 1 to 7) {
	for (compteur2 <- 1 to 4) {
		for (compteur21 <- 1 to 4) {
			avance()
		}
		gauche(); avance(); avance();
		droite()
		for (compteur23 <- 1 to 4) {
			avance()
		}
		droite(); avance(); avance();
		gauche()
		for (compteur25 <- 1 to 4) {
			avance()
		}
		gauche()
	}
}
