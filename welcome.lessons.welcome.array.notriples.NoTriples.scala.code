def noTriples(nums:Array[Int]): Boolean = {
	var isNotTriple = true;
	var countTable = new Array[Int](nums.length);
	
	for(i <- 0 to table.length - 1)
	{
		countTable()
		
		
	}
	return isNotTriple;

}