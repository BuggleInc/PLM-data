override def run() {
    def keepHandOnRightWall() = {
    while (!(estFaceMur)) {droite(); if (!estFaceMur) {avance}}
    }
    while (!(estSurBiscuit)) {
    	keepHandOnRightWall();
    	if (!estFaceMur) {avance()};
    }
    prendBiscuit();
}
