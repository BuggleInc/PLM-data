override def run() {
  setDirection(Direction.NORTH)
  while(!estSurBiscuit()){
    while(!estFaceMur()){
      avance()
    }
    while(estFaceMur()){
      keepHandOnSideWall()
    }
  }
}

def keepHandOnSideWall(){
  if (estFaceMur()){
    droite()
  }
  else {
    avance();gauche()
    while (!estFaceMur()){
      avance();gauche()
    }
    droite()
  }
}