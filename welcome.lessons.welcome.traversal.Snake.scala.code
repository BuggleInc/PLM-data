override def run() {
	baisseBrosse();
while (!estFaceMur() && !(getDirection()==Direction.NORTH))
		{
if (!estFaceMur)
{
	avance();
}
if (estFaceMur()) 
	{
	gauche();
	avance();
	droite();
	}
		}
			    }
