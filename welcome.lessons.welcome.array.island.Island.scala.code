def island(num:Array[Int]): Int = {
  var nb=0
  var h=0
  var hmin=0
  
  	for (i<-0 to num.length -1) {
      if (num(i) > h ){
        nb = nb+1
        h=num(i)
      } 
      else h = num(i)
            
      
    }
  return nb}