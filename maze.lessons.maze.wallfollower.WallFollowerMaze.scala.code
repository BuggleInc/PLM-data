override def run() {
    def keepHandOnRightWall() = {
    	droite();
    	if (estFaceMur) {gauche();avance()}
    	else {avance()}
    }
    while (!(estSurBiscuit)) {
    	keepHandOnRightWall();
    }
    prendBiscuit();
}
