do {
  var nbBaggle=0
  do{
      if (isOverBaggle()==true){
       nbBaggle=nbBaggle+1
      }
      forward()
   } while(crossing()==false)
  if (nbBaggle>=3){
       left()
  }
  else {
    right()
  }
  
} while(exitReached==false)