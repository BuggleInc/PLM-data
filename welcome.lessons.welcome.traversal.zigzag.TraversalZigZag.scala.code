override def run() {
}

def pasDeSerpent() {
  while (!estFaceMur()){
    avance()
  }
  if (getDirection==Direction.OUEST) {
    droite()
    avance()
    droite()
  } else if (getDirection==Direction.EST) {
    gauche()
    avance()
    gauche()
  }
  
}
