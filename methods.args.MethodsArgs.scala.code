def move(stepCount: Int,forward:Boolean)= {
  for (i<- 1 to stepCount){
    if (forward==true) {
      right();
      right();
      backward();
      right();
      right();
    } else {
      backward();
    }
  }
}