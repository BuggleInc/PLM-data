def last2(str:String):Int = {
  var T = str slice(str.length-2, str.length)
  var c=0
  for (i<-0 to str.length-3){
    if (str slice(i,i+1) + str slice(i+1,i+2) == T){
      c+=1
    }
  }
  return c
}