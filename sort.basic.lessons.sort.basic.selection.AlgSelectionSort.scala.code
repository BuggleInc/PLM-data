def selectionSort() {
	var longueur = getNombreValeurs();
	for(i<- 0 to longueur-1){
		var min = i ; 
		for(j<- i+1 to longueur -1) {
			if (getValeur(j) < getValeur(min)){
				min = j ; 
			}
		}
		if(min != i){
			echange(i,min);
		}
	}
}	