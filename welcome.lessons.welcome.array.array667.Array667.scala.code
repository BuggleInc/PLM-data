def array667(nums:Array[Int]): Int = {
  var compteur = 0
  for (k <- 0 to nums.length-2) {
    if (nums(k) == 6 && (nums(k+1) == 6 || nums(k+1) == 7)) {
      compteur = compteur + 1
    }
  }
  return compteur
}
