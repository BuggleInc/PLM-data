int withoutDoubles(int die1, int die2, boolean noDoubles) {
	if(noDoubles && die1 == die2){
		return die1 + nextValue(die2);
	} else {
		return die1 + die2;
	}
}

int nextValue(int dice){
	//1 .. 6
	return dice % 6 + 1;
}

