def shellSort()  {
  var lgr = getNombreValeurs()
  var ecart = lgr/2
  while(ecart>0){
  var v = 0
  for (i<- 1 to getNombreValeurs()-1){
    v = getValeur(i)
    var j = i-ecart
    while(j>=0 && !plusPetitQue(j,v)){
      copie(j,j+1)
      j-=1
    }
    
    setValeur(j+1,v)
    
  }
}
}