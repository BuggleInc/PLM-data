override def run() {
  var direction=0
  while (!estSurBiscuit){
  random3() match {
    case 1 => droite()
    case 2 => gauche()
    case 0 => if (!estFaceMur){avance()} else {retourne()
                                              retourne()}
  }
}
  prendBiscuit()
}
