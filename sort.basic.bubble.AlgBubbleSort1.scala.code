def bubbleSort()  {
  
  var echange:Boolean = true
  var n:Int = getNombreValeurs()
  while (echange){
    echange = false
    for (i <- 0 to n-2){
      if(plusPetit(i+1,i)){
        var tampon:Int = getValeur(i)
        setValeur(i, getValeur(i+1))
        setValeur(i+1, tampon)
        echange = true
        
      }
    }
  }
  
  
}