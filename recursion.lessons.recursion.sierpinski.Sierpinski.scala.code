def sierpinski(level:Int, length:Double) {

def triforce(level,length){
	level match{
		case 0 => avance(length); droite(120)
				avance(length); droite(120)
				avance(length); droite(120)
		case _ => triforce(level-1,length/3); droite(120)
				triforce(level-1,length/3); droite(120)
				triforce(level-1,length/3); droite(120)
	}
}
triforce(level,length)

}
