override def run() {
  var colors = Array(Color.black, Color.red, Color.orange, Color.magenta)
  
  for(i <- 0 to colors.length - 1) {
    setColor(colors(i))
    
    for(i <- 1 to 90) {
      forward(100)
      backward(100)
      right(1)
    }
  }
}