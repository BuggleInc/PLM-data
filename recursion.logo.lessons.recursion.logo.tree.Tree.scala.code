def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if (steps==1){
    current(steps)
  	avance(length)
    current(steps)
    gauche(angle)
    avance(length)
    recule(length)
    droite(2*angle)
    avance(length)
    recule(length)
  } else{
    tree(steps-1, length*shrink, angle,shrink)    
  }
}