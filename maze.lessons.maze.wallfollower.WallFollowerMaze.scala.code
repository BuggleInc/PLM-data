override def run() {
def keepHandOnSideWall() ={
	if (!estFaceMur) avance
	else droite
	gauche
	if (estFaceMur) droite
	
}
def sortir() ={
	while (!estSurBiscuit) {
		keepHandOnSideWall
	}
	prendBiscuit
}
}
