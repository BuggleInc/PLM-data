def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {
  var nombre = 0;
  if (13<=a && a<=19){nombre=nombre+1};
  if (13<=b && b<=19){nombre=nombre+1};
  if (13<=c && c<=19){nombre=nombre+1};
  if (13<=d && d<=19){nombre=nombre+1};
  return nombre}
