override def run() {
	baisseBrosse()
	var b : Boolean = false
	while (!b) {
	if (!estFaceMur) {avance()}
	else if (getDirection() == Direction.EST && !estFaceMur) {gauche();avance();gauche()}
	else if (getDirection() == Direction.OUEST) {droite();avance();droite()}
	else if (getDirection() == Direction.EST) {gauche()
	if (estFaceMur) {b = true;droite()}
	else {avance();gauche()}}
}}
