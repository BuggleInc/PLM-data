def increasing(l:List[Int]): Boolean = {
  
  var t = l.tail;
  
  if(t == Nil)
  {
    return true;
  }
  else{
    
    if(t.head < l.head)
    {
      return false;
    }
    else{
      return increasing(l.tail);
    }
    
  }
  
}