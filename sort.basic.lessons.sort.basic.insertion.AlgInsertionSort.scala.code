def insertionSort() {
  var v : Int =0
  var cpt:Int =0
  for (i<- 1 to getNombreValeurs()){
    v=getValeur(i)
    cpt=i
    while (v<getValeur(cpt-1) && cpt!=0){
      copie (cpt-1,cpt)
      cpt-=1
    }
    setValeur(v,cpt)
  }

}