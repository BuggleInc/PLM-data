def bubbleSort()  {
  var l=getNombreValeurs()
  var ech=true
  while (l>0 && ech==true){
    ech=false
  for (i<-0 to l) {
 		if (plusPetit(i+1,i)==true) {
   	echange(i+1,i);
   	ech==true;
 		} 
  }
  l=l-1
 }
}