def stringX(str:String):String =
{
  var s=""
  var  suppression=false;
 	var i=0;
  var j=str.length();
  
  str.drop(i).take(1)=="x"
  str.drop(j).take(1)=="x";
  
 for(k <- 1 to j-1)
  {
    
    if(str.drop(k).take(1)!="x" )
    {
      s+=str.drop(k).take(1)
    }
    
  }

  return s
}