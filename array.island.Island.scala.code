def island(num:Array[Int]): Int = {
  var lvl : Int = 0
  var h : Int = 0
  for (i <- num) {
      if (i >h) {
        lvl = lvl + 1
        h = i
      }
      else {
        if (i != h) {
           
          h = i
        }
      }
    }
  return lvl
}