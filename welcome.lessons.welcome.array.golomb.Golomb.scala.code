def golomb(num:Int): Int = {

	var tab = new Array[Int](num);
	var i:Int = 2;
	var j:Int = 2;
	var k:Int = 0;
	tab(0) = 1;
	tab(1) = 2;

	while (j < num)
	{
		k = 0;
		
		while (k < i && j < num)
		{
			tab(j) = i;
			k = k + 1;
			j = j + 1;	
		}

		i = tab(i);
	}

	return tab(num - 1);
}
