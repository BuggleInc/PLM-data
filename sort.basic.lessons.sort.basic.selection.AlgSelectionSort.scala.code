def selectionSort() {
  var indice = 0
  while (indice < getNombreValeurs()) {
    var indiceMin = indice
    for (i <- indice + 1 to getNombreValeurs()-1) {
      if (getValeur(i)<getValeur(indiceMin))
      	indiceMin = i
    }
    echange(indice,indiceMin)
    indice += 1
  }
}