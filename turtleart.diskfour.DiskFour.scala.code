override def run() {
  var x0=getX()
  var y0=getY()
  fourth(Color.BLACK)
  fourth(Color.RED)
  fourth(Color.ORANGE)
  fourth(Color.MAGENTA)
}

def fourth(c:Color){
  var i=0
  setCouleur(c)
  for (i <- 0 to 89){
    avance(100)
    recule(100)
    droite(1.0)
		}
}