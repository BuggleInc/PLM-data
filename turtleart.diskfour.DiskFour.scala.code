override def run() {
    for(x<-1 to 90){
    forward(100)
    backward(100)
    right(1)
  }
    for(x<-1 to 90){
      setColor(Color.RED)
    forward(100)
    backward(100)
    right(1)
  }
    for(x<-1 to 90){
      setColor(Color.YELLOW)
    forward(100)
    backward(100)
    right(1)
  }
    for(x<-1 to 90){
      setColor(Color.PINK)
    forward(100)
    backward(100)
    right(1)
  }
}