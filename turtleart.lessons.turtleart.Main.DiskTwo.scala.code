override def run() {
	for (i <- 1 to 9) {
	setCouleur(Color.red)
	for (k <- 1 to 18) {
		avance(100)
		recule(100)
		droite(1)
	}
	setCouleur(Color.red)
	for (k <- 1 to 18) {
		avance(100)
		recule(100)
		droite(1)
	}
}
}
