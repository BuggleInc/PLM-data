def hunt() {
	if (isFacingTrail==true) {
		avance()
		baisseBrosse()
	}
	while (isFacingTrail==false) {
		gauche()
		
	}

}
def isFacingTrail():Boolean = {
	var estVert = false;
	if (!estFaceMur) {
		avance()

		if (getCouleurSol()==Color.green) {
			estVert = true;
		}
		recule()

	}
	return estVert;
}        