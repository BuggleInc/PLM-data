public void run() { 
	boolean block = true;

		droite();
		droite();
	
	while(block) {
		avance();
		while(!estSurBiscuit()) {
			avance();

			if (estFaceMur() && !estSurBiscuit()) {
				droite();
				avance();
				gauche();
				avance();
				block = false;
				break;
			}
		}
		if (!block)
			break;
		prendBiscuit();
		while(!estSurBiscuit())
			recule();
		avance();
		poseBiscuit();
		}
	}
