override def run() {
	var nb:Int = 0
  do{
    ecritMessage(nb)
    nb+=1
    setPos((getX()+1)%getMondeLargeur(), (getY()+1)%getMondeHauteur())
   }while(getX() != getMondeLargeur() && getY() != 0)
  
  
}