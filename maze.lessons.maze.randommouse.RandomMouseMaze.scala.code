override def run() {
  while(!estSurBiscuit()){
    var ra = random3()
    if(ra==0 && !estFaceMur()){
      avance()
    }else if (ra==1){
      droite()
    }else{
      gauche()
    }
  }
  prendBiscuit()
}
