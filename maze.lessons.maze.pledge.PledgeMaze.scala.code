override def run() {
  avance()
  avance()
  while(!estSurBiscuit){
    longerMur
  }
  prendBiscuit()
}
  
  
  
  
def longerMur(){
  var dir=getDirection()
  do {
    gauche()
    if (estFaceMur()){
      retourne()
      if (estFaceMur()){
        droite()
        avance()
      }
    }
  } while(!estSurBiscuit() && getDirection!=dir)
  while (!estFaceMur){
    avance()
  }
}