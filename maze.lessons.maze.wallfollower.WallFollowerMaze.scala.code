override def run() {
    def keepHandOnRightWall() = {
    	droite();
    	if (estFaceMur) {gauche();avance()}
    	else if (!(estFaceMur())) {avance()}
    		else {droite()}
    }
    while (!(estSurBiscuit)) {
    	keepHandOnRightWall();
    }
    prendBiscuit();
}
