public void run() { 
	while (true) {
		back();
		while (!isOverBaggle() && !isFacingWall())
			forward();
		if (isFacingWall())
			break;
		pickupBaggle();
		back();
		while (!isOverBaggle())
			forward();
		backward();
		dropBaggle();
	}
	right();
	forward();
	left();
	forward();
}
