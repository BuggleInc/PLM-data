override def run()
{
  for (i <- 1 to 4)
  {
    faireMaison();
  	droite(90);
  	avance(50);
    gauche(90);
  }
}

def faireMaison()
{
  avance(30);
  
  droite(30);
  avance(30);
  droite(120);
  avance(30);
  droite(120);
  avance(30);
  
  droite(180);
  avance(30);
  droite(90);;
  avance(30);
  droite(90);
  avance(30);
  droite(90);
}