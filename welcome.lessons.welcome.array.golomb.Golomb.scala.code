def golomb(num:Int): Int = {
	var tab = new Array[Int](num)
	var progres = 1
	tab(0) = 1
	if(num == 1){
		return 1
	}
	var x = tab(progres)
	for(i <- 0 to num - 1){
		tab(i) = progres + 1
		x = x-1
		if(x==0){progres = progres +1 ; x = tab(progres)}
		}
		return tab(num-1)
}
