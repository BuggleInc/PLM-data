override def run() {
  for (j<-1 to 2){
  	for (i<-1 to 2){
    	faireMotif()
  	}
    gauche()
    avance(4)
    droite()
  }
}

def faireMotif(){
  faireDiag(Color.RED)
  faireDiag(Color.BLUE)
  faireDiag(Color.GREEN)
  faireDiag(Color.YELLOW)
  gauche()
  avance(2)
  gauche()
  avance(3)
}

def faireDiag(c:Color){
  avance()
  marquer(c)
  gauche()
  avance()
  gauche()
  avance()
  marquer(c)
  gauche()
  avance()
}

def marquer(c:Color){
  setCouleurBrosse(c)
  baisseBrosse()
  leveBrosse()
}