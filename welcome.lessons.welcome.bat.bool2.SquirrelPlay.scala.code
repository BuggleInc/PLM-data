def squirrelPlay(temp:Int, isSummer:Boolean):Boolean = {
  (temp,isSummer) match {
    case (temp,_) if temp<60 =>false
    case (temps,false) if temp>90=>false
    case (temp,true) if temp>100 =>false
    case (_,_)=>true
  }
}