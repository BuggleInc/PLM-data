override def run() {

	def maison() {
		avance(30)
	droite(30)

	for (i <- 1 to 3) {
		avance(30)
		droite(120)
	}

	gauche(120)
	recule(30)
	gauche(90)
	
	for (i <- 1 to 2) {
		avance(30)
		droite(90)
	}
	}

	for (k<-1 to 4) {
		maison()
		droite(90)
		avance(50)
		gauche(90)
	}

}
