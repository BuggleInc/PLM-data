def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  var a=length
  var b= steps
  if (b!=0){
  	droite(angle)
  	tree(b-1,shrink*a,angle,shrink)
    recule(shrink*a)
    gauche(2*angle)
    tree(b-1,shrink*a,angle,shrink)
    recule(shrink*a)
  } else{avance(a)}
  
}