def indexOfMaxValue(nums:Array[Int]):Int = {
  var max:Int =nums(0)
  for (i<- 1 to nums.length-1) {
    if (nums(i)>max) {max=nums(i)}
  }
  for (k <- 0 to nums.length-1) {
    if (nums(k)==max) {return k}
  }
}
