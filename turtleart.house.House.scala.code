def polygone(nbCotes:Int,longeur:Double,angleOffset:Double) {
  
  if (nbCotes<3) {return}
  
  droite(angleOffset)
  
  for (c <- 1 to nbCotes) {
    
    avance(longeur)
    droite(360/nbCotes)
    
  }
  
}

override def run() {
  
  polygone(4,100,0)
  avance(100)
  polygone(3,100,30)
  gauche(30)
  recule(100)
  
}