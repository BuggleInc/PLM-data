def selectionSort() {
	for (i <- 0 to getNombreValeurs()-2) { 
		val minpos=i
		for (j <- i+1 to getNombreValeurs()-1) {
			if (getValeur(j)<getValeur(minpos)) { 
				copie(j,minpos)
			}
		}
		echange(i,minpos)
	}
}
