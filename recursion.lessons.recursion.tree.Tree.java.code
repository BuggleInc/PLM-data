public void tree(int steps, double length, double angle, double shrink)    {
	if(steps != 0){
		avance(length);
		droite(angle);
		var longueur = length * shrink;
		tree(steps-1, longueur, angle, shrink);
	}
}
