def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {
	def ado(d:Int): Boolean = {
		if ( d > 12 && d < 20) {
		return true
		}else{
			return false
		}
	}
	var x: Int
	if (ado(a)) {
		x = x +1
	}
	if (ado(b)) {
		x = x +1
	}
	if (ado(c)) {
		x = x +1
	}
	if (ado(d)) {
		x = x +1
	}
	return x
}
