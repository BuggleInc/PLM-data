def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {
   for (l<-0 to length){
   	for (p<-0 to increment){
   	avance(l+p);
   	gauche(angle);
   	avance(l);
   	gauche(90);
   	avance(l);
   	gauche(90);
   	avance(l);
   	gauche(90);
   }
}
}