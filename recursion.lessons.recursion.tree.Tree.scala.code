def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
	var X1=0
	var X2=0
	avance(length:Double)
gauche(angle:Double)
avance(length*shrink)
tree(steps-1,length*shrink,angle/2,shrink*shrink)
}


