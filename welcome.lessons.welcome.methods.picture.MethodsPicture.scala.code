def faireV(c:Color) {
  for (compteur1 <-1 to 3) {
    for (compteur <-1 to 5) {
      avance()
      avance()
      baisseBrosse()
      leveBrosse()
      avance()
      gauche()
      avance()
      baisseBrosse()
      leveBrosse()
      recule()
      droite()
      avance()
      baisseBrosse()
      leveBrosse()
      gauche()
    }
    droite() 
    avance()
    gauche()
    avance()
    avance()
    avance()
    avance()
    avance()
    droite()
  }
}

override def run() {
}