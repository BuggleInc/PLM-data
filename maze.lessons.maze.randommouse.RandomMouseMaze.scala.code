override def run() 
{
	var rand:Int = 0;

	while (getX() != 3 || getY() != 3)
	{
		rand = random3();

		rand match
		{
			case 0 => if(!estFaceMur) avance();
			case 1 => gauche();
			case 2 => droite();
		}
	}	

    prendBiscuit();
}
