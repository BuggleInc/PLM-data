def insertionSort() {
  for(i <- 1 to getNombreValeurs()-1){
    var v:Int = getValeur(i)
    var k:Int=i
    while( k>0 && plusPetit(k-1,k)){
      copie(k-1,k)
      k=k-1
    }
    setValeur(k,v)
  }
  
}