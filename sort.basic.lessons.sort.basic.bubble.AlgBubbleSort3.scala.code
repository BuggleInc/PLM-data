def bubbleSort3() {
  var nbLignes=0
  var estTriee=false
  for(i<-0 to getNombreValeurs()){
    var a = 0
    var b = 0
    if (estTriee){
      return
    }
    estTriee=true
    for (i <-0 to getNombreValeurs()-(2+nbLignes)) {
      if (!plusPetit(i,i+1)){
        echange(i,i+1)
        estTriee=false
      }
    }
    nbLignes=nbLignes+1
  }
}
