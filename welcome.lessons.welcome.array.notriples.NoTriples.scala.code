def noTriples(nums:Array[Int]): Boolean = {
  for(k<-1 to nums.length-3){
    var a= nums(k)
    var b =nums(k+1)
    var c= nums (k+2)
    if (a==b==c){
      return false
      
    }
  
  }
  return true
}