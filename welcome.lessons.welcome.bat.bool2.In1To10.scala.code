def in1To10(n:Int, outsideMode:Boolean):Boolean = {
  var inter : Boolean = (n>=1)&&(n<=10)
  var exter : Boolean = (n<=1)||(n>=10)
  if(outsideMode){
    return exter
  }
  else{
    return inter
  }
}