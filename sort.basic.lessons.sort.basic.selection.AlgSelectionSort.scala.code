def selectionSort() {
	for (i <- 0 to getNombreValeurs()-1) { 
		val minpos=i
		for (j <- i to getNombreValeurs()-1) {
			if (getValeur(j)<getValeur(minpos)) { 
				copie(j,minpos)
			}
		}
		echange(i,minpos)
	}
}
