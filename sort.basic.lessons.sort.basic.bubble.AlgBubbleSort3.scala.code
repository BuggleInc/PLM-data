def bubbleSort3() {
	var x:Boolean = false
	for (i <- getNombreValeurs() -2 to 0 by -1) {
		x = false
		for (j <- 0 to i) {
			if (plusPetit(j+1,j)) {
			echange(j,j+1);
			x = true}
			
		}
		if (x==false) {return}
	}
}

