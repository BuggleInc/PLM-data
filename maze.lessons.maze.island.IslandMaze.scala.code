override def run() {
	def keepHandOnRightWall()
	{
	if (!estFaceMur)
	{
		droite()
		if(estFaceMur)
		{
		gauche()
		}
		if (!estFaceMur) avance()
	}
	else droite()
	}
	
		
		
			while(!estSurBiscuit())
			{
		if (!estFaceMur && (getDirection() == Direction.NORTH))
		{
			avance()
		}
		else 
		keepHandOnRightWall();
	
}
}
