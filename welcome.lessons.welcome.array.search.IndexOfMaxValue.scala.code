def indexOfMaxValue(nums:Array[Int]) : Int = {
  var taille : Int = nums.length
  var maxi : Int = nums(0)
  var indice : Int = 0
  for (i <- 1 to taille-1) {
    if (nums(i) > maxi) {
      maxi = nums(i)
      indice = i
    }
  }
  return indice
}