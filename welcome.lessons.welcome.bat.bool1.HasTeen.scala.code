def hasTeen(a:Int, b:Int, c:Int): Boolean = {
	def ado(d:Int): Boolean = {
		if ( d > 12 && d < 20) {
		return true
		}else{
			return false
		}
	}
	if (!ado(a) && !ado(b) && !ado(c)) {
		return false
	}else {
	return true
	}
}
