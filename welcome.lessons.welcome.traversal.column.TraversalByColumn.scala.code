override def run() {
	var wh=0; //hauteur du monde
	var wl=0; // largeur du monde
	wh=getMondeHauteur();
	wl=getMondeLargeur();
	while (!(getX()==wl-1 && getY()==wh-1)) { // -1 car premiere ligne est 0
		// tant qu'on est pas en pos finale
		if  (getY()==wh-1 && getX()!=wl-1) {
			setPos(getX()+1, 0);
		}
		else {
			setPos(getX(), getY()+1);
		}
	}
}
