def squirrelPlay(temp:Int, isSummer:Boolean):Boolean = {
  if (temp >= 60) {
    if (isSummer) {
      if (temp <= 100)
      	return true
      return false
    }
    if (temp <= 90)
    	return true
   	return false
  }
  return false
}