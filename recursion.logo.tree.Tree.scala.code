def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  
  
  if(steps!=0){
    
    avance(length:Double)
    
    droite(angle:Double)
    
    avance(length/shrink:Double)
    
  }
  
  else{
    
    avance(length:Double)
    
    droite(angle:Double)
    
    tree(steps-1, length/shrink, angle, shrink)
    
    gauche(2*angle:Double)
    
    tree(steps-1, length/shrink, angle, shrink)
    
    recule(length:Double)
    
  }
  
}