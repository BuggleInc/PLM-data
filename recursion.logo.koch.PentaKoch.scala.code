def pentaKoch(levels:Int, length:Double) {

	if (levels == 0) {
		avance(length);
	} else {
	
	pentaKoch(levels - 1, length * 0.4 );
	gauche(180 - 72);
	
	
	for (i <- 1 to 4) {
		pentaKoch(levels - 1, length * 0.4 );
		droite(72);
	}
	
	
	
	gauche(180);
	pentaKoch(levels - 1, length * 0.4 );
	}
}
