def compare(list,val):
  if list==None:
    return val
  elif list.head>=val
    return compare(list.tail,val)
  else:
    return compare(list.tail,list.head)

def min(list):
  return compare(list,list.head)