def bubbleSort3() {
  var switch:Boolean=true
  for(i<-0 to getNombreValeurs()-1){
		for(k<- i to getNombreValeurs()){
      if (plusPetit(k+1,k)) {echange(k+1,k);switch=true}
			if (!switch){return}
        }

    
  }  
  
  
 }
