def spiral(nbPas:Int, angle:Int, length:Int, increment:Int)    {
  if(nbPas==0)
  {
    
    avance(length)
    gauche(angle)
  }else{
    avance(length)
    gauche(angle)
    spiral(nbPas-1,angle,length+increment,increment)
  }
}