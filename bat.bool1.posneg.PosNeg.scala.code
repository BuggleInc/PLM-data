def posNeg(a:Int, b:Int, negative:Boolean):Boolean = {
  
  if (a*b <= 0) {
    
    return true
  
  }
  
  else {
    
    if (negative && (b <=0 && a <=0)) {
      
      return true
      
    } return false
    
  }
    
}
