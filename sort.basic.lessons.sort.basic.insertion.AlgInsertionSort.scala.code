def insertionSort() {
	for (i<-1 to getNombreValeurs()-1) {
		var variable = getValeur(i)
		for (j <- 1 to i) {
			if (!plusPetitQue(i-j,variable))
				{
				setValeur(i-j,getValeur(i-j+1))
				}
		}
	}
}
