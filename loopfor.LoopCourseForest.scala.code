var tours:Int = 0
val posX:Int = getX():Int
val posY:Int = getY():Int

while(tours < 7) {
  
  avance(4) ; gauche() ; avance(2) ; droite() ; avance (4) ; droite() ; avance(4)
  gauche()
  
  if (getX() == posX && getY() == posY) {
    tours += 1
  }
  
}