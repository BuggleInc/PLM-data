def shellSort()  {
	var v = 0
	var ecart = getNombreValeurs()/2
	
	for (i <- 1 to getNombreValeurs()-1) {
		var k = i
		v = getValeur(i)
		while (k > ecart-1 && v < getValeur(k-ecart)) {
			copie(k-ecart,k)
			k=k-ecart
		}
		ecart = ecart /2
		setValeur(k,v)
	}
}
