def coin() {avance(20);droite(90);avance(20);gauche(45);avance(20);gauche(90);avance(20);droite(90);avance(20);droite(90);avance(40);droite(90);avance(20);droite(90);avance(20);gauche(90);avance(20);gauche(45);avance(20);droite(90);avance(20);droite(180)}

override def run():Unit= {
  for (j <- 1 to 2) {
    for (i <- 1 to 4) {coin()}
    droite(45)}
}
