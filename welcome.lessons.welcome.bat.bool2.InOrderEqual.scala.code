def inOrderEqual(a:Int, b:Int, c:Int, equalOk:Boolean):Boolean = {
  (a, b, c, equalOk) match {
    case (i, j, k, false) if ((i < j) & (j < k)) => return true
    case (i, j, k, true) if ((i <= j) & (j <= k)) => return true
    case (_, _, _, _) => return false
  }
}