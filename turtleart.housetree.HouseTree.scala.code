def maison()
{
  avance(30)
  droite(30)
  avance(30)
  droite(120)
  avance(30)
  droite(120)
  avance(30)
  droite(180)


  for (i<- 1 to 3)
  {
    avance(30)
    droite(90)
  }
}


override def run() {
  for (i<- 1 to 4)
  {
    maison()
    leveCrayon()
    droite(90)
    avance(50)
    gauche(90)
    baisseCrayon()
  }

}