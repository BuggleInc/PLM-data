var diag = 0;
override def run() {
  var x = 0
  var y = 0
  for (k<-1 to 6){
    diag = diag + 1
    y=y+1
    setPos(x,y)
    for(i<-1 to diag){
      x=x+1
      y=y-1
      setPos(x,y)
    }
    x=x-diag
    y=y+diag
  }
}