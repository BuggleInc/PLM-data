def bubbleSort3() {
  var m=getNombreValeurs()
  var change=true
  for (j<- m-2 to 0 by -1){
    
    
    	for (i <-0 to j){
        while (change){
      change = false
      	if (getValeur(i)>getValeur(i+1)){
       	  echange(i,i+1)
          change=true
          
    }
    
    
  }
        return
}
    
}
}