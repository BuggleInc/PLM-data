boolean isFacingTrail() {
	boolean verif = false;
  
  avance();

  if(getCouleurSol().equals(Color.green)){
    while(estFaceMur()){
      gauche();
    }
    verif= true;
  } else {
    recule();
  }
  return verif;
}