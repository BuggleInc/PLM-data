def hunt(c:Color) {while(!estSurBiscuit){if (isFacingTrail(c)){avance();baisseBrosse();leveBrosse()};else{droite()} };prendBiscuit()
     
 }


// Copy your  isFacingTrail() here
def isFacingTrail(a:Color):Boolean = {
    if(estFaceMur()){return false};
  	else{ avance()
  					if(getCouleurSol()==a){recule ();return true};
        			else{recule();return false}}}