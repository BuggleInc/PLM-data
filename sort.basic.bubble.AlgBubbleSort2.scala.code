def bubbleSort2() {
  var len = getNombreValeurs()
  for (i<-1 to len+1) {
    var nbParcours = 0
    for (j<-0 to len -2 - nbParcours) {
      nbParcours += 1
      if (plusPetitQue(j+1,j)) {
        echange(j+1,j)
      }
    }
  }
}