def insertionSort() {
  var n = getNombreValeurs();
  for ( i <- 1 to n-1 ){
    var v = i;
    var j = 0;
    while ( i-j-1>=0 && plusPetit(i-j,i-j-1) ){
      copie(i-j-1,i-j);
      j += 1;
    }
    copie(v,i-j);
  }
}
