def sierpinski(level:Int, length:Double) {
  if (level==0){
    avance(length)
    return
  }
  baisseCrayon()
  sierpinski(level-1,length/2)
  droite(120)
  sierpinski(level-1,3*length/2)
  droite(120)
  sierpinski(level-1,3*length/2)
  droite(120)
  leveCrayon()
}
