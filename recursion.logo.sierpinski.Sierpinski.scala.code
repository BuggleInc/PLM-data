def sierpinski(level:Int, length:Double) {
  for (i<- 1 to 3^level){
  if (level!=0){
    sierpinski(level-1,length/2)
    right(120)
    sierpinski(level-1,length/2)
    right(120)
    sierpinski(level-1,length/2)
    right(120)  
  }else{
    forward(length)
	}
}
}