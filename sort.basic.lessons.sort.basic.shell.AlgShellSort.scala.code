def shellSort()  {
  var ecart=getNombreValeurs()/2
  
  while(ecart>0){
  
    
    for (i<-ecart+1 to getNombreValeurs()-1){
      var j=i
      var memory=getValeur(i)
      while (j>=1 && !plusPetitQue(j-ecart, memory)){
        copie(j-1, j)
        j=j-1
      }
      setValeur(j, memory) 
    }
  
  ecart=ecart/2  
  }
  
}