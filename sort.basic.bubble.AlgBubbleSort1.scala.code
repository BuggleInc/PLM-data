def bubbleSort()  { val a = getNombreValeurs() - 1
  for (i <- 0 to a) {
                      for (j <- 1 to i) { 
                         if (plusPetit(i,j)) { echange(i,j)
                           }
                        
                       } 
                     }
}