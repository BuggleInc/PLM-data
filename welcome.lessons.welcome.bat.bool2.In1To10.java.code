boolean in1To10(int n, boolean outsideMode) {
	return  n == 1 
		|| n == 10
		|| (outsideMode && !between(n,1,10));
		|| (!outsideMode && between(n,1,10));
}

boolean between(int n, int a, int b){
	return a < n && n < b;
}
