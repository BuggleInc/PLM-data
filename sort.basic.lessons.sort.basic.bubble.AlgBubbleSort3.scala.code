def bubbleSort3() {
	for(i<-0 to getNombreValeurs()-2 by 1){
		var test:Boolean =true;
		for(j<-0 to (getNombreValeurs()-2-i) by 1){
			if(plusPetit(j+1,j)){
				echange(j,j+1);
				test=false;
			}
		}
		if(test) return;
	}
}
