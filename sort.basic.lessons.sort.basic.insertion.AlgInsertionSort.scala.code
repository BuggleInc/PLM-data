def insertionSort() {
  var len = getValueCount();
  
    
    for(i <- 1 to len-1){
      var v = i;
      
      for(j <- i to 1 by -1){
        if(getValue(j) > getValue(j-1)){
          copy(j-1,j);
        }else{
          setValue(j,v);
        }
      }
    }
  
}