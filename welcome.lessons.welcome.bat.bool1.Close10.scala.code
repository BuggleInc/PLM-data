def close10(a:Int, b:Int): Int = {
	var c : Int = 10 - a;
	var d : Int = 10 - b;
	
	if (Math.abs(c)<Math.abs(d)) {return a 
	};
	if (Math.abs(c)>Math.abs(d)) {return b
	};	
	if (Math.abs(c)==Math.abs(d)) {return 0
	};
	return 666;
}
