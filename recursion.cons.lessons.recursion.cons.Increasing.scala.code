def increasing(l:List[Int]): Boolean = {
	def fonctionInterne(l:List[int], mem:Int): Boolean{
		if(l==Nil){
			return true;
		}
		else if (mem=<l.head) {
			return fonctionInterne(l.head,l.tail);
		}
		else {
			return false;
		}
	}
	return fonctionInterne(l.head,l.tail);
}
