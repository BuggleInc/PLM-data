def max1020(a:Int, b:Int):Int = {
var rep = 0 ;
if (a<=20 & a>=10) { if (b<=20 & b>=10) {rep = Math.max(a,b)}
					else {rep = a}
	else { if (b<=20 & b>=10) {rep = b}
					else {rep = 0}

		return rep
	
}
