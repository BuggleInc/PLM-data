override def run() {
  
  for(j <- 0 to 1){
  
    for(i <- 0 to 3){

      avance(100.00);
      droite(90.00);

    }

    droite(180.00);

    for(i <- 0 to 3){

      avance(100.00);
      gauche(90.00);

    }
  
  }
  
}