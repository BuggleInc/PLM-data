override def run() {
  for(i<- 0 to 3){
  avance(30);
  droite(30);
  avance(30);
  droite(120);
  avance(30);
  droite(120);
  avance(30);
  droite(180);
  avance(30);
  droite(90);
  avance(30);
  droite(90);
  avance(30);
  droite(180);
  avance(30);
  leveCrayon();
  avance(20);
  gauche(90);
  baisseCrayon
}
}