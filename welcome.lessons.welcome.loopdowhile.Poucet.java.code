public void run() { 
	while (!exitReached()){
		brushDown();
		int baggleCount = 0;
		for (int i = 0; i < 5; i++){
			if (isOverBaggle()) baggleCount++;	
			forward();		
		}
		if (baggleCount <= 2) right();
		else left();
	}
}
