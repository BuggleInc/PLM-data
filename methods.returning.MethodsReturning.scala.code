def haveBaggle():Boolean = {
  for (i <- 1 to 7) {
    forward()
    if (isOverBaggle()) {
      sawBaggle = true
    }
  backward(7)
  }
  if (sawBaggle) {
    seenBaggle = true
  }
  return seenBaggle

var seenBaggle = false
var sawBaggle = false
  
}