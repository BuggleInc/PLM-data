droite()
droite() //Buggle vers la droite
while (!estFaceMur) {
	while (!estSurBiscuit) {
		avance()
	} else {
		prendBiscuit() //Buggle tient biscuit
		droite()
		droite()  //Buggle vers la gauche
		while (!estSurBiscuit) {
			avance()
		} else {
			droite()
			droite() //Buggle vers la droite
			poseBiscuit() //Buggle sans biscuit
		} // 1 Rocher posé.
	}
} //Rochers déplacés
droite()
avance()
gauche()
avance()