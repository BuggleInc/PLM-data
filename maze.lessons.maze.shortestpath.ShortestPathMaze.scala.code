override def run() {
    // Your code here
  val H = getWorldHeight()
  val L = getWorldWidth()
  val coordSortie = findExit(H,L)
  val xSortie = coordSortie(0)
  val ySortie = coordSortie(1)
  
  
}

def findExit(H:Int, L:Int):Array[Int] ={
  /* Renvoie la sortie d'un monde de taille (x,y) donnée */
  var x = 0
  var y = 0
  while (!hasBaggle(x,y) && x<= L-1){
    x = x+1
    y=0
    while (!hasBaggle(x,y) && y<= H-1){
      y = y + 1 
    }
  }
  return Array(x,y)
  
}