def shellSort()  {
  var v:Int = 0
  var h:Int = 1
  var lastCopied:Int = 1
  var copied = true
  
  for (i <- 1 to getValueCount()-1) {
    v = getValue(i)
    lastCopied = i
    copied = true
    h = 1
    
    while (i-h >= 0) {
      if (getValue(i-h) > v) {
      	copy(i-h, i-h+1);
      	lastCopied = i-h
    	} else {
        copied = false
      }
      
      h+=1;
    }
    setValue(lastCopied, v)
  }
}
