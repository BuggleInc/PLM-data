def isFacingTrail():Boolean = {
  if (! isFacingWall()) {
        forward()
        if (getGroundColor() == "Color.green") {
          backward()
          return true
        }
        else {
          backward()
          left()
          if (! isFacingWall()) {
            forward()
            if (getGroundColor() == "Color.green") {
              backward()
              return true
        			}
            else {
              back()
              return true
            }
          }
          else {
            back()
            return true
          }
        }
      }
      else {
        left()
        if (! isFacingWall()) {
            forward()
            if (getGroundColor() == "Color.green") {
              backward()
              return true
        			}
            else {
              back()
              return true
            }
         }
    }
}