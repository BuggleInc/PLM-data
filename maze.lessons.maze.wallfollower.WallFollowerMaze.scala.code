override def run() {
	while(!estSurBiscuit())
  keepHandOnSideWall()
}

def keepHandOnSideWall(){
  if (estDosMur()){
  droite()
  avance()
    gauche()}
  else if (estFaceMur()){
    droite()
    recule()}
  else{
  recule()
    droite()}
  
}