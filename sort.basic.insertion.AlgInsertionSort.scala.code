def insertionSort() {
  var i=0
  var v=0
  var k=(-1)
  var h=0
  var l=getValueCount()
  for (i<-1 to l-1){
    v=getValue(i)
    k=(-1)
    while((h==0) && ((i+k)!=(-1))){
      if(!isSmallerThan((getValue(i+k)),v)){
        copy(i+k,i)
        setValue(i+k,v)
        h=1
      }
      else{k=k-1}
    }
  }
}