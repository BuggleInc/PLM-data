def sleepIn(weekday:Boolean, vacation:Boolean): Boolean = {
	var weekday : Boolean ;
	var vacation : Boolean ;
	
	if(weekday){
		if(vacation == false) {
			return false ;
		}
	}
	if(weekday == false){
		if(vacation) {
			return true ;
		}
	}
}
