public void run() {
	colByCol();
}

void colByCol(){
int x = getWorldWidth();
int y = getWorldHeight();

setPos(0, 0);

	for(int i = 0; i < x; i++){
		setPos(0, i);
	}
}

boolean finalPos(){
	if(getX() == getWorldWidth()){
		return true;
	}

	return false;
}//end finalPos
