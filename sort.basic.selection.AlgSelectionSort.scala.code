def selectionSort() {
  var n:Int=getNombreValeurs()
  for (k <- 0 to n-2) {
    var i:Int=k
    var valeur:Int=getValeur(k)
    for (l <- k to n-1){
      if (plusPetitQue(l,valeur)){
        i=l
        valeur=getValeur(i)
      }
    }
    echange(k,i)
  }
}