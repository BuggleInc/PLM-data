def bubbleSort()  {
  val n = getNombreValeurs()
  var swap = false
  do {
    swap = false
    for (i <- 0 to (n-2)) {
    	if (plusPetit(i+1,i)) {
     		echange(i,i+1);
     		swap = true
      }
    }
  } while (swap)
}
