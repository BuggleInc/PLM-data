def move(stepCount,forward):
    if forward:
        forward(stepCount)
    else:
        backward(stepCount)

stepCount = getY()

if getDirection() == Direction.NORTH:
    forward = True
else:
    forward = False
move(stepCount,forward)