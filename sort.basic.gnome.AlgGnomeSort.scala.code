def gnomeSort() {
  var gnome: Int = 0
  while (gnome<getValueCount()-1 ) {
    if (isSmaller(gnome+1,gnome)) {
      swap(gnome+1,gnome)
      gnome=gnome-1
      if (gnome<0) {gnome=0}
    } else {gnome=gnome+1}
  }
  
  
  
}