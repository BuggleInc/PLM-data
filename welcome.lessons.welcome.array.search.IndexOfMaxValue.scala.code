def indexOfMaxValue(nums:Array[Int]):Int = {
  var max:Int = nums(0)
  var maxIndex:Int = 0
  
  for ((n, i) <- nums.zipWithIndex){
    if (n > max){
      max = n
      maxIndex = i
    }
  }
  
  return maxIndex
}
