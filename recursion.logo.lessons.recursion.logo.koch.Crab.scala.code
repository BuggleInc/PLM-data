def crab(levels:Int, length:Double) {
  
  if(levels > 0){
    var nlvl = levels-1;
    var nlng = length/(Math.sqrt(2));
    
    left(45);
    crab(nlvl,nlng);
    right(45);
    crab(nlvl,nlng);
  }
  else{
    forward(length);
  }
}