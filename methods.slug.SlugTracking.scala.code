def isFacingTrail():Boolean = {
    var bool = false
  	if(!estFaceMur())
      {
        	avance()
        if(getCouleurSol()==Color.green)
          {
            bool = true
            recule()
          }
        recule()
      }
  return bool
  
}