def lastDigit(a:Int, b:Int):Boolean = {
  if (derChiffre(a)==derChiffre(b)) {
    return true
  } else return false
}
  

def derChiffre (x:Int):Int = {
  while (x>=10) {
    x = x%10
  }
  return x
}