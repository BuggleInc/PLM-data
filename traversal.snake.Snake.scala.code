override def run() {
  brushDown()
  while (! isInPosition) {
    doTheMove()
  }
}

def isInPosition() {
  
}

def doTheMove() {
  forward(6)
  if (getDirection() == Direction.EAST) {
    left()
    forward()
    left()
  }
}