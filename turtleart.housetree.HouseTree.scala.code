override def run() {
  for(i <- 0 to 4) {
    faireMaison()
    droite(90)
    leveCrayon()
    avance(50)
    baisseCrayon()
    gauche(90)
  }
  
  def faireMaison() {
      avance(30)
      droite(30)
      
      for(k <- 1 to 3) {
        avance(30)
        droite(120)
      }
      
      droite(60)
      
      for(k <- 1 to 3) {
        avance(30)
        droite(90)
      }
  }
}