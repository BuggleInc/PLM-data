def insertionSort() {
	var v = 0
	var ech = 0

	
	for (i <- 1 to getNombreValeurs()-1) {
		var k = i
		v = getValeur(i)
		while (k >= 0 && v < getValeur(k-1)) {
			
				copie(k-1,k)
				k=k-1
		}
		

		setValeur(k,v)
	
	}
}
