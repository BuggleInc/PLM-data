def bubbleSort3() {
	var lrg = getNombreValeurs;
	var hasSwap= true;
		for ( i<-lrg-2 to 0 by -1)
		{
			if(hasSwap==true)
			{
				hasSwap=false;
				for (j <-0 to i)
				{
					if( plusPetit(j+1, j))
					{
						echange(j,j+1);
						hasSwap = true
					}
				}
			}
		}
}
