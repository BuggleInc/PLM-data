override def run() {
  var y:Int=0;
  var count:Int=0;
  
  while(y!=getWorldHeight()){
    ligne(y);
    y=y+1;
  }
  def ligne(y:Int){
  var x:Int=0
  while(x!=getWorldHeight()){
    setPos(x,y);
    writeMessage(count);
    x=x+1;
    count=count+1;
  }
    }

}