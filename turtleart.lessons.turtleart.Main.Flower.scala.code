override def run() {
	def octogone(taille:Int) ={
		for (i<-1 to 8) {
			avance(taille)
			droite(45)
		}
	}
	octogone(50)
	
	def cote() ={
		avance(25)
		droite(72)
		avance(25)
		gauche(144)
	}
	var i = 0
	for (i<-1 to 8){
		cote
	}
}
