def array():
    brushDown()
    for b in L:
    setBrushColor(b)
    forward()    
    
                
def storeArray():
    global L
    L = []
    for a in range(getWorldHeight()):
        L.append(getGroundColor())
        forward()
    L.append(getGroundColor())

storeArray()
for c in range(getWorldHeight()-1):
    left()
    forward()
    right()
    array()

    