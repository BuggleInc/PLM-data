def insertionSort() {
  var length : Int = getValueCount();
  var i : Int =0;
  var j : Int =0;
  var v : Int =0;
  
  for(i<-1 to length-1)
  {
    v = getValue(i);
    j=i;
    while(j>0 && isSmallerThan(v,j-1))
    {
      swap(j,j-1);
      j-=1;
    }
    setValue(j,v);
  }
}