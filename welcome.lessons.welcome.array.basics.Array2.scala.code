override def run() {
  var x:Int = getMondeHauteur()
  var T = new Array[Color](x+1)
  T(0)=getCouleurSol()
  for(i<- 1 to x-1){
  	avance()
    T(i)=getCouleurSol()
  }
  T(x)=T(0)
  for(i<- 1 to x-1){recule()}
  for(k<- 1 to x-1){
    val decalage = litMessage().toInt
    gauche()
    avance()
    droite()
    for(j<- 1 to x-1){
      setCouleurBrosse(T((j+1)%x))
      baisseBrosse()
      leveBrosse()
      avance()
    }
    for(j<- 1 to x-1){recule()}
  }
}