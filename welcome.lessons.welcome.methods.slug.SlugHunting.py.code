# Copy your isFacingTrail, and write your code below
def Mark():
    brushDown()
    brushUp()

def isFacingTrail():
    if not isFacingWall() and not isOverBaggle():
        forward()
        getGroundColor()
        if getGroundColor() != green:
            backward()
            left()
            return False
        else:
            backward()
            return True
    else: 
        left()
        return False
def hunt():
    while not isOverBaggle():
        isFacingTrail()
        if isFacingTrail():
            forward()
            Mark()
hunt()
pickupBaggle()

