def brosse(c:Color){
  setCouleurBrosse(c)
  baisseBrosse()
  leveBrosse()
}

def slash(c:Color){
  avance()
  brosse(c)
  recule()
  gauche()
  avance()
  brosse(c)
  recule()
  droite()
}

def aslash(c:Color){
  brosse(c)
  avance()
  gauche()
  avance()
  brosse(c)
  recule()
  droite()
  recule()
}

def motif(){
  slash(Color.red)
  avance(2)
  aslash(Color.blue)
  slash(Color.green)
  aslash(Color.yellow)
}

override def run() {
}