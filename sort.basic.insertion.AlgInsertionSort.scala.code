def insertionSort() {
  var v = 0
  var mem = 0
  for (i<-1 to getNombreValeurs()-1) {
    v=getValeur(i)
    for (j<-0 to i-1) {
      if (getValeur(i-j-1)>v) {
        copie(i-j-1,i-j)
        mem = i
      }
    	copie(v,mem)
    }
  }
}