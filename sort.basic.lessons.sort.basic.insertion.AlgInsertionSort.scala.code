def insertionSort() {
  var valeur=0
  var last=0
  for (i<-2 to getNombreValeurs()-1){
    valeur = getValeur(i)
    for (n<- i to 2 by -1){
      if (getValeur(n)>valeur){
        copie(n-1,n)
        last=n
      }
    }
    setValeur(last, valeur)
  }
}