def teaParty(tea:Int, candy:Int): Int = {
var outcome = 0
	(tea,candy) match {
		case (x,y) if (x<5 || y<5)		=> return 0
		case (x,y) if (x>=2*y || y>=2*x)	=> return 2
		case (x,y) if (x>=5 && y>=5)		=> return 1
	}
}