public void hunt() 
{
	while (!estSurBiscuit()) 
	{
		if (isFacingTrail()) 
		{
			baisseBrosse();
			avance();
			leveBrosse();
		} 
		else 
		{
			gauche();
		}
	}
pickupBaggle();
}
boolean isFacingTrail() 
{
	if (estFaceMur())
		return false;
	forward();
	boolean vert = getGroundColor().equals(Color.green); 
	backward();
	return vert;
}