var nbP = 0
        def goAndGet() {
        while(!estSurBiscuit){
        	avance();nbP = nbP+1;
        	}
          prendBiscuit();
          for(x<-1 to nbP ){
          	recule();
          }
          poseBiscuit();
          nbP = 0;
        	}
        	 

        for (i <- 1 to 7) {
            goAndGet();
            right();
            forward();
            left();
        }
