def island(num:Array[Int]): Int = {
  var nb = 0
	var diff = new Array[Int](num.length -1)
  for (i <- 0 to num.length -2){
    diff(i) = num(i+1) - num(i);
  }
  for (i <- 0 to diff.length -1){
    if (diff(i) > 0){
      nb += 1;
    }
  }
  return nb
}


