def spiral(steps:Int, angle:Int, length:Int, increment:Int)   { steps match { 
                          case 0 => avance (0.0)
                            case _ => {avance (length) ;
               
                                       gauche(angle) ; 
                                       spiral (steps-1, angle, length+4, increment)}
}
                                                                        

}