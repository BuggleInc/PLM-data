def withoutDoubles(die1:Int, die2:Int, noDoubles:Boolean):Int = {
	if ((die1==die2) && (noDoubles)) {
		var die11 = die1
		if (die1==6) { 
			die11 = 1
		} 
		else { 
			die11 = die11 + 1 
			}
		return (die11 + die2);
	}
	return (die1 + die2);
}