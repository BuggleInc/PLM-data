def spiral(steps, angle, length, increment):
      if steps==0:
         return
      else :
         avance(length)
         gauche(angle)
         spiral(steps-1, angle, length+increment, increment)

stamp()
spiral(5, 90, 0, 3)