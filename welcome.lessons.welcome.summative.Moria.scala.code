back()
while (! isFacingWall) {
	forward();
  
  if (isOverBaggle())
  	moveBaggle();
}
right(); forward(); left(); forward();


def moveBaggle() {
  pickupBaggle();
  back();
  forward();
  while (!isFacingWall() && !isOverBaggle()) {
    forward();
  }
  
  
  backward();
  dropBaggle();
  back()
}

/**def isFacingWest():Boolean = {
  if (getDirection() == Direction.WEST)
  	return true
  
  return false
} */