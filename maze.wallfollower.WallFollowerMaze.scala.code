override def run() {var x:Int=0;
                    while (!isFacingWall()) {right()}
                    while (!isOverBaggle()) {while(isFacingWall()) {right(); forward(); left()}}
pickupBaggle()
    // Your code here 
}