override def run()
{
  for (i <- 1 to 180)
  {
    avance(2);
    droite(1);
	}
  
  leveCrayon();
  avance(2);
  droite(1);
  baisseCrayon();
  
  for (i <- 1 to 179)
  {
    avance(2);
    droite(1);
	}
}