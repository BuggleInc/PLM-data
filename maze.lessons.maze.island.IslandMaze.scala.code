override def run() {
	while (!estSurBiscuit()) {nord();
	keepHandOnSideWall()}
prendBiscuit()
}

	 
def keepHandOnSideWall() {
	gauche()
	if (!estFaceMur()) {avance()} else 
	{droite(); if (!estFaceMur()) {
		avance()} else {droite()}
	}
	}

def nord() {
	while(!estFaceMur()) {avance()}
	if (estFaceMur()) {droite()}
}
