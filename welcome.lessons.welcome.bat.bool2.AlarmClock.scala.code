def alarmClock(day:Int, vacation:Boolean): String = {
  if( (day<4 && day!0)  && !vacation){return "7:00"}
  if( (4<day || day==0) && !vacation){return "10:00"}
  if( (day<4 && day!0) && vacation){return "10:00"}
  else{return "off"}
}