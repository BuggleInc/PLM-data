override def run() {
  avance()
  avance()
  while(!estSurBiscuit){
    longerMur
  }
  prendBiscuit()
}
  
  
  
  
def longerMur(){
  var dir=getDirection()
  do {
    while (!estFaceMur){
      avance()
    }
    gauche()
    if (estFaceMur()){
      retourne()
      if (estFaceMur()){
        droite()
        avance()
      }
    }
    avance()
    droite()
    if (estFaceMur()){
      gauche()
    }
  } while(!estSurBiscuit() && getDirection!=dir)
  while (!estFaceMur){
    avance()
  }
}
