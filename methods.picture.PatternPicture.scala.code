override def run() {
    round(Color.RED)
    round(Color.YELLOW)
  	round(Color.GREEN)
  round(Color.BLUE)
  forward(4)
  round(Color.RED)
    round(Color.YELLOW)
  	round(Color.GREEN)
  round(Color.BLUE)
  forward(3)
  left()
  forward(3)
  round(Color.RED)
    round(Color.YELLOW)
  	round(Color.GREEN)
  round(Color.BLUE)
}  
  
  
  
def round(c:Color) {
  setBrushColor(c)
  left()
  forward()
  brushDown()
  brushUp()
  right()
  forward()
  right()
  forward()
  brushDown()
  brushUp()
  left()
  forward(2)
  left()
}