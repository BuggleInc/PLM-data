override def run() {
def testg(): Boolean = {
	gauche();
	if (estFaceMur()) {
		droite();
		return true;
	} else { return false}
}

def keepHandOnSideWall() {
	gauche();
	if (estFaceMur()) {
	droite();
	avance();
	gauche();
	if (!estFaceMur()) {
		avance();
		gauche();
		if (!estFaceMur()) {
			avance();
			gauche();
		} else {droite()}
		
	} else { droite();
	}
	}
	droite();
		}



 while (!(estSurBiscuit())) {
 	keepHandOnSideWall()
 }
    prendBiscuit();
}
