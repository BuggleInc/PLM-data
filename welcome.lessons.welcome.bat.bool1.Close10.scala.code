def close10(a:Int, b:Int): Int = {
  if (math.abs(10-a)>math.abs(10-b)){
    return b
  }
  if (math.abs(10-a)<math.abs(10-b)){
    return a
  }
  if (math.abs(a)>math.abs(b)){
    return b
  }
  if (math.abs(a)<math.abs(b)){
    return a
  }
  return a
}