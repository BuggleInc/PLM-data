override def run() {
def keepHandOnSideWall() = { 
	gauche()
	while (estFaceMur()) {
		droite()
		
	} 
	avance()
		
}

var etat = 0 ;
while (!(estSurBiscuit()) {
	match etat {
		case 0 => setDirection(Direction.NORD); while (!(estFaceMur())) {
			avance()
			
		};	etat = 1
		case 1 => while (getDirection() != Direction.NORD) {
			keepHandOnSideWall()
		}; etat = 0;
		case _ => printInt( "Ce cas ne devrait pas arriver. Corrigez ce probléme svp" )
	}
}
prendBiscuit()
}
}