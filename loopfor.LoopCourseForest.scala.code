for(i <- 1 to 7) {
  for(k <- 1 to 4) {
    for(j <- 1 to 4) {
      forward();
    }

    left();

    forward();
    forward();
    right();

    for(j <- 1 to 4) {
      forward();
    }

    right();
    forward();
    forward();

    left();

    for(j <- 1 to 4) {
      forward();
    }
    
    left();
  }
}