override def run() {
  for(i<- 1 to 9) {
    setColor(Color.black)
    for (j <- 1 to 20) {
      forward(100);
      backward(100);
      right(1)
    }
    setColor(Color.red)
    for (j <- 1 to 20) {
      forward(100);
      backward(100);
      right(1)
    }

  }
}