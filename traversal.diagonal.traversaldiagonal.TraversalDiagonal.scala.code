override def run() {
  var diag = 0
  var count = 0
  while (diag!=12){
    ecritMessage(count)
        count=count+1
    if (getX()==diag && getY()==0){
      diag=diag+1
      setX(0);setY(diag)
      
    }
    else{
      setX(getX()+1);setY(getY()-1)
    }
  }
}

