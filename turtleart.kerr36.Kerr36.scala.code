override def run():Unit= {
  for (petale <- 1 to 8){
    avance(20)
    avdroite()
    gauche(90)
    avance(20)
    avdroite()
    droite(90)
    avance(20)
    avgauche()
    gauche(135)
    avance(20)
    avdroite()
    gauche(90)
    avance(20)
    avdroite()
    gauche(135)
    avance(20)
    avgauche()
    droite(90)
    avance(20)
    avdroite()
    gauche(90)
    avance(20)
    avdroite()
  }
}

def avdroite(){
  droite(45)
  avance(20)
}
def avgauche(){
  gauche(45)
  avance(20)
}