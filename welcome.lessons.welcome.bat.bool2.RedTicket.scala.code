def redTicket(a:Int, b:Int, c:Int):Int = {
	var rep = 0
	if ( a== 2 && b == 2 && c ==2 ) {
		rep = 10 
	} else {
		if ( a== b && b == c) {
			rep = 5
		}
		 else {
		if ( a != b && a != c ) { 
			rep = 1
		}
	}
	}
	return rep
}
