var booleen : Boolean = false
def isFacingTrail():Boolean = {
  for (i <- 1 to 7) {
    avance();
    if (estFaceMur) { booleen = false } 
    if (getCouleurSol() == Color.green)
    {booleen = true}
  }
  return booleen
}