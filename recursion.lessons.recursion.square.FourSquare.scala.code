override def run() {
	def square(p:Double,a:Double)={
		for (i<- 1 to 4) {
			avance(p);
			droite(a);
		}
		
	}

var b = 100
var c= 90
square(b,c);
gauche(90)
recule(b)
gauche(90)
square(b,c);
droite(90)
avance(b)
square(b,c);
gauche(90)
square(b,c);
gauche(180)

}