def stringSplosion(str:String):String = {
  val n = str.length
  var r = ""
  var s = ""
  for (i <- 0 to n) {s = ""
    for (j <- 0 to i-1) {
      s = s + str.charAt(j).toString
    }
  r = r + s
  }
  return r }
