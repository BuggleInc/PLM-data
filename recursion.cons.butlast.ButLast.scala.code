def butLast(l:List[Int]):List[Int] = {
  
	def compute_butLast(l:List[Int], acc:List[Int]):List[Int] = {
    if(l.tail == Nil){
      return acc;
    } else {
      compute_butLast(l.tail, acc :+ l.head);
    }
    
    
  }
  return compute_butLast(l, List())
}