override def run() 
{

def maison()
{
	for (n<-1 to 3)
		{
			avance(30)
			droite(120)
		}
		droite(60)
		for (n<-1 to 3)
		{
			avance(30)
			droite(90)
		}
}

	for (n<-1 to 4)
	{
		avance(30)
		droite(30)
		maison()
	leveCrayon()
	droite(90)
	avance(50)
	gauche(90)
	baisseCrayon()
	}

	leveCrayon()
	gauche(90)
	avance(50)
	droite(90)
	avance(75)
	baisseCrayon()
	avance(30)
	droite(30)
	
	maison()
	
}
