override def run() {
  while (!estSurBiscuit) {
    if (keepHandOnSideWall && !estFaceMur) {
      avance()
    } else if (!keepHandOnSideWall) {
      gauche()
      avance()
    }
  }
  prendBiscuit()
}

def keepHandOnSideWall(): Boolean = {
  gauche()
  if (estFaceMur) {
    droite()
    return true
  }
  droite()
  return false
}