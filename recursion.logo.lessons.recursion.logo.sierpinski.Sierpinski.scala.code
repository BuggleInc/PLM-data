def sierpinski(level:Int, length:Double) {
  for(i<-0 to 2) {
    if (level == 0) {
    avance(length)
  	}
  	else{
  	  sierpinski(level-1,length/2)
 		  droite(120)
    	sierpinski(level-1,length/2)
    	droite(120)
    	sierpinski(level-1,length/2)
    	droite(120)
    	sierpinski(level-1,length)
  }
  droite(120)
  }

  
}