def bubbleSort3()  {
  var trie = false;
  var nb = getValueCount();
  var j = 0;
	do{
		trie = true;
    for(i <- 0 to nb-2-j){
      if(!isSmaller(i,i+1)){
        swap(i,i+1);
        trie = false;     
      }
    }  
  j = j+1;
  }while(trie == false);
}