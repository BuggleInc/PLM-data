def nearTen(num:Int):Boolean = 
{
//Soit un entier positif "num", renvoyez vrai si num est à distance 2 ou moins d'un multiple de 10. 
//Note: ( a % b ) est le reste de la division de a par b, donc (7 % 5) vaut 2.

	(num) match
	{
		case(num) if(num%10 == 0) => return true
		case(num) if((num +1)%10 ==0 ) => return true
		case(num) if((num%10+2) ==0 ) => return true
		case(num) if((num-1)%10  ==0 ) => return true
		case(num) if((num%10 -2) ==0 ) => return true
		case _					=> return false
	}
}