def isFacingTrail(color):
	v=True
	if estFaceMur():
		v=False
	if v==True:
		avance()
		if getCouleurSol()!=color:
			v=False
		recule()
		return(v)

def av():
  baisseBrosse()
  avance()
  leveBrosse()
  
def hunt(color):
	while estSurBiscuit==False:
		if isFacingTrail(color)==True:
			av()
		else:
			gauche()
			if isFacingTrail(color)==True:
				av()
			else:
				gauche()
				gauche()
				if isFacingTrail(color)==True:
					av()
	prendBiscuit()

avance()
c=getCouleurSol()
recule()
hunt(c)