def arrayFront9(nums:Array[Int]): Boolean = {

	var taille:Int = Math.min(4, nums.length);
	var bool:Boolean = false;

	for (i <- 1 to taille)
	{
		if (nums(i - 1) == 9) bool = true;
	}

	return bool;
}
