def gnomeSort() {
	val n:Int = getNombreValeurs()
	var pos:Int = 0
	
	def Sort(pos:Int) {
		pos match {
			case 0 => Sort(pos+1)
			case n => return
			case _ => if (plusPetit(pos, pos-1)) {echange(pos, pos-1); Sort(pos-1)} else Sort(pos+1)
		}
	}
}
