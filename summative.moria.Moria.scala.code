def clearMoria(){
  droite();droite();
  while(!estFaceMur()){
    while((getDirection() == Direction.EAST) && !estSurBiscuit() && !porteBiscuit()){
      avance();
    }
    prendBiscuit();droite();droite()
    while((getDirection() == Direction.WEST) && !estSurBiscuit() && porteBiscuit()){
      avance();
    }
    recule();poseBiscuit();droite();droite();
  }
  droite();avance();gauche();avance();
}