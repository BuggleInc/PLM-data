def polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double) {
  
  if(levels==1){
    var angle = 360/sides
    
    for(var i=0 to sides){
    	avance(length)
      droit(angle)
    }
    
    
    
    
  }
  
  
  
}