override def run() {
  for (compt <- 1 to 90) {
    avance(100)
    recule(100)
    droite(1)
  }
  setCouleur(Color.red)
  for (compt <- 1 to 90) {
    avance(100)
    recule(100)
    droite(1)
  }
  setCouleur(Color.yellow)
  for (compt <- 1 to 90) {
    avance(100)
    recule(100)
    droite(1)
  }
  setCouleur(Color.magenta)
  for (compt <- 1 to 90) {
    avance(100)
    recule(100)
    droite(1)
  }
}