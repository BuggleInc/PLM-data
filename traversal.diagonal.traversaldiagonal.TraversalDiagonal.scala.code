var diag = 0;
var cpt:Int=0;
override def run() {
 	
  while(diag!=6){
    parcours()
    diag+=1
  }

  
  

  
  
}


def parcours(){
  var x:Int=0
  var y:Int=0
  while(estFaceMur()==false && cpt!=0){
    x=getX()
    y=getY()
    ecritMessage(cpt)

    setPos(x,y+diag+1)
    cpt+=1
    ecritMessage(cpt)
    setPos(x+diag+1,y)
    cpt+=1
    ecritMessage(cpt)
    
  }

  
  
}
def changeDiag(){

}