override def run() {
  while (!estSurBiscuit()) {
    while (getDirection != Direction.NORTH) {
      gauche()
    }
    while (!estFaceMur) {
      avance()
    }
    while (estFaceMur()) {
      keepHandOnSideWall()
    }
  }
}

def keepHandOnSideWall() = {
  while (estFaceMur()) {
    droite()
  }
  avance()
}