def polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double) {
	if(levels==0){
		for(i<- 1 to sides){
			avance(length)
		}
		droite(360.0/sides)
	}else{
		avance(length)
		droite(360/sides+180)
		for(j<-1 to sides){
			polygonFractal(levels-1,sides,length*shrink,shrink)
			droite(180)
		}
		
	}	
}