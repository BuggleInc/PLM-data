while sortieTrouvee()==0:
    baggle=0
    corridor=1
    while croisement()==0 or corridor==1:
        corridor=0
        if estSurBiscuit()==1:
            baggle=baggle+1
        forward()
    if baggle<=2:
        droite()
    else:
        gauche()
forward()
