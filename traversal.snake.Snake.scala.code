override def run() {
  while (!fonctionTest()) {
    baisseBrosse();
    avance();
  }
}

def fonctionTest() : Boolean = {
  if (estFaceMur()) {
    if (getDirection() == Direction.WEST)
    { retourne(); }
    if (estFaceMur()) {
      droite();
      avance();
      droite();
    } 
    if (getDirection() == Direction.EAST)
    { retourne(); }
    if (estFaceMur()) {
      gauche();
      avance();
      gauche();
    }
    return true


  } 
  else {return false}
}