var diag = 0;
override def run() {
	var x = 0 
	var y = 0
	var num = 0
	
	while (diag != 12)
	{
		if (estFaceMur && diag < getMondeHauteur+2)
		{
			diag = diag + 1
			x = 0 
			y = diag - 1
			setPos(x , y )
		}
		else 
		{	
			droite; 
			if (estFaceMur)
			{
				gauche; 
				diag = diag + 1 
				x = diag - getMondeHauteur - 1
				y = getMondeHauteur -
				setPos(x , y )
			}
			else 
			{
				gauche ; 
				y = y - 1
				x = x + 1
				setPos(x , y )
			}
		
		}	
			
	}
}
