var diag = 0;
var cpt:Int=0;
var x:Int=0
var y:Int=0
x=getX()
y=getY()
override def run() {
 	ecritMessage(cpt)
  setPos(x,y+diag+1)
  cpt+=1
  while(diag!=6){
    parcours()
    diag+=1
  }

  
  

  
  
}


def parcours(){

  while(estFaceMur()==false){

    ecritMessage(cpt)

    setPos(x,y+diag+1)
    ecritMessage(cpt)
    setPos(x+diag+1,y)
    cpt+=1
    ecritMessage(cpt)
    
  }
  
}
