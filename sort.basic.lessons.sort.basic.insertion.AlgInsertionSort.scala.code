def insertionSort() {
  for (i <- 1 to getNombreValeurs()-1) {
    var v = getValeur(i)
    for (j <- 0 to i-1) {
      if (!plusPetit(i-j,v)) {
        copie(i-j,i)
        var derniereCase=i-j
      }
      copie(v,i-j)
    }
  }
}