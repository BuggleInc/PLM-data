def insertionSort() {
  var len = getValueCount();
 	for(i <- 1 to len-1) {
    var diff = 1;
    var v = getValue(i);
   
    while(i - diff >= 0 && v < getValue(i - diff)) {
      setValue(i-diff+1, getValue(i-diff));
      diff = diff+1;
    }
    
    setValue(i-diff+1, v);
  }   
}

