def crab(levels:Int, length:Double) {
	if (levels < 0) {
		return;
	}
	recule(length/Math.sqrt(2));
	droite(90);
	recule(length);
	droite(90);
	recule(length);
	crab(levels-1,length/Math.sqrt(2));
}
