def stringYak(str:String):String = 
{
	var chaine = ""
	var i = 0

	while(i <= str.length-1)
	{
		if(i<=str.length-2 && str.substring(i,3)=="yak")
			i+=3
		else			
		{
			chaine += str(i)
			i+=1
		}
	}

	return chaine
}
