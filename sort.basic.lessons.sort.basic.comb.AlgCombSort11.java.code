public void combSort11() {
	boolean swapped;
	int gap = getValueCount();
	do {
		swapped = false;
		if(gap > 1){
			gap = (int)(gap / 1.3);
			if(gap == 10 || gap == 9)
				gap = 11;
		}
		int i = 0;
		while(gap + i < getValueCount()){
			if(isSmaller(i+gap, i)){
				swap(i+gap, i);
				swapped = true;
			}
			i++;
		}
	} while(gap > 1 || swapped);
}
