for (tour <- 1 to 28) {
  for (pas <- 1 to 4) {
    avance()
  }
  gauche()
  for (pas <- 1 to 2) {
    avance()
  }
  droite()
  for (pas <- 1 to 4) {
  	avance()  
  }
  droite()
  for (pas <- 1 to 2) {
  	avance()  
  }
  gauche()
  for (pas <- 1 to 4) {
  	avance()  
  }
  gauche()
  
}