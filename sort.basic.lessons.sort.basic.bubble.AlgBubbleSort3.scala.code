def bubbleSort3() {

	var inverse = true

	for ( i <- 0 to getNombreValeurs -2) {
		inverse = false
		for (j <- 0 to (getNombreValeurs -2-i)) {
			if (!plusPetit(j,j+1)) {
				echange(j,j+1)
				inverse = true
			}
			if (inverse==false) {
				return
			}
		}
	}

}
