def bubbleSort2() {
  var temp:Int =0;
	
  for(i <- 0 to (getNombreDeValeurs-2){
    for(j <- getNombreDeValeurs to 0 by -1){
      if(plusPetit(i+1,i)){
        echange(i+1,i);
      }
    }    
  }
}