override def run() {
	def faireMotif() {
		def faireV(c:Color) = {
		avance()
		
		baisseBrosse()
		leveBrosse()
		avance()
		gauche()
		avance()
		baisseBrosse()
		leveBrosse()
		recule()
		droite()
		avance()
		baisseBrosse()
		leveBrosse()
	}
	for (i <- 1 to 4) {
		faireV()
		avance()
		gauche()
	}
	}
	for (compteur <- 1 to 9) {
		for (i1 <- 1 to 9) {
		faireMotif()
		for (i11 <- 1 to 5) {
			avance()}
		}
	gauche()
	for (i2 <- 1 to 5) {
		avance()
	}
	droite()
}
}
