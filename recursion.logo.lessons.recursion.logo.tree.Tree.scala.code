def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  if (steps!=0) {
    current(steps)
    avance(length)
    tree(steps-1, length*shrink, angle+90,shrink)
    tree(steps-1, length*shrink, angle-90,shrink)
    recule(length)
  }
}