def insertionSort() {
  val lgr = getNombreValeurs()
  for (i <- 1 to (lgr - 1)) {
  	var v = getValeur(i)
    var compt = i
    while (getValeur(compt-1) > v) {
      echange(compt,compt-1)
      if (compt > 1) {
      	compt = compt - 1
      }
    }
  }
}