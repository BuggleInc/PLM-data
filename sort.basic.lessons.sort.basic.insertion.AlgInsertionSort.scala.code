def insertionSort() {
  for (i<-1 to getNombreValeurs()-1){
    var v=getValeur(i)
    var boucle=i
    while (getValeur(boucle-1)>v) {
      setValeur(boucle,getValeur(boucle-1))
      boucle=boucle-1
    }
    setValeur(boucle,v)
  }
}