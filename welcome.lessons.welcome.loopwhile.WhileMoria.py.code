while(isBackingWall()==False):
    backward()
    if(isOverBaggle()):
        pickupBaggle()
        while(isCarryingBaggle()):
            forward()
            if(isOverBaggle()):
                backward()
                dropBaggle()

if(isBackingWall()==True)
    right()
    backward()
    right()
    forward()