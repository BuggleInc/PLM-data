def isFacingTrail():Boolean =
{
  var isVert = false;
  var nbPas = 0;
  
  while (!isVert && !estFaceMur() && nbPas < 7)
  {
    isVert = (getCouleurSol() == Color.green);
    avance();
    nbPas += 1;
  }
  for (i <- 1 to nbPas) recule();
  
  return isVert;
}