def bubbleSort3() {
	val taille = getNombreValeurs();
	var échangefait = false;
     for (x <- taille-2 to 0 by -1) {
     	échangefait = false;
	  for (i <- 0 to x) {
		if ( plusPetit(i,i+1) == false) {
			echange(i,i+1);
			échangefait = true;
		} 
	}
	if (échangefait == false)
	return
	}
}
