def keepHandOnSideWall(){
  for(t<-1 to 2){
    droite();
      if(getDirection()==Direction.NORTH && !estFaceMur()){
      avance();
      return
    }
  }
  gauche();
  while(estFaceMur()){
    droite();
  }
  avance();
}

override def run() {
  while(!estSurBiscuit()){
    keepHandOnSideWall();
  }
  prendBiscuit();
}