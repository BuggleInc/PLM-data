n = getTaillePile()

while n > 1:
  im = 0
  rm = 0
  for i in range(n):
    r = getRayonCrepe(i)
    if r > rm:
      rm = r
      im = i
  retourne (im+1)
  retourne (n)
  rst = n-im-1
  if rst>0:
	  retourne (rst)
  n = n-1