override def run() {
  def keepHandOnSideWall(){
    droite()
    if(estFaceMur()){ gauche(); return true}
    else{gauche;return false}
  }
  
  while(!estSurBiscuit()){
    if(keepHandOnSideWall()){avance()}
    
  }
  prendBiscuit()
}