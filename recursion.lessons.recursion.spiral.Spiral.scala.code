def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {

	if(steps!=0){
		var l:Int=length
		var i:Int=increment
		var s:Int = steps
		avance(l)
		gauche(angle)
		s=s-1
		l=l+i
		spiral(s,angle,l,i)
	}

}
