def haveBaggle():
    seenBaggle = False
    steps = 0
    while steps <= 6 and seenBaggle != True:
        forward()
        steps = steps + 1
        if isOverBaggle() == True:
            seenBaggle = True
    while steps >= 0:
        backward()
        steps = steps -1
    return seenBaggle