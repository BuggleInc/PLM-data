def hunt(c:color) {
    // Write your code here
  while (estSurBiscuit == false){
    while (isFacingTrail(c:color) == false) {
    droite()
  	}
    avance() ; baisseBrosse() ; leveBrosse()
  }
  prendBiscuit()
}

// Copy your isFacingTrail() here

def isFacingTrail(c:color):Boolean = {
  if (estFaceMur() == true ){
    return false
  }
  avance()
  if (getCouleurSol == c){
    recule()
    return true
  } else {
    recule()
    return false
  }
}