override def run() {
    def keepHandOnRightWall() = {
    	while (!(estFaceMur()) {droite()}
    	gauche()
    	avance()
    }
    while (!(estSurBiscuit)) {
    	keepHandOnRightWall();
    }
    prendBiscuit();
}
