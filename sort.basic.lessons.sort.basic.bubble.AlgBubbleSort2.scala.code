def bubbleSort2() {
  var trie = false
  var c:Int=0
  while(!trie){
    trie=true
    for(i<- 0 to getValueCount()-2-c){
      if (getValue(i) > getValue(i+1)){
        echange(i,i+1)
        trie = false
      }
    }
    c=c+1
  }
}
