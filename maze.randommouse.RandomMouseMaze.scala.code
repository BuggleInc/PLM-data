override def run() {
     while (!estSurBiscuit) {
       var n = random3 ()
       n match {
         case 0 => if (!estFaceMur) {avance()}
         case 1 => gauche()
         case 2 => droite()
       }
     }
  prendBiscuit()
}