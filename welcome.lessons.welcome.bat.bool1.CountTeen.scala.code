def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {
	var nbAdos=0
	if (a>12 && a<20) nbAdos=nbAdos+1
	if (b>12 && b<20) nbAdos=nbAdos+1
	if (c>12 && c<20) nbAdos=nbAdos+1
	if (d>12 && d<20) nbAdos=nbAdos+1
	return nbAdos
}
