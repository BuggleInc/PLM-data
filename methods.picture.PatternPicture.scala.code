override def run() {
  motif()
  motif()
  left()
  avance(4)
  left()
  avance(7)
  droite()
  droite()
  motif()
  motif()
  recule(3)
}

def dessine(){
  baisseBrosse()
  leveBrosse()
}

def faireDess(c:Color){
  setCouleurBrosse(c)
  dessine()
  avance()
  left()
  avance()
  dessine()
  avance()
}

def motif(){
  avance(2)
  faireDess(Color.BLUE)
  faireDess(Color.GREEN)
  faireDess(Color.YELLOW)
  faireDess(Color.RED)
  avance()
  if(!estFaceMur()){
    avance()
  }
}
