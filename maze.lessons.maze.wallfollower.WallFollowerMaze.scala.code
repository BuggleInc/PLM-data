override def run() {
    def keepHandOnRightWall() = {
    	if (estFaceMur) {droite()} 
    	else {while (!(estFaceMur)) {droite()}}
    }
    while (!(estSurBiscuit)) {
    	keepHandOnRightWall();
    	if (!estFaceMur) {avance()};
    }
    prendBiscuit();
}
