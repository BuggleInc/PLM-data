def insertionSort() {
	var stockage=0
  var k=0
  for (i<- 1 to getNombreValeurs()-1) {
    stockage=getValeur(i)
    k=i
    while(!plusPetitQue(k-1,stockage) && k>1) {
      k-=1
      copie(k,k+1)
      if (k==0) {setValeur(k,stockage)}
    }
    setValeur(k,stockage)
  }
}