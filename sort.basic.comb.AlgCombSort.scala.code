def combSort() {
  val n = getNombreValeurs()
  val ecart = n
  if (ecart > 1) {
    ecart = (ecart.toDouble / 1.3).toInt
  }
  var i = 0
  while(i+ecart<n) {
    if(plusPetit(i+ecart,i)) {
      echange(i,i+ecart)
      i += 1
    }
  }
}