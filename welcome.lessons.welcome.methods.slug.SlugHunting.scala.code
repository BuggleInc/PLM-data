def hunt() {
  while ( !estSurBiscuit() ) {
    if ( isFacingTrail == true ) {
    	avance();
    	baisseBrosse();
    } else {
    	gauche();
    }
    }
    prendBiscuit();
}





def isFacingTrail():Boolean = { 
	 if ( !estFaceMur() ) { avance(); }
	 else { gauche(); avance(); }
	if (getCouleurSol() == Color.green) {
		 recule();
		 return true
	} else {
           recule();
		 return false
		}
		
	} 
           