var nbpiste = 0;
var n = 0;
var m = 0;

for (nbpiste <- 1 to 7) {
  
  for (m <- 1 to 4) { 
    
    for(n <- 1 to 4) {
      avance();   
  	}
    gauche();
    
    for(n <- 1 to 2) {
      avance();   
  	}
    droite();
    
    for(n <- 1 to 4) {
      avance();   
  	}
    droite();
    
    for(n <- 1 to 2) {
      avance();   
  	}
    gauche();
    
    for(n <- 1 to 4) {
      avance();   
  	}
   	gauche();
  }
}