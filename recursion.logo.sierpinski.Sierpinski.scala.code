def drawSierpinski(level:Int, length:Double){
  
  sierpinski(level, length);
  right(120.00);
  
  sierpinski(level, length);
  right(120.00);
  
  sierpinski(level, length);
  right(120.00);
  
}

def sierpinski(level:Int, length:Double) {
  
  if(level == 0){

    	avance(length); 
    
  }
  
  
  
}