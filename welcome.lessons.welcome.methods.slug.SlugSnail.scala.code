def hunt(c:Color) {
    while(!estSurBiscuit){
      while(!isFacingTrail(c)){gauche();}
    avance()
    baisseBrosse();leveBrosse();  
    }
  	prendBiscuit()
}

def isFacingTrail(c:Color):Boolean = {
    if(estFaceMur()){return(false)}
  	else{
      avance()
      if(getCouleurSol() == Color.c){
        recule()
      	return(true)
      }
     	else{
        recule()
        return(false)
      }
    }
}