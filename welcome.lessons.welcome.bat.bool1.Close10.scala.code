def close10(a:Int, b:Int): Int = {
	var rep = 0
	if (!(math.abs(a) == math.abs(b))) {
		if ( math.abs(10 - math.abs(a)) < math.abs(10 - math.abs(b) )) {
			rep = a 
		}
	} else {
		rep = b
	}
	return rep
}
