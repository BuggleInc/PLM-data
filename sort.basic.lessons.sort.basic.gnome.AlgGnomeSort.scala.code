def gnomeSort() {
  for (i<- 0 to getValueCount()-2){
    if (isSmaller(i+1, i)){
      swap(i+1, i);
      var a=false;
      var j=i;
      while (!a) {
        if isSmaller(j-1, j){
          swap(j-1, j);
          j-=1;
        } else {
          a=true;
        }
      }
    }
  }
}