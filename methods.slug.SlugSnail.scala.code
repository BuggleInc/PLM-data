def hunt(c:Color) {
     // Write your code here
 }

 // here comes your isFacingTrail method
def isFacingTrail(Couleur:Color):Boolean = {
  if (estFaceMur()) { return (false)}
  avance()
  if (getCouleurSol()==Couleur) {recule();return (true)}
  else { recule();return false}
}