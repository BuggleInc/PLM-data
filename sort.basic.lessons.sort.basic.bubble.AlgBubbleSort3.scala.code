def bubbleSort3() {
	for (i<-getNombreValeurs()-2 to 0 by -1)
	{
		for (j<-0 to i) {
			if (plusPetit(j:Int, j+1:Int)==false) 
		{	
			echange(j:Int, j+1:Int)
		}
			if (plusPetit(j:Int, j+1:Int)==true)
			{ return
		}
} 
}
}
