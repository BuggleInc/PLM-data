var diag = 0
var cpt = 0;
override def run() {
ecritMessage(cpt)
cpt=cpt+1
	for (i <- 1 to 6){
		for (i <- 1 to diag){
			ecritMessage(cpt)
			setPos(getX()+1, getY()-1)
			cpt=cpt+1
		}
		ecritMessage(cpt)
		setPos(0, getY()+diag+1)
		diag=diag+1	
	}
}
