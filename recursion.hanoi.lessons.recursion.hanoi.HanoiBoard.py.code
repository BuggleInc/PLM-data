def solve(src,dst,other, height=-1):
    if height==-1: # initial case
        height=getSlotSize(src) 
    if getRayon(src)>getRayon(dst) or getTaillePiquet(other)==0:
        deplace(src, dst)
        

        
    
    # Your code here
