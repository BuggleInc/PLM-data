def bubbleSort()  {
  var B : Boolean = true
  var n = getNombreValeurs()
    while (B) {
      for (j <- 0 to n-2 ){
      for (i <- j to n-2) {
    		if (plusPetit(i+1,i)) {
      		echange(i,i+1)
          B = true}
        else
      		B = false
      }}
   }
}