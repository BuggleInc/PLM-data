def bubbleSort2() {var Tri = true
  var nbCompteur=0
    var compteur=0
    for (i<-0 to getNombreValeurs()-2) {
      if (!plusPetit(i, i+1)) {
        echange (i,i+1)
        compteur=1
        nbCompteur=nbCompteur+1
        
      }
    }
    if (compteur == 0) {
      Tri=false
    }
    
  
}