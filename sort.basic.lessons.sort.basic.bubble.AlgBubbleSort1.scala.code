def bubbleSort()  {
  var switch:Boolean=true
  while switch=true {
    switch=false
    for(k<-0 to getNombreValeurs()-2){
      if (plusPetit(k+1,k)) {echange(k+1,k)} switch=true
    }
  }
      
}
