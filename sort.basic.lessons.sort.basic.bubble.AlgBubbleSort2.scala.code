def bubbleSort()
{
  var stop = false
 
  while (getNombreValeurs())
  {
    stop = true
    for (i <- 0 to getNombreValeurs() - 2)
    {
      if (plusPetit(i + 1, i))
      {
    		echange(i, i + 1)
        stop = false
      }
    }
  }
}