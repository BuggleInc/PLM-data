def golomb(num:Int): Int = {
  
  var T:Array[Int] = new Array [Int](num)
  var cpt : Int = 0
  var nb : Int = 1
  
  while (cpt!=num){  
    for (j<- 1 to nb){
      cpt+=1
      T(cpt-1)=cpt
      if(cpt==num-1){return T(cpt)}
      
        
    }
    nb=cpt
  } 
	return T(cpt)
}