def haveBaggle():Boolean = {
	var seenBaggle : Boolean = false ;
  for (i <- 1 to 7) {
    if (isOverBaggle()) {
      seenBaggle = true
    }
    forward()
  }
  return seenBaggle ;
}


for (row <- 1 to 7) {
  if (! haveBaggle() ) {
    right() ; forward() ; left()
  }
}