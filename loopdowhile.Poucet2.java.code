public void run() {
  int nbBiscuit = 0;
  int nbCase = 0
    
  do{
    if(estSurBiscuit){
      nbBiscuit++;
    }
    
    if(crossing() && nbBiscuit >=3 && !sortieTrouvee()){
      
    }else if (crossing() && nbBiscuit <=2 && !sortieTrouvee()){
      
    }
    
  }while(!crossing())  
  
}