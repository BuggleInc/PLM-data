var compteur = 0;
if (estSurBiscuit()){
	compteur = compteur + 1
	avance();
}


do {
   if (!croisement()){
    if (estSurBiscuit()){
	compteur = compteur + 1
	
}
  avance()
  }
  else if (compteur <= 2) {
  	gauche(); compteur = 0;
  }
  else   droite(); 
         compteur = 0; 
}
while (!sortieTrouvee());
avance();


