boolean haveBaggle() {
  var vuBiscuit = false
  for (i <- 1 to 7) {
   for (i <- 1 to 6 ) {
     avance;
     if (estSurBiscuit) vuBiscuit= true;
   }
   for (i <- 1 to 6) recule;
   if (vuBiscuit==true) return;
   else droite;
  }
}