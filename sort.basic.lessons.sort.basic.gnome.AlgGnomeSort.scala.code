def gnomeSort() 
{
	var pos=0
	while(pos!=getNombreValeurs())
	{
		if(pos==0)
			pos=pos+1
		else
		{
			if(plusPetit(pos-1,pos))
				pos=pos+1
			else
			{
				echange(pos-1,pos)
					pos=pos-1
			}
		}
	}
}
