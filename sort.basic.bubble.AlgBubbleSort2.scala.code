def bubbleSort2()  {
  var lastEchange: Boolean = false
  do {
    lastEchange = false
    for (i <- 0 to getNombreValeurs()-2) {
      for (j <- 0 to i) {
        if (plusPetit(j + 1,j)) {
        	echange(j,j+1)
        	lastEchange = true
      	}
    	}
    }
  } while (lastEchange==true);
}