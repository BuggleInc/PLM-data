var nbBaggle = 0
while(!sortieTrouvee())
  {
		for(compteur<-1 to 5)
    	{
        if(estSurBiscuit())
        	nbBaggle = nbBaggle + 1
      }
    
    if (nbBaggle>=3)
    	gauche()
      
    else
    	droite()
    
    nbBaggle=0

  }