def bubbleSort3() {
  var swapped= false
  var c:Int=getValueCount()-2
  do {
    swapped=false;
    for (i<-0 to c){
      if (!isSmaller(i,i+1)) {
        swap(i,i+1);
        swapped= true      
      }
    }
    c=c-1
  }while (swapped);
  
}