var isover : Boolean = false
def bubbleSort()  {
  for (a <- 0 to (getValueCount()-3) ){
    for (i <- 0 to getValueCount()-2 ){
      if (isSmaller(i, i+1) == false){
        swap(i, i+1)
      }
    }
  }
}
