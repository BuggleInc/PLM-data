        def goAndGet() {
        	var pas=0
        	while(!estSurBiscuit) {
        		avance(); pas=pas+1
        	}
        	prendBiscuit();
        	var pas2=0
        	while(pas2 != pas) {
        		recule(); pas2=pas2+1
        	}
        	poseBiscuit();
        }

        for (i <- 1 to 7) {
            goAndGet();
            right();
            forward();
            left();
        }
