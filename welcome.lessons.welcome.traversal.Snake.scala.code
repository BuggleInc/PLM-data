override def run() {
  if(!arriver()){
    avance()
  }
}

def arriver(){
	if (estFaceMur() && getDirection() == Direction.EST){
    gauche();
    if (estFaceMur()){
      droite()
      return true
    }
  }
  return false
}

