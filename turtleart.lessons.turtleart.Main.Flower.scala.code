override def run() {
	octogone();
	avance(25);
	droite(45);
	deuxiemeLigne();
	avance(25);
}
def ligne() {
	avance(50);
	droite(45);
}
def octogone() {
	for (i<- 0 to 7) ligne();
}
def deuxiemeLigne() {
	for (i<- 0 to 7) {
		avance(25);
		gauche(45);
		avance(25);
		droite(90);
	}
}
def Fleur() {
	for (i<- 0 to 6) {
		droite(45);
		avance(25);
		droite(45);
		avance(25);
		droite(180);
		avance(25);
		droite(45);
		avance(25);
		droite(270);
	}
}