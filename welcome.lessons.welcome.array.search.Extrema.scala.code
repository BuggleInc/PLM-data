def extrema(nums:Array[Int]): Int = {
  var max = nums(0)
  var min = nums(0)
  for (element <- nums){
    if (element > max){
      max = element
    }
    if (element < min){
      min = element
    }
  }
  return max- min
}