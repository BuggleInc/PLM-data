def step() {
  var col = getGroundColor()
  if (col == Color.black){
    setBrushColor(Color.white)
    paint()
    left()
    forward()
  }else if (col == Color.white){
    setBrushColor(Color.black)
    paint()
    right()
    forward()
  }
}

def paint(){
  brushDown()
  brushUp()
}
