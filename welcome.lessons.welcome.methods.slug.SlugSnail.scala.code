def hunt(c:Color) {
      while (!estSurBiscuit){
        if (isFacingTrail(c)){
          baisseBrosse()
      		avance()
          leveBrosse()
    }
      
      	else {
          gauche()
        	if (!isFacingTrail(c)){
            retourne()
            baisseBrosse()
            avance()
            leveBrosse()
          }
          baisseBrosse()
          avance()
          leveBrosse()
        }
          
    }
  prendBiscuit()
}

def isFacingTrail(c:Color):Boolean = {
    if (!estFaceMur()){
      	avance()
  			if (getCouleurSol()== c){
          recule()
          return true
        }
      	else {
          recule()
          return false
        }
     }
  return false
}
