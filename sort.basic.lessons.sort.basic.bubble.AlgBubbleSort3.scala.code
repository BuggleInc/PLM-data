def bubbleSort3() 
{
	var fini = false
	
	for(i <- getNombreValeurs()-2 to 0 by (-1))
	{
		for(j <- 0 to i)
		{
			fini = true
			
			if(plusPetit(j+1,j))
			{
				echange(j,j+1)
				fini = false
			}
		}

		if(fini) return
	}
}
