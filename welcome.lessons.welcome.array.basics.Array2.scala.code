override def run() {
     var j=0;
	var T = new Array[Color](getMondeHauteur());
	var haut=0;
	var i=0;
	haut=getMondeHauteur();
	for(i<-0 to haut-2){
		T(i)= getCouleurSol();
          avance();
	}
	T(haut-1) = getCouleurSol();
	recule(haut-1);
	for (j <-0 to haut-2){
		gauche(); avance(); droite();
		for(i<-0 to haut-2) { 
			
			setCouleurBrosse(T(i+1));
			baisseBrosse(); 
			leveBrosse();
			avance();
		}	
	
	}

















	
}
