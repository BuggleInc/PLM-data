def pentaKoch(levels:Int, length:Double) {
	if (levels == 0) 
	{avance(length)
} else {
	avance(length*0.4)
	gauche(112)
	avance(length*0.4)
	droite(80)
	avance(length*0.4)
	droite(80)
	avance(length*0.4)
	droite(80)
	avance(length*0.4)
	gauche(112)
	}
}
