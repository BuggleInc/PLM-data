def insertionSort() {
  var l = getNombreValeurs()
  var v = 0
  var j = 0
  for (i <- 1 to l) {
    j = i
    v = getValeur(i)
    while (v < getValeur(j) && j > 0) {
      setValeur(j,getValeur(j-1))
      j -= 1
    }
  	setValeur(j,v)
  }
}