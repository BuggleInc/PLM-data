def bubbleSort()  {
  
  var x=getValueCount();
  
  for (i <- 0 to x){
    
    for (j <- 0 to x-1){
      
      if (isSmaller((getValue(i+1)), getValue(i))){
        
        swap(i,i+1)
        
      }
      
    }
    
  }
  
}