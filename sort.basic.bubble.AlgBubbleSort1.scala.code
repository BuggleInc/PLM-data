def bubbleSort()  {
  var B : Boolean = true
  var n = getNombreValeurs()
    while (B) {
      for (i <- 0 to n-2) {
    		if (plusPetit(i+1,i)) {
      		echange(i,i+1)
          B = true}
        else
      		B = false
    }
   }
}