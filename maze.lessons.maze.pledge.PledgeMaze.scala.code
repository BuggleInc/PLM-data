override def run() {
 
def isDirectionFree () {
  var direction = getDirection();
  setDirection(dir);
  if(!estFaceMur()){avance()};
  else {setDirection(direction)};
}


def suivre_mur() {
  gauche();
  while(estFaceMur()){droite()}
  avance();
  }

var angle = 0;
var dir = getDirection();
do {suivre_mur()} while (!(angle==0 && getDirection==dir))
}