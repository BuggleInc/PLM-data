def polygonFractal (levels:Int, sides:Int, length:Double, shrink:Double) {

if(levels==0){
	avance(length*shrink)
	
}else{
	for(x<-1 to sides){
	polygonFractal(levels-1, sides, length/shrink, shrink)
	droite(360/sides)
	polygonFractal(levels-2, sides, length/(shrink*shrink), shrink)
	}
	
	
}
}