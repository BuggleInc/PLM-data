def alarmClock(day:Int, vacation:Boolean): String = {
  if (we(day) && !vacation) {return "7:00"}
  else if (!we(day) && vacation) {return "off"}
  else {return "10:00"}
}

def we(a:Int):Boolean= {
  if (a>0 && a<6) {return true}
  else {return false}
}
