def indexOfMaxValue(nums:Array[Int]):Int = {
  var championne=nums(0)
  var championne2=nums(0)
  for (i<-0 to (nums.length -1)) {
    if (nums(i)>championne) {
      championne=nums(i)
    } else { 
      if (championne2==championne) {
        return championne
      }

    }
  }
}
