def withoutDoubles(die1:Int, die2:Int, noDoubles:Boolean):Int = {
  noDoubles match {
    case false => return die1+die2
    case true => if (die1==die2) {return (die2 + (die1%6+1))} else 
    	{return die1 + die2}
  }
}