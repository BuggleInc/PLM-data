def step() {
  getGroundColor() match {
  case Color.white =>	mark(Color.black);	right(); forward();
  case Color.black =>	mark(Color.white);	left(); forward();
  }
}


def mark(c:Color) {
  setBrushColor(c);
  brushDown();
  brushUp();
}
