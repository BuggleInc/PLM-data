var biscuits:Int = 0

while (!sortieTrouvee()) {
  for (cases <- 1 to 5) {
    avance()
    if (estSurBiscuit()) {
      biscuits = biscuits + 1
    }
  } if (biscuits > 2) {
    gauche()
  } else {droite()}
}