def lessBy10(a:Int, b:Int, c:Int):Boolean = {
  var x=Math.min(a,b)
  var y=Math.min(x,c)
  if (Math.min(Math.min(a,b),c)<a-10) {
    return true
  }
   if (Math.min(Math.min(a,b),c)<b-10) {
    return true
  } if (Math.min(Math.min(a,b),c)<c-10) {
    return true
  }
  return false }