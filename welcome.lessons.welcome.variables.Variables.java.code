public void run() {
int stepAmount=0;
int doneSteps=0;
	while (isOverBaggle() == false) {
		forward();
		stepAmount = stepAmount + 1;
		if (isOverBaggle() == true) {pickupBaggle();}
		}
		while (isCarryingBaggle() == true && stepAmount != doneSteps){doneSteps = doneSteps +1; backward();}
		dropBaggle(); 
}

/* isFacingWall() isOverBaggle(), isCarryingBaggle(), pickupBaggle() or dropBaggle().*/
