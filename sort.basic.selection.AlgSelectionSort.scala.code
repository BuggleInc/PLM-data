def selectionSort() {
  var r = 0
  var debut = 1
  var len = getNombreValeurs()-1
  var lgr = len-1
  for (i<-1 to lgr){
    r=i
  	  for (k<-i to len){
      	if (plusPetit(k,r)){
        	r=k
     }
    }
  echange(i,r)
  }
}