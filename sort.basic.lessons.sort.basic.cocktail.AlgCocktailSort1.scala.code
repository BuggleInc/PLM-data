def cocktailSort() {
  var tri = true
	while (tri) {
    tri = false
  	for (i<- 0 to getNombreValeurs()-2) {
    	if (!(plusPetit(i,i+1))) {
      	echange(i,i+1)
      }
    }
  	for (j <- getNombreValeurs()-2 to 0) {
    	if (plusPetit(j,j-1)) {
      	echange(j,j-1)

      }
  	}
    
  }
}