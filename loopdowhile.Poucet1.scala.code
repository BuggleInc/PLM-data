var compt = 0

do {
  compt = 0
  for (i <- 0 to 4)
  	{
      avance();
      if (estSurBiscuit())
      	compt = compt + 1
      if (compt > 2)
      	gauche();
      else
      	droite();
    }
} while (sortieTrouvee == false)