def bubbleSort2() {
  var tab_has_changed = false;
  var i = 0;
  
  do{
  for (i <- 0 until (getNombreValeurs	- i) - 1){
    if (plusPetit(i+1, i)){
    	echange(i, i+1)
      tab_has_changed = true
    }
  }
  }while(tab_has_changed)
}