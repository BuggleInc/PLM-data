def insertionSort() {
  var len = getValueCount();
  
 
  
 	for(i <- 1 to len-1){
    
    var v = getValue(i);
   
    var j = i;
    var fin = false;
    while(j > 0 && !fin){
      if( getValue(j-1) > getValue(j)){
        copy(j-1, j);
      }
      else {
        setValue(j, v);
        
      }
      j = j-1;
    }
    
  } 
  
 
  
}