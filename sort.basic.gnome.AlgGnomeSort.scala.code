def gnomeSort() {
  var x=0
  var ok:Boolean=true
  x=getNombreValeurs
  for (i <- x-2 to 0 by -1) {
    ok=true
    for (j <- 0 to i) {
      if (plusPetit(j+1,j)) {
        echange(j+1,j)
        ok=false
      }
    }
    if (ok) {
      return
    }
  }
}