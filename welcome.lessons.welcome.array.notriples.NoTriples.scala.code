def noTriples(nums:Array[Int]): Boolean = {
  var pre = nums(0)
  var deu = nums(1)
  var troi = nums(2)
  for (l <- 3 to nums.length-1){
    if (pre==deu && deu==troi){
      return true
    }
    troi=nums(l)
    deu=num(l-1)
    pre=num(l-2)
  }
  return false
}