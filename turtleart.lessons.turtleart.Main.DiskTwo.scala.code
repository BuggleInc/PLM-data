override def run() {
	for (i <- 1 to 9) {
	setCouleur(Color.black)
	for (k <- 1 to 30) {
		avance(100)
		recule(100)
		droite(0.75)
	}
	setCouleur(Color.red)
	for (k <- 1 to 30) {
		avance(100)
		recule(100)
		droite(0.75)
	}
}
}
