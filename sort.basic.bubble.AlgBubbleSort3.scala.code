def bubbleSort3()  {
  var n =  getValueCount();
  for(i <- 2 to n)
  	traverse(n-i);
}


def traverse(fin:Int) : Unit  = {
   var swapped = false;
   for (i <- 0 until fin){
     if(isSmaller(i+1, i)){
   		 swap(i,i+1);
       swapped = true;
	   }
   }
   if(!swapped)
  	 return;
}