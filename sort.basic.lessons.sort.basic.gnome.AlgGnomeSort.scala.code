def gnomeSort() {
  var position:Int = 0
  while (position < getNombreValeurs()-1) {
    if (getValeur(position)>getValeur(position+1)) {
      echange(position,position+1)
      if (position != 0) {position -= 1}
    } else {position += 1}
  }
}