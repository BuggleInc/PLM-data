def solve() {
  while (isSorted()==false){
    var i =1
    var max=0
    var n =1
    for (i <- n to getStackSize()-1) {
      if (getPancakeRadius(i)>max){
      max=i
      }
    }
    if (i==0){
      flip(getStackSize()-1)
    }
    else {
    flip(i)
    }
  }
}