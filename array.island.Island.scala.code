def island(num:Array[Int]): Int = {
  
  var nbils = 0
  var n = 0
  var long = num.length
  
  for (n <- 0 to (long-2)) {
    if (num(n) < num(n+1)) {
      nbils += 1
    }
  }
       
  return nbils
  
}