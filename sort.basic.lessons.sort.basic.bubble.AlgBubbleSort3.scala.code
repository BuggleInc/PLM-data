def bubbleSort3() {
  var swapped=false
  do{
    swapped=false
    for (i<-getValueCount() to 1 by -1){
      if (!isSmaller(i,i-1)){
        swap(i,i-1)
        swapped=true
      }
    }
  } while(swapped)
}