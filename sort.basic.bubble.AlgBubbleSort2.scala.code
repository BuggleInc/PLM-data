def bubbleSort2() {
  var tab_has_changed = false;
  var i = 0;
  var nb_occurence = 0;
  
  do{
    tab_has_changed = false;
  for (i <- 0 until (getNombreValeurs	- nb_occurence) - 1){
    if (getValeur(i+1) < getValeur(i)){
    	echange(i, i+1);
      tab_has_changed = true;
    }
  }
    nb_occurence += 1;
  }while(tab_has_changed);
}