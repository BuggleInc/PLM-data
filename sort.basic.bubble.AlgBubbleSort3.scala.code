def bubbleSort3() {
  var g=false; for(i<-(getNombreValeurs()-2) to 0 by -1)
                   	{for (j<- 0 to i)
                      	{g=false;if(plusPetit(j+1,j)){echange(i,j);g=true}}
                     if (g==false){return}}
}