override def run() {    while(!estSurBiscuit()){

var random3:Int = random() + random() + random()
    
    if (random3 == 0) {droite()}
    if (random3 ==1 && !estFaceMur()) {avance()}
    if (random3 == 2) {gauche()} 
}
prendBiscuit();
}
