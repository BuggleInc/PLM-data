def has271(nums:Array[Int]): Boolean = {
	var est = false
	var l = nums.length
	for(i<- 0 to (l-3)){
		if(nums(i+1)==(nums(i)+5) && (nums(i+2) <= (nums(i)+1) || (nums(i)-3)<=(nums(i+2)))){
			est = true
		}
	}
	return est
}
