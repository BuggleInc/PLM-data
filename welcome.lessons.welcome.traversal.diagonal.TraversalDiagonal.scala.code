override def run() {
  var worldSize = getWorldHeight()
  var diag = 0
  
  for (i <- 1 to worldSize){
    for (j <- 1 to i){
      diag += 1
      setPos(j - 1, i-j)
      writeMessage(diag)
    }
  }
  
}