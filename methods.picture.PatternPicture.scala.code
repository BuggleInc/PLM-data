override def run() {
  def Motif(c:Color){
    setCouleurBrosse(c)
    avance()
    baisseBrosse();leveBrosse()
    recule()
    gauche();avance()
    baisseBrosse();leveBrosse()
    recule();droite();avance(3);gauche()
  }
def Ligne(){
  for (i<-1 to 2){
    Motif(Color.RED)
  Motif(Color.BLUE)
  Motif(Color.GREEN)
  Motif(Color.YELLOW)
  avance(4)
  }
  
}
	Ligne()
	recule(4);gauche();avance(4)
  Ligne()
}