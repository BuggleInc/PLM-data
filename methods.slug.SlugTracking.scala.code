def isFacingTrail():Boolean = {
  val rep = false;
  println(1);
  if (!estFaceMur()) {
    println(2);
    avance()
    if (getCouleurSol()==Color.green) {
      val rep = true
    }
    recule()
  }
  return rep
}