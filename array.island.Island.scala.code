
def max(liste:Array[Int]):Int={
  
  var max:Int=liste(0);
  
  
  for(i <- 1 to liste.length -1){
   			if( max < liste(i) ){max=liste(i);}
  }
  return max;
  
}


def island(num:Array[Int]): Int = {
	
  if ( num.length == 0){return 0}
  return max(num)
  
  
 
  
}