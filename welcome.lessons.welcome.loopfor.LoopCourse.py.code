pas=0        #nombre de case parcourue
cote=0       #nombre de coté parcouru
for pas in range(8):
    if(estFaceMur()==False):
        forward()
        pas=pas+1
    else:
        gauche()
        cote=cote+1
        pas=0

