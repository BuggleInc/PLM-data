def gnomeSort() {
  var len = getValueCount();
  var nain = 0;
  
  while(nain < len-1){
    if(getValue(nain) < getValue(nain+1)){
      nain = nain + 1;
    }
    else {
      swap(nain, nain+1);
      if(nain < 1){
        nain = nain + 1;
      }
      else {
      	nain = nain - 1;
      }
    }
  }
}