override def run() {
	def octogone(taille:Int) ={
		for (i<-1 to 8) {
			avance(taille)
			droite(45)
		}
	}
	octogone(50)
	
	def cote() ={
		avance(50)
		droite(72)
		avance(50)
		gauche(144)
	}
	var i = 0
	for (i<-1 to 5){
		cote
	}
}
