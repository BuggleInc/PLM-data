def solve() {
  var afterBlue = 0
  var whiteRed = getSize() - 1
  var beforeWhite = getSize() - 1
  
  while(!isSorted()){
    for (i <- 0 to getSize()-1){
      if (i <= afterBlue && getColour(i) != Colour.BLUE){
        swap(i,i+1)
      }
    }
  
  
}
}