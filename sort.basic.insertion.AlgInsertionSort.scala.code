def insertionSort() {
  var len=getValueCount()-1;
  var tmp=0;
  for (i <- 1 to len-1) {
    tmp=getValue(i)
    for (j <- 1 to i) {
      if (!isSmaller(i-j,i)) {
        swap(i-j,i-j+1)
      }
    }
  }
}