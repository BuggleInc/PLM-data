def bubbleSort2() {
  

  var n:Int = getNombreValeurs()
  for (i <- 0 to n-2 ){
    
    for (j <- 0 to n-2-i){

      if (plusPetit(j+1, j)){
        var tampon:Int = getValeur(j)
        setValeur(j, getValeur(j+1))
        setValeur(j+1, tampon)

      }
    
    }
  }
}