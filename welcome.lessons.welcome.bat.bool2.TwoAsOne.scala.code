def twoAsOne(a:Int, b:Int, c:Int):Boolean = 
{
/*Soit trois entiers, a b c, renvoyez vrai si il est possible d'en additionner deux d'entre eux pour obtenir
le troisième.*/

	(a,b,c) match
	{
	case(a,b,c) if(a==b+c || b==a+c || c==a+b) => return true
	case _							   => return false
	}
}