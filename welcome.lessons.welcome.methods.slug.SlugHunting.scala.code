def hunt() {
  	while(!estSurBiscuit()){
      while (!isFacingTrail()){
      	gauche();
    	}
      avance();
  	baisseBrosse();
    leveBrosse();
    }
  	prendBiscuit();
}

def isFacingTrail():Boolean = {
  if (estFaceMur()){
    return false;
  }
  avance();
  if (getCouleurSol() == Color.green) {
    recule();
    return true;
  }
  recule();
  return false
}

