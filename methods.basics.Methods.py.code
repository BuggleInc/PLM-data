# Add your code here

def goAndGet() :
	"""avance jusqu'? trouver un biscuit, ramasse le biscuit, redescend ? la position initiale, et d?pose le biscuit"""
	pas = 0
	while not (estSurBiscuit()):
		avance()
		pas += 1
	# l? on devrait ?tre sur le biscuit.
	prendBiscuit()
	for i in range(0,pas):
		recule()


for i in range(7):
  goAndGet()
  right()
  forward()
  left()