def solve() {
  var lgr = getTaillePile()
  var i = 1
  var maxtemp = getRayonCrepe(0)
  var indicemax = 0
  var rayon = 0
  while (!estTriee()) {
    maxtemp = getRayonCrepe(i-1)
    indicemax=i-1
    for (j <- 0 to lgr-i by -1) {
      rayon = getRayonCrepe(j)
    	if (rayon>maxtemp) {
      	maxtemp = rayon
        indicemax=j
      }
    }
    retourne(indicemax+1)
    retourne(lgr)
    i+=1
  }
}