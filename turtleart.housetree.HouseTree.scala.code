override def run() {
  
  var i:Int=0
  var j:Int=0
  var k:Int=0
  
  for(k<- 0 to 3){
    for(i<- 0 to 3){
      avance(100)
      droite(90)
    }
    avance(100)


    droite(30)
    for(j <- 0 to 2){
      avance(100)
      droite(120)
    }
    gauche(30)
    recule(100)
		droite(90)
		avance(50)
    gauche(90)
    
  }
}