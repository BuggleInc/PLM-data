override def run() {
  baisseBrosse;
  while (!finTest){
    if (!estFaceMur){
      avance;
    }else{droite;
         if (estFaceMur){
           gauche;gauche;avance;gauche;
         }else{avance;droite;}
         }
  }
}

def finTest () :Boolean ={
  var a:Boolean=false
  if (getDirection==Direction.EST&&estFaceMur){
    return true
  }else{return false}
	return false
}
