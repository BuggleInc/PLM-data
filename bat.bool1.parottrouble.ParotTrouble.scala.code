def parotTrouble(talking:Boolean, hour:Int):Boolean = {
  var probleme:Boolean=false
  if (talking) {
    if (hour<7) {
      probleme=true
    } else if (hour >20) {
      probleme=true
    }
  }
  return probleme
  }