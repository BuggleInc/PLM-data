override def run() {
  doTheThing()
}

def doTheThing() {
  var compt:Int = 0
  ecritMessage(""+compt)
  while (getX() != getMondeLargeur() - 1 && getY() != getMondeHauteur() - 1) {
    compt += 1
    setPos(compt / getMondeHauteur(), compt % getMondeHauteur())
    ecritMessage(""+compt)
  }
}