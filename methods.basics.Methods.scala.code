var x=0
def goAndGet() {
  while(!isOverBaggle()){
  forward();
    x=x+1
  }
  if(isOverBaggle()){
    pickupBaggle();
    left();
    left();
    left();
    while(x!=0){
    forward();
    }
    if(x==0 && isCarryingBaggle()){
    dropBaggle()
  
    }
  }   
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}