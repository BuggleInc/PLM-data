var compteBaggle = 0
var i

while (!sortieTrouvee() ) {

	for (i <- 1 to 5) {

		if(estSurBiscuit())
		{
			compteBaggle = compteBaggle + 1
		}
	}

	if(compteBaggle >= 3)
		gauche();
	else
		droite();

	
	compteBaggle = 0
}