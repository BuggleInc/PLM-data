void crab(int levels, double length) {

	if(levels == 0){
		avance(length);
	}else{
		gauche(45);
		crab(levels-1, length/Math.sqrt(2));
		droite(45*2);
		crab(levels-1, length/Math.sqrt(2));	
		gauche(45);
	}
}
