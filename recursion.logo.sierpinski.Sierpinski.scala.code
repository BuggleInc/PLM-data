def sierpinski(level:Int, length:Double) {
  if (level==0){
    avance(length)
  droite(60)
  avance(length)
  droite(60)
  avance(length)
  droite(120)
  }
  avance(length)
  droite(60)
  avance(length)
  droite(60)
  avance(length)
  droite(60)
  sierpinski(level,length/2)
  
}