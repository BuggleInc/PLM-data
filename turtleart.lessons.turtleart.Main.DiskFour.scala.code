override def run() {
	for (l <- 1 to 4) {
		for (i <- 1 to 90) {
			avance(100);
			recule(100);
			droite(1); 
		}
		if (l==1) {
			setCouleur(Color.red);
		}
		else if (l==2) {
			setCouleur(Color.yellow);
		}
		else if (l==3) {
			setCouleur(Color.magenta);
		}
	}
}
