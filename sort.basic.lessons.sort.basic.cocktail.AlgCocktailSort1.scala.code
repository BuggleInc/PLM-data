def cocktailSort() {
	var swap=false
	do {
	for (i <- 0 to getNombreValeurs()-2) {
		if ( plusPetit(i,i+1) ) {
			swap=true
			echange (i+1,i); 
			
		
	}
}
for (j <- getNombreValeurs()-2 to 0) {
		if ( plusPetit(j,j+1) ) {
			swap=true
			echange (j,j+1);
	
		}
		}
		}
		while( swap==true)
}
