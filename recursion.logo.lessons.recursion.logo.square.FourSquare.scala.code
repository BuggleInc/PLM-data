override def run() {
  carre()
  recule(100)
  carre()
  gauche(90)
  avance(100)
  droite(90)
  carre()
  avance(100)
  carre()
  droite(90)
  avance(100)
  gauche(90)
}

def carre (){
  for (i <- 1 to 4){
    avance(100)
    droite(90)
  }
}
