do {var compteur:Int = 0;
	for (pas <- 1 to 5) {
	if (estSurBiscuit()) {
		(compteur = compteur + 1);
		avance();
	} else { 
		avance();
	} 
	}
	if (compteur >= 3) {
			gauche();
		} else {
			droite();
		}
		compteur = 0;

}


while (!sortieTrouvee())
