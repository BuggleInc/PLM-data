def insertionSort() {
  var L = getNombreValeurs()
  var v = 0
  var cpt = -1
  for (i <- 1 to L-1){
    v = getValeur(i)
    while (plusPetit(i-1,i)) {
      setValeur(i,getValeur(i-1))
      cpt = cpt +1
      if (i!=0){
        i = i - 1
      }
    }
    setValeur(i-cpt,v)
  }
}