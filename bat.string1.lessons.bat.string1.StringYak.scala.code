def stringYak(str:String):String = 
{
  var s=""
  for(i<- 0 to str.length-3 by 3)
  {
    println(str.drop(i).take(3));
    
    if(i<str.length()-4 && str.drop(i).take(3)!="yak")
    {
      s+=str.drop(i).take(3)
    }
  }
  
  return s
}