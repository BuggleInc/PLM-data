def gnomeSort() {
	var ok : Boolean = false;
  var i : Int = 0
  while(ok == false){
    if(i == getNombreValeurs()){ok=true;}
    else if(plusPetit(i+1,i)){
    	echange(i+1,i);
      if(i==0){i=1;}
        else{i+= -1;}
    }
    else{i+=1}
    
  }
}