override def run() {
  baisseBrosse
  while(!arrivee) {
    avance()
  }
}


def arrivee():Boolean = {
  if(estFaceMur){
    if (getDirection==Direction.OUEST){
      droite()
      if (estFaceMur){
        gauche()
        return true
      }
      avance()
      droite()
      return false
    }
    if (getDirection==Direction.EST){
      gauche()
      if (estFaceMur){
        droite()
        return true
      }
      avance()
      gauche()
      return false
    }
  }
  return false
}