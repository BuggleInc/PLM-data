def indexOfMaxValue(nums):
    tab = nums
    max = tab[0]
    maxPosition = 0
    for i in range(len(tab)):
        if tab[i]>max:
            max = tab[i]
            maxPosition = i
    return maxPosition