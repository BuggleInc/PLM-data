import math
def close10(a, b):
    dista=math.fabs(a-10)
    distb=math.fabs(b-10)
    if a==b:
        if math.fabs(a)<math.fabs(b):
            return a
        else:
            return b
    else:
        if dista<distb:
            return a
        else:
            return b
