override def run() {
	retourne();
	var hauteur=getMondeHauteur();
	var largeur=getMondeLargeur();
	var num =0 ;
	var x=0 ;
	var y=0 ;
	while ( !(getX()==largeur) && !(getY()==hauteur))
{
	ecritMessage(num)
	for ( i<- 0 to  48 )
	{
	
	setPos(x,y+1)
	y=y+1
	num=num+1
	ecritMessage(num)
	}
	if (estFaceMur())
	{
	setPos(x+1,0)
	y==0
	}
	
}
}