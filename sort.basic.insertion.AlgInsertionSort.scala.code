def insertionSort() {
  var n = getNombreValeurs()
  var v = getValeur(0)
  for (i <- 1 to n-1) {
    v = getValeur (i)
    var j = 1
    do {
      if (plusPetit(i,i-j)) {
        copie(i-j,i-j+1)
        j += 1
      }
    }
    while (plusPetit(i,i-j) || j<i)
    setValeur(i-j,v)
  }
}