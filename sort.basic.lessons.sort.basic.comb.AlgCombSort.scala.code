def combSort() {

	 var ecart = getNombreValeurs()
 while (ecart>1 || ) 
   if  (ecart>1)
   {
     ecart = ecart / 1.3
   i = O
   }
   do {
     if (plusPetit(i+1:Int, i:Int))
     {
     	echange(i,i+1)
     }
     i=i+1
   }
  while( i+ecart < lgr)
	
}
