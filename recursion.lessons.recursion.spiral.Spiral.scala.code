def spiral(steps:Int, angle:Int, length:Int, increment:Int)    {
	if ( steps == 0){
		return
	}else{
		avance(length + 10 )
		droite(angle)
		spiral(steps = steps - 1 ,angle, length + 10 ,increment + 1 );
	}	
}


