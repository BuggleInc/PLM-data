def insertionSort() {
  var stock=getValeur(0)
  for (i<-0 to getNombreValeurs()-1){
    stock=getValeur(i)
    var j = i
    while ((j>0) &(plusPetitQue(j,stock))){
      stock=getValeur(j)
      copie(i,j)
      j=j-1
    }
  }
}