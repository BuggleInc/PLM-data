override def run() {
	var T = new Array[Color](getMondeHauteur());
	T(0) = getCouleurSol();
	for(w<- 1 to T.length-1) {
		T(w) = getCouleurSol();
		avance();
	}
	for(x<-1 to T.length-1) {
		recule();
	}
	def Beta() {
		gauche();
		avance();
		droite();
		val decalage = litMessage().toInt
		for ( w<- 1 to T.Length - 1 ) {
			var x:Int = ( w + decalage ) % T.Length
			var c:Color = T( x );
			
			baisseBrosse();
			leveBrosse();
		}
	
}
