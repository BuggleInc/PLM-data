var caseVerte = false

def isFacingTrail():Boolean = {
  if (estFaceMur==true) {
    gauche();
  
  } else {
		avance();
  	if (getCouleurSol()==Color.green) {
    	recule();
    	caseVerte = true;
  	
    }else { recule();
  		gauche();
 	 }
  }
  return caseVerte
}