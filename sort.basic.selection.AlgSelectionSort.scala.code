def selectionSort() {
  var m?moire=0
  var compteur=1
  var len=getValueCount()
  while (compteur!=(len-1)){
    m?moire=compteur
    for (i<-compteur to (len)){
      if (isSmaller(i,m?moire)){
        m?moire=i
      }
    }
    swap(compteur,m?moire)
    compteur=compteur+1
  }
}