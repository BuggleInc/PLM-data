def plusOne(l:List[Int]):List[Int] = {
  
	def compute_plusOne(l:List[Int], acc:List[Int]):List[Int] = {
    if(l == Nil){
      return acc;
    } else {
      compute_plusOne(l.tail, acc ::: List(l.head + 1));
    }
    
    
  }
  return compute_plusOne(l, List())
}