def isFacingTrail():Boolean =
{
  var isVert = false;
  var nbPas = 0;
  
  while (!isVert && !estFaceMur())
  {
    isVert = (getCouleurSol() == Color.green || isVert);
    avance();
    nbPas += 1;
  }
  for (i <- 1 to nbPas) recule();
  
  return isVert;
}