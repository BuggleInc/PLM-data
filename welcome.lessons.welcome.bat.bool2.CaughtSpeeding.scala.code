def caughtSpeeding(speed:Int, isBirthday:Boolean):Int = {
  (speed,isBirthday) match {
    case (speed,false) if speed <61 =>0
    case (speed,false) if speed <81 =>1
    case (speed,false) =>2
    case (speed,true) if speed<66 =>0
    case (speed,true) if speed<86 =>1
    case (_,_)=>2
  }
}