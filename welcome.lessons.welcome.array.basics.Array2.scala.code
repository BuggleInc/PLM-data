override def run() {
	var T=new Array[Color](getMondeHauteur())
	for (i<-0 to T.length-2) {
	T(i)=getCouleurSol()
	avance();
	}
	T(T.length-1)=getCouleurSol();
	recule(getMondeHauteur()-1); // enregistrement de la 1er colonne

val decalage = litMessage().toInt //lecteure décalage
	
	for (i<-1 to getMondeLargeur()-1) {
		gauche();
		avance();
		droite();
		for(i<-0 to T.length-1) {
			setCouleurBrosse(T(i-decalage))
			baisseBrosse();
			leveBrosse();
			avance();
		}
	}
}