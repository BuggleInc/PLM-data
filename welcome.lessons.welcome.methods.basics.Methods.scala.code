def goAndGet() {
  var pas=0;
  while (!estSurBiscuit()) {
    avance();
    pas=pas+1;
  }
  prendBiscuit();
  for (marche <- 1 to pas) {
    recule()
  };  
}

for (i <- 1 to 7) {
    goAndGet();
    right();
    forward();
    left();
}