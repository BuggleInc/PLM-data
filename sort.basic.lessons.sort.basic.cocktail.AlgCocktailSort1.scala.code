def cocktailSort() {
	var swapped:Boolean=true;
	while(swapped==true){
		for (i<- 0 to getValueCount()-2 by 1)
			if( isSmaller(i+1,i)){
				swap(i,i+1);
				swapped=false;
			}
		for(i<- getValueCount()-2 to 0 by -1)	
			if( isSmaller(i+1,i)){
				swap(i,i+1);
				swapped=false;
			}	
	}
}

