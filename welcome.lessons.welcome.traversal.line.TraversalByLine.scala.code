def testd():Boolean= {
	var x:Boolean=false;

	droite();
	if (estFaceMur) x= true;
	gauche();
	return x
}

def testdd():Boolean= {
	var x:Boolean=false;
	
	droite();
	droite();
	if (estFaceMur) x= true;
	droite();
	droite();
	return x
}

override def run() {
	var x,y,z:Int=0;

	for(i<-0 to getMondeHauteur-1) {
		for (j<-0 to getMondeLargeur-1) {
			setPos(x,y);
			if (testdd==false) x=x+1;
			writeMessage(z);
			z=z+1;
		}
		if (testd==true && estFaceMur()) {
			y=y+1;
		}
		x=0
		setPos(x,y);
	}
	setPos(getMondeLargeur-1, getMondeHauteur-1);
}