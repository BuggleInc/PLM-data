while not sortieTrouvee() :
    seen = 0
    premiereFois = False
    while not premiereFois or not croisement():
        premiereFois = True
        forward()
        if estSurBiscuit():
            seen += 1
    if seen > 2:
        left()
    else:
        right()
forward()