def indexOfValue(nums:Array[Int] ,lookingFor:Int): Int = {
  var i = 0
  var compteur = 0
  while (compteur < nums.length)
  {
    if (nums(compteur)==lookingFor)
    {
      return compteur
    }
    else compteur += 1
  }
  return -1
}
