def haveBaggle():Boolean = {
  var vuBiscuit:Boolean = false
  for (i <- 1 to 7) {
    for (j <- 1 to 6) {
      avance()
      if (estSurBiscuit()) {
        vuBiscuit = true
        recule(j)
        return vuBiscuit
      }
    }
    for (i <- 1 to 6) {
      recule()
    }
    droite()
    avance()
    gauche()
  }
  return vuBiscuit
}