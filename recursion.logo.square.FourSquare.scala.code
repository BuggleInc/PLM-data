override def run() {
  
  val xpos = getX(),
  		ypos = getY();
  
  
  carre(xpos, ypos, 100);
  
  
}


def carre(fromx:Int, fromy:Int, taille:Int) = {
  setX(fromx); setY(fromy);
  setCap(90);
  
  avance(100);
  droite(90);
  avance(100);
  droite(90);
  avance(100);
  droite(90);
  avance(100);
  
}