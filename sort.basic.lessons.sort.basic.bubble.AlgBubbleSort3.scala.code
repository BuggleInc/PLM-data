def bubbleSort3() {	
	var swapped:Boolean = true
	val n:Int = getNombreValeurs()
	for (l<-n-2 to 0 by -1) {
		swapped = false
		for (k<-0 to l) {
			
			if (plusPetit(k+1,k)){echange(k+1,k);swapped=true}
		}
		if (!swapped) {return}
	}
}
