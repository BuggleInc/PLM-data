override def run() {var x:Int=0;
                    while (!isFacingWall()) {right()}
                    while (!isOverBaggle()) {while(isFacingWall()) {right(); if (!isFacingWall()) {forward()}; left()}}
pickupBaggle()
    // Your code here 
}