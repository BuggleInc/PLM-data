def bubbleSort()  {
  for (i <- 0 to getValueCount()-1){
    var swapped = false
    var j = 0
    while (!swapped){
      if (isSmaller(i,j)){
        swap(i,j)
        swapped = true
      }
    }
  }
}
