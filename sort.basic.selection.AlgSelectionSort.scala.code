def selectionSort() {
  for(i<- 1 to getNombreValeurs()-2){
    var petit = i-1
    for(o<- i-1 to getNombreValeurs()-1){
      if(getValeur(o)<getValeur(petit)){
        petit=o
      }
    }
    echange(petit,i-1)
  }
}