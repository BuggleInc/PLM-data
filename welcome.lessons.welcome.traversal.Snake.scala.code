override def run() {
  baisseBrosse()
  for (cpt <- 0 to 5){
    pasDeSerpent()
  }
  avance(6)
  
}

def pasDeSerpent() {
  while (!estFaceMur()){
    avance()
  }
  if (getDirection==Direction.OUEST) {
    droite()
    avance()
    droite()
  } else if (getDirection==Direction.EST) {
    gauche()
    avance()
    gauche()
  }
  
}