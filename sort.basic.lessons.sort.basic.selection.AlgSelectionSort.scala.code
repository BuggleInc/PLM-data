def selectionSort() {
	val l =getNombreValeurs()-1
	for (i<-0 to l ) {
		var indice = i
		for (h<- i to l ) {
			if (plusPetitQue(h,getValeur(indice)) {
				indice = h
			}
		}
		echange(i,indice)
	}

}
