override def run() {
    def keepHandOnRightWall() = {
    	droite();
    	if (estFaceMur) {gauche(); if (!(estFaceMur)) {avance()} else gauche()}
    	else if (!(estFaceMur())) {avance()}
    		else {droite()}
    }
    if (getDirection()==Direction.NORD) {
    		if (!(estFaceMur)) {keepHandOnRightWall()} else {avance()}
    			
    		}
}
