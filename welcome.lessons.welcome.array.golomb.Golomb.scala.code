def golomb(num:Int): Int = {
  var suite = new Array[Int](num)
  suite(0) = 1
  suite(1) = 2
  var rang:Int = 0
  for (i <- 1 to num-1) {
    for (j <- 0 to suite(i)-1) {
      suite(rang+j) = i
    }
  }
  return suite(num-1)
}
