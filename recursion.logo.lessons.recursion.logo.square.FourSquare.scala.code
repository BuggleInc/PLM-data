override def run() {
 	var nbPas : Double = 100.00;
  var angle : Double = 90.00;
  var i : Int = 0;
  
  
  for(i<-0 to 3){
  avance(nbPas);
  droite(angle);
  avance(nbPas);
  droite(angle);
  avance(nbPas);
	droite(angle);
  avance(nbPas);
  droite(2*angle);
  }
  
  
}
