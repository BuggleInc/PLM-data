public int indexOfMaximum(int[] tab) {
  var t : Int = tab.length
  var a : Int = tab(0)
  var p : Int = 0
  for ( i <- 1 to t){
      if (tab(i)>a) {
        a = tab(i)
        p=i
      }
    }
  return p
 }
