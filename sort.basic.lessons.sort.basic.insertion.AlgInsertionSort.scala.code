def insertionSort() {
  
  for (i<-1 to getNombreValeurs-1){
    var v=getValeur(i)
    var j=i
    while (getValeur(j-1)>v && j>=0){
      setValeur(j,getValeur(j-1))
      j=j-1
    }
    setValeur(j,v)
  }
}