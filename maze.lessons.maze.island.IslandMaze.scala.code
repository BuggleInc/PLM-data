def keepHandOnSideWall = {gauche(); if (!estFaceMur) {avance()} else {while(estFaceMur()) {droite};avance()} }

override def run() { 
  
  
  while (!estSurBiscuit()) { while (!estFaceMur){setDirection(Direction.NORTH);avance()};keepHandOnSideWall}; prendBiscuit()
}