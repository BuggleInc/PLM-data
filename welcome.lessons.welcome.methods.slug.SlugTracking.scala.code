def isFacingTrail():Boolean = {
  
  var caseVerte = false
  
  if (estFaceMur == false) {
    avance;
		if (getCouleurSol == Color.green) {
  		recule();
  		avance();
    	caseVerte = true;
		} else {
      recule()
    };
  }
  return caseVerte
}