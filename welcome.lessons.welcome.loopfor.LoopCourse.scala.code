val positionDepartX:Int = getX()
val positionDepartY:Int = getY()
var nbTour:Int = 0

while (nbTour < 10)
{
  avance()
  
  if (estFaceMur())
  	gauche()
  
  if (getX() == positionDepartX && getY() == positionDepartY)
  	nbTour = nbTour + 1
}