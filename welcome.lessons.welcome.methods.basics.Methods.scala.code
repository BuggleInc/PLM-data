def goAndGet() {

        var nbPas:Int = 0
        while (!estSurBiscuit()) {
        	avance();
        	(nbPas = nbPas + 1) ;
        }
        prendBiscuit();
        for ( pas <- 1 to nbPas) {
        	recule();
        }
        poseBiscuit();
}
