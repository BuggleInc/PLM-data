
while not sortieTrouvee() :
    baggel = 0
    premiereFois = True
    while premiereFois or not croisement() :
            premiereFois == False
            forward()
              if estSurBiscuit() :
                baggel=baggel+1
    if baggel>=3 :
            gauche()
    else:
            droite()
