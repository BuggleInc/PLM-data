def bubbleSort2()
{
  var stop = false
 
  for (l <- 1 to getNombreValeurs())
  {
    if (!stop)
    {
      stop = true
      for (i <- 0 to getNombreValeurs() - 2)
      {
        if (plusPetit(i + 1, i))
        {
          echange(i, i + 1)
          stop = false
        }
      }
    }
  }
}