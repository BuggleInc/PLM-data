def bubbleSort3() {
  for (i<- getValueCount()-1 to 0 by -1) {
    var swapped= false ;
    for (j<- 0 to i-1) {
      if (isSmaller(j+1,j)) {
        swap(j,j+1);
        swapped = true;
      }
    }
    if (!swapped) {
      return;
    }
  }
}