def countTeen(a:Int, b:Int,c:Int,d:Int): Int = {
  var nb:Int = 0
  if (estAdo(a)){
    nb += 1
  }
    if (estAdo(b)){
    nb += 1
  }
    if (estAdo(c)){
    nb += 1
  }
    if (estAdo(d)){
    nb += 1
  }
  return nb
}



def estAdo(a:Int): Boolean ={
  return a <= 19 && a >= 13
}