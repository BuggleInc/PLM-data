override def run() {

disc(Color.black);
disc(Color.red);
disc(Color.orange);
disc(Color.magenta);
}

def disc(c:Color)
{
	setCouleur(c);
	for (i <- 1 to 90)
	{
		avance(100);
		recule(100);
		droite(1);
	}
}
