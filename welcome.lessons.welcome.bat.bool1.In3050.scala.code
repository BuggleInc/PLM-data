def in3050(a:Int, b:Int):Boolean = {
	(a,b) match {
		case (a,b) if ((a>=30 && a<=40) && (b>=30 && b<=40))	=> return true
		case (a,b) if ((a>=40 && a<=50) && (b>=40 && b<=50))	=> return true
		case (_,_)									=> return false
		
	}
}