def insertionSort() {
  for (i <- 1 to getValueCount()) {
    var j = 1
    var v = getValue(i)
    while (isSmaller(v,getValue(i-j))) {
      copy (i-j,i)
      j=j+1
    }
    setValue(i-j+1,v)
  }
}