override def run() {
  while(!isOverBaggle()){

    	while(!isFacingWall() && wallToLeft()){
        forward()
      }
    left()
  }
  pickupBaggle()
}

def wallToLeft() : Boolean = {
  left()
  var mur : Boolean = isFacingWall()
  right()
  return mur
}