override def run() {
	def tabColor(x:Int):Array[Color] = {
		var T = new Array[Color](x)
		for (i <- 0 to x-2) {
			T(i)=getCouleurSol();
			avance();
		}
		T(x-1) = getCouleurSol()
		for(i <- 0 to x-2) {recule()}
	return T
	}
tabColor(6)
}