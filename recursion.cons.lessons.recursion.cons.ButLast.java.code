RecList last(RecList seq) {
	RecList butLastList = new RecList();
	butLast(butLastList, seq);
}

RecList butLast(RecList butLastList, RecList seq){
	if (seq.tail == null) return butLastList;	
	butLastList = cons(seq.head, butLastList);
	return butLast(butLastList, seq.tail);
}
