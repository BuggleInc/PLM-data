def bubbleSort()  {
  var echange : Boolean = true
  while (echange) {
    echange = false
    for (i <- 0 to getNombreValeurs()-1) {
      if (!plusPetit(i,i+1)) {
        echange(i,i+1)
        echange = true
      }
    }
  }
}