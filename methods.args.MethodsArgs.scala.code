def move(nbPas: Int,verslAvant: Boolean) {
    if (verslAvant) {
      for(compteur <- 1 to nbPas) {
         avance()
      }
    }
  	else {
      for(compteur <- 1 to nbPas) {
         recule()
    }
}
}