def insertionSort() {
	var v = 0
	var ech = 0
	
	for (i <- getNombreValeurs()-1 to 1 by -1) {
		v = getValeur(i)
		for (k <- i-1 to 1 by -1) {
			if (v < getValeur(k)) {
				copie(k,k+1)
				ech = k
			}
		}
		setValeur(ech,v)
	}
}
