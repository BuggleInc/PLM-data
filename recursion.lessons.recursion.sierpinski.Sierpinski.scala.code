def sierpinski(level:Int, length:Double) {
	if (level==0) {
		for (i <- 1 to 3) {
			avance(length)
			droite(120)
		}
	} else {
		for (i <- 1 to 3) {
			sierpinski(level-1,length/3)
			droite(120)
		}
	}
	avance(2*length)
}
