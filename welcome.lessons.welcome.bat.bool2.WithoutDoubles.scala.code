def withoutDoubles(die1:Int, die2:Int, noDoubles:Boolean):Int = {
	if (die1==die2 && noDoubles=true) {
		if (die1<6) {
			die1=die1 +1
		}
		else {die1==1}
	}
	else {
		return die1+die2
	}
}

%Renvoyez la somme du jet de deux dés à six faces. Cependant, 
si noDoubles est vrai et que les deux dés ont la même valeur, 
incrémentez l'un des dés à la valeur suivante, en le mettant à 1 si sa valeur était 6.%