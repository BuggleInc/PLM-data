def insertionSort() {
  var j=0
  for (i<-1 to getNombreValeurs-1){
    var v=getValeur(i)
    for (k<-(i-1) to 0 by -1){
      if (!plusPetitQue(k,v) && getValeur(k)!=v){
        var a = getValeur(k)
        setValeur(k+1,a)
        j=k
      }
    }
    setValeur(j,v)
  } 
}