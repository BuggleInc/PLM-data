def insertionSort() {
  for(i <- 1 to getNombreValeurs()-1){
    var c = i
    var n = getValeur(i:Int)
    while(c>=1 && getValeur(c-1)>n){
      copie(c:Int,c+1:Int)
      c = c-1
    }
    setValeur(c:Int, n:Int)
  }
}