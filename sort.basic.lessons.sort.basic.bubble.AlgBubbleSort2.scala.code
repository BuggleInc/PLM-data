def bubbleSort2()  {
  var n = getNombreValeurs();
  var parcours = true;
  while (parcours) {
    parcours=false
    n=n-1
  	for (i<-0 to n-2) {
    	if (getValeur(i)>getValeur(i+1)) {
      	echange(i,i+1)
        parcours=true
      }
      
    }
  
  }
}