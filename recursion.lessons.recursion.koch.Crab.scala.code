def crab(levels:Int, length:Double) {
	if ( levels == 0){
		avance(length: Double)
	}
	else{
		gauche( :Double)
		crab(levels-1 , length*Math.sqrt(2)/2)
		droite(90)
		crab(levels-1 , length*Math.sqrt(2)/2)
		}
}
