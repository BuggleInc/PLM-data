def trace() :
	baisseBrosse()
	leveBrosse()

def faireMotif(c) :
	setCouleurBrosse(c)
	avance()
  trace()
  recule()
  gauche()
  avance()
  trace()
  recule()
  droite()
  avance(3)
  gauche()
    
couleur = [Color.red, Color.blue, Color.green, Color.yellow]
for k in range(0,2):
    for j in range(0,2):
        for i in range(0,4):
            faireMotif(couleur[i])
        avance(4)
    gauche()
    avance(4)
    droite()