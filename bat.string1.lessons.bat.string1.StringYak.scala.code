def stringYak(str:String):String = 
{
  var s=""
  var h=0;
  for(i<- 0 to str.length-1)
  {
    if(str.drop(i).take(3)!="yak")
    {
      s+=str.drop(i).take(3)
      i=i+2
    }
  }
  
  return s
}