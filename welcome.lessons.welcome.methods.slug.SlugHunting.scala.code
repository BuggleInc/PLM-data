def hunt() {
while (!estSurBiscuit()) {
while (isFacingTrail() == true && !estSurBiscuit()) {
	avance(); baisseBrosse();
}

if (isFacingTrail() == false) {
	droite(); leveBrosse();
}
if (isFacingTrail() == false) {
	gauche(); gauche(); leveBrosse();
}
}    
}  

def isFacingTrail():Boolean = {
	if (estFaceMur) {
		return false
	}
    
avance(); getCouleurSol(); 
    if (getCouleurSol() == Color.green) { recule();
    	return true
    } else{ recule();
    	return false
    }
}