override def run() {
    for(x<-0 to 7){
    avance(50)
    droite(360/8)}
  avance(25)
  droite(45)
  for(x<-0 to 7){avance(25)
                gauche(45)
                 avance(25)
                droite(90)}
  
}