override def run() {
	val H = getMondeLargeur();
	var T = new Array[Color](H+1)
	for (i <- 1 to H) {
		T(i) = getCouleurSol() ;
		avance();
	}
	for (i <- 1 to H-1) {
	   gauche(); avance(); droite();
		for (tmp <- 1 to H) {
			avance();
			setCouleurBrosse(T(tmp));
			baisseBrosse();
			leveBrosse();
			gauche(); avance(); droite();
		}
	}
	
}
