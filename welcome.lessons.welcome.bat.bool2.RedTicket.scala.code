def redTicket(a:Int, b:Int, c:Int):Int = {
	if (a==b && b==c && a==2)
		return 10;
	else if (a==b && c==b && a==1 || a==b && c==b && a==0)
		return 5;
	else if(b!=a && c!=a)
            return 1;
           else
	       return 0;
}