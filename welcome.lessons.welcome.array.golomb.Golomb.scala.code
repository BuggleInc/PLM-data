def golomb(num:Int): Int = {
  var avance=1
  var test =1
  var nombre=0
  var T= new Array[Int](num)
  T(0)=test
  while (avance<num){
    test =test+1
    nombre =test
    while (test !=0 && avance < num){
      test=test-1
      avance=avance+1
      T(avance)=nombre
    }
  }
  return T(num-1)
}