def selectionSort() {
  val n = getNombreValeurs
  def min(a:Int,b:Int):Int = {
    var j = a
    for (i <- a+1 to b) {
      if (plusPetit(i,j)) {j=i}
    }
   return j
  }
  for (i <- 0 to n-2) {
    echange(i,min(i,n-1))
  }
}
