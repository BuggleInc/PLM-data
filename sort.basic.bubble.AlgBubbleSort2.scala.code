def bubbleSort2()  {
  var swapped = false;
  var count = 0;
  
  do {
    count += 1;
    swapped = false;
    for (i <- 0 to getValueCount()-2) {
      if (isSmaller(i+1, i)) {
        swap(i, i+1);
        swapped = true;
      }
    }
  } while (swapped && count == getValueCount());
}