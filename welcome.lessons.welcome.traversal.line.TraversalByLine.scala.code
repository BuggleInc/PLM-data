override def run() {
  var cpt:Int = 0
  ecritMessage(cpt)
  cpt+=1
	for (i <- 0 to 47) {
    if (getX()==6) {
      setPos(0, getY()+1)
    }
    else {
      setPos(getX()+1, getY())
    }
    ecritMessage(cpt)
    cpt=cpt+1
  }
}