def noTriples(nums:Array[Int]): Boolean = {
  var occurences = 0
  var test = false
  var valeur=nums(0)
  for (i <- 1 to nums.length -1) {
    if (nums(i)==valeur) {
      occurences+=1
    } else {
      occurences = 0
    }
   valeur=nums(i)
   if (occurences == 3) test=true
  }
  return test
}