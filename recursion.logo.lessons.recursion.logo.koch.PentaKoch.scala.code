def pentaKoch(levels:Int, length:Double) {if(levels == 0){
  avance(length)
  return
    
  }
  else{
    squareSide(levels-1 , length/0.4)
    gauche(60)
    squareSide(levels-1 , length/0.4)
    droite(60)
    squareSide(levels-1 , length/0.4)
    droite(60)
    squareSide(levels-1 , length/0.4)
    droite(60)
    squareSide(levels-1 , length/0.4)
    gauche(30)
	}
}