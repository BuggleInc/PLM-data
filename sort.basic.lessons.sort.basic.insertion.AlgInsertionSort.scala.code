def insertionSort() {
  var len = getValueCount();
  
    
    for(i <- 1 to len-1){
      var v = i;
      if(getValue(i-1) > getValue(i)){
        copy(i-1, i);
        setValue(i-1,v);
      }
    }
  
}