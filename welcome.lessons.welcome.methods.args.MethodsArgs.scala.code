def move(nbPas: Int, versLAvant: Boolean) {
  for (k <- 1 to nbPas) {
    mouvement(versLAvant);
  }
}
def mouvement(versLAvant: Boolean) {
  if (versLAvant) {
    avance();
  } else {
    recule();
  }
}
