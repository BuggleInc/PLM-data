def keepHandOnSideWall = {if (estFaceMur()) {gauche(); if (estFaceMur) {retourne();avance()}; avance()} else {avance()} }

override def run() { 
  
  
  while (!estSurBiscuit()) {keepHandOnSideWall}; prendBiscuit()
}