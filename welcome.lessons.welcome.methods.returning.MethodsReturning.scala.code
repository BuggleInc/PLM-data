var vubiscuit = false

def haveBaggle():Boolean = {
  for (i <- 1 to 6) {
    estSurBiscuit()
    if (estSurBiscuit()==true) {
      vubiscuit=true
    }
    avance()
  }
  for (j <- 1 to 6) {
    recule()
  }
  return vubiscuit
}
