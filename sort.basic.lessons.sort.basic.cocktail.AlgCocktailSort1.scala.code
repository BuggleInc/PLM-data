def cocktailSort() {
  var n = getNombreValeurs()
  var swap = false
  do {
    swap = false
    for (i <- 0 to (n-2)) {
    	if (plusPetit(i+1,i)) {
     		echange(i,i+1);
     		swap = true
      }
    };
    for (i <- (n-1) to 1 by -1) {
    	if (plusPetit(i,i-1)) {
     		echange(i,i-1);
     		swap = true
      }
    }
  } while (swap)
}
