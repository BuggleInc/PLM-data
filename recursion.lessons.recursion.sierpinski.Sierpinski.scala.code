def sierpinski(level:Int, length:Double) {
	
		if (level > 0 ) {
			sierpinski(level-1,length/2);
			avance(length);
			right(60);
			sierpinski(level-1,length/2);
			avance(length);
			right(60);
			sierpinski(level-1,length/2);
			avance(length);
			right(60);
		} else {
			avance(length);
		}
}
