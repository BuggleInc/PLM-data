override def run():Unit= {
  for(i<- 0 to 7){
    petale()
  }
  cache()
}

def petale(){
  droite(45)
  avance(20)
  gauche(90)
  avance(20)
  droite(45)
  avance(20)
  droite(90)
  avance(20)
  gauche(90)
  avance(20)
  gauche(90)
  avance(20)
  
  avance(20)
  
  gauche(90)
  avance(20)
  gauche(90)
  avance(20)
  droite(90)
  avance(20)
  droite(45)
  avance(20)
  gauche(90)
  avance(20)
  
  droite(90)
  droite(180)
  droite(45)
}