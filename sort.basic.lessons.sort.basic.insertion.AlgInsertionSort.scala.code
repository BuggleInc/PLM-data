def insertionSort() {
  var stock=getValeur(0)
  for (i<-1 to getNombreValeurs()-1){
    stock=getValeur(i)
    for (j<-i to 1 by -1){
      if (plusPetitQue(j,stock)){
        echange(j,j-1)
      }
    }
  }
}
