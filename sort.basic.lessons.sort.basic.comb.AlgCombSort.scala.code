def combSort() {
  var ecart:Int = getNombreValeurs();
  var incr:Boolean=false;
  var i:Int = 0;
  
  while((ecart > 1) || (incr==true)){
    incr=false;
    if(ecart > 1){
      ecart = (ecart.asInstanceOf[Double]/1.3).asInstanceOf[Int];
    }
    i=0;
    while(i+ecart < getNombreValeurs()){
      if(plusPetit(i+ecart,i)){
        echange(i+ecart,i);
        incr=true;
      }
      i=i+1;
    }
    
  }
}
