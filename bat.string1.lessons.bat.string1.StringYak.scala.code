def stringYak(str:String):String = 
{
  var s=""
  var h=0;
  for(i<- 0 to str.length-4 by 3)
  {
    if(str.drop(i).take(3)!="yak")
    {
      s+=str.drop(i).take(3)
    }
    h=i+3
  }
  s+=str.drop(h)
  
  return s
}