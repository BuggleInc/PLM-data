def bubbleSort()  {
  var echanges = true
  while (echanges){
    echanges = false
    for (k <- 0 to getNombreValeurs() -2 ){
      if (getValeur(k) > getValeur(k+1)){
        echange(k, k+1);
        echanges = true;
      }
    }
  }
}