def bubbleSort()  {
  for (i<-0 to getNombreValeurs()-1){
    var indice=i
    for (j<-i to getNombreValeurs()-1){
      if (plusPetit(j,indice)){
        echange(j,i)
        indice=j
      }
    }
  }
}
