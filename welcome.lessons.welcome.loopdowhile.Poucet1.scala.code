do {
  var nbBiscuits: Int=0
  for (n <- 1 to 5) {
    avance();
    if (estSurBiscuit) {
      nbBiscuits = nbBiscuits + 1
    }
  } 
  if (nbBiscuits >= 3) {
       gauche();
  } else {
       droite();
  } 
} while (!sortieTrouvee)
avance();
