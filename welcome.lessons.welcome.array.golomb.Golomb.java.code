int golomb(int num) {
int result = 0;

	if(num == 1){
		return num;
	}
/*
c = [,1];
i = c.length;

function g(n){
	for(i; i < n; i++) 
		c[i] = g(i);

	return c[n] || (c[n] = 1 + g(n-g (g (n-1) ) ) )
}
*/
	return result = 1 + golomb(num -golomb(golomb(num -1) ) );
}
