def insertionSort() {
var temp =0
var j=0
    for (i<-0 to getNombreValeurs()){
      j=i
      while (j>0 && plusPetit(j,j-1)){
        echange(temp,j)
        echange(j,j-1)
        echange(j-1,temp)
        j=j-1
        
      }
    }
}
