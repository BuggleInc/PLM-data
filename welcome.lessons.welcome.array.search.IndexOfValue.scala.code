def indexOfValue(nums:Array[Int] ,lookingFor:Int): Int = {
  var n =nums.lenght
  for ( j <- o to n){
    if(lookingFor!=nums(j)){
      return -1
    }
    else if ( lookingFor==nums(j)){
      return nums (j)
    }
    else {
      return 0
    }
  }
}