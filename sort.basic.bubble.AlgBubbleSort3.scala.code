def bubbleSort3() {
  var len=getValueCount();
  var swapped:Boolean = true
  for (i <- 0 to len-2) {
    for (j <- 0 to len-2-i) {
      while (swapped) {
        swapped=false
        if (!isSmaller(j,j+1)) {
          swap(j,j+1) ;
          swapped=true
        }
     	}
    }
  }
  return
}