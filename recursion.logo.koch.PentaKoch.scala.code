def pentaKoch(levels:Int, length:Double) {
  if(levels==0){
    avance(length)
  }
  else{
    pentaKoch(levels-1, length*0.4)
    left(260)
    pentaKoch(levels-1, length*0.4)
    right(180)
    pentaKoch(levels-1, length*0.4)
    right(180)
    pentaKoch(levels-1, length*0.4)
    right(180)
    pentaKoch(levels-1, length*0.4)
    left(60)
    pentaKoch(levels-1, length*0.4)
  }
}