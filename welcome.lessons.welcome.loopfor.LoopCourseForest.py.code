tour=0
cote=0

for tour in range(7)
    for cote in range(4)
        avance()
        gauche()
        avance(2)
        droite()
        avance(4)
        droite()
        avance(2)
        gauche()
        avance(4)
        gauche()
    cote=cote+1
tour=tour+1
cote=0