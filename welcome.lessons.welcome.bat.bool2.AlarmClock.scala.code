def alarmClock(day:Int, vacation:Boolean): String = {
  (day, vacation) match {
    case (0|6, vacation) => "off"
    case ( _, vacation) => "10:00"
    case (0|6, -) => "10:00"
    case (-,-) => "7:00"
  }
}