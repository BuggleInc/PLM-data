def rangement () {
  prendBiscuit()
  while (!estSurBiscuit()) {recule()}
  avance();poseBiscuit();avance()
}

def chemin () {
  var t = true
  droite();droite()
  while (t){
  while (!estSurBiscuit() ) {
    avance(); if (estFaceMur() && !estSurBiscuit()) {
      droite();avance();gauche();avance();t=false
    }
  }
    rangement()
  }}

chemin()