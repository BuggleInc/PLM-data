def lastDigit(a:Int, b:Int, c:Int):Boolean =
{
/* Soit trois entiers, a b c, renvoyez vrai si deux ou plus d'entre eux ont le même chiffre le plus à droite. 
 *  Les entiers sont positifs.   
 Note: l'opérateur % "mod" calcule le reste, i.e. 17 % 10 vaut 7. */

 (a,b,c) match
 {
 	case(a,b,_) if((a%10)==(b%10)) => return true
 	case(a,_,c) if((a%10)==(c%10)) => return true
 	case(_,b,c) if((b%10)==(c%10)) => return true
 	case(_,_,_) 				 => return false
 }
}