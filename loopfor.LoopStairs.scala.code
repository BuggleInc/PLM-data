avance() //la première case est déjà colorée

for (c <- 1 to 8) {
  baisseBrosse()

  while (!estFaceMur()) {
    avance()
  }

  leveBrosse()
  gauche()
  avance()
  droite()
  avance()
  
}

baisseBrosse()
avance(3)