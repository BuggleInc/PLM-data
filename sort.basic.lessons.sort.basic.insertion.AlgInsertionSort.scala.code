def insertionSort() {
  var valeur=0
  var last=0
  for (i<-2 to getNombreValeurs()){
    valeur = getValeur(i)
    for (n<- i-1 to 1 by -1){
      if (getValeur(n)>valeur){
        copie(n-1,n)
        last=n
      }
    }
    setValeur(last, valeur)
  }
}