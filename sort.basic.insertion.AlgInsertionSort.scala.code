def insertionSort() {
  var v = 0
  var k = 0
  var l = 0
  for (i<- 1 to getNombreValeurs()-1){
    v = getValeur(i)
    k = getValeur(i-1)
    l = i-1
    while( plusPetit(v,k) && l>1 ){
      
      copy(l-1,l)
      l-=1
      k = getValeur(l)
    }
    setValeur(l,v)
    
    
  }
}