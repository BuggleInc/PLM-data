def min(l:List[Int]): Int = {
	def fonctionInterne(l:List[Int],v:Int): Int ={
		if(l==Nil) return v;
		else if(l.head<v){
			return fonctionInterne(l.tail,l.head);
		}else{
			return fonctionInterne(l.tail,v);
		}
	}
	return fonctionInterne(l,l.head);
}
