def keepHandOnSideWall(){
  for(t<-1 to 4){
    gauche();
      if(getDirection()==Direction.NORTH && !estFaceMur()){
      avance();
      return
    }
  }
  gauche();
  while(estFaceMur()){
    droite();
  }
  avance();
}

override def run() {
  while(!estSurBiscuit()){
    keepHandOnSideWall();
  }
  prendBiscuit();
}