override def run() {
  carre()
  droite(90:Double)
  carre()
  droite(90:Double)
  carre()
  droite(90:Double)
  carre()
  droite(90:Double)
}

def carre(){
  avance(100:Double)
  droite(90:Double)
  avance(100:Double)
  droite(90:Double)
  avance(100:Double)
  droite(90:Double)
  avance(100:Double)
  droite(90:Double)
}