def indexOfValue(nums:Array[Int] ,value:Int): Int = {
   val pasla = -1;
   var pos = 0;
   while ({nums(pos) != value}&&{pos != nums.length}) {
   	pos = pos + 1;
   }
   if ( nums(pos) == value ) {
   	pos;
   } else {
   	pasla;
   }
}
