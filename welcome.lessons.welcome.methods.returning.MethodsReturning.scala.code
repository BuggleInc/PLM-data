def haveBaggle():Boolean = {
var vueBiscuit = false;
var tab = 0;
def testColonne() = {
	for (i<- 0 to 5 ) { if (estSurBiscuit()) {vueBiscuit = true}
	avance()}
	for (h<- 0 to 5 ) {recule()}
}

while (!vueBiscuit) { testColonne();
	droite();avance();gauche();
	
	}
gauche();avance();droite()
return vueBiscuit




}
