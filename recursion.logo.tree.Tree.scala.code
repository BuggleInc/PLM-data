def tree(steps:Int, length:Double, angle:Double, shrink:Double)    {
  avance(lenghth)
  if (steps>0) {
    droite(angle)
    avance (length)
    gauche(angle)
    recule(length)
    gauche(length)
    avance(length)
    tree(steps-1,length*schrink,angle,shrink)
    
  }
}